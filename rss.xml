<?xml version="1.0" encoding="UTF-8"?>
<rss xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:atom="http://www.w3.org/2005/Atom" version="2.0">
    <channel>
        <title><![CDATA[Matthew Daly's Blog]]></title>
        <description><![CDATA[I'm a web developer in Norfolk. This is my blog...]]></description>
        <link>http://matthewdaly.co.uk</link>
        <generator>grunt-blogbuilder https://github.com/matthewbdaly/grunt-blogbuilder</generator>
        <lastBuildDate>Wed, 13 May 2015 00:01:27 GMT</lastBuildDate>
        <atom:link href="http://matthewdaly.co.uk/rss.xml" rel="self" type="application/rss+xml"/>
        <copyright><![CDATA[Matthew Daly 2015]]></copyright>
        <item>
            <title><![CDATA[My static site generator post on Sitepoint]]></title>
            <description><![CDATA[<p>I wrote an article for Sitepoint recently about creating a static site generator as a Grunt plugin, similar to the one for this site. You can find it <a href="http://www.sitepoint.com/building-static-site-generator-grunt-plugin/">here</a>.</p>
]]></description>
            <link>http://matthewdaly.co.uk/blog/2015/05/03/my-static-site-generator-post-on-sitepoint/</link>
            <guid isPermaLink="true">http://matthewdaly.co.uk/blog/2015/05/03/my-static-site-generator-post-on-sitepoint/</guid>
            <pubDate>Sun, 03 May 2015 18:55:30 GMT</pubDate>
        </item>
        <item>
            <title><![CDATA[How I added search to my site with Lunr.js]]></title>
            <description><![CDATA[<p>As I mentioned a while back, I recently switched the search on my site from Google’s site-specific search to <a href="http://lunrjs.com/">Lunr.js</a>. Since my site is built with a static site generator, I can’t implement search using database queries, and I was keen to have an integrated search method that would be fast and not require server-side scripting, and Lunr.js seemed to fit the bill.</p>
<p>The first task in implementing it was to generate the index. As I wrote the Grunt task that generates the blog, I amended that task to generate an index at the same time as I generated the posts. I installed Lunr.js with the following command:</p>
<pre><code class="hljs lang-bash singleline">npm install lunr --save</code></pre>
<p>I then imported it in the task, and set up the field names:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	    <span class="hljs-keyword">var</span> lunr = <span class="hljs-built_in">require</span>(<span class="hljs-string">'lunr'</span>);
<span class="linenos">2</span>	
<span class="linenos">3</span>	    searchIndex = lunr(<span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">()</span> </span>{
<span class="linenos">4</span>	        <span class="hljs-keyword">this</span>.field(<span class="hljs-string">'title'</span>, { boost: <span class="hljs-number">10</span> });
<span class="linenos">5</span>	        <span class="hljs-keyword">this</span>.field(<span class="hljs-string">'body'</span>);
<span class="linenos">6</span>	        <span class="hljs-keyword">this</span>.ref(<span class="hljs-string">'href'</span>);
<span class="linenos">7</span>	    });</code></pre>
<p>This defined fields for the title, body, and hyperlink, and set the hyperlink as the reference. The variable <code>searchIndex</code> represents the Lunr index.</p>
<p>Next, I looped through the posts, and passed the appropriate details to be added to the index:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	    <span class="hljs-keyword">for</span> (post <span class="hljs-keyword">in</span> post_items) {
<span class="linenos">2</span>	        <span class="hljs-keyword">var</span> doc = {
<span class="linenos">3</span>	            <span class="hljs-string">'title'</span>: post_items[post].meta.title,
<span class="linenos">4</span>	            <span class="hljs-string">'body'</span>: post_items[post].post.rawcontent,
<span class="linenos">5</span>	            <span class="hljs-string">'href'</span>: post_items[post].path
<span class="linenos">6</span>	        };
<span class="linenos">7</span>	        store[doc.href] = {
<span class="linenos">8</span>	            <span class="hljs-string">'title'</span>: doc.title
<span class="linenos">9</span>	        };
<span class="linenos">10</span>	        searchIndex.add(doc);
<span class="linenos">11</span>	    }</code></pre>
<p>At this point, <code>post_items</code> represents an array of objects, with each object representing a blog post. Note that the <code>body</code> field is set to the value of the item’s attribute <code>post.rawcontent</code>, which represents the raw Markdown rather than the compiled HTML.</p>
<p>I then store the title in the <code>store</code> object, so that it can be accessed using the <code>href</code> field as a key.</p>
<p>I then do the same thing when generating the pages:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	    <span class="hljs-comment">// Add them to the index</span>
<span class="linenos">2</span>	    <span class="hljs-keyword">var</span> doc = {
<span class="linenos">3</span>	        <span class="hljs-string">'title'</span>: data.meta.title,
<span class="linenos">4</span>	        <span class="hljs-string">'body'</span>: data.post.rawcontent,
<span class="linenos">5</span>	        <span class="hljs-string">'href'</span>: permalink + <span class="hljs-string">'/'</span>
<span class="linenos">6</span>	    };
<span class="linenos">7</span>	    store[doc.href] = {
<span class="linenos">8</span>	        <span class="hljs-string">'title'</span>: data.meta.title
<span class="linenos">9</span>	    };
<span class="linenos">10</span>	    searchIndex.add(doc);</code></pre>
<p>Note that this is already inside the loop that generates the pages, so I don’t include that.</p>
<p>We then write the index to a file:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	    <span class="hljs-comment">// Write index</span>
<span class="linenos">2</span>	    grunt.file.write(options.www.dest + <span class="hljs-string">'/lunr.json'</span>, <span class="hljs-built_in">JSON</span>.stringify({
<span class="linenos">3</span>	        index: searchIndex.toJSON(),
<span class="linenos">4</span>	        store: store
<span class="linenos">5</span>	    }));</code></pre>
<p>That takes care of generating our index, but we need to implement some client-side code to handle the search. We need to include Lunr.js on the client side as well, (I recommend using Bower to do so), alongside jQuery. If you include both, the following code should do the trick:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	$(<span class="hljs-built_in">document</span>).ready(<span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">()</span> </span>{
<span class="linenos">2</span>	<span class="hljs-pi">    'use strict'</span>;
<span class="linenos">3</span>	
<span class="linenos">4</span>	    <span class="hljs-comment">// Set up search</span>
<span class="linenos">5</span>	    <span class="hljs-keyword">var</span> index, store;
<span class="linenos">6</span>	    $.getJSON(<span class="hljs-string">'/lunr.json'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(response)</span> </span>{
<span class="linenos">7</span>	
<span class="linenos">8</span>	        <span class="hljs-comment">// Create index</span>
<span class="linenos">9</span>	        index = lunr.Index.load(response.index);
<span class="linenos">10</span>	
<span class="linenos">11</span>	        <span class="hljs-comment">// Create store</span>
<span class="linenos">12</span>	        store = response.store;
<span class="linenos">13</span>	
<span class="linenos">14</span>	        <span class="hljs-comment">// Handle search</span>
<span class="linenos">15</span>	        $(<span class="hljs-string">'input#search'</span>).on(<span class="hljs-string">'keyup'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">()</span> </span>{
<span class="linenos">16</span>	            <span class="hljs-comment">// Get query</span>
<span class="linenos">17</span>	            <span class="hljs-keyword">var</span> query = $(<span class="hljs-keyword">this</span>).val();
<span class="linenos">18</span>	
<span class="linenos">19</span>	            <span class="hljs-comment">// Search for it</span>
<span class="linenos">20</span>	            <span class="hljs-keyword">var</span> result = index.search(query);
<span class="linenos">21</span>	
<span class="linenos">22</span>	            <span class="hljs-comment">// Output it</span>
<span class="linenos">23</span>	            <span class="hljs-keyword">var</span> resultdiv = $(<span class="hljs-string">'ul.searchresults'</span>);
<span class="linenos">24</span>	            <span class="hljs-keyword">if</span> (result.length === <span class="hljs-number">0</span>) {
<span class="linenos">25</span>	                <span class="hljs-comment">// Hide results</span>
<span class="linenos">26</span>	                resultdiv.hide();
<span class="linenos">27</span>	            } <span class="hljs-keyword">else</span> {
<span class="linenos">28</span>	                <span class="hljs-comment">// Show results</span>
<span class="linenos">29</span>	                resultdiv.empty();
<span class="linenos">30</span>	                <span class="hljs-keyword">for</span> (<span class="hljs-keyword">var</span> item <span class="hljs-keyword">in</span> result) {
<span class="linenos">31</span>	                    <span class="hljs-keyword">var</span> ref = result[item].ref;
<span class="linenos">32</span>	                    <span class="hljs-keyword">var</span> searchitem = <span class="hljs-string">'&lt;li&gt;&lt;a href="'</span> + ref + <span class="hljs-string">'"&gt;'</span> + store[ref].title + <span class="hljs-string">'&lt;/a&gt;&lt;/li&gt;'</span>;
<span class="linenos">33</span>	                    resultdiv.append(searchitem);
<span class="linenos">34</span>	                }
<span class="linenos">35</span>	                resultdiv.show();
<span class="linenos">36</span>	            }
<span class="linenos">37</span>	        });
<span class="linenos">38</span>	    });
<span class="linenos">39</span>	});</code></pre>
<p>This should be easy to understand. On load, we fetch and parse the <code>lunr.json</code> file from the server, and load the index. We then set up an event handler for the <code>keyup</code> event on an input with the ID of <code>search</code>. We get the value of the input, and query our index, and we loop through our results and display them.</p>
<p>I was pleased with how straightforward it was to implement search with Lunr.js, and it works well. It’s also a lot faster than any server-side solution since the index is generated during the build process, and is loaded with the test of the site, so the only factor in the speed of the response is how quick your browser executes JavaScript. You could probably also use it with a Node.js application by generating the index dynamically, although you’d probably want to cache it to some extent.</p>
]]></description>
            <link>http://matthewdaly.co.uk/blog/2015/04/18/how-i-added-search-to-my-site-with-lunr-dot-js/</link>
            <guid isPermaLink="true">http://matthewdaly.co.uk/blog/2015/04/18/how-i-added-search-to-my-site-with-lunr-dot-js/</guid>
            <pubDate>Sat, 18 Apr 2015 14:05:41 GMT</pubDate>
        </item>
        <item>
            <title><![CDATA[Adding a new search engine to my site]]></title>
            <description><![CDATA[<p>I’ve just finished implementing a new search engine for this site. Obviously, with it using a static site generator, searching a relational database isn’t an option. For a long while I’d just been getting by with Google’s site-specific search, which worked, but meant leaving the site to view the search results.</p>
<p>Now, I’ve implemented a client-side search system using <a href="http://lunrjs.com/">Lunr.js</a>. It wasn’t too time consuming, and as the index is generated with the rest of the site and loaded with the page, the response is almost instantaneous. I may write a future blog post on how to integrate Lunr.js with your site, as it’s very handy and is an ideal solution for implementing search on a static site.</p>
]]></description>
            <link>http://matthewdaly.co.uk/blog/2015/04/04/adding-a-new-search-engine-to-my-site/</link>
            <guid isPermaLink="true">http://matthewdaly.co.uk/blog/2015/04/04/adding-a-new-search-engine-to-my-site/</guid>
            <pubDate>Sat, 04 Apr 2015 00:47:43 GMT</pubDate>
        </item>
        <item>
            <title><![CDATA[Syntax highlighting in fenced code blocks in Vim]]></title>
            <description><![CDATA[<p>Just thought I’d share a little trick I picked up recently. As you may know, GitHub flavoured Markdown (which I use for this blog) supports fenced code blocks, allowing you to specify a language for a block of code in a Markdown file.</p>
<p>If you put the following code in your <code>.vimrc</code>, you can get syntax highlighting in those code blocks when you open up a Markdown file in Vim:</p>
<pre><code class="hljs lang-viml"><span class="linenos">1</span>	<span class="hljs-string">"Syntax highlighting in Markdown
<span class="linenos">2</span>	</span><span class="hljs-keyword">au</span> BufNewFile,BufReadPost *.md <span class="hljs-keyword">set</span> <span class="hljs-keyword">filetype</span>=markdown
<span class="linenos">3</span>	<span class="hljs-keyword">let</span> <span class="hljs-variable">g:markdown_fenced_languages</span> = [<span class="hljs-string">'bash=sh'</span>, <span class="hljs-string">'css'</span>, <span class="hljs-string">'django'</span>, <span class="hljs-string">'handlebars'</span>, <span class="hljs-string">'javascript'</span>, <span class="hljs-string">'js=javascript'</span>, <span class="hljs-string">'json=javascript'</span>, <span class="hljs-string">'perl'</span>, <span class="hljs-string">'php'</span>, <span class="hljs-string">'python'</span>, <span class="hljs-string">'ruby'</span>, <span class="hljs-string">'sass'</span>, <span class="hljs-string">'xml'</span>, <span class="hljs-string">'html'</span>]</code></pre>
<p>This does depend on having the appropriate syntax files installed. However, you can easily add in syntax files for many other languages that Vim supports, and there are third-party ones available to install - in my case, I’ve got the <code>handlebars</code> one installed, which doesn’t come with Vim.</p>
]]></description>
            <link>http://matthewdaly.co.uk/blog/2015/03/02/syntax-highlighting-in-fenced-code-blocks-in-vim/</link>
            <guid isPermaLink="true">http://matthewdaly.co.uk/blog/2015/03/02/syntax-highlighting-in-fenced-code-blocks-in-vim/</guid>
            <pubDate>Mon, 02 Mar 2015 23:25:43 GMT</pubDate>
        </item>
        <item>
            <title><![CDATA[Extending our Node.js and Redis chat server]]></title>
            <description><![CDATA[<p>In this tutorial, we’re going to extend the chat system we built in <a href="/blog/2014/12/31/building-a-chat-server-with-node-dot-js-and-redis/">the first tutorial</a> to include the following functionality:</p>
<ul>
<li>Persisting the data</li>
<li>Prompting users to sign in and storing their details in a Redis-backed session</li>
</ul>
<p>In the process, we’ll pick up a bit more about using Redis.</p>
<h2 id="persistence">Persistence</h2>
<p>Our first task is to make our messages persist when the session ends. Now, in order to do this, we’re going to use a list. A list in Redis can be thought of as equivalent to an array or list in most programming languages, and can be retrieved by passing the key in a similar fashion to how you would retrieve a string.</p>
<p>As usual, we will write our test first. Open up <code>test/test.js</code> and replace the test for sending a message with this:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	    <span class="hljs-comment">// Test sending a message</span>
<span class="linenos">2</span>	    describe(<span class="hljs-string">'Test sending a message'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">()</span> </span>{
<span class="linenos">3</span>	        it(<span class="hljs-string">"should return 'Message received'"</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(done)</span> </span>{
<span class="linenos">4</span>	            <span class="hljs-comment">// Connect to server</span>
<span class="linenos">5</span>	            <span class="hljs-keyword">var</span> socket = io.connect(<span class="hljs-string">'http://localhost:5000'</span>, {
<span class="linenos">6</span>	                <span class="hljs-string">'reconnection delay'</span> : <span class="hljs-number">0</span>,
<span class="linenos">7</span>	                <span class="hljs-string">'reopen delay'</span> : <span class="hljs-number">0</span>,
<span class="linenos">8</span>	                <span class="hljs-string">'force new connection'</span> : <span class="hljs-literal">true</span>
<span class="linenos">9</span>	            });
<span class="linenos">10</span>	
<span class="linenos">11</span>	            <span class="hljs-comment">// Handle the message being received</span>
<span class="linenos">12</span>	            socket.on(<span class="hljs-string">'message'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(data)</span> </span>{
<span class="linenos">13</span>	                expect(data).to.include(<span class="hljs-string">'Message received'</span>);
<span class="linenos">14</span>	
<span class="linenos">15</span>	                client.lrange(<span class="hljs-string">'chat:messages'</span>, <span class="hljs-number">0</span>, -<span class="hljs-number">1</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(err, messages)</span> </span>{
<span class="linenos">16</span>	                    <span class="hljs-comment">// Check message has been persisted</span>
<span class="linenos">17</span>	                    <span class="hljs-keyword">var</span> message_list = [];
<span class="linenos">18</span>	                    messages.forEach(<span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(message, i)</span> </span>{
<span class="linenos">19</span>	                        message_list.push(message);
<span class="linenos">20</span>	                    });
<span class="linenos">21</span>	                    expect(message_list[<span class="hljs-number">0</span>]).to.include(<span class="hljs-string">'Message received'</span>);
<span class="linenos">22</span>	
<span class="linenos">23</span>	                    <span class="hljs-comment">// Finish up</span>
<span class="linenos">24</span>	                    socket.disconnect();
<span class="linenos">25</span>	                    done();
<span class="linenos">26</span>	                });
<span class="linenos">27</span>	            });
<span class="linenos">28</span>	
<span class="linenos">29</span>	            <span class="hljs-comment">// Send the message</span>
<span class="linenos">30</span>	            socket.emit(<span class="hljs-string">'send'</span>, { message: <span class="hljs-string">'Message received'</span> });
<span class="linenos">31</span>	        });
<span class="linenos">32</span>	    });</code></pre>
<p>The main difference here is that we use our Redis client to get the list <code>chat:messages</code>, and check to see if our message appears in it. Now, let’s run our test to ensure it fails:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ npm <span class="hljs-built_in">test</span>
<span class="linenos">2</span>	
<span class="linenos">3</span>	&gt; babblr@<span class="hljs-number">1.0</span>.<span class="hljs-number">0</span> <span class="hljs-built_in">test</span> /Users/matthewdaly/Projects/babblr
<span class="linenos">4</span>	&gt; grunt <span class="hljs-built_in">test</span> --verbose
<span class="linenos">5</span>	
<span class="linenos">6</span>	Initializing
<span class="linenos">7</span>	Command-line options: --verbose
<span class="linenos">8</span>	
<span class="linenos">9</span>	Reading <span class="hljs-string">"Gruntfile.js"</span> Gruntfile...OK
<span class="linenos">10</span>	
<span class="linenos">11</span>	Registering Gruntfile tasks.
<span class="linenos">12</span>	Initializing config...OK
<span class="linenos">13</span>	
<span class="linenos">14</span>	Registering <span class="hljs-string">"grunt-contrib-jshint"</span> <span class="hljs-built_in">local</span> Npm module tasks.
<span class="linenos">15</span>	Reading /Users/matthewdaly/Projects/babblr/node_modules/grunt-contrib-jshint/package.json...OK
<span class="linenos">16</span>	Parsing /Users/matthewdaly/Projects/babblr/node_modules/grunt-contrib-jshint/package.json...OK
<span class="linenos">17</span>	Loading <span class="hljs-string">"jshint.js"</span> tasks...OK
<span class="linenos">18</span>	+ jshint
<span class="linenos">19</span>	
<span class="linenos">20</span>	Registering <span class="hljs-string">"grunt-coveralls"</span> <span class="hljs-built_in">local</span> Npm module tasks.
<span class="linenos">21</span>	Reading /Users/matthewdaly/Projects/babblr/node_modules/grunt-coveralls/package.json...OK
<span class="linenos">22</span>	Parsing /Users/matthewdaly/Projects/babblr/node_modules/grunt-coveralls/package.json...OK
<span class="linenos">23</span>	Loading <span class="hljs-string">"coverallsTask.js"</span> tasks...OK
<span class="linenos">24</span>	+ coveralls
<span class="linenos">25</span>	
<span class="linenos">26</span>	Registering <span class="hljs-string">"grunt-mocha-istanbul"</span> <span class="hljs-built_in">local</span> Npm module tasks.
<span class="linenos">27</span>	Reading /Users/matthewdaly/Projects/babblr/node_modules/grunt-mocha-istanbul/package.json...OK
<span class="linenos">28</span>	Parsing /Users/matthewdaly/Projects/babblr/node_modules/grunt-mocha-istanbul/package.json...OK
<span class="linenos">29</span>	Loading <span class="hljs-string">"index.js"</span> tasks...OK
<span class="linenos">30</span>	+ istanbul_check_coverage, mocha_istanbul
<span class="linenos">31</span>	Loading <span class="hljs-string">"Gruntfile.js"</span> tasks...OK
<span class="linenos">32</span>	+ <span class="hljs-built_in">test</span>
<span class="linenos">33</span>	
<span class="linenos">34</span>	Running tasks: <span class="hljs-built_in">test</span>
<span class="linenos">35</span>	
<span class="linenos">36</span>	Running <span class="hljs-string">"test"</span> task
<span class="linenos">37</span>	
<span class="linenos">38</span>	Running <span class="hljs-string">"jshint"</span> task
<span class="linenos">39</span>	
<span class="linenos">40</span>	Running <span class="hljs-string">"jshint:all"</span> (jshint) task
<span class="linenos">41</span>	Verifying property jshint.all exists <span class="hljs-keyword">in</span> config...OK
<span class="linenos">42</span>	Files: <span class="hljs-built_in">test</span>/test.js, index.js -&gt; all
<span class="linenos">43</span>	Options: force=<span class="hljs-literal">false</span>, reporterOutput=null
<span class="linenos">44</span>	OK
<span class="linenos">45</span>	&gt;&gt; <span class="hljs-number">2</span> files lint free.
<span class="linenos">46</span>	
<span class="linenos">47</span>	Running <span class="hljs-string">"mocha_istanbul:coverage"</span> (mocha_istanbul) task
<span class="linenos">48</span>	Verifying property mocha_istanbul.coverage exists <span class="hljs-keyword">in</span> config...OK
<span class="linenos">49</span>	Files: <span class="hljs-built_in">test</span>
<span class="linenos">50</span>	Options: require=[], ui=<span class="hljs-literal">false</span>, globals=[], reporter=<span class="hljs-literal">false</span>, timeout=<span class="hljs-literal">false</span>, coverage=<span class="hljs-literal">false</span>, slow=<span class="hljs-literal">false</span>, grep=<span class="hljs-literal">false</span>, dryRun=<span class="hljs-literal">false</span>, quiet=<span class="hljs-literal">false</span>, recursive=<span class="hljs-literal">false</span>, mask=<span class="hljs-string">"*.js"</span>, root=<span class="hljs-literal">false</span>, <span class="hljs-built_in">print</span>=<span class="hljs-literal">false</span>, noColors=<span class="hljs-literal">false</span>, harmony=<span class="hljs-literal">false</span>, coverageFolder=<span class="hljs-string">"coverage"</span>, reportFormats=[<span class="hljs-string">"cobertura"</span>,<span class="hljs-string">"html"</span>,<span class="hljs-string">"lcovonly"</span>], check={<span class="hljs-string">"statements"</span>:<span class="hljs-literal">false</span>,<span class="hljs-string">"lines"</span>:<span class="hljs-literal">false</span>,<span class="hljs-string">"functions"</span>:<span class="hljs-literal">false</span>,<span class="hljs-string">"branches"</span>:<span class="hljs-literal">false</span>}, excludes=<span class="hljs-literal">false</span>, mochaOptions=<span class="hljs-literal">false</span>, istanbulOptions=<span class="hljs-literal">false</span>
<span class="linenos">51</span>	&gt;&gt; Will execute: node /Users/matthewdaly/Projects/babblr/node_modules/istanbul/lib/cli.js cover --dir=/Users/matthewdaly/Projects/babblr/coverage --report=cobertura --report=html --report=lcovonly /Users/matthewdaly/Projects/babblr/node_modules/mocha/bin/_mocha -- <span class="hljs-built_in">test</span>/*.js
<span class="linenos">52</span>	Listening on port <span class="hljs-number">5000</span>
<span class="linenos">53</span>	
<span class="linenos">54</span>	
<span class="linenos">55</span>	  server
<span class="linenos">56</span>	Starting the server
<span class="linenos">57</span>	    Test the index route
<span class="linenos">58</span>	      ✓ should <span class="hljs-built_in">return</span> a page with the title Babblr (<span class="hljs-number">484</span>ms)
<span class="linenos">59</span>	    Test sending a message
<span class="linenos">60</span>	      <span class="hljs-number">1</span>) should <span class="hljs-built_in">return</span> <span class="hljs-string">'Message received'</span>
<span class="linenos">61</span>	Stopping the server
<span class="linenos">62</span>	
<span class="linenos">63</span>	
<span class="linenos">64</span>	  <span class="hljs-number">1</span> passing (<span class="hljs-number">552</span>ms)
<span class="linenos">65</span>	  <span class="hljs-number">1</span> failing
<span class="linenos">66</span>	
<span class="linenos">67</span>	  <span class="hljs-number">1</span>) server Test sending a message should <span class="hljs-built_in">return</span> <span class="hljs-string">'Message received'</span>:
<span class="linenos">68</span>	     Uncaught AssertionError: expected undefined to include <span class="hljs-string">'Message received'</span>
<span class="linenos">69</span>	      at /Users/matthewdaly/Projects/babblr/<span class="hljs-built_in">test</span>/test.js:<span class="hljs-number">62</span>:<span class="hljs-number">48</span>
<span class="linenos">70</span>	      at try_callback (/Users/matthewdaly/Projects/babblr/node_modules/redis/index.js:<span class="hljs-number">592</span>:<span class="hljs-number">9</span>)
<span class="linenos">71</span>	      at RedisClient.return_reply (/Users/matthewdaly/Projects/babblr/node_modules/redis/index.js:<span class="hljs-number">685</span>:<span class="hljs-number">13</span>)
<span class="linenos">72</span>	      at HiredisReplyParser.&lt;anonymous&gt; (/Users/matthewdaly/Projects/babblr/node_modules/redis/index.js:<span class="hljs-number">321</span>:<span class="hljs-number">14</span>)
<span class="linenos">73</span>	      at HiredisReplyParser.emit (events.js:<span class="hljs-number">95</span>:<span class="hljs-number">17</span>)
<span class="linenos">74</span>	      at HiredisReplyParser.execute (/Users/matthewdaly/Projects/babblr/node_modules/redis/lib/parser/hiredis.js:<span class="hljs-number">43</span>:<span class="hljs-number">18</span>)
<span class="linenos">75</span>	      at RedisClient.on_data (/Users/matthewdaly/Projects/babblr/node_modules/redis/index.js:<span class="hljs-number">547</span>:<span class="hljs-number">27</span>)
<span class="linenos">76</span>	      at Socket.&lt;anonymous&gt; (/Users/matthewdaly/Projects/babblr/node_modules/redis/index.js:<span class="hljs-number">102</span>:<span class="hljs-number">14</span>)
<span class="linenos">77</span>	      at Socket.emit (events.js:<span class="hljs-number">95</span>:<span class="hljs-number">17</span>)
<span class="linenos">78</span>	      at Socket.&lt;anonymous&gt; (_stream_readable.js:<span class="hljs-number">765</span>:<span class="hljs-number">14</span>)
<span class="linenos">79</span>	      at Socket.emit (events.js:<span class="hljs-number">92</span>:<span class="hljs-number">17</span>)
<span class="linenos">80</span>	      at emitReadable_ (_stream_readable.js:<span class="hljs-number">427</span>:<span class="hljs-number">10</span>)
<span class="linenos">81</span>	      at emitReadable (_stream_readable.js:<span class="hljs-number">423</span>:<span class="hljs-number">5</span>)
<span class="linenos">82</span>	      at readableAddChunk (_stream_readable.js:<span class="hljs-number">166</span>:<span class="hljs-number">9</span>)
<span class="linenos">83</span>	      at Socket.Readable.push (_stream_readable.js:<span class="hljs-number">128</span>:<span class="hljs-number">10</span>)
<span class="linenos">84</span>	      at TCP.onread (net.js:<span class="hljs-number">529</span>:<span class="hljs-number">21</span>)
<span class="linenos">85</span>	
<span class="linenos">86</span>	
<span class="linenos">87</span>	
<span class="linenos">88</span>	=============================================================================
<span class="linenos">89</span>	Writing coverage object [/Users/matthewdaly/Projects/babblr/coverage/coverage.json]
<span class="linenos">90</span>	Writing coverage reports at [/Users/matthewdaly/Projects/babblr/coverage]
<span class="linenos">91</span>	=============================================================================
<span class="linenos">92</span>	
<span class="linenos">93</span>	=============================== Coverage summary ===============================
<span class="linenos">94</span>	Statements   : <span class="hljs-number">96.97</span>% ( <span class="hljs-number">32</span>/<span class="hljs-number">33</span> ), <span class="hljs-number">5</span> ignored
<span class="linenos">95</span>	Branches     : <span class="hljs-number">100</span>% ( <span class="hljs-number">6</span>/<span class="hljs-number">6</span> ), <span class="hljs-number">1</span> ignored
<span class="linenos">96</span>	Functions    : <span class="hljs-number">80</span>% ( <span class="hljs-number">4</span>/<span class="hljs-number">5</span> )
<span class="linenos">97</span>	Lines        : <span class="hljs-number">96.97</span>% ( <span class="hljs-number">32</span>/<span class="hljs-number">33</span> )
<span class="linenos">98</span>	================================================================================
<span class="linenos">99</span>	&gt;&gt;
<span class="linenos">100</span>	Warning: Task <span class="hljs-string">"mocha_istanbul:coverage"</span> failed. Use --force to continue.
<span class="linenos">101</span>	
<span class="linenos">102</span>	Aborted due to warnings.
<span class="linenos">103</span>	npm ERR! Test failed.  See above <span class="hljs-keyword">for</span> more details.
<span class="linenos">104</span>	npm ERR! not ok code <span class="hljs-number">0</span></code></pre>
<p>Our test fails, so now we can start work on implementing the functionality we need. First of all, when a new message is sent, we need to push it to the list. Amend the new message handler in <code>index.js</code> to look like this:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	<span class="hljs-comment">// Handle new messages</span>
<span class="linenos">2</span>	io.sockets.on(<span class="hljs-string">'connection'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(socket)</span> </span>{
<span class="linenos">3</span>	    <span class="hljs-comment">// Subscribe to the Redis channel</span>
<span class="linenos">4</span>	    subscribe.subscribe(<span class="hljs-string">'ChatChannel'</span>);
<span class="linenos">5</span>	
<span class="linenos">6</span>	    <span class="hljs-comment">// Handle incoming messages</span>
<span class="linenos">7</span>	    socket.on(<span class="hljs-string">'send'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(data)</span> </span>{
<span class="linenos">8</span>	        <span class="hljs-comment">// Publish it</span>
<span class="linenos">9</span>	        client.publish(<span class="hljs-string">'ChatChannel'</span>, data.message);
<span class="linenos">10</span>	
<span class="linenos">11</span>	        <span class="hljs-comment">// Persist it to a Redis list</span>
<span class="linenos">12</span>	        client.rpush(<span class="hljs-string">'chat:messages'</span>, <span class="hljs-string">'Anonymous Coward : '</span> + data.message);
<span class="linenos">13</span>	    });
<span class="linenos">14</span>	
<span class="linenos">15</span>	    <span class="hljs-comment">// Handle receiving messages</span>
<span class="linenos">16</span>	    <span class="hljs-keyword">var</span> callback = <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(channel, data)</span> </span>{
<span class="linenos">17</span>	        socket.emit(<span class="hljs-string">'message'</span>, data);
<span class="linenos">18</span>	    };
<span class="linenos">19</span>	    subscribe.on(<span class="hljs-string">'message'</span>, callback);
<span class="linenos">20</span>	
<span class="linenos">21</span>	    <span class="hljs-comment">// Handle disconnect</span>
<span class="linenos">22</span>	    socket.on(<span class="hljs-string">'disconnect'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">()</span> </span>{
<span class="linenos">23</span>	        subscribe.removeListener(<span class="hljs-string">'message'</span>, callback);
<span class="linenos">24</span>	    });
<span class="linenos">25</span>	});</code></pre>
<p>The only significant change is the <code>Persist it to a Redis list</code> section. Here we call the <code>RPUSH</code> command to push the current message to <code>chat:messages</code>. <code>RPUSH</code> pushes a message to the end of the list. There’s a similar command, <code>LPUSH</code>, which pushes an item to the beginning of the list, as well as <code>LPOP</code> and <code>RPOP</code>, which remove and return an item from the beginning and end of the list respectively.</p>
<p>Next we need to handle displaying the list when the main route loads. Replace the index route in <code>index.js</code> with this:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	<span class="hljs-comment">// Define index route</span>
<span class="linenos">2</span>	app.get(<span class="hljs-string">'/'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(req, res)</span> </span>{
<span class="linenos">3</span>	    <span class="hljs-comment">// Get messages</span>
<span class="linenos">4</span>	    client.lrange(<span class="hljs-string">'chat:messages'</span>, <span class="hljs-number">0</span>, -<span class="hljs-number">1</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(err, messages)</span> </span>{
<span class="linenos">5</span>	        <span class="hljs-comment">/* istanbul ignore if */</span>
<span class="linenos">6</span>	        <span class="hljs-keyword">if</span> (err) {
<span class="linenos">7</span>	            <span class="hljs-built_in">console</span>.log(err);
<span class="linenos">8</span>	        } <span class="hljs-keyword">else</span> {
<span class="linenos">9</span>	            <span class="hljs-comment">// Get messages</span>
<span class="linenos">10</span>	            <span class="hljs-keyword">var</span> message_list = [];
<span class="linenos">11</span>	            messages.forEach(<span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(message, i)</span> </span>{
<span class="linenos">12</span>	                <span class="hljs-comment">/* istanbul ignore next */</span>
<span class="linenos">13</span>	                message_list.push(message);
<span class="linenos">14</span>	            });
<span class="linenos">15</span>	
<span class="linenos">16</span>	            <span class="hljs-comment">// Render page</span>
<span class="linenos">17</span>	            res.render(<span class="hljs-string">'index'</span>, { messages: message_list});
<span class="linenos">18</span>	        }
<span class="linenos">19</span>	    });
<span class="linenos">20</span>	});</code></pre>
<p>Here we use the client to return all messages in the list by using the <code>LRANGE</code> command and defining the slice as being from the start to the end of the list. We then loop through the mesages and push each to a list, before passing that list to the view.</p>
<p>Speaking of which, we also need to update <code>views/index.hbs</code>:</p>
<pre><code class="hljs lang-handlebars"><span class="linenos">1</span>	<span class="xml"></span><span class="hljs-expression">{{&gt; <span class="hljs-variable">header</span> }}</span><span class="xml">
<span class="linenos">2</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"container"</span>&gt;</span>
<span class="linenos">3</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"row"</span>&gt;</span>
<span class="linenos">4</span>	                <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"col-md-8"</span>&gt;</span>
<span class="linenos">5</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"conversation"</span>&gt;</span>
<span class="linenos">6</span>	                        </span><span class="hljs-expression">{{<span class="hljs-begin-block">#<span class="hljs-keyword">each</span> messages</span>}}</span><span class="xml">
<span class="linenos">7</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">p</span>&gt;</span></span><span class="hljs-expression">{{<span class="hljs-variable">this</span>}}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">p</span>&gt;</span>
<span class="linenos">8</span>	                        </span><span class="hljs-expression">{{<span class="hljs-end-block">/<span class="hljs-keyword">each</span></span>}}</span><span class="xml">
<span class="linenos">9</span>	                    <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">10</span>	                <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">11</span>	                <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"col-md-4"</span>&gt;</span>
<span class="linenos">12</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">form</span>&gt;</span>
<span class="linenos">13</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"form-group"</span>&gt;</span>
<span class="linenos">14</span>	                            <span class="hljs-tag">&lt;<span class="hljs-title">label</span> <span class="hljs-attribute">for</span>=<span class="hljs-value">"message"</span>&gt;</span>Message<span class="hljs-tag">&lt;/<span class="hljs-title">label</span>&gt;</span>
<span class="linenos">15</span>	                            <span class="hljs-tag">&lt;<span class="hljs-title">textarea</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"form-control"</span> <span class="hljs-attribute">id</span>=<span class="hljs-value">"message"</span> <span class="hljs-attribute">rows</span>=<span class="hljs-value">"20"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">textarea</span>&gt;</span>
<span class="linenos">16</span>	                            <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">id</span>=<span class="hljs-value">"submitbutton"</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"btn btn-primary form-control"</span>&gt;</span>Submit<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">17</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">18</span>	                    <span class="hljs-tag">&lt;/<span class="hljs-title">form</span>&gt;</span>
<span class="linenos">19</span>	                <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">20</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">21</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">22</span>	</span><span class="hljs-expression">{{&gt; <span class="hljs-variable">footer</span> }}</span><span class="xml"></span></code></pre>
<p>This just loops through the messages and prints each one in turn. Now let’s run our tests and make sure they pass:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ npm <span class="hljs-built_in">test</span>
<span class="linenos">2</span>	
<span class="linenos">3</span>	&gt; babblr@<span class="hljs-number">1.0</span>.<span class="hljs-number">0</span> <span class="hljs-built_in">test</span> /Users/matthewdaly/Projects/babblr
<span class="linenos">4</span>	&gt; grunt <span class="hljs-built_in">test</span> --verbose
<span class="linenos">5</span>	
<span class="linenos">6</span>	Initializing
<span class="linenos">7</span>	Command-line options: --verbose
<span class="linenos">8</span>	
<span class="linenos">9</span>	Reading <span class="hljs-string">"Gruntfile.js"</span> Gruntfile...OK
<span class="linenos">10</span>	
<span class="linenos">11</span>	Registering Gruntfile tasks.
<span class="linenos">12</span>	Initializing config...OK
<span class="linenos">13</span>	
<span class="linenos">14</span>	Registering <span class="hljs-string">"grunt-contrib-jshint"</span> <span class="hljs-built_in">local</span> Npm module tasks.
<span class="linenos">15</span>	Reading /Users/matthewdaly/Projects/babblr/node_modules/grunt-contrib-jshint/package.json...OK
<span class="linenos">16</span>	Parsing /Users/matthewdaly/Projects/babblr/node_modules/grunt-contrib-jshint/package.json...OK
<span class="linenos">17</span>	Loading <span class="hljs-string">"jshint.js"</span> tasks...OK
<span class="linenos">18</span>	+ jshint
<span class="linenos">19</span>	
<span class="linenos">20</span>	Registering <span class="hljs-string">"grunt-coveralls"</span> <span class="hljs-built_in">local</span> Npm module tasks.
<span class="linenos">21</span>	Reading /Users/matthewdaly/Projects/babblr/node_modules/grunt-coveralls/package.json...OK
<span class="linenos">22</span>	Parsing /Users/matthewdaly/Projects/babblr/node_modules/grunt-coveralls/package.json...OK
<span class="linenos">23</span>	Loading <span class="hljs-string">"coverallsTask.js"</span> tasks...OK
<span class="linenos">24</span>	+ coveralls
<span class="linenos">25</span>	
<span class="linenos">26</span>	Registering <span class="hljs-string">"grunt-mocha-istanbul"</span> <span class="hljs-built_in">local</span> Npm module tasks.
<span class="linenos">27</span>	Reading /Users/matthewdaly/Projects/babblr/node_modules/grunt-mocha-istanbul/package.json...OK
<span class="linenos">28</span>	Parsing /Users/matthewdaly/Projects/babblr/node_modules/grunt-mocha-istanbul/package.json...OK
<span class="linenos">29</span>	Loading <span class="hljs-string">"index.js"</span> tasks...OK
<span class="linenos">30</span>	+ istanbul_check_coverage, mocha_istanbul
<span class="linenos">31</span>	Loading <span class="hljs-string">"Gruntfile.js"</span> tasks...OK
<span class="linenos">32</span>	+ <span class="hljs-built_in">test</span>
<span class="linenos">33</span>	
<span class="linenos">34</span>	Running tasks: <span class="hljs-built_in">test</span>
<span class="linenos">35</span>	
<span class="linenos">36</span>	Running <span class="hljs-string">"test"</span> task
<span class="linenos">37</span>	
<span class="linenos">38</span>	Running <span class="hljs-string">"jshint"</span> task
<span class="linenos">39</span>	
<span class="linenos">40</span>	Running <span class="hljs-string">"jshint:all"</span> (jshint) task
<span class="linenos">41</span>	Verifying property jshint.all exists <span class="hljs-keyword">in</span> config...OK
<span class="linenos">42</span>	Files: <span class="hljs-built_in">test</span>/test.js, index.js -&gt; all
<span class="linenos">43</span>	Options: force=<span class="hljs-literal">false</span>, reporterOutput=null
<span class="linenos">44</span>	OK
<span class="linenos">45</span>	&gt;&gt; <span class="hljs-number">2</span> files lint free.
<span class="linenos">46</span>	
<span class="linenos">47</span>	Running <span class="hljs-string">"mocha_istanbul:coverage"</span> (mocha_istanbul) task
<span class="linenos">48</span>	Verifying property mocha_istanbul.coverage exists <span class="hljs-keyword">in</span> config...OK
<span class="linenos">49</span>	Files: <span class="hljs-built_in">test</span>
<span class="linenos">50</span>	Options: require=[], ui=<span class="hljs-literal">false</span>, globals=[], reporter=<span class="hljs-literal">false</span>, timeout=<span class="hljs-literal">false</span>, coverage=<span class="hljs-literal">false</span>, slow=<span class="hljs-literal">false</span>, grep=<span class="hljs-literal">false</span>, dryRun=<span class="hljs-literal">false</span>, quiet=<span class="hljs-literal">false</span>, recursive=<span class="hljs-literal">false</span>, mask=<span class="hljs-string">"*.js"</span>, root=<span class="hljs-literal">false</span>, <span class="hljs-built_in">print</span>=<span class="hljs-literal">false</span>, noColors=<span class="hljs-literal">false</span>, harmony=<span class="hljs-literal">false</span>, coverageFolder=<span class="hljs-string">"coverage"</span>, reportFormats=[<span class="hljs-string">"cobertura"</span>,<span class="hljs-string">"html"</span>,<span class="hljs-string">"lcovonly"</span>], check={<span class="hljs-string">"statements"</span>:<span class="hljs-literal">false</span>,<span class="hljs-string">"lines"</span>:<span class="hljs-literal">false</span>,<span class="hljs-string">"functions"</span>:<span class="hljs-literal">false</span>,<span class="hljs-string">"branches"</span>:<span class="hljs-literal">false</span>}, excludes=<span class="hljs-literal">false</span>, mochaOptions=<span class="hljs-literal">false</span>, istanbulOptions=<span class="hljs-literal">false</span>
<span class="linenos">51</span>	&gt;&gt; Will execute: node /Users/matthewdaly/Projects/babblr/node_modules/istanbul/lib/cli.js cover --dir=/Users/matthewdaly/Projects/babblr/coverage --report=cobertura --report=html --report=lcovonly /Users/matthewdaly/Projects/babblr/node_modules/mocha/bin/_mocha -- <span class="hljs-built_in">test</span>/*.js
<span class="linenos">52</span>	Listening on port <span class="hljs-number">5000</span>
<span class="linenos">53</span>	
<span class="linenos">54</span>	
<span class="linenos">55</span>	  server
<span class="linenos">56</span>	Starting the server
<span class="linenos">57</span>	    Test the index route
<span class="linenos">58</span>	      ✓ should <span class="hljs-built_in">return</span> a page with the title Babblr (<span class="hljs-number">1262</span>ms)
<span class="linenos">59</span>	    Test sending a message
<span class="linenos">60</span>	      ✓ should <span class="hljs-built_in">return</span> <span class="hljs-string">'Message received'</span> (<span class="hljs-number">48</span>ms)
<span class="linenos">61</span>	Stopping the server
<span class="linenos">62</span>	
<span class="linenos">63</span>	
<span class="linenos">64</span>	  <span class="hljs-number">2</span> passing (<span class="hljs-number">2</span>s)
<span class="linenos">65</span>	
<span class="linenos">66</span>	=============================================================================
<span class="linenos">67</span>	Writing coverage object [/Users/matthewdaly/Projects/babblr/coverage/coverage.json]
<span class="linenos">68</span>	Writing coverage reports at [/Users/matthewdaly/Projects/babblr/coverage]
<span class="linenos">69</span>	=============================================================================
<span class="linenos">70</span>	
<span class="linenos">71</span>	=============================== Coverage summary ===============================
<span class="linenos">72</span>	Statements   : <span class="hljs-number">100</span>% ( <span class="hljs-number">40</span>/<span class="hljs-number">40</span> ), <span class="hljs-number">7</span> ignored
<span class="linenos">73</span>	Branches     : <span class="hljs-number">100</span>% ( <span class="hljs-number">8</span>/<span class="hljs-number">8</span> ), <span class="hljs-number">2</span> ignored
<span class="linenos">74</span>	Functions    : <span class="hljs-number">85.71</span>% ( <span class="hljs-number">6</span>/<span class="hljs-number">7</span> )
<span class="linenos">75</span>	Lines        : <span class="hljs-number">100</span>% ( <span class="hljs-number">40</span>/<span class="hljs-number">40</span> )
<span class="linenos">76</span>	================================================================================
<span class="linenos">77</span>	&gt;&gt; Done. Check coverage folder.
<span class="linenos">78</span>	
<span class="linenos">79</span>	Running <span class="hljs-string">"coveralls"</span> task
<span class="linenos">80</span>	
<span class="linenos">81</span>	Running <span class="hljs-string">"coveralls:app"</span> (coveralls) task
<span class="linenos">82</span>	Verifying property coveralls.app exists <span class="hljs-keyword">in</span> config...OK
<span class="linenos">83</span>	Files: coverage/lcov.info
<span class="linenos">84</span>	Options: src=<span class="hljs-string">"coverage/lcov.info"</span>, force=<span class="hljs-literal">false</span>
<span class="linenos">85</span>	Submitting file to coveralls.io: coverage/lcov.info
<span class="linenos">86</span>	&gt;&gt; Failed to submit <span class="hljs-string">'coverage/lcov.info'</span> to coveralls: Bad response: <span class="hljs-number">422</span> {<span class="hljs-string">"message"</span>:<span class="hljs-string">"Couldn't find a repository matching this job."</span>,<span class="hljs-string">"error"</span>:<span class="hljs-literal">true</span>}
<span class="linenos">87</span>	&gt;&gt; Failed to submit coverage results to coveralls
<span class="linenos">88</span>	Warning: Task <span class="hljs-string">"coveralls:app"</span> failed. Use --force to continue.
<span class="linenos">89</span>	
<span class="linenos">90</span>	Aborted due to warnings.
<span class="linenos">91</span>	npm ERR! Test failed.  See above <span class="hljs-keyword">for</span> more details.
<span class="linenos">92</span>	npm ERR! not ok code <span class="hljs-number">0</span></code></pre>
<p>As before, don’t worry about Coveralls not working - it’s only an issue when it runs on Travis CI. If everything else is fine, our chat server should now persist our changes.</p>
<h2 id="sessions-and-user-login">Sessions and user login</h2>
<p>At present, it’s hard to carry on a conversation with someone using this site because you can’t see who is responding to you. We need to implement a mechanism to obtain a username for each user, store it in a session, and then use it to identify all of a user’s messages. In this case, we’re going to just prompt the user to enter a username of their choice, but if you wish, you can use something like <a href="http://passportjs.org/">Passport.js</a> to allow authentication using third-party services - I’ll leave that as an exercise for the reader.</p>
<p>Now, Express doesn’t include any support for sessions out of the box, so we have to install some additional libraries:</p>
<pre><code class="hljs lang-bash singleline">$ npm install connect-redis express-session body-parser --save</code></pre>
<p>The <code>express-session</code> library is middleware for Express that allows for storing and retrieving session variables, while <code>connect-redis</code> allows it to use Redis to store this data. We used <code>body-parser</code> for the URL shortener to process <code>POST</code> data, so we will use it again here. Now, we need to set it up. Replace the part of <code>index.js</code> before we set up the templating with this:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	<span class="hljs-comment">/*jslint node: true */</span>
<span class="linenos">2</span>	<span class="hljs-pi">'use strict'</span>;
<span class="linenos">3</span>	
<span class="linenos">4</span>	<span class="hljs-comment">// Declare variables used</span>
<span class="linenos">5</span>	<span class="hljs-keyword">var</span> app, base_url, client, express, hbs, io, port, RedisStore, rtg, session, subscribe;
<span class="linenos">6</span>	
<span class="linenos">7</span>	<span class="hljs-comment">// Define values</span>
<span class="linenos">8</span>	express = <span class="hljs-built_in">require</span>(<span class="hljs-string">'express'</span>);
<span class="linenos">9</span>	app = express();
<span class="linenos">10</span>	port = process.env.PORT || <span class="hljs-number">5000</span>;
<span class="linenos">11</span>	base_url = process.env.BASE_URL || <span class="hljs-string">'http://localhost:5000'</span>;
<span class="linenos">12</span>	hbs = <span class="hljs-built_in">require</span>(<span class="hljs-string">'hbs'</span>);
<span class="linenos">13</span>	session = <span class="hljs-built_in">require</span>(<span class="hljs-string">'express-session'</span>);
<span class="linenos">14</span>	RedisStore = <span class="hljs-built_in">require</span>(<span class="hljs-string">'connect-redis'</span>)(session);
<span class="linenos">15</span>	
<span class="linenos">16</span>	<span class="hljs-comment">// Set up connection to Redis</span>
<span class="linenos">17</span>	<span class="hljs-comment">/* istanbul ignore if */</span>
<span class="linenos">18</span>	<span class="hljs-keyword">if</span> (process.env.REDISTOGO_URL) {
<span class="linenos">19</span>	    rtg  = <span class="hljs-built_in">require</span>(<span class="hljs-string">'url'</span>).parse(process.env.REDISTOGO_URL);
<span class="linenos">20</span>	    client = <span class="hljs-built_in">require</span>(<span class="hljs-string">'redis'</span>).createClient(rtg.port, rtg.hostname);
<span class="linenos">21</span>	    subscribe = <span class="hljs-built_in">require</span>(<span class="hljs-string">'redis'</span>).createClient(rtg.port, rtg.hostname);
<span class="linenos">22</span>	    client.auth(rtg.auth.split(<span class="hljs-string">':'</span>)[<span class="hljs-number">1</span>]);
<span class="linenos">23</span>	    subscribe.auth(rtg.auth.split(<span class="hljs-string">':'</span>)[<span class="hljs-number">1</span>]);
<span class="linenos">24</span>	} <span class="hljs-keyword">else</span> {
<span class="linenos">25</span>	    client = <span class="hljs-built_in">require</span>(<span class="hljs-string">'redis'</span>).createClient();
<span class="linenos">26</span>	    subscribe = <span class="hljs-built_in">require</span>(<span class="hljs-string">'redis'</span>).createClient();
<span class="linenos">27</span>	}
<span class="linenos">28</span>	
<span class="linenos">29</span>	<span class="hljs-comment">// Set up session</span>
<span class="linenos">30</span>	app.use(session({
<span class="linenos">31</span>	    store: <span class="hljs-keyword">new</span> RedisStore({
<span class="linenos">32</span>	        client: client
<span class="linenos">33</span>	    }),
<span class="linenos">34</span>	    secret: <span class="hljs-string">'blibble'</span>
<span class="linenos">35</span>	}));</code></pre>
<p>This just sets up the session and configures it to use Redis as the back end. Don’t forget to change the value of <code>secret</code>.</p>
<p>Now, let’s plan out how our username system is going to work. If a user visits the site and there is no session set, then they should be redirected to a new route, <code>/login</code>. Here they will be prompted to enter a username. Once a satisfactory username (eg one or more characters) has been submitted via the form, it should be stored in the session and the user redirected to the index. There should also be a <code>/logout</code> route to destroy the session and redirect the user back to the login form.</p>
<p>First, let’s implement a test for fetching the login form in <code>test/test.js</code>:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	    <span class="hljs-comment">// Test submitting to the login route</span>
<span class="linenos">2</span>	    describe(<span class="hljs-string">'Test submitting to the login route'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">()</span> </span>{
<span class="linenos">3</span>	        it(<span class="hljs-string">'should store the username in the session and redirect the user to the index'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(done)</span> </span>{
<span class="linenos">4</span>	            request.post({ url: <span class="hljs-string">'http://localhost:5000/login'</span>,
<span class="linenos">5</span>	                form:{username: <span class="hljs-string">'bobsmith'</span>},
<span class="linenos">6</span>	                followRedirect: <span class="hljs-literal">false</span>},
<span class="linenos">7</span>	                <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(error, response, body)</span> </span>{
<span class="linenos">8</span>	                    expect(response.headers.location).to.equal(<span class="hljs-string">'/'</span>);
<span class="linenos">9</span>	                    expect(response.statusCode).to.equal(<span class="hljs-number">302</span>);
<span class="linenos">10</span>	                    done();
<span class="linenos">11</span>	            });
<span class="linenos">12</span>	        });
<span class="linenos">13</span>	    });</code></pre>
<p>This test sends a <code>POST</code> request containing the field <code>username</code> with the value <code>bobsmith</code>. We expect to be redirected to the index route.</p>
<p>Let’s run the test to make sure it fails:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ npm <span class="hljs-built_in">test</span>
<span class="linenos">2</span>	
<span class="linenos">3</span>	&gt; babblr@<span class="hljs-number">1.0</span>.<span class="hljs-number">0</span> <span class="hljs-built_in">test</span> /Users/matthewdaly/Projects/babblr
<span class="linenos">4</span>	&gt; grunt <span class="hljs-built_in">test</span> --verbose
<span class="linenos">5</span>	
<span class="linenos">6</span>	Initializing
<span class="linenos">7</span>	Command-line options: --verbose
<span class="linenos">8</span>	
<span class="linenos">9</span>	Reading <span class="hljs-string">"Gruntfile.js"</span> Gruntfile...OK
<span class="linenos">10</span>	
<span class="linenos">11</span>	Registering Gruntfile tasks.
<span class="linenos">12</span>	Initializing config...OK
<span class="linenos">13</span>	
<span class="linenos">14</span>	Registering <span class="hljs-string">"grunt-contrib-jshint"</span> <span class="hljs-built_in">local</span> Npm module tasks.
<span class="linenos">15</span>	Reading /Users/matthewdaly/Projects/babblr/node_modules/grunt-contrib-jshint/package.json...OK
<span class="linenos">16</span>	Parsing /Users/matthewdaly/Projects/babblr/node_modules/grunt-contrib-jshint/package.json...OK
<span class="linenos">17</span>	Loading <span class="hljs-string">"jshint.js"</span> tasks...OK
<span class="linenos">18</span>	+ jshint
<span class="linenos">19</span>	
<span class="linenos">20</span>	Registering <span class="hljs-string">"grunt-coveralls"</span> <span class="hljs-built_in">local</span> Npm module tasks.
<span class="linenos">21</span>	Reading /Users/matthewdaly/Projects/babblr/node_modules/grunt-coveralls/package.json...OK
<span class="linenos">22</span>	Parsing /Users/matthewdaly/Projects/babblr/node_modules/grunt-coveralls/package.json...OK
<span class="linenos">23</span>	Loading <span class="hljs-string">"coverallsTask.js"</span> tasks...OK
<span class="linenos">24</span>	+ coveralls
<span class="linenos">25</span>	
<span class="linenos">26</span>	Registering <span class="hljs-string">"grunt-mocha-istanbul"</span> <span class="hljs-built_in">local</span> Npm module tasks.
<span class="linenos">27</span>	Reading /Users/matthewdaly/Projects/babblr/node_modules/grunt-mocha-istanbul/package.json...OK
<span class="linenos">28</span>	Parsing /Users/matthewdaly/Projects/babblr/node_modules/grunt-mocha-istanbul/package.json...OK
<span class="linenos">29</span>	Loading <span class="hljs-string">"index.js"</span> tasks...OK
<span class="linenos">30</span>	+ istanbul_check_coverage, mocha_istanbul
<span class="linenos">31</span>	Loading <span class="hljs-string">"Gruntfile.js"</span> tasks...OK
<span class="linenos">32</span>	+ <span class="hljs-built_in">test</span>
<span class="linenos">33</span>	
<span class="linenos">34</span>	Running tasks: <span class="hljs-built_in">test</span>
<span class="linenos">35</span>	
<span class="linenos">36</span>	Running <span class="hljs-string">"test"</span> task
<span class="linenos">37</span>	
<span class="linenos">38</span>	Running <span class="hljs-string">"jshint"</span> task
<span class="linenos">39</span>	
<span class="linenos">40</span>	Running <span class="hljs-string">"jshint:all"</span> (jshint) task
<span class="linenos">41</span>	Verifying property jshint.all exists <span class="hljs-keyword">in</span> config...OK
<span class="linenos">42</span>	Files: <span class="hljs-built_in">test</span>/test.js, index.js -&gt; all
<span class="linenos">43</span>	Options: force=<span class="hljs-literal">false</span>, reporterOutput=null
<span class="linenos">44</span>	OK
<span class="linenos">45</span>	&gt;&gt; <span class="hljs-number">2</span> files lint free.
<span class="linenos">46</span>	
<span class="linenos">47</span>	Running <span class="hljs-string">"mocha_istanbul:coverage"</span> (mocha_istanbul) task
<span class="linenos">48</span>	Verifying property mocha_istanbul.coverage exists <span class="hljs-keyword">in</span> config...OK
<span class="linenos">49</span>	Files: <span class="hljs-built_in">test</span>
<span class="linenos">50</span>	Options: require=[], ui=<span class="hljs-literal">false</span>, globals=[], reporter=<span class="hljs-literal">false</span>, timeout=<span class="hljs-literal">false</span>, coverage=<span class="hljs-literal">false</span>, slow=<span class="hljs-literal">false</span>, grep=<span class="hljs-literal">false</span>, dryRun=<span class="hljs-literal">false</span>, quiet=<span class="hljs-literal">false</span>, recursive=<span class="hljs-literal">false</span>, mask=<span class="hljs-string">"*.js"</span>, root=<span class="hljs-literal">false</span>, <span class="hljs-built_in">print</span>=<span class="hljs-literal">false</span>, noColors=<span class="hljs-literal">false</span>, harmony=<span class="hljs-literal">false</span>, coverageFolder=<span class="hljs-string">"coverage"</span>, reportFormats=[<span class="hljs-string">"cobertura"</span>,<span class="hljs-string">"html"</span>,<span class="hljs-string">"lcovonly"</span>], check={<span class="hljs-string">"statements"</span>:<span class="hljs-literal">false</span>,<span class="hljs-string">"lines"</span>:<span class="hljs-literal">false</span>,<span class="hljs-string">"functions"</span>:<span class="hljs-literal">false</span>,<span class="hljs-string">"branches"</span>:<span class="hljs-literal">false</span>}, excludes=<span class="hljs-literal">false</span>, mochaOptions=<span class="hljs-literal">false</span>, istanbulOptions=<span class="hljs-literal">false</span>
<span class="linenos">51</span>	&gt;&gt; Will execute: node /Users/matthewdaly/Projects/babblr/node_modules/istanbul/lib/cli.js cover --dir=/Users/matthewdaly/Projects/babblr/coverage --report=cobertura --report=html --report=lcovonly /Users/matthewdaly/Projects/babblr/node_modules/mocha/bin/_mocha -- <span class="hljs-built_in">test</span>/*.js
<span class="linenos">52</span>	express-session deprecated undefined resave option; provide resave option index.js:<span class="hljs-number">9</span>:<span class="hljs-number">1585</span>
<span class="linenos">53</span>	express-session deprecated undefined saveUninitialized option; provide saveUninitialized option index.js:<span class="hljs-number">9</span>:<span class="hljs-number">1585</span>
<span class="linenos">54</span>	Listening on port <span class="hljs-number">5000</span>
<span class="linenos">55</span>	
<span class="linenos">56</span>	
<span class="linenos">57</span>	  server
<span class="linenos">58</span>	Starting the server
<span class="linenos">59</span>	    Test the index route
<span class="linenos">60</span>	      ✓ should <span class="hljs-built_in">return</span> a page with the title Babblr (<span class="hljs-number">45</span>ms)
<span class="linenos">61</span>	    Test the login route
<span class="linenos">62</span>	      ✓ should <span class="hljs-built_in">return</span> a page with the text Please enter a handle
<span class="linenos">63</span>	    Test submitting to the login route
<span class="linenos">64</span>	      <span class="hljs-number">1</span>) should store the username <span class="hljs-keyword">in</span> the session and redirect the user to the index
<span class="linenos">65</span>	    Test sending a message
<span class="linenos">66</span>	      ✓ should <span class="hljs-built_in">return</span> <span class="hljs-string">'Message received'</span> (<span class="hljs-number">42</span>ms)
<span class="linenos">67</span>	Stopping the server
<span class="linenos">68</span>	
<span class="linenos">69</span>	
<span class="linenos">70</span>	  <span class="hljs-number">3</span> passing (<span class="hljs-number">122</span>ms)
<span class="linenos">71</span>	  <span class="hljs-number">1</span> failing
<span class="linenos">72</span>	
<span class="linenos">73</span>	  <span class="hljs-number">1</span>) server Test submitting to the login route should store the username <span class="hljs-keyword">in</span> the session and redirect the user to the index:
<span class="linenos">74</span>	     Uncaught AssertionError: expected undefined to equal <span class="hljs-string">'/'</span>
<span class="linenos">75</span>	      at Request._callback (/Users/matthewdaly/Projects/babblr/<span class="hljs-built_in">test</span>/test.js:<span class="hljs-number">61</span>:<span class="hljs-number">58</span>)
<span class="linenos">76</span>	      at Request.self.callback (/Users/matthewdaly/Projects/babblr/node_modules/request/request.js:<span class="hljs-number">373</span>:<span class="hljs-number">22</span>)
<span class="linenos">77</span>	      at Request.emit (events.js:<span class="hljs-number">98</span>:<span class="hljs-number">17</span>)
<span class="linenos">78</span>	      at Request.&lt;anonymous&gt; (/Users/matthewdaly/Projects/babblr/node_modules/request/request.js:<span class="hljs-number">1318</span>:<span class="hljs-number">14</span>)
<span class="linenos">79</span>	      at Request.emit (events.js:<span class="hljs-number">117</span>:<span class="hljs-number">20</span>)
<span class="linenos">80</span>	      at IncomingMessage.&lt;anonymous&gt; (/Users/matthewdaly/Projects/babblr/node_modules/request/request.js:<span class="hljs-number">1266</span>:<span class="hljs-number">12</span>)
<span class="linenos">81</span>	      at IncomingMessage.emit (events.js:<span class="hljs-number">117</span>:<span class="hljs-number">20</span>)
<span class="linenos">82</span>	      at _stream_readable.js:<span class="hljs-number">944</span>:<span class="hljs-number">16</span>
<span class="linenos">83</span>	      at process._tickCallback (node.js:<span class="hljs-number">442</span>:<span class="hljs-number">13</span>)
<span class="linenos">84</span>	
<span class="linenos">85</span>	
<span class="linenos">86</span>	
<span class="linenos">87</span>	=============================================================================
<span class="linenos">88</span>	Writing coverage object [/Users/matthewdaly/Projects/babblr/coverage/coverage.json]
<span class="linenos">89</span>	Writing coverage reports at [/Users/matthewdaly/Projects/babblr/coverage]
<span class="linenos">90</span>	=============================================================================
<span class="linenos">91</span>	
<span class="linenos">92</span>	=============================== Coverage summary ===============================
<span class="linenos">93</span>	Statements   : <span class="hljs-number">100</span>% ( <span class="hljs-number">45</span>/<span class="hljs-number">45</span> ), <span class="hljs-number">7</span> ignored
<span class="linenos">94</span>	Branches     : <span class="hljs-number">100</span>% ( <span class="hljs-number">8</span>/<span class="hljs-number">8</span> ), <span class="hljs-number">2</span> ignored
<span class="linenos">95</span>	Functions    : <span class="hljs-number">87.5</span>% ( <span class="hljs-number">7</span>/<span class="hljs-number">8</span> )
<span class="linenos">96</span>	Lines        : <span class="hljs-number">100</span>% ( <span class="hljs-number">45</span>/<span class="hljs-number">45</span> )
<span class="linenos">97</span>	================================================================================
<span class="linenos">98</span>	&gt;&gt;
<span class="linenos">99</span>	Warning: Task <span class="hljs-string">"mocha_istanbul:coverage"</span> failed. Use --force to continue.
<span class="linenos">100</span>	
<span class="linenos">101</span>	Aborted due to warnings.
<span class="linenos">102</span>	npm ERR! Test failed.  See above <span class="hljs-keyword">for</span> more details.
<span class="linenos">103</span>	npm ERR! not ok code <span class="hljs-number">0</span></code></pre>
<p>Now, all we need to do to make this test pass is create a view containing the form and define a route to display it. First, we’ll define our new route in <code>index.js</code>:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	<span class="hljs-comment">// Define login route</span>
<span class="linenos">2</span>	app.get(<span class="hljs-string">'/login'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(req, res)</span> </span>{
<span class="linenos">3</span>	    <span class="hljs-comment">// Render view</span>
<span class="linenos">4</span>	    res.render(<span class="hljs-string">'login'</span>);
<span class="linenos">5</span>	});</code></pre>
<p>Next, we’ll create our new template at <code>views/login.hbs</code>:</p>
<pre><code class="hljs lang-handlebars"><span class="linenos">1</span>	<span class="xml"></span><span class="hljs-expression">{{&gt; <span class="hljs-variable">header</span> }}</span><span class="xml">
<span class="linenos">2</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"container"</span>&gt;</span>
<span class="linenos">3</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"row"</span>&gt;</span>
<span class="linenos">4</span>	                <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"col-md-12"</span>&gt;</span>
<span class="linenos">5</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">form</span> <span class="hljs-attribute">action</span>=<span class="hljs-value">"/login"</span> <span class="hljs-attribute">method</span>=<span class="hljs-value">"POST"</span>&gt;</span>
<span class="linenos">6</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"form-group"</span>&gt;</span>
<span class="linenos">7</span>	                            <span class="hljs-tag">&lt;<span class="hljs-title">label</span> <span class="hljs-attribute">for</span>=<span class="hljs-value">"Username"</span>&gt;</span>Please enter a handle<span class="hljs-tag">&lt;/<span class="hljs-title">label</span>&gt;</span>
<span class="linenos">8</span>	                            <span class="hljs-tag">&lt;<span class="hljs-title">input</span> <span class="hljs-attribute">type</span>=<span class="hljs-value">"text"</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"form-control"</span> <span class="hljs-attribute">size</span>=<span class="hljs-value">"20"</span> <span class="hljs-attribute">required</span> <span class="hljs-attribute">id</span>=<span class="hljs-value">"username"</span> <span class="hljs-attribute">name</span>=<span class="hljs-value">"username"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">input</span>&gt;</span>
<span class="linenos">9</span>	                            <span class="hljs-tag">&lt;<span class="hljs-title">input</span> <span class="hljs-attribute">type</span>=<span class="hljs-value">"submit"</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"btn btn-primary form-control"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">input</span>&gt;</span>
<span class="linenos">10</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">11</span>	                    <span class="hljs-tag">&lt;/<span class="hljs-title">form</span>&gt;</span>
<span class="linenos">12</span>	                <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">13</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">14</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">15</span>	</span><span class="hljs-expression">{{&gt; <span class="hljs-variable">footer</span> }}</span><span class="xml"></span></code></pre>
<p>Let’s run our tests and make sure they pass:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ npm <span class="hljs-built_in">test</span>
<span class="linenos">2</span>	
<span class="linenos">3</span>	&gt; babblr@<span class="hljs-number">1.0</span>.<span class="hljs-number">0</span> <span class="hljs-built_in">test</span> /Users/matthewdaly/Projects/babblr
<span class="linenos">4</span>	&gt; grunt <span class="hljs-built_in">test</span> --verbose
<span class="linenos">5</span>	
<span class="linenos">6</span>	Initializing
<span class="linenos">7</span>	Command-line options: --verbose
<span class="linenos">8</span>	
<span class="linenos">9</span>	Reading <span class="hljs-string">"Gruntfile.js"</span> Gruntfile...OK
<span class="linenos">10</span>	
<span class="linenos">11</span>	Registering Gruntfile tasks.
<span class="linenos">12</span>	Initializing config...OK
<span class="linenos">13</span>	
<span class="linenos">14</span>	Registering <span class="hljs-string">"grunt-contrib-jshint"</span> <span class="hljs-built_in">local</span> Npm module tasks.
<span class="linenos">15</span>	Reading /Users/matthewdaly/Projects/babblr/node_modules/grunt-contrib-jshint/package.json...OK
<span class="linenos">16</span>	Parsing /Users/matthewdaly/Projects/babblr/node_modules/grunt-contrib-jshint/package.json...OK
<span class="linenos">17</span>	Loading <span class="hljs-string">"jshint.js"</span> tasks...OK
<span class="linenos">18</span>	+ jshint
<span class="linenos">19</span>	
<span class="linenos">20</span>	Registering <span class="hljs-string">"grunt-coveralls"</span> <span class="hljs-built_in">local</span> Npm module tasks.
<span class="linenos">21</span>	Reading /Users/matthewdaly/Projects/babblr/node_modules/grunt-coveralls/package.json...OK
<span class="linenos">22</span>	Parsing /Users/matthewdaly/Projects/babblr/node_modules/grunt-coveralls/package.json...OK
<span class="linenos">23</span>	Loading <span class="hljs-string">"coverallsTask.js"</span> tasks...OK
<span class="linenos">24</span>	+ coveralls
<span class="linenos">25</span>	
<span class="linenos">26</span>	Registering <span class="hljs-string">"grunt-mocha-istanbul"</span> <span class="hljs-built_in">local</span> Npm module tasks.
<span class="linenos">27</span>	Reading /Users/matthewdaly/Projects/babblr/node_modules/grunt-mocha-istanbul/package.json...OK
<span class="linenos">28</span>	Parsing /Users/matthewdaly/Projects/babblr/node_modules/grunt-mocha-istanbul/package.json...OK
<span class="linenos">29</span>	Loading <span class="hljs-string">"index.js"</span> tasks...OK
<span class="linenos">30</span>	+ istanbul_check_coverage, mocha_istanbul
<span class="linenos">31</span>	Loading <span class="hljs-string">"Gruntfile.js"</span> tasks...OK
<span class="linenos">32</span>	+ <span class="hljs-built_in">test</span>
<span class="linenos">33</span>	
<span class="linenos">34</span>	Running tasks: <span class="hljs-built_in">test</span>
<span class="linenos">35</span>	
<span class="linenos">36</span>	Running <span class="hljs-string">"test"</span> task
<span class="linenos">37</span>	
<span class="linenos">38</span>	Running <span class="hljs-string">"jshint"</span> task
<span class="linenos">39</span>	
<span class="linenos">40</span>	Running <span class="hljs-string">"jshint:all"</span> (jshint) task
<span class="linenos">41</span>	Verifying property jshint.all exists <span class="hljs-keyword">in</span> config...OK
<span class="linenos">42</span>	Files: <span class="hljs-built_in">test</span>/test.js, index.js -&gt; all
<span class="linenos">43</span>	Options: force=<span class="hljs-literal">false</span>, reporterOutput=null
<span class="linenos">44</span>	OK
<span class="linenos">45</span>	&gt;&gt; <span class="hljs-number">2</span> files lint free.
<span class="linenos">46</span>	
<span class="linenos">47</span>	Running <span class="hljs-string">"mocha_istanbul:coverage"</span> (mocha_istanbul) task
<span class="linenos">48</span>	Verifying property mocha_istanbul.coverage exists <span class="hljs-keyword">in</span> config...OK
<span class="linenos">49</span>	Files: <span class="hljs-built_in">test</span>
<span class="linenos">50</span>	Options: require=[], ui=<span class="hljs-literal">false</span>, globals=[], reporter=<span class="hljs-literal">false</span>, timeout=<span class="hljs-literal">false</span>, coverage=<span class="hljs-literal">false</span>, slow=<span class="hljs-literal">false</span>, grep=<span class="hljs-literal">false</span>, dryRun=<span class="hljs-literal">false</span>, quiet=<span class="hljs-literal">false</span>, recursive=<span class="hljs-literal">false</span>, mask=<span class="hljs-string">"*.js"</span>, root=<span class="hljs-literal">false</span>, <span class="hljs-built_in">print</span>=<span class="hljs-literal">false</span>, noColors=<span class="hljs-literal">false</span>, harmony=<span class="hljs-literal">false</span>, coverageFolder=<span class="hljs-string">"coverage"</span>, reportFormats=[<span class="hljs-string">"cobertura"</span>,<span class="hljs-string">"html"</span>,<span class="hljs-string">"lcovonly"</span>], check={<span class="hljs-string">"statements"</span>:<span class="hljs-literal">false</span>,<span class="hljs-string">"lines"</span>:<span class="hljs-literal">false</span>,<span class="hljs-string">"functions"</span>:<span class="hljs-literal">false</span>,<span class="hljs-string">"branches"</span>:<span class="hljs-literal">false</span>}, excludes=<span class="hljs-literal">false</span>, mochaOptions=<span class="hljs-literal">false</span>, istanbulOptions=<span class="hljs-literal">false</span>
<span class="linenos">51</span>	&gt;&gt; Will execute: node /Users/matthewdaly/Projects/babblr/node_modules/istanbul/lib/cli.js cover --dir=/Users/matthewdaly/Projects/babblr/coverage --report=cobertura --report=html --report=lcovonly /Users/matthewdaly/Projects/babblr/node_modules/mocha/bin/_mocha -- <span class="hljs-built_in">test</span>/*.js
<span class="linenos">52</span>	express-session deprecated undefined resave option; provide resave option index.js:<span class="hljs-number">9</span>:<span class="hljs-number">1585</span>
<span class="linenos">53</span>	express-session deprecated undefined saveUninitialized option; provide saveUninitialized option index.js:<span class="hljs-number">9</span>:<span class="hljs-number">1585</span>
<span class="linenos">54</span>	Listening on port <span class="hljs-number">5000</span>
<span class="linenos">55</span>	
<span class="linenos">56</span>	
<span class="linenos">57</span>	  server
<span class="linenos">58</span>	Starting the server
<span class="linenos">59</span>	    Test the index route
<span class="linenos">60</span>	      ✓ should <span class="hljs-built_in">return</span> a page with the title Babblr (<span class="hljs-number">64</span>ms)
<span class="linenos">61</span>	    Test the login route
<span class="linenos">62</span>	      ✓ should <span class="hljs-built_in">return</span> a page with the text Please enter a handle
<span class="linenos">63</span>	    Test sending a message
<span class="linenos">64</span>	      ✓ should <span class="hljs-built_in">return</span> <span class="hljs-string">'Message received'</span> (<span class="hljs-number">78</span>ms)
<span class="linenos">65</span>	Stopping the server
<span class="linenos">66</span>	
<span class="linenos">67</span>	
<span class="linenos">68</span>	  <span class="hljs-number">3</span> passing (<span class="hljs-number">179</span>ms)
<span class="linenos">69</span>	
<span class="linenos">70</span>	=============================================================================
<span class="linenos">71</span>	Writing coverage object [/Users/matthewdaly/Projects/babblr/coverage/coverage.json]
<span class="linenos">72</span>	Writing coverage reports at [/Users/matthewdaly/Projects/babblr/coverage]
<span class="linenos">73</span>	=============================================================================
<span class="linenos">74</span>	
<span class="linenos">75</span>	=============================== Coverage summary ===============================
<span class="linenos">76</span>	Statements   : <span class="hljs-number">100</span>% ( <span class="hljs-number">45</span>/<span class="hljs-number">45</span> ), <span class="hljs-number">7</span> ignored
<span class="linenos">77</span>	Branches     : <span class="hljs-number">100</span>% ( <span class="hljs-number">8</span>/<span class="hljs-number">8</span> ), <span class="hljs-number">2</span> ignored
<span class="linenos">78</span>	Functions    : <span class="hljs-number">87.5</span>% ( <span class="hljs-number">7</span>/<span class="hljs-number">8</span> )
<span class="linenos">79</span>	Lines        : <span class="hljs-number">100</span>% ( <span class="hljs-number">45</span>/<span class="hljs-number">45</span> )
<span class="linenos">80</span>	================================================================================
<span class="linenos">81</span>	&gt;&gt; Done. Check coverage folder.
<span class="linenos">82</span>	
<span class="linenos">83</span>	Running <span class="hljs-string">"coveralls"</span> task
<span class="linenos">84</span>	
<span class="linenos">85</span>	Running <span class="hljs-string">"coveralls:app"</span> (coveralls) task
<span class="linenos">86</span>	Verifying property coveralls.app exists <span class="hljs-keyword">in</span> config...OK
<span class="linenos">87</span>	Files: coverage/lcov.info
<span class="linenos">88</span>	Options: src=<span class="hljs-string">"coverage/lcov.info"</span>, force=<span class="hljs-literal">false</span>
<span class="linenos">89</span>	Submitting file to coveralls.io: coverage/lcov.info
<span class="linenos">90</span>	&gt;&gt; Failed to submit <span class="hljs-string">'coverage/lcov.info'</span> to coveralls: Bad response: <span class="hljs-number">422</span> {<span class="hljs-string">"message"</span>:<span class="hljs-string">"Couldn't find a repository matching this job."</span>,<span class="hljs-string">"error"</span>:<span class="hljs-literal">true</span>}
<span class="linenos">91</span>	&gt;&gt; Failed to submit coverage results to coveralls
<span class="linenos">92</span>	Warning: Task <span class="hljs-string">"coveralls:app"</span> failed. Use --force to continue.
<span class="linenos">93</span>	
<span class="linenos">94</span>	Aborted due to warnings.
<span class="linenos">95</span>	npm ERR! Test failed.  See above <span class="hljs-keyword">for</span> more details.
<span class="linenos">96</span>	npm ERR! not ok code <span class="hljs-number">0</span></code></pre>
<p>Next, we need to process the submitted form, set the session, and redirect the user back to the index. First, let’s add another test:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	    <span class="hljs-comment">// Test submitting to the login route</span>
<span class="linenos">2</span>	    describe(<span class="hljs-string">'Test submitting to the login route'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">()</span> </span>{
<span class="linenos">3</span>	        it(<span class="hljs-string">'should store the username in the session and redirect the user to the index'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(done)</span> </span>{
<span class="linenos">4</span>	            request.post({ url: <span class="hljs-string">'http://localhost:5000/login'</span>,
<span class="linenos">5</span>	                form:{username: <span class="hljs-string">'bobsmith'</span>},
<span class="linenos">6</span>	                followRedirect: <span class="hljs-literal">false</span>},
<span class="linenos">7</span>	                <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(error, response, body)</span> </span>{
<span class="linenos">8</span>	                    expect(response.headers.location).to.equal(<span class="hljs-string">'http://localhost:5000'</span>);
<span class="linenos">9</span>	                    expect(response.statusCode).to.equal(<span class="hljs-number">301</span>);
<span class="linenos">10</span>	                    done();
<span class="linenos">11</span>	            });
<span class="linenos">12</span>	        });
<span class="linenos">13</span>	    });</code></pre>
<p>This test submits the username, and makes sure that the response received is a 301 redirect to the index route. Let’s check to make sure it fails:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ npm <span class="hljs-built_in">test</span>
<span class="linenos">2</span>	
<span class="linenos">3</span>	&gt; babblr@<span class="hljs-number">1.0</span>.<span class="hljs-number">0</span> <span class="hljs-built_in">test</span> /Users/matthewdaly/Projects/babblr
<span class="linenos">4</span>	&gt; grunt <span class="hljs-built_in">test</span> --verbose
<span class="linenos">5</span>	
<span class="linenos">6</span>	Initializing
<span class="linenos">7</span>	Command-line options: --verbose
<span class="linenos">8</span>	
<span class="linenos">9</span>	Reading <span class="hljs-string">"Gruntfile.js"</span> Gruntfile...OK
<span class="linenos">10</span>	
<span class="linenos">11</span>	Registering Gruntfile tasks.
<span class="linenos">12</span>	Initializing config...OK
<span class="linenos">13</span>	
<span class="linenos">14</span>	Registering <span class="hljs-string">"grunt-contrib-jshint"</span> <span class="hljs-built_in">local</span> Npm module tasks.
<span class="linenos">15</span>	Reading /Users/matthewdaly/Projects/babblr/node_modules/grunt-contrib-jshint/package.json...OK
<span class="linenos">16</span>	Parsing /Users/matthewdaly/Projects/babblr/node_modules/grunt-contrib-jshint/package.json...OK
<span class="linenos">17</span>	Loading <span class="hljs-string">"jshint.js"</span> tasks...OK
<span class="linenos">18</span>	+ jshint
<span class="linenos">19</span>	
<span class="linenos">20</span>	Registering <span class="hljs-string">"grunt-coveralls"</span> <span class="hljs-built_in">local</span> Npm module tasks.
<span class="linenos">21</span>	Reading /Users/matthewdaly/Projects/babblr/node_modules/grunt-coveralls/package.json...OK
<span class="linenos">22</span>	Parsing /Users/matthewdaly/Projects/babblr/node_modules/grunt-coveralls/package.json...OK
<span class="linenos">23</span>	Loading <span class="hljs-string">"coverallsTask.js"</span> tasks...OK
<span class="linenos">24</span>	+ coveralls
<span class="linenos">25</span>	
<span class="linenos">26</span>	Registering <span class="hljs-string">"grunt-mocha-istanbul"</span> <span class="hljs-built_in">local</span> Npm module tasks.
<span class="linenos">27</span>	Reading /Users/matthewdaly/Projects/babblr/node_modules/grunt-mocha-istanbul/package.json...OK
<span class="linenos">28</span>	Parsing /Users/matthewdaly/Projects/babblr/node_modules/grunt-mocha-istanbul/package.json...OK
<span class="linenos">29</span>	Loading <span class="hljs-string">"index.js"</span> tasks...OK
<span class="linenos">30</span>	+ istanbul_check_coverage, mocha_istanbul
<span class="linenos">31</span>	Loading <span class="hljs-string">"Gruntfile.js"</span> tasks...OK
<span class="linenos">32</span>	+ <span class="hljs-built_in">test</span>
<span class="linenos">33</span>	
<span class="linenos">34</span>	Running tasks: <span class="hljs-built_in">test</span>
<span class="linenos">35</span>	
<span class="linenos">36</span>	Running <span class="hljs-string">"test"</span> task
<span class="linenos">37</span>	
<span class="linenos">38</span>	Running <span class="hljs-string">"jshint"</span> task
<span class="linenos">39</span>	
<span class="linenos">40</span>	Running <span class="hljs-string">"jshint:all"</span> (jshint) task
<span class="linenos">41</span>	Verifying property jshint.all exists <span class="hljs-keyword">in</span> config...OK
<span class="linenos">42</span>	Files: <span class="hljs-built_in">test</span>/test.js, index.js -&gt; all
<span class="linenos">43</span>	Options: force=<span class="hljs-literal">false</span>, reporterOutput=null
<span class="linenos">44</span>	OK
<span class="linenos">45</span>	&gt;&gt; <span class="hljs-number">2</span> files lint free.
<span class="linenos">46</span>	
<span class="linenos">47</span>	Running <span class="hljs-string">"mocha_istanbul:coverage"</span> (mocha_istanbul) task
<span class="linenos">48</span>	Verifying property mocha_istanbul.coverage exists <span class="hljs-keyword">in</span> config...OK
<span class="linenos">49</span>	Files: <span class="hljs-built_in">test</span>
<span class="linenos">50</span>	Options: require=[], ui=<span class="hljs-literal">false</span>, globals=[], reporter=<span class="hljs-literal">false</span>, timeout=<span class="hljs-literal">false</span>, coverage=<span class="hljs-literal">false</span>, slow=<span class="hljs-literal">false</span>, grep=<span class="hljs-literal">false</span>, dryRun=<span class="hljs-literal">false</span>, quiet=<span class="hljs-literal">false</span>, recursive=<span class="hljs-literal">false</span>, mask=<span class="hljs-string">"*.js"</span>, root=<span class="hljs-literal">false</span>, <span class="hljs-built_in">print</span>=<span class="hljs-literal">false</span>, noColors=<span class="hljs-literal">false</span>, harmony=<span class="hljs-literal">false</span>, coverageFolder=<span class="hljs-string">"coverage"</span>, reportFormats=[<span class="hljs-string">"cobertura"</span>,<span class="hljs-string">"html"</span>,<span class="hljs-string">"lcovonly"</span>], check={<span class="hljs-string">"statements"</span>:<span class="hljs-literal">false</span>,<span class="hljs-string">"lines"</span>:<span class="hljs-literal">false</span>,<span class="hljs-string">"functions"</span>:<span class="hljs-literal">false</span>,<span class="hljs-string">"branches"</span>:<span class="hljs-literal">false</span>}, excludes=<span class="hljs-literal">false</span>, mochaOptions=<span class="hljs-literal">false</span>, istanbulOptions=<span class="hljs-literal">false</span>
<span class="linenos">51</span>	&gt;&gt; Will execute: node /Users/matthewdaly/Projects/babblr/node_modules/istanbul/lib/cli.js cover --dir=/Users/matthewdaly/Projects/babblr/coverage --report=cobertura --report=html --report=lcovonly /Users/matthewdaly/Projects/babblr/node_modules/mocha/bin/_mocha -- <span class="hljs-built_in">test</span>/*.js
<span class="linenos">52</span>	express-session deprecated undefined resave option; provide resave option index.js:<span class="hljs-number">9</span>:<span class="hljs-number">1585</span>
<span class="linenos">53</span>	express-session deprecated undefined saveUninitialized option; provide saveUninitialized option index.js:<span class="hljs-number">9</span>:<span class="hljs-number">1585</span>
<span class="linenos">54</span>	Listening on port <span class="hljs-number">5000</span>
<span class="linenos">55</span>	
<span class="linenos">56</span>	
<span class="linenos">57</span>	  server
<span class="linenos">58</span>	Starting the server
<span class="linenos">59</span>	    Test the index route
<span class="linenos">60</span>	      ✓ should <span class="hljs-built_in">return</span> a page with the title Babblr (<span class="hljs-number">476</span>ms)
<span class="linenos">61</span>	    Test the login route
<span class="linenos">62</span>	      ✓ should <span class="hljs-built_in">return</span> a page with the text Please enter a handle
<span class="linenos">63</span>	    Test submitting to the login route
<span class="linenos">64</span>	      <span class="hljs-number">1</span>) should store the username <span class="hljs-keyword">in</span> the session and redirect the user to the index
<span class="linenos">65</span>	    Test sending a message
<span class="linenos">66</span>	      ✓ should <span class="hljs-built_in">return</span> <span class="hljs-string">'Message received'</span> (<span class="hljs-number">42</span>ms)
<span class="linenos">67</span>	Stopping the server
<span class="linenos">68</span>	
<span class="linenos">69</span>	
<span class="linenos">70</span>	  <span class="hljs-number">3</span> passing (<span class="hljs-number">557</span>ms)
<span class="linenos">71</span>	  <span class="hljs-number">1</span> failing
<span class="linenos">72</span>	
<span class="linenos">73</span>	  <span class="hljs-number">1</span>) server Test submitting to the login route should store the username <span class="hljs-keyword">in</span> the session and redirect the user to the index:
<span class="linenos">74</span>	     Uncaught AssertionError: expected undefined to equal <span class="hljs-string">'http://localhost:5000'</span>
<span class="linenos">75</span>	      at Request._callback (/Users/matthewdaly/Projects/babblr/<span class="hljs-built_in">test</span>/test.js:<span class="hljs-number">61</span>:<span class="hljs-number">58</span>)
<span class="linenos">76</span>	      at Request.self.callback (/Users/matthewdaly/Projects/babblr/node_modules/request/request.js:<span class="hljs-number">373</span>:<span class="hljs-number">22</span>)
<span class="linenos">77</span>	      at Request.emit (events.js:<span class="hljs-number">98</span>:<span class="hljs-number">17</span>)
<span class="linenos">78</span>	      at Request.&lt;anonymous&gt; (/Users/matthewdaly/Projects/babblr/node_modules/request/request.js:<span class="hljs-number">1318</span>:<span class="hljs-number">14</span>)
<span class="linenos">79</span>	      at Request.emit (events.js:<span class="hljs-number">117</span>:<span class="hljs-number">20</span>)
<span class="linenos">80</span>	      at IncomingMessage.&lt;anonymous&gt; (/Users/matthewdaly/Projects/babblr/node_modules/request/request.js:<span class="hljs-number">1266</span>:<span class="hljs-number">12</span>)
<span class="linenos">81</span>	      at IncomingMessage.emit (events.js:<span class="hljs-number">117</span>:<span class="hljs-number">20</span>)
<span class="linenos">82</span>	      at _stream_readable.js:<span class="hljs-number">944</span>:<span class="hljs-number">16</span>
<span class="linenos">83</span>	      at process._tickCallback (node.js:<span class="hljs-number">442</span>:<span class="hljs-number">13</span>)
<span class="linenos">84</span>	
<span class="linenos">85</span>	
<span class="linenos">86</span>	
<span class="linenos">87</span>	=============================================================================
<span class="linenos">88</span>	Writing coverage object [/Users/matthewdaly/Projects/babblr/coverage/coverage.json]
<span class="linenos">89</span>	Writing coverage reports at [/Users/matthewdaly/Projects/babblr/coverage]
<span class="linenos">90</span>	=============================================================================
<span class="linenos">91</span>	
<span class="linenos">92</span>	=============================== Coverage summary ===============================
<span class="linenos">93</span>	Statements   : <span class="hljs-number">100</span>% ( <span class="hljs-number">45</span>/<span class="hljs-number">45</span> ), <span class="hljs-number">7</span> ignored
<span class="linenos">94</span>	Branches     : <span class="hljs-number">100</span>% ( <span class="hljs-number">8</span>/<span class="hljs-number">8</span> ), <span class="hljs-number">2</span> ignored
<span class="linenos">95</span>	Functions    : <span class="hljs-number">87.5</span>% ( <span class="hljs-number">7</span>/<span class="hljs-number">8</span> )
<span class="linenos">96</span>	Lines        : <span class="hljs-number">100</span>% ( <span class="hljs-number">45</span>/<span class="hljs-number">45</span> )
<span class="linenos">97</span>	================================================================================
<span class="linenos">98</span>	&gt;&gt;
<span class="linenos">99</span>	Warning: Task <span class="hljs-string">"mocha_istanbul:coverage"</span> failed. Use --force to continue.
<span class="linenos">100</span>	
<span class="linenos">101</span>	Aborted due to warnings.
<span class="linenos">102</span>	npm ERR! Test failed.  See above <span class="hljs-keyword">for</span> more details.
<span class="linenos">103</span>	npm ERR! not ok code <span class="hljs-number">0</span></code></pre>
<p>Now, in order to process <code>POST</code> data we’ll need to use <code>body-parser</code>. Amend the top of <code>index.js</code> to look like this::</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	<span class="hljs-comment">/*jslint node: true */</span>
<span class="linenos">2</span>	<span class="hljs-pi">'use strict'</span>;
<span class="linenos">3</span>	
<span class="linenos">4</span>	<span class="hljs-comment">// Declare variables used</span>
<span class="linenos">5</span>	<span class="hljs-keyword">var</span> app, base_url, bodyParser, client, express, hbs, io, port, RedisStore, rtg, session, subscribe;
<span class="linenos">6</span>	
<span class="linenos">7</span>	<span class="hljs-comment">// Define values</span>
<span class="linenos">8</span>	express = <span class="hljs-built_in">require</span>(<span class="hljs-string">'express'</span>);
<span class="linenos">9</span>	app = express();
<span class="linenos">10</span>	bodyParser = <span class="hljs-built_in">require</span>(<span class="hljs-string">'body-parser'</span>);
<span class="linenos">11</span>	port = process.env.PORT || <span class="hljs-number">5000</span>;
<span class="linenos">12</span>	base_url = process.env.BASE_URL || <span class="hljs-string">'http://localhost:5000'</span>;
<span class="linenos">13</span>	hbs = <span class="hljs-built_in">require</span>(<span class="hljs-string">'hbs'</span>);
<span class="linenos">14</span>	session = <span class="hljs-built_in">require</span>(<span class="hljs-string">'express-session'</span>);
<span class="linenos">15</span>	RedisStore = <span class="hljs-built_in">require</span>(<span class="hljs-string">'connect-redis'</span>)(session);
<span class="linenos">16</span>	
<span class="linenos">17</span>	<span class="hljs-comment">// Set up connection to Redis</span>
<span class="linenos">18</span>	<span class="hljs-comment">/* istanbul ignore if */</span>
<span class="linenos">19</span>	<span class="hljs-keyword">if</span> (process.env.REDISTOGO_URL) {
<span class="linenos">20</span>	    rtg  = <span class="hljs-built_in">require</span>(<span class="hljs-string">'url'</span>).parse(process.env.REDISTOGO_URL);
<span class="linenos">21</span>	    client = <span class="hljs-built_in">require</span>(<span class="hljs-string">'redis'</span>).createClient(rtg.port, rtg.hostname);
<span class="linenos">22</span>	    subscribe = <span class="hljs-built_in">require</span>(<span class="hljs-string">'redis'</span>).createClient(rtg.port, rtg.hostname);
<span class="linenos">23</span>	    client.auth(rtg.auth.split(<span class="hljs-string">':'</span>)[<span class="hljs-number">1</span>]);
<span class="linenos">24</span>	    subscribe.auth(rtg.auth.split(<span class="hljs-string">':'</span>)[<span class="hljs-number">1</span>]);
<span class="linenos">25</span>	} <span class="hljs-keyword">else</span> {
<span class="linenos">26</span>	    client = <span class="hljs-built_in">require</span>(<span class="hljs-string">'redis'</span>).createClient();
<span class="linenos">27</span>	    subscribe = <span class="hljs-built_in">require</span>(<span class="hljs-string">'redis'</span>).createClient();
<span class="linenos">28</span>	}
<span class="linenos">29</span>	
<span class="linenos">30</span>	<span class="hljs-comment">// Set up session</span>
<span class="linenos">31</span>	app.use(session({
<span class="linenos">32</span>	    store: <span class="hljs-keyword">new</span> RedisStore({
<span class="linenos">33</span>	        client: client
<span class="linenos">34</span>	    }),
<span class="linenos">35</span>	    secret: <span class="hljs-string">'blibble'</span>
<span class="linenos">36</span>	}));
<span class="linenos">37</span>	
<span class="linenos">38</span>	<span class="hljs-comment">// Set up templating</span>
<span class="linenos">39</span>	app.set(<span class="hljs-string">'views'</span>, __dirname + <span class="hljs-string">'/views'</span>);
<span class="linenos">40</span>	app.set(<span class="hljs-string">'view engine'</span>, <span class="hljs-string">"hbs"</span>);
<span class="linenos">41</span>	app.engine(<span class="hljs-string">'hbs'</span>, <span class="hljs-built_in">require</span>(<span class="hljs-string">'hbs'</span>).__express);
<span class="linenos">42</span>	
<span class="linenos">43</span>	<span class="hljs-comment">// Register partials</span>
<span class="linenos">44</span>	hbs.registerPartials(__dirname + <span class="hljs-string">'/views/partials'</span>);
<span class="linenos">45</span>	
<span class="linenos">46</span>	<span class="hljs-comment">// Set URL</span>
<span class="linenos">47</span>	app.set(<span class="hljs-string">'base_url'</span>, base_url);
<span class="linenos">48</span>	
<span class="linenos">49</span>	<span class="hljs-comment">// Handle POST data</span>
<span class="linenos">50</span>	app.use(bodyParser.json());
<span class="linenos">51</span>	app.use(bodyParser.urlencoded({
<span class="linenos">52</span>	      extended: <span class="hljs-literal">true</span>
<span class="linenos">53</span>	}));</code></pre>
<p>Next, we define a <code>POST</code> route to handle the username input:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	<span class="hljs-comment">// Process login</span>
<span class="linenos">2</span>	app.post(<span class="hljs-string">'/login'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(req, res)</span> </span>{
<span class="linenos">3</span>	    <span class="hljs-comment">// Get username</span>
<span class="linenos">4</span>	    <span class="hljs-keyword">var</span> username = req.body.username;
<span class="linenos">5</span>	
<span class="linenos">6</span>	    <span class="hljs-comment">// If username length is zero, reload the page</span>
<span class="linenos">7</span>	    <span class="hljs-keyword">if</span> (username.length === <span class="hljs-number">0</span>) {
<span class="linenos">8</span>	        res.render(<span class="hljs-string">'login'</span>);
<span class="linenos">9</span>	    } <span class="hljs-keyword">else</span> {
<span class="linenos">10</span>	        <span class="hljs-comment">// Store username in session and redirect to index</span>
<span class="linenos">11</span>	        req.session.username = username;
<span class="linenos">12</span>	        res.redirect(<span class="hljs-string">'/'</span>);
<span class="linenos">13</span>	    }
<span class="linenos">14</span>	});</code></pre>
<p>This should be fairly straightforward. This route accepts a username parameter. If this parameter is not present, the user will see the login form again. Otherwise, they are redirected back to the index.</p>
<p>Now, if you check <code>coverage/index.html</code> after running the tests again, you’ll notice that there’s a gap in our coverage for the scenario when a user submits an empty username. Let’s fix that - add the following test to <code>test/test.js</code>:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	    <span class="hljs-comment">// Test empty login</span>
<span class="linenos">2</span>	    describe(<span class="hljs-string">'Test empty login'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">()</span> </span>{
<span class="linenos">3</span>	        it(<span class="hljs-string">'should show the login form'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(done)</span> </span>{
<span class="linenos">4</span>	            request.post({ url: <span class="hljs-string">'http://localhost:5000/login'</span>,
<span class="linenos">5</span>	                form:{username: <span class="hljs-string">''</span>},
<span class="linenos">6</span>	                followRedirect: <span class="hljs-literal">false</span>},
<span class="linenos">7</span>	                <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(error, response, body)</span> </span>{
<span class="linenos">8</span>	                    expect(response.statusCode).to.equal(<span class="hljs-number">200</span>);
<span class="linenos">9</span>	                    expect(body).to.include(<span class="hljs-string">'Please enter a handle'</span>);
<span class="linenos">10</span>	                    done();
<span class="linenos">11</span>	            });
<span class="linenos">12</span>	        });
<span class="linenos">13</span>	    });</code></pre>
<p>Let’s run our tests again:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ npm <span class="hljs-built_in">test</span>
<span class="linenos">2</span>	
<span class="linenos">3</span>	&gt; babblr@<span class="hljs-number">1.0</span>.<span class="hljs-number">0</span> <span class="hljs-built_in">test</span> /Users/matthewdaly/Projects/babblr
<span class="linenos">4</span>	&gt; grunt <span class="hljs-built_in">test</span> --verbose
<span class="linenos">5</span>	
<span class="linenos">6</span>	Initializing
<span class="linenos">7</span>	Command-line options: --verbose
<span class="linenos">8</span>	
<span class="linenos">9</span>	Reading <span class="hljs-string">"Gruntfile.js"</span> Gruntfile...OK
<span class="linenos">10</span>	
<span class="linenos">11</span>	Registering Gruntfile tasks.
<span class="linenos">12</span>	Initializing config...OK
<span class="linenos">13</span>	
<span class="linenos">14</span>	Registering <span class="hljs-string">"grunt-contrib-jshint"</span> <span class="hljs-built_in">local</span> Npm module tasks.
<span class="linenos">15</span>	Reading /Users/matthewdaly/Projects/babblr/node_modules/grunt-contrib-jshint/package.json...OK
<span class="linenos">16</span>	Parsing /Users/matthewdaly/Projects/babblr/node_modules/grunt-contrib-jshint/package.json...OK
<span class="linenos">17</span>	Loading <span class="hljs-string">"jshint.js"</span> tasks...OK
<span class="linenos">18</span>	+ jshint
<span class="linenos">19</span>	
<span class="linenos">20</span>	Registering <span class="hljs-string">"grunt-coveralls"</span> <span class="hljs-built_in">local</span> Npm module tasks.
<span class="linenos">21</span>	Reading /Users/matthewdaly/Projects/babblr/node_modules/grunt-coveralls/package.json...OK
<span class="linenos">22</span>	Parsing /Users/matthewdaly/Projects/babblr/node_modules/grunt-coveralls/package.json...OK
<span class="linenos">23</span>	Loading <span class="hljs-string">"coverallsTask.js"</span> tasks...OK
<span class="linenos">24</span>	+ coveralls
<span class="linenos">25</span>	
<span class="linenos">26</span>	Registering <span class="hljs-string">"grunt-mocha-istanbul"</span> <span class="hljs-built_in">local</span> Npm module tasks.
<span class="linenos">27</span>	Reading /Users/matthewdaly/Projects/babblr/node_modules/grunt-mocha-istanbul/package.json...OK
<span class="linenos">28</span>	Parsing /Users/matthewdaly/Projects/babblr/node_modules/grunt-mocha-istanbul/package.json...OK
<span class="linenos">29</span>	Loading <span class="hljs-string">"index.js"</span> tasks...OK
<span class="linenos">30</span>	+ istanbul_check_coverage, mocha_istanbul
<span class="linenos">31</span>	Loading <span class="hljs-string">"Gruntfile.js"</span> tasks...OK
<span class="linenos">32</span>	+ <span class="hljs-built_in">test</span>
<span class="linenos">33</span>	
<span class="linenos">34</span>	Running tasks: <span class="hljs-built_in">test</span>
<span class="linenos">35</span>	
<span class="linenos">36</span>	Running <span class="hljs-string">"test"</span> task
<span class="linenos">37</span>	
<span class="linenos">38</span>	Running <span class="hljs-string">"jshint"</span> task
<span class="linenos">39</span>	
<span class="linenos">40</span>	Running <span class="hljs-string">"jshint:all"</span> (jshint) task
<span class="linenos">41</span>	Verifying property jshint.all exists <span class="hljs-keyword">in</span> config...OK
<span class="linenos">42</span>	Files: <span class="hljs-built_in">test</span>/test.js, index.js -&gt; all
<span class="linenos">43</span>	Options: force=<span class="hljs-literal">false</span>, reporterOutput=null
<span class="linenos">44</span>	OK
<span class="linenos">45</span>	&gt;&gt; <span class="hljs-number">2</span> files lint free.
<span class="linenos">46</span>	
<span class="linenos">47</span>	Running <span class="hljs-string">"mocha_istanbul:coverage"</span> (mocha_istanbul) task
<span class="linenos">48</span>	Verifying property mocha_istanbul.coverage exists <span class="hljs-keyword">in</span> config...OK
<span class="linenos">49</span>	Files: <span class="hljs-built_in">test</span>
<span class="linenos">50</span>	Options: require=[], ui=<span class="hljs-literal">false</span>, globals=[], reporter=<span class="hljs-literal">false</span>, timeout=<span class="hljs-literal">false</span>, coverage=<span class="hljs-literal">false</span>, slow=<span class="hljs-literal">false</span>, grep=<span class="hljs-literal">false</span>, dryRun=<span class="hljs-literal">false</span>, quiet=<span class="hljs-literal">false</span>, recursive=<span class="hljs-literal">false</span>, mask=<span class="hljs-string">"*.js"</span>, root=<span class="hljs-literal">false</span>, <span class="hljs-built_in">print</span>=<span class="hljs-literal">false</span>, noColors=<span class="hljs-literal">false</span>, harmony=<span class="hljs-literal">false</span>, coverageFolder=<span class="hljs-string">"coverage"</span>, reportFormats=[<span class="hljs-string">"cobertura"</span>,<span class="hljs-string">"html"</span>,<span class="hljs-string">"lcovonly"</span>], check={<span class="hljs-string">"statements"</span>:<span class="hljs-literal">false</span>,<span class="hljs-string">"lines"</span>:<span class="hljs-literal">false</span>,<span class="hljs-string">"functions"</span>:<span class="hljs-literal">false</span>,<span class="hljs-string">"branches"</span>:<span class="hljs-literal">false</span>}, excludes=<span class="hljs-literal">false</span>, mochaOptions=<span class="hljs-literal">false</span>, istanbulOptions=<span class="hljs-literal">false</span>
<span class="linenos">51</span>	&gt;&gt; Will execute: node /Users/matthewdaly/Projects/babblr/node_modules/istanbul/lib/cli.js cover --dir=/Users/matthewdaly/Projects/babblr/coverage --report=cobertura --report=html --report=lcovonly /Users/matthewdaly/Projects/babblr/node_modules/mocha/bin/_mocha -- <span class="hljs-built_in">test</span>/*.js
<span class="linenos">52</span>	express-session deprecated undefined resave option; provide resave option index.js:<span class="hljs-number">9</span>:<span class="hljs-number">1669</span>
<span class="linenos">53</span>	express-session deprecated undefined saveUninitialized option; provide saveUninitialized option index.js:<span class="hljs-number">9</span>:<span class="hljs-number">1669</span>
<span class="linenos">54</span>	Listening on port <span class="hljs-number">5000</span>
<span class="linenos">55</span>	
<span class="linenos">56</span>	
<span class="linenos">57</span>	  server
<span class="linenos">58</span>	Starting the server
<span class="linenos">59</span>	    Test the index route
<span class="linenos">60</span>	      ✓ should <span class="hljs-built_in">return</span> a page with the title Babblr (<span class="hljs-number">44</span>ms)
<span class="linenos">61</span>	    Test the login route
<span class="linenos">62</span>	      ✓ should <span class="hljs-built_in">return</span> a page with the text Please enter a handle
<span class="linenos">63</span>	    Test submitting to the login route
<span class="linenos">64</span>	      ✓ should store the username <span class="hljs-keyword">in</span> the session and redirect the user to the index
<span class="linenos">65</span>	    Test empty login
<span class="linenos">66</span>	      ✓ should show the login form
<span class="linenos">67</span>	    Test sending a message
<span class="linenos">68</span>	      ✓ should <span class="hljs-built_in">return</span> <span class="hljs-string">'Message received'</span> (<span class="hljs-number">41</span>ms)
<span class="linenos">69</span>	Stopping the server
<span class="linenos">70</span>	
<span class="linenos">71</span>	
<span class="linenos">72</span>	  <span class="hljs-number">5</span> passing (<span class="hljs-number">145</span>ms)
<span class="linenos">73</span>	
<span class="linenos">74</span>	=============================================================================
<span class="linenos">75</span>	Writing coverage object [/Users/matthewdaly/Projects/babblr/coverage/coverage.json]
<span class="linenos">76</span>	Writing coverage reports at [/Users/matthewdaly/Projects/babblr/coverage]
<span class="linenos">77</span>	=============================================================================
<span class="linenos">78</span>	
<span class="linenos">79</span>	=============================== Coverage summary ===============================
<span class="linenos">80</span>	Statements   : <span class="hljs-number">100</span>% ( <span class="hljs-number">54</span>/<span class="hljs-number">54</span> ), <span class="hljs-number">7</span> ignored
<span class="linenos">81</span>	Branches     : <span class="hljs-number">100</span>% ( <span class="hljs-number">10</span>/<span class="hljs-number">10</span> ), <span class="hljs-number">2</span> ignored
<span class="linenos">82</span>	Functions    : <span class="hljs-number">88.89</span>% ( <span class="hljs-number">8</span>/<span class="hljs-number">9</span> )
<span class="linenos">83</span>	Lines        : <span class="hljs-number">100</span>% ( <span class="hljs-number">54</span>/<span class="hljs-number">54</span> )
<span class="linenos">84</span>	================================================================================
<span class="linenos">85</span>	&gt;&gt; Done. Check coverage folder.
<span class="linenos">86</span>	
<span class="linenos">87</span>	Running <span class="hljs-string">"coveralls"</span> task
<span class="linenos">88</span>	
<span class="linenos">89</span>	Running <span class="hljs-string">"coveralls:app"</span> (coveralls) task
<span class="linenos">90</span>	Verifying property coveralls.app exists <span class="hljs-keyword">in</span> config...OK
<span class="linenos">91</span>	Files: coverage/lcov.info
<span class="linenos">92</span>	Options: src=<span class="hljs-string">"coverage/lcov.info"</span>, force=<span class="hljs-literal">false</span>
<span class="linenos">93</span>	Submitting file to coveralls.io: coverage/lcov.info
<span class="linenos">94</span>	&gt;&gt; Failed to submit <span class="hljs-string">'coverage/lcov.info'</span> to coveralls: Bad response: <span class="hljs-number">422</span> {<span class="hljs-string">"message"</span>:<span class="hljs-string">"Couldn't find a repository matching this job."</span>,<span class="hljs-string">"error"</span>:<span class="hljs-literal">true</span>}
<span class="linenos">95</span>	&gt;&gt; Failed to submit coverage results to coveralls
<span class="linenos">96</span>	Warning: Task <span class="hljs-string">"coveralls:app"</span> failed. Use --force to continue.
<span class="linenos">97</span>	
<span class="linenos">98</span>	Aborted due to warnings.
<span class="linenos">99</span>	npm ERR! Test failed.  See above <span class="hljs-keyword">for</span> more details.
<span class="linenos">100</span>	npm ERR! not ok code <span class="hljs-number">0</span></code></pre>
<p>Our test now passes (bar, of course, Coveralls failing). Our next step is to actually do something with the session. Now, the <code>request</code> module we use in our test requires a third-party module called <code>tough-cookie</code> to work with cookies, so we need to install that:</p>
<pre><code class="hljs lang-bash singleline">$ npm install tough-cookie --save-dev</code></pre>
<p>Next, amend the login test as follows:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	    <span class="hljs-comment">// Test submitting to the login route</span>
<span class="linenos">2</span>	    describe(<span class="hljs-string">'Test submitting to the login route'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">()</span> </span>{
<span class="linenos">3</span>	        it(<span class="hljs-string">'should store the username in the session and redirect the user to the index'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(done)</span> </span>{
<span class="linenos">4</span>	            request.post({ url: <span class="hljs-string">'http://localhost:5000/login'</span>,
<span class="linenos">5</span>	                form:{username: <span class="hljs-string">'bobsmith'</span>},
<span class="linenos">6</span>	                jar: <span class="hljs-literal">true</span>,
<span class="linenos">7</span>	                followRedirect: <span class="hljs-literal">false</span>},
<span class="linenos">8</span>	                <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(error, response, body)</span> </span>{
<span class="linenos">9</span>	                    expect(response.headers.location).to.equal(<span class="hljs-string">'/'</span>);
<span class="linenos">10</span>	                    expect(response.statusCode).to.equal(<span class="hljs-number">302</span>);
<span class="linenos">11</span>	
<span class="linenos">12</span>	                    <span class="hljs-comment">// Check the username</span>
<span class="linenos">13</span>	                    request.get({ url: <span class="hljs-string">'http://localhost:5000/'</span>, jar: <span class="hljs-literal">true</span> }, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(error, response, body)</span> </span>{
<span class="linenos">14</span>	                        expect(body).to.include(<span class="hljs-string">'bobsmith'</span>);
<span class="linenos">15</span>	                        done();
<span class="linenos">16</span>	                    });
<span class="linenos">17</span>	            });
<span class="linenos">18</span>	        });
<span class="linenos">19</span>	    });</code></pre>
<p>Here we’re using a new parameter, namely <code>jar</code> - this tells <code>request</code> to store the cookies. We POST the username to the login form, and then we get the index route and verify that the username is shown in the request. Check the test fails, then amend the index route in <code>index.js</code> as follows:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	<span class="hljs-comment">// Define index route</span>
<span class="linenos">2</span>	app.get(<span class="hljs-string">'/'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(req, res)</span> </span>{
<span class="linenos">3</span>	    <span class="hljs-comment">// Get messages</span>
<span class="linenos">4</span>	    client.lrange(<span class="hljs-string">'chat:messages'</span>, <span class="hljs-number">0</span>, -<span class="hljs-number">1</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(err, messages)</span> </span>{
<span class="linenos">5</span>	        <span class="hljs-comment">/* istanbul ignore if */</span>
<span class="linenos">6</span>	        <span class="hljs-keyword">if</span> (err) {
<span class="linenos">7</span>	            <span class="hljs-built_in">console</span>.log(err);
<span class="linenos">8</span>	        } <span class="hljs-keyword">else</span> {
<span class="linenos">9</span>	            <span class="hljs-comment">// Get username</span>
<span class="linenos">10</span>	            <span class="hljs-keyword">var</span> username = req.session.username;
<span class="linenos">11</span>	
<span class="linenos">12</span>	            <span class="hljs-comment">// Get messages</span>
<span class="linenos">13</span>	            <span class="hljs-keyword">var</span> message_list = [];
<span class="linenos">14</span>	            messages.forEach(<span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(message, i)</span> </span>{
<span class="linenos">15</span>	                <span class="hljs-comment">/* istanbul ignore next */</span>
<span class="linenos">16</span>	                message_list.push(message);
<span class="linenos">17</span>	            });
<span class="linenos">18</span>	
<span class="linenos">19</span>	            <span class="hljs-comment">// Render page</span>
<span class="linenos">20</span>	            res.render(<span class="hljs-string">'index'</span>, { messages: message_list, username: username });
<span class="linenos">21</span>	        }
<span class="linenos">22</span>	    });
<span class="linenos">23</span>	});</code></pre>
<p>Note we get the username and pass it through to the view. We need to adapt the header view to display the username. Amend <code>views/partials/header.hbs</code> to look like this:</p>
<pre><code class="hljs lang-handlebars"><span class="linenos">1</span>	<span class="xml"><span class="hljs-doctype">&lt;!DOCTYPE html&gt;</span>
<span class="linenos">2</span>	<span class="hljs-comment">&lt;!--[if lt IE 7]&gt;      &lt;html class="no-js lt-ie9 lt-ie8 lt-ie7"&gt; &lt;![endif]--&gt;</span>
<span class="linenos">3</span>	<span class="hljs-comment">&lt;!--[if IE 7]&gt;         &lt;html class="no-js lt-ie9 lt-ie8"&gt; &lt;![endif]--&gt;</span>
<span class="linenos">4</span>	<span class="hljs-comment">&lt;!--[if IE 8]&gt;         &lt;html class="no-js lt-ie9"&gt; &lt;![endif]--&gt;</span>
<span class="linenos">5</span>	<span class="hljs-comment">&lt;!--[if gt IE 8]&gt;&lt;!--&gt;</span> <span class="hljs-tag">&lt;<span class="hljs-title">html</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"no-js"</span>&gt;</span> <span class="hljs-comment">&lt;!--&lt;![endif]--&gt;</span>
<span class="linenos">6</span>	    <span class="hljs-tag">&lt;<span class="hljs-title">head</span>&gt;</span>
<span class="linenos">7</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">meta</span> <span class="hljs-attribute">charset</span>=<span class="hljs-value">"utf-8"</span>&gt;</span>
<span class="linenos">8</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">meta</span> <span class="hljs-attribute">http-equiv</span>=<span class="hljs-value">"X-UA-Compatible"</span> <span class="hljs-attribute">content</span>=<span class="hljs-value">"IE=edge"</span>&gt;</span>
<span class="linenos">9</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">title</span>&gt;</span>Babblr<span class="hljs-tag">&lt;/<span class="hljs-title">title</span>&gt;</span>
<span class="linenos">10</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">meta</span> <span class="hljs-attribute">name</span>=<span class="hljs-value">"description"</span> <span class="hljs-attribute">content</span>=<span class="hljs-value">""</span>&gt;</span>
<span class="linenos">11</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">meta</span> <span class="hljs-attribute">name</span>=<span class="hljs-value">"viewport"</span> <span class="hljs-attribute">content</span>=<span class="hljs-value">"width=device-width, initial-scale=1"</span>&gt;</span>
<span class="linenos">12</span>	
<span class="linenos">13</span>	        <span class="hljs-comment">&lt;!-- Place favicon.ico and apple-touch-icon.png in the root directory --&gt;</span>
<span class="linenos">14</span>	
<span class="linenos">15</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/bower_components/bootstrap/dist/css/bootstrap.min.css"</span>&gt;</span>
<span class="linenos">16</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/bower_components/bootstrap/dist/css/bootstrap-theme.min.css"</span>&gt;</span>
<span class="linenos">17</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/css/style.css"</span>&gt;</span>
<span class="linenos">18</span>	    <span class="hljs-tag">&lt;/<span class="hljs-title">head</span>&gt;</span>
<span class="linenos">19</span>	    <span class="hljs-tag">&lt;<span class="hljs-title">body</span>&gt;</span>
<span class="linenos">20</span>	        <span class="hljs-comment">&lt;!--[if lt IE 7]&gt;
<span class="linenos">21</span>	            &lt;p class="browsehappy"&gt;You are using an &lt;strong&gt;outdated&lt;/strong&gt; browser. Please &lt;a href="http://browsehappy.com/"&gt;upgrade your browser&lt;/a&gt; to improve your experience.&lt;/p&gt;
<span class="linenos">22</span>	        &lt;![endif]--&gt;</span>
<span class="linenos">23</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">nav</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"navbar navbar-inverse navbar-static-top"</span> <span class="hljs-attribute">role</span>=<span class="hljs-value">"navigation"</span>&gt;</span>
<span class="linenos">24</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"container-fluid"</span>&gt;</span>
<span class="linenos">25</span>	                <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"navbar-header"</span>&gt;</span>
<span class="linenos">26</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">button</span> <span class="hljs-attribute">type</span>=<span class="hljs-value">"button"</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"navbar-toggle"</span> <span class="hljs-attribute">data-toggle</span>=<span class="hljs-value">"collapse"</span> <span class="hljs-attribute">data-target</span>=<span class="hljs-value">"#header-nav"</span>&gt;</span>
<span class="linenos">27</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"icon-bar"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span>
<span class="linenos">28</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"icon-bar"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span>
<span class="linenos">29</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"icon-bar"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span>
<span class="linenos">30</span>	                    <span class="hljs-tag">&lt;/<span class="hljs-title">button</span>&gt;</span>
<span class="linenos">31</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"navbar-brand"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/"</span>&gt;</span>Babblr<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">32</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"collapse navbar-collapse navbar-right"</span> <span class="hljs-attribute">id</span>=<span class="hljs-value">"header-nav"</span>&gt;</span>
<span class="linenos">33</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">ul</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"nav navbar-nav"</span>&gt;</span>
<span class="linenos">34</span>	                            </span><span class="hljs-expression">{{<span class="hljs-begin-block">#<span class="hljs-keyword">if</span> username</span>}}</span><span class="xml">
<span class="linenos">35</span>	                            <span class="hljs-tag">&lt;<span class="hljs-title">li</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/logout"</span>&gt;</span>Logged in as </span><span class="hljs-expression">{{ <span class="hljs-variable">username</span> }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">li</span>&gt;</span>
<span class="linenos">36</span>	                            </span><span class="hljs-expression">{{<span class="hljs-variable"><span class="hljs-keyword">else</span></span>}}</span><span class="xml">
<span class="linenos">37</span>	                            <span class="hljs-tag">&lt;<span class="hljs-title">li</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/login"</span>&gt;</span>Log in<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">li</span>&gt;</span>
<span class="linenos">38</span>	                            </span><span class="hljs-expression">{{<span class="hljs-end-block">/<span class="hljs-keyword">if</span></span>}}</span><span class="xml">
<span class="linenos">39</span>	                        <span class="hljs-tag">&lt;/<span class="hljs-title">ul</span>&gt;</span>
<span class="linenos">40</span>	                    <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">41</span>	                <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">42</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">43</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">nav</span>&gt;</span></span></code></pre>
<p>Note the addition of a logout link, which we will implement later. Let’s check our tests pass:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ grunt <span class="hljs-built_in">test</span>
<span class="linenos">2</span>	Running <span class="hljs-string">"jshint:all"</span> (jshint) task
<span class="linenos">3</span>	&gt;&gt; <span class="hljs-number">2</span> files lint free.
<span class="linenos">4</span>	
<span class="linenos">5</span>	Running <span class="hljs-string">"mocha_istanbul:coverage"</span> (mocha_istanbul) task
<span class="linenos">6</span>	express-session deprecated undefined resave option; provide resave option index.js:<span class="hljs-number">9</span>:<span class="hljs-number">1669</span>
<span class="linenos">7</span>	express-session deprecated undefined saveUninitialized option; provide saveUninitialized option index.js:<span class="hljs-number">9</span>:<span class="hljs-number">1669</span>
<span class="linenos">8</span>	Listening on port <span class="hljs-number">5000</span>
<span class="linenos">9</span>	
<span class="linenos">10</span>	
<span class="linenos">11</span>	  server
<span class="linenos">12</span>	Starting the server
<span class="linenos">13</span>	    Test the index route
<span class="linenos">14</span>	      ✓ should <span class="hljs-built_in">return</span> a page with the title Babblr (<span class="hljs-number">44</span>ms)
<span class="linenos">15</span>	    Test the login route
<span class="linenos">16</span>	      ✓ should <span class="hljs-built_in">return</span> a page with the text Please enter a handle
<span class="linenos">17</span>	    Test submitting to the login route
<span class="linenos">18</span>	      ✓ should store the username <span class="hljs-keyword">in</span> the session and redirect the user to the index
<span class="linenos">19</span>	    Test empty login
<span class="linenos">20</span>	      ✓ should show the login form
<span class="linenos">21</span>	    Test sending a message
<span class="linenos">22</span>	      ✓ should <span class="hljs-built_in">return</span> <span class="hljs-string">'Message received'</span> (<span class="hljs-number">45</span>ms)
<span class="linenos">23</span>	Stopping the server
<span class="linenos">24</span>	
<span class="linenos">25</span>	
<span class="linenos">26</span>	  <span class="hljs-number">5</span> passing (<span class="hljs-number">156</span>ms)
<span class="linenos">27</span>	
<span class="linenos">28</span>	=============================================================================
<span class="linenos">29</span>	Writing coverage object [/Users/matthewdaly/Projects/babblr/coverage/coverage.json]
<span class="linenos">30</span>	Writing coverage reports at [/Users/matthewdaly/Projects/babblr/coverage]
<span class="linenos">31</span>	=============================================================================
<span class="linenos">32</span>	
<span class="linenos">33</span>	=============================== Coverage summary ===============================
<span class="linenos">34</span>	Statements   : <span class="hljs-number">100</span>% ( <span class="hljs-number">55</span>/<span class="hljs-number">55</span> ), <span class="hljs-number">7</span> ignored
<span class="linenos">35</span>	Branches     : <span class="hljs-number">100</span>% ( <span class="hljs-number">10</span>/<span class="hljs-number">10</span> ), <span class="hljs-number">2</span> ignored
<span class="linenos">36</span>	Functions    : <span class="hljs-number">88.89</span>% ( <span class="hljs-number">8</span>/<span class="hljs-number">9</span> )
<span class="linenos">37</span>	Lines        : <span class="hljs-number">100</span>% ( <span class="hljs-number">55</span>/<span class="hljs-number">55</span> )
<span class="linenos">38</span>	================================================================================
<span class="linenos">39</span>	&gt;&gt; Done. Check coverage folder.
<span class="linenos">40</span>	
<span class="linenos">41</span>	Running <span class="hljs-string">"coveralls:app"</span> (coveralls) task
<span class="linenos">42</span>	&gt;&gt; Failed to submit <span class="hljs-string">'coverage/lcov.info'</span> to coveralls: Bad response: <span class="hljs-number">422</span> {<span class="hljs-string">"message"</span>:<span class="hljs-string">"Couldn't find a repository matching this job."</span>,<span class="hljs-string">"error"</span>:<span class="hljs-literal">true</span>}
<span class="linenos">43</span>	&gt;&gt; Failed to submit coverage results to coveralls
<span class="linenos">44</span>	Warning: Task <span class="hljs-string">"coveralls:app"</span> failed. Use --force to continue.
<span class="linenos">45</span>	
<span class="linenos">46</span>	Aborted due to warnings.</code></pre>
<p>Excellent! Next, let’s implement the test for our logout route:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	    <span class="hljs-comment">// Test logout</span>
<span class="linenos">2</span>	    describe(<span class="hljs-string">'Test logout'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">()</span> </span>{
<span class="linenos">3</span>	        it(<span class="hljs-string">'should log the user out'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(done)</span> </span>{
<span class="linenos">4</span>	            request.post({ url: <span class="hljs-string">'http://localhost:5000/login'</span>,
<span class="linenos">5</span>	                form:{username: <span class="hljs-string">'bobsmith'</span>},
<span class="linenos">6</span>	                jar: <span class="hljs-literal">true</span>,
<span class="linenos">7</span>	                followRedirect: <span class="hljs-literal">false</span>},
<span class="linenos">8</span>	                <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(error, response, body)</span> </span>{
<span class="linenos">9</span>	                    expect(response.headers.location).to.equal(<span class="hljs-string">'/'</span>);
<span class="linenos">10</span>	                    expect(response.statusCode).to.equal(<span class="hljs-number">302</span>);
<span class="linenos">11</span>	
<span class="linenos">12</span>	                    <span class="hljs-comment">// Check the username</span>
<span class="linenos">13</span>	                    request.get({ url: <span class="hljs-string">'http://localhost:5000/'</span>, jar: <span class="hljs-literal">true</span> }, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(error, response, body)</span> </span>{
<span class="linenos">14</span>	                        expect(body).to.include(<span class="hljs-string">'bobsmith'</span>);
<span class="linenos">15</span>	
<span class="linenos">16</span>	                        <span class="hljs-comment">// Log the user out</span>
<span class="linenos">17</span>	                        request.get({ url: <span class="hljs-string">'http://localhost:5000/logout'</span>, jar: <span class="hljs-literal">true</span> }, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(error, response, body)</span> </span>{
<span class="linenos">18</span>	                            expect(body).to.include(<span class="hljs-string">'Log in'</span>);
<span class="linenos">19</span>	                            done();
<span class="linenos">20</span>	                            });
<span class="linenos">21</span>	                    });
<span class="linenos">22</span>	            });
<span class="linenos">23</span>	        });
<span class="linenos">24</span>	    });</code></pre>
<p>This is largely the same as the previous test, but adds some additional content at the end to test logging out afterwards. Let’s run the test:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ grunt <span class="hljs-built_in">test</span>
<span class="linenos">2</span>	Running <span class="hljs-string">"jshint:all"</span> (jshint) task
<span class="linenos">3</span>	&gt;&gt; <span class="hljs-number">2</span> files lint free.
<span class="linenos">4</span>	
<span class="linenos">5</span>	Running <span class="hljs-string">"mocha_istanbul:coverage"</span> (mocha_istanbul) task
<span class="linenos">6</span>	express-session deprecated undefined resave option; provide resave option index.js:<span class="hljs-number">9</span>:<span class="hljs-number">1669</span>
<span class="linenos">7</span>	express-session deprecated undefined saveUninitialized option; provide saveUninitialized option index.js:<span class="hljs-number">9</span>:<span class="hljs-number">1669</span>
<span class="linenos">8</span>	Listening on port <span class="hljs-number">5000</span>
<span class="linenos">9</span>	
<span class="linenos">10</span>	
<span class="linenos">11</span>	  server
<span class="linenos">12</span>	Starting the server
<span class="linenos">13</span>	    Test the index route
<span class="linenos">14</span>	      ✓ should <span class="hljs-built_in">return</span> a page with the title Babblr (<span class="hljs-number">536</span>ms)
<span class="linenos">15</span>	    Test the login route
<span class="linenos">16</span>	      ✓ should <span class="hljs-built_in">return</span> a page with the text Please enter a handle
<span class="linenos">17</span>	    Test submitting to the login route
<span class="linenos">18</span>	      ✓ should store the username <span class="hljs-keyword">in</span> the session and redirect the user to the index
<span class="linenos">19</span>	    Test empty login
<span class="linenos">20</span>	      ✓ should show the login form
<span class="linenos">21</span>	    Test <span class="hljs-built_in">logout</span>
<span class="linenos">22</span>	      <span class="hljs-number">1</span>) should <span class="hljs-built_in">log</span> the user out
<span class="linenos">23</span>	    Test sending a message
<span class="linenos">24</span>	      ✓ should <span class="hljs-built_in">return</span> <span class="hljs-string">'Message received'</span> (<span class="hljs-number">49</span>ms)
<span class="linenos">25</span>	Stopping the server
<span class="linenos">26</span>	
<span class="linenos">27</span>	
<span class="linenos">28</span>	  <span class="hljs-number">5</span> passing (<span class="hljs-number">682</span>ms)
<span class="linenos">29</span>	  <span class="hljs-number">1</span> failing
<span class="linenos">30</span>	
<span class="linenos">31</span>	  <span class="hljs-number">1</span>) server Test <span class="hljs-built_in">logout</span> should <span class="hljs-built_in">log</span> the user out:
<span class="linenos">32</span>	     Uncaught AssertionError: expected <span class="hljs-string">'Cannot GET /logout\n'</span> to include <span class="hljs-string">'Log in'</span>
<span class="linenos">33</span>	      at Request._callback (/Users/matthewdaly/Projects/babblr/<span class="hljs-built_in">test</span>/test.js:<span class="hljs-number">105</span>:<span class="hljs-number">45</span>)
<span class="linenos">34</span>	      at Request.self.callback (/Users/matthewdaly/Projects/babblr/node_modules/request/request.js:<span class="hljs-number">373</span>:<span class="hljs-number">22</span>)
<span class="linenos">35</span>	      at Request.emit (events.js:<span class="hljs-number">98</span>:<span class="hljs-number">17</span>)
<span class="linenos">36</span>	      at Request.&lt;anonymous&gt; (/Users/matthewdaly/Projects/babblr/node_modules/request/request.js:<span class="hljs-number">1318</span>:<span class="hljs-number">14</span>)
<span class="linenos">37</span>	      at Request.emit (events.js:<span class="hljs-number">117</span>:<span class="hljs-number">20</span>)
<span class="linenos">38</span>	      at IncomingMessage.&lt;anonymous&gt; (/Users/matthewdaly/Projects/babblr/node_modules/request/request.js:<span class="hljs-number">1266</span>:<span class="hljs-number">12</span>)
<span class="linenos">39</span>	      at IncomingMessage.emit (events.js:<span class="hljs-number">117</span>:<span class="hljs-number">20</span>)
<span class="linenos">40</span>	      at _stream_readable.js:<span class="hljs-number">944</span>:<span class="hljs-number">16</span>
<span class="linenos">41</span>	      at process._tickCallback (node.js:<span class="hljs-number">442</span>:<span class="hljs-number">13</span>)
<span class="linenos">42</span>	
<span class="linenos">43</span>	
<span class="linenos">44</span>	
<span class="linenos">45</span>	=============================================================================
<span class="linenos">46</span>	Writing coverage object [/Users/matthewdaly/Projects/babblr/coverage/coverage.json]
<span class="linenos">47</span>	Writing coverage reports at [/Users/matthewdaly/Projects/babblr/coverage]
<span class="linenos">48</span>	=============================================================================
<span class="linenos">49</span>	
<span class="linenos">50</span>	=============================== Coverage summary ===============================
<span class="linenos">51</span>	Statements   : <span class="hljs-number">100</span>% ( <span class="hljs-number">55</span>/<span class="hljs-number">55</span> ), <span class="hljs-number">7</span> ignored
<span class="linenos">52</span>	Branches     : <span class="hljs-number">100</span>% ( <span class="hljs-number">10</span>/<span class="hljs-number">10</span> ), <span class="hljs-number">2</span> ignored
<span class="linenos">53</span>	Functions    : <span class="hljs-number">88.89</span>% ( <span class="hljs-number">8</span>/<span class="hljs-number">9</span> )
<span class="linenos">54</span>	Lines        : <span class="hljs-number">100</span>% ( <span class="hljs-number">55</span>/<span class="hljs-number">55</span> )
<span class="linenos">55</span>	================================================================================
<span class="linenos">56</span>	&gt;&gt;
<span class="linenos">57</span>	Warning: Task <span class="hljs-string">"mocha_istanbul:coverage"</span> failed. Use --force to continue.
<span class="linenos">58</span>	
<span class="linenos">59</span>	Aborted due to warnings.</code></pre>
<p>Now we have a failing test, let’s implement our logout route. Add the following route to <code>index.js</code>:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	<span class="hljs-comment">// Process logout</span>
<span class="linenos">2</span>	app.get(<span class="hljs-string">'/logout'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(req, res)</span> </span>{
<span class="linenos">3</span>	    <span class="hljs-comment">// Delete username from session</span>
<span class="linenos">4</span>	    req.session.username = <span class="hljs-literal">null</span>;
<span class="linenos">5</span>	
<span class="linenos">6</span>	    <span class="hljs-comment">// Redirect user</span>
<span class="linenos">7</span>	    res.redirect(<span class="hljs-string">'/'</span>);
<span class="linenos">8</span>	});</code></pre>
<p>If you run your tests again, they should now pass.</p>
<p>Now that we have the user’s name stored in the session, we can make use of it. First, let’s amend <code>static/js/main.js</code> so that it no longer adds a default username:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	$(<span class="hljs-built_in">document</span>).ready(<span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">()</span> </span>{
<span class="linenos">2</span>	<span class="hljs-pi">    'use strict'</span>;
<span class="linenos">3</span>	
<span class="linenos">4</span>	    <span class="hljs-comment">// Set up the connection</span>
<span class="linenos">5</span>	    <span class="hljs-keyword">var</span> field, socket, output;
<span class="linenos">6</span>	    socket = io.connect(<span class="hljs-built_in">window</span>.location.href);
<span class="linenos">7</span>	
<span class="linenos">8</span>	    <span class="hljs-comment">// Get a reference to the input</span>
<span class="linenos">9</span>	    field = $(<span class="hljs-string">'textarea#message'</span>);
<span class="linenos">10</span>	
<span class="linenos">11</span>	    <span class="hljs-comment">// Get a reference to the output</span>
<span class="linenos">12</span>	    output = $(<span class="hljs-string">'div.conversation'</span>);
<span class="linenos">13</span>	
<span class="linenos">14</span>	    <span class="hljs-comment">// Handle message submit</span>
<span class="linenos">15</span>	    $(<span class="hljs-string">'a#submitbutton'</span>).on(<span class="hljs-string">'click'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">()</span> </span>{
<span class="linenos">16</span>	        <span class="hljs-comment">// Create the message</span>
<span class="linenos">17</span>	        <span class="hljs-keyword">var</span> msg;
<span class="linenos">18</span>	        msg = field.val();
<span class="linenos">19</span>	        socket.emit(<span class="hljs-string">'send'</span>, { message: msg });
<span class="linenos">20</span>	        field.val(<span class="hljs-string">''</span>);
<span class="linenos">21</span>	    });
<span class="linenos">22</span>	
<span class="linenos">23</span>	    <span class="hljs-comment">// Handle incoming messages</span>
<span class="linenos">24</span>	    socket.on(<span class="hljs-string">'message'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(data)</span> </span>{
<span class="linenos">25</span>	        <span class="hljs-comment">// Insert the message</span>
<span class="linenos">26</span>	        output.append(<span class="hljs-string">'&lt;p&gt;'</span> + data + <span class="hljs-string">'&lt;/p&gt;'</span>);
<span class="linenos">27</span>	    });
<span class="linenos">28</span>	});</code></pre>
<p>Then, in <code>index.js</code>, we need to declare a variable for our session middleware, which will be shared between Socket.IO and Express:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	<span class="hljs-comment">// Declare variables used</span>
<span class="linenos">2</span>	<span class="hljs-keyword">var</span> app, base_url, bodyParser, client, express, hbs, io, port, RedisStore, rtg, session, sessionMiddleware, subscribe;</code></pre>
<p>Then we amend the session setup to make it easier to reuse for Socket.IO:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	<span class="hljs-comment">// Set up session</span>
<span class="linenos">2</span>	sessionMiddleware = session({
<span class="linenos">3</span>	    store: <span class="hljs-keyword">new</span> RedisStore({
<span class="linenos">4</span>	        client: client
<span class="linenos">5</span>	    }),
<span class="linenos">6</span>	    secret: <span class="hljs-string">'blibble'</span>
<span class="linenos">7</span>	});
<span class="linenos">8</span>	app.use(sessionMiddleware);</code></pre>
<p>Towards the end of the file, before we set up our handlers for Socket.IO, we integrate our sessions:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	<span class="hljs-comment">// Integrate sessions</span>
<span class="linenos">2</span>	io.use(<span class="hljs-function"><span class="hljs-keyword">function</span><span class="hljs-params">(socket, next)</span> </span>{
<span class="linenos">3</span>	    sessionMiddleware(socket.request, socket.request.res, next);
<span class="linenos">4</span>	});</code></pre>
<p>Finally, we rewrite our session handlers to use the username from the session:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	<span class="hljs-comment">// Handle new messages</span>
<span class="linenos">2</span>	io.sockets.on(<span class="hljs-string">'connection'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(socket)</span> </span>{
<span class="linenos">3</span>	    <span class="hljs-comment">// Subscribe to the Redis channel</span>
<span class="linenos">4</span>	    subscribe.subscribe(<span class="hljs-string">'ChatChannel'</span>);
<span class="linenos">5</span>	
<span class="linenos">6</span>	    <span class="hljs-comment">// Handle incoming messages</span>
<span class="linenos">7</span>	    socket.on(<span class="hljs-string">'send'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(data)</span> </span>{
<span class="linenos">8</span>	        <span class="hljs-comment">// Define variables</span>
<span class="linenos">9</span>	        <span class="hljs-keyword">var</span> username, message;
<span class="linenos">10</span>	
<span class="linenos">11</span>	        <span class="hljs-comment">// Get username</span>
<span class="linenos">12</span>	        username = socket.request.session.username;
<span class="linenos">13</span>	        <span class="hljs-keyword">if</span> (!username) {
<span class="linenos">14</span>	            username = <span class="hljs-string">'Anonymous Coward'</span>;
<span class="linenos">15</span>	        }
<span class="linenos">16</span>	        message = username + <span class="hljs-string">': '</span> + data.message;
<span class="linenos">17</span>	
<span class="linenos">18</span>	        <span class="hljs-comment">// Publish it</span>
<span class="linenos">19</span>	        client.publish(<span class="hljs-string">'ChatChannel'</span>, message);
<span class="linenos">20</span>	
<span class="linenos">21</span>	        <span class="hljs-comment">// Persist it to a Redis list</span>
<span class="linenos">22</span>	        client.rpush(<span class="hljs-string">'chat:messages'</span>, message);
<span class="linenos">23</span>	    });
<span class="linenos">24</span>	
<span class="linenos">25</span>	    <span class="hljs-comment">// Handle receiving messages</span>
<span class="linenos">26</span>	    <span class="hljs-keyword">var</span> callback = <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(channel, data)</span> </span>{
<span class="linenos">27</span>	        socket.emit(<span class="hljs-string">'message'</span>, data);
<span class="linenos">28</span>	    };
<span class="linenos">29</span>	    subscribe.on(<span class="hljs-string">'message'</span>, callback);
<span class="linenos">30</span>	
<span class="linenos">31</span>	    <span class="hljs-comment">// Handle disconnect</span>
<span class="linenos">32</span>	    socket.on(<span class="hljs-string">'disconnect'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">()</span> </span>{
<span class="linenos">33</span>	        subscribe.removeListener(<span class="hljs-string">'message'</span>, callback);
<span class="linenos">34</span>	    });
<span class="linenos">35</span>	});</code></pre>
<p>Note here that when a message is sent, we get the username from the session, and if it’s empty, set it to Anonymous Coward. We then prepend it to the message, publish it, and persist it.</p>
<h2 id="one-final-thing-">One final thing…</h2>
<p>One last job remains. At present, users can pass JavaScript through in messages, which is not terribly secure! We need to fix it. Amend the <code>send</code> handler as follows:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	    <span class="hljs-comment">// Handle incoming messages</span>
<span class="linenos">2</span>	    socket.on(<span class="hljs-string">'send'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(data)</span> </span>{
<span class="linenos">3</span>	        <span class="hljs-comment">// Define variables</span>
<span class="linenos">4</span>	        <span class="hljs-keyword">var</span> username, message;
<span class="linenos">5</span>	
<span class="linenos">6</span>	        <span class="hljs-comment">// Strip tags from message</span>
<span class="linenos">7</span>	        message = data.message.replace(<span class="hljs-regexp">/&lt;[^&gt;]*&gt;/g</span>, <span class="hljs-string">''</span>);
<span class="linenos">8</span>	
<span class="linenos">9</span>	        <span class="hljs-comment">// Get username</span>
<span class="linenos">10</span>	        username = socket.request.session.username;
<span class="linenos">11</span>	        <span class="hljs-keyword">if</span> (!username) {
<span class="linenos">12</span>	            username = <span class="hljs-string">'Anonymous Coward'</span>;
<span class="linenos">13</span>	        }
<span class="linenos">14</span>	        message = username + <span class="hljs-string">': '</span> + message;
<span class="linenos">15</span>	
<span class="linenos">16</span>	        <span class="hljs-comment">// Publish it</span>
<span class="linenos">17</span>	        client.publish(<span class="hljs-string">'ChatChannel'</span>, message);
<span class="linenos">18</span>	
<span class="linenos">19</span>	        <span class="hljs-comment">// Persist it to a Redis list</span>
<span class="linenos">20</span>	        client.rpush(<span class="hljs-string">'chat:messages'</span>, message);
<span class="linenos">21</span>	    });</code></pre>
<p>Here we use a regex to strip out any HTML tags from the message - this will prevent anyone injecting JavaScript into our chat client.</p>
<p>And that’s all, folks! If you want to check out the source for this lesson it’s in the repository on GitHub, tagged <code>lesson-2</code>. If you want to carry on working on this on your own, there’s still plenty you can do, such as:</p>
<ul>
<li>Adding support for multiple rooms</li>
<li>Using Passport.js to allow logging in using third-party services such as Twitter or Facebook</li>
<li>Adding formatting for messages, either by using something like Markdown, or a client-side rich text editor</li>
</ul>
<p>As you can see, it’s surprising how much you can accomplish using only Redis, and under certain circumstances it offers a lot of advantages over a relational database. It’s always worth thinking about whether Redis can be used for your project.</p>
]]></description>
            <link>http://matthewdaly.co.uk/blog/2015/03/02/extending-our-node-dot-js-and-redis-chat-server/</link>
            <guid isPermaLink="true">http://matthewdaly.co.uk/blog/2015/03/02/extending-our-node-dot-js-and-redis-chat-server/</guid>
            <pubDate>Mon, 02 Mar 2015 23:03:48 GMT</pubDate>
        </item>
        <item>
            <title><![CDATA[Switching to my own static site generator]]></title>
            <description><![CDATA[<p>As you may have seen if you’re visiting the site, I’ve finally switched over from Octopress to the static site generator I’ve been working on for the last few months. Apologies if you’re seeing lots of old posts in your RSS reader - there must have been an inconsistency between the RSS feed for this and that for Octopress.</p>
<p>I actually still really like Octopress, however I’m not and have never been a big fan of Ruby. Python and JavaScript are my two main go-to languages (although I do a lot of work professionally with PHP as well), so I wanted a solution in one of those languages, but I wanted something that was very similar to Octopress in every other way. I also wanted the facility to easily concatenate and minify static files as part of my deployment process to make the whole thing as lean as possible, so it made sense to build it as a Grunt plugin and create a Yeoman generator for building the boilerplate for the blog. Also, it’s always easier to work with your own code, and so using templates I wrote myself should make it quicker and easier for me to customise the blog how I want.</p>
<p>While deploying it did throw up a few errors that I’ve had to fix, it’s gone fairly smoothly and I’m pretty happy with it, although I will no doubt spend some time tweaking it over the next few weeks. It’s built with GitHub Pages in mind, but the fact that it’s built using Grunt should make it straightforward to switch to a different deployment method - during development I’ve actually used <code>grunt-rsync</code> to deploy to my Raspberry Pi and <code>grunt-bitbucket-pages</code> to deploy to Bitbucket in order to test it and both work absolutely fine. There are also Grunt plugins for deploying via FTP around, so if you want to check it out, then as long as you have at least some familiarity with Grunt you should be able to deploy it however you wish. The generator is meant to be only a starting point for your own site, so by all means check it out, tinker with the styling and templates, and make it your own. I will be very happy indeed if I see someone else using it in the wild.</p>
<p>Static site generators are generally somewhat harder to use than a CMS like WordPress, but they have many advantages:</p>
<ul>
<li>Lighter - you can quite easily host a static site with just Nginx on a Raspberry Pi</li>
<li>Faster - with no database or actual dynamic content on the server, just flat HTML, your site will be far quicker to load than a WordPress blog</li>
<li>Cheaper to host</li>
<li>Easy to deploy - if your workflow is very command-line based like mine is, it’s very quick and easy to get blogging</li>
</ul>
<p>If you can get away with using a static site generator rather than a database-driven blogging system, then it’s well worth doing so.</p>
]]></description>
            <link>http://matthewdaly.co.uk/blog/2015/02/15/switching-to-my-own-static-site-generator/</link>
            <guid isPermaLink="true">http://matthewdaly.co.uk/blog/2015/02/15/switching-to-my-own-static-site-generator/</guid>
            <pubDate>Sun, 15 Feb 2015 18:11:22 GMT</pubDate>
        </item>
        <item>
            <title><![CDATA[Building a chat server with Node.js and Redis]]></title>
            <description><![CDATA[<p>One of the more interesting capabilities Redis offers is its support for Pub/Sub. This allows you to subscribe to a specific channel, and then react when some content is published to that channel. In this tutorial, we’ll build a very simple web-based chat system that demonstrates Redis’s Pub/Sub support in action. Chat systems are pretty much synonymous with Node.js - it’s widely considered the “Hello, World!” of Node.js. Since we already used Node with the prior Redis tutorial, then it also makes sense to stick with it for this project too.</p>
<h2 id="installing-node-js">Installing Node.js</h2>
<p>Since the last tutorial, I’ve discovered <a href="https://github.com/creationix/nvm">NVM</a>, and if you’re using any flavour of Unix, I highly recommend using it. It’s not an option if you’re using Windows, however Redis doesn’t officially support Windows anyway, so if you want to follow along on a Windows machine I’d recommend using a VM.</p>
<p>If you followed the URL shortener tutorial, you should already have everything you need, though I’d still recommend switching to NVM as it’s very convenient. We’ll be using Grunt again, so you’ll need to make sure you have <code>grunt-cli</code> installed with the following command:</p>
<pre><code class="hljs lang-bash singleline">$ npm install -g grunt-cli</code></pre>
<p>This assumes you used NVM to install Node - if it’s installed globally, you may need to use <code>sudo</code>.</p>
<h2 id="installing-dependencies">Installing dependencies</h2>
<p>As usual with a Node.js project, our first step is to create our <code>package.json</code> file:</p>
<pre><code class="hljs lang-bash singleline">$ npm init</code></pre>
<p>Answer the questions so you end up with something like this (or just paste this into <code>package.json</code> and amend it as you see fit):</p>
<pre><code class="hljs lang-json"><span class="linenos">1</span>	{
<span class="linenos">2</span>	  "<span class="hljs-attribute">name</span>": <span class="hljs-value"><span class="hljs-string">"babblr"</span></span>,
<span class="linenos">3</span>	  "<span class="hljs-attribute">version</span>": <span class="hljs-value"><span class="hljs-string">"1.0.0"</span></span>,
<span class="linenos">4</span>	  "<span class="hljs-attribute">description</span>": <span class="hljs-value"><span class="hljs-string">"Chat client"</span></span>,
<span class="linenos">5</span>	  "<span class="hljs-attribute">main</span>": <span class="hljs-value"><span class="hljs-string">"index.js"</span></span>,
<span class="linenos">6</span>	  "<span class="hljs-attribute">scripts</span>": <span class="hljs-value">{
<span class="linenos">7</span>	    "<span class="hljs-attribute">test</span>": <span class="hljs-value"><span class="hljs-string">"grunt test --verbose"</span>
<span class="linenos">8</span>	  </span>}</span>,
<span class="linenos">9</span>	  "<span class="hljs-attribute">keywords</span>": <span class="hljs-value">[
<span class="linenos">10</span>	    <span class="hljs-string">"chat"</span>
<span class="linenos">11</span>	  ]</span>,
<span class="linenos">12</span>	  "<span class="hljs-attribute">author</span>": <span class="hljs-value"><span class="hljs-string">"Matthew Daly &lt;matthew@matthewdaly.co.uk&gt; (http://matthewdaly.co.uk/)"</span></span>,
<span class="linenos">13</span>	  "<span class="hljs-attribute">license</span>": <span class="hljs-value"><span class="hljs-string">"GPLv2"</span>
<span class="linenos">14</span>	</span>}</code></pre>
<p>Now let’s install our dependencies:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ npm install express hbs redis hiredis socket.io socket.io-client --save
<span class="linenos">2</span>	$ npm install chai grunt grunt-contrib-jshint grunt-coveralls grunt-mocha-istanbul istanbul mocha request --save-dev</code></pre>
<p>These two commands will install our dependencies.</p>
<p>Now, if you followed on with the URL shortener tutorial, you’ll notice that we aren’t using Jade - instead we’re going to use Handlebars. Jade is quite a nice templating system, but I find it gets in the way for larger projects - you spend too much time looking up the syntax for things you already know in HTML. Handlebars is closer to HTML so we will use that. We’ll also use Socket.IO extensively on this project.</p>
<h2 id="support-files">Support files</h2>
<p>As before, we’ll also use Mocha for our unit tests and Istanbul to generate coverage stats. We’ll need a Grunt configuration for that, so here it is:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	<span class="hljs-built_in">module</span>.exports = <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(grunt)</span> </span>{
<span class="linenos">2</span>	<span class="hljs-pi">    'use strict'</span>;
<span class="linenos">3</span>	
<span class="linenos">4</span>	    grunt.initConfig({
<span class="linenos">5</span>	        jshint: {
<span class="linenos">6</span>	            all: [
<span class="linenos">7</span>	                <span class="hljs-string">'test/*.js'</span>,
<span class="linenos">8</span>	                <span class="hljs-string">'index.js'</span>
<span class="linenos">9</span>	            ]
<span class="linenos">10</span>	        },
<span class="linenos">11</span>	        mocha_istanbul: {
<span class="linenos">12</span>	            coverage: {
<span class="linenos">13</span>	                src: <span class="hljs-string">'test'</span>, <span class="hljs-comment">// the folder, not the files,</span>
<span class="linenos">14</span>	                options: {
<span class="linenos">15</span>	                    mask: <span class="hljs-string">'*.js'</span>,
<span class="linenos">16</span>	                    reportFormats: [<span class="hljs-string">'cobertura'</span>, <span class="hljs-string">'html'</span>, <span class="hljs-string">'lcovonly'</span>]
<span class="linenos">17</span>	                }
<span class="linenos">18</span>	            }
<span class="linenos">19</span>	        },
<span class="linenos">20</span>	        coveralls: {
<span class="linenos">21</span>	            options: {
<span class="linenos">22</span>	                src: <span class="hljs-string">'coverage/lcov.info'</span>,
<span class="linenos">23</span>	                force: <span class="hljs-literal">false</span>
<span class="linenos">24</span>	            },
<span class="linenos">25</span>	            app: {
<span class="linenos">26</span>	                src: <span class="hljs-string">'coverage/lcov.info'</span>
<span class="linenos">27</span>	            }
<span class="linenos">28</span>	        }
<span class="linenos">29</span>	    });
<span class="linenos">30</span>	
<span class="linenos">31</span>	    <span class="hljs-comment">// Load tasks</span>
<span class="linenos">32</span>	    grunt.loadNpmTasks(<span class="hljs-string">'grunt-contrib-jshint'</span>);
<span class="linenos">33</span>	    grunt.loadNpmTasks(<span class="hljs-string">'grunt-coveralls'</span>);
<span class="linenos">34</span>	    grunt.loadNpmTasks(<span class="hljs-string">'grunt-mocha-istanbul'</span>);
<span class="linenos">35</span>	
<span class="linenos">36</span>	    <span class="hljs-comment">// Register tasks</span>
<span class="linenos">37</span>	    grunt.registerTask(<span class="hljs-string">'test'</span>, [<span class="hljs-string">'jshint'</span>, <span class="hljs-string">'mocha_istanbul:coverage'</span>, <span class="hljs-string">'coveralls'</span>]);
<span class="linenos">38</span>	};</code></pre>
<p>We also need a <code>.bowerrc</code>:</p>
<pre><code class="hljs lang-json"><span class="linenos">1</span>	{
<span class="linenos">2</span>	    "<span class="hljs-attribute">directory</span>": <span class="hljs-value"><span class="hljs-string">"static/bower_components"</span>
<span class="linenos">3</span>	</span>}</code></pre>
<p>And a <code>bower.json</code>:</p>
<pre><code class="hljs lang-json"><span class="linenos">1</span>	{
<span class="linenos">2</span>	  "<span class="hljs-attribute">name</span>": <span class="hljs-value"><span class="hljs-string">"babblr"</span></span>,
<span class="linenos">3</span>	  "<span class="hljs-attribute">main</span>": <span class="hljs-value"><span class="hljs-string">"index.js"</span></span>,
<span class="linenos">4</span>	  "<span class="hljs-attribute">version</span>": <span class="hljs-value"><span class="hljs-string">"1.0.0"</span></span>,
<span class="linenos">5</span>	  "<span class="hljs-attribute">authors</span>": <span class="hljs-value">[
<span class="linenos">6</span>	    <span class="hljs-string">"Matthew Daly &lt;matthewbdaly@gmail.com&gt;"</span>
<span class="linenos">7</span>	  ]</span>,
<span class="linenos">8</span>	  "<span class="hljs-attribute">description</span>": <span class="hljs-value"><span class="hljs-string">"A simple chat server"</span></span>,
<span class="linenos">9</span>	  "<span class="hljs-attribute">moduleType</span>": <span class="hljs-value">[
<span class="linenos">10</span>	    <span class="hljs-string">"node"</span>
<span class="linenos">11</span>	  ]</span>,
<span class="linenos">12</span>	  "<span class="hljs-attribute">keywords</span>": <span class="hljs-value">[
<span class="linenos">13</span>	    <span class="hljs-string">"chat"</span>
<span class="linenos">14</span>	  ]</span>,
<span class="linenos">15</span>	  "<span class="hljs-attribute">license</span>": <span class="hljs-value"><span class="hljs-string">"GPLv2"</span></span>,
<span class="linenos">16</span>	  "<span class="hljs-attribute">homepage</span>": <span class="hljs-value"><span class="hljs-string">"http://matthewdaly.co.uk"</span></span>,
<span class="linenos">17</span>	  "<span class="hljs-attribute">private</span>": <span class="hljs-value"><span class="hljs-literal">true</span></span>,
<span class="linenos">18</span>	  "<span class="hljs-attribute">ignore</span>": <span class="hljs-value">[
<span class="linenos">19</span>	    <span class="hljs-string">"**/.*"</span>,
<span class="linenos">20</span>	    <span class="hljs-string">"node_modules"</span>,
<span class="linenos">21</span>	    <span class="hljs-string">"bower_components"</span>,
<span class="linenos">22</span>	    <span class="hljs-string">"test"</span>,
<span class="linenos">23</span>	    <span class="hljs-string">"tests"</span>
<span class="linenos">24</span>	  ]</span>,
<span class="linenos">25</span>	  "<span class="hljs-attribute">dependencies</span>": <span class="hljs-value">{
<span class="linenos">26</span>	    "<span class="hljs-attribute">html5-boilerplate</span>": <span class="hljs-value"><span class="hljs-string">"~4.3.0"</span></span>,
<span class="linenos">27</span>	    "<span class="hljs-attribute">jquery</span>": <span class="hljs-value"><span class="hljs-string">"~2.1.1"</span></span>,
<span class="linenos">28</span>	    "<span class="hljs-attribute">bootstrap</span>": <span class="hljs-value"><span class="hljs-string">"~3.3.1"</span>
<span class="linenos">29</span>	  </span>}
<span class="linenos">30</span>	</span>}</code></pre>
<p>Then install the Bower dependencies:</p>
<pre><code class="hljs lang-bash singleline">$ bower install</code></pre>
<p>We also need a <code>Procfile</code> so we can run it on Heroku:</p>
<pre><code class="hljs lang-bash singleline">web: node index.js</code></pre>
<p>Now, let’s create the main file:</p>
<pre><code class="hljs lang-bash singleline">$ touch index.js</code></pre>
<p>And our test file:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ mkdir <span class="hljs-built_in">test</span>
<span class="linenos">2</span>	$ touch <span class="hljs-built_in">test</span>/test.js</code></pre>
<h2 id="implementing-the-chat-server">Implementing the chat server</h2>
<p>Next, let’s implement our first test. First of all, we’ll verify that the index route works:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	<span class="hljs-comment">/*jslint node: true */</span>
<span class="linenos">2</span>	<span class="hljs-comment">/*global describe: false, before: false, after: false, it: false */</span>
<span class="linenos">3</span>	<span class="hljs-pi">"use strict"</span>;
<span class="linenos">4</span>	
<span class="linenos">5</span>	<span class="hljs-comment">// Declare the variables used</span>
<span class="linenos">6</span>	<span class="hljs-keyword">var</span> expect = <span class="hljs-built_in">require</span>(<span class="hljs-string">'chai'</span>).expect,
<span class="linenos">7</span>	    request = <span class="hljs-built_in">require</span>(<span class="hljs-string">'request'</span>),
<span class="linenos">8</span>	    server = <span class="hljs-built_in">require</span>(<span class="hljs-string">'../index'</span>),
<span class="linenos">9</span>	    redis = <span class="hljs-built_in">require</span>(<span class="hljs-string">'redis'</span>),
<span class="linenos">10</span>	    io = <span class="hljs-built_in">require</span>(<span class="hljs-string">'socket.io-client'</span>),
<span class="linenos">11</span>	    client;
<span class="linenos">12</span>	client = redis.createClient();
<span class="linenos">13</span>	
<span class="linenos">14</span>	<span class="hljs-comment">// Server tasks</span>
<span class="linenos">15</span>	describe(<span class="hljs-string">'server'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">()</span> </span>{
<span class="linenos">16</span>	
<span class="linenos">17</span>	    <span class="hljs-comment">// Beforehand, start the server</span>
<span class="linenos">18</span>	    before(<span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(done)</span> </span>{
<span class="linenos">19</span>	        <span class="hljs-built_in">console</span>.log(<span class="hljs-string">'Starting the server'</span>);
<span class="linenos">20</span>	        done();
<span class="linenos">21</span>	    });
<span class="linenos">22</span>	
<span class="linenos">23</span>	    <span class="hljs-comment">// Afterwards, stop the server and empty the database</span>
<span class="linenos">24</span>	    after(<span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(done)</span> </span>{
<span class="linenos">25</span>	        <span class="hljs-built_in">console</span>.log(<span class="hljs-string">'Stopping the server'</span>);
<span class="linenos">26</span>	        client.flushdb();
<span class="linenos">27</span>	        done();
<span class="linenos">28</span>	    });
<span class="linenos">29</span>	
<span class="linenos">30</span>	    <span class="hljs-comment">// Test the index route</span>
<span class="linenos">31</span>	    describe(<span class="hljs-string">'Test the index route'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">()</span> </span>{
<span class="linenos">32</span>	        it(<span class="hljs-string">'should return a page with the title Babblr'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(done)</span> </span>{
<span class="linenos">33</span>	            request.get({ url: <span class="hljs-string">'http://localhost:5000/'</span> }, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(error, response, body)</span> </span>{
<span class="linenos">34</span>	                expect(body).to.include(<span class="hljs-string">'Babblr'</span>);
<span class="linenos">35</span>	                expect(response.statusCode).to.equal(<span class="hljs-number">200</span>);
<span class="linenos">36</span>	                expect(response.headers[<span class="hljs-string">'content-type'</span>]).to.equal(<span class="hljs-string">'text/html; charset=utf-8'</span>);
<span class="linenos">37</span>	                done();
<span class="linenos">38</span>	            });
<span class="linenos">39</span>	        });
<span class="linenos">40</span>	    });
<span class="linenos">41</span>	});</code></pre>
<p>Note that this is very similar to the first test for the URL shortener, because it’s doing basically the same thing.</p>
<p>Now, run the test and make sure it fails:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ grunt <span class="hljs-built_in">test</span>
<span class="linenos">2</span>	Running <span class="hljs-string">"jshint:all"</span> (jshint) task
<span class="linenos">3</span>	&gt;&gt; <span class="hljs-number">2</span> files lint free.
<span class="linenos">4</span>	
<span class="linenos">5</span>	Running <span class="hljs-string">"mocha_istanbul:coverage"</span> (mocha_istanbul) task
<span class="linenos">6</span>	
<span class="linenos">7</span>	
<span class="linenos">8</span>	  server
<span class="linenos">9</span>	Starting the server
<span class="linenos">10</span>	    Test the index route
<span class="linenos">11</span>	      <span class="hljs-number">1</span>) should <span class="hljs-built_in">return</span> a page with the title Babblr
<span class="linenos">12</span>	Stopping the server
<span class="linenos">13</span>	
<span class="linenos">14</span>	
<span class="linenos">15</span>	  <span class="hljs-number">0</span> passing (<span class="hljs-number">873</span>ms)
<span class="linenos">16</span>	  <span class="hljs-number">1</span> failing
<span class="linenos">17</span>	
<span class="linenos">18</span>	  <span class="hljs-number">1</span>) server Test the index route should <span class="hljs-built_in">return</span> a page with the title Babblr:
<span class="linenos">19</span>	     Uncaught AssertionError: expected undefined to include <span class="hljs-string">'Babblr'</span>
<span class="linenos">20</span>	      at Request._callback (/Users/matthewdaly/Projects/babblr/<span class="hljs-built_in">test</span>/test.js:<span class="hljs-number">34</span>:<span class="hljs-number">33</span>)
<span class="linenos">21</span>	      at self.callback (/Users/matthewdaly/Projects/babblr/node_modules/request/request.js:<span class="hljs-number">373</span>:<span class="hljs-number">22</span>)
<span class="linenos">22</span>	      at Request.emit (events.js:<span class="hljs-number">95</span>:<span class="hljs-number">17</span>)
<span class="linenos">23</span>	      at Request.onRequestError (/Users/matthewdaly/Projects/babblr/node_modules/request/request.js:<span class="hljs-number">971</span>:<span class="hljs-number">8</span>)
<span class="linenos">24</span>	      at ClientRequest.emit (events.js:<span class="hljs-number">95</span>:<span class="hljs-number">17</span>)
<span class="linenos">25</span>	      at Socket.socketErrorListener (http.js:<span class="hljs-number">1552</span>:<span class="hljs-number">9</span>)
<span class="linenos">26</span>	      at Socket.emit (events.js:<span class="hljs-number">95</span>:<span class="hljs-number">17</span>)
<span class="linenos">27</span>	      at net.js:<span class="hljs-number">441</span>:<span class="hljs-number">14</span>
<span class="linenos">28</span>	      at process._tickCallback (node.js:<span class="hljs-number">442</span>:<span class="hljs-number">13</span>)
<span class="linenos">29</span>	
<span class="linenos">30</span>	
<span class="linenos">31</span>	
<span class="linenos">32</span>	=============================================================================
<span class="linenos">33</span>	Writing coverage object [/Users/matthewdaly/Projects/babblr/coverage/coverage.json]
<span class="linenos">34</span>	Writing coverage reports at [/Users/matthewdaly/Projects/babblr/coverage]
<span class="linenos">35</span>	=============================================================================
<span class="linenos">36</span>	
<span class="linenos">37</span>	=============================== Coverage summary ===============================
<span class="linenos">38</span>	Statements   : <span class="hljs-number">100</span>% ( <span class="hljs-number">0</span>/<span class="hljs-number">0</span> )
<span class="linenos">39</span>	Branches     : <span class="hljs-number">100</span>% ( <span class="hljs-number">0</span>/<span class="hljs-number">0</span> )
<span class="linenos">40</span>	Functions    : <span class="hljs-number">100</span>% ( <span class="hljs-number">0</span>/<span class="hljs-number">0</span> )
<span class="linenos">41</span>	Lines        : <span class="hljs-number">100</span>% ( <span class="hljs-number">0</span>/<span class="hljs-number">0</span> )
<span class="linenos">42</span>	================================================================================
<span class="linenos">43</span>	&gt;&gt;
<span class="linenos">44</span>	Warning: Task <span class="hljs-string">"mocha_istanbul:coverage"</span> failed. Use --force to continue.
<span class="linenos">45</span>	
<span class="linenos">46</span>	Aborted due to warnings.</code></pre>
<p>With that confirmed, we can start writing code to make the test pass:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	<span class="hljs-comment">/*jslint node: true */</span>
<span class="linenos">2</span>	<span class="hljs-pi">'use strict'</span>;
<span class="linenos">3</span>	
<span class="linenos">4</span>	<span class="hljs-comment">// Declare variables used</span>
<span class="linenos">5</span>	<span class="hljs-keyword">var</span> app, base_url, client, express, hbs, io, port, rtg, subscribe;
<span class="linenos">6</span>	
<span class="linenos">7</span>	<span class="hljs-comment">// Define values</span>
<span class="linenos">8</span>	express = <span class="hljs-built_in">require</span>(<span class="hljs-string">'express'</span>);
<span class="linenos">9</span>	app = express();
<span class="linenos">10</span>	port = process.env.PORT || <span class="hljs-number">5000</span>;
<span class="linenos">11</span>	base_url = process.env.BASE_URL || <span class="hljs-string">'http://localhost:5000'</span>;
<span class="linenos">12</span>	hbs = <span class="hljs-built_in">require</span>(<span class="hljs-string">'hbs'</span>);
<span class="linenos">13</span>	
<span class="linenos">14</span>	<span class="hljs-comment">// Set up connection to Redis</span>
<span class="linenos">15</span>	<span class="hljs-comment">/* istanbul ignore if */</span>
<span class="linenos">16</span>	<span class="hljs-keyword">if</span> (process.env.REDISTOGO_URL) {
<span class="linenos">17</span>	    rtg  = <span class="hljs-built_in">require</span>(<span class="hljs-string">"url"</span>).parse(process.env.REDISTOGO_URL);
<span class="linenos">18</span>	    client = <span class="hljs-built_in">require</span>(<span class="hljs-string">"redis"</span>).createClient(rtg.port, rtg.hostname);
<span class="linenos">19</span>	    subscribe = <span class="hljs-built_in">require</span>(<span class="hljs-string">"redis"</span>).createClient(rtg.port, rtg.hostname);
<span class="linenos">20</span>	    client.auth(rtg.auth.split(<span class="hljs-string">":"</span>)[<span class="hljs-number">1</span>]);
<span class="linenos">21</span>	    subscribe.auth(rtg.auth.split(<span class="hljs-string">":"</span>)[<span class="hljs-number">1</span>]);
<span class="linenos">22</span>	} <span class="hljs-keyword">else</span> {
<span class="linenos">23</span>	    client = <span class="hljs-built_in">require</span>(<span class="hljs-string">'redis'</span>).createClient();
<span class="linenos">24</span>	    subscribe = <span class="hljs-built_in">require</span>(<span class="hljs-string">'redis'</span>).createClient();
<span class="linenos">25</span>	}
<span class="linenos">26</span>	
<span class="linenos">27</span>	<span class="hljs-comment">// Set up templating</span>
<span class="linenos">28</span>	app.set(<span class="hljs-string">'views'</span>, __dirname + <span class="hljs-string">'/views'</span>);
<span class="linenos">29</span>	app.set(<span class="hljs-string">'view engine'</span>, <span class="hljs-string">"hbs"</span>);
<span class="linenos">30</span>	app.engine(<span class="hljs-string">'hbs'</span>, <span class="hljs-built_in">require</span>(<span class="hljs-string">'hbs'</span>).__express);
<span class="linenos">31</span>	
<span class="linenos">32</span>	<span class="hljs-comment">// Register partials</span>
<span class="linenos">33</span>	hbs.registerPartials(__dirname + <span class="hljs-string">'/views/partials'</span>);
<span class="linenos">34</span>	
<span class="linenos">35</span>	<span class="hljs-comment">// Set URL</span>
<span class="linenos">36</span>	app.set(<span class="hljs-string">'base_url'</span>, base_url);
<span class="linenos">37</span>	
<span class="linenos">38</span>	<span class="hljs-comment">// Define index route</span>
<span class="linenos">39</span>	app.get(<span class="hljs-string">'/'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(req, res)</span> </span>{
<span class="linenos">40</span>	    res.render(<span class="hljs-string">'index'</span>);
<span class="linenos">41</span>	});
<span class="linenos">42</span>	
<span class="linenos">43</span>	<span class="hljs-comment">// Serve static files</span>
<span class="linenos">44</span>	app.use(express.static(__dirname + <span class="hljs-string">'/static'</span>));
<span class="linenos">45</span>	
<span class="linenos">46</span>	<span class="hljs-comment">// Listen</span>
<span class="linenos">47</span>	io = <span class="hljs-built_in">require</span>(<span class="hljs-string">'socket.io'</span>)({
<span class="linenos">48</span>	}).listen(app.listen(port));
<span class="linenos">49</span>	<span class="hljs-built_in">console</span>.log(<span class="hljs-string">"Listening on port "</span> + port);</code></pre>
<p>If you compare this to the code for the URL shortener, you’ll notice a few fairly substantial differences. For one thing, we set up two Redis connections, not one - that’s because we need to do so when using Pub/Sub with Redis. You’ll also notice that we register Handlebars (<code>hbs</code>) rather than Jade, and define not just a directory for views, but another directory inside it for partials. Finally, setting it up to listen at the end is a bit more involved because we’ll be using Socket.IO.</p>
<p>Now, you can run your tests again at this point, but they won’t pass because we haven’t created our views. So let’s do that. Create the directory <code>views</code> and the subdirectory <code>partials</code> inside it. Then add the following content to <code>views/index.hbs</code>:</p>
<pre><code class="hljs lang-hbs"><span class="linenos">1</span>	<span class="xml"></span><span class="hljs-expression">{{&gt; <span class="hljs-variable">header</span> }}</span><span class="xml">
<span class="linenos">2</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"container"</span>&gt;</span>
<span class="linenos">3</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"row"</span>&gt;</span>
<span class="linenos">4</span>	                <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"col-md-8"</span>&gt;</span>
<span class="linenos">5</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"conversation"</span>&gt;</span>
<span class="linenos">6</span>	                    <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">7</span>	                <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">8</span>	                <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"col-md-4"</span>&gt;</span>
<span class="linenos">9</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">form</span>&gt;</span>
<span class="linenos">10</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"form-group"</span>&gt;</span>
<span class="linenos">11</span>	                            <span class="hljs-tag">&lt;<span class="hljs-title">label</span> <span class="hljs-attribute">for</span>=<span class="hljs-value">"message"</span>&gt;</span>Message<span class="hljs-tag">&lt;/<span class="hljs-title">label</span>&gt;</span>
<span class="linenos">12</span>	                            <span class="hljs-tag">&lt;<span class="hljs-title">textarea</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"form-control"</span> <span class="hljs-attribute">id</span>=<span class="hljs-value">"message"</span> <span class="hljs-attribute">rows</span>=<span class="hljs-value">"20"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">textarea</span>&gt;</span>
<span class="linenos">13</span>	                            <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">id</span>=<span class="hljs-value">"submitbutton"</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"btn btn-primary form-control"</span>&gt;</span>Submit<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">14</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">15</span>	                    <span class="hljs-tag">&lt;/<span class="hljs-title">form</span>&gt;</span>
<span class="linenos">16</span>	                <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">17</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">18</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">19</span>	</span><span class="hljs-expression">{{&gt; <span class="hljs-variable">footer</span> }}</span><span class="xml"></span></code></pre>
<p>Add this to <code>views/partials/header.hbs</code>:</p>
<pre><code class="hljs lang-hbs"><span class="linenos">1</span>	<span class="hljs-doctype">&lt;!DOCTYPE html&gt;</span>
<span class="linenos">2</span>	<span class="hljs-comment">&lt;!--[if lt IE 7]&gt;      &lt;html class="no-js lt-ie9 lt-ie8 lt-ie7"&gt; &lt;![endif]--&gt;</span>
<span class="linenos">3</span>	<span class="hljs-comment">&lt;!--[if IE 7]&gt;         &lt;html class="no-js lt-ie9 lt-ie8"&gt; &lt;![endif]--&gt;</span>
<span class="linenos">4</span>	<span class="hljs-comment">&lt;!--[if IE 8]&gt;         &lt;html class="no-js lt-ie9"&gt; &lt;![endif]--&gt;</span>
<span class="linenos">5</span>	<span class="hljs-comment">&lt;!--[if gt IE 8]&gt;&lt;!--&gt;</span> <span class="hljs-tag">&lt;<span class="hljs-title">html</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"no-js"</span>&gt;</span> <span class="hljs-comment">&lt;!--&lt;![endif]--&gt;</span>
<span class="linenos">6</span>	    <span class="hljs-tag">&lt;<span class="hljs-title">head</span>&gt;</span>
<span class="linenos">7</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">meta</span> <span class="hljs-attribute">charset</span>=<span class="hljs-value">"utf-8"</span>&gt;</span>
<span class="linenos">8</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">meta</span> <span class="hljs-attribute">http-equiv</span>=<span class="hljs-value">"X-UA-Compatible"</span> <span class="hljs-attribute">content</span>=<span class="hljs-value">"IE=edge"</span>&gt;</span>
<span class="linenos">9</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">title</span>&gt;</span>Babblr<span class="hljs-tag">&lt;/<span class="hljs-title">title</span>&gt;</span>
<span class="linenos">10</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">meta</span> <span class="hljs-attribute">name</span>=<span class="hljs-value">"description"</span> <span class="hljs-attribute">content</span>=<span class="hljs-value">""</span>&gt;</span>
<span class="linenos">11</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">meta</span> <span class="hljs-attribute">name</span>=<span class="hljs-value">"viewport"</span> <span class="hljs-attribute">content</span>=<span class="hljs-value">"width=device-width, initial-scale=1"</span>&gt;</span>
<span class="linenos">12</span>	
<span class="linenos">13</span>	        <span class="hljs-comment">&lt;!-- Place favicon.ico and apple-touch-icon.png in the root directory --&gt;</span>
<span class="linenos">14</span>	
<span class="linenos">15</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/bower_components/bootstrap/dist/css/bootstrap.min.css"</span>&gt;</span>
<span class="linenos">16</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/bower_components/bootstrap/dist/css/bootstrap-theme.min.css"</span>&gt;</span>
<span class="linenos">17</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/css/style.css"</span>&gt;</span>
<span class="linenos">18</span>	    <span class="hljs-tag">&lt;/<span class="hljs-title">head</span>&gt;</span>
<span class="linenos">19</span>	    <span class="hljs-tag">&lt;<span class="hljs-title">body</span>&gt;</span>
<span class="linenos">20</span>	        <span class="hljs-comment">&lt;!--[if lt IE 7]&gt;
<span class="linenos">21</span>	            &lt;p class="browsehappy"&gt;You are using an &lt;strong&gt;outdated&lt;/strong&gt; browser. Please &lt;a href="http://browsehappy.com/"&gt;upgrade your browser&lt;/a&gt; to improve your experience.&lt;/p&gt;
<span class="linenos">22</span>	        &lt;![endif]--&gt;</span>
<span class="linenos">23</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">nav</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"navbar navbar-inverse navbar-static-top"</span> <span class="hljs-attribute">role</span>=<span class="hljs-value">"navigation"</span>&gt;</span>
<span class="linenos">24</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"container"</span>&gt;</span>
<span class="linenos">25</span>	                <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"navbar-header"</span>&gt;</span>
<span class="linenos">26</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"navbar-brand"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"#"</span>&gt;</span>Babblr<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">27</span>	                <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">28</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">29</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">nav</span>&gt;</span></code></pre>
<p>And add this to <code>views/partials/footer.hbs</code>:</p>
<pre><code class="hljs lang-hbs"><span class="linenos">1</span>	
<span class="linenos">2</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span> <span class="hljs-attribute">src</span>=<span class="hljs-value">"/bower_components/jquery/dist/jquery.min.js"</span>&gt;</span><span class="undefined"></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">3</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span> <span class="hljs-attribute">src</span>=<span class="hljs-value">"/bower_components/bootstrap/dist/js/bootstrap.min.js"</span>&gt;</span><span class="undefined"></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">4</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span> <span class="hljs-attribute">src</span>=<span class="hljs-value">"/socket.io/socket.io.js"</span>&gt;</span><span class="undefined"></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">5</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span> <span class="hljs-attribute">src</span>=<span class="hljs-value">"/js/main.js"</span>&gt;</span><span class="undefined"></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">6</span>	
<span class="linenos">7</span>	    <span class="hljs-tag">&lt;/<span class="hljs-title">body</span>&gt;</span>
<span class="linenos">8</span>	<span class="hljs-tag">&lt;/<span class="hljs-title">html</span>&gt;</span></code></pre>
<p>You’ll also want to create placeholder CSS and JavaScript files:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ mkdir static/js
<span class="linenos">2</span>	$ mkdir static/css
<span class="linenos">3</span>	$ touch static/js/main.js
<span class="linenos">4</span>	$ touch static/css/style.css</code></pre>
<p>The test should now pass:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ grunt <span class="hljs-built_in">test</span>
<span class="linenos">2</span>	Running <span class="hljs-string">"jshint:all"</span> (jshint) task
<span class="linenos">3</span>	&gt;&gt; <span class="hljs-number">2</span> files lint free.
<span class="linenos">4</span>	
<span class="linenos">5</span>	Running <span class="hljs-string">"mocha_istanbul:coverage"</span> (mocha_istanbul) task
<span class="linenos">6</span>	Listening on port <span class="hljs-number">5000</span>
<span class="linenos">7</span>	
<span class="linenos">8</span>	
<span class="linenos">9</span>	  server
<span class="linenos">10</span>	Starting the server
<span class="linenos">11</span>	    Test the index route
<span class="linenos">12</span>	      ✓ should <span class="hljs-built_in">return</span> a page with the title Babblr (<span class="hljs-number">41</span>ms)
<span class="linenos">13</span>	Stopping the server
<span class="linenos">14</span>	
<span class="linenos">15</span>	
<span class="linenos">16</span>	  <span class="hljs-number">1</span> passing (<span class="hljs-number">54</span>ms)
<span class="linenos">17</span>	
<span class="linenos">18</span>	=============================================================================
<span class="linenos">19</span>	Writing coverage object [/Users/matthewdaly/Projects/babblr/coverage/coverage.json]
<span class="linenos">20</span>	Writing coverage reports at [/Users/matthewdaly/Projects/babblr/coverage]
<span class="linenos">21</span>	=============================================================================
<span class="linenos">22</span>	
<span class="linenos">23</span>	=============================== Coverage summary ===============================
<span class="linenos">24</span>	Statements   : <span class="hljs-number">100</span>% ( <span class="hljs-number">24</span>/<span class="hljs-number">24</span> ), <span class="hljs-number">5</span> ignored
<span class="linenos">25</span>	Branches     : <span class="hljs-number">100</span>% ( <span class="hljs-number">6</span>/<span class="hljs-number">6</span> ), <span class="hljs-number">1</span> ignored
<span class="linenos">26</span>	Functions    : <span class="hljs-number">100</span>% ( <span class="hljs-number">1</span>/<span class="hljs-number">1</span> )
<span class="linenos">27</span>	Lines        : <span class="hljs-number">100</span>% ( <span class="hljs-number">24</span>/<span class="hljs-number">24</span> )
<span class="linenos">28</span>	================================================================================
<span class="linenos">29</span>	&gt;&gt; Done. Check coverage folder.
<span class="linenos">30</span>	
<span class="linenos">31</span>	Running <span class="hljs-string">"coveralls:app"</span> (coveralls) task
<span class="linenos">32</span>	&gt;&gt; Failed to submit <span class="hljs-string">'coverage/lcov.info'</span> to coveralls: Bad response: <span class="hljs-number">422</span> {<span class="hljs-string">"message"</span>:<span class="hljs-string">"Couldn't find a repository matching this job."</span>,<span class="hljs-string">"error"</span>:<span class="hljs-literal">true</span>}
<span class="linenos">33</span>	&gt;&gt; Failed to submit coverage results to coveralls
<span class="linenos">34</span>	Warning: Task <span class="hljs-string">"coveralls:app"</span> failed. Use --force to continue.
<span class="linenos">35</span>	
<span class="linenos">36</span>	Aborted due to warnings.</code></pre>
<p>Don’t worry about the coveralls task failing, as that only needs to pass when it runs on Travis CI.</p>
<p>So we now have our main route in place. The next step is to actually implement the chat functionality. Add this code to the test file:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	
<span class="linenos">2</span>	    <span class="hljs-comment">// Test sending a message</span>
<span class="linenos">3</span>	    describe(<span class="hljs-string">'Test sending a message'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">()</span> </span>{
<span class="linenos">4</span>	        it(<span class="hljs-string">"should return 'Message received'"</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(done)</span> </span>{
<span class="linenos">5</span>	            <span class="hljs-comment">// Connect to server</span>
<span class="linenos">6</span>	            <span class="hljs-keyword">var</span> socket = io.connect(<span class="hljs-string">'http://localhost:5000'</span>, {
<span class="linenos">7</span>	                <span class="hljs-string">'reconnection delay'</span> : <span class="hljs-number">0</span>,
<span class="linenos">8</span>	                <span class="hljs-string">'reopen delay'</span> : <span class="hljs-number">0</span>,
<span class="linenos">9</span>	                <span class="hljs-string">'force new connection'</span> : <span class="hljs-literal">true</span>
<span class="linenos">10</span>	            });
<span class="linenos">11</span>	
<span class="linenos">12</span>	            <span class="hljs-comment">// Handle the message being received</span>
<span class="linenos">13</span>	            socket.on(<span class="hljs-string">'message'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(data)</span> </span>{
<span class="linenos">14</span>	                expect(data).to.include(<span class="hljs-string">'Message received'</span>);
<span class="linenos">15</span>	                socket.disconnect();
<span class="linenos">16</span>	                done();
<span class="linenos">17</span>	            });
<span class="linenos">18</span>	
<span class="linenos">19</span>	            <span class="hljs-comment">// Send the message</span>
<span class="linenos">20</span>	            socket.emit(<span class="hljs-string">'send'</span>, { message: <span class="hljs-string">'Message received'</span> });
<span class="linenos">21</span>	        });
<span class="linenos">22</span>	    });</code></pre>
<p>This code should be fairly straightforward to understand. First, we connect to the server. Then, we set up a handler to verify the content of the message when it gets sent. Finally, we send the message. Let’s run the tests to make sure we get the expected result:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ grunt <span class="hljs-built_in">test</span>
<span class="linenos">2</span>	Running <span class="hljs-string">"jshint:all"</span> (jshint) task
<span class="linenos">3</span>	&gt;&gt; <span class="hljs-number">2</span> files lint free.
<span class="linenos">4</span>	
<span class="linenos">5</span>	Running <span class="hljs-string">"mocha_istanbul:coverage"</span> (mocha_istanbul) task
<span class="linenos">6</span>	Listening on port <span class="hljs-number">5000</span>
<span class="linenos">7</span>	
<span class="linenos">8</span>	
<span class="linenos">9</span>	  server
<span class="linenos">10</span>	Starting the server
<span class="linenos">11</span>	    Test the index route
<span class="linenos">12</span>	      ✓ should <span class="hljs-built_in">return</span> a page with the title Babblr (<span class="hljs-number">337</span>ms)
<span class="linenos">13</span>	    Test sending a message
<span class="linenos">14</span>	      <span class="hljs-number">1</span>) should <span class="hljs-built_in">return</span> <span class="hljs-string">'Message received'</span>
<span class="linenos">15</span>	Stopping the server
<span class="linenos">16</span>	
<span class="linenos">17</span>	
<span class="linenos">18</span>	  <span class="hljs-number">1</span> passing (<span class="hljs-number">2</span>s)
<span class="linenos">19</span>	  <span class="hljs-number">1</span> failing
<span class="linenos">20</span>	
<span class="linenos">21</span>	  <span class="hljs-number">1</span>) server Test sending a message should <span class="hljs-built_in">return</span> <span class="hljs-string">'Message received'</span>:
<span class="linenos">22</span>	     Error: timeout of <span class="hljs-number">2000</span>ms exceeded
<span class="linenos">23</span>	      at null.&lt;anonymous&gt; (/Users/matthewdaly/Projects/babblr/node_modules/mocha/lib/runnable.js:<span class="hljs-number">159</span>:<span class="hljs-number">19</span>)
<span class="linenos">24</span>	      at Timer.listOnTimeout [as ontimeout] (timers.js:<span class="hljs-number">112</span>:<span class="hljs-number">15</span>)
<span class="linenos">25</span>	
<span class="linenos">26</span>	
<span class="linenos">27</span>	
<span class="linenos">28</span>	=============================================================================
<span class="linenos">29</span>	Writing coverage object [/Users/matthewdaly/Projects/babblr/coverage/coverage.json]
<span class="linenos">30</span>	Writing coverage reports at [/Users/matthewdaly/Projects/babblr/coverage]
<span class="linenos">31</span>	=============================================================================
<span class="linenos">32</span>	
<span class="linenos">33</span>	=============================== Coverage summary ===============================
<span class="linenos">34</span>	Statements   : <span class="hljs-number">100</span>% ( <span class="hljs-number">24</span>/<span class="hljs-number">24</span> ), <span class="hljs-number">5</span> ignored
<span class="linenos">35</span>	Branches     : <span class="hljs-number">100</span>% ( <span class="hljs-number">6</span>/<span class="hljs-number">6</span> ), <span class="hljs-number">1</span> ignored
<span class="linenos">36</span>	Functions    : <span class="hljs-number">100</span>% ( <span class="hljs-number">1</span>/<span class="hljs-number">1</span> )
<span class="linenos">37</span>	Lines        : <span class="hljs-number">100</span>% ( <span class="hljs-number">24</span>/<span class="hljs-number">24</span> )
<span class="linenos">38</span>	================================================================================
<span class="linenos">39</span>	&gt;&gt;
<span class="linenos">40</span>	Warning: Task <span class="hljs-string">"mocha_istanbul:coverage"</span> failed. Use --force to continue.
<span class="linenos">41</span>	
<span class="linenos">42</span>	Aborted due to warnings.</code></pre>
<p>Now, let’s implement this functionality. Add this at the end of <code>index.js</code>:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	<span class="hljs-comment">// Handle new messages</span>
<span class="linenos">2</span>	io.sockets.on(<span class="hljs-string">'connection'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(socket)</span> </span>{
<span class="linenos">3</span>	    <span class="hljs-comment">// Subscribe to the Redis channel</span>
<span class="linenos">4</span>	    subscribe.subscribe(<span class="hljs-string">'ChatChannel'</span>);
<span class="linenos">5</span>	
<span class="linenos">6</span>	    <span class="hljs-comment">// Handle incoming messages</span>
<span class="linenos">7</span>	    socket.on(<span class="hljs-string">'send'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(data)</span> </span>{
<span class="linenos">8</span>	        <span class="hljs-comment">// Publish it</span>
<span class="linenos">9</span>	        client.publish(<span class="hljs-string">'ChatChannel'</span>, data.message);
<span class="linenos">10</span>	    });
<span class="linenos">11</span>	
<span class="linenos">12</span>	    <span class="hljs-comment">// Handle receiving messages</span>
<span class="linenos">13</span>	    <span class="hljs-keyword">var</span> callback = <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(channel, data)</span> </span>{
<span class="linenos">14</span>	        socket.emit(<span class="hljs-string">'message'</span>, data);
<span class="linenos">15</span>	    };
<span class="linenos">16</span>	    subscribe.on(<span class="hljs-string">'message'</span>, callback);
<span class="linenos">17</span>	
<span class="linenos">18</span>	    <span class="hljs-comment">// Handle disconnect</span>
<span class="linenos">19</span>	    socket.on(<span class="hljs-string">'disconnect'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">()</span> </span>{
<span class="linenos">20</span>	        subscribe.removeListener(<span class="hljs-string">'message'</span>, callback);
<span class="linenos">21</span>	    });
<span class="linenos">22</span>	});</code></pre>
<p>We’ll go through this. First, we create a callback for when a new connection is received. Inside the callback, we then subscribe to a Pub/Sub channel in Redis called <code>ChatChannel</code>.</p>
<p>Then, we define another callback so that on a <code>send</code> event from Socket.IO, we get the message and publish it to <code>ChatChannel</code>. After that, we define another callback to handle receiving messages, and set it to run when a new message is published to <code>ChatChannel</code>. Finally, we set up a callback to handle removing the listener when a user disconnects.</p>
<p>Note the two different connections to Redis - <code>client</code> and <code>subscribe</code>. As mentioned earlier, you need to use two connections to Redis when using Pub/Sub. This is because a client subscribed to one or more channels should not issue commands, so we use <code>subscribe</code> as a dedicated connection to handle subscriptions, and use <code>client</code> to publish new messages.</p>
<p>We’ll also need a bit of client-side JavaScript to handle sending and receiving messages. Amend <code>main.js</code> as follows:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	$(<span class="hljs-built_in">document</span>).ready(<span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">()</span> </span>{
<span class="linenos">2</span>	<span class="hljs-pi">    'use strict'</span>;
<span class="linenos">3</span>	
<span class="linenos">4</span>	    <span class="hljs-comment">// Set up the connection</span>
<span class="linenos">5</span>	    <span class="hljs-keyword">var</span> field, socket, output;
<span class="linenos">6</span>	    socket = io.connect(<span class="hljs-built_in">window</span>.location.href);
<span class="linenos">7</span>	
<span class="linenos">8</span>	    <span class="hljs-comment">// Get a reference to the input</span>
<span class="linenos">9</span>	    field = $(<span class="hljs-string">'textarea#message'</span>);
<span class="linenos">10</span>	
<span class="linenos">11</span>	    <span class="hljs-comment">// Get a reference to the output</span>
<span class="linenos">12</span>	    output = $(<span class="hljs-string">'div.conversation'</span>);
<span class="linenos">13</span>	
<span class="linenos">14</span>	    <span class="hljs-comment">// Handle message submit</span>
<span class="linenos">15</span>	    $(<span class="hljs-string">'a#submitbutton'</span>).on(<span class="hljs-string">'click'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">()</span> </span>{
<span class="linenos">16</span>	        <span class="hljs-comment">// Create the message</span>
<span class="linenos">17</span>	        <span class="hljs-keyword">var</span> msg;
<span class="linenos">18</span>	        msg = field.val();
<span class="linenos">19</span>	        socket.emit(<span class="hljs-string">'send'</span>, { message: msg });
<span class="linenos">20</span>	        field.val(<span class="hljs-string">''</span>);
<span class="linenos">21</span>	    });
<span class="linenos">22</span>	
<span class="linenos">23</span>	    <span class="hljs-comment">// Handle incoming messages</span>
<span class="linenos">24</span>	    socket.on(<span class="hljs-string">'message'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(data)</span> </span>{
<span class="linenos">25</span>	        <span class="hljs-comment">// Insert the message</span>
<span class="linenos">26</span>	        output.append(<span class="hljs-string">'&lt;p&gt;Anonymous Coward : '</span> + data + <span class="hljs-string">'&lt;/p&gt;'</span>);
<span class="linenos">27</span>	    });
<span class="linenos">28</span>	});</code></pre>
<p>Here we have one callback that handles sending messages, and another that handles receiving messages. Note that every message will be preceded with Anonymous Coward - we won’t implement user names at this point (though I plan it for a future instalment).</p>
<p>We’ll also add a little bit of additional styling:</p>
<pre><code class="hljs lang-css"><span class="linenos">1</span>	<span class="hljs-tag">div</span><span class="hljs-class">.conversation</span> <span class="hljs-rules">{
<span class="linenos">2</span>	    <span class="hljs-rule"><span class="hljs-attribute">height</span>:<span class="hljs-value"> <span class="hljs-number">500px</span></span></span>;
<span class="linenos">3</span>	    <span class="hljs-rule"><span class="hljs-attribute">overflow-y</span>:<span class="hljs-value"> scroll</span></span>;
<span class="linenos">4</span>	    <span class="hljs-rule"><span class="hljs-attribute">border</span>:<span class="hljs-value"> <span class="hljs-number">1px</span> solid <span class="hljs-hexcolor">#000</span></span></span>;
<span class="linenos">5</span>	    <span class="hljs-rule"><span class="hljs-attribute">padding</span>:<span class="hljs-value"> <span class="hljs-number">10px</span></span></span>;
<span class="linenos">6</span>	}</span></code></pre>
<p>Now, if you run your tests, they should pass:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ grunt <span class="hljs-built_in">test</span>
<span class="linenos">2</span>	Running <span class="hljs-string">"jshint:all"</span> (jshint) task
<span class="linenos">3</span>	&gt;&gt; <span class="hljs-number">2</span> files lint free.
<span class="linenos">4</span>	
<span class="linenos">5</span>	Running <span class="hljs-string">"mocha_istanbul:coverage"</span> (mocha_istanbul) task
<span class="linenos">6</span>	Listening on port <span class="hljs-number">5000</span>
<span class="linenos">7</span>	
<span class="linenos">8</span>	
<span class="linenos">9</span>	  server
<span class="linenos">10</span>	Starting the server
<span class="linenos">11</span>	    Test the index route
<span class="linenos">12</span>	      ✓ should <span class="hljs-built_in">return</span> a page with the title Babblr (<span class="hljs-number">40</span>ms)
<span class="linenos">13</span>	    Test sending a message
<span class="linenos">14</span>	      ✓ should <span class="hljs-built_in">return</span> <span class="hljs-string">'Message received'</span> (<span class="hljs-number">45</span>ms)
<span class="linenos">15</span>	Stopping the server
<span class="linenos">16</span>	
<span class="linenos">17</span>	
<span class="linenos">18</span>	  <span class="hljs-number">2</span> passing (<span class="hljs-number">101</span>ms)
<span class="linenos">19</span>	
<span class="linenos">20</span>	=============================================================================
<span class="linenos">21</span>	Writing coverage object [/Users/matthewdaly/Projects/babblr/coverage/coverage.json]
<span class="linenos">22</span>	Writing coverage reports at [/Users/matthewdaly/Projects/babblr/coverage]
<span class="linenos">23</span>	=============================================================================
<span class="linenos">24</span>	
<span class="linenos">25</span>	=============================== Coverage summary ===============================
<span class="linenos">26</span>	Statements   : <span class="hljs-number">100</span>% ( <span class="hljs-number">33</span>/<span class="hljs-number">33</span> ), <span class="hljs-number">5</span> ignored
<span class="linenos">27</span>	Branches     : <span class="hljs-number">100</span>% ( <span class="hljs-number">6</span>/<span class="hljs-number">6</span> ), <span class="hljs-number">1</span> ignored
<span class="linenos">28</span>	Functions    : <span class="hljs-number">100</span>% ( <span class="hljs-number">5</span>/<span class="hljs-number">5</span> )
<span class="linenos">29</span>	Lines        : <span class="hljs-number">100</span>% ( <span class="hljs-number">33</span>/<span class="hljs-number">33</span> )
<span class="linenos">30</span>	================================================================================
<span class="linenos">31</span>	&gt;&gt; Done. Check coverage folder.
<span class="linenos">32</span>	
<span class="linenos">33</span>	Running <span class="hljs-string">"coveralls:app"</span> (coveralls) task
<span class="linenos">34</span>	&gt;&gt; Failed to submit <span class="hljs-string">'coverage/lcov.info'</span> to coveralls: Bad response: <span class="hljs-number">422</span> {<span class="hljs-string">"message"</span>:<span class="hljs-string">"Couldn't find a repository matching this job."</span>,<span class="hljs-string">"error"</span>:<span class="hljs-literal">true</span>}
<span class="linenos">35</span>	&gt;&gt; Failed to submit coverage results to coveralls
<span class="linenos">36</span>	Warning: Task <span class="hljs-string">"coveralls:app"</span> failed. Use --force to continue.
<span class="linenos">37</span>	
<span class="linenos">38</span>	Aborted due to warnings.</code></pre>
<p>If you now run the following command:</p>
<pre><code class="hljs lang-bash singleline">$ node index.js</code></pre>
<p>Then visit <code>http://localhost:5000</code>, you should be able to create new messages. If you then open it up in a second tab, you can see messages added in one tab appear in another. Deploying to Heroku using Redis To Go will be straightforward, and you can then access it from multiple devices and see new chat messages appear in real time.</p>
<h2 id="wrapping-up">Wrapping up</h2>
<p>This illustrates just how straightforward it is to use Redis’s Pub/Sub capability. The chat system is still quite limited, so in a future instalment we’ll develop it further. You can get the source code from the <a href="https://github.com/matthewbdaly/babblr">Github repository</a> - just switch to the <code>lesson-1</code> tag.</p>
]]></description>
            <link>http://matthewdaly.co.uk/blog/2014/12/31/building-a-chat-server-with-node-dot-js-and-redis/</link>
            <guid isPermaLink="true">http://matthewdaly.co.uk/blog/2014/12/31/building-a-chat-server-with-node-dot-js-and-redis/</guid>
            <pubDate>Wed, 31 Dec 2014 14:10:57 GMT</pubDate>
        </item>
        <item>
            <title><![CDATA[My first Grunt plugin]]></title>
            <description><![CDATA[<p>A while back, I mentioned that I’d written a Yeoman generator for creating a flat HTML blog, called <a href="https://github.com/matthewbdaly/generator-simple-static-blog">generator-simple-static-blog</a>. For this, I’d used the first Grunt plugin I could find for the purpose, which was <a href="https://github.com/testdouble/grunt-markdown-blog">grunt-markdown-blog</a>. This worked, but I wasn’t really very happy with it.</p>
<p>The ideal Grunt plugin I had in mind was as follows:</p>
<ul>
<li>Used Handlebars for templating</li>
<li>Generated posts from Markdown files</li>
<li>Saved files in named folders with a single <code>index.html</code> file in each one (like Octopress does) so that no file extension is visible on a page</li>
<li>Generated index pages, rather than just showing the latest post as the first page</li>
</ul>
<p>Unfortunately, <code>grunt-markdown-blog</code> only fulfilled the second criteria, so it was never going to be something I stuck with long-term. However, I couldn’t find anything else that would do the trick, so it looked like my only option was to write a suitable plugin myself.</p>
<p>I started a new Git repository a while back, but didn’t make much progress. Then, on Christmas Eve, I suddenly got the urge to start working on this again, and in a matter of a few hours I’d gotten a working Grunt plugin that ticked all of these boxes. I had to delay getting it integrated into the generator due to Christmas day, and then an unfortunate bout of flu, but I’ve now published it as <a href="https://github.com/matthewbdaly/grunt-blogbuilder">grunt-blogbuilder</a> and amended the Yeoman generator to use it instead.</p>
<p>I’m really pleased with the outcome, and while I’m still not yet ready to migrate over to it from Octopress, it’s a massive step forward, and building a Grunt plugin has been an interesting experience.</p>
]]></description>
            <link>http://matthewdaly.co.uk/blog/2014/12/28/my-first-grunt-plugin/</link>
            <guid isPermaLink="true">http://matthewdaly.co.uk/blog/2014/12/28/my-first-grunt-plugin/</guid>
            <pubDate>Sun, 28 Dec 2014 17:04:41 GMT</pubDate>
        </item>
        <item>
            <title><![CDATA[Building a URL shortener with Node.js and Redis]]></title>
            <description><![CDATA[<p>The NoSQL movement is an exciting one for web developers. While relational databases such as MySQL are applicable to solving a wide range of problems, they aren’t the best solution for every problem. Sometimes you may find yourself dealing with a problem where an alternative data store may make more sense.</p>
<p>Redis is one of the data stores that have appeared as part of this movement, and is arguably one of the more generally useful ones. Since it solves different problems to a relational database, it’s not generally useful as an alternative to them - instead it is often used alongside them.</p>
<h2 id="what-is-redis-">What is Redis?</h2>
<p>Redis is described as follows on the website:</p>
<blockquote>
<p>“Redis is an open source, BSD licensed, advanced key-value cache and store. It is often referred to as a data structure server since keys can contain strings, hashes, lists, sets, sorted sets, bitmaps and hyperloglogs”.</p>
</blockquote>
<p>In other words, its core functionality is that it allows you to store a value by a key, and later retrieve that data using the key. It also allows you to set an optional expiry time for that key-value pair. It’s quite similar to Memcached in that respect, and indeed one obvious use case for Redis is as an alternative to Memcached. However, it offers a number of additional benefits - for one thing, it supports more data types, and for another, it allows you to persist your data to disk (unlike Memcached, which only retains the data in memory, meaning it’s lost on restart or if Memcached crashes). The latter means that for some very simple web applications, Redis can be used as your sole data store.</p>
<p>In this tutorial, we’ll build a simple URL shortener, using Redis as the sole data store. A URL shortener only really requires two fields:</p>
<ul>
<li>A string to identify the correct URL</li>
<li>The URL</li>
</ul>
<p>That makes Redis a good fit for this use case since all we need to do is generate an ID for each URL, then when a link is followed, look up the URL for that key, and redirect the user to it. As long as this is all our application needs to do, we can quite happily use Redis for this rather than a relational database, and it will be significantly faster than a relational database would be for this use case.</p>
<h2 id="getting-started">Getting started</h2>
<p>We’re more interested in the fundamentals of using Redis in our application than a specific language here. As JavaScript is pretty much required to be a web developer, I think it’s a fairly safe bet to use Node.js rather than PHP or Python, since that way, even if your only experience of JavaScript is client-side, you shouldn’t have too much trouble following along.</p>
<p>You’ll need to have Node.js installed, and I’ll leave the details of installing this to you. You’ll also need the Grunt CLI - install this globally as follows:</p>
<pre><code class="hljs lang-bash singleline">$ sudo npm install -g grunt-cli</code></pre>
<p>Finally, you’ll want to have Redis itself installed. You might also want to install hiredis, which is a faster Redis client that gets used automatically where available.</p>
<p>Now, let’s create our <code>package.json</code> file:</p>
<pre><code class="hljs lang-bash singleline">$ npm init</code></pre>
<p>You’ll see a number of questions. Your generated <code>package.json</code> file should look something like this:</p>
<pre><code class="hljs lang-json"><span class="linenos">1</span>	{
<span class="linenos">2</span>	  "<span class="hljs-attribute">name</span>": <span class="hljs-value"><span class="hljs-string">"url-shortener"</span></span>,
<span class="linenos">3</span>	  "<span class="hljs-attribute">version</span>": <span class="hljs-value"><span class="hljs-string">"1.0.0"</span></span>,
<span class="linenos">4</span>	  "<span class="hljs-attribute">description</span>": <span class="hljs-value"><span class="hljs-string">"A URL shortener"</span></span>,
<span class="linenos">5</span>	  "<span class="hljs-attribute">main</span>": <span class="hljs-value"><span class="hljs-string">"index.js"</span></span>,
<span class="linenos">6</span>	  "<span class="hljs-attribute">scripts</span>": <span class="hljs-value">{
<span class="linenos">7</span>	    "<span class="hljs-attribute">test</span>": <span class="hljs-value"><span class="hljs-string">"grunt test"</span>
<span class="linenos">8</span>	  </span>}</span>,
<span class="linenos">9</span>	  "<span class="hljs-attribute">keywords</span>": <span class="hljs-value">[
<span class="linenos">10</span>	    <span class="hljs-string">"URL"</span>,
<span class="linenos">11</span>	    <span class="hljs-string">"shortener"</span>
<span class="linenos">12</span>	  ]</span>,
<span class="linenos">13</span>	  "<span class="hljs-attribute">author</span>": <span class="hljs-value"><span class="hljs-string">"Matthew Daly &lt;matthew@matthewdaly.co.uk&gt; (http://matthewdaly.co.uk/)"</span></span>,
<span class="linenos">14</span>	  "<span class="hljs-attribute">license</span>": <span class="hljs-value"><span class="hljs-string">"GPLv2"</span>
<span class="linenos">15</span>	</span>}</code></pre>
<p>Note in particular that we set our test command to <code>grunt test</code>.</p>
<p>Next, we install our required Node.js modules. First, install, the normal dependencies:</p>
<pre><code class="hljs lang-bash singleline">$ npm install --save body-parser express redis hiredis jade shortid</code></pre>
<p>Next, install the development dependencies:</p>
<pre><code class="hljs lang-bash singleline">$ npm install --save-dev grunt grunt-contrib-jshint grunt-mocha-istanbul istanbul mocha chai request</code></pre>
<p>Now, we’re going to use Grunt to run our tests, so that we can easily lint the JavaScript and generate code coverage details. Here’s the Gruntfile:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	<span class="hljs-built_in">module</span>.exports = <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(grunt)</span> </span>{
<span class="linenos">2</span>	<span class="hljs-pi">    'use strict'</span>;
<span class="linenos">3</span>	
<span class="linenos">4</span>	    grunt.initConfig({
<span class="linenos">5</span>	        jshint: {
<span class="linenos">6</span>	            all: [
<span class="linenos">7</span>	                <span class="hljs-string">'test/*.js'</span>,
<span class="linenos">8</span>	                <span class="hljs-string">'index.js'</span>
<span class="linenos">9</span>	            ]
<span class="linenos">10</span>	        },
<span class="linenos">11</span>	        mocha_istanbul: {
<span class="linenos">12</span>	            coverage: {
<span class="linenos">13</span>	                src: <span class="hljs-string">'test'</span>, <span class="hljs-comment">// the folder, not the files,</span>
<span class="linenos">14</span>	                options: {
<span class="linenos">15</span>	                    mask: <span class="hljs-string">'*.js'</span>,
<span class="linenos">16</span>	                    reportFormats: [<span class="hljs-string">'cobertura'</span>, <span class="hljs-string">'html'</span>]
<span class="linenos">17</span>	                }
<span class="linenos">18</span>	            }
<span class="linenos">19</span>	        }
<span class="linenos">20</span>	    });
<span class="linenos">21</span>	
<span class="linenos">22</span>	    <span class="hljs-comment">// Load tasks</span>
<span class="linenos">23</span>	    grunt.loadNpmTasks(<span class="hljs-string">'grunt-contrib-jshint'</span>);
<span class="linenos">24</span>	    grunt.loadNpmTasks(<span class="hljs-string">'grunt-mocha-istanbul'</span>);
<span class="linenos">25</span>	
<span class="linenos">26</span>	    <span class="hljs-comment">// Register tasks</span>
<span class="linenos">27</span>	    grunt.registerTask(<span class="hljs-string">'test'</span>, [<span class="hljs-string">'jshint'</span>, <span class="hljs-string">'mocha_istanbul:coverage'</span>]);
<span class="linenos">28</span>	};</code></pre>
<p>We’ll also create a <code>Procfile</code> in anticipation of deploying the app to Heroku:</p>
<pre><code class="hljs lang-bash singleline">web: node index.js</code></pre>
<h2 id="creating-the-views">Creating the views</h2>
<p>For this application we’ll be using the Express framework and the Jade templating system. We need three templates:</p>
<ul>
<li>Submission form</li>
<li>Output form</li>
<li>404 page</li>
</ul>
<p>Create the folder <code>views</code> under the application directory and add the files <code>views/index.jade</code>:</p>
<pre><code class="hljs lang-jade"><span class="linenos">1</span>	doctype <span class="hljs-tag">html</span>
<span class="linenos">2</span>	<span class="hljs-function"><span class="hljs-title">html</span><span class="hljs-params">(lang=<span class="hljs-string">"en"</span>)</span></span>
<span class="linenos">3</span>	    head
<span class="linenos">4</span>	        title=<span class="hljs-string">"Shortbread"</span>
<span class="linenos">5</span>	
<span class="linenos">6</span>	    <span class="hljs-tag">body</span>
<span class="linenos">7</span>	        <span class="hljs-tag">div</span><span class="hljs-class">.container</span>
<span class="linenos">8</span>	            <span class="hljs-tag">div</span><span class="hljs-class">.row</span>
<span class="linenos">9</span>	                <span class="hljs-tag">h1</span> Shortbread
<span class="linenos">10</span>	
<span class="linenos">11</span>	            <span class="hljs-tag">div</span><span class="hljs-class">.row</span>
<span class="linenos">12</span>	                <span class="hljs-function"><span class="hljs-title">form</span><span class="hljs-params">(action=<span class="hljs-string">"/"</span>, method=<span class="hljs-string">"POST"</span>)</span></span>
<span class="linenos">13</span>	                    <span class="hljs-function"><span class="hljs-title">input</span><span class="hljs-params">(type=<span class="hljs-string">"url"</span>, name=<span class="hljs-string">"url"</span>)</span></span>
<span class="linenos">14</span>	                    <span class="hljs-function"><span class="hljs-title">input</span><span class="hljs-params">(type=<span class="hljs-string">"submit"</span>, value=<span class="hljs-string">"Submit"</span>)</span></span></code></pre>
<p>Also <code>views/output.jade</code>:</p>
<pre><code class="hljs lang-jade"><span class="linenos">1</span>	doctype html
<span class="linenos">2</span>	html(lang=<span class="hljs-string">"en"</span>)
<span class="linenos">3</span>	    head
<span class="linenos">4</span>	        title=Shortbread
<span class="linenos">5</span>	
<span class="linenos">6</span>	    body
<span class="linenos">7</span>	        <span class="hljs-operator">div</span>.container
<span class="linenos">8</span>	            <span class="hljs-operator">div</span>.row
<span class="linenos">9</span>	                h1 Shortbread
<span class="linenos">10</span>	                p
<span class="linenos">11</span>	                    | Your shortened <span class="hljs-built_in">URL</span> is
<span class="linenos">12</span>	                    <span class="hljs-operator">a</span>(href=base_url+<span class="hljs-string">'/'</span>+id) <span class="hljs-comment">#{base_url}/#{id}</span></code></pre>
<p>and <code>views/error.jade</code>:</p>
<pre><code class="hljs lang-jade"><span class="linenos">1</span>	doctype <span class="hljs-tag">html</span>
<span class="linenos">2</span>	<span class="hljs-function"><span class="hljs-title">html</span><span class="hljs-params">(lang=<span class="hljs-string">"en"</span>)</span></span>
<span class="linenos">3</span>	    head
<span class="linenos">4</span>	        title=<span class="hljs-string">"Shortbread"</span>
<span class="linenos">5</span>	
<span class="linenos">6</span>	    <span class="hljs-tag">body</span>
<span class="linenos">7</span>	        <span class="hljs-tag">div</span><span class="hljs-class">.container</span>
<span class="linenos">8</span>	            <span class="hljs-tag">div</span><span class="hljs-class">.row</span>
<span class="linenos">9</span>	                <span class="hljs-tag">h1</span> Shortbread
<span class="linenos">10</span>	                <span class="hljs-tag">p</span> Link not found</code></pre>
<h2 id="writing-our-first-test">Writing our first test</h2>
<p>We’re going to use Mocha for our tests, together with the Chai assertion library. Create a folder called <code>test</code>, and put the following in <code>test/test.js</code>:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	<span class="hljs-comment">/*jslint node: true */</span>
<span class="linenos">2</span>	<span class="hljs-comment">/*global describe: false, before: false, after: false, it: false */</span>
<span class="linenos">3</span>	<span class="hljs-pi">"use strict"</span>;
<span class="linenos">4</span>	
<span class="linenos">5</span>	<span class="hljs-comment">// Declare the variables used</span>
<span class="linenos">6</span>	<span class="hljs-keyword">var</span> expect = <span class="hljs-built_in">require</span>(<span class="hljs-string">'chai'</span>).expect,
<span class="linenos">7</span>	    request = <span class="hljs-built_in">require</span>(<span class="hljs-string">'request'</span>),
<span class="linenos">8</span>	    server = <span class="hljs-built_in">require</span>(<span class="hljs-string">'../index'</span>),
<span class="linenos">9</span>	    redis = <span class="hljs-built_in">require</span>(<span class="hljs-string">'redis'</span>),
<span class="linenos">10</span>	    client;
<span class="linenos">11</span>	client = redis.createClient();
<span class="linenos">12</span>	
<span class="linenos">13</span>	<span class="hljs-comment">// Server tasks</span>
<span class="linenos">14</span>	describe(<span class="hljs-string">'server'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">()</span> </span>{
<span class="linenos">15</span>	
<span class="linenos">16</span>	    <span class="hljs-comment">// Beforehand, start the server</span>
<span class="linenos">17</span>	    before(<span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(done)</span> </span>{
<span class="linenos">18</span>	        <span class="hljs-built_in">console</span>.log(<span class="hljs-string">'Starting the server'</span>);
<span class="linenos">19</span>	        done();
<span class="linenos">20</span>	    });
<span class="linenos">21</span>	
<span class="linenos">22</span>	    <span class="hljs-comment">// Afterwards, stop the server and empty the database</span>
<span class="linenos">23</span>	    after(<span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(done)</span> </span>{
<span class="linenos">24</span>	        <span class="hljs-built_in">console</span>.log(<span class="hljs-string">'Stopping the server'</span>);
<span class="linenos">25</span>	        client.flushdb();
<span class="linenos">26</span>	        done();
<span class="linenos">27</span>	    });
<span class="linenos">28</span>	
<span class="linenos">29</span>	    <span class="hljs-comment">// Test the index route</span>
<span class="linenos">30</span>	    describe(<span class="hljs-string">'Test the index route'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">()</span> </span>{
<span class="linenos">31</span>	        it(<span class="hljs-string">'should return a page with the title Shortbread'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(done)</span> </span>{
<span class="linenos">32</span>	            request.get({ url: <span class="hljs-string">'http://localhost:5000'</span> }, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(error, response, body)</span> </span>{
<span class="linenos">33</span>	                expect(body).to.include(<span class="hljs-string">'Shortbread'</span>);
<span class="linenos">34</span>	                expect(response.statusCode).to.equal(<span class="hljs-number">200</span>);
<span class="linenos">35</span>	                expect(response.headers[<span class="hljs-string">'content-type'</span>]).to.equal(<span class="hljs-string">'text/html; charset=utf-8'</span>);
<span class="linenos">36</span>	                done();
<span class="linenos">37</span>	            });
<span class="linenos">38</span>	        });
<span class="linenos">39</span>	    });
<span class="linenos">40</span>	});</code></pre>
<p>This code bears a little explanation. First, we import the required modules, as well as our <code>index.js</code> file (which we have yet to add). Then we create a callback to contain our tests.</p>
<p>Inside the callback, we call the <code>before()</code> and <code>after()</code> functions, which let us set up and tear down our tests. As part of the teardown process, we flush the Redis database.</p>
<p>Finally, we fetch our home page and verify that it returns a 200 status code and a content type of text/html, as well as including the name or our application.</p>
<p>We’ll need to create our <code>index.js</code> file to avoid a nasty error, but we won’t populate it just yet:</p>
<pre><code class="hljs lang-bash singleline">$ touch index.js</code></pre>
<p>Let’s run our tests:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ grunt <span class="hljs-built_in">test</span>
<span class="linenos">2</span>	Running <span class="hljs-string">"jshint:all"</span> (jshint) task
<span class="linenos">3</span>	&gt;&gt; <span class="hljs-number">2</span> files lint free.
<span class="linenos">4</span>	
<span class="linenos">5</span>	Running <span class="hljs-string">"mocha_istanbul:coverage"</span> (mocha_istanbul) task
<span class="linenos">6</span>	
<span class="linenos">7</span>	
<span class="linenos">8</span>	  server
<span class="linenos">9</span>	Starting the server
<span class="linenos">10</span>	    Test the index route
<span class="linenos">11</span>	      <span class="hljs-number">1</span>) should <span class="hljs-built_in">return</span> a page with the title Shortbread
<span class="linenos">12</span>	Stopping the server
<span class="linenos">13</span>	
<span class="linenos">14</span>	
<span class="linenos">15</span>	  <span class="hljs-number">0</span> passing (<span class="hljs-number">152</span>ms)
<span class="linenos">16</span>	  <span class="hljs-number">1</span> failing
<span class="linenos">17</span>	
<span class="linenos">18</span>	  <span class="hljs-number">1</span>) server Test the index route should <span class="hljs-built_in">return</span> a page with the title Shortbread:
<span class="linenos">19</span>	     Uncaught AssertionError: expected undefined to include <span class="hljs-string">'Shortbread'</span>
<span class="linenos">20</span>	      at Request._callback (/Users/matthewdaly/Projects/url-shortener/<span class="hljs-built_in">test</span>/test.js:<span class="hljs-number">33</span>:<span class="hljs-number">33</span>)
<span class="linenos">21</span>	      at self.callback (/Users/matthewdaly/Projects/url-shortener/node_modules/request/request.js:<span class="hljs-number">372</span>:<span class="hljs-number">22</span>)
<span class="linenos">22</span>	      at Request.emit (events.js:<span class="hljs-number">95</span>:<span class="hljs-number">17</span>)
<span class="linenos">23</span>	      at Request.onRequestError (/Users/matthewdaly/Projects/url-shortener/node_modules/request/request.js:<span class="hljs-number">963</span>:<span class="hljs-number">8</span>)
<span class="linenos">24</span>	      at ClientRequest.emit (events.js:<span class="hljs-number">95</span>:<span class="hljs-number">17</span>)
<span class="linenos">25</span>	      at Socket.socketErrorListener (http.js:<span class="hljs-number">1551</span>:<span class="hljs-number">9</span>)
<span class="linenos">26</span>	      at Socket.emit (events.js:<span class="hljs-number">95</span>:<span class="hljs-number">17</span>)
<span class="linenos">27</span>	      at net.js:<span class="hljs-number">440</span>:<span class="hljs-number">14</span>
<span class="linenos">28</span>	      at process._tickCallback (node.js:<span class="hljs-number">419</span>:<span class="hljs-number">13</span>)
<span class="linenos">29</span>	
<span class="linenos">30</span>	
<span class="linenos">31</span>	
<span class="linenos">32</span>	=============================================================================
<span class="linenos">33</span>	Writing coverage object [/Users/matthewdaly/Projects/url-shortener/coverage/coverage.json]
<span class="linenos">34</span>	Writing coverage reports at [/Users/matthewdaly/Projects/url-shortener/coverage]
<span class="linenos">35</span>	=============================================================================
<span class="linenos">36</span>	
<span class="linenos">37</span>	=============================== Coverage summary ===============================
<span class="linenos">38</span>	Statements   : <span class="hljs-number">100</span>% ( <span class="hljs-number">0</span>/<span class="hljs-number">0</span> )
<span class="linenos">39</span>	Branches     : <span class="hljs-number">100</span>% ( <span class="hljs-number">0</span>/<span class="hljs-number">0</span> )
<span class="linenos">40</span>	Functions    : <span class="hljs-number">100</span>% ( <span class="hljs-number">0</span>/<span class="hljs-number">0</span> )
<span class="linenos">41</span>	Lines        : <span class="hljs-number">100</span>% ( <span class="hljs-number">0</span>/<span class="hljs-number">0</span> )
<span class="linenos">42</span>	================================================================================
<span class="linenos">43</span>	&gt;&gt;
<span class="linenos">44</span>	Warning: Task <span class="hljs-string">"mocha_istanbul:coverage"</span> failed. Use --force to continue.
<span class="linenos">45</span>	
<span class="linenos">46</span>	Aborted due to warnings.</code></pre>
<p>Now that we have a failing test, we can start work on our app proper. Open up <code>index.js</code> and add the following code:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	<span class="hljs-comment">/*jslint node: true */</span>
<span class="linenos">2</span>	<span class="hljs-pi">'use strict'</span>;
<span class="linenos">3</span>	
<span class="linenos">4</span>	<span class="hljs-comment">// Declare variables used</span>
<span class="linenos">5</span>	<span class="hljs-keyword">var</span> app, base_url, bodyParser, client, express, port, rtg, shortid;
<span class="linenos">6</span>	
<span class="linenos">7</span>	<span class="hljs-comment">// Define values</span>
<span class="linenos">8</span>	express = <span class="hljs-built_in">require</span>(<span class="hljs-string">'express'</span>);
<span class="linenos">9</span>	app = express();
<span class="linenos">10</span>	port = process.env.PORT || <span class="hljs-number">5000</span>;
<span class="linenos">11</span>	shortid = <span class="hljs-built_in">require</span>(<span class="hljs-string">'shortid'</span>);
<span class="linenos">12</span>	bodyParser = <span class="hljs-built_in">require</span>(<span class="hljs-string">'body-parser'</span>);
<span class="linenos">13</span>	base_url = process.env.BASE_URL || <span class="hljs-string">'http://localhost:5000'</span>;
<span class="linenos">14</span>	
<span class="linenos">15</span>	<span class="hljs-comment">// Set up connection to Redis</span>
<span class="linenos">16</span>	<span class="hljs-keyword">if</span> (process.env.REDISTOGO_URL) {
<span class="linenos">17</span>	  rtg  = <span class="hljs-built_in">require</span>(<span class="hljs-string">"url"</span>).parse(process.env.REDISTOGO_URL);
<span class="linenos">18</span>	  client = <span class="hljs-built_in">require</span>(<span class="hljs-string">"redis"</span>).createClient(rtg.port, rtg.hostname);
<span class="linenos">19</span>	  client.auth(rtg.auth.split(<span class="hljs-string">":"</span>)[<span class="hljs-number">1</span>]);
<span class="linenos">20</span>	} <span class="hljs-keyword">else</span> {
<span class="linenos">21</span>	  client = <span class="hljs-built_in">require</span>(<span class="hljs-string">'redis'</span>).createClient();
<span class="linenos">22</span>	}
<span class="linenos">23</span>	
<span class="linenos">24</span>	<span class="hljs-comment">// Set up templating</span>
<span class="linenos">25</span>	app.set(<span class="hljs-string">'views'</span>, __dirname + <span class="hljs-string">'/views'</span>);
<span class="linenos">26</span>	app.set(<span class="hljs-string">'view engine'</span>, <span class="hljs-string">"jade"</span>);
<span class="linenos">27</span>	app.engine(<span class="hljs-string">'jade'</span>, <span class="hljs-built_in">require</span>(<span class="hljs-string">'jade'</span>).__express);
<span class="linenos">28</span>	
<span class="linenos">29</span>	<span class="hljs-comment">// Set URL</span>
<span class="linenos">30</span>	app.set(<span class="hljs-string">'base_url'</span>, base_url);
<span class="linenos">31</span>	
<span class="linenos">32</span>	<span class="hljs-comment">// Handle POST data</span>
<span class="linenos">33</span>	app.use(bodyParser.json());
<span class="linenos">34</span>	app.use(bodyParser.urlencoded({
<span class="linenos">35</span>	  extended: <span class="hljs-literal">true</span>
<span class="linenos">36</span>	}));
<span class="linenos">37</span>	
<span class="linenos">38</span>	<span class="hljs-comment">// Define index route</span>
<span class="linenos">39</span>	app.get(<span class="hljs-string">'/'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(req, res)</span> </span>{
<span class="linenos">40</span>	  res.render(<span class="hljs-string">'index'</span>);
<span class="linenos">41</span>	});
<span class="linenos">42</span>	
<span class="linenos">43</span>	<span class="hljs-comment">// Serve static files</span>
<span class="linenos">44</span>	app.use(express.static(__dirname + <span class="hljs-string">'/static'</span>));
<span class="linenos">45</span>	
<span class="linenos">46</span>	<span class="hljs-comment">// Listen</span>
<span class="linenos">47</span>	app.listen(port);
<span class="linenos">48</span>	<span class="hljs-built_in">console</span>.log(<span class="hljs-string">'Listening on port '</span> + port);</code></pre>
<p>Let’s go through this code. First we confirm that linting tools should treat this as a Node app, and use strict mode (I recommend always using strict mode in JavaScript).</p>
<p>Then we declare our variables and import the required modules. Note here that we set the port to 5000, but can also set it based on the <code>PORT</code> environment variable, which is used by Heroku. We also define a base URL, which again can be overriden from an environment variable when hosted on Heroku.</p>
<p>We then set up our connection to our Redis instance. When we push the code up to Heroku, we’ll use the Redis To Go addon, so we check for an environment variable containing the Redis URL. If it’s set, we use that to connect. Otherwise, we just connect as normal.</p>
<p>We then set up templating using Jade, and define the folder containing our views, and store the base URL within the app. Then we set up <code>bodyParser</code> so that Express can handle POST data.</p>
<p>Next, we define our index route to just render the <code>index.jade</code> file. Finally, we set up our static folder and set the app to listen on the correct port.</p>
<p>Let’s run our test:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ grunt <span class="hljs-built_in">test</span>
<span class="linenos">2</span>	Running <span class="hljs-string">"jshint:all"</span> (jshint) task
<span class="linenos">3</span>	&gt;&gt; <span class="hljs-number">2</span> files lint free.
<span class="linenos">4</span>	
<span class="linenos">5</span>	Running <span class="hljs-string">"mocha_istanbul:coverage"</span> (mocha_istanbul) task
<span class="linenos">6</span>	Listening on port <span class="hljs-number">5000</span>
<span class="linenos">7</span>	
<span class="linenos">8</span>	
<span class="linenos">9</span>	  server
<span class="linenos">10</span>	Starting the server
<span class="linenos">11</span>	    Test the index route
<span class="linenos">12</span>	      ✓ should <span class="hljs-built_in">return</span> a page with the title Shortbread (<span class="hljs-number">116</span>ms)
<span class="linenos">13</span>	Stopping the server
<span class="linenos">14</span>	
<span class="linenos">15</span>	
<span class="linenos">16</span>	  <span class="hljs-number">1</span> passing (<span class="hljs-number">128</span>ms)
<span class="linenos">17</span>	
<span class="linenos">18</span>	=============================================================================
<span class="linenos">19</span>	Writing coverage object [/Users/matthewdaly/Projects/url-shortener/coverage/coverage.json]
<span class="linenos">20</span>	Writing coverage reports at [/Users/matthewdaly/Projects/url-shortener/coverage]
<span class="linenos">21</span>	=============================================================================
<span class="linenos">22</span>	
<span class="linenos">23</span>	=============================== Coverage summary ===============================
<span class="linenos">24</span>	Statements   : <span class="hljs-number">86.96</span>% ( <span class="hljs-number">20</span>/<span class="hljs-number">23</span> )
<span class="linenos">25</span>	Branches     : <span class="hljs-number">83.33</span>% ( <span class="hljs-number">5</span>/<span class="hljs-number">6</span> )
<span class="linenos">26</span>	Functions    : <span class="hljs-number">100</span>% ( <span class="hljs-number">1</span>/<span class="hljs-number">1</span> )
<span class="linenos">27</span>	Lines        : <span class="hljs-number">86.96</span>% ( <span class="hljs-number">20</span>/<span class="hljs-number">23</span> )
<span class="linenos">28</span>	================================================================================
<span class="linenos">29</span>	&gt;&gt; Done. Check coverage folder.
<span class="linenos">30</span>	
<span class="linenos">31</span>	Done, without errors.</code></pre>
<p>Note that Istanbul will have generated a nice HTML coverage report, which will be at <code>coverage/index.html</code>, but this won’t show 100% test coverage due to the Heroku-specific Redis section. To fix this, amend that section as follows:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	<span class="hljs-comment">/* istanbul ignore if */</span>
<span class="linenos">2</span>	<span class="hljs-keyword">if</span> (process.env.REDISTOGO_URL) {
<span class="linenos">3</span>	    rtg  = <span class="hljs-built_in">require</span>(<span class="hljs-string">"url"</span>).parse(process.env.REDISTOGO_URL);
<span class="linenos">4</span>	    client = <span class="hljs-built_in">require</span>(<span class="hljs-string">"redis"</span>).createClient(rtg.port, rtg.hostname);
<span class="linenos">5</span>	    client.auth(rtg.auth.split(<span class="hljs-string">":"</span>)[<span class="hljs-number">1</span>]);
<span class="linenos">6</span>	} <span class="hljs-keyword">else</span> {
<span class="linenos">7</span>	    client = <span class="hljs-built_in">require</span>(<span class="hljs-string">'redis'</span>).createClient();
<span class="linenos">8</span>	}</code></pre>
<p>Telling Istanbul to ignore the if clause resolves that problem nicely.</p>
<h2 id="submitting-a-url">Submitting a URL</h2>
<p>Next, let’s add the ability to add a URL. First, add the following test, after the one for the index:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	    <span class="hljs-comment">// Test submitting a URL</span>
<span class="linenos">2</span>	    describe(<span class="hljs-string">'Test submitting a URL'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">()</span> </span>{
<span class="linenos">3</span>	        it(<span class="hljs-string">'should return the shortened URL'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(done)</span> </span>{
<span class="linenos">4</span>	            request.post(<span class="hljs-string">'http://localhost:5000'</span>, {form: {url: <span class="hljs-string">'http://www.google.co.uk'</span>}}, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(error, response, body)</span> </span>{
<span class="linenos">5</span>	                expect(body).to.include(<span class="hljs-string">'Your shortened URL is'</span>);
<span class="linenos">6</span>	                expect(response.statusCode).to.equal(<span class="hljs-number">200</span>);
<span class="linenos">7</span>	                expect(response.headers[<span class="hljs-string">'content-type'</span>]).to.equal(<span class="hljs-string">'text/html; charset=utf-8'</span>);
<span class="linenos">8</span>	                done();
<span class="linenos">9</span>	            });
<span class="linenos">10</span>	        });
<span class="linenos">11</span>	    });</code></pre>
<p>This test submits a URL via POST, and checks to see that the response view gets returned. Now, let’s run our tests again:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ grunt <span class="hljs-built_in">test</span>
<span class="linenos">2</span>	Running <span class="hljs-string">"jshint:all"</span> (jshint) task
<span class="linenos">3</span>	&gt;&gt; <span class="hljs-number">2</span> files lint free.
<span class="linenos">4</span>	
<span class="linenos">5</span>	Running <span class="hljs-string">"mocha_istanbul:coverage"</span> (mocha_istanbul) task
<span class="linenos">6</span>	Listening on port <span class="hljs-number">5000</span>
<span class="linenos">7</span>	
<span class="linenos">8</span>	
<span class="linenos">9</span>	  server
<span class="linenos">10</span>	Starting the server
<span class="linenos">11</span>	    Test the index route
<span class="linenos">12</span>	      ✓ should <span class="hljs-built_in">return</span> a page with the title Shortbread (<span class="hljs-number">223</span>ms)
<span class="linenos">13</span>	    Test submitting a URL
<span class="linenos">14</span>	      <span class="hljs-number">1</span>) should <span class="hljs-built_in">return</span> the shortened URL
<span class="linenos">15</span>	Stopping the server
<span class="linenos">16</span>	
<span class="linenos">17</span>	
<span class="linenos">18</span>	  <span class="hljs-number">1</span> passing (<span class="hljs-number">318</span>ms)
<span class="linenos">19</span>	  <span class="hljs-number">1</span> failing
<span class="linenos">20</span>	
<span class="linenos">21</span>	  <span class="hljs-number">1</span>) server Test submitting a URL should <span class="hljs-built_in">return</span> the shortened URL:
<span class="linenos">22</span>	     Uncaught AssertionError: expected <span class="hljs-string">'Cannot POST /\n'</span> to include <span class="hljs-string">'Your shortened URL is'</span>
<span class="linenos">23</span>	      at Request._callback (/Users/matthewdaly/Projects/url-shortener/<span class="hljs-built_in">test</span>/test.js:<span class="hljs-number">45</span>:<span class="hljs-number">33</span>)
<span class="linenos">24</span>	      at Request.self.callback (/Users/matthewdaly/Projects/url-shortener/node_modules/request/request.js:<span class="hljs-number">372</span>:<span class="hljs-number">22</span>)
<span class="linenos">25</span>	      at Request.emit (events.js:<span class="hljs-number">98</span>:<span class="hljs-number">17</span>)
<span class="linenos">26</span>	      at Request.&lt;anonymous&gt; (/Users/matthewdaly/Projects/url-shortener/node_modules/request/request.js:<span class="hljs-number">1310</span>:<span class="hljs-number">14</span>)
<span class="linenos">27</span>	      at Request.emit (events.js:<span class="hljs-number">117</span>:<span class="hljs-number">20</span>)
<span class="linenos">28</span>	      at IncomingMessage.&lt;anonymous&gt; (/Users/matthewdaly/Projects/url-shortener/node_modules/request/request.js:<span class="hljs-number">1258</span>:<span class="hljs-number">12</span>)
<span class="linenos">29</span>	      at IncomingMessage.emit (events.js:<span class="hljs-number">117</span>:<span class="hljs-number">20</span>)
<span class="linenos">30</span>	      at _stream_readable.js:<span class="hljs-number">943</span>:<span class="hljs-number">16</span>
<span class="linenos">31</span>	      at process._tickCallback (node.js:<span class="hljs-number">419</span>:<span class="hljs-number">13</span>)
<span class="linenos">32</span>	
<span class="linenos">33</span>	
<span class="linenos">34</span>	
<span class="linenos">35</span>	=============================================================================
<span class="linenos">36</span>	Writing coverage object [/Users/matthewdaly/Projects/url-shortener/coverage/coverage.json]
<span class="linenos">37</span>	Writing coverage reports at [/Users/matthewdaly/Projects/url-shortener/coverage]
<span class="linenos">38</span>	=============================================================================
<span class="linenos">39</span>	
<span class="linenos">40</span>	=============================== Coverage summary ===============================
<span class="linenos">41</span>	Statements   : <span class="hljs-number">100</span>% ( <span class="hljs-number">23</span>/<span class="hljs-number">23</span> ), <span class="hljs-number">3</span> ignored
<span class="linenos">42</span>	Branches     : <span class="hljs-number">100</span>% ( <span class="hljs-number">6</span>/<span class="hljs-number">6</span> ), <span class="hljs-number">1</span> ignored
<span class="linenos">43</span>	Functions    : <span class="hljs-number">100</span>% ( <span class="hljs-number">1</span>/<span class="hljs-number">1</span> )
<span class="linenos">44</span>	Lines        : <span class="hljs-number">100</span>% ( <span class="hljs-number">23</span>/<span class="hljs-number">23</span> )
<span class="linenos">45</span>	================================================================================
<span class="linenos">46</span>	&gt;&gt;
<span class="linenos">47</span>	Warning: Task <span class="hljs-string">"mocha_istanbul:coverage"</span> failed. Use --force to continue.
<span class="linenos">48</span>	
<span class="linenos">49</span>	Aborted due to warnings.</code></pre>
<p>We have a failing test, so let’s make it pass. Add the following route after the index one:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	<span class="hljs-comment">// Define submit route</span>
<span class="linenos">2</span>	app.post(<span class="hljs-string">'/'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(req, res)</span> </span>{
<span class="linenos">3</span>	    <span class="hljs-comment">// Declare variables</span>
<span class="linenos">4</span>	    <span class="hljs-keyword">var</span> url, id;
<span class="linenos">5</span>	
<span class="linenos">6</span>	    <span class="hljs-comment">// Get URL</span>
<span class="linenos">7</span>	    url = req.body.url;
<span class="linenos">8</span>	
<span class="linenos">9</span>	    <span class="hljs-comment">// Create a hashed short version</span>
<span class="linenos">10</span>	    id = shortid.generate();
<span class="linenos">11</span>	
<span class="linenos">12</span>	    <span class="hljs-comment">// Store them in Redis</span>
<span class="linenos">13</span>	    client.set(id, url, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">()</span> </span>{
<span class="linenos">14</span>	        <span class="hljs-comment">// Display the response</span>
<span class="linenos">15</span>	        res.render(<span class="hljs-string">'output'</span>, { id: id, base_url: base_url });
<span class="linenos">16</span>	    });
<span class="linenos">17</span>	});</code></pre>
<p>This route is fairly simple. It handles POST requests to the index route, and first of all it gets the URL from the POST request. Then it randomly generates a hash to use as the key.</p>
<p>The next part is where we see Redis in action. We create a new key-value pair, with the key set to the newly generated ID, and the value set to the URL. Once Redis confirms that has been done, the callback is fired, which renders the <code>output.jade</code> view with the ID and base URL passed through, so that we can see our shortened URL.</p>
<p>With that done, our test should pass:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ grunt <span class="hljs-built_in">test</span>
<span class="linenos">2</span>	Running <span class="hljs-string">"jshint:all"</span> (jshint) task
<span class="linenos">3</span>	&gt;&gt; <span class="hljs-number">2</span> files lint free.
<span class="linenos">4</span>	
<span class="linenos">5</span>	Running <span class="hljs-string">"mocha_istanbul:coverage"</span> (mocha_istanbul) task
<span class="linenos">6</span>	Listening on port <span class="hljs-number">5000</span>
<span class="linenos">7</span>	
<span class="linenos">8</span>	
<span class="linenos">9</span>	  server
<span class="linenos">10</span>	Starting the server
<span class="linenos">11</span>	    Test the index route
<span class="linenos">12</span>	      ✓ should <span class="hljs-built_in">return</span> a page with the title Shortbread (<span class="hljs-number">89</span>ms)
<span class="linenos">13</span>	    Test submitting a URL
<span class="linenos">14</span>	      ✓ should <span class="hljs-built_in">return</span> the shortened URL (<span class="hljs-number">65</span>ms)
<span class="linenos">15</span>	Stopping the server
<span class="linenos">16</span>	
<span class="linenos">17</span>	
<span class="linenos">18</span>	  <span class="hljs-number">2</span> passing (<span class="hljs-number">167</span>ms)
<span class="linenos">19</span>	
<span class="linenos">20</span>	=============================================================================
<span class="linenos">21</span>	Writing coverage object [/Users/matthewdaly/Projects/url-shortener/coverage/coverage.json]
<span class="linenos">22</span>	Writing coverage reports at [/Users/matthewdaly/Projects/url-shortener/coverage]
<span class="linenos">23</span>	=============================================================================
<span class="linenos">24</span>	
<span class="linenos">25</span>	=============================== Coverage summary ===============================
<span class="linenos">26</span>	Statements   : <span class="hljs-number">100</span>% ( <span class="hljs-number">29</span>/<span class="hljs-number">29</span> ), <span class="hljs-number">3</span> ignored
<span class="linenos">27</span>	Branches     : <span class="hljs-number">100</span>% ( <span class="hljs-number">6</span>/<span class="hljs-number">6</span> ), <span class="hljs-number">1</span> ignored
<span class="linenos">28</span>	Functions    : <span class="hljs-number">100</span>% ( <span class="hljs-number">3</span>/<span class="hljs-number">3</span> )
<span class="linenos">29</span>	Lines        : <span class="hljs-number">100</span>% ( <span class="hljs-number">29</span>/<span class="hljs-number">29</span> )
<span class="linenos">30</span>	================================================================================
<span class="linenos">31</span>	&gt;&gt; Done. Check coverage folder.
<span class="linenos">32</span>	
<span class="linenos">33</span>	Done, without errors.</code></pre>
<p>Our final task is to implement the short URL handling. We want to check to see if a short URL exists. If it does, we redirect the user to the destination. If it doesn’t, we raise a 404 error. For that we need two more tests. Here they are:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	
<span class="linenos">2</span>	    <span class="hljs-comment">// Test following a URL</span>
<span class="linenos">3</span>	    describe(<span class="hljs-string">'Test following a URL'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">()</span> </span>{
<span class="linenos">4</span>	        it(<span class="hljs-string">'should redirect the user to the shortened URL'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(done)</span> </span>{
<span class="linenos">5</span>	            <span class="hljs-comment">// Create the URL</span>
<span class="linenos">6</span>	            client.set(<span class="hljs-string">'testurl'</span>, <span class="hljs-string">'http://www.google.com'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">()</span> </span>{
<span class="linenos">7</span>	                <span class="hljs-comment">// Follow the link</span>
<span class="linenos">8</span>	                request.get({
<span class="linenos">9</span>	                    url: <span class="hljs-string">'http://localhost:5000/testurl'</span>,
<span class="linenos">10</span>	                    followRedirect: <span class="hljs-literal">false</span>
<span class="linenos">11</span>	                }, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(error, response, body)</span> </span>{
<span class="linenos">12</span>	                    expect(response.headers.location).to.equal(<span class="hljs-string">'http://www.google.com'</span>);
<span class="linenos">13</span>	                    expect(response.statusCode).to.equal(<span class="hljs-number">301</span>);
<span class="linenos">14</span>	                    done();
<span class="linenos">15</span>	                });
<span class="linenos">16</span>	            });
<span class="linenos">17</span>	        });
<span class="linenos">18</span>	    });
<span class="linenos">19</span>	
<span class="linenos">20</span>	    <span class="hljs-comment">// Test non-existent link</span>
<span class="linenos">21</span>	    describe(<span class="hljs-string">'Test following a non-existent-link'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">()</span> </span>{
<span class="linenos">22</span>	        it(<span class="hljs-string">'should return a 404 error'</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(done)</span> </span>{
<span class="linenos">23</span>	            <span class="hljs-comment">// Follow the link</span>
<span class="linenos">24</span>	            request.get({
<span class="linenos">25</span>	                url: <span class="hljs-string">'http://localhost:5000/nonexistenturl'</span>,
<span class="linenos">26</span>	                followRedirect: <span class="hljs-literal">false</span>
<span class="linenos">27</span>	            }, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(error, response, body)</span> </span>{
<span class="linenos">28</span>	                expect(response.statusCode).to.equal(<span class="hljs-number">404</span>);
<span class="linenos">29</span>	                expect(body).to.include(<span class="hljs-string">'Link not found'</span>);
<span class="linenos">30</span>	                done();
<span class="linenos">31</span>	            });
<span class="linenos">32</span>	        });
<span class="linenos">33</span>	    });</code></pre>
<p>The first test creates a URL for testing purposes. It then navigates to that URL. Note that we set <code>followRedirect</code> to <code>true</code> - this is because, by default, <code>request</code> will follow any redirect, so we need to prevent it from doing so to ensure that the headers to redirect the user are set correctly.</p>
<p>Once the response has been received, we then check that the status code is 301 (Moved Permanently), and that the <code>Location</code> header is set to the correct destination. When a real browser visits this page, it will be redirected accordingly.</p>
<p>The second test tries to fetch a non-existent URL, and checks that the status code is 404, and the response contains the words <code>Link not found</code>.</p>
<p>If we run our tests, they should now fail:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ grunt <span class="hljs-built_in">test</span>
<span class="linenos">2</span>	Running <span class="hljs-string">"jshint:all"</span> (jshint) task
<span class="linenos">3</span>	&gt;&gt; <span class="hljs-number">2</span> files lint free.
<span class="linenos">4</span>	
<span class="linenos">5</span>	Running <span class="hljs-string">"mocha_istanbul:coverage"</span> (mocha_istanbul) task
<span class="linenos">6</span>	Listening on port <span class="hljs-number">5000</span>
<span class="linenos">7</span>	
<span class="linenos">8</span>	
<span class="linenos">9</span>	  server
<span class="linenos">10</span>	Starting the server
<span class="linenos">11</span>	    Test the index route
<span class="linenos">12</span>	      ✓ should <span class="hljs-built_in">return</span> a page with the title Shortbread (<span class="hljs-number">252</span>ms)
<span class="linenos">13</span>	    Test submitting a URL
<span class="linenos">14</span>	      ✓ should <span class="hljs-built_in">return</span> the shortened URL (<span class="hljs-number">47</span>ms)
<span class="linenos">15</span>	    Test following a URL
<span class="linenos">16</span>	      <span class="hljs-number">1</span>) should redirect the user to the shortened URL
<span class="linenos">17</span>	    Test following a non-existent-link
<span class="linenos">18</span>	      <span class="hljs-number">2</span>) should <span class="hljs-built_in">return</span> a <span class="hljs-number">404</span> error
<span class="linenos">19</span>	Stopping the server
<span class="linenos">20</span>	
<span class="linenos">21</span>	
<span class="linenos">22</span>	  <span class="hljs-number">2</span> passing (<span class="hljs-number">322</span>ms)
<span class="linenos">23</span>	  <span class="hljs-number">2</span> failing
<span class="linenos">24</span>	
<span class="linenos">25</span>	  <span class="hljs-number">1</span>) server Test following a URL should redirect the user to the shortened URL:
<span class="linenos">26</span>	     Uncaught AssertionError: expected undefined to equal <span class="hljs-string">'http://www.google.com'</span>
<span class="linenos">27</span>	      at Request._callback (/Users/matthewdaly/Projects/url-shortener/<span class="hljs-built_in">test</span>/test.js:<span class="hljs-number">63</span>:<span class="hljs-number">58</span>)
<span class="linenos">28</span>	      at Request.self.callback (/Users/matthewdaly/Projects/url-shortener/node_modules/request/request.js:<span class="hljs-number">372</span>:<span class="hljs-number">22</span>)
<span class="linenos">29</span>	      at Request.emit (events.js:<span class="hljs-number">98</span>:<span class="hljs-number">17</span>)
<span class="linenos">30</span>	      at Request.&lt;anonymous&gt; (/Users/matthewdaly/Projects/url-shortener/node_modules/request/request.js:<span class="hljs-number">1310</span>:<span class="hljs-number">14</span>)
<span class="linenos">31</span>	      at Request.emit (events.js:<span class="hljs-number">117</span>:<span class="hljs-number">20</span>)
<span class="linenos">32</span>	      at IncomingMessage.&lt;anonymous&gt; (/Users/matthewdaly/Projects/url-shortener/node_modules/request/request.js:<span class="hljs-number">1258</span>:<span class="hljs-number">12</span>)
<span class="linenos">33</span>	      at IncomingMessage.emit (events.js:<span class="hljs-number">117</span>:<span class="hljs-number">20</span>)
<span class="linenos">34</span>	      at _stream_readable.js:<span class="hljs-number">943</span>:<span class="hljs-number">16</span>
<span class="linenos">35</span>	      at process._tickCallback (node.js:<span class="hljs-number">419</span>:<span class="hljs-number">13</span>)
<span class="linenos">36</span>	
<span class="linenos">37</span>	  <span class="hljs-number">2</span>) server Test following a non-existent-link should <span class="hljs-built_in">return</span> a <span class="hljs-number">404</span> error:
<span class="linenos">38</span>	     Uncaught AssertionError: expected <span class="hljs-string">'Cannot GET /nonexistenturl\n'</span> to include <span class="hljs-string">'Link not found'</span>
<span class="linenos">39</span>	      at Request._callback (/Users/matthewdaly/Projects/url-shortener/<span class="hljs-built_in">test</span>/test.js:<span class="hljs-number">80</span>:<span class="hljs-number">33</span>)
<span class="linenos">40</span>	      at Request.self.callback (/Users/matthewdaly/Projects/url-shortener/node_modules/request/request.js:<span class="hljs-number">372</span>:<span class="hljs-number">22</span>)
<span class="linenos">41</span>	      at Request.emit (events.js:<span class="hljs-number">98</span>:<span class="hljs-number">17</span>)
<span class="linenos">42</span>	      at Request.&lt;anonymous&gt; (/Users/matthewdaly/Projects/url-shortener/node_modules/request/request.js:<span class="hljs-number">1310</span>:<span class="hljs-number">14</span>)
<span class="linenos">43</span>	      at Request.emit (events.js:<span class="hljs-number">117</span>:<span class="hljs-number">20</span>)
<span class="linenos">44</span>	      at IncomingMessage.&lt;anonymous&gt; (/Users/matthewdaly/Projects/url-shortener/node_modules/request/request.js:<span class="hljs-number">1258</span>:<span class="hljs-number">12</span>)
<span class="linenos">45</span>	      at IncomingMessage.emit (events.js:<span class="hljs-number">117</span>:<span class="hljs-number">20</span>)
<span class="linenos">46</span>	      at _stream_readable.js:<span class="hljs-number">943</span>:<span class="hljs-number">16</span>
<span class="linenos">47</span>	      at process._tickCallback (node.js:<span class="hljs-number">419</span>:<span class="hljs-number">13</span>)
<span class="linenos">48</span>	
<span class="linenos">49</span>	
<span class="linenos">50</span>	
<span class="linenos">51</span>	=============================================================================
<span class="linenos">52</span>	Writing coverage object [/Users/matthewdaly/Projects/url-shortener/coverage/coverage.json]
<span class="linenos">53</span>	Writing coverage reports at [/Users/matthewdaly/Projects/url-shortener/coverage]
<span class="linenos">54</span>	=============================================================================
<span class="linenos">55</span>	
<span class="linenos">56</span>	=============================== Coverage summary ===============================
<span class="linenos">57</span>	Statements   : <span class="hljs-number">100</span>% ( <span class="hljs-number">29</span>/<span class="hljs-number">29</span> ), <span class="hljs-number">3</span> ignored
<span class="linenos">58</span>	Branches     : <span class="hljs-number">100</span>% ( <span class="hljs-number">6</span>/<span class="hljs-number">6</span> ), <span class="hljs-number">1</span> ignored
<span class="linenos">59</span>	Functions    : <span class="hljs-number">100</span>% ( <span class="hljs-number">3</span>/<span class="hljs-number">3</span> )
<span class="linenos">60</span>	Lines        : <span class="hljs-number">100</span>% ( <span class="hljs-number">29</span>/<span class="hljs-number">29</span> )
<span class="linenos">61</span>	================================================================================
<span class="linenos">62</span>	&gt;&gt;
<span class="linenos">63</span>	Warning: Task <span class="hljs-string">"mocha_istanbul:coverage"</span> failed. Use --force to continue.
<span class="linenos">64</span>	
<span class="linenos">65</span>	Aborted due to warnings.</code></pre>
<p>Now, let’s add our final route:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	<span class="hljs-comment">// Define link route</span>
<span class="linenos">2</span>	app.route(<span class="hljs-string">'/:id'</span>).all(<span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(req, res)</span> </span>{
<span class="linenos">3</span>	    <span class="hljs-comment">// Get ID</span>
<span class="linenos">4</span>	    <span class="hljs-keyword">var</span> id = req.params.id.trim();
<span class="linenos">5</span>	
<span class="linenos">6</span>	    <span class="hljs-comment">// Look up the URL</span>
<span class="linenos">7</span>	    client.get(id, <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(err, reply)</span> </span>{
<span class="linenos">8</span>	        <span class="hljs-keyword">if</span> (!err &amp;&amp; reply) {
<span class="linenos">9</span>	            <span class="hljs-comment">// Redirect user to it</span>
<span class="linenos">10</span>	            res.status(<span class="hljs-number">301</span>);
<span class="linenos">11</span>	            res.set(<span class="hljs-string">'Location'</span>, reply);
<span class="linenos">12</span>	            res.send();
<span class="linenos">13</span>	        } <span class="hljs-keyword">else</span> {
<span class="linenos">14</span>	            <span class="hljs-comment">// Confirm no such link in database</span>
<span class="linenos">15</span>	            res.status(<span class="hljs-number">404</span>);
<span class="linenos">16</span>	            res.render(<span class="hljs-string">'error'</span>);
<span class="linenos">17</span>	        }
<span class="linenos">18</span>	    });
<span class="linenos">19</span>	});</code></pre>
<p>We accept the ID as a parameter in the URL. We trim off any whitespace around it, and then we query Redis for a URL with that ID. If we find one, we set the status code to 301, and the location to the URL, and send the response. Otherwise, we set the status to 404 and render the error view.</p>
<p>Now, let’s check it passes:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ grunt <span class="hljs-built_in">test</span>
<span class="linenos">2</span>	Running <span class="hljs-string">"jshint:all"</span> (jshint) task
<span class="linenos">3</span>	&gt;&gt; <span class="hljs-number">2</span> files lint free.
<span class="linenos">4</span>	
<span class="linenos">5</span>	Running <span class="hljs-string">"mocha_istanbul:coverage"</span> (mocha_istanbul) task
<span class="linenos">6</span>	Listening on port <span class="hljs-number">5000</span>
<span class="linenos">7</span>	
<span class="linenos">8</span>	
<span class="linenos">9</span>	  server
<span class="linenos">10</span>	Starting the server
<span class="linenos">11</span>	    Test the index route
<span class="linenos">12</span>	      ✓ should <span class="hljs-built_in">return</span> a page with the title Shortbread (<span class="hljs-number">90</span>ms)
<span class="linenos">13</span>	    Test submitting a URL
<span class="linenos">14</span>	      ✓ should <span class="hljs-built_in">return</span> the shortened URL (<span class="hljs-number">47</span>ms)
<span class="linenos">15</span>	    Test following a URL
<span class="linenos">16</span>	      ✓ should redirect the user to the shortened URL
<span class="linenos">17</span>	    Test following a non-existent-link
<span class="linenos">18</span>	      ✓ should <span class="hljs-built_in">return</span> a <span class="hljs-number">404</span> error
<span class="linenos">19</span>	Stopping the server
<span class="linenos">20</span>	
<span class="linenos">21</span>	
<span class="linenos">22</span>	  <span class="hljs-number">4</span> passing (<span class="hljs-number">191</span>ms)
<span class="linenos">23</span>	
<span class="linenos">24</span>	=============================================================================
<span class="linenos">25</span>	Writing coverage object [/Users/matthewdaly/Projects/url-shortener/coverage/coverage.json]
<span class="linenos">26</span>	Writing coverage reports at [/Users/matthewdaly/Projects/url-shortener/coverage]
<span class="linenos">27</span>	=============================================================================
<span class="linenos">28</span>	
<span class="linenos">29</span>	=============================== Coverage summary ===============================
<span class="linenos">30</span>	Statements   : <span class="hljs-number">100</span>% ( <span class="hljs-number">38</span>/<span class="hljs-number">38</span> ), <span class="hljs-number">3</span> ignored
<span class="linenos">31</span>	Branches     : <span class="hljs-number">100</span>% ( <span class="hljs-number">10</span>/<span class="hljs-number">10</span> ), <span class="hljs-number">1</span> ignored
<span class="linenos">32</span>	Functions    : <span class="hljs-number">100</span>% ( <span class="hljs-number">5</span>/<span class="hljs-number">5</span> )
<span class="linenos">33</span>	Lines        : <span class="hljs-number">100</span>% ( <span class="hljs-number">38</span>/<span class="hljs-number">38</span> )
<span class="linenos">34</span>	================================================================================
<span class="linenos">35</span>	&gt;&gt; Done. Check coverage folder.
<span class="linenos">36</span>	
<span class="linenos">37</span>	Done, without errors.</code></pre>
<p>Excellent! Our URL shortener is now complete. From here, deploying it to Heroku is straightforward - you’ll need to install the Redis to Go addon, and refer to Heroku’s documentation on deploying Node.js applications for more details.</p>
<h2 id="wrapping-up">Wrapping up</h2>
<p>You’ll find the source for this application at <a href="https://github.com/matthewbdaly/Shortbread">https://github.com/matthewbdaly/Shortbread</a> and a demo at <a href="http://shortbread-example.herokuapp.com/">http://shortbread-example.herokuapp.com/</a>.</p>
<p>I hope you’ve enjoyed this brief introduction to Redis, and that it’s opened your eyes to at least one of the alternatives out there to a relational database. I’ll hopefully be able to follow this up with examples of some other problems Redis is ideal for solving.</p>
]]></description>
            <link>http://matthewdaly.co.uk/blog/2014/11/09/building-a-url-shortener-with-node-dot-js-and-redis/</link>
            <guid isPermaLink="true">http://matthewdaly.co.uk/blog/2014/11/09/building-a-url-shortener-with-node-dot-js-and-redis/</guid>
            <pubDate>Sun, 09 Nov 2014 17:13:16 GMT</pubDate>
        </item>
        <item>
            <title><![CDATA[My Django web server setup]]></title>
            <description><![CDATA[<p>This isn’t really part of my Django tutorial series (that has now definitely concluded!), but I thought I’d share the setup I generally use for deploying Django applications, partly for my own reference, and partly because it is quite complex, and those readers who don’t wish to deploy to Heroku may want some guidance on how to deploy their new blogs to a VPS.</p>
<h2 id="operating-system">Operating system</h2>
<p>This isn’t actually that much of a big deal, but while I prefer Ubuntu on desktops, I generally use Debian Stable on servers, since it’s fanatically stable.</p>
<h2 id="database-server">Database server</h2>
<p>For my first commercial Django app, I used MySQL. However, South had one or two issues with MySQL, and I figured that since using an ORM and migrations meant that I wouldn’t need to write much SQL anyway, I might as well jump to PostgreSQL for the Django app I’m currently in the process of deploying at work. So far I haven’t had any problems with it.</p>
<h2 id="web-server">Web server</h2>
<p>It’s customary to use two web servers with Django. One handles the static content, and reverse proxies everything else to a different port, where another web server serves the dynamic content.</p>
<p>For serving the static files, I use Nginx - it’s generally considered to be faster than Apache for this use case. Here’s a typical Nginx config file:</p>
<pre><code class="hljs lang-nginx"><span class="linenos">1</span>	<span class="hljs-title">server</span> {
<span class="linenos">2</span>	    <span class="hljs-title">listen</span> <span class="hljs-number">80</span>;
<span class="linenos">3</span>	    <span class="hljs-title">server_name</span> example.com;
<span class="linenos">4</span>	    <span class="hljs-title">client_max_body_size</span> <span class="hljs-number">50M</span>;
<span class="linenos">5</span>	
<span class="linenos">6</span>	    <span class="hljs-title">access_log</span> /var/log/nginx/access.log;
<span class="linenos">7</span>	    <span class="hljs-title">error_log</span> /var/log/nginx/error.log;
<span class="linenos">8</span>	
<span class="linenos">9</span>	    <span class="hljs-title">location</span> /static {
<span class="linenos">10</span>	        <span class="hljs-title">root</span> /var/www/mysite;
<span class="linenos">11</span>	    }
<span class="linenos">12</span>	
<span class="linenos">13</span>	    <span class="hljs-title">location</span> /media {
<span class="linenos">14</span>	        <span class="hljs-title">root</span> /var/www/mysite;
<span class="linenos">15</span>	    }
<span class="linenos">16</span>	
<span class="linenos">17</span>	    <span class="hljs-title">location</span> / {
<span class="linenos">18</span>	        <span class="hljs-title">proxy_pass</span> <span class="hljs-url">http://127.0.0.1:8000</span>;
<span class="linenos">19</span>	    }
<span class="linenos">20</span>	}</code></pre>
<p>For the application server, I use Gunicorn. You can install this with <code>pip install gunicorn</code>, then add it to <code>INSTALLED_APPS</code>. Then add the following config file to the root of your project, as <code>gunicorn.conf.py</code>:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	bind = <span class="hljs-string">"127.0.0.1:8000"</span>
<span class="linenos">2</span>	logfile = <span class="hljs-string">"/var/log/gunicorn.log"</span>
<span class="linenos">3</span>	loglevel = <span class="hljs-string">"debug"</span>
<span class="linenos">4</span>	workers = <span class="hljs-number">3</span></code></pre>
<p>You should normally set the number of workers to 2 times the number of cores on your machine, plus one.</p>
<p>In order to keep Gunicorn running, I use Supervisor. As the installation commands will depend on your OS, I won’t give details here - your package manager of choice should have a suitable package available. Here’s a typical Supervisor config file I might use for running Gunicorn for a Django app, named <code>mysite-supervisor.conf</code>:</p>
<pre><code class="hljs lang-ini"><span class="linenos">1</span>	<span class="hljs-title">[program:mysite]</span>
<span class="linenos">2</span>	<span class="hljs-setting">command=<span class="hljs-value">/var/www/mysite/venv/bin/gunicorn myapp.wsgi:application --workers=<span class="hljs-number">3</span></span></span>
<span class="linenos">3</span>	<span class="hljs-setting">directory=<span class="hljs-value">/var/www/mysite/</span></span>
<span class="linenos">4</span>	<span class="hljs-setting">user=<span class="hljs-value">nobody</span></span>
<span class="linenos">5</span>	<span class="hljs-setting">autostart=<span class="hljs-value"><span class="hljs-keyword">true</span></span></span>
<span class="linenos">6</span>	<span class="hljs-setting">autorestart=<span class="hljs-value"><span class="hljs-keyword">true</span></span></span></code></pre>
<p>Once that’s in place, you can easily add the new app:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ sudo supervisorctl reread
<span class="linenos">2</span>	$ sudo supervisorctl update</code></pre>
<p>Then to start it:</p>
<pre><code class="hljs lang-bash singleline">$ sudo supervisorctl start mysite</code></pre>
<p>Or stop it with:</p>
<pre><code class="hljs lang-bash singleline">$ sudo supervisorctl stop mysite</code></pre>
<p>Or restart it:</p>
<pre><code class="hljs lang-bash singleline">$ sudo supervisorctl restart mysite</code></pre>
<h2 id="celery">Celery</h2>
<p>So far, both of the web apps I’ve built professionally have been ones where it made sense to use <a href="http://www.celeryproject.org/">Celery</a> for some tasks. For the uninitiated, Celery lets you pass a task to a queue to be handled, rather than handling it within the context of the same HTTP request. This offers the following advantages:</p>
<ul>
<li>The user doesn’t need to wait for the task to be completed before getting a response, improving performance</li>
<li>It’s more robust, since if the task fails, it can be automatically retried</li>
<li>The task queue can be moved to another server if desired, making it easier to scale</li>
<li>Scheduling tasks</li>
</ul>
<p>I’ve used it in cases where I needed to send an email or a push notification, since these don’t have to be done within the context of the same HTTP request, but need to be reliable.</p>
<p>I generally use RabbitMQ as my message queue. I’ll leave setting this up as an exercise for the reader since it’s covered pretty well in the Celery documentation, but like with Gunicorn, I use Supervisor to run the Celery worker. Here’s a typical config file, which might be called <code>celery-supervisor.conf</code>:</p>
<pre><code class="hljs lang-ini"><span class="linenos">1</span>	<span class="hljs-title">[program:celeryd]</span>
<span class="linenos">2</span>	<span class="hljs-setting">command=<span class="hljs-value">/var/www/mysite/venv/bin/python manage.py celery worker</span></span>
<span class="linenos">3</span>	<span class="hljs-setting">directory=<span class="hljs-value">/var/www/mysite/</span></span>
<span class="linenos">4</span>	<span class="hljs-setting">user=<span class="hljs-value">nobody</span></span>
<span class="linenos">5</span>	<span class="hljs-setting">autostart=<span class="hljs-value"><span class="hljs-keyword">true</span></span></span>
<span class="linenos">6</span>	<span class="hljs-setting">autorestart=<span class="hljs-value"><span class="hljs-keyword">true</span></span></span></code></pre>
<p>Then start it up:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ sudo supervisorctl reread
<span class="linenos">2</span>	$ sudo supervisorctl update
<span class="linenos">3</span>	$ sudo supervisorctl start celeryd</code></pre>
<p>I make no claims about how good this setup is, but it works well for me. I haven’t yet had the occasion to deploy a Django app to anywhere other than Heroku that really benefited from caching, so I haven’t got any tips to share about that, but if I were building a content-driven web app, I would use Memcached since it’s well-supported.</p>
]]></description>
            <link>http://matthewdaly.co.uk/blog/2014/10/19/my-django-web-server-setup/</link>
            <guid isPermaLink="true">http://matthewdaly.co.uk/blog/2014/10/19/my-django-web-server-setup/</guid>
            <pubDate>Sun, 19 Oct 2014 18:52:28 GMT</pubDate>
        </item>
        <item>
            <title><![CDATA[Introducing generator-simple-static-blog]]></title>
            <description><![CDATA[<p>I’m a big fan of static site generators. I ditched WordPress for Octopress over two years ago because it was free to host on GitHub Pages and much faster, had much better syntax highlighting, and I liked being able to write posts in Vim, and I’ve never looked back since.</p>
<p>That said, Octopress is written in Ruby, a language I’ve never been that keen on. Ideally I’d prefer to use Python or JavaScript, but none of the solutions I’ve found have been to my liking. Recently I’ve been using Grunt and Yeoman to some extent, and I’ve wondered about the idea of creating a Yeoman generator to build a static blogging engine. After discovering <code>grunt-markdown-blog</code>, I took the plunge and have built a simple blog generator called <a href="https://github.com/matthewbdaly/generator-simple-static-blog">generator-simple-static-blog</a>.</p>
<p>I’ve published it to NPM, so feel free to check it out. It includes code highlighting with the Zenburn colour scheme by default (although highlight.js includes many other themes, so just switch to another one if you want), and it should be easy to edit the templates. I’ve also included the ability to deploy automatically to GitHub Pages using Grunt.</p>
<p>I don’t anticipate moving over to this from Octopress for the foreseeable future, but it’s been an interesting project for the weekend.</p>
]]></description>
            <link>http://matthewdaly.co.uk/blog/2014/10/05/introducing-generator-simple-static-blog/</link>
            <guid isPermaLink="true">http://matthewdaly.co.uk/blog/2014/10/05/introducing-generator-simple-static-blog/</guid>
            <pubDate>Sun, 05 Oct 2014 18:56:46 GMT</pubDate>
        </item>
        <item>
            <title><![CDATA[Django Blog Tutorial - the Next Generation - Part 9]]></title>
            <description><![CDATA[<p>Yes, I know the eight instalment was meant to be the last one! Within 24 hours of that post going live, Django 1.7 was released, so naturally I’d like to show you how to upgrade to it.</p>
<p>The biggest change is that Django 1.7 introduces its own migration system, which means South is now surplus to requirements. We therefore need to switch from South to Django’s native migrations. Fortunately, this is fairly straightforward.</p>
<p>First of all, activate your virtualenv:</p>
<pre><code class="hljs lang-bash singleline">$ virtualenv venv</code></pre>
<p>Then make sure your migrations are up to date:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py syncdb
<span class="linenos">2</span>	$ python manage.py migrate</code></pre>
<p>Then, upgrade your Django version and uninstall South:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ pip install Django --upgrade
<span class="linenos">2</span>	$ pip uninstall South
<span class="linenos">3</span>	$ pip freeze &gt; requirements.txt</code></pre>
<p>Next, remove South from <code>INSTALLED_APPS</code> in <code>django_tutorial_blog_ng/settings.py</code>.</p>
<p>You now need to delete all of the numbered migration files in <code>blogengine/migrations/</code>, and the relevant <code>.pyc</code> files, but NOT the directory or the <code>__init__.py</code> file. You can do so with this command on Linux or OS X:</p>
<pre><code class="hljs lang-bash singleline">$ rm blogengine/migrations/<span class="hljs-number">00</span>*</code></pre>
<p>Next, we recreate our migrations with the following command:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py makemigrations
<span class="linenos">2</span>	Migrations <span class="hljs-keyword">for</span> <span class="hljs-string">'blogengine'</span>:
<span class="linenos">3</span>	  <span class="hljs-number">0001</span>_initial.py:
<span class="linenos">4</span>	    - Create model Category
<span class="linenos">5</span>	    - Create model Post
<span class="linenos">6</span>	    - Create model Tag
<span class="linenos">7</span>	    - Add field tags to post</code></pre>
<p>Then we run the migrations:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py migrate
<span class="linenos">2</span>	Operations to perform:
<span class="linenos">3</span>	  Synchronize unmigrated apps: sitemaps, django_jenkins, debug_toolbar
<span class="linenos">4</span>	  Apply all migrations: sessions, admin, sites, flatpages, contenttypes, auth, blogengine
<span class="linenos">5</span>	Synchronizing apps without migrations:
<span class="linenos">6</span>	  Creating tables...
<span class="linenos">7</span>	  Installing custom SQL...
<span class="linenos">8</span>	  Installing indexes...
<span class="linenos">9</span>	Running migrations:
<span class="linenos">10</span>	  Applying contenttypes.<span class="hljs-number">0001</span>_initial... FAKED
<span class="linenos">11</span>	  Applying auth.<span class="hljs-number">0001</span>_initial... FAKED
<span class="linenos">12</span>	  Applying admin.<span class="hljs-number">0001</span>_initial... FAKED
<span class="linenos">13</span>	  Applying sites.<span class="hljs-number">0001</span>_initial... FAKED
<span class="linenos">14</span>	  Applying blogengine.<span class="hljs-number">0001</span>_initial... FAKED
<span class="linenos">15</span>	  Applying flatpages.<span class="hljs-number">0001</span>_initial... FAKED
<span class="linenos">16</span>	  Applying sessions.<span class="hljs-number">0001</span>_initial... FAKED</code></pre>
<p>Don’t worry too much if the output doesn’t look exactly the same as this - as long as it works, that’s the main thing.</p>
<p>Let’s run our test suite to ensure it works:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py jenkins
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	....FF.F.FFFFFF..............
<span class="linenos">4</span>	======================================================================
<span class="linenos">5</span>	FAIL: <span class="hljs-built_in">test</span>_create_post (blogengine.tests.AdminTest)
<span class="linenos">6</span>	----------------------------------------------------------------------
<span class="linenos">7</span>	Traceback (most recent call last):
<span class="linenos">8</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">385</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_create_post
<span class="linenos">9</span>	    self.assertTrue(<span class="hljs-string">'added successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">10</span>	AssertionError: False is not <span class="hljs-literal">true</span>
<span class="linenos">11</span>	
<span class="linenos">12</span>	======================================================================
<span class="linenos">13</span>	FAIL: <span class="hljs-built_in">test</span>_create_post_without_tag (blogengine.tests.AdminTest)
<span class="linenos">14</span>	----------------------------------------------------------------------
<span class="linenos">15</span>	Traceback (most recent call last):
<span class="linenos">16</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">417</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_create_post_without_tag
<span class="linenos">17</span>	    self.assertTrue(<span class="hljs-string">'added successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">18</span>	AssertionError: False is not <span class="hljs-literal">true</span>
<span class="linenos">19</span>	
<span class="linenos">20</span>	======================================================================
<span class="linenos">21</span>	FAIL: <span class="hljs-built_in">test</span>_delete_category (blogengine.tests.AdminTest)
<span class="linenos">22</span>	----------------------------------------------------------------------
<span class="linenos">23</span>	Traceback (most recent call last):
<span class="linenos">24</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">278</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_delete_category
<span class="linenos">25</span>	    self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">26</span>	AssertionError: <span class="hljs-number">404</span> != <span class="hljs-number">200</span>
<span class="linenos">27</span>	
<span class="linenos">28</span>	======================================================================
<span class="linenos">29</span>	FAIL: <span class="hljs-built_in">test</span>_delete_tag (blogengine.tests.AdminTest)
<span class="linenos">30</span>	----------------------------------------------------------------------
<span class="linenos">31</span>	Traceback (most recent call last):
<span class="linenos">32</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">346</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_delete_tag
<span class="linenos">33</span>	    self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">34</span>	AssertionError: <span class="hljs-number">404</span> != <span class="hljs-number">200</span>
<span class="linenos">35</span>	
<span class="linenos">36</span>	======================================================================
<span class="linenos">37</span>	FAIL: <span class="hljs-built_in">test</span>_edit_category (blogengine.tests.AdminTest)
<span class="linenos">38</span>	----------------------------------------------------------------------
<span class="linenos">39</span>	Traceback (most recent call last):
<span class="linenos">40</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">255</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_edit_category
<span class="linenos">41</span>	    self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">42</span>	AssertionError: <span class="hljs-number">404</span> != <span class="hljs-number">200</span>
<span class="linenos">43</span>	
<span class="linenos">44</span>	======================================================================
<span class="linenos">45</span>	FAIL: <span class="hljs-built_in">test</span>_edit_post (blogengine.tests.AdminTest)
<span class="linenos">46</span>	----------------------------------------------------------------------
<span class="linenos">47</span>	Traceback (most recent call last):
<span class="linenos">48</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">447</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_edit_post
<span class="linenos">49</span>	    self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">50</span>	AssertionError: <span class="hljs-number">404</span> != <span class="hljs-number">200</span>
<span class="linenos">51</span>	
<span class="linenos">52</span>	======================================================================
<span class="linenos">53</span>	FAIL: <span class="hljs-built_in">test</span>_edit_tag (blogengine.tests.AdminTest)
<span class="linenos">54</span>	----------------------------------------------------------------------
<span class="linenos">55</span>	Traceback (most recent call last):
<span class="linenos">56</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">323</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_edit_tag
<span class="linenos">57</span>	    self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">58</span>	AssertionError: <span class="hljs-number">404</span> != <span class="hljs-number">200</span>
<span class="linenos">59</span>	
<span class="linenos">60</span>	======================================================================
<span class="linenos">61</span>	FAIL: <span class="hljs-built_in">test</span>_login (blogengine.tests.AdminTest)
<span class="linenos">62</span>	----------------------------------------------------------------------
<span class="linenos">63</span>	Traceback (most recent call last):
<span class="linenos">64</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">183</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_login
<span class="linenos">65</span>	    self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">66</span>	AssertionError: <span class="hljs-number">302</span> != <span class="hljs-number">200</span>
<span class="linenos">67</span>	
<span class="linenos">68</span>	======================================================================
<span class="linenos">69</span>	FAIL: <span class="hljs-built_in">test</span>_<span class="hljs-built_in">logout</span> (blogengine.tests.AdminTest)
<span class="linenos">70</span>	----------------------------------------------------------------------
<span class="linenos">71</span>	Traceback (most recent call last):
<span class="linenos">72</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">214</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_<span class="hljs-built_in">logout</span>
<span class="linenos">73</span>	    self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">74</span>	AssertionError: <span class="hljs-number">302</span> != <span class="hljs-number">200</span>
<span class="linenos">75</span>	
<span class="linenos">76</span>	----------------------------------------------------------------------
<span class="linenos">77</span>	Ran <span class="hljs-number">29</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">7.383</span>s
<span class="linenos">78</span>	
<span class="linenos">79</span>	FAILED (failures=<span class="hljs-number">9</span>)
<span class="linenos">80</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>We have an issue here. A load of the tests for the admin interface now fail. If we now try running the dev server, we see this error:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py runserver
<span class="linenos">2</span>	Performing system checks...
<span class="linenos">3</span>	
<span class="linenos">4</span>	System check identified no issues (<span class="hljs-number">0</span> silenced).
<span class="linenos">5</span>	September <span class="hljs-number">28</span>, <span class="hljs-number">2014</span> - <span class="hljs-number">20</span>:<span class="hljs-number">16</span>:<span class="hljs-number">47</span>
<span class="linenos">6</span>	Django version <span class="hljs-number">1.7</span>, using settings <span class="hljs-string">'django_tutorial_blog_ng.settings'</span>
<span class="linenos">7</span>	Starting development server at http://<span class="hljs-number">127.0</span>.<span class="hljs-number">0.1</span>:<span class="hljs-number">8000</span>/
<span class="linenos">8</span>	Quit the server with CONTROL-C.
<span class="linenos">9</span>	Unhandled exception <span class="hljs-keyword">in</span> thread started by &lt;<span class="hljs-keyword">function</span> wrapper at <span class="hljs-number">0</span>x1024a5ed8&gt;
<span class="linenos">10</span>	Traceback (most recent call last):
<span class="linenos">11</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/utils/autoreload.py"</span>, line <span class="hljs-number">222</span>, <span class="hljs-keyword">in</span> wrapper
<span class="linenos">12</span>	    fn(*args, **kwargs)
<span class="linenos">13</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/core/management/commands/runserver.py"</span>, line <span class="hljs-number">132</span>, <span class="hljs-keyword">in</span> inner_run
<span class="linenos">14</span>	    handler = self.get_handler(*args, **options)
<span class="linenos">15</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/contrib/staticfiles/management/commands/runserver.py"</span>, line <span class="hljs-number">25</span>, <span class="hljs-keyword">in</span> get_handler
<span class="linenos">16</span>	    handler = super(Command, self).get_handler(*args, **options)
<span class="linenos">17</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/core/management/commands/runserver.py"</span>, line <span class="hljs-number">48</span>, <span class="hljs-keyword">in</span> get_handler
<span class="linenos">18</span>	    <span class="hljs-built_in">return</span> get_internal_wsgi_application()
<span class="linenos">19</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/core/servers/basehttp.py"</span>, line <span class="hljs-number">66</span>, <span class="hljs-keyword">in</span> get_internal_wsgi_application
<span class="linenos">20</span>	    sys.exc_info()[<span class="hljs-number">2</span>])
<span class="linenos">21</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/core/servers/basehttp.py"</span>, line <span class="hljs-number">56</span>, <span class="hljs-keyword">in</span> get_internal_wsgi_application
<span class="linenos">22</span>	    <span class="hljs-built_in">return</span> import_string(app_path)
<span class="linenos">23</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/utils/module_loading.py"</span>, line <span class="hljs-number">26</span>, <span class="hljs-keyword">in</span> import_string
<span class="linenos">24</span>	    module = import_module(module_path)
<span class="linenos">25</span>	  File <span class="hljs-string">"/usr/local/Cellar/python/2.7.8_1/Frameworks/Python.framework/Versions/2.7/lib/python2.7/importlib/__init__.py"</span>, line <span class="hljs-number">37</span>, <span class="hljs-keyword">in</span> import_module
<span class="linenos">26</span>	    __import__(name)
<span class="linenos">27</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/django_tutorial_blog_ng/wsgi.py"</span>, line <span class="hljs-number">14</span>, <span class="hljs-keyword">in</span> &lt;module&gt;
<span class="linenos">28</span>	    from dj_static import Cling
<span class="linenos">29</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/dj_static.py"</span>, line <span class="hljs-number">7</span>, <span class="hljs-keyword">in</span> &lt;module&gt;
<span class="linenos">30</span>	    from django.core.handlers.base import get_path_info
<span class="linenos">31</span>	django.core.exceptions.ImproperlyConfigured: WSGI application <span class="hljs-string">'django_tutorial_blog_ng.wsgi.application'</span> could not be loaded; Error importing module: <span class="hljs-string">'cannot import name get_path_info'</span></code></pre>
<p>Fortunately, the error above is easy to fix by upgrading <code>dj_static</code>:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ pip install dj_static --upgrade
<span class="linenos">2</span>	$ pip freeze &gt; requirements.txt</code></pre>
<p>That resolves the error in serving static files, but not the error with the admin. If you run the dev server, you’ll be able to see that the admin actually works fine. The problem is caused by the test client not following redirects in the admin. We can easily run just the admin tests with the following command:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py <span class="hljs-built_in">test</span> blogengine.tests.AdminTest
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	.FF.F.FFFFFF
<span class="linenos">4</span>	======================================================================
<span class="linenos">5</span>	FAIL: <span class="hljs-built_in">test</span>_create_post (blogengine.tests.AdminTest)
<span class="linenos">6</span>	----------------------------------------------------------------------
<span class="linenos">7</span>	Traceback (most recent call last):
<span class="linenos">8</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">385</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_create_post
<span class="linenos">9</span>	    self.assertTrue(<span class="hljs-string">'added successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">10</span>	AssertionError: False is not <span class="hljs-literal">true</span>
<span class="linenos">11</span>	
<span class="linenos">12</span>	======================================================================
<span class="linenos">13</span>	FAIL: <span class="hljs-built_in">test</span>_create_post_without_tag (blogengine.tests.AdminTest)
<span class="linenos">14</span>	----------------------------------------------------------------------
<span class="linenos">15</span>	Traceback (most recent call last):
<span class="linenos">16</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">417</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_create_post_without_tag
<span class="linenos">17</span>	    self.assertTrue(<span class="hljs-string">'added successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">18</span>	AssertionError: False is not <span class="hljs-literal">true</span>
<span class="linenos">19</span>	
<span class="linenos">20</span>	======================================================================
<span class="linenos">21</span>	FAIL: <span class="hljs-built_in">test</span>_delete_category (blogengine.tests.AdminTest)
<span class="linenos">22</span>	----------------------------------------------------------------------
<span class="linenos">23</span>	Traceback (most recent call last):
<span class="linenos">24</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">278</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_delete_category
<span class="linenos">25</span>	    self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">26</span>	AssertionError: <span class="hljs-number">404</span> != <span class="hljs-number">200</span>
<span class="linenos">27</span>	
<span class="linenos">28</span>	======================================================================
<span class="linenos">29</span>	FAIL: <span class="hljs-built_in">test</span>_delete_tag (blogengine.tests.AdminTest)
<span class="linenos">30</span>	----------------------------------------------------------------------
<span class="linenos">31</span>	Traceback (most recent call last):
<span class="linenos">32</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">346</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_delete_tag
<span class="linenos">33</span>	    self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">34</span>	AssertionError: <span class="hljs-number">404</span> != <span class="hljs-number">200</span>
<span class="linenos">35</span>	
<span class="linenos">36</span>	======================================================================
<span class="linenos">37</span>	FAIL: <span class="hljs-built_in">test</span>_edit_category (blogengine.tests.AdminTest)
<span class="linenos">38</span>	----------------------------------------------------------------------
<span class="linenos">39</span>	Traceback (most recent call last):
<span class="linenos">40</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">255</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_edit_category
<span class="linenos">41</span>	    self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">42</span>	AssertionError: <span class="hljs-number">404</span> != <span class="hljs-number">200</span>
<span class="linenos">43</span>	
<span class="linenos">44</span>	======================================================================
<span class="linenos">45</span>	FAIL: <span class="hljs-built_in">test</span>_edit_post (blogengine.tests.AdminTest)
<span class="linenos">46</span>	----------------------------------------------------------------------
<span class="linenos">47</span>	Traceback (most recent call last):
<span class="linenos">48</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">447</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_edit_post
<span class="linenos">49</span>	    self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">50</span>	AssertionError: <span class="hljs-number">404</span> != <span class="hljs-number">200</span>
<span class="linenos">51</span>	
<span class="linenos">52</span>	======================================================================
<span class="linenos">53</span>	FAIL: <span class="hljs-built_in">test</span>_edit_tag (blogengine.tests.AdminTest)
<span class="linenos">54</span>	----------------------------------------------------------------------
<span class="linenos">55</span>	Traceback (most recent call last):
<span class="linenos">56</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">323</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_edit_tag
<span class="linenos">57</span>	    self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">58</span>	AssertionError: <span class="hljs-number">404</span> != <span class="hljs-number">200</span>
<span class="linenos">59</span>	
<span class="linenos">60</span>	======================================================================
<span class="linenos">61</span>	FAIL: <span class="hljs-built_in">test</span>_login (blogengine.tests.AdminTest)
<span class="linenos">62</span>	----------------------------------------------------------------------
<span class="linenos">63</span>	Traceback (most recent call last):
<span class="linenos">64</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">183</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_login
<span class="linenos">65</span>	    self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">66</span>	AssertionError: <span class="hljs-number">302</span> != <span class="hljs-number">200</span>
<span class="linenos">67</span>	
<span class="linenos">68</span>	======================================================================
<span class="linenos">69</span>	FAIL: <span class="hljs-built_in">test</span>_<span class="hljs-built_in">logout</span> (blogengine.tests.AdminTest)
<span class="linenos">70</span>	----------------------------------------------------------------------
<span class="linenos">71</span>	Traceback (most recent call last):
<span class="linenos">72</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">214</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_<span class="hljs-built_in">logout</span>
<span class="linenos">73</span>	    self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">74</span>	AssertionError: <span class="hljs-number">302</span> != <span class="hljs-number">200</span>
<span class="linenos">75</span>	
<span class="linenos">76</span>	----------------------------------------------------------------------
<span class="linenos">77</span>	Ran <span class="hljs-number">12</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">3.283</span>s
<span class="linenos">78</span>	
<span class="linenos">79</span>	FAILED (failures=<span class="hljs-number">9</span>)
<span class="linenos">80</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>Let’s commit our changes so far first:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add django_tutorial_blog_ng/ requirements.txt blogengine/
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Upgraded to Django 1.7'</span></code></pre>
<p>Now let’s fix our tests. Here’s the amended version of the <code>AdminTest</code> class:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">AdminTest</span><span class="hljs-params">(BaseAcceptanceTest)</span>:</span>
<span class="linenos">2</span>	    fixtures = [<span class="hljs-string">'users.json'</span>]
<span class="linenos">3</span>	
<span class="linenos">4</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_login</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">5</span>	        <span class="hljs-comment"># Get login page</span>
<span class="linenos">6</span>	        response = self.client.get(<span class="hljs-string">'/admin/'</span>, follow=<span class="hljs-keyword">True</span>)
<span class="linenos">7</span>	
<span class="linenos">8</span>	        <span class="hljs-comment"># Check response code</span>
<span class="linenos">9</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">10</span>	
<span class="linenos">11</span>	        <span class="hljs-comment"># Check 'Log in' in response</span>
<span class="linenos">12</span>	        self.assertTrue(<span class="hljs-string">'Log in'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">13</span>	
<span class="linenos">14</span>	        <span class="hljs-comment"># Log the user in</span>
<span class="linenos">15</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">16</span>	
<span class="linenos">17</span>	        <span class="hljs-comment"># Check response code</span>
<span class="linenos">18</span>	        response = self.client.get(<span class="hljs-string">'/admin/'</span>)
<span class="linenos">19</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">20</span>	
<span class="linenos">21</span>	        <span class="hljs-comment"># Check 'Log out' in response</span>
<span class="linenos">22</span>	        self.assertTrue(<span class="hljs-string">'Log out'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">23</span>	
<span class="linenos">24</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_logout</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">25</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">26</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">27</span>	
<span class="linenos">28</span>	        <span class="hljs-comment"># Check response code</span>
<span class="linenos">29</span>	        response = self.client.get(<span class="hljs-string">'/admin/'</span>)
<span class="linenos">30</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">31</span>	
<span class="linenos">32</span>	        <span class="hljs-comment"># Check 'Log out' in response</span>
<span class="linenos">33</span>	        self.assertTrue(<span class="hljs-string">'Log out'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">34</span>	
<span class="linenos">35</span>	        <span class="hljs-comment"># Log out</span>
<span class="linenos">36</span>	        self.client.logout()
<span class="linenos">37</span>	
<span class="linenos">38</span>	        <span class="hljs-comment"># Check response code</span>
<span class="linenos">39</span>	        response = self.client.get(<span class="hljs-string">'/admin/'</span>, follow=<span class="hljs-keyword">True</span>)
<span class="linenos">40</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">41</span>	
<span class="linenos">42</span>	        <span class="hljs-comment"># Check 'Log in' in response</span>
<span class="linenos">43</span>	        self.assertTrue(<span class="hljs-string">'Log in'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">44</span>	
<span class="linenos">45</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_create_category</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">46</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">47</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">48</span>	
<span class="linenos">49</span>	        <span class="hljs-comment"># Check response code</span>
<span class="linenos">50</span>	        response = self.client.get(<span class="hljs-string">'/admin/blogengine/category/add/'</span>)
<span class="linenos">51</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">52</span>	
<span class="linenos">53</span>	        <span class="hljs-comment"># Create the new category</span>
<span class="linenos">54</span>	        response = self.client.post(<span class="hljs-string">'/admin/blogengine/category/add/'</span>, {
<span class="linenos">55</span>	            <span class="hljs-string">'name'</span>: <span class="hljs-string">'python'</span>,
<span class="linenos">56</span>	            <span class="hljs-string">'description'</span>: <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">57</span>	            },
<span class="linenos">58</span>	            follow=<span class="hljs-keyword">True</span>
<span class="linenos">59</span>	        )
<span class="linenos">60</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">61</span>	
<span class="linenos">62</span>	        <span class="hljs-comment"># Check added successfully</span>
<span class="linenos">63</span>	        self.assertTrue(<span class="hljs-string">'added successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">64</span>	
<span class="linenos">65</span>	        <span class="hljs-comment"># Check new category now in database</span>
<span class="linenos">66</span>	        all_categories = Category.objects.all()
<span class="linenos">67</span>	        self.assertEquals(len(all_categories), <span class="hljs-number">1</span>)
<span class="linenos">68</span>	
<span class="linenos">69</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_edit_category</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">70</span>	        <span class="hljs-comment"># Create the category</span>
<span class="linenos">71</span>	        category = CategoryFactory()
<span class="linenos">72</span>	
<span class="linenos">73</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">74</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">75</span>	
<span class="linenos">76</span>	        <span class="hljs-comment"># Edit the category</span>
<span class="linenos">77</span>	        response = self.client.post(<span class="hljs-string">'/admin/blogengine/category/'</span> + str(category.pk) + <span class="hljs-string">'/'</span>, {
<span class="linenos">78</span>	            <span class="hljs-string">'name'</span>: <span class="hljs-string">'perl'</span>,
<span class="linenos">79</span>	            <span class="hljs-string">'description'</span>: <span class="hljs-string">'The Perl programming language'</span>
<span class="linenos">80</span>	            }, follow=<span class="hljs-keyword">True</span>)
<span class="linenos">81</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">82</span>	
<span class="linenos">83</span>	        <span class="hljs-comment"># Check changed successfully</span>
<span class="linenos">84</span>	        self.assertTrue(<span class="hljs-string">'changed successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">85</span>	
<span class="linenos">86</span>	        <span class="hljs-comment"># Check category amended</span>
<span class="linenos">87</span>	        all_categories = Category.objects.all()
<span class="linenos">88</span>	        self.assertEquals(len(all_categories), <span class="hljs-number">1</span>)
<span class="linenos">89</span>	        only_category = all_categories[<span class="hljs-number">0</span>]
<span class="linenos">90</span>	        self.assertEquals(only_category.name, <span class="hljs-string">'perl'</span>)
<span class="linenos">91</span>	        self.assertEquals(only_category.description, <span class="hljs-string">'The Perl programming language'</span>)
<span class="linenos">92</span>	
<span class="linenos">93</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_delete_category</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">94</span>	        <span class="hljs-comment"># Create the category</span>
<span class="linenos">95</span>	        category = CategoryFactory()
<span class="linenos">96</span>	
<span class="linenos">97</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">98</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">99</span>	
<span class="linenos">100</span>	        <span class="hljs-comment"># Delete the category</span>
<span class="linenos">101</span>	        response = self.client.post(<span class="hljs-string">'/admin/blogengine/category/'</span> + str(category.pk) + <span class="hljs-string">'/delete/'</span>, {
<span class="linenos">102</span>	            <span class="hljs-string">'post'</span>: <span class="hljs-string">'yes'</span>
<span class="linenos">103</span>	        }, follow=<span class="hljs-keyword">True</span>)
<span class="linenos">104</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">105</span>	
<span class="linenos">106</span>	        <span class="hljs-comment"># Check deleted successfully</span>
<span class="linenos">107</span>	        self.assertTrue(<span class="hljs-string">'deleted successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">108</span>	
<span class="linenos">109</span>	        <span class="hljs-comment"># Check category deleted</span>
<span class="linenos">110</span>	        all_categories = Category.objects.all()
<span class="linenos">111</span>	        self.assertEquals(len(all_categories), <span class="hljs-number">0</span>)
<span class="linenos">112</span>	
<span class="linenos">113</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_create_tag</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">114</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">115</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">116</span>	
<span class="linenos">117</span>	        <span class="hljs-comment"># Check response code</span>
<span class="linenos">118</span>	        response = self.client.get(<span class="hljs-string">'/admin/blogengine/tag/add/'</span>)
<span class="linenos">119</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">120</span>	
<span class="linenos">121</span>	        <span class="hljs-comment"># Create the new tag</span>
<span class="linenos">122</span>	        response = self.client.post(<span class="hljs-string">'/admin/blogengine/tag/add/'</span>, {
<span class="linenos">123</span>	            <span class="hljs-string">'name'</span>: <span class="hljs-string">'python'</span>,
<span class="linenos">124</span>	            <span class="hljs-string">'description'</span>: <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">125</span>	            },
<span class="linenos">126</span>	            follow=<span class="hljs-keyword">True</span>
<span class="linenos">127</span>	        )
<span class="linenos">128</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">129</span>	
<span class="linenos">130</span>	        <span class="hljs-comment"># Check added successfully</span>
<span class="linenos">131</span>	        self.assertTrue(<span class="hljs-string">'added successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">132</span>	
<span class="linenos">133</span>	        <span class="hljs-comment"># Check new tag now in database</span>
<span class="linenos">134</span>	        all_tags = Tag.objects.all()
<span class="linenos">135</span>	        self.assertEquals(len(all_tags), <span class="hljs-number">1</span>)
<span class="linenos">136</span>	
<span class="linenos">137</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_edit_tag</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">138</span>	        <span class="hljs-comment"># Create the tag</span>
<span class="linenos">139</span>	        tag = TagFactory()
<span class="linenos">140</span>	
<span class="linenos">141</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">142</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">143</span>	
<span class="linenos">144</span>	        <span class="hljs-comment"># Edit the tag</span>
<span class="linenos">145</span>	        response = self.client.post(<span class="hljs-string">'/admin/blogengine/tag/'</span> + str(tag.pk) + <span class="hljs-string">'/'</span>, {
<span class="linenos">146</span>	            <span class="hljs-string">'name'</span>: <span class="hljs-string">'perl'</span>,
<span class="linenos">147</span>	            <span class="hljs-string">'description'</span>: <span class="hljs-string">'The Perl programming language'</span>
<span class="linenos">148</span>	            }, follow=<span class="hljs-keyword">True</span>)
<span class="linenos">149</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">150</span>	
<span class="linenos">151</span>	        <span class="hljs-comment"># Check changed successfully</span>
<span class="linenos">152</span>	        self.assertTrue(<span class="hljs-string">'changed successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">153</span>	
<span class="linenos">154</span>	        <span class="hljs-comment"># Check tag amended</span>
<span class="linenos">155</span>	        all_tags = Tag.objects.all()
<span class="linenos">156</span>	        self.assertEquals(len(all_tags), <span class="hljs-number">1</span>)
<span class="linenos">157</span>	        only_tag = all_tags[<span class="hljs-number">0</span>]
<span class="linenos">158</span>	        self.assertEquals(only_tag.name, <span class="hljs-string">'perl'</span>)
<span class="linenos">159</span>	        self.assertEquals(only_tag.description, <span class="hljs-string">'The Perl programming language'</span>)
<span class="linenos">160</span>	
<span class="linenos">161</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_delete_tag</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">162</span>	        <span class="hljs-comment"># Create the tag</span>
<span class="linenos">163</span>	        tag = TagFactory()
<span class="linenos">164</span>	
<span class="linenos">165</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">166</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">167</span>	
<span class="linenos">168</span>	        <span class="hljs-comment"># Delete the tag</span>
<span class="linenos">169</span>	        response = self.client.post(<span class="hljs-string">'/admin/blogengine/tag/'</span> + str(tag.pk) + <span class="hljs-string">'/delete/'</span>, {
<span class="linenos">170</span>	            <span class="hljs-string">'post'</span>: <span class="hljs-string">'yes'</span>
<span class="linenos">171</span>	        }, follow=<span class="hljs-keyword">True</span>)
<span class="linenos">172</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">173</span>	
<span class="linenos">174</span>	        <span class="hljs-comment"># Check deleted successfully</span>
<span class="linenos">175</span>	        self.assertTrue(<span class="hljs-string">'deleted successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">176</span>	
<span class="linenos">177</span>	        <span class="hljs-comment"># Check tag deleted</span>
<span class="linenos">178</span>	        all_tags = Tag.objects.all()
<span class="linenos">179</span>	        self.assertEquals(len(all_tags), <span class="hljs-number">0</span>)
<span class="linenos">180</span>	
<span class="linenos">181</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_create_post</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">182</span>	        <span class="hljs-comment"># Create the category</span>
<span class="linenos">183</span>	        category = CategoryFactory()
<span class="linenos">184</span>	
<span class="linenos">185</span>	        <span class="hljs-comment"># Create the tag</span>
<span class="linenos">186</span>	        tag = TagFactory()
<span class="linenos">187</span>	
<span class="linenos">188</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">189</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">190</span>	
<span class="linenos">191</span>	        <span class="hljs-comment"># Check response code</span>
<span class="linenos">192</span>	        response = self.client.get(<span class="hljs-string">'/admin/blogengine/post/add/'</span>)
<span class="linenos">193</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">194</span>	
<span class="linenos">195</span>	        <span class="hljs-comment"># Create the new post</span>
<span class="linenos">196</span>	        response = self.client.post(<span class="hljs-string">'/admin/blogengine/post/add/'</span>, {
<span class="linenos">197</span>	            <span class="hljs-string">'title'</span>: <span class="hljs-string">'My first post'</span>,
<span class="linenos">198</span>	            <span class="hljs-string">'text'</span>: <span class="hljs-string">'This is my first post'</span>,
<span class="linenos">199</span>	            <span class="hljs-string">'pub_date_0'</span>: <span class="hljs-string">'2013-12-28'</span>,
<span class="linenos">200</span>	            <span class="hljs-string">'pub_date_1'</span>: <span class="hljs-string">'22:00:04'</span>,
<span class="linenos">201</span>	            <span class="hljs-string">'slug'</span>: <span class="hljs-string">'my-first-post'</span>,
<span class="linenos">202</span>	            <span class="hljs-string">'site'</span>: <span class="hljs-string">'1'</span>,
<span class="linenos">203</span>	            <span class="hljs-string">'category'</span>: str(category.pk),
<span class="linenos">204</span>	            <span class="hljs-string">'tags'</span>: str(tag.pk)
<span class="linenos">205</span>	        },
<span class="linenos">206</span>	        follow=<span class="hljs-keyword">True</span>
<span class="linenos">207</span>	        )
<span class="linenos">208</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">209</span>	
<span class="linenos">210</span>	        <span class="hljs-comment"># Check added successfully</span>
<span class="linenos">211</span>	        self.assertTrue(<span class="hljs-string">'added successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">212</span>	
<span class="linenos">213</span>	        <span class="hljs-comment"># Check new post now in database</span>
<span class="linenos">214</span>	        all_posts = Post.objects.all()
<span class="linenos">215</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">216</span>	
<span class="linenos">217</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_create_post_without_tag</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">218</span>	        <span class="hljs-comment"># Create the category</span>
<span class="linenos">219</span>	        category = CategoryFactory()
<span class="linenos">220</span>	
<span class="linenos">221</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">222</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">223</span>	
<span class="linenos">224</span>	        <span class="hljs-comment"># Check response code</span>
<span class="linenos">225</span>	        response = self.client.get(<span class="hljs-string">'/admin/blogengine/post/add/'</span>)
<span class="linenos">226</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">227</span>	
<span class="linenos">228</span>	        <span class="hljs-comment"># Create the new post</span>
<span class="linenos">229</span>	        response = self.client.post(<span class="hljs-string">'/admin/blogengine/post/add/'</span>, {
<span class="linenos">230</span>	            <span class="hljs-string">'title'</span>: <span class="hljs-string">'My first post'</span>,
<span class="linenos">231</span>	            <span class="hljs-string">'text'</span>: <span class="hljs-string">'This is my first post'</span>,
<span class="linenos">232</span>	            <span class="hljs-string">'pub_date_0'</span>: <span class="hljs-string">'2013-12-28'</span>,
<span class="linenos">233</span>	            <span class="hljs-string">'pub_date_1'</span>: <span class="hljs-string">'22:00:04'</span>,
<span class="linenos">234</span>	            <span class="hljs-string">'slug'</span>: <span class="hljs-string">'my-first-post'</span>,
<span class="linenos">235</span>	            <span class="hljs-string">'site'</span>: <span class="hljs-string">'1'</span>,
<span class="linenos">236</span>	            <span class="hljs-string">'category'</span>: str(category.pk)
<span class="linenos">237</span>	        },
<span class="linenos">238</span>	        follow=<span class="hljs-keyword">True</span>
<span class="linenos">239</span>	        )
<span class="linenos">240</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">241</span>	
<span class="linenos">242</span>	        <span class="hljs-comment"># Check added successfully</span>
<span class="linenos">243</span>	        self.assertTrue(<span class="hljs-string">'added successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">244</span>	
<span class="linenos">245</span>	        <span class="hljs-comment"># Check new post now in database</span>
<span class="linenos">246</span>	        all_posts = Post.objects.all()
<span class="linenos">247</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">248</span>	
<span class="linenos">249</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_edit_post</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">250</span>	        <span class="hljs-comment"># Create the post</span>
<span class="linenos">251</span>	        post = PostFactory()
<span class="linenos">252</span>	
<span class="linenos">253</span>	        <span class="hljs-comment"># Create the category</span>
<span class="linenos">254</span>	        category = CategoryFactory()
<span class="linenos">255</span>	
<span class="linenos">256</span>	        <span class="hljs-comment"># Create the tag</span>
<span class="linenos">257</span>	        tag = TagFactory()
<span class="linenos">258</span>	        post.tags.add(tag)
<span class="linenos">259</span>	
<span class="linenos">260</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">261</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">262</span>	
<span class="linenos">263</span>	        <span class="hljs-comment"># Edit the post</span>
<span class="linenos">264</span>	        response = self.client.post(<span class="hljs-string">'/admin/blogengine/post/'</span> + str(post.pk) + <span class="hljs-string">'/'</span>, {
<span class="linenos">265</span>	            <span class="hljs-string">'title'</span>: <span class="hljs-string">'My second post'</span>,
<span class="linenos">266</span>	            <span class="hljs-string">'text'</span>: <span class="hljs-string">'This is my second blog post'</span>,
<span class="linenos">267</span>	            <span class="hljs-string">'pub_date_0'</span>: <span class="hljs-string">'2013-12-28'</span>,
<span class="linenos">268</span>	            <span class="hljs-string">'pub_date_1'</span>: <span class="hljs-string">'22:00:04'</span>,
<span class="linenos">269</span>	            <span class="hljs-string">'slug'</span>: <span class="hljs-string">'my-second-post'</span>,
<span class="linenos">270</span>	            <span class="hljs-string">'site'</span>: <span class="hljs-string">'1'</span>,
<span class="linenos">271</span>	            <span class="hljs-string">'category'</span>: str(category.pk),
<span class="linenos">272</span>	            <span class="hljs-string">'tags'</span>: str(tag.pk)
<span class="linenos">273</span>	        },
<span class="linenos">274</span>	        follow=<span class="hljs-keyword">True</span>
<span class="linenos">275</span>	        )
<span class="linenos">276</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">277</span>	
<span class="linenos">278</span>	        <span class="hljs-comment"># Check changed successfully</span>
<span class="linenos">279</span>	        self.assertTrue(<span class="hljs-string">'changed successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">280</span>	
<span class="linenos">281</span>	        <span class="hljs-comment"># Check post amended</span>
<span class="linenos">282</span>	        all_posts = Post.objects.all()
<span class="linenos">283</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">284</span>	        only_post = all_posts[<span class="hljs-number">0</span>]
<span class="linenos">285</span>	        self.assertEquals(only_post.title, <span class="hljs-string">'My second post'</span>)
<span class="linenos">286</span>	        self.assertEquals(only_post.text, <span class="hljs-string">'This is my second blog post'</span>)
<span class="linenos">287</span>	
<span class="linenos">288</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_delete_post</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">289</span>	        <span class="hljs-comment"># Create the post</span>
<span class="linenos">290</span>	        post = PostFactory()
<span class="linenos">291</span>	
<span class="linenos">292</span>	        <span class="hljs-comment"># Create the tag</span>
<span class="linenos">293</span>	        tag = TagFactory()
<span class="linenos">294</span>	        post.tags.add(tag)
<span class="linenos">295</span>	
<span class="linenos">296</span>	        <span class="hljs-comment"># Check new post saved</span>
<span class="linenos">297</span>	        all_posts = Post.objects.all()
<span class="linenos">298</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">299</span>	
<span class="linenos">300</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">301</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">302</span>	
<span class="linenos">303</span>	        <span class="hljs-comment"># Delete the post</span>
<span class="linenos">304</span>	        response = self.client.post(<span class="hljs-string">'/admin/blogengine/post/'</span> + str(post.pk) + <span class="hljs-string">'/delete/'</span>, {
<span class="linenos">305</span>	            <span class="hljs-string">'post'</span>: <span class="hljs-string">'yes'</span>
<span class="linenos">306</span>	        }, follow=<span class="hljs-keyword">True</span>)
<span class="linenos">307</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">308</span>	
<span class="linenos">309</span>	        <span class="hljs-comment"># Check deleted successfully</span>
<span class="linenos">310</span>	        self.assertTrue(<span class="hljs-string">'deleted successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">311</span>	
<span class="linenos">312</span>	        <span class="hljs-comment"># Check post deleted</span>
<span class="linenos">313</span>	        all_posts = Post.objects.all()
<span class="linenos">314</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">0</span>)</code></pre>
<p>There are two main issues here. The first is that when we try to edit or delete an existing item, or refer to it when creating something else, we can no longer rely on the number representing the primary key being set to 1. So we need to specifically obtain this, rather than hard-coding it to 1. Therefore, whenever we pass through a number to represent an item (with the exception of the site, but including tags, categories and posts), we need to instead fetch its primary key and return it. So, above where we try to delete a post, we replace <code>1</code> with <code>str(post.pk)</code>. This will solve a lot of the problems. As there’s a lot of them, I won’t go through each one, but you can see the entire class above for reference, and if you’ve followed along so far, you shouldn’t have any problems.</p>
<p>The other issue we need to fix is the login and logout tests. We simply add <code>follow=True</code> to these to ensure that the test client follows the redirects.</p>
<p>Let’s run our tests to make sure they pass:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py jenkins
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	.............................
<span class="linenos">4</span>	----------------------------------------------------------------------
<span class="linenos">5</span>	Ran <span class="hljs-number">29</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">8.210</span>s
<span class="linenos">6</span>	
<span class="linenos">7</span>	OK
<span class="linenos">8</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>With that done, you can commit your changes:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add blogengine/tests.py
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Fixed broken tests'</span></code></pre>
<p>Don’t forget to deploy your changes:</p>
<pre><code class="hljs lang-bash singleline">$ fab deploy</code></pre>
<p>Our blog has now been happily migrated over to Django 1.7!</p>
]]></description>
            <link>http://matthewdaly.co.uk/blog/2014/09/28/django-blog-tutorial-the-next-generation-part-9/</link>
            <guid isPermaLink="true">http://matthewdaly.co.uk/blog/2014/09/28/django-blog-tutorial-the-next-generation-part-9/</guid>
            <pubDate>Sun, 28 Sep 2014 19:51:02 GMT</pubDate>
        </item>
        <item>
            <title><![CDATA[Changing date format from DD/MM/YYYY to YYYY-MM-DD in Vim]]></title>
            <description><![CDATA[<p>Recently I had the occasion to reformat a load of dates in Vim from <code>DD/MM/YYYY</code> to <code>YYYY-MM-DD</code>. In Vim, this is quite simple:</p>
<pre><code class="hljs lang-viml singleline">:<span class="hljs-preprocessor">%</span>s/\<span class="hljs-comment">(\d\{2}\)</span>\/\<span class="hljs-comment">(\d\{2}\)</span>\/\<span class="hljs-comment">(\d\{4}\)</span>/\<span class="hljs-number">3</span>-\<span class="hljs-number">2</span>-\<span class="hljs-number">1</span>/g</code></pre>
<p>This should be easy to adapt to reformatting other date formats.</p>
]]></description>
            <link>http://matthewdaly.co.uk/blog/2014/09/28/changing-date-format-from-dd-slash-mm-slash-yyyy-to-yyyy-mm-dd-in-vim/</link>
            <guid isPermaLink="true">http://matthewdaly.co.uk/blog/2014/09/28/changing-date-format-from-dd-slash-mm-slash-yyyy-to-yyyy-mm-dd-in-vim/</guid>
            <pubDate>Sun, 28 Sep 2014 18:53:34 GMT</pubDate>
        </item>
        <item>
            <title><![CDATA[Django Blog Tutorial - the Next Generation - Part 8]]></title>
            <description><![CDATA[<p>Hello again! In our final instalment, we’ll wrap up our blog by:</p>
<ul>
<li>Implementing a sitemap</li>
<li>Optimising and tidying up the site</li>
<li>Creating a Fabric task for easier deployment</li>
</ul>
<p>I’ll also cover development tools and practices that can make using Django easier. But first there’s a few housekeeping tasks that need doing…</p>
<p>Don’t forget to activate your virtualenv - you should know how to do this off by heart by now!</p>
<h1 id="upgrading-django">Upgrading Django</h1>
<p>At the time of writing, Django 1.7 is due any day now, but it’s not out yet so I won’t cover it. The biggest change is the addition of a built-in migration system, but switching from South to this is well-documented. When Django 1.7 comes out, it shouldn’t be difficult to upgrade to it - because we have good test coverage, we shouldn’t have much trouble catching errors.</p>
<p>However, Django 1.6.6 was recently released, and we need to upgrade to it. Just enter the following command to upgrade:</p>
<pre><code class="hljs lang-bash singleline">$ pip install Django --upgrade</code></pre>
<p>Then add it to your <code>requirements.txt</code>:</p>
<pre><code class="hljs lang-bash singleline">$ pip freeze &gt; requirements.txt</code></pre>
<p>Then commit your changes:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add requirements.txt
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Upgraded Django version'</span></code></pre>
<h1 id="implementing-a-sitemap">Implementing a sitemap</h1>
<p>Creating a sitemap for your blog is a good idea - it can be submitted to search engines, so that they can easily find your content. With Django, it’s pretty straightforward too.</p>
<p>First, let’s create a test for our sitemap. Add the following code at the end of <code>tests.py</code>:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">SitemapTest</span><span class="hljs-params">(BaseAcceptanceTest)</span>:</span>
<span class="linenos">2</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_sitemap</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">3</span>	        <span class="hljs-comment"># Create a post</span>
<span class="linenos">4</span>	        post = PostFactory()
<span class="linenos">5</span>	
<span class="linenos">6</span>	        <span class="hljs-comment"># Create a flat page</span>
<span class="linenos">7</span>	        page = FlatPageFactory()
<span class="linenos">8</span>	
<span class="linenos">9</span>	        <span class="hljs-comment"># Get sitemap</span>
<span class="linenos">10</span>	        response = self.client.get(<span class="hljs-string">'/sitemap.xml'</span>)
<span class="linenos">11</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">12</span>	
<span class="linenos">13</span>	        <span class="hljs-comment"># Check post is present in sitemap</span>
<span class="linenos">14</span>	        self.assertTrue(<span class="hljs-string">'my-first-post'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">15</span>	
<span class="linenos">16</span>	        <span class="hljs-comment"># Check page is present in sitemap</span>
<span class="linenos">17</span>	        self.assertTrue(<span class="hljs-string">'/about/'</span> <span class="hljs-keyword">in</span> response.content)</code></pre>
<p>Run it, and you should see the test fail:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py <span class="hljs-built_in">test</span> blogengine
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	...........................F
<span class="linenos">4</span>	======================================================================
<span class="linenos">5</span>	FAIL: <span class="hljs-built_in">test</span>_sitemap (blogengine.tests.SitemapTest)
<span class="linenos">6</span>	----------------------------------------------------------------------
<span class="linenos">7</span>	Traceback (most recent call last):
<span class="linenos">8</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">847</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_sitemap
<span class="linenos">9</span>	    self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">10</span>	AssertionError: <span class="hljs-number">404</span> != <span class="hljs-number">200</span>
<span class="linenos">11</span>	
<span class="linenos">12</span>	----------------------------------------------------------------------
<span class="linenos">13</span>	Ran <span class="hljs-number">28</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">6.873</span>s
<span class="linenos">14</span>	
<span class="linenos">15</span>	FAILED (failures=<span class="hljs-number">1</span>)
<span class="linenos">16</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>Now, let’s implement our sitemap. The sitemap application comes with Django, and needs to be activated in your settings file, under <code>INSTALLED_APPS</code>:</p>
<pre><code class="hljs lang-python singleline">    <span class="hljs-string">'django.contrib.sitemaps'</span>,</code></pre>
<p>Next, let’s think about what content we want to include in the sitemap. We want to index our flat pages and our blog posts, so our sitemap should reflect that. Create a new file at <code>blogengine/sitemap.py</code> and enter the following text:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-keyword">from</span> django.contrib.sitemaps <span class="hljs-keyword">import</span> Sitemap
<span class="linenos">2</span>	<span class="hljs-keyword">from</span> django.contrib.flatpages.models <span class="hljs-keyword">import</span> FlatPage
<span class="linenos">3</span>	<span class="hljs-keyword">from</span> blogengine.models <span class="hljs-keyword">import</span> Post
<span class="linenos">4</span>	
<span class="linenos">5</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">PostSitemap</span><span class="hljs-params">(Sitemap)</span>:</span>
<span class="linenos">6</span>	    changefreq = <span class="hljs-string">"always"</span>
<span class="linenos">7</span>	    priority = <span class="hljs-number">0.5</span>
<span class="linenos">8</span>	
<span class="linenos">9</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">items</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">10</span>	        <span class="hljs-keyword">return</span> Post.objects.all()
<span class="linenos">11</span>	
<span class="linenos">12</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">lastmod</span><span class="hljs-params">(self, obj)</span>:</span>
<span class="linenos">13</span>	        <span class="hljs-keyword">return</span> obj.pub_date
<span class="linenos">14</span>	
<span class="linenos">15</span>	
<span class="linenos">16</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">FlatpageSitemap</span><span class="hljs-params">(Sitemap)</span>:</span>
<span class="linenos">17</span>	    changefreq = <span class="hljs-string">"always"</span>
<span class="linenos">18</span>	    priority = <span class="hljs-number">0.5</span>
<span class="linenos">19</span>	
<span class="linenos">20</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">items</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">21</span>	        <span class="hljs-keyword">return</span> FlatPage.objects.all()</code></pre>
<p>We define two sitemaps, one for all the posts, and the other for all the flat pages. Note that this works in a very similar way to the syndication framework.</p>
<p>Next, we amend our URLs. Add the following text after the existing imports in your URL file:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-keyword">from</span> django.contrib.sitemaps.views <span class="hljs-keyword">import</span> sitemap
<span class="linenos">2</span>	<span class="hljs-keyword">from</span> blogengine.sitemap <span class="hljs-keyword">import</span> PostSitemap, FlatpageSitemap
<span class="linenos">3</span>	
<span class="linenos">4</span>	<span class="hljs-comment"># Define sitemaps</span>
<span class="linenos">5</span>	sitemaps = {
<span class="linenos">6</span>	    <span class="hljs-string">'posts'</span>: PostSitemap,
<span class="linenos">7</span>	    <span class="hljs-string">'pages'</span>: FlatpageSitemap
<span class="linenos">8</span>	}</code></pre>
<p>Then add the following after the existing routes:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	    <span class="hljs-comment"># Sitemap</span>
<span class="linenos">2</span>	    url(<span class="hljs-string">r'^sitemap\.xml$'</span>, sitemap, {<span class="hljs-string">'sitemaps'</span>: sitemaps},
<span class="linenos">3</span>	            name=<span class="hljs-string">'django.contrib.sitemaps.views.sitemap'</span>),</code></pre>
<p>Here we define what sitemaps we’re going to use, and we define a URL for them. It’s pretty straightforward to use.</p>
<p>Let’s run our tests:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py <span class="hljs-built_in">test</span> blogengine
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	............................
<span class="linenos">4</span>	----------------------------------------------------------------------
<span class="linenos">5</span>	Ran <span class="hljs-number">28</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">6.863</span>s
<span class="linenos">6</span>	
<span class="linenos">7</span>	OK
<span class="linenos">8</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>And done! Let’s commit our changes:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add blogengine/ django_tutorial_blog_ng/settings.py
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Implemented a sitemap'</span></code></pre>
<h1 id="fixing-test-coverage">Fixing test coverage</h1>
<p>Our blog is now feature-complete, but there are a few gaps in test coverage, so we’ll fix them. If, like me, you’re using Coveralls.io, <a href="https://coveralls.io/builds/1151177">you can easily see via their web interface where there are gaps in the coverage</a>.</p>
<p>Now, our gaps are all in our view file - if you <a href="https://coveralls.io/files/280228813">take a look at my build</a>, you can easily identify the gaps as they’re marked in red.</p>
<p>The first gap is where a tag does not exist. Interestingly, if we look at the code in the view, we can see that some of it is redundant:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">TagPostsFeed</span><span class="hljs-params">(PostsFeed)</span>:</span>
<span class="linenos">2</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">get_object</span><span class="hljs-params">(self, request, slug)</span>:</span>
<span class="linenos">3</span>	        <span class="hljs-keyword">return</span> get_object_or_404(Tag, slug=slug)
<span class="linenos">4</span>	
<span class="linenos">5</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">title</span><span class="hljs-params">(self, obj)</span>:</span>
<span class="linenos">6</span>	        <span class="hljs-keyword">return</span> <span class="hljs-string">"RSS feed - blog posts tagged  %s"</span> % obj.name
<span class="linenos">7</span>	
<span class="linenos">8</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">link</span><span class="hljs-params">(self, obj)</span>:</span>
<span class="linenos">9</span>	        <span class="hljs-keyword">return</span> obj.get_absolute_url()
<span class="linenos">10</span>	
<span class="linenos">11</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">description</span><span class="hljs-params">(self, obj)</span>:</span>
<span class="linenos">12</span>	        <span class="hljs-keyword">return</span> <span class="hljs-string">"RSS feed - blog posts tagged %s"</span> % obj.name
<span class="linenos">13</span>	
<span class="linenos">14</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">items</span><span class="hljs-params">(self, obj)</span>:</span>
<span class="linenos">15</span>	        <span class="hljs-keyword">try</span>:
<span class="linenos">16</span>	            tag = Tag.objects.get(slug=obj.slug)
<span class="linenos">17</span>	            <span class="hljs-keyword">return</span> tag.post_set.all()
<span class="linenos">18</span>	        <span class="hljs-keyword">except</span> Tag.DoesNotExist:
<span class="linenos">19</span>	            <span class="hljs-keyword">return</span> Post.objects.none()</code></pre>
<p>Under the <code>items</code> function, we check to see if the tag exists. However, under <code>get_object</code> we can see that if the object didn’t exist, it would already have returned a 404 error. We can therefore safely amend <code>items</code> to not check, since that try statement will never fail:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">TagPostsFeed</span><span class="hljs-params">(PostsFeed)</span>:</span>
<span class="linenos">2</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">get_object</span><span class="hljs-params">(self, request, slug)</span>:</span>
<span class="linenos">3</span>	        <span class="hljs-keyword">return</span> get_object_or_404(Tag, slug=slug)
<span class="linenos">4</span>	
<span class="linenos">5</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">title</span><span class="hljs-params">(self, obj)</span>:</span>
<span class="linenos">6</span>	        <span class="hljs-keyword">return</span> <span class="hljs-string">"RSS feed - blog posts tagged  %s"</span> % obj.name
<span class="linenos">7</span>	
<span class="linenos">8</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">link</span><span class="hljs-params">(self, obj)</span>:</span>
<span class="linenos">9</span>	        <span class="hljs-keyword">return</span> obj.get_absolute_url()
<span class="linenos">10</span>	
<span class="linenos">11</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">description</span><span class="hljs-params">(self, obj)</span>:</span>
<span class="linenos">12</span>	        <span class="hljs-keyword">return</span> <span class="hljs-string">"RSS feed - blog posts tagged %s"</span> % obj.name
<span class="linenos">13</span>	
<span class="linenos">14</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">items</span><span class="hljs-params">(self, obj)</span>:</span>
<span class="linenos">15</span>	        tag = Tag.objects.get(slug=obj.slug)
<span class="linenos">16</span>	        <span class="hljs-keyword">return</span> tag.post_set.all()</code></pre>
<p>The other two gaps are in our search view - we never get an empty result for the search in the following section:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">getSearchResults</span><span class="hljs-params">(request)</span>:</span>
<span class="linenos">2</span>	    <span class="hljs-string">"""
<span class="linenos">3</span>	    Search for a post by title or text
<span class="linenos">4</span>	    """</span>
<span class="linenos">5</span>	    <span class="hljs-comment"># Get the query data</span>
<span class="linenos">6</span>	    query = request.GET.get(<span class="hljs-string">'q'</span>, <span class="hljs-string">''</span>)
<span class="linenos">7</span>	    page = request.GET.get(<span class="hljs-string">'page'</span>, <span class="hljs-number">1</span>)
<span class="linenos">8</span>	
<span class="linenos">9</span>	    <span class="hljs-comment"># Query the database</span>
<span class="linenos">10</span>	    <span class="hljs-keyword">if</span> query:
<span class="linenos">11</span>	        results = Post.objects.filter(Q(text__icontains=query) | Q(title__icontains=query))
<span class="linenos">12</span>	    <span class="hljs-keyword">else</span>:
<span class="linenos">13</span>	        results = <span class="hljs-keyword">None</span>
<span class="linenos">14</span>	
<span class="linenos">15</span>	    <span class="hljs-comment"># Add pagination</span>
<span class="linenos">16</span>	    pages = Paginator(results, <span class="hljs-number">5</span>)
<span class="linenos">17</span>	
<span class="linenos">18</span>	    <span class="hljs-comment"># Get specified page</span>
<span class="linenos">19</span>	    <span class="hljs-keyword">try</span>:
<span class="linenos">20</span>	        returned_page = pages.page(page)
<span class="linenos">21</span>	    <span class="hljs-keyword">except</span> EmptyPage:
<span class="linenos">22</span>	        returned_page = pages.page(pages.num_pages)
<span class="linenos">23</span>	
<span class="linenos">24</span>	    <span class="hljs-comment"># Display the search results</span>
<span class="linenos">25</span>	    <span class="hljs-keyword">return</span> render_to_response(<span class="hljs-string">'blogengine/search_post_list.html'</span>,
<span class="linenos">26</span>	                              {<span class="hljs-string">'page_obj'</span>: returned_page,
<span class="linenos">27</span>	                               <span class="hljs-string">'object_list'</span>: returned_page.object_list,
<span class="linenos">28</span>	                               <span class="hljs-string">'search'</span>: query})</code></pre>
<p>So replace it with this:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">getSearchResults</span><span class="hljs-params">(request)</span>:</span>
<span class="linenos">2</span>	    <span class="hljs-string">"""
<span class="linenos">3</span>	    Search for a post by title or text
<span class="linenos">4</span>	    """</span>
<span class="linenos">5</span>	    <span class="hljs-comment"># Get the query data</span>
<span class="linenos">6</span>	    query = request.GET.get(<span class="hljs-string">'q'</span>, <span class="hljs-string">''</span>)
<span class="linenos">7</span>	    page = request.GET.get(<span class="hljs-string">'page'</span>, <span class="hljs-number">1</span>)
<span class="linenos">8</span>	
<span class="linenos">9</span>	    <span class="hljs-comment"># Query the database</span>
<span class="linenos">10</span>	    results = Post.objects.filter(Q(text__icontains=query) | Q(title__icontains=query))
<span class="linenos">11</span>	
<span class="linenos">12</span>	    <span class="hljs-comment"># Add pagination</span>
<span class="linenos">13</span>	    pages = Paginator(results, <span class="hljs-number">5</span>)
<span class="linenos">14</span>	
<span class="linenos">15</span>	    <span class="hljs-comment"># Get specified page</span>
<span class="linenos">16</span>	    <span class="hljs-keyword">try</span>:
<span class="linenos">17</span>	        returned_page = pages.page(page)
<span class="linenos">18</span>	    <span class="hljs-keyword">except</span> EmptyPage:
<span class="linenos">19</span>	        returned_page = pages.page(pages.num_pages)
<span class="linenos">20</span>	
<span class="linenos">21</span>	    <span class="hljs-comment"># Display the search results</span>
<span class="linenos">22</span>	    <span class="hljs-keyword">return</span> render_to_response(<span class="hljs-string">'blogengine/search_post_list.html'</span>,
<span class="linenos">23</span>	                              {<span class="hljs-string">'page_obj'</span>: returned_page,
<span class="linenos">24</span>	                               <span class="hljs-string">'object_list'</span>: returned_page.object_list,
<span class="linenos">25</span>	                               <span class="hljs-string">'search'</span>: query})</code></pre>
<p>We don’t need to check whether <code>query</code> is defined because if <code>q</code> is left blank, the value of <code>query</code> will be an empty string, so we may as well pull out the redundant code.</p>
<p>Finally, the other gap is for when a user tries to get an empty search page (eg, page two of something with five or less results). So let’s add another test to our <code>SearchViewTest</code> class:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_failing_search</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">2</span>	        <span class="hljs-comment"># Search for something that is not present</span>
<span class="linenos">3</span>	        response = self.client.get(<span class="hljs-string">'/search?q=wibble'</span>)
<span class="linenos">4</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">5</span>	        self.assertTrue(<span class="hljs-string">'No posts found'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">6</span>	
<span class="linenos">7</span>	        <span class="hljs-comment"># Try to get nonexistent second page</span>
<span class="linenos">8</span>	        response = self.client.get(<span class="hljs-string">'/search?q=wibble&amp;page=2'</span>)
<span class="linenos">9</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">10</span>	        self.assertTrue(<span class="hljs-string">'No posts found'</span> <span class="hljs-keyword">in</span> response.content)</code></pre>
<p>Run our tests and check the coverage:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ coverage run --include=<span class="hljs-string">"blogengine/*"</span> --omit=<span class="hljs-string">"blogengine/migrations/*"</span> manage.py <span class="hljs-built_in">test</span> blogengine
<span class="linenos">2</span>	$ coverage html</code></pre>
<p>If you open <code>htmlcov/index.html</code> in your browser, you should see that the test coverage is back up to 100%. With that done, it’s time to commit again:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add blogengine/
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Fixed gaps in coverage'</span></code></pre>
<p>Remember, it’s not always possible to achieve 100% test coverage, and you shouldn’t worry too much about it if it’s not possible - <a href="http://nedbatchelder.com/code/coverage/excluding.html">it’s possible to ignore code</a> if necessary. However, it’s a good idea to aim for 100%.</p>
<h1 id="using-fabric-for-deployment">Using Fabric for deployment</h1>
<p>Next we’ll cover using Fabric, a handy tool for deploying your changes (any pretty much any other task you want to automate). First, you need to install it:</p>
<pre><code class="hljs lang-bash singleline">$ pip install Fabric</code></pre>
<p>If you have any problems installing it, you should be able to resolve them via Google - most of them are likely to be absent libraries that Fabric depends upon. Once it’s installed, add it to your <code>requirements.tzt</code>:</p>
<pre><code class="hljs lang-bash singleline">$ pip freeze &gt; requirements.txt</code></pre>
<p>Next, create a file called <code>fabfile.py</code> and enter the following text:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-comment">#!/usr/bin/env python</span>
<span class="linenos">2</span>	<span class="hljs-keyword">from</span> fabric.api <span class="hljs-keyword">import</span> local
<span class="linenos">3</span>	
<span class="linenos">4</span>	<span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">deploy</span><span class="hljs-params">()</span>:</span>
<span class="linenos">5</span>	    <span class="hljs-string">"""
<span class="linenos">6</span>	    Deploy the latest version to Heroku
<span class="linenos">7</span>	    """</span>
<span class="linenos">8</span>	    <span class="hljs-comment"># Push changes to master</span>
<span class="linenos">9</span>	    local(<span class="hljs-string">"git push origin master"</span>)
<span class="linenos">10</span>	
<span class="linenos">11</span>	    <span class="hljs-comment"># Push changes to Heroku</span>
<span class="linenos">12</span>	    local(<span class="hljs-string">"git push heroku master"</span>)
<span class="linenos">13</span>	
<span class="linenos">14</span>	    <span class="hljs-comment"># Run migrations on Heroku</span>
<span class="linenos">15</span>	    local(<span class="hljs-string">"heroku run python manage.py migrate"</span>)</code></pre>
<p>Now, all this file does is push our changes to Github (or wherever else your repository is hosted) and to Heroku, and runs your migrations. It’s not a terribly big task anyway, but it’s handy to have it in place. Let’s commit our changes:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add fabfile.py requirements.txt
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Added Fabric task for deployment'</span></code></pre>
<p>Then, let’s try it out:</p>
<pre><code class="hljs lang-bash singleline">$ fab deploy</code></pre>
<p>There, wasn’t that more convenient? Fabric is much more powerful than this simple demonstration indicates, and can run tasks on remote servers via SSH easily. I recommend you take a look at the <a href="http://www.fabfile.org/">documentation</a> to see what else you can do with it. If you’re hosting your site on a VPS, you will probably find Fabric indispensable, as you will need to restart the application every time you push up a new revision.</p>
<h1 id="tidying-up">Tidying up</h1>
<p>We want our blog application to play nicely with other Django apps. For instance, say you’re working on a new site that includes a blogging engine. Wouldn’t it make sense to just be able to drop in this blogging engine and have it work immediately? At the moment, some of our URL’s are hard-coded, so we may have problems in doing so. Let’s fix that.</p>
<p>First we’ll amend our tests. Add this at the top of the tests file:</p>
<pre><code class="hljs lang-python singleline"><span class="hljs-keyword">from</span> django.core.urlresolvers <span class="hljs-keyword">import</span> reverse</code></pre>
<p>Next, replace every instance of this:</p>
<pre><code class="hljs lang-python singleline">        response = self.client.get(<span class="hljs-string">'/'</span>)</code></pre>
<p>with this:</p>
<pre><code class="hljs lang-python singleline">response = self.client.get(reverse(<span class="hljs-string">'blogengine:index'</span>))</code></pre>
<p>Then, rewrite the calls to the search route. For instance, this:</p>
<pre><code class="hljs lang-python singleline">        response = self.client.get(<span class="hljs-string">'/search?q=first'</span>)</code></pre>
<p>should become this:</p>
<pre><code class="hljs lang-python singleline">        response = self.client.get(reverse(<span class="hljs-string">'blogengine:search'</span>) + <span class="hljs-string">'?q=first'</span>)</code></pre>
<p>I’ll leave changing these as an exercise for the reader, but check the repository if you get stuck.</p>
<p>Next, we need to assign a namespace to our app’s routes:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-keyword">from</span> django.conf.urls <span class="hljs-keyword">import</span> patterns, include, url
<span class="linenos">2</span>	
<span class="linenos">3</span>	<span class="hljs-keyword">from</span> django.contrib <span class="hljs-keyword">import</span> admin
<span class="linenos">4</span>	admin.autodiscover()
<span class="linenos">5</span>	
<span class="linenos">6</span>	urlpatterns = patterns(<span class="hljs-string">''</span>,
<span class="linenos">7</span>	    <span class="hljs-comment"># Examples:</span>
<span class="linenos">8</span>	    <span class="hljs-comment"># url(r'^$', 'django_tutorial_blog_ng.views.home', name='home'),</span>
<span class="linenos">9</span>	    <span class="hljs-comment"># url(r'^blog/', include('blog.urls')),</span>
<span class="linenos">10</span>	
<span class="linenos">11</span>	    url(<span class="hljs-string">r'^admin/'</span>, include(admin.site.urls)),
<span class="linenos">12</span>	
<span class="linenos">13</span>	    <span class="hljs-comment"># Blog URLs</span>
<span class="linenos">14</span>	    url(<span class="hljs-string">r''</span>, include(<span class="hljs-string">'blogengine.urls'</span>, namespace=<span class="hljs-string">"blogengine"</span>)),
<span class="linenos">15</span>	
<span class="linenos">16</span>	    <span class="hljs-comment"># Flat pages</span>
<span class="linenos">17</span>	    url(<span class="hljs-string">r''</span>, include(<span class="hljs-string">'django.contrib.flatpages.urls'</span>)),
<span class="linenos">18</span>	)</code></pre>
<p>We then assign names to our routes in the app’s <code>urls.py</code>:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-keyword">from</span> django.conf.urls <span class="hljs-keyword">import</span> patterns, url
<span class="linenos">2</span>	<span class="hljs-keyword">from</span> django.views.generic <span class="hljs-keyword">import</span> ListView, DetailView
<span class="linenos">3</span>	<span class="hljs-keyword">from</span> blogengine.models <span class="hljs-keyword">import</span> Post, Category, Tag
<span class="linenos">4</span>	<span class="hljs-keyword">from</span> blogengine.views <span class="hljs-keyword">import</span> CategoryListView, TagListView, PostsFeed, CategoryPostsFeed, TagPostsFeed, getSearchResults
<span class="linenos">5</span>	<span class="hljs-keyword">from</span> django.contrib.sitemaps.views <span class="hljs-keyword">import</span> sitemap
<span class="linenos">6</span>	<span class="hljs-keyword">from</span> blogengine.sitemap <span class="hljs-keyword">import</span> PostSitemap, FlatpageSitemap
<span class="linenos">7</span>	
<span class="linenos">8</span>	<span class="hljs-comment"># Define sitemaps</span>
<span class="linenos">9</span>	sitemaps = {
<span class="linenos">10</span>	    <span class="hljs-string">'posts'</span>: PostSitemap,
<span class="linenos">11</span>	    <span class="hljs-string">'pages'</span>: FlatpageSitemap
<span class="linenos">12</span>	}
<span class="linenos">13</span>	
<span class="linenos">14</span>	urlpatterns = patterns(<span class="hljs-string">''</span>,
<span class="linenos">15</span>	    <span class="hljs-comment"># Index</span>
<span class="linenos">16</span>	    url(<span class="hljs-string">r'^(?P&lt;page&gt;\d+)?/?$'</span>, ListView.as_view(
<span class="linenos">17</span>	        model=Post,
<span class="linenos">18</span>	        paginate_by=<span class="hljs-number">5</span>,
<span class="linenos">19</span>	        ),
<span class="linenos">20</span>	        name=<span class="hljs-string">'index'</span>
<span class="linenos">21</span>	        ),
<span class="linenos">22</span>	
<span class="linenos">23</span>	    <span class="hljs-comment"># Individual posts</span>
<span class="linenos">24</span>	    url(<span class="hljs-string">r'^(?P&lt;pub_date__year&gt;\d{4})/(?P&lt;pub_date__month&gt;\d{1,2})/(?P&lt;slug&gt;[a-zA-Z0-9-]+)/?$'</span>, DetailView.as_view(
<span class="linenos">25</span>	        model=Post,
<span class="linenos">26</span>	        ),
<span class="linenos">27</span>	        name=<span class="hljs-string">'post'</span>
<span class="linenos">28</span>	        ),
<span class="linenos">29</span>	
<span class="linenos">30</span>	    <span class="hljs-comment"># Categories</span>
<span class="linenos">31</span>	    url(<span class="hljs-string">r'^category/(?P&lt;slug&gt;[a-zA-Z0-9-]+)/?$'</span>, CategoryListView.as_view(
<span class="linenos">32</span>	        paginate_by=<span class="hljs-number">5</span>,
<span class="linenos">33</span>	        model=Category,
<span class="linenos">34</span>	        ),
<span class="linenos">35</span>	        name=<span class="hljs-string">'category'</span>
<span class="linenos">36</span>	        ),
<span class="linenos">37</span>	
<span class="linenos">38</span>	
<span class="linenos">39</span>	    <span class="hljs-comment"># Tags</span>
<span class="linenos">40</span>	    url(<span class="hljs-string">r'^tag/(?P&lt;slug&gt;[a-zA-Z0-9-]+)/?$'</span>, TagListView.as_view(
<span class="linenos">41</span>	        paginate_by=<span class="hljs-number">5</span>,
<span class="linenos">42</span>	        model=Tag,
<span class="linenos">43</span>	        ),
<span class="linenos">44</span>	        name=<span class="hljs-string">'tag'</span>
<span class="linenos">45</span>	        ),
<span class="linenos">46</span>	
<span class="linenos">47</span>	    <span class="hljs-comment"># Post RSS feed</span>
<span class="linenos">48</span>	    url(<span class="hljs-string">r'^feeds/posts/$'</span>, PostsFeed()),
<span class="linenos">49</span>	
<span class="linenos">50</span>	    <span class="hljs-comment"># Category RSS feed</span>
<span class="linenos">51</span>	    url(<span class="hljs-string">r'^feeds/posts/category/(?P&lt;slug&gt;[a-zA-Z0-9-]+)/?$'</span>, CategoryPostsFeed()),
<span class="linenos">52</span>	
<span class="linenos">53</span>	    <span class="hljs-comment"># Tag RSS feed</span>
<span class="linenos">54</span>	    url(<span class="hljs-string">r'^feeds/posts/tag/(?P&lt;slug&gt;[a-zA-Z0-9-]+)/?$'</span>, TagPostsFeed()),
<span class="linenos">55</span>	
<span class="linenos">56</span>	    <span class="hljs-comment"># Search posts</span>
<span class="linenos">57</span>	    url(<span class="hljs-string">r'^search'</span>, getSearchResults, name=<span class="hljs-string">'search'</span>),
<span class="linenos">58</span>	
<span class="linenos">59</span>	    <span class="hljs-comment"># Sitemap</span>
<span class="linenos">60</span>	    url(<span class="hljs-string">r'^sitemap\.xml$'</span>, sitemap, {<span class="hljs-string">'sitemaps'</span>: sitemaps},
<span class="linenos">61</span>	            name=<span class="hljs-string">'django.contrib.sitemaps.views.sitemap'</span>),
<span class="linenos">62</span>	)</code></pre>
<p>You also need to amend two of your templates:</p>
<pre><code class="hljs lang-django"><span class="linenos">1</span>	<span class="xml"><span class="hljs-doctype">&lt;!DOCTYPE html&gt;</span>
<span class="linenos">2</span>	<span class="hljs-comment">&lt;!--[if lt IE 7]&gt;      &lt;html class="no-js lt-ie9 lt-ie8 lt-ie7"&gt; &lt;![endif]--&gt;</span>
<span class="linenos">3</span>	<span class="hljs-comment">&lt;!--[if IE 7]&gt;         &lt;html class="no-js lt-ie9 lt-ie8"&gt; &lt;![endif]--&gt;</span>
<span class="linenos">4</span>	<span class="hljs-comment">&lt;!--[if IE 8]&gt;         &lt;html class="no-js lt-ie9"&gt; &lt;![endif]--&gt;</span>
<span class="linenos">5</span>	<span class="hljs-comment">&lt;!--[if gt IE 8]&gt;&lt;!--&gt;</span> <span class="hljs-tag">&lt;<span class="hljs-title">html</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"no-js"</span>&gt;</span> <span class="hljs-comment">&lt;!--&lt;![endif]--&gt;</span>
<span class="linenos">6</span>	    <span class="hljs-tag">&lt;<span class="hljs-title">head</span>&gt;</span>
<span class="linenos">7</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">meta</span> <span class="hljs-attribute">charset</span>=<span class="hljs-value">"utf-8"</span>&gt;</span>
<span class="linenos">8</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">meta</span> <span class="hljs-attribute">http-equiv</span>=<span class="hljs-value">"X-UA-Compatible"</span> <span class="hljs-attribute">content</span>=<span class="hljs-value">"IE=edge"</span>&gt;</span>
<span class="linenos">9</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">title</span>&gt;</span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">block</span> title %}</span><span class="xml">My Django Blog</span><span class="hljs-template_tag">{% <span class="hljs-keyword">endblock</span> %}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">title</span>&gt;</span>
<span class="linenos">10</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">meta</span> <span class="hljs-attribute">name</span>=<span class="hljs-value">"description"</span> <span class="hljs-attribute">content</span>=<span class="hljs-value">""</span>&gt;</span>
<span class="linenos">11</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">meta</span> <span class="hljs-attribute">name</span>=<span class="hljs-value">"viewport"</span> <span class="hljs-attribute">content</span>=<span class="hljs-value">"width=device-width, initial-scale=1"</span>&gt;</span>
<span class="linenos">12</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"alternate"</span> <span class="hljs-attribute">type</span>=<span class="hljs-value">"application/rss+xml"</span> <span class="hljs-attribute">title</span>=<span class="hljs-value">"Blog posts"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/feeds/posts/"</span> &gt;</span>
<span class="linenos">13</span>	
<span class="linenos">14</span>	        <span class="hljs-comment">&lt;!-- Place favicon.ico and apple-touch-icon.png in the root directory --&gt;</span>
<span class="linenos">15</span>	
<span class="linenos">16</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">load</span> staticfiles %}</span><span class="xml">
<span class="linenos">17</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/html5-boilerplate/css/normalize.css' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>
<span class="linenos">18</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/html5-boilerplate/css/main.css' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>
<span class="linenos">19</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/bootstrap/dist/css/bootstrap.min.css' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>
<span class="linenos">20</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/bootstrap/dist/css/bootstrap-theme.min.css' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>
<span class="linenos">21</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'css/main.css' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>
<span class="linenos">22</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'css/code.css' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>
<span class="linenos">23</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span> <span class="hljs-attribute">src</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/html5-boilerplate/js/vendor/modernizr-2.6.2.min.js' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span><span class="undefined"></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">24</span>	    <span class="hljs-tag">&lt;/<span class="hljs-title">head</span>&gt;</span>
<span class="linenos">25</span>	    <span class="hljs-tag">&lt;<span class="hljs-title">body</span>&gt;</span>
<span class="linenos">26</span>	        <span class="hljs-comment">&lt;!--[if lt IE 7]&gt;
<span class="linenos">27</span>	            &lt;p class="browsehappy"&gt;You are using an &lt;strong&gt;outdated&lt;/strong&gt; browser. Please &lt;a href="http://browsehappy.com/"&gt;upgrade your browser&lt;/a&gt; to improve your experience.&lt;/p&gt;
<span class="linenos">28</span>	        &lt;![endif]--&gt;</span>
<span class="linenos">29</span>	
<span class="linenos">30</span>	        <span class="hljs-comment">&lt;!-- Add your site or application content here --&gt;</span>
<span class="linenos">31</span>	
<span class="linenos">32</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">id</span>=<span class="hljs-value">"fb-root"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">33</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span>&gt;</span><span class="clojure"><span class="hljs-list">(<span class="hljs-keyword">function</span><span class="hljs-list">(<span class="hljs-keyword">d</span>, s, id)</span> <span class="hljs-collection">{
<span class="linenos">34</span>	            var js, fjs = d.getElementsByTagName<span class="hljs-list">(<span class="hljs-keyword">s</span>)</span><span class="hljs-collection">[<span class="hljs-number">0</span>]</span><span class="hljs-comment">;</span>
<span class="linenos">35</span>	            if <span class="hljs-list">(<span class="hljs-keyword">d.getElementById</span><span class="hljs-list">(<span class="hljs-keyword">id</span>)</span>)</span> return;
<span class="linenos">36</span>	                js = d.createElement<span class="hljs-list">(<span class="hljs-keyword">s</span>)</span><span class="hljs-comment">; js.id = id;</span>
<span class="linenos">37</span>	                js.src = <span class="hljs-string">"//connect.facebook.net/en_GB/all.js#xfbml=1"</span><span class="hljs-comment">;</span>
<span class="linenos">38</span>	                fjs.parentNode.insertBefore<span class="hljs-list">(<span class="hljs-keyword">js</span>, fjs)</span><span class="hljs-comment">;</span>
<span class="linenos">39</span>	            }</span><span class="hljs-list">(<span class="hljs-keyword">document</span>, 'script', 'facebook-jssdk')</span>)</span><span class="hljs-comment">;</span></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">40</span>	
<span class="linenos">41</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"navbar navbar-static-top navbar-inverse"</span>&gt;</span>
<span class="linenos">42</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"container-fluid"</span>&gt;</span>
<span class="linenos">43</span>	                <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"navbar-header"</span>&gt;</span>
<span class="linenos">44</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">button</span> <span class="hljs-attribute">type</span>=<span class="hljs-value">"button"</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"navbar-toggle"</span> <span class="hljs-attribute">data-toggle</span>=<span class="hljs-value">"collapse"</span> <span class="hljs-attribute">data-target</span>=<span class="hljs-value">"#header-nav"</span>&gt;</span>
<span class="linenos">45</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"icon-bar"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span>
<span class="linenos">46</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"icon-bar"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span>
<span class="linenos">47</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"icon-bar"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span>
<span class="linenos">48</span>	                    <span class="hljs-tag">&lt;/<span class="hljs-title">button</span>&gt;</span>
<span class="linenos">49</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"navbar-brand"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">url</span> 'blogengine:index' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>My Django Blog<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">50</span>	                <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">51</span>	                <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"collapse navbar-collapse"</span> <span class="hljs-attribute">id</span>=<span class="hljs-value">"header-nav"</span>&gt;</span>
<span class="linenos">52</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">ul</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"nav navbar-nav"</span>&gt;</span>
<span class="linenos">53</span>	                        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">load</span> flatpages %}</span><span class="xml">
<span class="linenos">54</span>	                        </span><span class="hljs-template_tag">{% get_flatpages <span class="hljs-keyword">as</span> flatpages %}</span><span class="xml">
<span class="linenos">55</span>	                        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">for</span> flatpage <span class="hljs-keyword">in</span> flatpages %}</span><span class="xml">
<span class="linenos">56</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">li</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ flatpage.url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span></span><span class="hljs-variable">{{ flatpage.title }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">li</span>&gt;</span>
<span class="linenos">57</span>	                        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endfor</span> %}</span><span class="xml">
<span class="linenos">58</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">li</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/feeds/posts/"</span>&gt;</span>RSS feed<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">li</span>&gt;</span>
<span class="linenos">59</span>	
<span class="linenos">60</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">form</span> <span class="hljs-attribute">action</span>=<span class="hljs-value">"/search"</span> <span class="hljs-attribute">method</span>=<span class="hljs-value">"GET"</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"navbar-form navbar-left"</span>&gt;</span>
<span class="linenos">61</span>	                            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"form-group"</span>&gt;</span>
<span class="linenos">62</span>	                                <span class="hljs-tag">&lt;<span class="hljs-title">input</span> <span class="hljs-attribute">type</span>=<span class="hljs-value">"text"</span> <span class="hljs-attribute">name</span>=<span class="hljs-value">"q"</span> <span class="hljs-attribute">placeholder</span>=<span class="hljs-value">"Search..."</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"form-control"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">input</span>&gt;</span>
<span class="linenos">63</span>	                            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">64</span>	                            <span class="hljs-tag">&lt;<span class="hljs-title">button</span> <span class="hljs-attribute">type</span>=<span class="hljs-value">"submit"</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"btn btn-default"</span>&gt;</span>Search<span class="hljs-tag">&lt;/<span class="hljs-title">button</span>&gt;</span>
<span class="linenos">65</span>	                        <span class="hljs-tag">&lt;/<span class="hljs-title">form</span>&gt;</span>
<span class="linenos">66</span>	                    <span class="hljs-tag">&lt;/<span class="hljs-title">ul</span>&gt;</span>
<span class="linenos">67</span>	                <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">68</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">69</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">70</span>	
<span class="linenos">71</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"container"</span>&gt;</span>
<span class="linenos">72</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">block</span> header %}</span><span class="xml">
<span class="linenos">73</span>	                <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"page-header"</span>&gt;</span>
<span class="linenos">74</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">h1</span>&gt;</span>My Django Blog<span class="hljs-tag">&lt;/<span class="hljs-title">h1</span>&gt;</span>
<span class="linenos">75</span>	                <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">76</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endblock</span> %}</span><span class="xml">
<span class="linenos">77</span>	
<span class="linenos">78</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"row"</span>&gt;</span>
<span class="linenos">79</span>	                </span><span class="hljs-template_tag">{% <span class="hljs-keyword">block</span> content %}</span><span class="xml"></span><span class="hljs-template_tag">{% <span class="hljs-keyword">endblock</span> %}</span><span class="xml">
<span class="linenos">80</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">81</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">82</span>	
<span class="linenos">83</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"container footer"</span>&gt;</span>
<span class="linenos">84</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"row"</span>&gt;</span>
<span class="linenos">85</span>	                <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"span12"</span>&gt;</span>
<span class="linenos">86</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">p</span>&gt;</span>Copyright &amp;copy; </span><span class="hljs-template_tag">{% <span class="hljs-keyword">now</span> "Y" %}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">p</span>&gt;</span>
<span class="linenos">87</span>	                <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">88</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">89</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">90</span>	
<span class="linenos">91</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span> <span class="hljs-attribute">src</span>=<span class="hljs-value">"//ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js"</span>&gt;</span><span class="undefined"></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">92</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span>&gt;</span><span class="applescript">window.jQuery || document.<span class="hljs-command">write</span>('&lt;<span class="hljs-keyword">script</span> src=<span class="hljs-string">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/html5-boilerplate/js/vendor/jquery-1.10.2.min.js' %}</span><span class="xml"><span class="1c"><span class="hljs-string">"&gt;&lt;\/script&gt;')</span></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">93</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span> <span class="hljs-attribute">src</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/html5-boilerplate/js/plugins.js' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span><span class="undefined"></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">94</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span> <span class="hljs-attribute">src</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/bootstrap/dist/js/bootstrap.min.js' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span><span class="undefined"></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">95</span>	
<span class="linenos">96</span>	        <span class="hljs-comment">&lt;!-- Google Analytics: change UA-XXXXX-X to be your site's ID. --&gt;</span>
<span class="linenos">97</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span>&gt;</span><span class="scheme">
<span class="linenos">98</span>	            <span class="hljs-list">(<span class="hljs-keyword">function</span><span class="hljs-list">(<span class="hljs-keyword">b</span>,o,i,l,e,r)</span>{b.GoogleAnalyticsObject=l<span class="hljs-comment">;b[l]||(b[l]=</span>
<span class="linenos">99</span>	            function<span class="hljs-list">()</span>{<span class="hljs-list">(<span class="hljs-keyword">b</span><span class="hljs-list">[<span class="hljs-keyword">l</span>].q=b<span class="hljs-list">[<span class="hljs-keyword">l</span>].q||<span class="hljs-list">[]).push<span class="hljs-list">(<span class="hljs-keyword">arguments</span>)</span>})<span class="hljs-comment">;b[l].l=+new Date;</span>
<span class="linenos">100</span>	            e=o.createElement<span class="hljs-list">(<span class="hljs-keyword">i</span>)</span><span class="hljs-comment">;r=o.getElementsByTagName(i)[0];</span>
<span class="linenos">101</span>	            e.src=<span class="hljs-variable">'//www.google-analytics.com/analytics.js</span>'<span class="hljs-comment">;</span>
<span class="linenos">102</span>	            r.parentNode.insertBefore<span class="hljs-list">(<span class="hljs-keyword">e</span>,r)</span>}<span class="hljs-list">(<span class="hljs-keyword">window</span>,document,<span class="hljs-variable">'script</span>',<span class="hljs-variable">'ga</span>')</span>)<span class="hljs-comment">;</span>
<span class="linenos">103</span>	            ga<span class="hljs-list">(<span class="hljs-variable">'create</span>',<span class="hljs-variable">'UA-XXXXX-X</span>')</span><span class="hljs-comment">;ga('send','pageview');</span>
<span class="linenos">104</span>	        </span></span></span></span></span></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">105</span>	    <span class="hljs-tag">&lt;/<span class="hljs-title">body</span>&gt;</span>
<span class="linenos">106</span>	<span class="hljs-tag">&lt;/<span class="hljs-title">html</span>&gt;</span></span></code></pre>
<pre><code class="hljs lang-django"><span class="linenos">1</span>	<span class="xml"></span><span class="hljs-template_tag">{% <span class="hljs-keyword">extends</span> "blogengine/includes/base.html" %}</span><span class="xml">
<span class="linenos">2</span>	
<span class="linenos">3</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">load</span> custom_markdown %}</span><span class="xml">
<span class="linenos">4</span>	
<span class="linenos">5</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">block</span> content %}</span><span class="xml">
<span class="linenos">6</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> object_list %}</span><span class="xml">
<span class="linenos">7</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">for</span> post <span class="hljs-keyword">in</span> object_list %}</span><span class="xml">
<span class="linenos">8</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"post col-md-12"</span>&gt;</span>
<span class="linenos">9</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">h1</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ post.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span></span><span class="hljs-variable">{{ post.title }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">h1</span>&gt;</span>
<span class="linenos">10</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">h3</span>&gt;</span></span><span class="hljs-variable">{{ post.pub_date }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">h3</span>&gt;</span>
<span class="linenos">11</span>	            </span><span class="hljs-variable">{{ post.text<span class="hljs-filter">|custom</span>_markdown }}</span><span class="xml">
<span class="linenos">12</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">13</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> post.category %}</span><span class="xml">
<span class="linenos">14</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"col-md-12"</span>&gt;</span>
<span class="linenos">15</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ post.category.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"label label-primary"</span>&gt;</span></span><span class="hljs-variable">{{ post.category.name }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">16</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">17</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">18</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> post.tags %}</span><span class="xml">
<span class="linenos">19</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"col-md-12"</span>&gt;</span>
<span class="linenos">20</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">for</span> tag <span class="hljs-keyword">in</span> post.tags.all %}</span><span class="xml">
<span class="linenos">21</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ tag.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"label label-success"</span>&gt;</span></span><span class="hljs-variable">{{ tag.name }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">22</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endfor</span> %}</span><span class="xml">
<span class="linenos">23</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">24</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">25</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endfor</span> %}</span><span class="xml">
<span class="linenos">26</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">else</span> %}</span><span class="xml">
<span class="linenos">27</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">p</span>&gt;</span>No posts found<span class="hljs-tag">&lt;/<span class="hljs-title">p</span>&gt;</span>
<span class="linenos">28</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">29</span>	
<span class="linenos">30</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">ul</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"pager"</span>&gt;</span>
<span class="linenos">31</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> page_obj.has_previous %}</span><span class="xml">
<span class="linenos">32</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">li</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"previous"</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">url</span> 'blogengine:search' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">?page=</span></span></span><span class="hljs-variable">{{ page_obj.previous_page_number }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">&amp;q=</span></span></span><span class="hljs-variable">{{ search }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>Previous Page<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">li</span>&gt;</span>
<span class="linenos">33</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">34</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> page_obj.has_next %}</span><span class="xml">
<span class="linenos">35</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">li</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"next"</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">url</span> 'blogengine:search' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">?page=</span></span></span><span class="hljs-variable">{{ page_obj.next_page_number }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">&amp;q=</span></span></span><span class="hljs-variable">{{ search }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>Next Page<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">li</span>&gt;</span>
<span class="linenos">36</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">37</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">ul</span>&gt;</span>
<span class="linenos">38</span>	
<span class="linenos">39</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endblock</span> %}</span><span class="xml"></span></code></pre>
<p>Let’s run our tests:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py <span class="hljs-built_in">test</span> blogengine/
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	.............................
<span class="linenos">4</span>	----------------------------------------------------------------------
<span class="linenos">5</span>	Ran <span class="hljs-number">29</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">10.456</span>s
<span class="linenos">6</span>	
<span class="linenos">7</span>	OK
<span class="linenos">8</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>And commit our changes:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add .
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Now use named routes'</span></code></pre>
<h1 id="debugging-django">Debugging Django</h1>
<p>There are a number of handy ways to debug Django applications. One of the simplest is to use the Python debugger. To use it, just enter the following lines at the point you want to break at:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-keyword">import</span> pdb
<span class="linenos">2</span>	pdb.set_trace()</code></pre>
<p>Now, whenever that line of code is run, you’ll be dropped into an interactive shell that lets you play around to find out what’s going wrong. However, it doesn’t offer autocompletion, so we’ll install <code>ipdb</code>, which is an improved version:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ pip install ipdb
<span class="linenos">2</span>	$ pip freeze &gt; requirements.txt</code></pre>
<p>Now you can use <code>ipdb</code> in much the same way as you would use <code>pdb</code>:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-keyword">import</span> ipdb
<span class="linenos">2</span>	ipdb.set_trace()</code></pre>
<p>Now, <code>ipdb</code> is very useful, but it isn’t much help for profiling your application. For that you need the Django Debug Toolbar. Run the following commands:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ pip install django-debug-toolbar
<span class="linenos">2</span>	$ pip freeze &gt; requirements.txt</code></pre>
<p>Then add the following line to <code>INSTALLED_APPS</code> in your settings file:</p>
<pre><code class="hljs lang-python singleline">    <span class="hljs-string">'debug_toolbar'</span>,</code></pre>
<p>Then, try running the development server, and you’ll see a toolbar on the right-hand side of the screen that allows you to view some useful data about your page. For instance, you’ll notice a field called <code>SQL</code> - this contains details of the queries carried out when building the page. To actually see the queries carried out, you’ll want to disable caching in your settings file by commenting out all the constants that start with <code>CACHE</code>.</p>
<p>We won’t go into using the toolbar to optimise queries, but using this, you can easily see what queries are being executed on a specific page, how long they take, and the values they return. Sometimes, you may need to optimise a slow query - in this case, Django allows you to drop down to writing raw SQL if necessary.</p>
<p>Note that if you’re running Django in production, you should set <code>DEBUG</code> to <code>False</code> as otherwise it gives rather too much information to potential attackers, and with Django Debug Toolbar installed, that’s even more important.</p>
<p>Please also note that when you disable debug mode, Django no longer handles static files automatically, so you’ll need to run <code>python manage.py collectstatic</code> and commit the <code>staticfiles</code> directory.</p>
<p>Once you’ve disabled debug mode, collected the static files, and re-enables caching, you can commit your changes:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add .
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Installed debugging tools'</span></code></pre>
<h1 id="optimising-static-files">Optimising static files</h1>
<p>We want our blog to get the best SEO results it can, so making it fast is essential. One of the simplest things you can do is to concatenate and minify static assets such as CSS and JavaScript. There are numerous ways to do this, but I generally use Grunt. Let’s set up a Grunt config to concatenate and minify our CSS and JavaScript.</p>
<p>You’ll need to have Node.js installed on your development machine for this. Then, you need to install the Grunt command-line interface:</p>
<pre><code class="hljs lang-bash singleline">$ sudo npm install -g grunt-cli</code></pre>
<p>With that done, we need to create a <code>package.json</code> file. You can create one using the command <code>npm init</code>. Here’s mine:</p>
<pre><code class="hljs lang-json"><span class="linenos">1</span>	{
<span class="linenos">2</span>	  "<span class="hljs-attribute">name</span>": <span class="hljs-value"><span class="hljs-string">"django_tutorial_blog_ng"</span></span>,
<span class="linenos">3</span>	  "<span class="hljs-attribute">version</span>": <span class="hljs-value"><span class="hljs-string">"1.0.0"</span></span>,
<span class="linenos">4</span>	  "<span class="hljs-attribute">description</span>": <span class="hljs-value"><span class="hljs-string">"Django Tutorial Blog NG ======================="</span></span>,
<span class="linenos">5</span>	  "<span class="hljs-attribute">main</span>": <span class="hljs-value"><span class="hljs-string">"index.js"</span></span>,
<span class="linenos">6</span>	  "<span class="hljs-attribute">scripts</span>": <span class="hljs-value">{
<span class="linenos">7</span>	    "<span class="hljs-attribute">test</span>": <span class="hljs-value"><span class="hljs-string">"echo \"Error: no test specified\" &amp;&amp; exit 1"</span>
<span class="linenos">8</span>	  </span>}</span>,
<span class="linenos">9</span>	  "<span class="hljs-attribute">repository</span>": <span class="hljs-value">{
<span class="linenos">10</span>	    "<span class="hljs-attribute">type</span>": <span class="hljs-value"><span class="hljs-string">"git"</span></span>,
<span class="linenos">11</span>	    "<span class="hljs-attribute">url</span>": <span class="hljs-value"><span class="hljs-string">"https://github.com/matthewbdaly/django_tutorial_blog_ng.git"</span>
<span class="linenos">12</span>	  </span>}</span>,
<span class="linenos">13</span>	  "<span class="hljs-attribute">author</span>": <span class="hljs-value"><span class="hljs-string">"Matthew Daly &lt;matthew@matthewdaly.co.uk&gt; (http://matthewdaly.co.uk/)"</span></span>,
<span class="linenos">14</span>	  "<span class="hljs-attribute">license</span>": <span class="hljs-value"><span class="hljs-string">"ISC"</span></span>,
<span class="linenos">15</span>	  "<span class="hljs-attribute">bugs</span>": <span class="hljs-value">{
<span class="linenos">16</span>	    "<span class="hljs-attribute">url</span>": <span class="hljs-value"><span class="hljs-string">"https://github.com/matthewbdaly/django_tutorial_blog_ng/issues"</span>
<span class="linenos">17</span>	  </span>}</span>,
<span class="linenos">18</span>	  "<span class="hljs-attribute">homepage</span>": <span class="hljs-value"><span class="hljs-string">"https://github.com/matthewbdaly/django_tutorial_blog_ng"</span>
<span class="linenos">19</span>	</span>}</code></pre>
<p>Feel free to amend  it as you see fit.</p>
<p>Next we install Grunt and the required plugins:</p>
<pre><code class="hljs lang-bash singleline">$ npm install grunt grunt-contrib-cssmin grunt-contrib-concat grunt-contrib-uglify --save-dev</code></pre>
<p>We now need to create a Gruntfile for our tasks:</p>
<pre><code class="hljs lang-javascript"><span class="linenos">1</span>	<span class="hljs-built_in">module</span>.exports = <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-params">(grunt)</span> </span>{
<span class="linenos">2</span>	<span class="hljs-pi">    'use strict'</span>;
<span class="linenos">3</span>	
<span class="linenos">4</span>	    grunt.initConfig({
<span class="linenos">5</span>	        concat: {
<span class="linenos">6</span>	            dist: {
<span class="linenos">7</span>	                src: [
<span class="linenos">8</span>	                    <span class="hljs-string">'blogengine/static/bower_components/bootstrap/dist/css/bootstrap.css'</span>,
<span class="linenos">9</span>	                    <span class="hljs-string">'blogengine/static/bower_components/bootstrap/dist/css/bootstrap-theme.css'</span>,
<span class="linenos">10</span>	                    <span class="hljs-string">'blogengine/static/css/code.css'</span>,
<span class="linenos">11</span>	                    <span class="hljs-string">'blogengine/static/css/main.css'</span>,
<span class="linenos">12</span>	                ],
<span class="linenos">13</span>	                dest: <span class="hljs-string">'blogengine/static/css/style.css'</span>
<span class="linenos">14</span>	            }
<span class="linenos">15</span>	        },
<span class="linenos">16</span>	        uglify: {
<span class="linenos">17</span>	            dist: {
<span class="linenos">18</span>	                src: [
<span class="linenos">19</span>	                    <span class="hljs-string">'blogengine/static/bower_components/jquery/jquery.js'</span>,
<span class="linenos">20</span>	                    <span class="hljs-string">'blogengine/static/bower_components/bootstrap/dist/js/bootstrap.js'</span>
<span class="linenos">21</span>	                ],
<span class="linenos">22</span>	                dest: <span class="hljs-string">'blogengine/static/js/all.min.js'</span>
<span class="linenos">23</span>	            }
<span class="linenos">24</span>	        },
<span class="linenos">25</span>	        cssmin: {
<span class="linenos">26</span>	            dist: {
<span class="linenos">27</span>	                src: <span class="hljs-string">'blogengine/static/css/style.css'</span>,
<span class="linenos">28</span>	                dest: <span class="hljs-string">'blogengine/static/css/style.min.css'</span>
<span class="linenos">29</span>	            }
<span class="linenos">30</span>	        }
<span class="linenos">31</span>	    });
<span class="linenos">32</span>	
<span class="linenos">33</span>	    grunt.loadNpmTasks(<span class="hljs-string">'grunt-contrib-concat'</span>);
<span class="linenos">34</span>	    grunt.loadNpmTasks(<span class="hljs-string">'grunt-contrib-uglify'</span>);
<span class="linenos">35</span>	    grunt.loadNpmTasks(<span class="hljs-string">'grunt-contrib-cssmin'</span>);
<span class="linenos">36</span>	    grunt.registerTask(<span class="hljs-string">'default'</span>, [<span class="hljs-string">'concat'</span>, <span class="hljs-string">'uglify'</span>, <span class="hljs-string">'cssmin'</span>]);
<span class="linenos">37</span>	};</code></pre>
<p>You’ll also need to change the paths in your base HTML file to point to the minified versions:</p>
<pre><code class="hljs lang-django"><span class="linenos">1</span>	<span class="xml"><span class="hljs-doctype">&lt;!DOCTYPE html&gt;</span>
<span class="linenos">2</span>	<span class="hljs-comment">&lt;!--[if lt IE 7]&gt;      &lt;html class="no-js lt-ie9 lt-ie8 lt-ie7"&gt; &lt;![endif]--&gt;</span>
<span class="linenos">3</span>	<span class="hljs-comment">&lt;!--[if IE 7]&gt;         &lt;html class="no-js lt-ie9 lt-ie8"&gt; &lt;![endif]--&gt;</span>
<span class="linenos">4</span>	<span class="hljs-comment">&lt;!--[if IE 8]&gt;         &lt;html class="no-js lt-ie9"&gt; &lt;![endif]--&gt;</span>
<span class="linenos">5</span>	<span class="hljs-comment">&lt;!--[if gt IE 8]&gt;&lt;!--&gt;</span> <span class="hljs-tag">&lt;<span class="hljs-title">html</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"no-js"</span>&gt;</span> <span class="hljs-comment">&lt;!--&lt;![endif]--&gt;</span>
<span class="linenos">6</span>	    <span class="hljs-tag">&lt;<span class="hljs-title">head</span>&gt;</span>
<span class="linenos">7</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">meta</span> <span class="hljs-attribute">charset</span>=<span class="hljs-value">"utf-8"</span>&gt;</span>
<span class="linenos">8</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">meta</span> <span class="hljs-attribute">http-equiv</span>=<span class="hljs-value">"X-UA-Compatible"</span> <span class="hljs-attribute">content</span>=<span class="hljs-value">"IE=edge"</span>&gt;</span>
<span class="linenos">9</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">title</span>&gt;</span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">block</span> title %}</span><span class="xml">My Django Blog</span><span class="hljs-template_tag">{% <span class="hljs-keyword">endblock</span> %}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">title</span>&gt;</span>
<span class="linenos">10</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">meta</span> <span class="hljs-attribute">name</span>=<span class="hljs-value">"description"</span> <span class="hljs-attribute">content</span>=<span class="hljs-value">""</span>&gt;</span>
<span class="linenos">11</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">meta</span> <span class="hljs-attribute">name</span>=<span class="hljs-value">"viewport"</span> <span class="hljs-attribute">content</span>=<span class="hljs-value">"width=device-width, initial-scale=1"</span>&gt;</span>
<span class="linenos">12</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"alternate"</span> <span class="hljs-attribute">type</span>=<span class="hljs-value">"application/rss+xml"</span> <span class="hljs-attribute">title</span>=<span class="hljs-value">"Blog posts"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/feeds/posts/"</span> &gt;</span>
<span class="linenos">13</span>	
<span class="linenos">14</span>	        <span class="hljs-comment">&lt;!-- Place favicon.ico and apple-touch-icon.png in the root directory --&gt;</span>
<span class="linenos">15</span>	
<span class="linenos">16</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">load</span> staticfiles %}</span><span class="xml">
<span class="linenos">17</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'css/style.min.css' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>
<span class="linenos">18</span>	    <span class="hljs-tag">&lt;/<span class="hljs-title">head</span>&gt;</span>
<span class="linenos">19</span>	    <span class="hljs-tag">&lt;<span class="hljs-title">body</span>&gt;</span>
<span class="linenos">20</span>	        <span class="hljs-comment">&lt;!--[if lt IE 7]&gt;
<span class="linenos">21</span>	            &lt;p class="browsehappy"&gt;You are using an &lt;strong&gt;outdated&lt;/strong&gt; browser. Please &lt;a href="http://browsehappy.com/"&gt;upgrade your browser&lt;/a&gt; to improve your experience.&lt;/p&gt;
<span class="linenos">22</span>	        &lt;![endif]--&gt;</span>
<span class="linenos">23</span>	
<span class="linenos">24</span>	        <span class="hljs-comment">&lt;!-- Add your site or application content here --&gt;</span>
<span class="linenos">25</span>	
<span class="linenos">26</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">id</span>=<span class="hljs-value">"fb-root"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">27</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span>&gt;</span><span class="clojure"><span class="hljs-list">(<span class="hljs-keyword">function</span><span class="hljs-list">(<span class="hljs-keyword">d</span>, s, id)</span> <span class="hljs-collection">{
<span class="linenos">28</span>	            var js, fjs = d.getElementsByTagName<span class="hljs-list">(<span class="hljs-keyword">s</span>)</span><span class="hljs-collection">[<span class="hljs-number">0</span>]</span><span class="hljs-comment">;</span>
<span class="linenos">29</span>	            if <span class="hljs-list">(<span class="hljs-keyword">d.getElementById</span><span class="hljs-list">(<span class="hljs-keyword">id</span>)</span>)</span> return;
<span class="linenos">30</span>	                js = d.createElement<span class="hljs-list">(<span class="hljs-keyword">s</span>)</span><span class="hljs-comment">; js.id = id;</span>
<span class="linenos">31</span>	                js.src = <span class="hljs-string">"//connect.facebook.net/en_GB/all.js#xfbml=1"</span><span class="hljs-comment">;</span>
<span class="linenos">32</span>	                fjs.parentNode.insertBefore<span class="hljs-list">(<span class="hljs-keyword">js</span>, fjs)</span><span class="hljs-comment">;</span>
<span class="linenos">33</span>	            }</span><span class="hljs-list">(<span class="hljs-keyword">document</span>, 'script', 'facebook-jssdk')</span>)</span><span class="hljs-comment">;</span></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">34</span>	
<span class="linenos">35</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"navbar navbar-static-top navbar-inverse"</span>&gt;</span>
<span class="linenos">36</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"container-fluid"</span>&gt;</span>
<span class="linenos">37</span>	                <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"navbar-header"</span>&gt;</span>
<span class="linenos">38</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">button</span> <span class="hljs-attribute">type</span>=<span class="hljs-value">"button"</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"navbar-toggle"</span> <span class="hljs-attribute">data-toggle</span>=<span class="hljs-value">"collapse"</span> <span class="hljs-attribute">data-target</span>=<span class="hljs-value">"#header-nav"</span>&gt;</span>
<span class="linenos">39</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"icon-bar"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span>
<span class="linenos">40</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"icon-bar"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span>
<span class="linenos">41</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"icon-bar"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span>
<span class="linenos">42</span>	                    <span class="hljs-tag">&lt;/<span class="hljs-title">button</span>&gt;</span>
<span class="linenos">43</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"navbar-brand"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">url</span> 'blogengine:index' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>My Django Blog<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">44</span>	                <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">45</span>	                <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"collapse navbar-collapse"</span> <span class="hljs-attribute">id</span>=<span class="hljs-value">"header-nav"</span>&gt;</span>
<span class="linenos">46</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">ul</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"nav navbar-nav"</span>&gt;</span>
<span class="linenos">47</span>	                        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">load</span> flatpages %}</span><span class="xml">
<span class="linenos">48</span>	                        </span><span class="hljs-template_tag">{% get_flatpages <span class="hljs-keyword">as</span> flatpages %}</span><span class="xml">
<span class="linenos">49</span>	                        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">for</span> flatpage <span class="hljs-keyword">in</span> flatpages %}</span><span class="xml">
<span class="linenos">50</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">li</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ flatpage.url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span></span><span class="hljs-variable">{{ flatpage.title }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">li</span>&gt;</span>
<span class="linenos">51</span>	                        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endfor</span> %}</span><span class="xml">
<span class="linenos">52</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">li</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/feeds/posts/"</span>&gt;</span>RSS feed<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">li</span>&gt;</span>
<span class="linenos">53</span>	
<span class="linenos">54</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">form</span> <span class="hljs-attribute">action</span>=<span class="hljs-value">"/search"</span> <span class="hljs-attribute">method</span>=<span class="hljs-value">"GET"</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"navbar-form navbar-left"</span>&gt;</span>
<span class="linenos">55</span>	                            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"form-group"</span>&gt;</span>
<span class="linenos">56</span>	                                <span class="hljs-tag">&lt;<span class="hljs-title">input</span> <span class="hljs-attribute">type</span>=<span class="hljs-value">"text"</span> <span class="hljs-attribute">name</span>=<span class="hljs-value">"q"</span> <span class="hljs-attribute">placeholder</span>=<span class="hljs-value">"Search..."</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"form-control"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">input</span>&gt;</span>
<span class="linenos">57</span>	                            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">58</span>	                            <span class="hljs-tag">&lt;<span class="hljs-title">button</span> <span class="hljs-attribute">type</span>=<span class="hljs-value">"submit"</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"btn btn-default"</span>&gt;</span>Search<span class="hljs-tag">&lt;/<span class="hljs-title">button</span>&gt;</span>
<span class="linenos">59</span>	                        <span class="hljs-tag">&lt;/<span class="hljs-title">form</span>&gt;</span>
<span class="linenos">60</span>	                    <span class="hljs-tag">&lt;/<span class="hljs-title">ul</span>&gt;</span>
<span class="linenos">61</span>	                <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">62</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">63</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">64</span>	
<span class="linenos">65</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"container"</span>&gt;</span>
<span class="linenos">66</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">block</span> header %}</span><span class="xml">
<span class="linenos">67</span>	                <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"page-header"</span>&gt;</span>
<span class="linenos">68</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">h1</span>&gt;</span>My Django Blog<span class="hljs-tag">&lt;/<span class="hljs-title">h1</span>&gt;</span>
<span class="linenos">69</span>	                <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">70</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endblock</span> %}</span><span class="xml">
<span class="linenos">71</span>	
<span class="linenos">72</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"row"</span>&gt;</span>
<span class="linenos">73</span>	                </span><span class="hljs-template_tag">{% <span class="hljs-keyword">block</span> content %}</span><span class="xml"></span><span class="hljs-template_tag">{% <span class="hljs-keyword">endblock</span> %}</span><span class="xml">
<span class="linenos">74</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">75</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">76</span>	
<span class="linenos">77</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"container footer"</span>&gt;</span>
<span class="linenos">78</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"row"</span>&gt;</span>
<span class="linenos">79</span>	                <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"span12"</span>&gt;</span>
<span class="linenos">80</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">p</span>&gt;</span>Copyright &amp;copy; </span><span class="hljs-template_tag">{% <span class="hljs-keyword">now</span> "Y" %}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">p</span>&gt;</span>
<span class="linenos">81</span>	                <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">82</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">83</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">84</span>	
<span class="linenos">85</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span> <span class="hljs-attribute">src</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'js/all.min.js' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span><span class="undefined"></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">86</span>	
<span class="linenos">87</span>	        <span class="hljs-comment">&lt;!-- Google Analytics: change UA-XXXXX-X to be your site's ID. --&gt;</span>
<span class="linenos">88</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span>&gt;</span><span class="scheme">
<span class="linenos">89</span>	            <span class="hljs-list">(<span class="hljs-keyword">function</span><span class="hljs-list">(<span class="hljs-keyword">b</span>,o,i,l,e,r)</span>{b.GoogleAnalyticsObject=l<span class="hljs-comment">;b[l]||(b[l]=</span>
<span class="linenos">90</span>	            function<span class="hljs-list">()</span>{<span class="hljs-list">(<span class="hljs-keyword">b</span><span class="hljs-list">[<span class="hljs-keyword">l</span>].q=b<span class="hljs-list">[<span class="hljs-keyword">l</span>].q||<span class="hljs-list">[]).push<span class="hljs-list">(<span class="hljs-keyword">arguments</span>)</span>})<span class="hljs-comment">;b[l].l=+new Date;</span>
<span class="linenos">91</span>	            e=o.createElement<span class="hljs-list">(<span class="hljs-keyword">i</span>)</span><span class="hljs-comment">;r=o.getElementsByTagName(i)[0];</span>
<span class="linenos">92</span>	            e.src=<span class="hljs-variable">'//www.google-analytics.com/analytics.js</span>'<span class="hljs-comment">;</span>
<span class="linenos">93</span>	            r.parentNode.insertBefore<span class="hljs-list">(<span class="hljs-keyword">e</span>,r)</span>}<span class="hljs-list">(<span class="hljs-keyword">window</span>,document,<span class="hljs-variable">'script</span>',<span class="hljs-variable">'ga</span>')</span>)<span class="hljs-comment">;</span>
<span class="linenos">94</span>	            ga<span class="hljs-list">(<span class="hljs-variable">'create</span>',<span class="hljs-variable">'UA-XXXXX-X</span>')</span><span class="hljs-comment">;ga('send','pageview');</span>
<span class="linenos">95</span>	        </span></span></span></span></span></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">96</span>	    <span class="hljs-tag">&lt;/<span class="hljs-title">body</span>&gt;</span>
<span class="linenos">97</span>	<span class="hljs-tag">&lt;/<span class="hljs-title">html</span>&gt;</span></span></code></pre>
<p>Now, run the Grunt task:</p>
<pre><code class="hljs lang-bash singleline">$ grunt</code></pre>
<p>And collect the static files:</p>
<pre><code class="hljs lang-bash singleline">$ python manage.py collectstatic</code></pre>
<p>You’ll also want to add your <code>node_modules</code> folder to your <code>gitignore</code>:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	venv/
<span class="linenos">2</span>	*.pyc
<span class="linenos">3</span>	db.sqlite3
<span class="linenos">4</span>	reports/
<span class="linenos">5</span>	htmlcov/
<span class="linenos">6</span>	.coverage
<span class="linenos">7</span>	node_modules/</code></pre>
<p>Then commit your changes:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add .
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Optimised static assets'</span></code></pre>
<p>Now, our <code>package.json</code> will cause a problem - it will mean that this app is mistakenly identified as a Node.js app. To prevent this, create the <code>.slugignore</code> file:</p>
<pre><code class="hljs lang-bash singleline">package.json</code></pre>
<p>Then commit your changes and push them up:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add .slugignore
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Added slugignore'</span>
<span class="linenos">3</span>	$ fab deploy</code></pre>
<p>If you check, your site should now be loading the minified versions of the static files.</p>
<p>That’s our site done! As usual I’ve tagged the final commit with <code>lesson-8</code>.</p>
<p>Sadly, that’s our final instalment over with! I hope you’ve enjoyed these tutorials, and I look forward to seeing what you create with them.</p>
]]></description>
            <link>http://matthewdaly.co.uk/blog/2014/08/31/django-blog-tutorial-the-next-generation-part-8/</link>
            <guid isPermaLink="true">http://matthewdaly.co.uk/blog/2014/08/31/django-blog-tutorial-the-next-generation-part-8/</guid>
            <pubDate>Sun, 31 Aug 2014 21:00:00 GMT</pubDate>
        </item>
        <item>
            <title><![CDATA[Django Blog Tutorial - the Next Generation - Part 7]]></title>
            <description><![CDATA[<p>Hello once again! In this instalment we’ll cover:</p>
<ul>
<li>Caching your content with Memcached to improve your site’s performance</li>
<li>Refactoring and simplifying our tests</li>
<li>Implementing additional feeds</li>
<li>Creating a simple search engine</li>
</ul>
<p>Don’t forget to activate your virtualenv:</p>
<pre><code class="hljs lang-bash singleline">$ <span class="hljs-built_in">source</span> venv/bin/activate</code></pre>
<p>Now let’s get started!</p>
<h1 id="memcached">Memcached</h1>
<p>If you frequent (or used to frequent) social media sites like Reddit, Slashdot or Digg, you may be familiar with something called variously the Digg or Slashdot effect, whereby if a page gets submitted to a social media site, and subsequently becomes popular, it can be hit by a huge number of HTTP requests in a very short period of time, slowing it down or even taking the server down completely.</p>
<p>Now, as a general rule of thumb, for most dynamic websites such as blogs, the bottleneck is not the web server or the programming language, but the database. If you have a lot of people hitting the same page over and over again in quick succession, then you’re essentially running the same query over and over again and getting the same result each time, which is expensive in terms of processing power. What you need to be able to do is cache the results of the query in memory for a given period of time so that the number of queries is reduced.</p>
<p>That’s where Memcached comes in. It’s a simple key-value store that allows you to store values in memory for a given period of time so that they can be retrieved without having to query the database. Memcached is a very common choice for caching, and is by far the fastest and most efficient type of cache available for Django. It’s also available on Heroku’s free tier.</p>
<p>Django has a very powerful caching framework that supports numerous types of cache in addition to Memcached, such as:</p>
<ul>
<li>Database caching</li>
<li>Filesystem caching</li>
<li>Local memory caching</li>
</ul>
<p>There are also third-party backends for using other caching systems such as Redis.</p>
<p>Now, the cache can be used in a number of different ways. You can cache only certain parts of your site if you wish. However, because our site is heavily content-driven, we should be pretty safe to use the per-site cache, which is the simplest way to set up caching.</p>
<p>In order to set up Memcached, there’s a couple of Python libraries we’ll need. If you want to install them locally, however, you’ll need to install both memcached and libmemcached (on Ubuntu, the packages you need are called <code>memcached</code> and <code>libmemcached-dev</code>) on your development machine. If you don’t want to do this, then just copy and paste these lines into <code>requirements.txt</code> instead:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	django-pylibmc-sasl==<span class="hljs-number">0.2</span>.<span class="hljs-number">4</span>
<span class="linenos">2</span>	pylibmc==<span class="hljs-number">1.3</span>.<span class="hljs-number">0</span></code></pre>
<p>If you are happy to install these dependencies locally, then run this command once memcached and libmemcached are installed:</p>
<pre><code class="hljs lang-bash singleline">$ pip install pylibmc django-pylibmc-sasl</code></pre>
<p>With that done let’s configure Memcached. Open up the settings file and add the following at the bottom:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">get_cache</span><span class="hljs-params">()</span>:</span>
<span class="linenos">2</span>	  <span class="hljs-keyword">import</span> os
<span class="linenos">3</span>	  <span class="hljs-keyword">try</span>:
<span class="linenos">4</span>	    os.environ[<span class="hljs-string">'MEMCACHE_SERVERS'</span>] = os.environ[<span class="hljs-string">'MEMCACHIER_SERVERS'</span>].replace(<span class="hljs-string">','</span>, <span class="hljs-string">';'</span>)
<span class="linenos">5</span>	    os.environ[<span class="hljs-string">'MEMCACHE_USERNAME'</span>] = os.environ[<span class="hljs-string">'MEMCACHIER_USERNAME'</span>]
<span class="linenos">6</span>	    os.environ[<span class="hljs-string">'MEMCACHE_PASSWORD'</span>] = os.environ[<span class="hljs-string">'MEMCACHIER_PASSWORD'</span>]
<span class="linenos">7</span>	    <span class="hljs-keyword">return</span> {
<span class="linenos">8</span>	      <span class="hljs-string">'default'</span>: {
<span class="linenos">9</span>	        <span class="hljs-string">'BACKEND'</span>: <span class="hljs-string">'django_pylibmc.memcached.PyLibMCCache'</span>,
<span class="linenos">10</span>	        <span class="hljs-string">'TIMEOUT'</span>: <span class="hljs-number">300</span>,
<span class="linenos">11</span>	        <span class="hljs-string">'BINARY'</span>: <span class="hljs-keyword">True</span>,
<span class="linenos">12</span>	        <span class="hljs-string">'OPTIONS'</span>: { <span class="hljs-string">'tcp_nodelay'</span>: <span class="hljs-keyword">True</span> }
<span class="linenos">13</span>	      }
<span class="linenos">14</span>	    }
<span class="linenos">15</span>	  <span class="hljs-keyword">except</span>:
<span class="linenos">16</span>	    <span class="hljs-keyword">return</span> {
<span class="linenos">17</span>	      <span class="hljs-string">'default'</span>: {
<span class="linenos">18</span>	        <span class="hljs-string">'BACKEND'</span>: <span class="hljs-string">'django.core.cache.backends.locmem.LocMemCache'</span>
<span class="linenos">19</span>	      }
<span class="linenos">20</span>	    }
<span class="linenos">21</span>	
<span class="linenos">22</span>	CACHES = get_cache()
<span class="linenos">23</span>	CACHE_MIDDLEWARE_ALIAS = <span class="hljs-string">'default'</span>
<span class="linenos">24</span>	CACHE_MIDDLEWARE_SECONDS = <span class="hljs-number">300</span>
<span class="linenos">25</span>	CACHE_MIDDLEWARE_KEY_PREFIX = <span class="hljs-string">''</span></code></pre>
<p>Then add the following to <code>MIDDLEWARE_CLASSES</code>:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	    <span class="hljs-string">'django.middleware.cache.UpdateCacheMiddleware'</span>,
<span class="linenos">2</span>	    <span class="hljs-string">'django.middleware.common.CommonMiddleware'</span>,
<span class="linenos">3</span>	    <span class="hljs-string">'django.middleware.cache.FetchFromCacheMiddleware'</span>,</code></pre>
<p>That’s it! The first section configures the application to use Memcached to cache the content when running on Heroku, and sets some configuration parameters, while the second  section tells Django to use the per-site cache in order to cache all the site content.</p>
<p>Now, Heroku doesn’t include Memcached by default - instead it’s available as an add-on called Memcachier. To use add-ons you need to set up a credit card for billing. We will set it up to use the free developer plan, but if you outgrow this you can easily switch to a paid plan. To add Memcachier, run this command:</p>
<pre><code class="hljs lang-bash singleline">$ heroku addons:add memcachier:dev</code></pre>
<p>Please note that Memcachier can take a few minutes to get set up, so you may want to leave it a little while between adding it and pushing up your changes. Now we’ll commit our changes:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add requirements.txt django_tutorial_blog_ng/settings.py
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Implemented caching with Memcached'</span></code></pre>
<p>Then we’ll push them up to our remote repository and to Heroku:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git push origin master
<span class="linenos">2</span>	$ git push heroku master</code></pre>
<p>And that’s all you need to do to set up Memcached. In addition to storing your query results in Memcached, enabling the caching framework in Django will also set various HTTP headers to enable web proxies and browsers to cache content for an appropriate length of time. If you open up your browser’s developer tools and compare the response headers for your homepage on the latest version of the code with the previous version, you’ll notice that a number of additional headers appear, including <code>Cache-Control</code>, <code>Expires</code> and <code>Last-Modified</code>. These tell web browsers and web proxies how often to request the latest version of the HTML document, in order to help you reduce the bandwidth used.</p>
<p>As you can see, for a site like this where you are the only person adding content, it’s really easy to implement caching with Django, and for a blog there’s very little reason not to do it. If you’re not using Heroku and are instead hosting your site on a VPS, then the configuration will be somewhat different - see <a href="https://docs.djangoproject.com/en/1.6/topics/cache/#memcached">here</a> for details. You can also find information on using other cache backends on the same page.</p>
<p>That isn’t all you can do to speed up your site. Heroku doesn’t seem to be very good for serving static files, and if your site is attracting a lot of traffic you might want to host your static files elsewhere, such as on Amazon’s S3 service. Doing so is outside the scope of this tutorial, but for that use case, you should check out <a href="http://django-storages.readthedocs.org/en/latest/index.html">django-storages</a>.</p>
<h1 id="clearing-the-cache-automatically">Clearing the cache automatically</h1>
<p>There is one issue with this implementation. As it is right now, if you view the home page, add a post, then reload the page, you may not see the new post immediately because the cache will continue serving the old version until it has expired. That behaviour is less than ideal - we would like the cache to be cleared automatically when a new post gets added so that users will see the new version immediately. That response will still be cached afterwards, so it only means one extra query.</p>
<p>This is the ideal place to introduce <a href="https://docs.djangoproject.com/en/dev/topics/signals/">signals</a>. Signals are a way to carry out a given action when an event takes place. In our case, we plan to clear the cache when a post is saved (either created or updated).</p>
<p>Note that as we’ll be testing the behaviour of the cache at this point, you’ll need to install Memcached on your local machine, and we’ll need to change the settings to fall back to our local Memcached instance:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">get_cache</span><span class="hljs-params">()</span>:</span>
<span class="linenos">2</span>	  <span class="hljs-keyword">import</span> os
<span class="linenos">3</span>	  <span class="hljs-keyword">try</span>:
<span class="linenos">4</span>	    os.environ[<span class="hljs-string">'MEMCACHE_SERVERS'</span>] = os.environ[<span class="hljs-string">'MEMCACHIER_SERVERS'</span>].replace(<span class="hljs-string">','</span>, <span class="hljs-string">';'</span>)
<span class="linenos">5</span>	    os.environ[<span class="hljs-string">'MEMCACHE_USERNAME'</span>] = os.environ[<span class="hljs-string">'MEMCACHIER_USERNAME'</span>]
<span class="linenos">6</span>	    os.environ[<span class="hljs-string">'MEMCACHE_PASSWORD'</span>] = os.environ[<span class="hljs-string">'MEMCACHIER_PASSWORD'</span>]
<span class="linenos">7</span>	    <span class="hljs-keyword">return</span> {
<span class="linenos">8</span>	      <span class="hljs-string">'default'</span>: {
<span class="linenos">9</span>	        <span class="hljs-string">'BACKEND'</span>: <span class="hljs-string">'django_pylibmc.memcached.PyLibMCCache'</span>,
<span class="linenos">10</span>	        <span class="hljs-string">'TIMEOUT'</span>: <span class="hljs-number">300</span>,
<span class="linenos">11</span>	        <span class="hljs-string">'BINARY'</span>: <span class="hljs-keyword">True</span>,
<span class="linenos">12</span>	        <span class="hljs-string">'OPTIONS'</span>: { <span class="hljs-string">'tcp_nodelay'</span>: <span class="hljs-keyword">True</span> }
<span class="linenos">13</span>	      }
<span class="linenos">14</span>	    }
<span class="linenos">15</span>	  <span class="hljs-keyword">except</span>:
<span class="linenos">16</span>	    <span class="hljs-keyword">return</span> {
<span class="linenos">17</span>	      <span class="hljs-string">'default'</span>: {
<span class="linenos">18</span>	        <span class="hljs-string">'BACKEND'</span>: <span class="hljs-string">'django.core.cache.backends.memcached.PyLibMCCache'</span>,
<span class="linenos">19</span>	        <span class="hljs-string">'LOCATION'</span>: <span class="hljs-string">'127.0.0.1:11211'</span>
<span class="linenos">20</span>	      }
<span class="linenos">21</span>	    }
<span class="linenos">22</span>	
<span class="linenos">23</span>	CACHES = get_cache()
<span class="linenos">24</span>	CACHE_MIDDLEWARE_ALIAS = <span class="hljs-string">'default'</span>
<span class="linenos">25</span>	CACHE_MIDDLEWARE_SECONDS = <span class="hljs-number">300</span>
<span class="linenos">26</span>	CACHE_MIDDLEWARE_KEY_PREFIX = <span class="hljs-string">''</span></code></pre>
<p>If you don’t want to install Memcached locally, you can skip this step, but be aware that the test we write for clearing the cache will always pass if you do skip it.</p>
<p>Then we’ll run our tests to make sure nothing has been broken:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py jenkins blogengine
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	.......................
<span class="linenos">4</span>	----------------------------------------------------------------------
<span class="linenos">5</span>	Ran <span class="hljs-number">23</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">6.164</span>s
<span class="linenos">6</span>	
<span class="linenos">7</span>	OK</code></pre>
<p>Let’s commit:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add django_tutorial_blog_ng/settings.py
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Now use Memcached in development'</span></code></pre>
<p>Now we’ll add a test for clearing the cache to <code>PostViewTest</code>:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_clear_cache</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">2</span>	        <span class="hljs-comment"># Create the category</span>
<span class="linenos">3</span>	        category = Category()
<span class="linenos">4</span>	        category.name = <span class="hljs-string">'python'</span>
<span class="linenos">5</span>	        category.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">6</span>	        category.save()
<span class="linenos">7</span>	
<span class="linenos">8</span>	        <span class="hljs-comment"># Create the tag</span>
<span class="linenos">9</span>	        tag = Tag()
<span class="linenos">10</span>	        tag.name = <span class="hljs-string">'perl'</span>
<span class="linenos">11</span>	        tag.description = <span class="hljs-string">'The Perl programming language'</span>
<span class="linenos">12</span>	        tag.save()
<span class="linenos">13</span>	
<span class="linenos">14</span>	        <span class="hljs-comment"># Create the author</span>
<span class="linenos">15</span>	        author = User.objects.create_user(<span class="hljs-string">'testuser'</span>, <span class="hljs-string">'user@example.com'</span>, <span class="hljs-string">'password'</span>)
<span class="linenos">16</span>	        author.save()
<span class="linenos">17</span>	
<span class="linenos">18</span>	        <span class="hljs-comment"># Create the site</span>
<span class="linenos">19</span>	        site = Site()
<span class="linenos">20</span>	        site.name = <span class="hljs-string">'example.com'</span>
<span class="linenos">21</span>	        site.domain = <span class="hljs-string">'example.com'</span>
<span class="linenos">22</span>	        site.save()
<span class="linenos">23</span>	
<span class="linenos">24</span>	        <span class="hljs-comment"># Create the first post</span>
<span class="linenos">25</span>	        post = Post()
<span class="linenos">26</span>	        post.title = <span class="hljs-string">'My first post'</span>
<span class="linenos">27</span>	        post.text = <span class="hljs-string">'This is [my first blog post](http://127.0.0.1:8000/)'</span>
<span class="linenos">28</span>	        post.slug = <span class="hljs-string">'my-first-post'</span>
<span class="linenos">29</span>	        post.pub_date = timezone.now()
<span class="linenos">30</span>	        post.author = author
<span class="linenos">31</span>	        post.site = site
<span class="linenos">32</span>	        post.category = category
<span class="linenos">33</span>	        post.save()
<span class="linenos">34</span>	        post.tags.add(tag)
<span class="linenos">35</span>	
<span class="linenos">36</span>	        <span class="hljs-comment"># Check new post saved</span>
<span class="linenos">37</span>	        all_posts = Post.objects.all()
<span class="linenos">38</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">39</span>	
<span class="linenos">40</span>	        <span class="hljs-comment"># Fetch the index</span>
<span class="linenos">41</span>	        response = self.client.get(<span class="hljs-string">'/'</span>)
<span class="linenos">42</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">43</span>	
<span class="linenos">44</span>	        <span class="hljs-comment"># Create the second post</span>
<span class="linenos">45</span>	        post = Post()
<span class="linenos">46</span>	        post.title = <span class="hljs-string">'My second post'</span>
<span class="linenos">47</span>	        post.text = <span class="hljs-string">'This is [my second blog post](http://127.0.0.1:8000/)'</span>
<span class="linenos">48</span>	        post.slug = <span class="hljs-string">'my-second-post'</span>
<span class="linenos">49</span>	        post.pub_date = timezone.now()
<span class="linenos">50</span>	        post.author = author
<span class="linenos">51</span>	        post.site = site
<span class="linenos">52</span>	        post.category = category
<span class="linenos">53</span>	        post.save()
<span class="linenos">54</span>	        post.tags.add(tag)
<span class="linenos">55</span>	
<span class="linenos">56</span>	        <span class="hljs-comment"># Fetch the index again</span>
<span class="linenos">57</span>	        response = self.client.get(<span class="hljs-string">'/'</span>)
<span class="linenos">58</span>	
<span class="linenos">59</span>	        <span class="hljs-comment"># Check second post present</span>
<span class="linenos">60</span>	        self.assertTrue(<span class="hljs-string">'my second blog post'</span> <span class="hljs-keyword">in</span> response.content)</code></pre>
<p>This should be fairly self-explanatory. We create one post, and request the index page. We then add a second post, request the index page again, and check for the second post. The test should fail because the cached version is returned, rather than the version in the database.</p>
<p>Now we have a test in place, we can implement a fix. First, add this to the top of your <code>models.py</code>:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-keyword">from</span> django.db.models.signals <span class="hljs-keyword">import</span> post_save
<span class="linenos">2</span>	<span class="hljs-keyword">from</span> django.core.cache <span class="hljs-keyword">import</span> cache</code></pre>
<p>Then add the following at the bottom of the file:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	
<span class="linenos">2</span>	<span class="hljs-comment"># Define signals</span>
<span class="linenos">3</span>	<span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">new_post</span><span class="hljs-params">(sender, instance, created, **kwargs)</span>:</span>
<span class="linenos">4</span>	    cache.clear()
<span class="linenos">5</span>	
<span class="linenos">6</span>	<span class="hljs-comment"># Set up signals</span>
<span class="linenos">7</span>	post_save.connect(new_post, sender=Post)</code></pre>
<p>This is fairly straightforward. What we’re doing is first defining a function called <code>new_post</code> that is called when a new post is created. We then connect it to the <code>post_save</code> signal. When a post is saved, it calls <code>new_post</code>, which clears the cache, making sure users are seeing the latest and greatest version of your site immediately.</p>
<p>Let’s test it:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py jenkins blogengine
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	........................
<span class="linenos">4</span>	----------------------------------------------------------------------
<span class="linenos">5</span>	Ran <span class="hljs-number">24</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">8.473</span>s
<span class="linenos">6</span>	
<span class="linenos">7</span>	OK
<span class="linenos">8</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>There are a number of signals available, and when you create one, you have access to the created object via the <code>instance</code> parameter. Using signals you can implement all kinds of functionality. For instance, you could implement the functionality to send an email when a new post is published.</p>
<p>If you’re using Travis CI, you’ll also need to update the config file:</p>
<pre><code class="hljs lang-yaml"><span class="linenos">1</span>	<span class="hljs-label">language:</span> python
<span class="linenos">2</span>	<span class="hljs-label">python:</span>
<span class="linenos">3</span>	- <span class="hljs-string">"2.7"</span>
<span class="linenos">4</span>	<span class="hljs-label">services:</span> memcached
<span class="linenos">5</span>	<span class="hljs-label">before_install:</span>
<span class="linenos">6</span>	    - sudo apt-get install -<span class="hljs-built_in">y</span> libmemcached-dev
<span class="linenos">7</span>	<span class="hljs-preprocessor"># command to install dependencies</span>
<span class="linenos">8</span>	<span class="hljs-label">install:</span> <span class="hljs-string">"pip install -r requirements.txt"</span>
<span class="linenos">9</span>	<span class="hljs-preprocessor"># command to run tests</span>
<span class="linenos">10</span>	<span class="hljs-label">script:</span> coverage run --include=<span class="hljs-string">"blogengine/*"</span> --omit=<span class="hljs-string">"blogengine/migrations/*"</span> manage.py test blogengine
<span class="linenos">11</span>	<span class="hljs-label">after_success:</span>
<span class="linenos">12</span>	    coveralls</code></pre>
<p>Time to commit:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add blogengine/ .travis.yml
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Now clear cache when post added'</span></code></pre>
<h1 id="formatting-for-rss-feeds">Formatting for RSS feeds</h1>
<p>Now, we want to offer more than one option for RSS feeds. For instance, if your blog is aggregated on a site such as <a href="http://planet.python.org/">Planet Python</a>, but you also blog about JavaScript, you may want to be able to provide a feed for posts in the <code>python</code> category only.</p>
<p>If you have written any posts that use any of Markdown’s custom formatting, you may notice that if you load your RSS feed in a reader, it isn’t formatted as Markdown. Let’s fix that. First we’ll amend our test:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">FeedTest</span><span class="hljs-params">(BaseAcceptanceTest)</span>:</span>
<span class="linenos">2</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_all_post_feed</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">3</span>	        <span class="hljs-comment"># Create the category</span>
<span class="linenos">4</span>	        category = Category()
<span class="linenos">5</span>	        category.name = <span class="hljs-string">'python'</span>
<span class="linenos">6</span>	        category.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">7</span>	        category.save()
<span class="linenos">8</span>	
<span class="linenos">9</span>	        <span class="hljs-comment"># Create the tag</span>
<span class="linenos">10</span>	        tag = Tag()
<span class="linenos">11</span>	        tag.name = <span class="hljs-string">'python'</span>
<span class="linenos">12</span>	        tag.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">13</span>	        tag.save()
<span class="linenos">14</span>	
<span class="linenos">15</span>	        <span class="hljs-comment"># Create the author</span>
<span class="linenos">16</span>	        author = User.objects.create_user(<span class="hljs-string">'testuser'</span>, <span class="hljs-string">'user@example.com'</span>, <span class="hljs-string">'password'</span>)
<span class="linenos">17</span>	        author.save()
<span class="linenos">18</span>	
<span class="linenos">19</span>	        <span class="hljs-comment"># Create the site</span>
<span class="linenos">20</span>	        site = Site()
<span class="linenos">21</span>	        site.name = <span class="hljs-string">'example.com'</span>
<span class="linenos">22</span>	        site.domain = <span class="hljs-string">'example.com'</span>
<span class="linenos">23</span>	        site.save()
<span class="linenos">24</span>	
<span class="linenos">25</span>	        <span class="hljs-comment"># Create a post</span>
<span class="linenos">26</span>	        post = Post()
<span class="linenos">27</span>	        post.title = <span class="hljs-string">'My first post'</span>
<span class="linenos">28</span>	        post.text = <span class="hljs-string">'This is my *first* blog post'</span>
<span class="linenos">29</span>	        post.slug = <span class="hljs-string">'my-first-post'</span>
<span class="linenos">30</span>	        post.pub_date = timezone.now()
<span class="linenos">31</span>	        post.author = author
<span class="linenos">32</span>	        post.site = site
<span class="linenos">33</span>	        post.category = category
<span class="linenos">34</span>	
<span class="linenos">35</span>	        <span class="hljs-comment"># Save it</span>
<span class="linenos">36</span>	        post.save()
<span class="linenos">37</span>	
<span class="linenos">38</span>	        <span class="hljs-comment"># Add the tag</span>
<span class="linenos">39</span>	        post.tags.add(tag)
<span class="linenos">40</span>	        post.save()
<span class="linenos">41</span>	
<span class="linenos">42</span>	        <span class="hljs-comment"># Check we can find it</span>
<span class="linenos">43</span>	        all_posts = Post.objects.all()
<span class="linenos">44</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">45</span>	        only_post = all_posts[<span class="hljs-number">0</span>]
<span class="linenos">46</span>	        self.assertEquals(only_post, post)
<span class="linenos">47</span>	
<span class="linenos">48</span>	        <span class="hljs-comment"># Fetch the feed</span>
<span class="linenos">49</span>	        response = self.client.get(<span class="hljs-string">'/feeds/posts/'</span>)
<span class="linenos">50</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">51</span>	
<span class="linenos">52</span>	        <span class="hljs-comment"># Parse the feed</span>
<span class="linenos">53</span>	        feed = feedparser.parse(response.content)
<span class="linenos">54</span>	
<span class="linenos">55</span>	        <span class="hljs-comment"># Check length</span>
<span class="linenos">56</span>	        self.assertEquals(len(feed.entries), <span class="hljs-number">1</span>)
<span class="linenos">57</span>	
<span class="linenos">58</span>	        <span class="hljs-comment"># Check post retrieved is the correct one</span>
<span class="linenos">59</span>	        feed_post = feed.entries[<span class="hljs-number">0</span>]
<span class="linenos">60</span>	        self.assertEquals(feed_post.title, post.title)
<span class="linenos">61</span>	        self.assertTrue(<span class="hljs-string">'This is my &lt;em&gt;first&lt;/em&gt; blog post'</span> <span class="hljs-keyword">in</span> feed_post.description)</code></pre>
<p>Don’t forget to run the tests to make sure they fail. Now, let’s fix it:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-keyword">from</span> django.shortcuts <span class="hljs-keyword">import</span> render
<span class="linenos">2</span>	<span class="hljs-keyword">from</span> django.views.generic <span class="hljs-keyword">import</span> ListView
<span class="linenos">3</span>	<span class="hljs-keyword">from</span> blogengine.models <span class="hljs-keyword">import</span> Category, Post, Tag
<span class="linenos">4</span>	<span class="hljs-keyword">from</span> django.contrib.syndication.views <span class="hljs-keyword">import</span> Feed
<span class="linenos">5</span>	<span class="hljs-keyword">from</span> django.utils.encoding <span class="hljs-keyword">import</span> force_unicode
<span class="linenos">6</span>	<span class="hljs-keyword">from</span> django.utils.safestring <span class="hljs-keyword">import</span> mark_safe
<span class="linenos">7</span>	<span class="hljs-keyword">import</span> markdown2
<span class="linenos">8</span>	
<span class="linenos">9</span>	<span class="hljs-comment"># Create your views here.</span>
<span class="linenos">10</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">CategoryListView</span><span class="hljs-params">(ListView)</span>:</span>
<span class="linenos">11</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">get_queryset</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">12</span>	        slug = self.kwargs[<span class="hljs-string">'slug'</span>]
<span class="linenos">13</span>	        <span class="hljs-keyword">try</span>:
<span class="linenos">14</span>	            category = Category.objects.get(slug=slug)
<span class="linenos">15</span>	            <span class="hljs-keyword">return</span> Post.objects.filter(category=category)
<span class="linenos">16</span>	        <span class="hljs-keyword">except</span> Category.DoesNotExist:
<span class="linenos">17</span>	            <span class="hljs-keyword">return</span> Post.objects.none()
<span class="linenos">18</span>	
<span class="linenos">19</span>	
<span class="linenos">20</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">TagListView</span><span class="hljs-params">(ListView)</span>:</span>
<span class="linenos">21</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">get_queryset</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">22</span>	        slug = self.kwargs[<span class="hljs-string">'slug'</span>]
<span class="linenos">23</span>	        <span class="hljs-keyword">try</span>:
<span class="linenos">24</span>	            tag = Tag.objects.get(slug=slug)
<span class="linenos">25</span>	            <span class="hljs-keyword">return</span> tag.post_set.all()
<span class="linenos">26</span>	        <span class="hljs-keyword">except</span> Tag.DoesNotExist:
<span class="linenos">27</span>	            <span class="hljs-keyword">return</span> Post.objects.none()
<span class="linenos">28</span>	
<span class="linenos">29</span>	
<span class="linenos">30</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">PostsFeed</span><span class="hljs-params">(Feed)</span>:</span>
<span class="linenos">31</span>	    title = <span class="hljs-string">"RSS feed - posts"</span>
<span class="linenos">32</span>	    link = <span class="hljs-string">"feeds/posts/"</span>
<span class="linenos">33</span>	    description = <span class="hljs-string">"RSS feed - blog posts"</span>
<span class="linenos">34</span>	
<span class="linenos">35</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">items</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">36</span>	        <span class="hljs-keyword">return</span> Post.objects.order_by(<span class="hljs-string">'-pub_date'</span>)
<span class="linenos">37</span>	
<span class="linenos">38</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">item_title</span><span class="hljs-params">(self, item)</span>:</span>
<span class="linenos">39</span>	        <span class="hljs-keyword">return</span> item.title
<span class="linenos">40</span>	
<span class="linenos">41</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">item_description</span><span class="hljs-params">(self, item)</span>:</span>
<span class="linenos">42</span>	        extras = [<span class="hljs-string">"fenced-code-blocks"</span>]
<span class="linenos">43</span>	        content = mark_safe(markdown2.markdown(force_unicode(item.text),
<span class="linenos">44</span>	                                               extras = extras))
<span class="linenos">45</span>	        <span class="hljs-keyword">return</span> content</code></pre>
<p>All we’re doing here is amending the <code>item_description</code> method of <code>PostsFeed</code> to render it as Markdown. Now let’s run our tests again:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py jenkins
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	........................
<span class="linenos">4</span>	----------------------------------------------------------------------
<span class="linenos">5</span>	Ran <span class="hljs-number">24</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">9.370</span>s
<span class="linenos">6</span>	
<span class="linenos">7</span>	OK
<span class="linenos">8</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>With that done, we’ll commit our changes:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add blogengine/
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Fixed rendering for post feed'</span></code></pre>
<h1 id="refactoring-our-tests">Refactoring our tests</h1>
<p>Now, before we get into implementing the feed, our tests are a bit verbose. We create a lot of items over and over again - let’s sort that out. Factory Boy is a handy Python module that allows you to create easy-to-use factories for creating objects over and over again in tests. Let’s install it:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ pip install factory_boy
<span class="linenos">2</span>	$ pip freeze &gt; requirements.txt
<span class="linenos">3</span>	$ git add requirements.txt
<span class="linenos">4</span>	$ git commit -m <span class="hljs-string">'Installed Factory Boy'</span></code></pre>
<p>Now let’s set up a factory for creating posts. Add this at the top of the test file:</p>
<pre><code class="hljs lang-python singleline"><span class="hljs-keyword">import</span> factory.django</code></pre>
<p>Then, before your actual tests, insert the following:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-comment"># Factories</span>
<span class="linenos">2</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">SiteFactory</span><span class="hljs-params">(factory.django.DjangoModelFactory)</span>:</span>
<span class="linenos">3</span>	    <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Meta</span>:</span>
<span class="linenos">4</span>	        model = Site
<span class="linenos">5</span>	        django_get_or_create = (
<span class="linenos">6</span>	            <span class="hljs-string">'name'</span>,
<span class="linenos">7</span>	            <span class="hljs-string">'domain'</span>
<span class="linenos">8</span>	        )
<span class="linenos">9</span>	
<span class="linenos">10</span>	    name = <span class="hljs-string">'example.com'</span>
<span class="linenos">11</span>	    domain = <span class="hljs-string">'example.com'</span></code></pre>
<p>Now, wherever you call <code>Site()</code>, add its attributes, and save it, replace those lines with the following:</p>
<pre><code class="hljs lang-python singleline">        site = SiteFactory()</code></pre>
<p>Much simpler and more concise, I’m sure you’ll agree! Now, let’s run the tests to make sure they aren’t broken:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py <span class="hljs-built_in">test</span> blogengine
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	........................
<span class="linenos">4</span>	----------------------------------------------------------------------
<span class="linenos">5</span>	Ran <span class="hljs-number">24</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">7.482</span>s
<span class="linenos">6</span>	
<span class="linenos">7</span>	OK
<span class="linenos">8</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>Let’s commit again:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add blogengine/tests.py
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Now use Factory Boy for site objects'</span></code></pre>
<p>Let’s do the same thing with <code>Category</code> objects:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">CategoryFactory</span><span class="hljs-params">(factory.django.DjangoModelFactory)</span>:</span>
<span class="linenos">2</span>	    <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Meta</span>:</span>
<span class="linenos">3</span>	        model = Category
<span class="linenos">4</span>	        django_get_or_create = (
<span class="linenos">5</span>	            <span class="hljs-string">'name'</span>,
<span class="linenos">6</span>	            <span class="hljs-string">'description'</span>,
<span class="linenos">7</span>	            <span class="hljs-string">'slug'</span>
<span class="linenos">8</span>	        )
<span class="linenos">9</span>	
<span class="linenos">10</span>	    name = <span class="hljs-string">'python'</span>
<span class="linenos">11</span>	    description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">12</span>	    slug = <span class="hljs-string">'python'</span></code></pre>
<p>Again, just find every time we call <code>Category()</code> and replace it with the following:</p>
<pre><code class="hljs lang-python singleline">        category = CategoryFactory()</code></pre>
<p>Now if we run our tests, we’ll notice a serious error:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py <span class="hljs-built_in">test</span> blogengine
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	EE..EE.EE.EE...E.EEE..E.
<span class="linenos">4</span>	======================================================================
<span class="linenos">5</span>	ERROR: <span class="hljs-built_in">test</span>_create_category (blogengine.tests.PostTest)
<span class="linenos">6</span>	----------------------------------------------------------------------
<span class="linenos">7</span>	Traceback (most recent call last):
<span class="linenos">8</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">42</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_create_category
<span class="linenos">9</span>	    category = CategoryFactory()
<span class="linenos">10</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">82</span>, <span class="hljs-keyword">in</span> __call__
<span class="linenos">11</span>	    <span class="hljs-built_in">return</span> cls.create(**kwargs)
<span class="linenos">12</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">585</span>, <span class="hljs-keyword">in</span> create
<span class="linenos">13</span>	    <span class="hljs-built_in">return</span> cls._generate(True, attrs)
<span class="linenos">14</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">510</span>, <span class="hljs-keyword">in</span> _generate
<span class="linenos">15</span>	    obj = cls._prepare(create, **attrs)
<span class="linenos">16</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">485</span>, <span class="hljs-keyword">in</span> _prepare
<span class="linenos">17</span>	    <span class="hljs-built_in">return</span> cls._create(model_class, *args, **kwargs)
<span class="linenos">18</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/django.py"</span>, line <span class="hljs-number">151</span>, <span class="hljs-keyword">in</span> _create
<span class="linenos">19</span>	    <span class="hljs-built_in">return</span> cls._get_or_create(model_class, *args, **kwargs)
<span class="linenos">20</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/django.py"</span>, line <span class="hljs-number">142</span>, <span class="hljs-keyword">in</span> _get_or_create
<span class="linenos">21</span>	    obj, _created = manager.get_or_create(*args, **key_fields)
<span class="linenos">22</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/manager.py"</span>, line <span class="hljs-number">154</span>, <span class="hljs-keyword">in</span> get_or_create
<span class="linenos">23</span>	    <span class="hljs-built_in">return</span> self.get_queryset().get_or_create(**kwargs)
<span class="linenos">24</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/query.py"</span>, line <span class="hljs-number">383</span>, <span class="hljs-keyword">in</span> get_or_create
<span class="linenos">25</span>	    obj.save(force_insert=True, using=self.db)
<span class="linenos">26</span>	TypeError: save() got an unexpected keyword argument <span class="hljs-string">'force_insert'</span>
<span class="linenos">27</span>	
<span class="linenos">28</span>	======================================================================
<span class="linenos">29</span>	ERROR: <span class="hljs-built_in">test</span>_create_post (blogengine.tests.PostTest)
<span class="linenos">30</span>	----------------------------------------------------------------------
<span class="linenos">31</span>	Traceback (most recent call last):
<span class="linenos">32</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">80</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_create_post
<span class="linenos">33</span>	    category = CategoryFactory()
<span class="linenos">34</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">82</span>, <span class="hljs-keyword">in</span> __call__
<span class="linenos">35</span>	    <span class="hljs-built_in">return</span> cls.create(**kwargs)
<span class="linenos">36</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">585</span>, <span class="hljs-keyword">in</span> create
<span class="linenos">37</span>	    <span class="hljs-built_in">return</span> cls._generate(True, attrs)
<span class="linenos">38</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">510</span>, <span class="hljs-keyword">in</span> _generate
<span class="linenos">39</span>	    obj = cls._prepare(create, **attrs)
<span class="linenos">40</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">485</span>, <span class="hljs-keyword">in</span> _prepare
<span class="linenos">41</span>	    <span class="hljs-built_in">return</span> cls._create(model_class, *args, **kwargs)
<span class="linenos">42</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/django.py"</span>, line <span class="hljs-number">151</span>, <span class="hljs-keyword">in</span> _create
<span class="linenos">43</span>	    <span class="hljs-built_in">return</span> cls._get_or_create(model_class, *args, **kwargs)
<span class="linenos">44</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/django.py"</span>, line <span class="hljs-number">142</span>, <span class="hljs-keyword">in</span> _get_or_create
<span class="linenos">45</span>	    obj, _created = manager.get_or_create(*args, **key_fields)
<span class="linenos">46</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/manager.py"</span>, line <span class="hljs-number">154</span>, <span class="hljs-keyword">in</span> get_or_create
<span class="linenos">47</span>	    <span class="hljs-built_in">return</span> self.get_queryset().get_or_create(**kwargs)
<span class="linenos">48</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/query.py"</span>, line <span class="hljs-number">383</span>, <span class="hljs-keyword">in</span> get_or_create
<span class="linenos">49</span>	    obj.save(force_insert=True, using=self.db)
<span class="linenos">50</span>	TypeError: save() got an unexpected keyword argument <span class="hljs-string">'force_insert'</span>
<span class="linenos">51</span>	
<span class="linenos">52</span>	======================================================================
<span class="linenos">53</span>	ERROR: <span class="hljs-built_in">test</span>_create_post (blogengine.tests.AdminTest)
<span class="linenos">54</span>	----------------------------------------------------------------------
<span class="linenos">55</span>	Traceback (most recent call last):
<span class="linenos">56</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">339</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_create_post
<span class="linenos">57</span>	    category = CategoryFactory()
<span class="linenos">58</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">82</span>, <span class="hljs-keyword">in</span> __call__
<span class="linenos">59</span>	    <span class="hljs-built_in">return</span> cls.create(**kwargs)
<span class="linenos">60</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">585</span>, <span class="hljs-keyword">in</span> create
<span class="linenos">61</span>	    <span class="hljs-built_in">return</span> cls._generate(True, attrs)
<span class="linenos">62</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">510</span>, <span class="hljs-keyword">in</span> _generate
<span class="linenos">63</span>	    obj = cls._prepare(create, **attrs)
<span class="linenos">64</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">485</span>, <span class="hljs-keyword">in</span> _prepare
<span class="linenos">65</span>	    <span class="hljs-built_in">return</span> cls._create(model_class, *args, **kwargs)
<span class="linenos">66</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/django.py"</span>, line <span class="hljs-number">151</span>, <span class="hljs-keyword">in</span> _create
<span class="linenos">67</span>	    <span class="hljs-built_in">return</span> cls._get_or_create(model_class, *args, **kwargs)
<span class="linenos">68</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/django.py"</span>, line <span class="hljs-number">142</span>, <span class="hljs-keyword">in</span> _get_or_create
<span class="linenos">69</span>	    obj, _created = manager.get_or_create(*args, **key_fields)
<span class="linenos">70</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/manager.py"</span>, line <span class="hljs-number">154</span>, <span class="hljs-keyword">in</span> get_or_create
<span class="linenos">71</span>	    <span class="hljs-built_in">return</span> self.get_queryset().get_or_create(**kwargs)
<span class="linenos">72</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/query.py"</span>, line <span class="hljs-number">383</span>, <span class="hljs-keyword">in</span> get_or_create
<span class="linenos">73</span>	    obj.save(force_insert=True, using=self.db)
<span class="linenos">74</span>	TypeError: save() got an unexpected keyword argument <span class="hljs-string">'force_insert'</span>
<span class="linenos">75</span>	
<span class="linenos">76</span>	======================================================================
<span class="linenos">77</span>	ERROR: <span class="hljs-built_in">test</span>_create_post_without_tag (blogengine.tests.AdminTest)
<span class="linenos">78</span>	----------------------------------------------------------------------
<span class="linenos">79</span>	Traceback (most recent call last):
<span class="linenos">80</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">378</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_create_post_without_tag
<span class="linenos">81</span>	    category = CategoryFactory()
<span class="linenos">82</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">82</span>, <span class="hljs-keyword">in</span> __call__
<span class="linenos">83</span>	    <span class="hljs-built_in">return</span> cls.create(**kwargs)
<span class="linenos">84</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">585</span>, <span class="hljs-keyword">in</span> create
<span class="linenos">85</span>	    <span class="hljs-built_in">return</span> cls._generate(True, attrs)
<span class="linenos">86</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">510</span>, <span class="hljs-keyword">in</span> _generate
<span class="linenos">87</span>	    obj = cls._prepare(create, **attrs)
<span class="linenos">88</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">485</span>, <span class="hljs-keyword">in</span> _prepare
<span class="linenos">89</span>	    <span class="hljs-built_in">return</span> cls._create(model_class, *args, **kwargs)
<span class="linenos">90</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/django.py"</span>, line <span class="hljs-number">151</span>, <span class="hljs-keyword">in</span> _create
<span class="linenos">91</span>	    <span class="hljs-built_in">return</span> cls._get_or_create(model_class, *args, **kwargs)
<span class="linenos">92</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/django.py"</span>, line <span class="hljs-number">142</span>, <span class="hljs-keyword">in</span> _get_or_create
<span class="linenos">93</span>	    obj, _created = manager.get_or_create(*args, **key_fields)
<span class="linenos">94</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/manager.py"</span>, line <span class="hljs-number">154</span>, <span class="hljs-keyword">in</span> get_or_create
<span class="linenos">95</span>	    <span class="hljs-built_in">return</span> self.get_queryset().get_or_create(**kwargs)
<span class="linenos">96</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/query.py"</span>, line <span class="hljs-number">383</span>, <span class="hljs-keyword">in</span> get_or_create
<span class="linenos">97</span>	    obj.save(force_insert=True, using=self.db)
<span class="linenos">98</span>	TypeError: save() got an unexpected keyword argument <span class="hljs-string">'force_insert'</span>
<span class="linenos">99</span>	
<span class="linenos">100</span>	======================================================================
<span class="linenos">101</span>	ERROR: <span class="hljs-built_in">test</span>_delete_category (blogengine.tests.AdminTest)
<span class="linenos">102</span>	----------------------------------------------------------------------
<span class="linenos">103</span>	Traceback (most recent call last):
<span class="linenos">104</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">245</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_delete_category
<span class="linenos">105</span>	    category = CategoryFactory()
<span class="linenos">106</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">82</span>, <span class="hljs-keyword">in</span> __call__
<span class="linenos">107</span>	    <span class="hljs-built_in">return</span> cls.create(**kwargs)
<span class="linenos">108</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">585</span>, <span class="hljs-keyword">in</span> create
<span class="linenos">109</span>	    <span class="hljs-built_in">return</span> cls._generate(True, attrs)
<span class="linenos">110</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">510</span>, <span class="hljs-keyword">in</span> _generate
<span class="linenos">111</span>	    obj = cls._prepare(create, **attrs)
<span class="linenos">112</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">485</span>, <span class="hljs-keyword">in</span> _prepare
<span class="linenos">113</span>	    <span class="hljs-built_in">return</span> cls._create(model_class, *args, **kwargs)
<span class="linenos">114</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/django.py"</span>, line <span class="hljs-number">151</span>, <span class="hljs-keyword">in</span> _create
<span class="linenos">115</span>	    <span class="hljs-built_in">return</span> cls._get_or_create(model_class, *args, **kwargs)
<span class="linenos">116</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/django.py"</span>, line <span class="hljs-number">142</span>, <span class="hljs-keyword">in</span> _get_or_create
<span class="linenos">117</span>	    obj, _created = manager.get_or_create(*args, **key_fields)
<span class="linenos">118</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/manager.py"</span>, line <span class="hljs-number">154</span>, <span class="hljs-keyword">in</span> get_or_create
<span class="linenos">119</span>	    <span class="hljs-built_in">return</span> self.get_queryset().get_or_create(**kwargs)
<span class="linenos">120</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/query.py"</span>, line <span class="hljs-number">383</span>, <span class="hljs-keyword">in</span> get_or_create
<span class="linenos">121</span>	    obj.save(force_insert=True, using=self.db)
<span class="linenos">122</span>	TypeError: save() got an unexpected keyword argument <span class="hljs-string">'force_insert'</span>
<span class="linenos">123</span>	
<span class="linenos">124</span>	======================================================================
<span class="linenos">125</span>	ERROR: <span class="hljs-built_in">test</span>_delete_post (blogengine.tests.AdminTest)
<span class="linenos">126</span>	----------------------------------------------------------------------
<span class="linenos">127</span>	Traceback (most recent call last):
<span class="linenos">128</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">467</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_delete_post
<span class="linenos">129</span>	    category = CategoryFactory()
<span class="linenos">130</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">82</span>, <span class="hljs-keyword">in</span> __call__
<span class="linenos">131</span>	    <span class="hljs-built_in">return</span> cls.create(**kwargs)
<span class="linenos">132</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">585</span>, <span class="hljs-keyword">in</span> create
<span class="linenos">133</span>	    <span class="hljs-built_in">return</span> cls._generate(True, attrs)
<span class="linenos">134</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">510</span>, <span class="hljs-keyword">in</span> _generate
<span class="linenos">135</span>	    obj = cls._prepare(create, **attrs)
<span class="linenos">136</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">485</span>, <span class="hljs-keyword">in</span> _prepare
<span class="linenos">137</span>	    <span class="hljs-built_in">return</span> cls._create(model_class, *args, **kwargs)
<span class="linenos">138</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/django.py"</span>, line <span class="hljs-number">151</span>, <span class="hljs-keyword">in</span> _create
<span class="linenos">139</span>	    <span class="hljs-built_in">return</span> cls._get_or_create(model_class, *args, **kwargs)
<span class="linenos">140</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/django.py"</span>, line <span class="hljs-number">142</span>, <span class="hljs-keyword">in</span> _get_or_create
<span class="linenos">141</span>	    obj, _created = manager.get_or_create(*args, **key_fields)
<span class="linenos">142</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/manager.py"</span>, line <span class="hljs-number">154</span>, <span class="hljs-keyword">in</span> get_or_create
<span class="linenos">143</span>	    <span class="hljs-built_in">return</span> self.get_queryset().get_or_create(**kwargs)
<span class="linenos">144</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/query.py"</span>, line <span class="hljs-number">383</span>, <span class="hljs-keyword">in</span> get_or_create
<span class="linenos">145</span>	    obj.save(force_insert=True, using=self.db)
<span class="linenos">146</span>	TypeError: save() got an unexpected keyword argument <span class="hljs-string">'force_insert'</span>
<span class="linenos">147</span>	
<span class="linenos">148</span>	======================================================================
<span class="linenos">149</span>	ERROR: <span class="hljs-built_in">test</span>_edit_category (blogengine.tests.AdminTest)
<span class="linenos">150</span>	----------------------------------------------------------------------
<span class="linenos">151</span>	Traceback (most recent call last):
<span class="linenos">152</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">221</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_edit_category
<span class="linenos">153</span>	    category = CategoryFactory()
<span class="linenos">154</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">82</span>, <span class="hljs-keyword">in</span> __call__
<span class="linenos">155</span>	    <span class="hljs-built_in">return</span> cls.create(**kwargs)
<span class="linenos">156</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">585</span>, <span class="hljs-keyword">in</span> create
<span class="linenos">157</span>	    <span class="hljs-built_in">return</span> cls._generate(True, attrs)
<span class="linenos">158</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">510</span>, <span class="hljs-keyword">in</span> _generate
<span class="linenos">159</span>	    obj = cls._prepare(create, **attrs)
<span class="linenos">160</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">485</span>, <span class="hljs-keyword">in</span> _prepare
<span class="linenos">161</span>	    <span class="hljs-built_in">return</span> cls._create(model_class, *args, **kwargs)
<span class="linenos">162</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/django.py"</span>, line <span class="hljs-number">151</span>, <span class="hljs-keyword">in</span> _create
<span class="linenos">163</span>	    <span class="hljs-built_in">return</span> cls._get_or_create(model_class, *args, **kwargs)
<span class="linenos">164</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/django.py"</span>, line <span class="hljs-number">142</span>, <span class="hljs-keyword">in</span> _get_or_create
<span class="linenos">165</span>	    obj, _created = manager.get_or_create(*args, **key_fields)
<span class="linenos">166</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/manager.py"</span>, line <span class="hljs-number">154</span>, <span class="hljs-keyword">in</span> get_or_create
<span class="linenos">167</span>	    <span class="hljs-built_in">return</span> self.get_queryset().get_or_create(**kwargs)
<span class="linenos">168</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/query.py"</span>, line <span class="hljs-number">383</span>, <span class="hljs-keyword">in</span> get_or_create
<span class="linenos">169</span>	    obj.save(force_insert=True, using=self.db)
<span class="linenos">170</span>	TypeError: save() got an unexpected keyword argument <span class="hljs-string">'force_insert'</span>
<span class="linenos">171</span>	
<span class="linenos">172</span>	======================================================================
<span class="linenos">173</span>	ERROR: <span class="hljs-built_in">test</span>_edit_post (blogengine.tests.AdminTest)
<span class="linenos">174</span>	----------------------------------------------------------------------
<span class="linenos">175</span>	Traceback (most recent call last):
<span class="linenos">176</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">410</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_edit_post
<span class="linenos">177</span>	    category = CategoryFactory()
<span class="linenos">178</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">82</span>, <span class="hljs-keyword">in</span> __call__
<span class="linenos">179</span>	    <span class="hljs-built_in">return</span> cls.create(**kwargs)
<span class="linenos">180</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">585</span>, <span class="hljs-keyword">in</span> create
<span class="linenos">181</span>	    <span class="hljs-built_in">return</span> cls._generate(True, attrs)
<span class="linenos">182</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">510</span>, <span class="hljs-keyword">in</span> _generate
<span class="linenos">183</span>	    obj = cls._prepare(create, **attrs)
<span class="linenos">184</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">485</span>, <span class="hljs-keyword">in</span> _prepare
<span class="linenos">185</span>	    <span class="hljs-built_in">return</span> cls._create(model_class, *args, **kwargs)
<span class="linenos">186</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/django.py"</span>, line <span class="hljs-number">151</span>, <span class="hljs-keyword">in</span> _create
<span class="linenos">187</span>	    <span class="hljs-built_in">return</span> cls._get_or_create(model_class, *args, **kwargs)
<span class="linenos">188</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/django.py"</span>, line <span class="hljs-number">142</span>, <span class="hljs-keyword">in</span> _get_or_create
<span class="linenos">189</span>	    obj, _created = manager.get_or_create(*args, **key_fields)
<span class="linenos">190</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/manager.py"</span>, line <span class="hljs-number">154</span>, <span class="hljs-keyword">in</span> get_or_create
<span class="linenos">191</span>	    <span class="hljs-built_in">return</span> self.get_queryset().get_or_create(**kwargs)
<span class="linenos">192</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/query.py"</span>, line <span class="hljs-number">383</span>, <span class="hljs-keyword">in</span> get_or_create
<span class="linenos">193</span>	    obj.save(force_insert=True, using=self.db)
<span class="linenos">194</span>	TypeError: save() got an unexpected keyword argument <span class="hljs-string">'force_insert'</span>
<span class="linenos">195</span>	
<span class="linenos">196</span>	======================================================================
<span class="linenos">197</span>	ERROR: <span class="hljs-built_in">test</span>_all_post_feed (blogengine.tests.FeedTest)
<span class="linenos">198</span>	----------------------------------------------------------------------
<span class="linenos">199</span>	Traceback (most recent call last):
<span class="linenos">200</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">810</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_all_post_feed
<span class="linenos">201</span>	    category = CategoryFactory()
<span class="linenos">202</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">82</span>, <span class="hljs-keyword">in</span> __call__
<span class="linenos">203</span>	    <span class="hljs-built_in">return</span> cls.create(**kwargs)
<span class="linenos">204</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">585</span>, <span class="hljs-keyword">in</span> create
<span class="linenos">205</span>	    <span class="hljs-built_in">return</span> cls._generate(True, attrs)
<span class="linenos">206</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">510</span>, <span class="hljs-keyword">in</span> _generate
<span class="linenos">207</span>	    obj = cls._prepare(create, **attrs)
<span class="linenos">208</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">485</span>, <span class="hljs-keyword">in</span> _prepare
<span class="linenos">209</span>	    <span class="hljs-built_in">return</span> cls._create(model_class, *args, **kwargs)
<span class="linenos">210</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/django.py"</span>, line <span class="hljs-number">151</span>, <span class="hljs-keyword">in</span> _create
<span class="linenos">211</span>	    <span class="hljs-built_in">return</span> cls._get_or_create(model_class, *args, **kwargs)
<span class="linenos">212</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/django.py"</span>, line <span class="hljs-number">142</span>, <span class="hljs-keyword">in</span> _get_or_create
<span class="linenos">213</span>	    obj, _created = manager.get_or_create(*args, **key_fields)
<span class="linenos">214</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/manager.py"</span>, line <span class="hljs-number">154</span>, <span class="hljs-keyword">in</span> get_or_create
<span class="linenos">215</span>	    <span class="hljs-built_in">return</span> self.get_queryset().get_or_create(**kwargs)
<span class="linenos">216</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/query.py"</span>, line <span class="hljs-number">383</span>, <span class="hljs-keyword">in</span> get_or_create
<span class="linenos">217</span>	    obj.save(force_insert=True, using=self.db)
<span class="linenos">218</span>	TypeError: save() got an unexpected keyword argument <span class="hljs-string">'force_insert'</span>
<span class="linenos">219</span>	
<span class="linenos">220</span>	======================================================================
<span class="linenos">221</span>	ERROR: <span class="hljs-built_in">test</span>_category_page (blogengine.tests.PostViewTest)
<span class="linenos">222</span>	----------------------------------------------------------------------
<span class="linenos">223</span>	Traceback (most recent call last):
<span class="linenos">224</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">640</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_category_page
<span class="linenos">225</span>	    category = CategoryFactory()
<span class="linenos">226</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">82</span>, <span class="hljs-keyword">in</span> __call__
<span class="linenos">227</span>	    <span class="hljs-built_in">return</span> cls.create(**kwargs)
<span class="linenos">228</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">585</span>, <span class="hljs-keyword">in</span> create
<span class="linenos">229</span>	    <span class="hljs-built_in">return</span> cls._generate(True, attrs)
<span class="linenos">230</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">510</span>, <span class="hljs-keyword">in</span> _generate
<span class="linenos">231</span>	    obj = cls._prepare(create, **attrs)
<span class="linenos">232</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">485</span>, <span class="hljs-keyword">in</span> _prepare
<span class="linenos">233</span>	    <span class="hljs-built_in">return</span> cls._create(model_class, *args, **kwargs)
<span class="linenos">234</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/django.py"</span>, line <span class="hljs-number">151</span>, <span class="hljs-keyword">in</span> _create
<span class="linenos">235</span>	    <span class="hljs-built_in">return</span> cls._get_or_create(model_class, *args, **kwargs)
<span class="linenos">236</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/django.py"</span>, line <span class="hljs-number">142</span>, <span class="hljs-keyword">in</span> _get_or_create
<span class="linenos">237</span>	    obj, _created = manager.get_or_create(*args, **key_fields)
<span class="linenos">238</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/manager.py"</span>, line <span class="hljs-number">154</span>, <span class="hljs-keyword">in</span> get_or_create
<span class="linenos">239</span>	    <span class="hljs-built_in">return</span> self.get_queryset().get_or_create(**kwargs)
<span class="linenos">240</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/query.py"</span>, line <span class="hljs-number">383</span>, <span class="hljs-keyword">in</span> get_or_create
<span class="linenos">241</span>	    obj.save(force_insert=True, using=self.db)
<span class="linenos">242</span>	TypeError: save() got an unexpected keyword argument <span class="hljs-string">'force_insert'</span>
<span class="linenos">243</span>	
<span class="linenos">244</span>	======================================================================
<span class="linenos">245</span>	ERROR: <span class="hljs-built_in">test</span>_clear_cache (blogengine.tests.PostViewTest)
<span class="linenos">246</span>	----------------------------------------------------------------------
<span class="linenos">247</span>	Traceback (most recent call last):
<span class="linenos">248</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">753</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_clear_cache
<span class="linenos">249</span>	    category = CategoryFactory()
<span class="linenos">250</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">82</span>, <span class="hljs-keyword">in</span> __call__
<span class="linenos">251</span>	    <span class="hljs-built_in">return</span> cls.create(**kwargs)
<span class="linenos">252</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">585</span>, <span class="hljs-keyword">in</span> create
<span class="linenos">253</span>	    <span class="hljs-built_in">return</span> cls._generate(True, attrs)
<span class="linenos">254</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">510</span>, <span class="hljs-keyword">in</span> _generate
<span class="linenos">255</span>	    obj = cls._prepare(create, **attrs)
<span class="linenos">256</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">485</span>, <span class="hljs-keyword">in</span> _prepare
<span class="linenos">257</span>	    <span class="hljs-built_in">return</span> cls._create(model_class, *args, **kwargs)
<span class="linenos">258</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/django.py"</span>, line <span class="hljs-number">151</span>, <span class="hljs-keyword">in</span> _create
<span class="linenos">259</span>	    <span class="hljs-built_in">return</span> cls._get_or_create(model_class, *args, **kwargs)
<span class="linenos">260</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/django.py"</span>, line <span class="hljs-number">142</span>, <span class="hljs-keyword">in</span> _get_or_create
<span class="linenos">261</span>	    obj, _created = manager.get_or_create(*args, **key_fields)
<span class="linenos">262</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/manager.py"</span>, line <span class="hljs-number">154</span>, <span class="hljs-keyword">in</span> get_or_create
<span class="linenos">263</span>	    <span class="hljs-built_in">return</span> self.get_queryset().get_or_create(**kwargs)
<span class="linenos">264</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/query.py"</span>, line <span class="hljs-number">383</span>, <span class="hljs-keyword">in</span> get_or_create
<span class="linenos">265</span>	    obj.save(force_insert=True, using=self.db)
<span class="linenos">266</span>	TypeError: save() got an unexpected keyword argument <span class="hljs-string">'force_insert'</span>
<span class="linenos">267</span>	
<span class="linenos">268</span>	======================================================================
<span class="linenos">269</span>	ERROR: <span class="hljs-built_in">test</span>_index (blogengine.tests.PostViewTest)
<span class="linenos">270</span>	----------------------------------------------------------------------
<span class="linenos">271</span>	Traceback (most recent call last):
<span class="linenos">272</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">518</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_index
<span class="linenos">273</span>	    category = CategoryFactory()
<span class="linenos">274</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">82</span>, <span class="hljs-keyword">in</span> __call__
<span class="linenos">275</span>	    <span class="hljs-built_in">return</span> cls.create(**kwargs)
<span class="linenos">276</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">585</span>, <span class="hljs-keyword">in</span> create
<span class="linenos">277</span>	    <span class="hljs-built_in">return</span> cls._generate(True, attrs)
<span class="linenos">278</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">510</span>, <span class="hljs-keyword">in</span> _generate
<span class="linenos">279</span>	    obj = cls._prepare(create, **attrs)
<span class="linenos">280</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">485</span>, <span class="hljs-keyword">in</span> _prepare
<span class="linenos">281</span>	    <span class="hljs-built_in">return</span> cls._create(model_class, *args, **kwargs)
<span class="linenos">282</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/django.py"</span>, line <span class="hljs-number">151</span>, <span class="hljs-keyword">in</span> _create
<span class="linenos">283</span>	    <span class="hljs-built_in">return</span> cls._get_or_create(model_class, *args, **kwargs)
<span class="linenos">284</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/django.py"</span>, line <span class="hljs-number">142</span>, <span class="hljs-keyword">in</span> _get_or_create
<span class="linenos">285</span>	
<span class="linenos">286</span>	
<span class="linenos">287</span>	    obj, _created = manager.get_or_create(*args, **key_fields)
<span class="linenos">288</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/manager.py"</span>, line <span class="hljs-number">154</span>, <span class="hljs-keyword">in</span> get_or_create
<span class="linenos">289</span>	    <span class="hljs-built_in">return</span> self.get_queryset().get_or_create(**kwargs)
<span class="linenos">290</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/query.py"</span>, line <span class="hljs-number">383</span>, <span class="hljs-keyword">in</span> get_or_create
<span class="linenos">291</span>	    obj.save(force_insert=True, using=self.db)
<span class="linenos">292</span>	TypeError: save() got an unexpected keyword argument <span class="hljs-string">'force_insert'</span>
<span class="linenos">293</span>	
<span class="linenos">294</span>	======================================================================
<span class="linenos">295</span>	ERROR: <span class="hljs-built_in">test</span>_post_page (blogengine.tests.PostViewTest)
<span class="linenos">296</span>	----------------------------------------------------------------------
<span class="linenos">297</span>	Traceback (most recent call last):
<span class="linenos">298</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">576</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_post_page
<span class="linenos">299</span>	    category = CategoryFactory()
<span class="linenos">300</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">82</span>, <span class="hljs-keyword">in</span> __call__
<span class="linenos">301</span>	    <span class="hljs-built_in">return</span> cls.create(**kwargs)
<span class="linenos">302</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">585</span>, <span class="hljs-keyword">in</span> create
<span class="linenos">303</span>	    <span class="hljs-built_in">return</span> cls._generate(True, attrs)
<span class="linenos">304</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">510</span>, <span class="hljs-keyword">in</span> _generate
<span class="linenos">305</span>	    obj = cls._prepare(create, **attrs)
<span class="linenos">306</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/base.py"</span>, line <span class="hljs-number">485</span>, <span class="hljs-keyword">in</span> _prepare
<span class="linenos">307</span>	    <span class="hljs-built_in">return</span> cls._create(model_class, *args, **kwargs)
<span class="linenos">308</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/django.py"</span>, line <span class="hljs-number">151</span>, <span class="hljs-keyword">in</span> _create
<span class="linenos">309</span>	
<span class="linenos">310</span>	
<span class="linenos">311</span>	    <span class="hljs-built_in">return</span> cls._get_or_create(model_class, *args, **kwargs)
<span class="linenos">312</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/factory/django.py"</span>, line <span class="hljs-number">142</span>, <span class="hljs-keyword">in</span> _get_or_create
<span class="linenos">313</span>	    obj, _created = manager.get_or_create(*args, **key_fields)
<span class="linenos">314</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/manager.py"</span>, line <span class="hljs-number">154</span>, <span class="hljs-keyword">in</span> get_or_create
<span class="linenos">315</span>	    <span class="hljs-built_in">return</span> self.get_queryset().get_or_create(**kwargs)
<span class="linenos">316</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/query.py"</span>, line <span class="hljs-number">383</span>, <span class="hljs-keyword">in</span> get_or_create
<span class="linenos">317</span>	    obj.save(force_insert=True, using=self.db)
<span class="linenos">318</span>	TypeError: save() got an unexpected keyword argument <span class="hljs-string">'force_insert'</span>
<span class="linenos">319</span>	
<span class="linenos">320</span>	----------------------------------------------------------------------
<span class="linenos">321</span>	Ran <span class="hljs-number">24</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">5.162</span>s
<span class="linenos">322</span>	
<span class="linenos">323</span>	FAILED (errors=<span class="hljs-number">13</span>)
<span class="linenos">324</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>Thankfully, this is easy to fix. We just need to amend the custom <code>save()</code> method of the <code>Category</code> model:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">save</span><span class="hljs-params">(self, *args, **kwargs)</span>:</span>
<span class="linenos">2</span>	        <span class="hljs-keyword">if</span> <span class="hljs-keyword">not</span> self.slug:
<span class="linenos">3</span>	            self.slug = slugify(unicode(self.name))
<span class="linenos">4</span>	        super(Category, self).save(*args, **kwargs)</code></pre>
<p>That should resolve the issue:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py jenkins
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	........................
<span class="linenos">4</span>	----------------------------------------------------------------------
<span class="linenos">5</span>	Ran <span class="hljs-number">24</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">7.749</span>s
<span class="linenos">6</span>	
<span class="linenos">7</span>	OK
<span class="linenos">8</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>Let’s commit again:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add blogengine/
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Category now uses Factory Boy'</span></code></pre>
<p>Now let’s do the same thing for tags:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">TagFactory</span><span class="hljs-params">(factory.django.DjangoModelFactory)</span>:</span>
<span class="linenos">2</span>	    <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Meta</span>:</span>
<span class="linenos">3</span>	        model = Tag
<span class="linenos">4</span>	        django_get_or_create = (
<span class="linenos">5</span>	            <span class="hljs-string">'name'</span>,
<span class="linenos">6</span>	            <span class="hljs-string">'description'</span>,
<span class="linenos">7</span>	            <span class="hljs-string">'slug'</span>
<span class="linenos">8</span>	        )
<span class="linenos">9</span>	
<span class="linenos">10</span>	    name = <span class="hljs-string">'python'</span>
<span class="linenos">11</span>	    description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">12</span>	    slug = <span class="hljs-string">'python'</span></code></pre>
<p>And replace the sections where we create new <code>Tag</code> objects:</p>
<pre><code class="hljs lang-python singleline">        tag = TagFactory()</code></pre>
<p>Note that some tags have different values. We can easily pass different values to our <code>TagFactory()</code> to override the default values:</p>
<pre><code class="hljs lang-python singleline">        tag = TagFactory(name=<span class="hljs-string">'perl'</span>, description=<span class="hljs-string">'The Perl programming language'</span>)</code></pre>
<p>The <code>Tag</code> model has the same issue as the <code>Category</code> one did, so let’s fix that:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">save</span><span class="hljs-params">(self, *args, **kwargs)</span>:</span>
<span class="linenos">2</span>	        <span class="hljs-keyword">if</span> <span class="hljs-keyword">not</span> self.slug:
<span class="linenos">3</span>	            self.slug = slugify(unicode(self.name))
<span class="linenos">4</span>	        super(Tag, self).save(*args, **kwargs)</code></pre>
<p>We run our tests again:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py <span class="hljs-built_in">test</span> blogengine/
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	........................
<span class="linenos">4</span>	----------------------------------------------------------------------
<span class="linenos">5</span>	Ran <span class="hljs-number">24</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">7.153</span>s
<span class="linenos">6</span>	
<span class="linenos">7</span>	OK
<span class="linenos">8</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>Time to commit again:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add blogengine/
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Now use Factory Boy for tags'</span></code></pre>
<p>Next we’ll create a factory for adding users. Note that the factory name doesn’t have to match the object name, so you can create factories for different types of users. Here we create a factory for authors - you could, for instance, create a separate factory for subscribers if you wanted:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">AuthorFactory</span><span class="hljs-params">(factory.django.DjangoModelFactory)</span>:</span>
<span class="linenos">2</span>	    <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Meta</span>:</span>
<span class="linenos">3</span>	        model = User
<span class="linenos">4</span>	        django_get_or_create = (<span class="hljs-string">'username'</span>,<span class="hljs-string">'email'</span>, <span class="hljs-string">'password'</span>,)
<span class="linenos">5</span>	
<span class="linenos">6</span>	    username = <span class="hljs-string">'testuser'</span>
<span class="linenos">7</span>	    email = <span class="hljs-string">'user@example.com'</span>
<span class="linenos">8</span>	    password = <span class="hljs-string">'password'</span></code></pre>
<p>And as before, replace those sections where we create users with the following:</p>
<pre><code class="hljs lang-python singleline">        author = AuthorFactory()</code></pre>
<p>Run the tests again:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py <span class="hljs-built_in">test</span> blogengine
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	........................
<span class="linenos">4</span>	----------------------------------------------------------------------
<span class="linenos">5</span>	Ran <span class="hljs-number">24</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">5.808</span>s
<span class="linenos">6</span>	
<span class="linenos">7</span>	OK
<span class="linenos">8</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>We commit our changes:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add blogengine/
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Now use Factory Boy for creating authors'</span></code></pre>
<p>Now we’ll create a flat page factory:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	
<span class="linenos">2</span>	
<span class="linenos">3</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">FlatPageFactory</span><span class="hljs-params">(factory.django.DjangoModelFactory)</span>:</span>
<span class="linenos">4</span>	    <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Meta</span>:</span>
<span class="linenos">5</span>	        model = FlatPage
<span class="linenos">6</span>	        django_get_or_create = (
<span class="linenos">7</span>	            <span class="hljs-string">'url'</span>,
<span class="linenos">8</span>	            <span class="hljs-string">'title'</span>,
<span class="linenos">9</span>	            <span class="hljs-string">'content'</span>
<span class="linenos">10</span>	        )
<span class="linenos">11</span>	
<span class="linenos">12</span>	    url = <span class="hljs-string">'/about/'</span>
<span class="linenos">13</span>	    title = <span class="hljs-string">'About me'</span>
<span class="linenos">14</span>	    content = <span class="hljs-string">'All about me'</span></code></pre>
<p>And use it for our flat page test:</p>
<pre><code class="hljs lang-python singleline">        page = FlatPageFactory()</code></pre>
<p>Check the tests pass:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py <span class="hljs-built_in">test</span> blogengine
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	........................
<span class="linenos">4</span>	----------------------------------------------------------------------
<span class="linenos">5</span>	Ran <span class="hljs-number">24</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">5.796</span>s
<span class="linenos">6</span>	
<span class="linenos">7</span>	OK
<span class="linenos">8</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>And commit again:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add blogengine/
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Now use Factory Boy for flat page test'</span></code></pre>
<p>Now we’ll create a final factory for posts:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">PostFactory</span><span class="hljs-params">(factory.django.DjangoModelFactory)</span>:</span>
<span class="linenos">2</span>	    <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Meta</span>:</span>
<span class="linenos">3</span>	        model = Post
<span class="linenos">4</span>	        django_get_or_create = (
<span class="linenos">5</span>	            <span class="hljs-string">'title'</span>,
<span class="linenos">6</span>	            <span class="hljs-string">'text'</span>,
<span class="linenos">7</span>	            <span class="hljs-string">'slug'</span>,
<span class="linenos">8</span>	            <span class="hljs-string">'pub_date'</span>
<span class="linenos">9</span>	        )
<span class="linenos">10</span>	
<span class="linenos">11</span>	    title = <span class="hljs-string">'My first post'</span>
<span class="linenos">12</span>	    text = <span class="hljs-string">'This is my first blog post'</span>
<span class="linenos">13</span>	    slug = <span class="hljs-string">'my-first-post'</span>
<span class="linenos">14</span>	    pub_date = timezone.now()
<span class="linenos">15</span>	    author = factory.SubFactory(AuthorFactory)
<span class="linenos">16</span>	    site = factory.SubFactory(SiteFactory)
<span class="linenos">17</span>	    category = factory.SubFactory(CategoryFactory)</code></pre>
<p>This factory is a little bit different. Because our <code>Post</code> model depends on several others, we need to be able to create those additional objects on demand. By designating them as subfactories, we can easily create the associated objects for our <code>Post</code> object.</p>
<p>That means that not only can we get rid of our <code>Post()</code> calls, but we can also get rid of the factory calls to create the associated objects for <code>Post</code> models. Again, I’ll leave actually doing this as an exercise for the reader, but you can always refer to the GitHub repository if you’re not too sure.</p>
<p>Make sure your tests still pass, then commit the changes:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add blogengine/
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Now use Factory Boy for testing posts'</span></code></pre>
<p>Using Factory Boy made a big difference to the size of the test file - I was able to cut it down by over 200 lines of code. As your application gets bigger, it gets harder to maintain, so do what you can to keep the size down.</p>
<h1 id="additional-rss-feeds">Additional RSS feeds</h1>
<p>Now, let’s implement our additional RSS feeds. First, we’ll write a test for the category feed. Add this to the <code>FeedTest</code> class:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_category_feed</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">2</span>	        <span class="hljs-comment"># Create a post</span>
<span class="linenos">3</span>	        post = PostFactory(text=<span class="hljs-string">'This is my *first* blog post'</span>)
<span class="linenos">4</span>	
<span class="linenos">5</span>	        <span class="hljs-comment"># Create another post in a different category</span>
<span class="linenos">6</span>	        category = CategoryFactory(name=<span class="hljs-string">'perl'</span>, description=<span class="hljs-string">'The Perl programming language'</span>, slug=<span class="hljs-string">'perl'</span>)
<span class="linenos">7</span>	        post2 = PostFactory(text=<span class="hljs-string">'This is my *second* blog post'</span>, title=<span class="hljs-string">'My second post'</span>, slug=<span class="hljs-string">'my-second-post'</span>, category=category)
<span class="linenos">8</span>	
<span class="linenos">9</span>	        <span class="hljs-comment"># Fetch the feed</span>
<span class="linenos">10</span>	        response = self.client.get(<span class="hljs-string">'/feeds/posts/category/python/'</span>)
<span class="linenos">11</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">12</span>	
<span class="linenos">13</span>	        <span class="hljs-comment"># Parse the feed</span>
<span class="linenos">14</span>	        feed = feedparser.parse(response.content)
<span class="linenos">15</span>	
<span class="linenos">16</span>	        <span class="hljs-comment"># Check length</span>
<span class="linenos">17</span>	        self.assertEquals(len(feed.entries), <span class="hljs-number">1</span>)
<span class="linenos">18</span>	
<span class="linenos">19</span>	        <span class="hljs-comment"># Check post retrieved is the correct one</span>
<span class="linenos">20</span>	        feed_post = feed.entries[<span class="hljs-number">0</span>]
<span class="linenos">21</span>	        self.assertEquals(feed_post.title, post.title)
<span class="linenos">22</span>	        self.assertTrue(<span class="hljs-string">'This is my &lt;em&gt;first&lt;/em&gt; blog post'</span> <span class="hljs-keyword">in</span> feed_post.description)
<span class="linenos">23</span>	
<span class="linenos">24</span>	        <span class="hljs-comment"># Check other post is not in this feed</span>
<span class="linenos">25</span>	        self.assertTrue(<span class="hljs-string">'This is my &lt;em&gt;second&lt;/em&gt; blog post'</span> <span class="hljs-keyword">not</span> <span class="hljs-keyword">in</span> response.content)</code></pre>
<p>Here we create two posts in different categories (note that we create a new category and override the post category for it). We then fetch <code>/feeds/posts/category/python/</code> and assert that it contains only one post, with the content of the first post and not the content of the second.</p>
<p>Run the tests and they should fail:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py <span class="hljs-built_in">test</span> blogengine
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	................F........
<span class="linenos">4</span>	======================================================================
<span class="linenos">5</span>	FAIL: <span class="hljs-built_in">test</span>_category_feed (blogengine.tests.FeedTest)
<span class="linenos">6</span>	----------------------------------------------------------------------
<span class="linenos">7</span>	Traceback (most recent call last):
<span class="linenos">8</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">716</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_category_feed
<span class="linenos">9</span>	    self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">10</span>	AssertionError: <span class="hljs-number">404</span> != <span class="hljs-number">200</span>
<span class="linenos">11</span>	
<span class="linenos">12</span>	----------------------------------------------------------------------
<span class="linenos">13</span>	Ran <span class="hljs-number">25</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">5.804</span>s
<span class="linenos">14</span>	
<span class="linenos">15</span>	FAILED (failures=<span class="hljs-number">1</span>)
<span class="linenos">16</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>Because we haven’t yet implemented that route, we get a 404 error. So let’s create a route for this:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-keyword">from</span> django.conf.urls <span class="hljs-keyword">import</span> patterns, url
<span class="linenos">2</span>	<span class="hljs-keyword">from</span> django.views.generic <span class="hljs-keyword">import</span> ListView, DetailView
<span class="linenos">3</span>	<span class="hljs-keyword">from</span> blogengine.models <span class="hljs-keyword">import</span> Post, Category, Tag
<span class="linenos">4</span>	<span class="hljs-keyword">from</span> blogengine.views <span class="hljs-keyword">import</span> CategoryListView, TagListView, PostsFeed, CategoryPostsFeed
<span class="linenos">5</span>	
<span class="linenos">6</span>	urlpatterns = patterns(<span class="hljs-string">''</span>,
<span class="linenos">7</span>	    <span class="hljs-comment"># Index</span>
<span class="linenos">8</span>	    url(<span class="hljs-string">r'^(?P&lt;page&gt;\d+)?/?$'</span>, ListView.as_view(
<span class="linenos">9</span>	        model=Post,
<span class="linenos">10</span>	        paginate_by=<span class="hljs-number">5</span>,
<span class="linenos">11</span>	        )),
<span class="linenos">12</span>	
<span class="linenos">13</span>	    <span class="hljs-comment"># Individual posts</span>
<span class="linenos">14</span>	    url(<span class="hljs-string">r'^(?P&lt;pub_date__year&gt;\d{4})/(?P&lt;pub_date__month&gt;\d{1,2})/(?P&lt;slug&gt;[a-zA-Z0-9-]+)/?$'</span>, DetailView.as_view(
<span class="linenos">15</span>	        model=Post,
<span class="linenos">16</span>	        )),
<span class="linenos">17</span>	
<span class="linenos">18</span>	    <span class="hljs-comment"># Categories</span>
<span class="linenos">19</span>	    url(<span class="hljs-string">r'^category/(?P&lt;slug&gt;[a-zA-Z0-9-]+)/?$'</span>, CategoryListView.as_view(
<span class="linenos">20</span>	        paginate_by=<span class="hljs-number">5</span>,
<span class="linenos">21</span>	        model=Category,
<span class="linenos">22</span>	        )),
<span class="linenos">23</span>	
<span class="linenos">24</span>	    <span class="hljs-comment"># Tags</span>
<span class="linenos">25</span>	    url(<span class="hljs-string">r'^tag/(?P&lt;slug&gt;[a-zA-Z0-9-]+)/?$'</span>, TagListView.as_view(
<span class="linenos">26</span>	        paginate_by=<span class="hljs-number">5</span>,
<span class="linenos">27</span>	        model=Tag,
<span class="linenos">28</span>	        )),
<span class="linenos">29</span>	
<span class="linenos">30</span>	    <span class="hljs-comment"># Post RSS feed</span>
<span class="linenos">31</span>	    url(<span class="hljs-string">r'^feeds/posts/$'</span>, PostsFeed()),
<span class="linenos">32</span>	
<span class="linenos">33</span>	    <span class="hljs-comment"># Category RSS feed</span>
<span class="linenos">34</span>	    url(<span class="hljs-string">r'^feeds/posts/category/(?P&lt;slug&gt;[a-zA-Z0-9-]+)/?$'</span>, CategoryPostsFeed()),
<span class="linenos">35</span>	)</code></pre>
<p>Note that the category RSS feed route is similar to the post RSS feed route, but accepts a <code>slug</code> parameter. We will use this to pass through the slug for the category in question. Also note we import the <code>CategoryPostsFeed</code> view. Now, we need to create that view. Fortunately, because it’s written as a Python class, we can extend the existing <code>PostsFeed</code> class. Open up your views file and amend it to look like this:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-keyword">from</span> django.shortcuts <span class="hljs-keyword">import</span> get_object_or_404
<span class="linenos">2</span>	<span class="hljs-keyword">from</span> django.views.generic <span class="hljs-keyword">import</span> ListView
<span class="linenos">3</span>	<span class="hljs-keyword">from</span> blogengine.models <span class="hljs-keyword">import</span> Category, Post, Tag
<span class="linenos">4</span>	<span class="hljs-keyword">from</span> django.contrib.syndication.views <span class="hljs-keyword">import</span> Feed
<span class="linenos">5</span>	<span class="hljs-keyword">from</span> django.utils.encoding <span class="hljs-keyword">import</span> force_unicode
<span class="linenos">6</span>	<span class="hljs-keyword">from</span> django.utils.safestring <span class="hljs-keyword">import</span> mark_safe
<span class="linenos">7</span>	<span class="hljs-keyword">import</span> markdown2
<span class="linenos">8</span>	
<span class="linenos">9</span>	<span class="hljs-comment"># Create your views here.</span>
<span class="linenos">10</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">CategoryListView</span><span class="hljs-params">(ListView)</span>:</span>
<span class="linenos">11</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">get_queryset</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">12</span>	        slug = self.kwargs[<span class="hljs-string">'slug'</span>]
<span class="linenos">13</span>	        <span class="hljs-keyword">try</span>:
<span class="linenos">14</span>	            category = Category.objects.get(slug=slug)
<span class="linenos">15</span>	            <span class="hljs-keyword">return</span> Post.objects.filter(category=category)
<span class="linenos">16</span>	        <span class="hljs-keyword">except</span> Category.DoesNotExist:
<span class="linenos">17</span>	            <span class="hljs-keyword">return</span> Post.objects.none()
<span class="linenos">18</span>	
<span class="linenos">19</span>	
<span class="linenos">20</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">TagListView</span><span class="hljs-params">(ListView)</span>:</span>
<span class="linenos">21</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">get_queryset</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">22</span>	        slug = self.kwargs[<span class="hljs-string">'slug'</span>]
<span class="linenos">23</span>	        <span class="hljs-keyword">try</span>:
<span class="linenos">24</span>	            tag = Tag.objects.get(slug=slug)
<span class="linenos">25</span>	            <span class="hljs-keyword">return</span> tag.post_set.all()
<span class="linenos">26</span>	        <span class="hljs-keyword">except</span> Tag.DoesNotExist:
<span class="linenos">27</span>	            <span class="hljs-keyword">return</span> Post.objects.none()
<span class="linenos">28</span>	
<span class="linenos">29</span>	
<span class="linenos">30</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">PostsFeed</span><span class="hljs-params">(Feed)</span>:</span>
<span class="linenos">31</span>	    title = <span class="hljs-string">"RSS feed - posts"</span>
<span class="linenos">32</span>	    description = <span class="hljs-string">"RSS feed - blog posts"</span>
<span class="linenos">33</span>	    link = <span class="hljs-string">'/'</span>
<span class="linenos">34</span>	
<span class="linenos">35</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">items</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">36</span>	        <span class="hljs-keyword">return</span> Post.objects.order_by(<span class="hljs-string">'-pub_date'</span>)
<span class="linenos">37</span>	
<span class="linenos">38</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">item_title</span><span class="hljs-params">(self, item)</span>:</span>
<span class="linenos">39</span>	        <span class="hljs-keyword">return</span> item.title
<span class="linenos">40</span>	
<span class="linenos">41</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">item_description</span><span class="hljs-params">(self, item)</span>:</span>
<span class="linenos">42</span>	        extras = [<span class="hljs-string">"fenced-code-blocks"</span>]
<span class="linenos">43</span>	        content = mark_safe(markdown2.markdown(force_unicode(item.text),
<span class="linenos">44</span>	                                               extras = extras))
<span class="linenos">45</span>	        <span class="hljs-keyword">return</span> content
<span class="linenos">46</span>	
<span class="linenos">47</span>	
<span class="linenos">48</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">CategoryPostsFeed</span><span class="hljs-params">(PostsFeed)</span>:</span>
<span class="linenos">49</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">get_object</span><span class="hljs-params">(self, request, slug)</span>:</span>
<span class="linenos">50</span>	        <span class="hljs-keyword">return</span> get_object_or_404(Category, slug=slug)
<span class="linenos">51</span>	
<span class="linenos">52</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">title</span><span class="hljs-params">(self, obj)</span>:</span>
<span class="linenos">53</span>	        <span class="hljs-keyword">return</span> <span class="hljs-string">"RSS feed - blog posts in category %s"</span> % obj.name
<span class="linenos">54</span>	
<span class="linenos">55</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">link</span><span class="hljs-params">(self, obj)</span>:</span>
<span class="linenos">56</span>	        <span class="hljs-keyword">return</span> obj.get_absolute_url()
<span class="linenos">57</span>	
<span class="linenos">58</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">description</span><span class="hljs-params">(self, obj)</span>:</span>
<span class="linenos">59</span>	        <span class="hljs-keyword">return</span> <span class="hljs-string">"RSS feed - blog posts in category %s"</span> % obj.name
<span class="linenos">60</span>	
<span class="linenos">61</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">items</span><span class="hljs-params">(self, obj)</span>:</span>
<span class="linenos">62</span>	        <span class="hljs-keyword">return</span> Post.objects.filter(category=obj).order_by(<span class="hljs-string">'-pub_date'</span>)</code></pre>
<p>Note that many of our fields don’t have to be explicitly defined as they are inherited from <code>PostsFeed</code>. We can’t hard-code the title, link or description because they depend on the category, so we instead define methods to return the appropriate text.</p>
<p>Also note <code>get_object()</code> - we define this so that we can ensure the category exists. If it doesn’t exist, then it returns a 404 error rather than showing an empty feed.</p>
<p>We also override <code>items()</code> to filter it to just those posts that are in the given category.</p>
<p>If you run the tests again, they should now pass:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py <span class="hljs-built_in">test</span> blogengine
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	.........................
<span class="linenos">4</span>	----------------------------------------------------------------------
<span class="linenos">5</span>	Ran <span class="hljs-number">25</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">5.867</span>s
<span class="linenos">6</span>	
<span class="linenos">7</span>	OK
<span class="linenos">8</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>Let’s commit our changes:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add blogengine/
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Implemented category RSS feed'</span></code></pre>
<p>Now, we can get our category RSS feed, but how do we navigate to it? Let’s add a link to each category page that directs a user to its RSS feed. To do so, we’ll need to create a new template for category pages. First, let’s add some code to our tests to ensure that the right template is used at all times. Add the following to the end of the <code>test_index</code> method of <code>PostViewTest</code>:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	        <span class="hljs-comment"># Check the correct template was used</span>
<span class="linenos">2</span>	        self.assertTemplateUsed(response, <span class="hljs-string">'blogengine/post_list.html'</span>)</code></pre>
<p>Then, add this to the end of <code>test_post_page</code>:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	
<span class="linenos">2</span>	        <span class="hljs-comment"># Check the correct template was used</span>
<span class="linenos">3</span>	        self.assertTemplateUsed(response, <span class="hljs-string">'blogengine/post_detail.html'</span>)</code></pre>
<p>Finally, add this to the end of <code>test_category_page</code>:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	        <span class="hljs-comment"># Check the correct template was used</span>
<span class="linenos">2</span>	        self.assertTemplateUsed(response, <span class="hljs-string">'blogengine/category_post_list.html'</span>)</code></pre>
<p>These assertions confirm which template was used to generate which request.</p>
<p>Next, we head into our views file:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">CategoryListView</span><span class="hljs-params">(ListView)</span>:</span>
<span class="linenos">2</span>	    template_name = <span class="hljs-string">'blogengine/category_post_list.html'</span>
<span class="linenos">3</span>	
<span class="linenos">4</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">get_queryset</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">5</span>	        slug = self.kwargs[<span class="hljs-string">'slug'</span>]
<span class="linenos">6</span>	        <span class="hljs-keyword">try</span>:
<span class="linenos">7</span>	            category = Category.objects.get(slug=slug)
<span class="linenos">8</span>	            <span class="hljs-keyword">return</span> Post.objects.filter(category=category)
<span class="linenos">9</span>	        <span class="hljs-keyword">except</span> Category.DoesNotExist:
<span class="linenos">10</span>	            <span class="hljs-keyword">return</span> Post.objects.none()
<span class="linenos">11</span>	
<span class="linenos">12</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">get_context_data</span><span class="hljs-params">(self, **kwargs)</span>:</span>
<span class="linenos">13</span>	        context = super(CategoryListView, self).get_context_data(**kwargs)
<span class="linenos">14</span>	        slug = self.kwargs[<span class="hljs-string">'slug'</span>]
<span class="linenos">15</span>	        <span class="hljs-keyword">try</span>:
<span class="linenos">16</span>	            context[<span class="hljs-string">'category'</span>] = Category.objects.get(slug=slug)
<span class="linenos">17</span>	        <span class="hljs-keyword">except</span> Category.DoesNotExist:
<span class="linenos">18</span>	            context[<span class="hljs-string">'category'</span>] = <span class="hljs-keyword">None</span>
<span class="linenos">19</span>	        <span class="hljs-keyword">return</span> context</code></pre>
<p>Note that we first of all change the template used by this view. Then, we override <code>get_context_data</code> to add in additional data. What we’re doing is getting the slug that was passed through, looking up any category for which it is the slug, and returning it as additional context data. Using this method, you can easily add additional data that you may wish to render in your Django templates.</p>
<p>Finally, we create our new template:</p>
<pre><code class="hljs lang-django"><span class="linenos">1</span>	<span class="xml"></span><span class="hljs-template_tag">{% <span class="hljs-keyword">extends</span> "blogengine/includes/base.html" %}</span><span class="xml">
<span class="linenos">2</span>	
<span class="linenos">3</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">load</span> custom_markdown %}</span><span class="xml">
<span class="linenos">4</span>	
<span class="linenos">5</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">block</span> content %}</span><span class="xml">
<span class="linenos">6</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> object_list %}</span><span class="xml">
<span class="linenos">7</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">for</span> post <span class="hljs-keyword">in</span> object_list %}</span><span class="xml">
<span class="linenos">8</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"post col-md-12"</span>&gt;</span>
<span class="linenos">9</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">h1</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ post.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span></span><span class="hljs-variable">{{ post.title }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">h1</span>&gt;</span>
<span class="linenos">10</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">h3</span>&gt;</span></span><span class="hljs-variable">{{ post.pub_date }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">h3</span>&gt;</span>
<span class="linenos">11</span>	            </span><span class="hljs-variable">{{ post.text<span class="hljs-filter">|custom</span>_markdown }}</span><span class="xml">
<span class="linenos">12</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">13</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> post.category %}</span><span class="xml">
<span class="linenos">14</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"col-md-12"</span>&gt;</span>
<span class="linenos">15</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ post.category.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"label label-primary"</span>&gt;</span></span><span class="hljs-variable">{{ post.category.name }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">16</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">17</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">18</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> post.tags %}</span><span class="xml">
<span class="linenos">19</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"col-md-12"</span>&gt;</span>
<span class="linenos">20</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">for</span> tag <span class="hljs-keyword">in</span> post.tags.all %}</span><span class="xml">
<span class="linenos">21</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ tag.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"label label-success"</span>&gt;</span></span><span class="hljs-variable">{{ tag.name }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">22</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endfor</span> %}</span><span class="xml">
<span class="linenos">23</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">24</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">25</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endfor</span> %}</span><span class="xml">
<span class="linenos">26</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">else</span> %}</span><span class="xml">
<span class="linenos">27</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">p</span>&gt;</span>No posts found<span class="hljs-tag">&lt;/<span class="hljs-title">p</span>&gt;</span>
<span class="linenos">28</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">29</span>	
<span class="linenos">30</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">ul</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"pager"</span>&gt;</span>
<span class="linenos">31</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> page_obj.has_previous %}</span><span class="xml">
<span class="linenos">32</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">li</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"previous"</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/</span></span></span><span class="hljs-variable">{{ page_obj.previous_page_number }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">/"</span>&gt;</span>Previous Page<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">li</span>&gt;</span>
<span class="linenos">33</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">34</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> page_obj.has_next %}</span><span class="xml">
<span class="linenos">35</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">li</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"next"</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/</span></span></span><span class="hljs-variable">{{ page_obj.next_page_number }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">/"</span>&gt;</span>Next Page<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">li</span>&gt;</span>
<span class="linenos">36</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">37</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">ul</span>&gt;</span>
<span class="linenos">38</span>	
<span class="linenos">39</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/feeds/posts/category/</span></span></span><span class="hljs-variable">{{ category.slug }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">/"</span>&gt;</span>RSS feed for category </span><span class="hljs-variable">{{ category.name }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">40</span>	
<span class="linenos">41</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endblock</span> %}</span><span class="xml"></span></code></pre>
<p>Note that the category has been passed through to the template and is now accessible. If you run the tests, they should now pass:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py jenkins
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	.........................
<span class="linenos">4</span>	----------------------------------------------------------------------
<span class="linenos">5</span>	Ran <span class="hljs-number">25</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">7.232</span>s
<span class="linenos">6</span>	
<span class="linenos">7</span>	OK
<span class="linenos">8</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>With that done. we can commit our changes:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add templates/ blogengine/
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Added link to RSS feed from category page'</span></code></pre>
<p>Next up, let’s implement another RSS feed for tags. First, we’ll implement our test:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	      <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_tag_feed</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">2</span>	          <span class="hljs-comment"># Create a post</span>
<span class="linenos">3</span>	          post = PostFactory(text=<span class="hljs-string">'This is my *first* blog post'</span>)
<span class="linenos">4</span>	          tag = TagFactory()
<span class="linenos">5</span>	          post.tags.add(tag)
<span class="linenos">6</span>	          post.save()
<span class="linenos">7</span>	
<span class="linenos">8</span>	          <span class="hljs-comment"># Create another post with a different tag</span>
<span class="linenos">9</span>	          tag2 = TagFactory(name=<span class="hljs-string">'perl'</span>, description=<span class="hljs-string">'The Perl programming language'</span>, slug=<span class="hljs-string">'perl'</span>)
<span class="linenos">10</span>	          post2 = PostFactory(text=<span class="hljs-string">'This is my *second* blog post'</span>, title=<span class="hljs-string">'My second post'</span>, slug=<span class="hljs-string">'my-second-post'</span>)
<span class="linenos">11</span>	          post2.tags.add(tag2)
<span class="linenos">12</span>	          post2.save()
<span class="linenos">13</span>	
<span class="linenos">14</span>	          <span class="hljs-comment"># Fetch the feed</span>
<span class="linenos">15</span>	          response = self.client.get(<span class="hljs-string">'/feeds/posts/tag/python/'</span>)
<span class="linenos">16</span>	          self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">17</span>	
<span class="linenos">18</span>	          <span class="hljs-comment"># Parse the feed</span>
<span class="linenos">19</span>	          feed = feedparser.parse(response.content)
<span class="linenos">20</span>	
<span class="linenos">21</span>	          <span class="hljs-comment"># Check length</span>
<span class="linenos">22</span>	          self.assertEquals(len(feed.entries), <span class="hljs-number">1</span>)
<span class="linenos">23</span>	
<span class="linenos">24</span>	          <span class="hljs-comment"># Check post retrieved is the correct one</span>
<span class="linenos">25</span>	          feed_post = feed.entries[<span class="hljs-number">0</span>]
<span class="linenos">26</span>	          self.assertEquals(feed_post.title, post.title)
<span class="linenos">27</span>	          self.assertTrue(<span class="hljs-string">'This is my &lt;em&gt;first&lt;/em&gt; blog post'</span> <span class="hljs-keyword">in</span> feed_post.description)
<span class="linenos">28</span>	
<span class="linenos">29</span>	          <span class="hljs-comment"># Check other post is not in this feed</span>
<span class="linenos">30</span>	          self.assertTrue(<span class="hljs-string">'This is my &lt;em&gt;second&lt;/em&gt; blog post'</span> <span class="hljs-keyword">not</span> <span class="hljs-keyword">in</span> response.content)</code></pre>
<p>This is virtually identical to the test for the categroy feed, but we adjust it to work with the <code>Tag</code> attribute and change the URL. Let’s check that our test fails:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py <span class="hljs-built_in">test</span> blogengine
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	.................F........
<span class="linenos">4</span>	======================================================================
<span class="linenos">5</span>	FAIL: <span class="hljs-built_in">test</span>_tag_feed (blogengine.tests.FeedTest)
<span class="linenos">6</span>	----------------------------------------------------------------------
<span class="linenos">7</span>	Traceback (most recent call last):
<span class="linenos">8</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">757</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_tag_feed
<span class="linenos">9</span>	    self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">10</span>	AssertionError: <span class="hljs-number">404</span> != <span class="hljs-number">200</span>
<span class="linenos">11</span>	
<span class="linenos">12</span>	----------------------------------------------------------------------
<span class="linenos">13</span>	Ran <span class="hljs-number">26</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">5.760</span>s
<span class="linenos">14</span>	
<span class="linenos">15</span>	FAILED (failures=<span class="hljs-number">1</span>)
<span class="linenos">16</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>As before, we create a route for this:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-keyword">from</span> django.conf.urls <span class="hljs-keyword">import</span> patterns, url
<span class="linenos">2</span>	<span class="hljs-keyword">from</span> django.views.generic <span class="hljs-keyword">import</span> ListView, DetailView
<span class="linenos">3</span>	<span class="hljs-keyword">from</span> blogengine.models <span class="hljs-keyword">import</span> Post, Category, Tag
<span class="linenos">4</span>	<span class="hljs-keyword">from</span> blogengine.views <span class="hljs-keyword">import</span> CategoryListView, TagListView, PostsFeed, CategoryPostsFeed, TagPostsFeed
<span class="linenos">5</span>	
<span class="linenos">6</span>	urlpatterns = patterns(<span class="hljs-string">''</span>,
<span class="linenos">7</span>	    <span class="hljs-comment"># Index</span>
<span class="linenos">8</span>	    url(<span class="hljs-string">r'^(?P&lt;page&gt;\d+)?/?$'</span>, ListView.as_view(
<span class="linenos">9</span>	        model=Post,
<span class="linenos">10</span>	        paginate_by=<span class="hljs-number">5</span>,
<span class="linenos">11</span>	        )),
<span class="linenos">12</span>	
<span class="linenos">13</span>	    <span class="hljs-comment"># Individual posts</span>
<span class="linenos">14</span>	    url(<span class="hljs-string">r'^(?P&lt;pub_date__year&gt;\d{4})/(?P&lt;pub_date__month&gt;\d{1,2})/(?P&lt;slug&gt;[a-zA-Z0-9-]+)/?$'</span>, DetailView.as_view(
<span class="linenos">15</span>	        model=Post,
<span class="linenos">16</span>	        )),
<span class="linenos">17</span>	
<span class="linenos">18</span>	    <span class="hljs-comment"># Categories</span>
<span class="linenos">19</span>	    url(<span class="hljs-string">r'^category/(?P&lt;slug&gt;[a-zA-Z0-9-]+)/?$'</span>, CategoryListView.as_view(
<span class="linenos">20</span>	        paginate_by=<span class="hljs-number">5</span>,
<span class="linenos">21</span>	        model=Category,
<span class="linenos">22</span>	        )),
<span class="linenos">23</span>	
<span class="linenos">24</span>	    <span class="hljs-comment"># Tags</span>
<span class="linenos">25</span>	    url(<span class="hljs-string">r'^tag/(?P&lt;slug&gt;[a-zA-Z0-9-]+)/?$'</span>, TagListView.as_view(
<span class="linenos">26</span>	        paginate_by=<span class="hljs-number">5</span>,
<span class="linenos">27</span>	        model=Tag,
<span class="linenos">28</span>	        )),
<span class="linenos">29</span>	
<span class="linenos">30</span>	    <span class="hljs-comment"># Post RSS feed</span>
<span class="linenos">31</span>	    url(<span class="hljs-string">r'^feeds/posts/$'</span>, PostsFeed()),
<span class="linenos">32</span>	
<span class="linenos">33</span>	    <span class="hljs-comment"># Category RSS feed</span>
<span class="linenos">34</span>	    url(<span class="hljs-string">r'^feeds/posts/category/(?P&lt;slug&gt;[a-zA-Z0-9-]+)/?$'</span>, CategoryPostsFeed()),
<span class="linenos">35</span>	
<span class="linenos">36</span>	    <span class="hljs-comment"># Tag RSS feed</span>
<span class="linenos">37</span>	    url(<span class="hljs-string">r'^feeds/posts/tag/(?P&lt;slug&gt;[a-zA-Z0-9-]+)/?$'</span>, TagPostsFeed()),
<span class="linenos">38</span>	)</code></pre>
<p>Next, we create our view:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">TagPostsFeed</span><span class="hljs-params">(PostsFeed)</span>:</span>
<span class="linenos">2</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">get_object</span><span class="hljs-params">(self, request, slug)</span>:</span>
<span class="linenos">3</span>	        <span class="hljs-keyword">return</span> get_object_or_404(Tag, slug=slug)
<span class="linenos">4</span>	
<span class="linenos">5</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">title</span><span class="hljs-params">(self, obj)</span>:</span>
<span class="linenos">6</span>	        <span class="hljs-keyword">return</span> <span class="hljs-string">"RSS feed - blog posts tagged  %s"</span> % obj.name
<span class="linenos">7</span>	
<span class="linenos">8</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">link</span><span class="hljs-params">(self, obj)</span>:</span>
<span class="linenos">9</span>	        <span class="hljs-keyword">return</span> obj.get_absolute_url()
<span class="linenos">10</span>	
<span class="linenos">11</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">description</span><span class="hljs-params">(self, obj)</span>:</span>
<span class="linenos">12</span>	        <span class="hljs-keyword">return</span> <span class="hljs-string">"RSS feed - blog posts tagged %s"</span> % obj.name
<span class="linenos">13</span>	
<span class="linenos">14</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">items</span><span class="hljs-params">(self, obj)</span>:</span>
<span class="linenos">15</span>	        <span class="hljs-keyword">try</span>:
<span class="linenos">16</span>	            tag = Tag.objects.get(slug=obj.slug)
<span class="linenos">17</span>	            <span class="hljs-keyword">return</span> tag.post_set.all()
<span class="linenos">18</span>	        <span class="hljs-keyword">except</span> Tag.DoesNotExist:
<span class="linenos">19</span>	            <span class="hljs-keyword">return</span> Post.objects.none()</code></pre>
<p>Again, this inherits from <code>PostsFeed</code>, but the syntax for getting posts matching a tag is slightly different because they use a many-to-many relationship.</p>
<p>We also need a template for the tag pages. Add this to the end of the <code>test_tag_page</code> method:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	
<span class="linenos">2</span>	        <span class="hljs-comment"># Check the correct template was used</span>
<span class="linenos">3</span>	        self.assertTemplateUsed(response, <span class="hljs-string">'blogengine/tag_post_list.html'</span>)</code></pre>
<p>Let’s create that template:</p>
<pre><code class="hljs lang-django"><span class="linenos">1</span>	<span class="xml"></span><span class="hljs-template_tag">{% <span class="hljs-keyword">extends</span> "blogengine/includes/base.html" %}</span><span class="xml">
<span class="linenos">2</span>	
<span class="linenos">3</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">load</span> custom_markdown %}</span><span class="xml">
<span class="linenos">4</span>	
<span class="linenos">5</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">block</span> content %}</span><span class="xml">
<span class="linenos">6</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> object_list %}</span><span class="xml">
<span class="linenos">7</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">for</span> post <span class="hljs-keyword">in</span> object_list %}</span><span class="xml">
<span class="linenos">8</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"post col-md-12"</span>&gt;</span>
<span class="linenos">9</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">h1</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ post.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span></span><span class="hljs-variable">{{ post.title }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">h1</span>&gt;</span>
<span class="linenos">10</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">h3</span>&gt;</span></span><span class="hljs-variable">{{ post.pub_date }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">h3</span>&gt;</span>
<span class="linenos">11</span>	            </span><span class="hljs-variable">{{ post.text<span class="hljs-filter">|custom</span>_markdown }}</span><span class="xml">
<span class="linenos">12</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">13</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> post.category %}</span><span class="xml">
<span class="linenos">14</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"col-md-12"</span>&gt;</span>
<span class="linenos">15</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ post.category.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"label label-primary"</span>&gt;</span></span><span class="hljs-variable">{{ post.category.name }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">16</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">17</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">18</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> post.tags %}</span><span class="xml">
<span class="linenos">19</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"col-md-12"</span>&gt;</span>
<span class="linenos">20</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">for</span> tag <span class="hljs-keyword">in</span> post.tags.all %}</span><span class="xml">
<span class="linenos">21</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ tag.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"label label-success"</span>&gt;</span></span><span class="hljs-variable">{{ tag.name }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">22</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endfor</span> %}</span><span class="xml">
<span class="linenos">23</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">24</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">25</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endfor</span> %}</span><span class="xml">
<span class="linenos">26</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">else</span> %}</span><span class="xml">
<span class="linenos">27</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">p</span>&gt;</span>No posts found<span class="hljs-tag">&lt;/<span class="hljs-title">p</span>&gt;</span>
<span class="linenos">28</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">29</span>	
<span class="linenos">30</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">ul</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"pager"</span>&gt;</span>
<span class="linenos">31</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> page_obj.has_previous %}</span><span class="xml">
<span class="linenos">32</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">li</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"previous"</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/</span></span></span><span class="hljs-variable">{{ page_obj.previous_page_number }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">/"</span>&gt;</span>Previous Page<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">li</span>&gt;</span>
<span class="linenos">33</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">34</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> page_obj.has_next %}</span><span class="xml">
<span class="linenos">35</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">li</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"next"</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/</span></span></span><span class="hljs-variable">{{ page_obj.next_page_number }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">/"</span>&gt;</span>Next Page<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">li</span>&gt;</span>
<span class="linenos">36</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">37</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">ul</span>&gt;</span>
<span class="linenos">38</span>	
<span class="linenos">39</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/feeds/posts/tag/</span></span></span><span class="hljs-variable">{{ tag.slug }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">/"</span>&gt;</span>RSS feed for tag </span><span class="hljs-variable">{{ tag.name }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">40</span>	
<span class="linenos">41</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endblock</span> %}</span><span class="xml"></span></code></pre>
<p>This is virtually identical to the category template. You’ll also need to apply this template in the view for the tag list, and pass the tag name through as context data:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">TagListView</span><span class="hljs-params">(ListView)</span>:</span>
<span class="linenos">2</span>	    template_name = <span class="hljs-string">'blogengine/tag_post_list.html'</span>
<span class="linenos">3</span>	
<span class="linenos">4</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">get_queryset</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">5</span>	        slug = self.kwargs[<span class="hljs-string">'slug'</span>]
<span class="linenos">6</span>	        <span class="hljs-keyword">try</span>:
<span class="linenos">7</span>	            tag = Tag.objects.get(slug=slug)
<span class="linenos">8</span>	            <span class="hljs-keyword">return</span> tag.post_set.all()
<span class="linenos">9</span>	        <span class="hljs-keyword">except</span> Tag.DoesNotExist:
<span class="linenos">10</span>	            <span class="hljs-keyword">return</span> Post.objects.none()
<span class="linenos">11</span>	
<span class="linenos">12</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">get_context_data</span><span class="hljs-params">(self, **kwargs)</span>:</span>
<span class="linenos">13</span>	        context = super(TagListView, self).get_context_data(**kwargs)
<span class="linenos">14</span>	        slug = self.kwargs[<span class="hljs-string">'slug'</span>]
<span class="linenos">15</span>	        <span class="hljs-keyword">try</span>:
<span class="linenos">16</span>	            context[<span class="hljs-string">'tag'</span>] = Tag.objects.get(slug=slug)
<span class="linenos">17</span>	        <span class="hljs-keyword">except</span> Tag.DoesNotExist:
<span class="linenos">18</span>	            context[<span class="hljs-string">'tag'</span>] = <span class="hljs-keyword">None</span>
<span class="linenos">19</span>	        <span class="hljs-keyword">return</span> context</code></pre>
<p>Let’s run our tests:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py <span class="hljs-built_in">test</span> blogengine
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	..........................
<span class="linenos">4</span>	----------------------------------------------------------------------
<span class="linenos">5</span>	Ran <span class="hljs-number">26</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">5.770</span>s
<span class="linenos">6</span>	
<span class="linenos">7</span>	OK
<span class="linenos">8</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>You may want to do a quick check to ensure your tag feed link works as expected. Time to commit:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add blogengine templates
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Implemented tag feeds'</span></code></pre>
<h1 id="moving-our-templates">Moving our templates</h1>
<p>Before we crack on with implementing search, there’s one more piece of housekeeping. In Django, templates can be applied at project level or at app level. So far, we’ve been storing them in the project, but we would like our app to be as self-contained as possible so it can just be dropped into future projects where we need a blog. That way, it can be easily overridden for specific projects. You can move the folders and update the Git repository at the same time with this command:</p>
<pre><code class="hljs lang-bash singleline">$ git mv templates/ blogengine/</code></pre>
<p>We run the tests to make sure nothing untoward has happened:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py <span class="hljs-built_in">test</span>
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	..........................
<span class="linenos">4</span>	----------------------------------------------------------------------
<span class="linenos">5</span>	Ran <span class="hljs-number">26</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">5.847</span>s
<span class="linenos">6</span>	
<span class="linenos">7</span>	OK
<span class="linenos">8</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>And we commit:</p>
<pre><code class="hljs lang-bash singleline">$ git commit -m <span class="hljs-string">'Moved templates'</span></code></pre>
<p>Note that <code>git mv</code> updates Git and moves the files, so you don’t need to call <code>git add</code>.</p>
<h1 id="implementing-search">Implementing search</h1>
<p>For our final task today, we will be implementing a very simple search engine. Our requirements are:</p>
<ul>
<li>It should be in the header, to allow for easy access from anywhere in the front end.</li>
<li>It should search the title and text of posts.</li>
</ul>
<p>First, we’ll write our tests:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">SearchViewTest</span><span class="hljs-params">(BaseAcceptanceTest)</span>:</span>
<span class="linenos">2</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_search</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">3</span>	        <span class="hljs-comment"># Create a post</span>
<span class="linenos">4</span>	        post = PostFactory()
<span class="linenos">5</span>	
<span class="linenos">6</span>	        <span class="hljs-comment"># Create another post</span>
<span class="linenos">7</span>	        post2 = PostFactory(text=<span class="hljs-string">'This is my *second* blog post'</span>, title=<span class="hljs-string">'My second post'</span>, slug=<span class="hljs-string">'my-second-post'</span>)
<span class="linenos">8</span>	
<span class="linenos">9</span>	        <span class="hljs-comment"># Search for first post</span>
<span class="linenos">10</span>	        response = self.client.get(<span class="hljs-string">'/search?q=first'</span>)
<span class="linenos">11</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">12</span>	
<span class="linenos">13</span>	        <span class="hljs-comment"># Check the first post is contained in the results</span>
<span class="linenos">14</span>	        self.assertTrue(<span class="hljs-string">'My first post'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">15</span>	
<span class="linenos">16</span>	        <span class="hljs-comment"># Check the second post is not contained in the results</span>
<span class="linenos">17</span>	        self.assertTrue(<span class="hljs-string">'My second post'</span> <span class="hljs-keyword">not</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">18</span>	
<span class="linenos">19</span>	        <span class="hljs-comment"># Search for second post</span>
<span class="linenos">20</span>	        response = self.client.get(<span class="hljs-string">'/search?q=second'</span>)
<span class="linenos">21</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">22</span>	
<span class="linenos">23</span>	        <span class="hljs-comment"># Check the first post is not contained in the results</span>
<span class="linenos">24</span>	        self.assertTrue(<span class="hljs-string">'My first post'</span> <span class="hljs-keyword">not</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">25</span>	
<span class="linenos">26</span>	        <span class="hljs-comment"># Check the second post is contained in the results</span>
<span class="linenos">27</span>	        self.assertTrue(<span class="hljs-string">'My second post'</span> <span class="hljs-keyword">in</span> response.content)</code></pre>
<p>Don’t forget to run the tests to make sure they fail:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py <span class="hljs-built_in">test</span>
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	..........................F
<span class="linenos">4</span>	======================================================================
<span class="linenos">5</span>	FAIL: <span class="hljs-built_in">test</span>_search (blogengine.tests.SearchViewTest)
<span class="linenos">6</span>	----------------------------------------------------------------------
<span class="linenos">7</span>	Traceback (most recent call last):
<span class="linenos">8</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">819</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_search
<span class="linenos">9</span>	    self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">10</span>	AssertionError: <span class="hljs-number">404</span> != <span class="hljs-number">200</span>
<span class="linenos">11</span>	
<span class="linenos">12</span>	----------------------------------------------------------------------
<span class="linenos">13</span>	Ran <span class="hljs-number">27</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">6.919</span>s
<span class="linenos">14</span>	
<span class="linenos">15</span>	FAILED (failures=<span class="hljs-number">1</span>)
<span class="linenos">16</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>With that done, we can add the search form to the header:</p>
<pre><code class="hljs lang-django"><span class="linenos">1</span>	<span class="xml"><span class="hljs-doctype">&lt;!DOCTYPE html&gt;</span>
<span class="linenos">2</span>	<span class="hljs-comment">&lt;!--[if lt IE 7]&gt;      &lt;html class="no-js lt-ie9 lt-ie8 lt-ie7"&gt; &lt;![endif]--&gt;</span>
<span class="linenos">3</span>	<span class="hljs-comment">&lt;!--[if IE 7]&gt;         &lt;html class="no-js lt-ie9 lt-ie8"&gt; &lt;![endif]--&gt;</span>
<span class="linenos">4</span>	<span class="hljs-comment">&lt;!--[if IE 8]&gt;         &lt;html class="no-js lt-ie9"&gt; &lt;![endif]--&gt;</span>
<span class="linenos">5</span>	<span class="hljs-comment">&lt;!--[if gt IE 8]&gt;&lt;!--&gt;</span> <span class="hljs-tag">&lt;<span class="hljs-title">html</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"no-js"</span>&gt;</span> <span class="hljs-comment">&lt;!--&lt;![endif]--&gt;</span>
<span class="linenos">6</span>	    <span class="hljs-tag">&lt;<span class="hljs-title">head</span>&gt;</span>
<span class="linenos">7</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">meta</span> <span class="hljs-attribute">charset</span>=<span class="hljs-value">"utf-8"</span>&gt;</span>
<span class="linenos">8</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">meta</span> <span class="hljs-attribute">http-equiv</span>=<span class="hljs-value">"X-UA-Compatible"</span> <span class="hljs-attribute">content</span>=<span class="hljs-value">"IE=edge"</span>&gt;</span>
<span class="linenos">9</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">title</span>&gt;</span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">block</span> title %}</span><span class="xml">My Django Blog</span><span class="hljs-template_tag">{% <span class="hljs-keyword">endblock</span> %}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">title</span>&gt;</span>
<span class="linenos">10</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">meta</span> <span class="hljs-attribute">name</span>=<span class="hljs-value">"description"</span> <span class="hljs-attribute">content</span>=<span class="hljs-value">""</span>&gt;</span>
<span class="linenos">11</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">meta</span> <span class="hljs-attribute">name</span>=<span class="hljs-value">"viewport"</span> <span class="hljs-attribute">content</span>=<span class="hljs-value">"width=device-width, initial-scale=1"</span>&gt;</span>
<span class="linenos">12</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"alternate"</span> <span class="hljs-attribute">type</span>=<span class="hljs-value">"application/rss+xml"</span> <span class="hljs-attribute">title</span>=<span class="hljs-value">"Blog posts"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/feeds/posts/"</span> &gt;</span>
<span class="linenos">13</span>	
<span class="linenos">14</span>	        <span class="hljs-comment">&lt;!-- Place favicon.ico and apple-touch-icon.png in the root directory --&gt;</span>
<span class="linenos">15</span>	
<span class="linenos">16</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">load</span> staticfiles %}</span><span class="xml">
<span class="linenos">17</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/html5-boilerplate/css/normalize.css' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>
<span class="linenos">18</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/html5-boilerplate/css/main.css' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>
<span class="linenos">19</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/bootstrap/dist/css/bootstrap.min.css' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>
<span class="linenos">20</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/bootstrap/dist/css/bootstrap-theme.min.css' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>
<span class="linenos">21</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'css/main.css' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>
<span class="linenos">22</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'css/code.css' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>
<span class="linenos">23</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span> <span class="hljs-attribute">src</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/html5-boilerplate/js/vendor/modernizr-2.6.2.min.js' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span><span class="undefined"></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">24</span>	    <span class="hljs-tag">&lt;/<span class="hljs-title">head</span>&gt;</span>
<span class="linenos">25</span>	    <span class="hljs-tag">&lt;<span class="hljs-title">body</span>&gt;</span>
<span class="linenos">26</span>	        <span class="hljs-comment">&lt;!--[if lt IE 7]&gt;
<span class="linenos">27</span>	            &lt;p class="browsehappy"&gt;You are using an &lt;strong&gt;outdated&lt;/strong&gt; browser. Please &lt;a href="http://browsehappy.com/"&gt;upgrade your browser&lt;/a&gt; to improve your experience.&lt;/p&gt;
<span class="linenos">28</span>	        &lt;![endif]--&gt;</span>
<span class="linenos">29</span>	
<span class="linenos">30</span>	        <span class="hljs-comment">&lt;!-- Add your site or application content here --&gt;</span>
<span class="linenos">31</span>	
<span class="linenos">32</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">id</span>=<span class="hljs-value">"fb-root"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">33</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span>&gt;</span><span class="clojure"><span class="hljs-list">(<span class="hljs-keyword">function</span><span class="hljs-list">(<span class="hljs-keyword">d</span>, s, id)</span> <span class="hljs-collection">{
<span class="linenos">34</span>	            var js, fjs = d.getElementsByTagName<span class="hljs-list">(<span class="hljs-keyword">s</span>)</span><span class="hljs-collection">[<span class="hljs-number">0</span>]</span><span class="hljs-comment">;</span>
<span class="linenos">35</span>	            if <span class="hljs-list">(<span class="hljs-keyword">d.getElementById</span><span class="hljs-list">(<span class="hljs-keyword">id</span>)</span>)</span> return;
<span class="linenos">36</span>	                js = d.createElement<span class="hljs-list">(<span class="hljs-keyword">s</span>)</span><span class="hljs-comment">; js.id = id;</span>
<span class="linenos">37</span>	                js.src = <span class="hljs-string">"//connect.facebook.net/en_GB/all.js#xfbml=1"</span><span class="hljs-comment">;</span>
<span class="linenos">38</span>	                fjs.parentNode.insertBefore<span class="hljs-list">(<span class="hljs-keyword">js</span>, fjs)</span><span class="hljs-comment">;</span>
<span class="linenos">39</span>	            }</span><span class="hljs-list">(<span class="hljs-keyword">document</span>, 'script', 'facebook-jssdk')</span>)</span><span class="hljs-comment">;</span></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">40</span>	
<span class="linenos">41</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"navbar navbar-static-top navbar-inverse"</span>&gt;</span>
<span class="linenos">42</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"container-fluid"</span>&gt;</span>
<span class="linenos">43</span>	                <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"navbar-header"</span>&gt;</span>
<span class="linenos">44</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">button</span> <span class="hljs-attribute">type</span>=<span class="hljs-value">"button"</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"navbar-toggle"</span> <span class="hljs-attribute">data-toggle</span>=<span class="hljs-value">"collapse"</span> <span class="hljs-attribute">data-target</span>=<span class="hljs-value">"#header-nav"</span>&gt;</span>
<span class="linenos">45</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"icon-bar"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span>
<span class="linenos">46</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"icon-bar"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span>
<span class="linenos">47</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"icon-bar"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span>
<span class="linenos">48</span>	                    <span class="hljs-tag">&lt;/<span class="hljs-title">button</span>&gt;</span>
<span class="linenos">49</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"navbar-brand"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/"</span>&gt;</span>My Django Blog<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">50</span>	                <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">51</span>	                <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"collapse navbar-collapse"</span> <span class="hljs-attribute">id</span>=<span class="hljs-value">"header-nav"</span>&gt;</span>
<span class="linenos">52</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">ul</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"nav navbar-nav"</span>&gt;</span>
<span class="linenos">53</span>	                        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">load</span> flatpages %}</span><span class="xml">
<span class="linenos">54</span>	                        </span><span class="hljs-template_tag">{% get_flatpages <span class="hljs-keyword">as</span> flatpages %}</span><span class="xml">
<span class="linenos">55</span>	                        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">for</span> flatpage <span class="hljs-keyword">in</span> flatpages %}</span><span class="xml">
<span class="linenos">56</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">li</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ flatpage.url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span></span><span class="hljs-variable">{{ flatpage.title }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">li</span>&gt;</span>
<span class="linenos">57</span>	                        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endfor</span> %}</span><span class="xml">
<span class="linenos">58</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">li</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/feeds/posts/"</span>&gt;</span>RSS feed<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">li</span>&gt;</span>
<span class="linenos">59</span>	
<span class="linenos">60</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">form</span> <span class="hljs-attribute">action</span>=<span class="hljs-value">"/search"</span> <span class="hljs-attribute">method</span>=<span class="hljs-value">"GET"</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"navbar-form navbar-left"</span>&gt;</span>
<span class="linenos">61</span>	                            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"form-group"</span>&gt;</span>
<span class="linenos">62</span>	                                <span class="hljs-tag">&lt;<span class="hljs-title">input</span> <span class="hljs-attribute">type</span>=<span class="hljs-value">"text"</span> <span class="hljs-attribute">name</span>=<span class="hljs-value">"q"</span> <span class="hljs-attribute">placeholder</span>=<span class="hljs-value">"Search..."</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"form-control"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">input</span>&gt;</span>
<span class="linenos">63</span>	                            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">64</span>	                            <span class="hljs-tag">&lt;<span class="hljs-title">button</span> <span class="hljs-attribute">type</span>=<span class="hljs-value">"submit"</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"btn btn-default"</span>&gt;</span>Search<span class="hljs-tag">&lt;/<span class="hljs-title">button</span>&gt;</span>
<span class="linenos">65</span>	                        <span class="hljs-tag">&lt;/<span class="hljs-title">form</span>&gt;</span>
<span class="linenos">66</span>	                    <span class="hljs-tag">&lt;/<span class="hljs-title">ul</span>&gt;</span>
<span class="linenos">67</span>	                <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">68</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">69</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">70</span>	
<span class="linenos">71</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"container"</span>&gt;</span>
<span class="linenos">72</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">block</span> header %}</span><span class="xml">
<span class="linenos">73</span>	                <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"page-header"</span>&gt;</span>
<span class="linenos">74</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">h1</span>&gt;</span>My Django Blog<span class="hljs-tag">&lt;/<span class="hljs-title">h1</span>&gt;</span>
<span class="linenos">75</span>	                <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">76</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endblock</span> %}</span><span class="xml">
<span class="linenos">77</span>	
<span class="linenos">78</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"row"</span>&gt;</span>
<span class="linenos">79</span>	                </span><span class="hljs-template_tag">{% <span class="hljs-keyword">block</span> content %}</span><span class="xml"></span><span class="hljs-template_tag">{% <span class="hljs-keyword">endblock</span> %}</span><span class="xml">
<span class="linenos">80</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">81</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">82</span>	
<span class="linenos">83</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"container footer"</span>&gt;</span>
<span class="linenos">84</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"row"</span>&gt;</span>
<span class="linenos">85</span>	                <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"span12"</span>&gt;</span>
<span class="linenos">86</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">p</span>&gt;</span>Copyright &amp;copy; </span><span class="hljs-template_tag">{% <span class="hljs-keyword">now</span> "Y" %}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">p</span>&gt;</span>
<span class="linenos">87</span>	                <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">88</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">89</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">90</span>	
<span class="linenos">91</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span> <span class="hljs-attribute">src</span>=<span class="hljs-value">"//ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js"</span>&gt;</span><span class="undefined"></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">92</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span>&gt;</span><span class="applescript">window.jQuery || document.<span class="hljs-command">write</span>('&lt;<span class="hljs-keyword">script</span> src=<span class="hljs-string">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/html5-boilerplate/js/vendor/jquery-1.10.2.min.js' %}</span><span class="xml"><span class="1c"><span class="hljs-string">"&gt;&lt;\/script&gt;')</span></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">93</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span> <span class="hljs-attribute">src</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/html5-boilerplate/js/plugins.js' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span><span class="undefined"></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">94</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span> <span class="hljs-attribute">src</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/bootstrap/dist/js/bootstrap.min.js' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span><span class="undefined"></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">95</span>	
<span class="linenos">96</span>	        <span class="hljs-comment">&lt;!-- Google Analytics: change UA-XXXXX-X to be your site's ID. --&gt;</span>
<span class="linenos">97</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span>&gt;</span><span class="scheme">
<span class="linenos">98</span>	            <span class="hljs-list">(<span class="hljs-keyword">function</span><span class="hljs-list">(<span class="hljs-keyword">b</span>,o,i,l,e,r)</span>{b.GoogleAnalyticsObject=l<span class="hljs-comment">;b[l]||(b[l]=</span>
<span class="linenos">99</span>	            function<span class="hljs-list">()</span>{<span class="hljs-list">(<span class="hljs-keyword">b</span><span class="hljs-list">[<span class="hljs-keyword">l</span>].q=b<span class="hljs-list">[<span class="hljs-keyword">l</span>].q||<span class="hljs-list">[]).push<span class="hljs-list">(<span class="hljs-keyword">arguments</span>)</span>})<span class="hljs-comment">;b[l].l=+new Date;</span>
<span class="linenos">100</span>	            e=o.createElement<span class="hljs-list">(<span class="hljs-keyword">i</span>)</span><span class="hljs-comment">;r=o.getElementsByTagName(i)[0];</span>
<span class="linenos">101</span>	            e.src=<span class="hljs-variable">'//www.google-analytics.com/analytics.js</span>'<span class="hljs-comment">;</span>
<span class="linenos">102</span>	            r.parentNode.insertBefore<span class="hljs-list">(<span class="hljs-keyword">e</span>,r)</span>}<span class="hljs-list">(<span class="hljs-keyword">window</span>,document,<span class="hljs-variable">'script</span>',<span class="hljs-variable">'ga</span>')</span>)<span class="hljs-comment">;</span>
<span class="linenos">103</span>	            ga<span class="hljs-list">(<span class="hljs-variable">'create</span>',<span class="hljs-variable">'UA-XXXXX-X</span>')</span><span class="hljs-comment">;ga('send','pageview');</span>
<span class="linenos">104</span>	        </span></span></span></span></span></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">105</span>	    <span class="hljs-tag">&lt;/<span class="hljs-title">body</span>&gt;</span>
<span class="linenos">106</span>	<span class="hljs-tag">&lt;/<span class="hljs-title">html</span>&gt;</span></span></code></pre>
<p>Now we’ll actually implement our search. Implementing search using Django’s generic views can be fiddly, so we’ll write our search view as a function instead. First, amend the imports at the top of your view file to look like this:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-keyword">from</span> django.shortcuts <span class="hljs-keyword">import</span> get_object_or_404, render_to_response
<span class="linenos">2</span>	<span class="hljs-keyword">from</span> django.core.paginator <span class="hljs-keyword">import</span> Paginator, EmptyPage
<span class="linenos">3</span>	<span class="hljs-keyword">from</span> django.db.models <span class="hljs-keyword">import</span> Q
<span class="linenos">4</span>	<span class="hljs-keyword">from</span> django.views.generic <span class="hljs-keyword">import</span> ListView
<span class="linenos">5</span>	<span class="hljs-keyword">from</span> blogengine.models <span class="hljs-keyword">import</span> Category, Post, Tag
<span class="linenos">6</span>	<span class="hljs-keyword">from</span> django.contrib.syndication.views <span class="hljs-keyword">import</span> Feed
<span class="linenos">7</span>	<span class="hljs-keyword">from</span> django.utils.encoding <span class="hljs-keyword">import</span> force_unicode
<span class="linenos">8</span>	<span class="hljs-keyword">from</span> django.utils.safestring <span class="hljs-keyword">import</span> mark_safe
<span class="linenos">9</span>	<span class="hljs-keyword">import</span> markdown2</code></pre>
<p>Next, add the following code to the end of the file:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">getSearchResults</span><span class="hljs-params">(request)</span>:</span>
<span class="linenos">2</span>	    <span class="hljs-string">"""
<span class="linenos">3</span>	    Search for a post by title or text
<span class="linenos">4</span>	    """</span>
<span class="linenos">5</span>	    <span class="hljs-comment"># Get the query data</span>
<span class="linenos">6</span>	    query = request.GET.get(<span class="hljs-string">'q'</span>, <span class="hljs-string">''</span>)
<span class="linenos">7</span>	    page = request.GET.get(<span class="hljs-string">'page'</span>, <span class="hljs-number">1</span>)
<span class="linenos">8</span>	
<span class="linenos">9</span>	    <span class="hljs-comment"># Query the database</span>
<span class="linenos">10</span>	    <span class="hljs-keyword">if</span> query:
<span class="linenos">11</span>	        results = Post.objects.filter(Q(text__icontains=query) | Q(title__icontains=query))
<span class="linenos">12</span>	    <span class="hljs-keyword">else</span>:
<span class="linenos">13</span>	        results = <span class="hljs-keyword">None</span>
<span class="linenos">14</span>	
<span class="linenos">15</span>	    <span class="hljs-comment"># Add pagination</span>
<span class="linenos">16</span>	    pages = Paginator(results, <span class="hljs-number">5</span>)
<span class="linenos">17</span>	
<span class="linenos">18</span>	    <span class="hljs-comment"># Get specified page</span>
<span class="linenos">19</span>	    <span class="hljs-keyword">try</span>:
<span class="linenos">20</span>	        returned_page = pages.page(page)
<span class="linenos">21</span>	    <span class="hljs-keyword">except</span> EmptyPage:
<span class="linenos">22</span>	        returned_page = pages.page(pages.num_pages)
<span class="linenos">23</span>	
<span class="linenos">24</span>	    <span class="hljs-comment"># Display the search results</span>
<span class="linenos">25</span>	    <span class="hljs-keyword">return</span> render_to_response(<span class="hljs-string">'blogengine/search_post_list.html'</span>,
<span class="linenos">26</span>	                              {<span class="hljs-string">'page_obj'</span>: returned_page,
<span class="linenos">27</span>	                               <span class="hljs-string">'object_list'</span>: returned_page.object_list,
<span class="linenos">28</span>	                               <span class="hljs-string">'search'</span>: query})</code></pre>
<p>As this is the first time we’ve written a view without using generic views, a little explanation is called for. First we get the values of the <code>q</code> and <code>page</code> parameters passed to the view. <code>q</code> contains the query text and <code>page</code> contains the page number. Note also that our page defaults to 1 if not set.</p>
<p>We then use the Q object to perform a query. The Django ORM will <code>AND</code> together keyword argument queries, but that’s not the behaviour we want here. Instead we want to be able to search for content in the title or text, so we need to use a query with an <code>OR</code> statement, which necessitates using the <a href="https://docs.djangoproject.com/en/dev/topics/db/queries/#complex-lookups-with-q-objects">Q</a> object.</p>
<p>Next, we use the <code>Paginator</code> object to manually paginate the results, and if it raises an <code>EmptyPage</code> exception, to just show the last page instead. Finally we render the template <code>blogengine/search_post_list.html</code>, and pass through the parameters <code>page_obj</code> for the returned page, <code>object_list</code> for the objects, and <code>search</code> for the query.</p>
<p>We also need to add a route for our new view:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	
<span class="linenos">2</span>	    <span class="hljs-comment"># Search posts</span>
<span class="linenos">3</span>	    url(<span class="hljs-string">r'^search'</span>, <span class="hljs-string">'blogengine.views.getSearchResults'</span>),</code></pre>
<p>Finally, let’s create a new template to show our results:</p>
<pre><code class="hljs lang-django"><span class="linenos">1</span>	<span class="xml"></span><span class="hljs-template_tag">{% <span class="hljs-keyword">extends</span> "blogengine/includes/base.html" %}</span><span class="xml">
<span class="linenos">2</span>	
<span class="linenos">3</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">load</span> custom_markdown %}</span><span class="xml">
<span class="linenos">4</span>	
<span class="linenos">5</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">block</span> content %}</span><span class="xml">
<span class="linenos">6</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> object_list %}</span><span class="xml">
<span class="linenos">7</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">for</span> post <span class="hljs-keyword">in</span> object_list %}</span><span class="xml">
<span class="linenos">8</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"post col-md-12"</span>&gt;</span>
<span class="linenos">9</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">h1</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ post.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span></span><span class="hljs-variable">{{ post.title }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">h1</span>&gt;</span>
<span class="linenos">10</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">h3</span>&gt;</span></span><span class="hljs-variable">{{ post.pub_date }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">h3</span>&gt;</span>
<span class="linenos">11</span>	            </span><span class="hljs-variable">{{ post.text<span class="hljs-filter">|custom</span>_markdown }}</span><span class="xml">
<span class="linenos">12</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">13</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> post.category %}</span><span class="xml">
<span class="linenos">14</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"col-md-12"</span>&gt;</span>
<span class="linenos">15</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ post.category.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"label label-primary"</span>&gt;</span></span><span class="hljs-variable">{{ post.category.name }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">16</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">17</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">18</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> post.tags %}</span><span class="xml">
<span class="linenos">19</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"col-md-12"</span>&gt;</span>
<span class="linenos">20</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">for</span> tag <span class="hljs-keyword">in</span> post.tags.all %}</span><span class="xml">
<span class="linenos">21</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ tag.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"label label-success"</span>&gt;</span></span><span class="hljs-variable">{{ tag.name }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">22</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endfor</span> %}</span><span class="xml">
<span class="linenos">23</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">24</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">25</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endfor</span> %}</span><span class="xml">
<span class="linenos">26</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">else</span> %}</span><span class="xml">
<span class="linenos">27</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">p</span>&gt;</span>No posts found<span class="hljs-tag">&lt;/<span class="hljs-title">p</span>&gt;</span>
<span class="linenos">28</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">29</span>	
<span class="linenos">30</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">ul</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"pager"</span>&gt;</span>
<span class="linenos">31</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> page_obj.has_previous %}</span><span class="xml">
<span class="linenos">32</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">li</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"previous"</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/search?page=</span></span></span><span class="hljs-variable">{{ page_obj.previous_page_number }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">&amp;q=</span></span></span><span class="hljs-variable">{{ search }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>Previous Page<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">li</span>&gt;</span>
<span class="linenos">33</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">34</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> page_obj.has_next %}</span><span class="xml">
<span class="linenos">35</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">li</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"next"</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/search?page=</span></span></span><span class="hljs-variable">{{ page_obj.next_page_number }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">&amp;q=</span></span></span><span class="hljs-variable">{{ search }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>Next Page<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">li</span>&gt;</span>
<span class="linenos">36</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">37</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">ul</span>&gt;</span>
<span class="linenos">38</span>	
<span class="linenos">39</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endblock</span> %}</span><span class="xml"></span></code></pre>
<p>Let’s run our tests:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py <span class="hljs-built_in">test</span>
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	...........................
<span class="linenos">4</span>	----------------------------------------------------------------------
<span class="linenos">5</span>	Ran <span class="hljs-number">27</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">6.421</span>s
<span class="linenos">6</span>	
<span class="linenos">7</span>	OK
<span class="linenos">8</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>Don’t forget to do a quick sense check to make sure it’s all working as expected. Then it’s time to commit:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add blogengine/
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Implemented search'</span></code></pre>
<p>And push up your changes:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git push origin master
<span class="linenos">2</span>	$ git push heroku master</code></pre>
<p>And that’s the end of this instalment. Please note this particular search solution is quite basic, and if you want something more powerful, you may want to look at <a href="http://haystacksearch.org/">Haystack</a>.</p>
<p>As usual, you can get this lesson with <code>git checkout lesson-7</code> - if you have any problems, the <a href="https://github.com/matthewbdaly/django_tutorial_blog_ng">repository</a> should be the first place you look for answers as this is the working code base for the application, and with judicious use of a tool like <code>diff</code>, it’s generally pretty easy to track down most issues.</p>
<p>In our next, and final instalment, we’ll cover:</p>
<ul>
<li>Tidying everything up</li>
<li>Implementing an XML sitemap for search engines</li>
<li>Optimising our site</li>
<li>Using Fabric to make deployment easier</li>
</ul>
<p>Hope to see you then!</p>
]]></description>
            <link>http://matthewdaly.co.uk/blog/2014/08/25/django-blog-tutorial-the-next-generation-part-7/</link>
            <guid isPermaLink="true">http://matthewdaly.co.uk/blog/2014/08/25/django-blog-tutorial-the-next-generation-part-7/</guid>
            <pubDate>Mon, 25 Aug 2014 16:15:01 GMT</pubDate>
        </item>
        <item>
            <title><![CDATA[Django Blog Tutorial - the Next Generation - Part 6]]></title>
            <description><![CDATA[<p>Welcome back! In this tutorial we’ll cover the following:</p>
<ul>
<li>Fixing bugs the TDD way</li>
<li>Setting up syntax highlighting for code snippets</li>
<li>Tidying up the front end</li>
</ul>
<p>Apologies, but I’m holding over implementing the search and additional feeds for a future instalment - in the past I’ve tried to cover too much in one post and that has led to me putting them off for much too long. So this instalment and future ones are going to be shorter so I can get them out the door quicker.</p>
<p>Ready? Let’s get started!</p>
<h1 id="fixing-bugs">Fixing bugs</h1>
<p>When someone reports a bug, it’s tempting to just dive straight in and start fixing it. But TDD cautions us against this practice. If we have a bug, then our tests should have caught it. If they don’t, then before we fix the bug, we have to make sure we can catch it if it crops up in future by implementing a test for it, and ensuring that it fails. Once that test is in place, we can then go ahead and fix the bug, safe in the knowledge that if it should reappear in future, we will be warned when our tests run.</p>
<p>As it happens, we have a bug in our web app. If you activate your virtualenv in the usual way and run the development server, and then try to create a new post without adding a tag, you’ll see that it fails as the tag is empty. Now, it’s pretty obvious that this is because the <code>tags</code> attribute of the <code>Post</code> model cannot be blank, so we have a good idea of what we need to do to fix this. But to make sure it never occurs again, we need to implement a test first.</p>
<p>Add the following method to <code>AdminTest</code>:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_create_post_without_tag</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">2</span>	        <span class="hljs-comment"># Create the category</span>
<span class="linenos">3</span>	        category = Category()
<span class="linenos">4</span>	        category.name = <span class="hljs-string">'python'</span>
<span class="linenos">5</span>	        category.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">6</span>	        category.save()
<span class="linenos">7</span>	
<span class="linenos">8</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">9</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">10</span>	
<span class="linenos">11</span>	        <span class="hljs-comment"># Check response code</span>
<span class="linenos">12</span>	        response = self.client.get(<span class="hljs-string">'/admin/blogengine/post/add/'</span>)
<span class="linenos">13</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">14</span>	
<span class="linenos">15</span>	        <span class="hljs-comment"># Create the new post</span>
<span class="linenos">16</span>	        response = self.client.post(<span class="hljs-string">'/admin/blogengine/post/add/'</span>, {
<span class="linenos">17</span>	            <span class="hljs-string">'title'</span>: <span class="hljs-string">'My first post'</span>,
<span class="linenos">18</span>	            <span class="hljs-string">'text'</span>: <span class="hljs-string">'This is my first post'</span>,
<span class="linenos">19</span>	            <span class="hljs-string">'pub_date_0'</span>: <span class="hljs-string">'2013-12-28'</span>,
<span class="linenos">20</span>	            <span class="hljs-string">'pub_date_1'</span>: <span class="hljs-string">'22:00:04'</span>,
<span class="linenos">21</span>	            <span class="hljs-string">'slug'</span>: <span class="hljs-string">'my-first-post'</span>,
<span class="linenos">22</span>	            <span class="hljs-string">'site'</span>: <span class="hljs-string">'1'</span>,
<span class="linenos">23</span>	            <span class="hljs-string">'category'</span>: <span class="hljs-string">'1'</span>
<span class="linenos">24</span>	        },
<span class="linenos">25</span>	        follow=<span class="hljs-keyword">True</span>
<span class="linenos">26</span>	        )
<span class="linenos">27</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">28</span>	
<span class="linenos">29</span>	        <span class="hljs-comment"># Check added successfully</span>
<span class="linenos">30</span>	        self.assertTrue(<span class="hljs-string">'added successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">31</span>	
<span class="linenos">32</span>	        <span class="hljs-comment"># Check new post now in database</span>
<span class="linenos">33</span>	        all_posts = Post.objects.all()
<span class="linenos">34</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)</code></pre>
<p>This is virtually identical to our previous method for adding a post, but doesn’t add a tag to the post. If you run <code>python manage.py jenkins</code>, the test should fail. Let’s commit our changes:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add blogengine/tests.py
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Added failing test for posts without tags'</span></code></pre>
<p>Now we’re in a position to fix our bug. Let’s take a look at our models:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Post</span><span class="hljs-params">(models.Model)</span>:</span>
<span class="linenos">2</span>	    title = models.CharField(max_length=<span class="hljs-number">200</span>)
<span class="linenos">3</span>	    pub_date = models.DateTimeField()
<span class="linenos">4</span>	    text = models.TextField()
<span class="linenos">5</span>	    slug = models.SlugField(max_length=<span class="hljs-number">40</span>, unique=<span class="hljs-keyword">True</span>)
<span class="linenos">6</span>	    author = models.ForeignKey(User)
<span class="linenos">7</span>	    site = models.ForeignKey(Site)
<span class="linenos">8</span>	    category = models.ForeignKey(Category, blank=<span class="hljs-keyword">True</span>, null=<span class="hljs-keyword">True</span>)
<span class="linenos">9</span>	    tags = models.ManyToManyField(Tag)</code></pre>
<p>If you compare <code>category</code> and <code>tags</code>, you’ll immediately see that <code>category</code> has the additional parameters <code>blank</code> and <code>null</code> both set to <code>True</code>. So that’s what we need to do for <code>tags</code> as well. Amend the model to look like this:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Post</span><span class="hljs-params">(models.Model)</span>:</span>
<span class="linenos">2</span>	    title = models.CharField(max_length=<span class="hljs-number">200</span>)
<span class="linenos">3</span>	    pub_date = models.DateTimeField()
<span class="linenos">4</span>	    text = models.TextField()
<span class="linenos">5</span>	    slug = models.SlugField(max_length=<span class="hljs-number">40</span>, unique=<span class="hljs-keyword">True</span>)
<span class="linenos">6</span>	    author = models.ForeignKey(User)
<span class="linenos">7</span>	    site = models.ForeignKey(Site)
<span class="linenos">8</span>	    category = models.ForeignKey(Category, blank=<span class="hljs-keyword">True</span>, null=<span class="hljs-keyword">True</span>)
<span class="linenos">9</span>	    tags = models.ManyToManyField(Tag, blank=<span class="hljs-keyword">True</span>, null=<span class="hljs-keyword">True</span>)</code></pre>
<p>You shouldn’t have to create a migration for this. Let’s run our tests:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py jenkins --coverage-html-report=htmlcov
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	.......................
<span class="linenos">4</span>	----------------------------------------------------------------------
<span class="linenos">5</span>	Ran <span class="hljs-number">23</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">7.634</span>s
<span class="linenos">6</span>	
<span class="linenos">7</span>	OK
<span class="linenos">8</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>Our tests pass! So we’ve fixed our bug, and we’ve ensured that if it happens again, we’ll catch it. With that done, it’s time to commit:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add blogengine/models.py
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Fixed a bug with the Post model'</span></code></pre>
<p><strong>Remember</strong>: Always make the effort to create a test to reproduce your bug before fixing it. That way, you know you can catch it in future.</p>
<h1 id="syntax-highlighting">Syntax highlighting</h1>
<p>This is one feature that not everyone will want to implement. If you want to be able to show code snippets on your blog, then implementing syntax highlighting is well worth your time. However, if that’s not what you want to use your blog for, feel free to skip over this section.</p>
<p>Now, earlier in the series we implemented Markdown support. In Markdown there are two main ways to denote a code block. One is to indent the code by four spaces, while the other is to use <a href="https://help.github.com/articles/github-flavored-markdown#syntax-highlighting">fenced code blocks with syntax highlighting</a>. There are many flavours of Markdown available for Python, and unfortunately the one we’ve been using so far doesn’t support fenced code blocks, so we’ll be switching to one that does.</p>
<p>We also need to be able to generate a suitable stylesheet to highlight the code appropriately. For that, we’ll be using <a href="http://pygments.org/">Pygments</a>. So let’s first uninstall our existing implementation of Markdown:</p>
<pre><code class="hljs lang-bash singleline">$ pip uninstall Markdown</code></pre>
<p>And install the new modules:</p>
<pre><code class="hljs lang-bash singleline">$ pip install markdown2 Pygments</code></pre>
<p>Don’t forget to record the changes:</p>
<pre><code class="hljs lang-bash singleline">$ pip freeze &gt; requirements.txt</code></pre>
<p>Now, we need to amend our Markdown template tags to use the new version of Markdown:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-keyword">import</span> markdown2
<span class="linenos">2</span>	
<span class="linenos">3</span>	<span class="hljs-keyword">from</span> django <span class="hljs-keyword">import</span> template
<span class="linenos">4</span>	<span class="hljs-keyword">from</span> django.template.defaultfilters <span class="hljs-keyword">import</span> stringfilter
<span class="linenos">5</span>	<span class="hljs-keyword">from</span> django.utils.encoding <span class="hljs-keyword">import</span> force_unicode
<span class="linenos">6</span>	<span class="hljs-keyword">from</span> django.utils.safestring <span class="hljs-keyword">import</span> mark_safe
<span class="linenos">7</span>	
<span class="linenos">8</span>	register = template.Library()
<span class="linenos">9</span>	
<span class="linenos">10</span>	<span class="hljs-decorator">@register.filter(is_safe=True)</span>
<span class="linenos">11</span>	<span class="hljs-decorator">@stringfilter</span>
<span class="linenos">12</span>	<span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">custom_markdown</span><span class="hljs-params">(value)</span>:</span>
<span class="linenos">13</span>	    extras = [<span class="hljs-string">"fenced-code-blocks"</span>]
<span class="linenos">14</span>	
<span class="linenos">15</span>	    <span class="hljs-keyword">return</span> mark_safe(markdown2.markdown(force_unicode(value),
<span class="linenos">16</span>	                                       extras = extras))</code></pre>
<p>All we do here is change the Markdown module that gets imported, and amend how it is called. Note that we pass through the parameter <code>fenced-code-blocks</code> to enable this functionality.</p>
<p>If you now run the development server and create a post with some code in it (just copy the Ruby example from <a href="https://help.github.com/articles/github-flavored-markdown#syntax-highlighting">here</a>), then view it on the site, you should be able to see that it’s in a <code>&lt;code&gt;</code> block. However, it’s not highlighted yet. Let’s commit our changes:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add requirements/txt blogengine/templatetags/custom_markdown.py
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Now use Markdown2 to allow for syntax highlighting'</span></code></pre>
<p>Now, if you examine the markup for your code blocks using your browser’s developer tools, you’ll notice that the code is wrapped in many different spans with various classes. Pygments can generate a CSS file that uses those classes to highlight your code.</p>
<p>First, let’s create a folder to store our CSS files in:</p>
<pre><code class="hljs lang-bash singleline">$ mkdir blogengine/static/css</code></pre>
<p>Next, we’ll add a blank CSS file for any other styling we may want to apply:</p>
<pre><code class="hljs lang-bash singleline">$ touch blogengine/static/css/main.css</code></pre>
<p>Then, we generate our CSS file:</p>
<pre><code class="hljs lang-bash singleline">$ pygmentize -S default <span class="hljs-operator">-f</span> html &gt; blogengine/static/css/code.css</code></pre>
<p>We need to include our CSS files in our HTML template:</p>
<pre><code class="hljs lang-django"><span class="linenos">1</span>	<span class="xml"><span class="hljs-doctype">&lt;!DOCTYPE html&gt;</span>
<span class="linenos">2</span>	<span class="hljs-comment">&lt;!--[if lt IE 7]&gt;      &lt;html class="no-js lt-ie9 lt-ie8 lt-ie7"&gt; &lt;![endif]--&gt;</span>
<span class="linenos">3</span>	<span class="hljs-comment">&lt;!--[if IE 7]&gt;         &lt;html class="no-js lt-ie9 lt-ie8"&gt; &lt;![endif]--&gt;</span>
<span class="linenos">4</span>	<span class="hljs-comment">&lt;!--[if IE 8]&gt;         &lt;html class="no-js lt-ie9"&gt; &lt;![endif]--&gt;</span>
<span class="linenos">5</span>	<span class="hljs-comment">&lt;!--[if gt IE 8]&gt;&lt;!--&gt;</span> <span class="hljs-tag">&lt;<span class="hljs-title">html</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"no-js"</span>&gt;</span> <span class="hljs-comment">&lt;!--&lt;![endif]--&gt;</span>
<span class="linenos">6</span>	    <span class="hljs-tag">&lt;<span class="hljs-title">head</span>&gt;</span>
<span class="linenos">7</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">meta</span> <span class="hljs-attribute">charset</span>=<span class="hljs-value">"utf-8"</span>&gt;</span>
<span class="linenos">8</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">meta</span> <span class="hljs-attribute">http-equiv</span>=<span class="hljs-value">"X-UA-Compatible"</span> <span class="hljs-attribute">content</span>=<span class="hljs-value">"IE=edge"</span>&gt;</span>
<span class="linenos">9</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">title</span>&gt;</span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">block</span> title %}</span><span class="xml">My Django Blog</span><span class="hljs-template_tag">{% <span class="hljs-keyword">endblock</span> %}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">title</span>&gt;</span>
<span class="linenos">10</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">meta</span> <span class="hljs-attribute">name</span>=<span class="hljs-value">"description"</span> <span class="hljs-attribute">content</span>=<span class="hljs-value">""</span>&gt;</span>
<span class="linenos">11</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">meta</span> <span class="hljs-attribute">name</span>=<span class="hljs-value">"viewport"</span> <span class="hljs-attribute">content</span>=<span class="hljs-value">"width=device-width, initial-scale=1"</span>&gt;</span>
<span class="linenos">12</span>	
<span class="linenos">13</span>	        <span class="hljs-comment">&lt;!-- Place favicon.ico and apple-touch-icon.png in the root directory --&gt;</span>
<span class="linenos">14</span>	
<span class="linenos">15</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">load</span> staticfiles %}</span><span class="xml">
<span class="linenos">16</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/html5-boilerplate/css/normalize.css' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>
<span class="linenos">17</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/html5-boilerplate/css/main.css' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>
<span class="linenos">18</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/bootstrap/dist/css/bootstrap.min.css' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>
<span class="linenos">19</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/bootstrap/dist/css/bootstrap-theme.min.css' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>
<span class="linenos">20</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'css/main.css' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>
<span class="linenos">21</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'css/code.css' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>
<span class="linenos">22</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span> <span class="hljs-attribute">src</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/html5-boilerplate/js/vendor/modernizr-2.6.2.min.js' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span><span class="undefined"></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">23</span>	    <span class="hljs-tag">&lt;/<span class="hljs-title">head</span>&gt;</span>
<span class="linenos">24</span>	    <span class="hljs-tag">&lt;<span class="hljs-title">body</span>&gt;</span>
<span class="linenos">25</span>	        <span class="hljs-comment">&lt;!--[if lt IE 7]&gt;
<span class="linenos">26</span>	            &lt;p class="browsehappy"&gt;You are using an &lt;strong&gt;outdated&lt;/strong&gt; browser. Please &lt;a href="http://browsehappy.com/"&gt;upgrade your browser&lt;/a&gt; to improve your experience.&lt;/p&gt;
<span class="linenos">27</span>	        &lt;![endif]--&gt;</span>
<span class="linenos">28</span>	
<span class="linenos">29</span>	        <span class="hljs-comment">&lt;!-- Add your site or application content here --&gt;</span>
<span class="linenos">30</span>	
<span class="linenos">31</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">id</span>=<span class="hljs-value">"fb-root"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">32</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span>&gt;</span><span class="clojure"><span class="hljs-list">(<span class="hljs-keyword">function</span><span class="hljs-list">(<span class="hljs-keyword">d</span>, s, id)</span> <span class="hljs-collection">{
<span class="linenos">33</span>	            var js, fjs = d.getElementsByTagName<span class="hljs-list">(<span class="hljs-keyword">s</span>)</span><span class="hljs-collection">[<span class="hljs-number">0</span>]</span><span class="hljs-comment">;</span>
<span class="linenos">34</span>	            if <span class="hljs-list">(<span class="hljs-keyword">d.getElementById</span><span class="hljs-list">(<span class="hljs-keyword">id</span>)</span>)</span> return;
<span class="linenos">35</span>	                js = d.createElement<span class="hljs-list">(<span class="hljs-keyword">s</span>)</span><span class="hljs-comment">; js.id = id;</span>
<span class="linenos">36</span>	                js.src = <span class="hljs-string">"//connect.facebook.net/en_GB/all.js#xfbml=1"</span><span class="hljs-comment">;</span>
<span class="linenos">37</span>	                fjs.parentNode.insertBefore<span class="hljs-list">(<span class="hljs-keyword">js</span>, fjs)</span><span class="hljs-comment">;</span>
<span class="linenos">38</span>	            }</span><span class="hljs-list">(<span class="hljs-keyword">document</span>, 'script', 'facebook-jssdk')</span>)</span><span class="hljs-comment">;</span></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">39</span>	
<span class="linenos">40</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"navbar navbar-static-top navbar-inverse"</span>&gt;</span>
<span class="linenos">41</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"navbar-inner"</span>&gt;</span>
<span class="linenos">42</span>	                <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"container"</span>&gt;</span>
<span class="linenos">43</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"btn btn-navbar"</span> <span class="hljs-attribute">data-toggle</span>=<span class="hljs-value">"collapse"</span> <span class="hljs-attribute">data-target</span>=<span class="hljs-value">".nav-collapse"</span>&gt;</span>
<span class="linenos">44</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"icon-bar"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span>
<span class="linenos">45</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"icon-bar"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span>
<span class="linenos">46</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"icon-bar"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span>
<span class="linenos">47</span>	                    <span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">48</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"brand"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/"</span>&gt;</span>My Django Blog<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">49</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"nav-collapse collapse"</span>&gt;</span>
<span class="linenos">50</span>	                    <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">51</span>	                <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">52</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">53</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">54</span>	
<span class="linenos">55</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"container"</span>&gt;</span>
<span class="linenos">56</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">block</span> header %}</span><span class="xml">
<span class="linenos">57</span>	                <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"page-header"</span>&gt;</span>
<span class="linenos">58</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">h1</span>&gt;</span>My Django Blog<span class="hljs-tag">&lt;/<span class="hljs-title">h1</span>&gt;</span>
<span class="linenos">59</span>	                <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">60</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endblock</span> %}</span><span class="xml">
<span class="linenos">61</span>	
<span class="linenos">62</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"row"</span>&gt;</span>
<span class="linenos">63</span>	                </span><span class="hljs-template_tag">{% <span class="hljs-keyword">block</span> content %}</span><span class="xml"></span><span class="hljs-template_tag">{% <span class="hljs-keyword">endblock</span> %}</span><span class="xml">
<span class="linenos">64</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">65</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">66</span>	
<span class="linenos">67</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"container footer"</span>&gt;</span>
<span class="linenos">68</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"row"</span>&gt;</span>
<span class="linenos">69</span>	                <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"span12"</span>&gt;</span>
<span class="linenos">70</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">p</span>&gt;</span>Copyright &amp;copy; </span><span class="hljs-template_tag">{% <span class="hljs-keyword">now</span> "Y" %}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">p</span>&gt;</span>
<span class="linenos">71</span>	                <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">72</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">73</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">74</span>	
<span class="linenos">75</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span> <span class="hljs-attribute">src</span>=<span class="hljs-value">"//ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js"</span>&gt;</span><span class="undefined"></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">76</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span>&gt;</span><span class="applescript">window.jQuery || document.<span class="hljs-command">write</span>('&lt;<span class="hljs-keyword">script</span> src=<span class="hljs-string">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/html5-boilerplate/js/vendor/jquery-1.10.2.min.js' %}</span><span class="xml"><span class="1c"><span class="hljs-string">"&gt;&lt;\/script&gt;')</span></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">77</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span> <span class="hljs-attribute">src</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/html5-boilerplate/js/plugins.js' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span><span class="undefined"></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">78</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span> <span class="hljs-attribute">src</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/bootstrap/dist/js/bootstrap.min.js' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span><span class="undefined"></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">79</span>	
<span class="linenos">80</span>	        <span class="hljs-comment">&lt;!-- Google Analytics: change UA-XXXXX-X to be your site's ID. --&gt;</span>
<span class="linenos">81</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span>&gt;</span><span class="scheme">
<span class="linenos">82</span>	            <span class="hljs-list">(<span class="hljs-keyword">function</span><span class="hljs-list">(<span class="hljs-keyword">b</span>,o,i,l,e,r)</span>{b.GoogleAnalyticsObject=l<span class="hljs-comment">;b[l]||(b[l]=</span>
<span class="linenos">83</span>	            function<span class="hljs-list">()</span>{<span class="hljs-list">(<span class="hljs-keyword">b</span><span class="hljs-list">[<span class="hljs-keyword">l</span>].q=b<span class="hljs-list">[<span class="hljs-keyword">l</span>].q||<span class="hljs-list">[]).push<span class="hljs-list">(<span class="hljs-keyword">arguments</span>)</span>})<span class="hljs-comment">;b[l].l=+new Date;</span>
<span class="linenos">84</span>	            e=o.createElement<span class="hljs-list">(<span class="hljs-keyword">i</span>)</span><span class="hljs-comment">;r=o.getElementsByTagName(i)[0];</span>
<span class="linenos">85</span>	            e.src=<span class="hljs-variable">'//www.google-analytics.com/analytics.js</span>'<span class="hljs-comment">;</span>
<span class="linenos">86</span>	            r.parentNode.insertBefore<span class="hljs-list">(<span class="hljs-keyword">e</span>,r)</span>}<span class="hljs-list">(<span class="hljs-keyword">window</span>,document,<span class="hljs-variable">'script</span>',<span class="hljs-variable">'ga</span>')</span>)<span class="hljs-comment">;</span>
<span class="linenos">87</span>	            ga<span class="hljs-list">(<span class="hljs-variable">'create</span>',<span class="hljs-variable">'UA-XXXXX-X</span>')</span><span class="hljs-comment">;ga('send','pageview');</span>
<span class="linenos">88</span>	        </span></span></span></span></span></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">89</span>	    <span class="hljs-tag">&lt;/<span class="hljs-title">body</span>&gt;</span>
<span class="linenos">90</span>	<span class="hljs-tag">&lt;/<span class="hljs-title">html</span>&gt;</span></span></code></pre>
<p>Now, if you run the development server and reload the page, your code will be highlighted using the default Pygments style. If you don’t like it, there are plenty to choose from. Run the following command:</p>
<pre><code class="hljs lang-bash singleline">$ pygmentize -L styles</code></pre>
<p>That will list the various styles available. For instance, let’s say we want to try the Tango style:</p>
<pre><code class="hljs lang-bash singleline">$ pygmentize -S tango <span class="hljs-operator">-f</span> html &gt; blogengine/static/css/code.css</code></pre>
<p>If you like the Monokai theme in Sublime Text, there’s a Pygments version of that:</p>
<pre><code class="hljs lang-bash singleline">$ pygmentize -S monokai <span class="hljs-operator">-f</span> html &gt; blogengine/static/css/code.css</code></pre>
<p>If you like the Solarized theme, that’s not bundled with Pygments, but can be installed separately:</p>
<pre><code class="hljs lang-bash singleline">$ pip install pygments-style-solarized</code></pre>
<p>Then run this for the light version:</p>
<pre><code class="hljs lang-bash singleline">$ pygmentize -S solarizedlight <span class="hljs-operator">-f</span> html &gt; blogengine/static/css/code.css</code></pre>
<p>And this for the dark version:</p>
<pre><code class="hljs lang-bash singleline">$ pygmentize -S solarizeddark <span class="hljs-operator">-f</span> html &gt; blogengine/static/css/code.css</code></pre>
<p>Pick one that you like - I’m going to go for the dark version of Solarized.</p>
<p>Note that this doesn’t actually change the background colour of the code blocks. You will therefore need to set this manually using the CSS file we created earlier. If you’re using Solarized Dark like I am, then this should set it correctly:</p>
<pre><code class="hljs lang-css"><span class="linenos">1</span>	<span class="hljs-tag">div</span><span class="hljs-class">.codehilite</span> <span class="hljs-tag">pre</span> <span class="hljs-rules">{
<span class="linenos">2</span>	    <span class="hljs-rule"><span class="hljs-attribute">background-color</span>:<span class="hljs-value"> <span class="hljs-hexcolor">#002b36</span></span></span>;
<span class="linenos">3</span>	}</span></code></pre>
<p>If you’re using Solarized Light, then this should be more appropriate:</p>
<pre><code class="hljs lang-css"><span class="linenos">1</span>	<span class="hljs-tag">div</span><span class="hljs-class">.codehilite</span> <span class="hljs-tag">pre</span> <span class="hljs-rules">{
<span class="linenos">2</span>	    <span class="hljs-rule"><span class="hljs-attribute">background-color</span>:<span class="hljs-value"> <span class="hljs-hexcolor">#fdf6e3</span></span></span>;
<span class="linenos">3</span>	}</span></code></pre>
<p>Or, if you’re using Monokai, black will do:</p>
<pre><code class="hljs lang-css"><span class="linenos">1</span>	<span class="hljs-tag">div</span><span class="hljs-class">.codehilite</span> <span class="hljs-tag">pre</span> <span class="hljs-rules">{
<span class="linenos">2</span>	    <span class="hljs-rule"><span class="hljs-attribute">background-color</span>:<span class="hljs-value"> <span class="hljs-hexcolor">#000000</span></span></span>;
<span class="linenos">3</span>	}</span></code></pre>
<p>With that done, let’s record the additional Pygments style:</p>
<pre><code class="hljs lang-bash singleline">$ pip freeze &gt; requirements.txt</code></pre>
<p>And commit our changes:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add requirements.txt blogengine/static/css/ templates/blogengine/includes/base.html
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Styled code with Solarized Dark'</span></code></pre>
<p>Let’s run our tests:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py jenkins
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	E
<span class="linenos">4</span>	======================================================================
<span class="linenos">5</span>	ERROR: blogengine.tests (unittest.loader.ModuleImportFailure)
<span class="linenos">6</span>	----------------------------------------------------------------------
<span class="linenos">7</span>	ImportError: Failed to import <span class="hljs-built_in">test</span> module: blogengine.tests
<span class="linenos">8</span>	Traceback (most recent call last):
<span class="linenos">9</span>	  File <span class="hljs-string">"/usr/local/Cellar/python/2.7.6_1/Frameworks/Python.framework/Versions/2.7/lib/python2.7/unittest/loader.py"</span>, line <span class="hljs-number">254</span>, <span class="hljs-keyword">in</span> _find_tests
<span class="linenos">10</span>	    module = self._get_module_from_name(name)
<span class="linenos">11</span>	  File <span class="hljs-string">"/usr/local/Cellar/python/2.7.6_1/Frameworks/Python.framework/Versions/2.7/lib/python2.7/unittest/loader.py"</span>, line <span class="hljs-number">232</span>, <span class="hljs-keyword">in</span> _get_module_from_name
<span class="linenos">12</span>	    __import__(name)
<span class="linenos">13</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">7</span>, <span class="hljs-keyword">in</span> &lt;module&gt;
<span class="linenos">14</span>	    import markdown
<span class="linenos">15</span>	ImportError: No module named markdown
<span class="linenos">16</span>	
<span class="linenos">17</span>	
<span class="linenos">18</span>	----------------------------------------------------------------------
<span class="linenos">19</span>	Ran <span class="hljs-number">1</span> <span class="hljs-built_in">test</span> <span class="hljs-keyword">in</span> <span class="hljs-number">0.001</span>s
<span class="linenos">20</span>	
<span class="linenos">21</span>	FAILED (errors=<span class="hljs-number">1</span>)
<span class="linenos">22</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>Whoops! We introduced an error here. If we take a look, we can see that the problem is on line 7, where we import the Markdown module. That makes sense, as we now use a different implementation of Markdown. Fortunately, in Python you can import modules with a different name, which makes this a breeze to fix. Change the line to:</p>
<pre><code class="hljs lang-python singleline"><span class="hljs-keyword">import</span> markdown2 <span class="hljs-keyword">as</span> markdown</code></pre>
<p>Now, if you run your tests, they should pass. It’s important that if a test breaks, you fix it as soon as possible and don’t put it off. Let’s commit these changes:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add blogengine/tests.py
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Fixed a broken test'</span></code></pre>
<p>Our syntax highlighting is now done! If you want to see it in action using the Solarized Dark theme, check out the copy of this blogging engine hosted <a href="http://blog.shellshocked.info/">here</a>.</p>
<h1 id="tidying-up">Tidying up</h1>
<p>Now that our blog is up and running on Heroku, it could do with a bit of work on the front end to make it look a bit nicer. If you recall, we’re using Bootstrap for our front end, so you may want to refer to the documentation for that to give you some ideas on how you want to style your blog.</p>
<p>Bootstrap has a nifty pager class for Next and Previous links, so let’s apply that to our post list template:</p>
<pre><code class="hljs lang-django"><span class="linenos">1</span>	<span class="xml"><span class="hljs-tag">&lt;<span class="hljs-title">ul</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"pager"</span>&gt;</span>
<span class="linenos">2</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> page_obj.has_previous %}</span><span class="xml">
<span class="linenos">3</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">li</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"previous"</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/</span></span></span><span class="hljs-variable">{{ page_obj.previous_page_number }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">/"</span>&gt;</span>Previous Page<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">li</span>&gt;</span>
<span class="linenos">4</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">5</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> page_obj.has_next %}</span><span class="xml">
<span class="linenos">6</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">li</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"next"</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/</span></span></span><span class="hljs-variable">{{ page_obj.next_page_number }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">/"</span>&gt;</span>Next Page<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">li</span>&gt;</span>
<span class="linenos">7</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">8</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">ul</span>&gt;</span></span></code></pre>
<p>Let’s also add labels to our categories and tags. We’ll also place our posts and other content inside proper columns:</p>
<pre><code class="hljs lang-django"><span class="linenos">1</span>	<span class="xml"></span><span class="hljs-template_tag">{% <span class="hljs-keyword">extends</span> "blogengine/includes/base.html" %}</span><span class="xml">
<span class="linenos">2</span>	
<span class="linenos">3</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">load</span> custom_markdown %}</span><span class="xml">
<span class="linenos">4</span>	
<span class="linenos">5</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">block</span> content %}</span><span class="xml">
<span class="linenos">6</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> object_list %}</span><span class="xml">
<span class="linenos">7</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">for</span> post <span class="hljs-keyword">in</span> object_list %}</span><span class="xml">
<span class="linenos">8</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"post col-md-12"</span>&gt;</span>
<span class="linenos">9</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">h1</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ post.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span></span><span class="hljs-variable">{{ post.title }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">h1</span>&gt;</span>
<span class="linenos">10</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">h3</span>&gt;</span></span><span class="hljs-variable">{{ post.pub_date }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">h3</span>&gt;</span>
<span class="linenos">11</span>	            </span><span class="hljs-variable">{{ post.text<span class="hljs-filter">|custom</span>_markdown }}</span><span class="xml">
<span class="linenos">12</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">13</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> post.category %}</span><span class="xml">
<span class="linenos">14</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"col-md-12"</span>&gt;</span>
<span class="linenos">15</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ post.category.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"label label-primary"</span>&gt;</span></span><span class="hljs-variable">{{ post.category.name }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">16</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">17</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">18</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> post.tags %}</span><span class="xml">
<span class="linenos">19</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"col-md-12"</span>&gt;</span>
<span class="linenos">20</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">for</span> tag <span class="hljs-keyword">in</span> post.tags.all %}</span><span class="xml">
<span class="linenos">21</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ tag.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"label label-success"</span>&gt;</span></span><span class="hljs-variable">{{ tag.name }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">22</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endfor</span> %}</span><span class="xml">
<span class="linenos">23</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">24</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">25</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endfor</span> %}</span><span class="xml">
<span class="linenos">26</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">else</span> %}</span><span class="xml">
<span class="linenos">27</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">p</span>&gt;</span>No posts found<span class="hljs-tag">&lt;/<span class="hljs-title">p</span>&gt;</span>
<span class="linenos">28</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">29</span>	
<span class="linenos">30</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">ul</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"pager"</span>&gt;</span>
<span class="linenos">31</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> page_obj.has_previous %}</span><span class="xml">
<span class="linenos">32</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">li</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"previous"</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/</span></span></span><span class="hljs-variable">{{ page_obj.previous_page_number }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">/"</span>&gt;</span>Previous Page<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">li</span>&gt;</span>
<span class="linenos">33</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">34</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> page_obj.has_next %}</span><span class="xml">
<span class="linenos">35</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">li</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"next"</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/</span></span></span><span class="hljs-variable">{{ page_obj.next_page_number }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">/"</span>&gt;</span>Next Page<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">li</span>&gt;</span>
<span class="linenos">36</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">37</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">ul</span>&gt;</span>
<span class="linenos">38</span>	
<span class="linenos">39</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endblock</span> %}</span><span class="xml"></span></code></pre>
<p>We’ll also want to tidy up the layout for our individual post pages:</p>
<pre><code class="hljs lang-django"><span class="linenos">1</span>	<span class="xml"></span><span class="hljs-template_tag">{% <span class="hljs-keyword">extends</span> "blogengine/includes/base.html" %}</span><span class="xml">
<span class="linenos">2</span>	
<span class="linenos">3</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">load</span> custom_markdown %}</span><span class="xml">
<span class="linenos">4</span>	
<span class="linenos">5</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">block</span> content %}</span><span class="xml">
<span class="linenos">6</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"post col-md-12"</span>&gt;</span>
<span class="linenos">7</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">h1</span>&gt;</span></span><span class="hljs-variable">{{ object.title }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">h1</span>&gt;</span>
<span class="linenos">8</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">h3</span>&gt;</span></span><span class="hljs-variable">{{ object.pub_date }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">h3</span>&gt;</span>
<span class="linenos">9</span>	        </span><span class="hljs-variable">{{ object.text<span class="hljs-filter">|custom</span>_markdown }}</span><span class="xml">
<span class="linenos">10</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">11</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> object.category %}</span><span class="xml">
<span class="linenos">12</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"col-md-12"</span>&gt;</span>
<span class="linenos">13</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ object.category.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"label label-primary"</span>&gt;</span></span><span class="hljs-variable">{{ object.category.name }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">14</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">15</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">16</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> object.tags %}</span><span class="xml">
<span class="linenos">17</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"col-md-12"</span>&gt;</span>
<span class="linenos">18</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">for</span> tag <span class="hljs-keyword">in</span> object.tags.all %}</span><span class="xml">
<span class="linenos">19</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ tag.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"label label-success"</span>&gt;</span></span><span class="hljs-variable">{{ tag.name }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">20</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endfor</span> %}</span><span class="xml">
<span class="linenos">21</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">22</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">23</span>	
<span class="linenos">24</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"col-md-12"</span>&gt;</span>
<span class="linenos">25</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">h4</span>&gt;</span>Comments<span class="hljs-tag">&lt;/<span class="hljs-title">h4</span>&gt;</span>
<span class="linenos">26</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"fb-comments"</span> <span class="hljs-attribute">data-href</span>=<span class="hljs-value">"http://</span></span></span><span class="hljs-variable">{{ object.site }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value"></span></span></span><span class="hljs-variable">{{ object.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span> <span class="hljs-attribute">data-width</span>=<span class="hljs-value">"470"</span> <span class="hljs-attribute">data-num-posts</span>=<span class="hljs-value">"10"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">27</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">28</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">29</span>	
<span class="linenos">30</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endblock</span> %}</span><span class="xml"></span></code></pre>
<p>Time to commit our changes:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add templates/blogengine/
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Tidied up post templates'</span></code></pre>
<p>With that done, let’s turn our attention to our base template. We’ll amend the header to collapse down at smaller screen widths. This is easy to do with Bootstrap:</p>
<pre><code class="hljs lang-django"><span class="linenos">1</span>	<span class="xml"><span class="hljs-doctype">&lt;!DOCTYPE html&gt;</span>
<span class="linenos">2</span>	<span class="hljs-comment">&lt;!--[if lt IE 7]&gt;      &lt;html class="no-js lt-ie9 lt-ie8 lt-ie7"&gt; &lt;![endif]--&gt;</span>
<span class="linenos">3</span>	<span class="hljs-comment">&lt;!--[if IE 7]&gt;         &lt;html class="no-js lt-ie9 lt-ie8"&gt; &lt;![endif]--&gt;</span>
<span class="linenos">4</span>	<span class="hljs-comment">&lt;!--[if IE 8]&gt;         &lt;html class="no-js lt-ie9"&gt; &lt;![endif]--&gt;</span>
<span class="linenos">5</span>	<span class="hljs-comment">&lt;!--[if gt IE 8]&gt;&lt;!--&gt;</span> <span class="hljs-tag">&lt;<span class="hljs-title">html</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"no-js"</span>&gt;</span> <span class="hljs-comment">&lt;!--&lt;![endif]--&gt;</span>
<span class="linenos">6</span>	    <span class="hljs-tag">&lt;<span class="hljs-title">head</span>&gt;</span>
<span class="linenos">7</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">meta</span> <span class="hljs-attribute">charset</span>=<span class="hljs-value">"utf-8"</span>&gt;</span>
<span class="linenos">8</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">meta</span> <span class="hljs-attribute">http-equiv</span>=<span class="hljs-value">"X-UA-Compatible"</span> <span class="hljs-attribute">content</span>=<span class="hljs-value">"IE=edge"</span>&gt;</span>
<span class="linenos">9</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">title</span>&gt;</span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">block</span> title %}</span><span class="xml">My Django Blog</span><span class="hljs-template_tag">{% <span class="hljs-keyword">endblock</span> %}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">title</span>&gt;</span>
<span class="linenos">10</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">meta</span> <span class="hljs-attribute">name</span>=<span class="hljs-value">"description"</span> <span class="hljs-attribute">content</span>=<span class="hljs-value">""</span>&gt;</span>
<span class="linenos">11</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">meta</span> <span class="hljs-attribute">name</span>=<span class="hljs-value">"viewport"</span> <span class="hljs-attribute">content</span>=<span class="hljs-value">"width=device-width, initial-scale=1"</span>&gt;</span>
<span class="linenos">12</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"alternate"</span> <span class="hljs-attribute">type</span>=<span class="hljs-value">"application/rss+xml"</span> <span class="hljs-attribute">title</span>=<span class="hljs-value">"Blog posts"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/feeds/posts/"</span> &gt;</span>
<span class="linenos">13</span>	
<span class="linenos">14</span>	        <span class="hljs-comment">&lt;!-- Place favicon.ico and apple-touch-icon.png in the root directory --&gt;</span>
<span class="linenos">15</span>	
<span class="linenos">16</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">load</span> staticfiles %}</span><span class="xml">
<span class="linenos">17</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/html5-boilerplate/css/normalize.css' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>
<span class="linenos">18</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/html5-boilerplate/css/main.css' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>
<span class="linenos">19</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/bootstrap/dist/css/bootstrap.min.css' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>
<span class="linenos">20</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/bootstrap/dist/css/bootstrap-theme.min.css' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>
<span class="linenos">21</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'css/main.css' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>
<span class="linenos">22</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">link</span> <span class="hljs-attribute">rel</span>=<span class="hljs-value">"stylesheet"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'css/code.css' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span>
<span class="linenos">23</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span> <span class="hljs-attribute">src</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/html5-boilerplate/js/vendor/modernizr-2.6.2.min.js' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span><span class="undefined"></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">24</span>	    <span class="hljs-tag">&lt;/<span class="hljs-title">head</span>&gt;</span>
<span class="linenos">25</span>	    <span class="hljs-tag">&lt;<span class="hljs-title">body</span>&gt;</span>
<span class="linenos">26</span>	        <span class="hljs-comment">&lt;!--[if lt IE 7]&gt;
<span class="linenos">27</span>	            &lt;p class="browsehappy"&gt;You are using an &lt;strong&gt;outdated&lt;/strong&gt; browser. Please &lt;a href="http://browsehappy.com/"&gt;upgrade your browser&lt;/a&gt; to improve your experience.&lt;/p&gt;
<span class="linenos">28</span>	        &lt;![endif]--&gt;</span>
<span class="linenos">29</span>	
<span class="linenos">30</span>	        <span class="hljs-comment">&lt;!-- Add your site or application content here --&gt;</span>
<span class="linenos">31</span>	
<span class="linenos">32</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">id</span>=<span class="hljs-value">"fb-root"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">33</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span>&gt;</span><span class="clojure"><span class="hljs-list">(<span class="hljs-keyword">function</span><span class="hljs-list">(<span class="hljs-keyword">d</span>, s, id)</span> <span class="hljs-collection">{
<span class="linenos">34</span>	            var js, fjs = d.getElementsByTagName<span class="hljs-list">(<span class="hljs-keyword">s</span>)</span><span class="hljs-collection">[<span class="hljs-number">0</span>]</span><span class="hljs-comment">;</span>
<span class="linenos">35</span>	            if <span class="hljs-list">(<span class="hljs-keyword">d.getElementById</span><span class="hljs-list">(<span class="hljs-keyword">id</span>)</span>)</span> return;
<span class="linenos">36</span>	                js = d.createElement<span class="hljs-list">(<span class="hljs-keyword">s</span>)</span><span class="hljs-comment">; js.id = id;</span>
<span class="linenos">37</span>	                js.src = <span class="hljs-string">"//connect.facebook.net/en_GB/all.js#xfbml=1"</span><span class="hljs-comment">;</span>
<span class="linenos">38</span>	                fjs.parentNode.insertBefore<span class="hljs-list">(<span class="hljs-keyword">js</span>, fjs)</span><span class="hljs-comment">;</span>
<span class="linenos">39</span>	            }</span><span class="hljs-list">(<span class="hljs-keyword">document</span>, 'script', 'facebook-jssdk')</span>)</span><span class="hljs-comment">;</span></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">40</span>	
<span class="linenos">41</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"navbar navbar-static-top navbar-inverse"</span>&gt;</span>
<span class="linenos">42</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"container-fluid"</span>&gt;</span>
<span class="linenos">43</span>	                <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"navbar-header"</span>&gt;</span>
<span class="linenos">44</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">button</span> <span class="hljs-attribute">type</span>=<span class="hljs-value">"button"</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"navbar-toggle"</span> <span class="hljs-attribute">data-toggle</span>=<span class="hljs-value">"collapse"</span> <span class="hljs-attribute">data-target</span>=<span class="hljs-value">"#header-nav"</span>&gt;</span>
<span class="linenos">45</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"icon-bar"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span>
<span class="linenos">46</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"icon-bar"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span>
<span class="linenos">47</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"icon-bar"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span>
<span class="linenos">48</span>	                    <span class="hljs-tag">&lt;/<span class="hljs-title">button</span>&gt;</span>
<span class="linenos">49</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"navbar-brand"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/"</span>&gt;</span>My Django Blog<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">50</span>	                <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">51</span>	                <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"collapse navbar-collapse"</span> <span class="hljs-attribute">id</span>=<span class="hljs-value">"header-nav"</span>&gt;</span>
<span class="linenos">52</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">ul</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"nav navbar-nav"</span>&gt;</span>
<span class="linenos">53</span>	                        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">load</span> flatpages %}</span><span class="xml">
<span class="linenos">54</span>	                        </span><span class="hljs-template_tag">{% get_flatpages <span class="hljs-keyword">as</span> flatpages %}</span><span class="xml">
<span class="linenos">55</span>	                        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">for</span> flatpage <span class="hljs-keyword">in</span> flatpages %}</span><span class="xml">
<span class="linenos">56</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">li</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ flatpage.url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span></span><span class="hljs-variable">{{ flatpage.title }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">li</span>&gt;</span>
<span class="linenos">57</span>	                        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endfor</span> %}</span><span class="xml">
<span class="linenos">58</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">li</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/feeds/posts/"</span>&gt;</span>RSS feed<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">li</span>&gt;</span>
<span class="linenos">59</span>	                    <span class="hljs-tag">&lt;/<span class="hljs-title">ul</span>&gt;</span>
<span class="linenos">60</span>	                <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">61</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">62</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">63</span>	
<span class="linenos">64</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"container"</span>&gt;</span>
<span class="linenos">65</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">block</span> header %}</span><span class="xml">
<span class="linenos">66</span>	                <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"page-header"</span>&gt;</span>
<span class="linenos">67</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">h1</span>&gt;</span>My Django Blog<span class="hljs-tag">&lt;/<span class="hljs-title">h1</span>&gt;</span>
<span class="linenos">68</span>	                <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">69</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endblock</span> %}</span><span class="xml">
<span class="linenos">70</span>	
<span class="linenos">71</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"row"</span>&gt;</span>
<span class="linenos">72</span>	                </span><span class="hljs-template_tag">{% <span class="hljs-keyword">block</span> content %}</span><span class="xml"></span><span class="hljs-template_tag">{% <span class="hljs-keyword">endblock</span> %}</span><span class="xml">
<span class="linenos">73</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">74</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">75</span>	
<span class="linenos">76</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"container footer"</span>&gt;</span>
<span class="linenos">77</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"row"</span>&gt;</span>
<span class="linenos">78</span>	                <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"span12"</span>&gt;</span>
<span class="linenos">79</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">p</span>&gt;</span>Copyright &amp;copy; </span><span class="hljs-template_tag">{% <span class="hljs-keyword">now</span> "Y" %}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">p</span>&gt;</span>
<span class="linenos">80</span>	                <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">81</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">82</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">83</span>	
<span class="linenos">84</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span> <span class="hljs-attribute">src</span>=<span class="hljs-value">"//ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js"</span>&gt;</span><span class="undefined"></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">85</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span>&gt;</span><span class="applescript">window.jQuery || document.<span class="hljs-command">write</span>('&lt;<span class="hljs-keyword">script</span> src=<span class="hljs-string">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/html5-boilerplate/js/vendor/jquery-1.10.2.min.js' %}</span><span class="xml"><span class="1c"><span class="hljs-string">"&gt;&lt;\/script&gt;')</span></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">86</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span> <span class="hljs-attribute">src</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/html5-boilerplate/js/plugins.js' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span><span class="undefined"></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">87</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span> <span class="hljs-attribute">src</span>=<span class="hljs-value">"</span></span></span><span class="hljs-template_tag">{% <span class="hljs-keyword">static</span> 'bower_components/bootstrap/dist/js/bootstrap.min.js' %}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span><span class="undefined"></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">88</span>	
<span class="linenos">89</span>	        <span class="hljs-comment">&lt;!-- Google Analytics: change UA-XXXXX-X to be your site's ID. --&gt;</span>
<span class="linenos">90</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span>&gt;</span><span class="scheme">
<span class="linenos">91</span>	            <span class="hljs-list">(<span class="hljs-keyword">function</span><span class="hljs-list">(<span class="hljs-keyword">b</span>,o,i,l,e,r)</span>{b.GoogleAnalyticsObject=l<span class="hljs-comment">;b[l]||(b[l]=</span>
<span class="linenos">92</span>	            function<span class="hljs-list">()</span>{<span class="hljs-list">(<span class="hljs-keyword">b</span><span class="hljs-list">[<span class="hljs-keyword">l</span>].q=b<span class="hljs-list">[<span class="hljs-keyword">l</span>].q||<span class="hljs-list">[]).push<span class="hljs-list">(<span class="hljs-keyword">arguments</span>)</span>})<span class="hljs-comment">;b[l].l=+new Date;</span>
<span class="linenos">93</span>	            e=o.createElement<span class="hljs-list">(<span class="hljs-keyword">i</span>)</span><span class="hljs-comment">;r=o.getElementsByTagName(i)[0];</span>
<span class="linenos">94</span>	            e.src=<span class="hljs-variable">'//www.google-analytics.com/analytics.js</span>'<span class="hljs-comment">;</span>
<span class="linenos">95</span>	            r.parentNode.insertBefore<span class="hljs-list">(<span class="hljs-keyword">e</span>,r)</span>}<span class="hljs-list">(<span class="hljs-keyword">window</span>,document,<span class="hljs-variable">'script</span>',<span class="hljs-variable">'ga</span>')</span>)<span class="hljs-comment">;</span>
<span class="linenos">96</span>	            ga<span class="hljs-list">(<span class="hljs-variable">'create</span>',<span class="hljs-variable">'UA-XXXXX-X</span>')</span><span class="hljs-comment">;ga('send','pageview');</span>
<span class="linenos">97</span>	        </span></span></span></span></span></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">98</span>	    <span class="hljs-tag">&lt;/<span class="hljs-title">body</span>&gt;</span>
<span class="linenos">99</span>	<span class="hljs-tag">&lt;/<span class="hljs-title">html</span>&gt;</span></span></code></pre>
<p>Here we’ve also added a link to our RSS feed in the header, and another in the page head to facilitate working with browsers that support RSS better.</p>
<p>Our blog’s now looking much more presentable. All that remains is to commit it:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add templates/blogengine/includes/base.html
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Amended base template'</span></code></pre>
<p>Now we can push it to GitHub and deploy it on Heroku:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git push origin master
<span class="linenos">2</span>	$ git push heroku master</code></pre>
<p>And we’re done! Don’t forget, you can grab this lesson with <code>git checkout lesson-6</code>.</p>
<p>Next time, we’ll cover search (I promise!).</p>
]]></description>
            <link>http://matthewdaly.co.uk/blog/2014/05/25/django-blog-tutorial-the-next-generation-part-6/</link>
            <guid isPermaLink="true">http://matthewdaly.co.uk/blog/2014/05/25/django-blog-tutorial-the-next-generation-part-6/</guid>
            <pubDate>Sun, 25 May 2014 16:23:28 GMT</pubDate>
        </item>
        <item>
            <title><![CDATA[Django blog tutorial - the next generation - part 5]]></title>
            <description><![CDATA[<p>Hello again! I was originally planning to cover implementing a search system, adding more feeds, and tidying up the front end in this instalment. However, I felt it was time for a change of pace, so instead, we’re going to look at:</p>
<ul>
<li>Checking code coverage and getting it to 100%</li>
<li>Using continuous integration</li>
<li>Deploying to Heroku</li>
</ul>
<p>Don’t worry, the original lesson isn’t going anywhere. We’ll still be implementing all of that later on, but today is the day we get your Django blog up and running on the web. That way, you can get a better idea of how Django works in the wild, and you have something concrete to show for your efforts.</p>
<h1 id="continuous-integration">Continuous integration</h1>
<p>If you’re not familiar with continuous integration, it’s basically a process that carries out some tasks automatically when you push a new commit to the repository. These tasks may include running unit tests, linting your code, and checking it to see what percentage of the code base is covered by the tests (after all, if your tests don’t actually cover every scenario, then there’s more of a chance that something might slip through the net. It’s also possible to implement hooks to automatically deploy your application only if the tests pass.</p>
<p>Typically, you will have a continuous integration server running somewhere that regularly polls your Git repository for changes, and when it finds a new commit, will check it out and run the tests (or whatever other task you configure it to). One of the most popular continuous integration servers around is <a href="http://jenkins-ci.org/">Jenkins</a> - I use it at work and can highly recommend it. However, we aren’t going to cover using Jenkins here because setting it up is quite a big deal and it really is best kept on a server of its own (although feel free to use it if you prefer). Instead, we’re going to use <a href="https://travis-ci.org/">Travis CI</a>, which integrates nicely with GitHub and is free for open source projects. If you don’t mind your code being publicly available on GitHub, then Travis is a really great way to dip your toe into continuous integration.</p>
<p>NB: You don’t have to use continuous integration at all if you don’t want to - this isn’t a big project so you don’t really need it. If you don’t want to put your code on GitHub, then feel free to just follow along and not bother pushing your code to GitHub and configuring Travis.</p>
<h1 id="code-coverage">Code coverage</h1>
<p>As mentioned above, code coverage is a measure of the percentage of the code that is covered by tests. While not infallible, it’s a fairly good guide to how comprehensive your tests are. If you have 100% code coverage, you can be fairly confident that your tests are comprehensive enough to catch most errors, so it’s a good rule of thumb to aim for 100% test coverage on a project.</p>
<p>So how do we check our test coverage? The <code>coverage</code> Python module is the most common tool for this. There’s also a handy Django module called <code>django-jenkins</code>, which is designed to work with Jenkins, but can be used with any continuous integration server, that can not only run your tests, but also check code coverage at the same time. So, make sure your virtualenv is up and running:</p>
<pre><code class="hljs lang-bash singleline">$ <span class="hljs-built_in">source</span> venv/bin/activate</code></pre>
<p>Then, run the following command:</p>
<pre><code class="hljs lang-bash singleline">$ pip install coverage django-jenkins</code></pre>
<p>Once that’s done, add these to our requirements file:</p>
<pre><code class="hljs lang-bash singleline">$ pip freeze &gt; requirements.txt</code></pre>
<p>We now need to configure our Django project to use <code>django-jenkins</code>. Add the following to the bottom of the settings file:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	INSTALLED_APPS += (<span class="hljs-string">'django_jenkins'</span>,)
<span class="linenos">2</span>	JENKINS_TASKS = (
<span class="linenos">3</span>	    <span class="hljs-string">'django_jenkins.tasks.run_pylint'</span>,
<span class="linenos">4</span>	    <span class="hljs-string">'django_jenkins.tasks.with_coverage'</span>,
<span class="linenos">5</span>	)
<span class="linenos">6</span>	PROJECT_APPS = [<span class="hljs-string">'blogengine'</span>]</code></pre>
<p>This adds <code>django-jenkins</code> to our installed apps and tells it to include two additional tasks, besides running the tests. The first task runs Pylint to check our code quality (but we aren’t really concerned about that at this point). The second checks the coverage. Finally, we tell <code>django-jenkins</code> that the <code>blogengine</code> app is the only one to be tested.</p>
<p>You’ll also want to add the following lines to your <code>.gitignore</code>:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	reports/
<span class="linenos">2</span>	htmlcov/</code></pre>
<p>These are the reports generated by <code>django-jenkins</code>, and should not be kept under version control. With that done, it’s time to commit:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add .gitignore django_tutorial_blog_ng/ requirements.txt
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Added coverage checking using django-jenkins'</span></code></pre>
<p>Now, let’s run our tests. From now on, you’ll use the following command to run your tests:</p>
<pre><code class="hljs lang-bash singleline">$ python manage.py jenkins</code></pre>
<p>This ensures we check the coverage at the same time. Now, you’ll notice that the <code>reports</code> folder has been created, and it will contain three files, including one called <code>coverage.xml</code>. However, XML isn’t a very friendly format. Happily, we can easily generate reports in HTML instead:</p>
<pre><code class="hljs lang-bash singleline">$ python manage.py jenkins --coverage-html-report=htmlcov</code></pre>
<p>Running this command will create another folder called <code>htmlcov/</code>, and in here you will find your report, nicely formatted as HTML. Open up <code>index.html</code> in your web browser and you should see a file-by-file breakdown of your code coverage. Nice, huh?</p>
<p>Now, if your code so far is largely identical to mine, you’ll notice that the model and view files don’t have 100% coverage yet. If you click on each one, you’ll see a handy line-by-line breakdown of the test coverage for each file. You’ll notice that in the views file, the areas of the code for when tags and categories don’t exist are highlighted in pink - this tells you that these lines of code are never executed during the tests. So let’s fix that.</p>
<p>First, our template needs to be able to handle empty lists.</p>
<pre><code class="hljs lang-django"><span class="linenos">1</span>	<span class="xml"></span><span class="hljs-template_tag">{% <span class="hljs-keyword">extends</span> "blogengine/includes/base.html" %}</span><span class="xml">
<span class="linenos">2</span>	
<span class="linenos">3</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">load</span> custom_markdown %}</span><span class="xml">
<span class="linenos">4</span>	
<span class="linenos">5</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">block</span> content %}</span><span class="xml">
<span class="linenos">6</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> object_list %}</span><span class="xml">
<span class="linenos">7</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">for</span> post <span class="hljs-keyword">in</span> object_list %}</span><span class="xml">
<span class="linenos">8</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"post"</span>&gt;</span>
<span class="linenos">9</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">h1</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ post.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span></span><span class="hljs-variable">{{ post.title }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">h1</span>&gt;</span>
<span class="linenos">10</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">h3</span>&gt;</span></span><span class="hljs-variable">{{ post.pub_date }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">h3</span>&gt;</span>
<span class="linenos">11</span>	            </span><span class="hljs-variable">{{ post.text<span class="hljs-filter">|custom</span>_markdown }}</span><span class="xml">
<span class="linenos">12</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">13</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ post.category.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span></span><span class="hljs-variable">{{ post.category.name }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">14</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">for</span> tag <span class="hljs-keyword">in</span> post.tags.all %}</span><span class="xml">
<span class="linenos">15</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ tag.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span></span><span class="hljs-variable">{{ tag.name }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">16</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endfor</span> %}</span><span class="xml">
<span class="linenos">17</span>	            </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endfor</span> %}</span><span class="xml">
<span class="linenos">18</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">else</span> %}</span><span class="xml">
<span class="linenos">19</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">p</span>&gt;</span>No posts found<span class="hljs-tag">&lt;/<span class="hljs-title">p</span>&gt;</span>
<span class="linenos">20</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">21</span>	
<span class="linenos">22</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> page_obj.has_previous %}</span><span class="xml">
<span class="linenos">23</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/</span></span></span><span class="hljs-variable">{{ page_obj.previous_page_number }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">/"</span>&gt;</span>Previous Page<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">24</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">25</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> page_obj.has_next %}</span><span class="xml">
<span class="linenos">26</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/</span></span></span><span class="hljs-variable">{{ page_obj.next_page_number }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">/"</span>&gt;</span>Next Page<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">27</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">28</span>	
<span class="linenos">29</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endblock</span> %}</span><span class="xml"></span></code></pre>
<p>Let’s commit the changes:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add templates/blogengine/post_list.html
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Added a "No posts found" message to post list template'</span></code></pre>
<p>Next, we need to write tests to check that we get a “No posts found” message when we view a tag or category that does not exist. Add the following methods to the class <code>PostViewTest()</code>:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_nonexistent_category_page</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">2</span>	        category_url = <span class="hljs-string">'/category/blah/'</span>
<span class="linenos">3</span>	        response = self.client.get(category_url)
<span class="linenos">4</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">5</span>	        self.assertTrue(<span class="hljs-string">'No posts found'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">6</span>	
<span class="linenos">7</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_nonexistent_tag_page</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">8</span>	        tag_url = <span class="hljs-string">'/tag/blah/'</span>
<span class="linenos">9</span>	        response = self.client.get(tag_url)
<span class="linenos">10</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">11</span>	        self.assertTrue(<span class="hljs-string">'No posts found'</span> <span class="hljs-keyword">in</span> response.content)</code></pre>
<p>Now, let’s run our tests again:</p>
<pre><code class="hljs lang-bash singleline">$ python manage.py jenkins --coverage-html-report=htmlcov</code></pre>
<p>Assuming they all pass as expected, then your coverage reports will be regenerated. If you reload the coverage report, you should see that your views now have 100% test coverage. Let’s commit again:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add blogengine/tests.py
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Views file now has 100% coverage'</span></code></pre>
<p>Now, our models still don’t have 100% coverage yet. If you look at the breakdown for <code>models.py</code>, you’ll see that the line <code>if not self.slug:</code> has only partial coverage, because we missed out setting a slug for the categories and tags in our test. So, let’s fix that. In <code>PostTest()</code>, amend the <code>test_create_category()</code> and <code>test_create_tag()</code> methods as follows:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_create_category</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">2</span>	        <span class="hljs-comment"># Create the category</span>
<span class="linenos">3</span>	        category = Category()
<span class="linenos">4</span>	
<span class="linenos">5</span>	        <span class="hljs-comment"># Add attributes</span>
<span class="linenos">6</span>	        category.name = <span class="hljs-string">'python'</span>
<span class="linenos">7</span>	        category.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">8</span>	        category.slug = <span class="hljs-string">'python'</span>
<span class="linenos">9</span>	
<span class="linenos">10</span>	        <span class="hljs-comment"># Save it</span>
<span class="linenos">11</span>	        category.save()
<span class="linenos">12</span>	
<span class="linenos">13</span>	        <span class="hljs-comment"># Check we can find it</span>
<span class="linenos">14</span>	        all_categories = Category.objects.all()
<span class="linenos">15</span>	        self.assertEquals(len(all_categories), <span class="hljs-number">1</span>)
<span class="linenos">16</span>	        only_category = all_categories[<span class="hljs-number">0</span>]
<span class="linenos">17</span>	        self.assertEquals(only_category, category)
<span class="linenos">18</span>	
<span class="linenos">19</span>	        <span class="hljs-comment"># Check attributes</span>
<span class="linenos">20</span>	        self.assertEquals(only_category.name, <span class="hljs-string">'python'</span>)
<span class="linenos">21</span>	        self.assertEquals(only_category.description, <span class="hljs-string">'The Python programming language'</span>)
<span class="linenos">22</span>	        self.assertEquals(only_category.slug, <span class="hljs-string">'python'</span>)
<span class="linenos">23</span>	
<span class="linenos">24</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_create_tag</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">25</span>	        <span class="hljs-comment"># Create the tag</span>
<span class="linenos">26</span>	        tag = Tag()
<span class="linenos">27</span>	
<span class="linenos">28</span>	        <span class="hljs-comment"># Add attributes</span>
<span class="linenos">29</span>	        tag.name = <span class="hljs-string">'python'</span>
<span class="linenos">30</span>	        tag.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">31</span>	        tag.slug = <span class="hljs-string">'python'</span>
<span class="linenos">32</span>	
<span class="linenos">33</span>	        <span class="hljs-comment"># Save it</span>
<span class="linenos">34</span>	        tag.save()
<span class="linenos">35</span>	
<span class="linenos">36</span>	        <span class="hljs-comment"># Check we can find it</span>
<span class="linenos">37</span>	        all_tags = Tag.objects.all()
<span class="linenos">38</span>	        self.assertEquals(len(all_tags), <span class="hljs-number">1</span>)
<span class="linenos">39</span>	        only_tag = all_tags[<span class="hljs-number">0</span>]
<span class="linenos">40</span>	        self.assertEquals(only_tag, tag)
<span class="linenos">41</span>	
<span class="linenos">42</span>	        <span class="hljs-comment"># Check attributes</span>
<span class="linenos">43</span>	        self.assertEquals(only_tag.name, <span class="hljs-string">'python'</span>)
<span class="linenos">44</span>	        self.assertEquals(only_tag.description, <span class="hljs-string">'The Python programming language'</span>)
<span class="linenos">45</span>	        self.assertEquals(only_tag.slug, <span class="hljs-string">'python'</span>)</code></pre>
<p>Run the tests again:</p>
<pre><code class="hljs lang-bash singleline">$ python manage.py jenkins --coverage-html-report=htmlcov</code></pre>
<p>Then refresh your coverage page, and we should have hit 100% coverage. Excellent news! This means that we can be confident that if any problems get introduced in future, we can pick them up easily. To demonstrate this, let’s upgrade our Django install to the latest version and check everything still works as expected:</p>
<pre><code class="hljs lang-bash singleline">$ pip install Django --upgrade</code></pre>
<p>This will upgrade the copy of Django in our virtualenv to the latest version. Then we can run our tests again:</p>
<pre><code class="hljs lang-bash singleline">$ python manage.py jenkins --coverage-html-report=htmlcov</code></pre>
<p>Our tests should still pass, indicating that the upgrade to our Django version does not appear to have broken any functionality. Let’s update our <code>requirements.txt</code>:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ pip freeze &gt; requirements.txt
<span class="linenos">2</span>	$ git add requirements.txt
<span class="linenos">3</span>	$ git commit -m <span class="hljs-string">'Upgraded Django version'</span></code></pre>
<h1 id="preparing-our-web-app-for-deployment-to-heroku">Preparing our web app for deployment to Heroku</h1>
<p>As mentioned previously, I’m going to assume you plan to deploy your site on <a href="https://www.heroku.com/">Heroku</a>. It has good Django support, and you can quite happily host a blog there using their free tariff. If you’d prefer to use another hosting provider, then you should be able to adapt these instructions accordingly.</p>
<p>Now, so far we’ve used SQLite as our database for development purposes. However, SQLite isn’t really suitable for production purposes. Heroku provide a Postgresql database for each web app, so we will use that. To configure it, open up <code>settings.py</code> and amend the database configuration section to look like this:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	DATABASES = {
<span class="linenos">2</span>	    <span class="hljs-string">'default'</span>: {
<span class="linenos">3</span>	        <span class="hljs-string">'ENGINE'</span>: <span class="hljs-string">'django.db.backends.'</span>,
<span class="linenos">4</span>	        <span class="hljs-string">'NAME'</span>: <span class="hljs-string">''</span>,
<span class="linenos">5</span>	    }
<span class="linenos">6</span>	}</code></pre>
<p>Then, add the following at the end of the file:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	
<span class="linenos">2</span>	<span class="hljs-comment"># Heroku config</span>
<span class="linenos">3</span>	<span class="hljs-comment"># Parse database configuration from $DATABASE_URL</span>
<span class="linenos">4</span>	<span class="hljs-keyword">import</span> dj_database_url
<span class="linenos">5</span>	DATABASES[<span class="hljs-string">'default'</span>] =  dj_database_url.config(default=<span class="hljs-string">"sqlite:///db.sqlite3"</span>)
<span class="linenos">6</span>	
<span class="linenos">7</span>	<span class="hljs-comment"># Honor the 'X-Forwarded-Proto' header for request.is_secure()</span>
<span class="linenos">8</span>	SECURE_PROXY_SSL_HEADER = (<span class="hljs-string">'HTTP_X_FORWARDED_PROTO'</span>, <span class="hljs-string">'https'</span>)
<span class="linenos">9</span>	
<span class="linenos">10</span>	<span class="hljs-comment"># Allow all host headers</span>
<span class="linenos">11</span>	ALLOWED_HOSTS = [<span class="hljs-string">'*'</span>]
<span class="linenos">12</span>	
<span class="linenos">13</span>	<span class="hljs-comment"># Static asset configuration</span>
<span class="linenos">14</span>	STATIC_ROOT = <span class="hljs-string">'staticfiles'</span>
<span class="linenos">15</span>	
<span class="linenos">16</span>	STATICFILES_DIRS = (
<span class="linenos">17</span>	    os.path.join(BASE_DIR, <span class="hljs-string">'static'</span>),
<span class="linenos">18</span>	)</code></pre>
<p>A little explanation is called for. Remember when we first started out, we installed the <code>django-toolbelt</code> package, which included <code>dj-database-url</code>? Well, here we use the <code>dj_database_url</code> module to get the database from an environment variable set on Heroku. We set a default value so as to fall back to SQLite when that variable is not set. The other settings are required by Heroku.</p>
<p>You may want to run your tests again to ensure everything is still working before committing:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add django_tutorial_blog_ng/settings.py
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Amended settings to work on desktop and Heroku'</span></code></pre>
<h1 id="setting-up-continuous-integration-and-coverage">Setting up Continuous Integration and coverage</h1>
<p>Now, as mentioned previously, I’ll be demonstrating how to set up Travis CI for our project. Travis CI is only free for open-source projects, so if you don’t want to make your code publicly accessible, you may want to use Jenkins instead. I’ll leave setting that up and running it as an exercise for the reader, but I recommend a plugin called Cobertura which allows you to publish details of your code’s test coverage.</p>
<p>Unfortunately, Travis CI doesn’t have the capability to publish your coverage results. Fortunately, you can use <a href="https://coveralls.io/">Coveralls.io</a> to pick up the slack. Like Travis, it’s free for open-source projects, and if you host your code on GitHub, it’s pretty easy to use.</p>
<p>You can find instructions on setting up a project on Travis CI <a href="http://docs.travis-ci.com/user/getting-started/">here</a>. Once you’ve configured it, you’ll need to set up your <code>.travis.yml</code> file. This is a simple text file that tells Travis CI how to run your tests. Put the following content in the file:</p>
<pre><code class="hljs lang-yml"><span class="linenos">1</span>	<span class="hljs-label">language:</span> python
<span class="linenos">2</span>	<span class="hljs-label">python:</span>
<span class="linenos">3</span>	- <span class="hljs-string">"2.7"</span>
<span class="linenos">4</span>	<span class="hljs-preprocessor"># command to install dependencies</span>
<span class="linenos">5</span>	<span class="hljs-label">install:</span> <span class="hljs-string">"pip install -r requirements.txt"</span>
<span class="linenos">6</span>	<span class="hljs-preprocessor"># command to run tests</span>
<span class="linenos">7</span>	<span class="hljs-label">script:</span> coverage run --include=<span class="hljs-string">"blogengine/*"</span> --omit=<span class="hljs-string">"blogengine/migrations/*"</span> manage.py test blogengine
<span class="linenos">8</span>	<span class="hljs-label">after_success:</span>
<span class="linenos">9</span>	    coveralls</code></pre>
<p>Now, this tells Travis that this is a Python application, and we should be using Python 2.7. Please note you can test against multiple versions of Python if you wish - just add another line with the Python version you want to test against.</p>
<p>Then, we see that we use Pip to install our requirements. Finally we run our tests with Coverage, in order to generate coverage data, and afterwards call <code>coveralls</code> to pass the coverage data to Coveralls.io.</p>
<p>We also need to install the <code>coveralls</code> module:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ pip install coveralls
<span class="linenos">2</span>	$ pip freeze &gt; requirements.txt</code></pre>
<p>And we need to keep our coverage data out of version control:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	env/
<span class="linenos">2</span>	*.pyc
<span class="linenos">3</span>	db.sqlite3
<span class="linenos">4</span>	blogengine/static/bower_components/
<span class="linenos">5</span>	reports/
<span class="linenos">6</span>	htmlcov/
<span class="linenos">7</span>	.coverage</code></pre>
<p>You’ll also want to set up your project on Coveralls, as described <a href="https://coveralls.io/docs">here</a> - please note that this requires your project be in a public GitHub repository.</p>
<p>With that done, let’s commit our changes:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add .gitignore .travis.yml requirements.txt
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Added Travis config file and Coveralls support'</span></code></pre>
<p>With that done, assuming you have Travis CI and Coveralls configured, and your code is already hosted on GitHub, then you should be able to just push your code up to trigger the build:</p>
<pre><code class="hljs lang-bash singleline">$ git push origin master</code></pre>
<p>If you keep an eye on Travis in your browser, you can watch what happens as your tests are run. If for any reason the build fails, then it shouldn’t be too hard to figure out what has gone wrong using the documentation for Travis and Coveralls - both services are pretty easy to use.</p>
<p>Congratulations - you’re now using Continuous Integration! That wasn’t so hard, was it? Now, every time you push to GitHub, Travis will run your tests, and you’ll get an email if they fail, giving you early warning of any problems, and you can check your coverage at the same time. Both Travis and Coveralls offer badges that you can place in your README file on GitHub to show off your coverage and build status - feel free to add these to your repo.</p>
<p>You may want to try making a change that breaks your tests and committing it, then pushing it up, so that you can see what happens when the build breaks.</p>
<h1 id="deploying-to-heroku">Deploying to Heroku</h1>
<p>Our final task today is deploying our blog to Heroku so we can see it in action. First of all, if you don’t already have an account with Heroku, you’ll need to sign up <a href="https://www.heroku.com/">here</a>. You should also be prompted to install the Heroku toolbelt. Once that’s done, run the following command:</p>
<pre><code class="hljs lang-bash singleline">$ heroku login</code></pre>
<p>You’ll be prompted for your credentials - enter these and you should be able to log in successfully.</p>
<p>Now, in order to run our Django app on Heroku, we’ll need to add a <code>Procfile</code> to tell Heroku what command to run in order to start your app. In this case, we will be using Gunicorn as our web server. Assuming our project is called <code>django_tutorial_blog_ng</code>, this is what you need to put in this file:</p>
<pre><code class="hljs lang-bash singleline">web: gunicorn django_tutorial_blog_ng.wsgi</code></pre>
<p>That tells Heroku that the file we need to run for this application is <code>django_tutorial_blog_ng/wsgi.py</code>. To test it, run the following command:</p>
<pre><code class="hljs lang-bash singleline">$ foreman start</code></pre>
<p>That will start our web server on port 5000, using Gunicorn rather than the Django development server. You should be able to see it in action <a href="http://127.0.0.1:5000/">here</a>, but you’ll notice a very serious issue - namely that the static files aren’t being served. Now, Django has a command called <code>collectstatic</code> that collects all the static files and drops them into one convenient folder. Heroku will run this command automatically, so we need to ensure our static files are available. Amend your <code>.gitignore</code> file so it no longer excludes our static files:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	venv/
<span class="linenos">2</span>	*.pyc
<span class="linenos">3</span>	db.sqlite3
<span class="linenos">4</span>	reports/
<span class="linenos">5</span>	htmlcov/
<span class="linenos">6</span>	.coverage</code></pre>
<p>We also need to amend our wsgi.py to serve static files:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-keyword">import</span> os
<span class="linenos">2</span>	os.environ.setdefault(<span class="hljs-string">"DJANGO_SETTINGS_MODULE"</span>, <span class="hljs-string">"django_tutorial_blog_ng.settings"</span>)
<span class="linenos">3</span>	
<span class="linenos">4</span>	<span class="hljs-keyword">from</span> django.core.wsgi <span class="hljs-keyword">import</span> get_wsgi_application
<span class="linenos">5</span>	<span class="hljs-keyword">from</span> dj_static <span class="hljs-keyword">import</span> Cling
<span class="linenos">6</span>	
<span class="linenos">7</span>	application = Cling(get_wsgi_application())</code></pre>
<p>This should solve our problem. Let’s commit our changes:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add django_tutorial_blog_ng/wsgi.py Procfile blogengine/static/ .gitignore
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Configured app for deployment on Heroku'</span></code></pre>
<p>Now we’re ready to deploy our app!</p>
<h1 id="deployment">Deployment</h1>
<p>Every Heroku app needs a unique name. If you don’t specify one, then Heroku will generate one for you. Your app will have the domain name <code>appname.herokuapp.com</code> - however, if you already have a domain name lined up for your blog, you can point that domain name at the Heroku app if you wish. I’m going to deploy mine with the name <code>blog-shellshocked-info</code>.</p>
<p>You also need to consider where you want to deploy it. Heroku has two regions - North America and EU. By default it will deploy to North America, but as I’m in the EU, that’s where I want to deploy my app to - obviously if you’re in the Americas, you may be better off sticking with North America.</p>
<p>So, let’s create our app. Here’s the command you need to run:</p>
<pre><code class="hljs lang-bash singleline">$ heroku apps:create blog-shellshocked-info --region eu</code></pre>
<p>You will want to change the app name, or remove it entirely if you’re happy for Heroku to generate one for you. If you want to host it in North America, drop the <code>--region eu</code> section.</p>
<p>Once completed, this will have created your new app, and added a Git remote for it, but will not have deployed it. To deploy your app, run this command:</p>
<pre><code class="hljs lang-bash singleline">$ git push heroku master</code></pre>
<p>That will push your code up to Heroku. Please note that building the app may take a little while. Once it’s done, you can run <code>heroku open</code> to open it in your web browser. You should see an error message stating that the relation <code>blogengine_post</code> does not exist. That’s because we need to create our database structure. Heroku allows you to easily run commands on your app with <code>heroku run</code>, so let’s create our database and run our migrations:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ heroku run python manage.py syncdb
<span class="linenos">2</span>	$ heroku run python manage.py migrate</code></pre>
<p>These are exactly the same commands you would run locally to create your database, but prefaced with <code>heroku run</code> so that they get run by Heroku. As usual, you will be prompted to create a superuser - you’ll want to do this so you can log into the admin.</p>
<p>That’s all for today! We’ve finally got our site up and running on Heroku, and set up continuous integration so our tests will get run for us. You can see an example of the site working <a href="http://blog.shellshocked.info/">here</a>. As usual, you can check out the latest version of the code with <code>git checkout lesson-5</code>. If you’d like a homework assignment, then take a look at <a href="http://docs.travis-ci.com/user/deployment/heroku/">automating deployment to Heroku on successful builds</a> and see if you can get it set up successfully.</p>
<p>Next time around, we’ll get back to implementing our search and tidying up the front end. See you then!</p>
]]></description>
            <link>http://matthewdaly.co.uk/blog/2014/05/24/django-blog-tutorial-the-next-generation-part-5/</link>
            <guid isPermaLink="true">http://matthewdaly.co.uk/blog/2014/05/24/django-blog-tutorial-the-next-generation-part-5/</guid>
            <pubDate>Sat, 24 May 2014 19:15:54 GMT</pubDate>
        </item>
        <item>
            <title><![CDATA[Django blog tutorial - the next generation - part 4]]></title>
            <description><![CDATA[<p>Hello again! As promised, in this instalment we’ll implement categories and tags, as well as an RSS feed.</p>
<p>As usual, we need to switch into our virtualenv:</p>
<pre><code class="hljs lang-bash singleline">$ <span class="hljs-built_in">source</span> venv/bin/activate</code></pre>
<h2 id="categories">Categories</h2>
<p>It’s worth taking a little time at this point to set out what we mean by categories and tags in this case, as the two can be very similar. In this case, we’ll use the following criteria:</p>
<ul>
<li>A post can have only one category, or none, but a category can be applied to any number of posts</li>
<li>A post can have any number of tags, and a tag can be applied to any number of posts</li>
</ul>
<p>If you’re not too familiar with relational database theory, the significance of this may not be apparent, so here’s a quick explanation. Because the categories are limited to one per post, the relationship between a post and a category is known as <em>one-to-many</em>. In other words, one post can only have one category, but one category can have many posts. You can therefore define the categories in one table in your database, and refer to them by their ID (the reference to the category in the post table is referred to as a <em>foreign key</em>).</p>
<p>As usual, we will write a test first. Open up <code>blogengine/tests.py</code> and edit the line importing the <code>Post</code> model as follows:</p>
<pre><code class="hljs lang-python singleline"><span class="hljs-keyword">from</span> blogengine.models <span class="hljs-keyword">import</span> Post, Category</code></pre>
<p>Also, add the following method before <code>test_create_post</code>:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_create_category</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">2</span>	        <span class="hljs-comment"># Create the category</span>
<span class="linenos">3</span>	        category = Category()
<span class="linenos">4</span>	
<span class="linenos">5</span>	        <span class="hljs-comment"># Add attributes</span>
<span class="linenos">6</span>	        category.name = <span class="hljs-string">'python'</span>
<span class="linenos">7</span>	        category.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">8</span>	
<span class="linenos">9</span>	        <span class="hljs-comment"># Save it</span>
<span class="linenos">10</span>	        category.save()
<span class="linenos">11</span>	
<span class="linenos">12</span>	        <span class="hljs-comment"># Check we can find it</span>
<span class="linenos">13</span>	        all_categories = Category.objects.all()
<span class="linenos">14</span>	        self.assertEquals(len(all_categories), <span class="hljs-number">1</span>)
<span class="linenos">15</span>	        only_category = all_categories[<span class="hljs-number">0</span>]
<span class="linenos">16</span>	        self.assertEquals(only_category, category)
<span class="linenos">17</span>	
<span class="linenos">18</span>	        <span class="hljs-comment"># Check attributes</span>
<span class="linenos">19</span>	        self.assertEquals(only_category.name, <span class="hljs-string">'python'</span>)
<span class="linenos">20</span>	        self.assertEquals(only_category.description, <span class="hljs-string">'The Python programming language'</span>)</code></pre>
<p>This test checks that we can create categories. But categories aren’t much use unless we can actually apply them to our posts. So we need to edit our <code>Post</code> model as well, and to do so we need to have a test in place first. Edit the <code>test_create_post</code> method as follows:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_create_post</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">2</span>	        <span class="hljs-comment"># Create the category</span>
<span class="linenos">3</span>	        category = Category()
<span class="linenos">4</span>	        category.name = <span class="hljs-string">'python'</span>
<span class="linenos">5</span>	        category.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">6</span>	        category.save()
<span class="linenos">7</span>	
<span class="linenos">8</span>	        <span class="hljs-comment"># Create the author</span>
<span class="linenos">9</span>	        author = User.objects.create_user(<span class="hljs-string">'testuser'</span>, <span class="hljs-string">'user@example.com'</span>, <span class="hljs-string">'password'</span>)
<span class="linenos">10</span>	        author.save()
<span class="linenos">11</span>	
<span class="linenos">12</span>	        <span class="hljs-comment"># Create the site</span>
<span class="linenos">13</span>	        site = Site()
<span class="linenos">14</span>	        site.name = <span class="hljs-string">'example.com'</span>
<span class="linenos">15</span>	        site.domain = <span class="hljs-string">'example.com'</span>
<span class="linenos">16</span>	        site.save()
<span class="linenos">17</span>	
<span class="linenos">18</span>	        <span class="hljs-comment"># Create the post</span>
<span class="linenos">19</span>	        post = Post()
<span class="linenos">20</span>	
<span class="linenos">21</span>	        <span class="hljs-comment"># Set the attributes</span>
<span class="linenos">22</span>	        post.title = <span class="hljs-string">'My first post'</span>
<span class="linenos">23</span>	        post.text = <span class="hljs-string">'This is my first blog post'</span>
<span class="linenos">24</span>	        post.slug = <span class="hljs-string">'my-first-post'</span>
<span class="linenos">25</span>	        post.pub_date = timezone.now()
<span class="linenos">26</span>	        post.author = author
<span class="linenos">27</span>	        post.site = site
<span class="linenos">28</span>	        post.category = category
<span class="linenos">29</span>	
<span class="linenos">30</span>	        <span class="hljs-comment"># Save it</span>
<span class="linenos">31</span>	        post.save()
<span class="linenos">32</span>	
<span class="linenos">33</span>	        <span class="hljs-comment"># Check we can find it</span>
<span class="linenos">34</span>	        all_posts = Post.objects.all()
<span class="linenos">35</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">36</span>	        only_post = all_posts[<span class="hljs-number">0</span>]
<span class="linenos">37</span>	        self.assertEquals(only_post, post)
<span class="linenos">38</span>	
<span class="linenos">39</span>	        <span class="hljs-comment"># Check attributes</span>
<span class="linenos">40</span>	        self.assertEquals(only_post.title, <span class="hljs-string">'My first post'</span>)
<span class="linenos">41</span>	        self.assertEquals(only_post.text, <span class="hljs-string">'This is my first blog post'</span>)
<span class="linenos">42</span>	        self.assertEquals(only_post.slug, <span class="hljs-string">'my-first-post'</span>)
<span class="linenos">43</span>	        self.assertEquals(only_post.site.name, <span class="hljs-string">'example.com'</span>)
<span class="linenos">44</span>	        self.assertEquals(only_post.site.domain, <span class="hljs-string">'example.com'</span>)
<span class="linenos">45</span>	        self.assertEquals(only_post.pub_date.day, post.pub_date.day)
<span class="linenos">46</span>	        self.assertEquals(only_post.pub_date.month, post.pub_date.month)
<span class="linenos">47</span>	        self.assertEquals(only_post.pub_date.year, post.pub_date.year)
<span class="linenos">48</span>	        self.assertEquals(only_post.pub_date.hour, post.pub_date.hour)
<span class="linenos">49</span>	        self.assertEquals(only_post.pub_date.minute, post.pub_date.minute)
<span class="linenos">50</span>	        self.assertEquals(only_post.pub_date.second, post.pub_date.second)
<span class="linenos">51</span>	        self.assertEquals(only_post.author.username, <span class="hljs-string">'testuser'</span>)
<span class="linenos">52</span>	        self.assertEquals(only_post.author.email, <span class="hljs-string">'user@example.com'</span>)
<span class="linenos">53</span>	        self.assertEquals(only_post.category.name, <span class="hljs-string">'python'</span>)
<span class="linenos">54</span>	        self.assertEquals(only_post.category.description, <span class="hljs-string">'The Python programming language'</span>)</code></pre>
<p>What we’re doing here is adding a <code>category</code> attribute to the posts. This attribute contains a reference to a <code>Category</code> object.</p>
<p>Now, we also want to test adding, editing, and deleting a category from the admin interface. Add this code to the <code>AdminTest</code> class:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_create_category</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">2</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">3</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">4</span>	
<span class="linenos">5</span>	        <span class="hljs-comment"># Check response code</span>
<span class="linenos">6</span>	        response = self.client.get(<span class="hljs-string">'/admin/blogengine/category/add/'</span>)
<span class="linenos">7</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">8</span>	
<span class="linenos">9</span>	        <span class="hljs-comment"># Create the new category</span>
<span class="linenos">10</span>	        response = self.client.post(<span class="hljs-string">'/admin/blogengine/category/add/'</span>, {
<span class="linenos">11</span>	            <span class="hljs-string">'name'</span>: <span class="hljs-string">'python'</span>,
<span class="linenos">12</span>	            <span class="hljs-string">'description'</span>: <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">13</span>	            },
<span class="linenos">14</span>	            follow=<span class="hljs-keyword">True</span>
<span class="linenos">15</span>	        )
<span class="linenos">16</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">17</span>	
<span class="linenos">18</span>	        <span class="hljs-comment"># Check added successfully</span>
<span class="linenos">19</span>	        self.assertTrue(<span class="hljs-string">'added successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">20</span>	
<span class="linenos">21</span>	        <span class="hljs-comment"># Check new category now in database</span>
<span class="linenos">22</span>	        all_categories = Category.objects.all()
<span class="linenos">23</span>	        self.assertEquals(len(all_categories), <span class="hljs-number">1</span>)
<span class="linenos">24</span>	
<span class="linenos">25</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_edit_category</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">26</span>	        <span class="hljs-comment"># Create the category</span>
<span class="linenos">27</span>	        category = Category()
<span class="linenos">28</span>	        category.name = <span class="hljs-string">'python'</span>
<span class="linenos">29</span>	        category.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">30</span>	        category.save()
<span class="linenos">31</span>	
<span class="linenos">32</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">33</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">34</span>	
<span class="linenos">35</span>	        <span class="hljs-comment"># Edit the category</span>
<span class="linenos">36</span>	        response = self.client.post(<span class="hljs-string">'/admin/blogengine/category/1/'</span>, {
<span class="linenos">37</span>	            <span class="hljs-string">'name'</span>: <span class="hljs-string">'perl'</span>,
<span class="linenos">38</span>	            <span class="hljs-string">'description'</span>: <span class="hljs-string">'The Perl programming language'</span>
<span class="linenos">39</span>	            }, follow=<span class="hljs-keyword">True</span>)
<span class="linenos">40</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">41</span>	
<span class="linenos">42</span>	        <span class="hljs-comment"># Check changed successfully</span>
<span class="linenos">43</span>	        self.assertTrue(<span class="hljs-string">'changed successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">44</span>	
<span class="linenos">45</span>	        <span class="hljs-comment"># Check category amended</span>
<span class="linenos">46</span>	        all_categories = Category.objects.all()
<span class="linenos">47</span>	        self.assertEquals(len(all_categories), <span class="hljs-number">1</span>)
<span class="linenos">48</span>	        only_category = all_categories[<span class="hljs-number">0</span>]
<span class="linenos">49</span>	        self.assertEquals(only_category.name, <span class="hljs-string">'perl'</span>)
<span class="linenos">50</span>	        self.assertEquals(only_category.description, <span class="hljs-string">'The Perl programming language'</span>)
<span class="linenos">51</span>	
<span class="linenos">52</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_delete_category</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">53</span>	        <span class="hljs-comment"># Create the category</span>
<span class="linenos">54</span>	        category = Category()
<span class="linenos">55</span>	        category.name = <span class="hljs-string">'python'</span>
<span class="linenos">56</span>	        category.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">57</span>	        category.save()
<span class="linenos">58</span>	
<span class="linenos">59</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">60</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">61</span>	
<span class="linenos">62</span>	        <span class="hljs-comment"># Delete the category</span>
<span class="linenos">63</span>	        response = self.client.post(<span class="hljs-string">'/admin/blogengine/category/1/delete/'</span>, {
<span class="linenos">64</span>	            <span class="hljs-string">'post'</span>: <span class="hljs-string">'yes'</span>
<span class="linenos">65</span>	        }, follow=<span class="hljs-keyword">True</span>)
<span class="linenos">66</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">67</span>	
<span class="linenos">68</span>	        <span class="hljs-comment"># Check deleted successfully</span>
<span class="linenos">69</span>	        self.assertTrue(<span class="hljs-string">'deleted successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">70</span>	
<span class="linenos">71</span>	        <span class="hljs-comment"># Check category deleted</span>
<span class="linenos">72</span>	        all_categories = Category.objects.all()
<span class="linenos">73</span>	        self.assertEquals(len(all_categories), <span class="hljs-number">0</span>)</code></pre>
<p>This is very similar to the prior code for the posts, and just checks we can create categories via the admin. We also need to check we can apply these categories to posts, and that they don’t break the existing tests:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_create_post</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">2</span>	        <span class="hljs-comment"># Create the category</span>
<span class="linenos">3</span>	        category = Category()
<span class="linenos">4</span>	        category.name = <span class="hljs-string">'python'</span>
<span class="linenos">5</span>	        category.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">6</span>	        category.save()
<span class="linenos">7</span>	
<span class="linenos">8</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">9</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">10</span>	
<span class="linenos">11</span>	        <span class="hljs-comment"># Check response code</span>
<span class="linenos">12</span>	        response = self.client.get(<span class="hljs-string">'/admin/blogengine/post/add/'</span>)
<span class="linenos">13</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">14</span>	
<span class="linenos">15</span>	        <span class="hljs-comment"># Create the new post</span>
<span class="linenos">16</span>	        response = self.client.post(<span class="hljs-string">'/admin/blogengine/post/add/'</span>, {
<span class="linenos">17</span>	            <span class="hljs-string">'title'</span>: <span class="hljs-string">'My first post'</span>,
<span class="linenos">18</span>	            <span class="hljs-string">'text'</span>: <span class="hljs-string">'This is my first post'</span>,
<span class="linenos">19</span>	            <span class="hljs-string">'pub_date_0'</span>: <span class="hljs-string">'2013-12-28'</span>,
<span class="linenos">20</span>	            <span class="hljs-string">'pub_date_1'</span>: <span class="hljs-string">'22:00:04'</span>,
<span class="linenos">21</span>	            <span class="hljs-string">'slug'</span>: <span class="hljs-string">'my-first-post'</span>,
<span class="linenos">22</span>	            <span class="hljs-string">'site'</span>: <span class="hljs-string">'1'</span>,
<span class="linenos">23</span>	            <span class="hljs-string">'category'</span>: <span class="hljs-string">'1'</span>
<span class="linenos">24</span>	        },
<span class="linenos">25</span>	        follow=<span class="hljs-keyword">True</span>
<span class="linenos">26</span>	        )
<span class="linenos">27</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">28</span>	
<span class="linenos">29</span>	        <span class="hljs-comment"># Check added successfully</span>
<span class="linenos">30</span>	        self.assertTrue(<span class="hljs-string">'added successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">31</span>	
<span class="linenos">32</span>	        <span class="hljs-comment"># Check new post now in database</span>
<span class="linenos">33</span>	        all_posts = Post.objects.all()
<span class="linenos">34</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">35</span>	
<span class="linenos">36</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_edit_post</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">37</span>	        <span class="hljs-comment"># Create the category</span>
<span class="linenos">38</span>	        category = Category()
<span class="linenos">39</span>	        category.name = <span class="hljs-string">'python'</span>
<span class="linenos">40</span>	        category.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">41</span>	        category.save()
<span class="linenos">42</span>	
<span class="linenos">43</span>	        <span class="hljs-comment"># Create the author</span>
<span class="linenos">44</span>	        author = User.objects.create_user(<span class="hljs-string">'testuser'</span>, <span class="hljs-string">'user@example.com'</span>, <span class="hljs-string">'password'</span>)
<span class="linenos">45</span>	        author.save()
<span class="linenos">46</span>	
<span class="linenos">47</span>	        <span class="hljs-comment"># Create the site</span>
<span class="linenos">48</span>	        site = Site()
<span class="linenos">49</span>	        site.name = <span class="hljs-string">'example.com'</span>
<span class="linenos">50</span>	        site.domain = <span class="hljs-string">'example.com'</span>
<span class="linenos">51</span>	        site.save()
<span class="linenos">52</span>	
<span class="linenos">53</span>	        <span class="hljs-comment"># Create the post</span>
<span class="linenos">54</span>	        post = Post()
<span class="linenos">55</span>	        post.title = <span class="hljs-string">'My first post'</span>
<span class="linenos">56</span>	        post.text = <span class="hljs-string">'This is my first blog post'</span>
<span class="linenos">57</span>	        post.slug = <span class="hljs-string">'my-first-post'</span>
<span class="linenos">58</span>	        post.pub_date = timezone.now()
<span class="linenos">59</span>	        post.author = author
<span class="linenos">60</span>	        post.site = site
<span class="linenos">61</span>	        post.save()
<span class="linenos">62</span>	
<span class="linenos">63</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">64</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">65</span>	
<span class="linenos">66</span>	        <span class="hljs-comment"># Edit the post</span>
<span class="linenos">67</span>	        response = self.client.post(<span class="hljs-string">'/admin/blogengine/post/1/'</span>, {
<span class="linenos">68</span>	            <span class="hljs-string">'title'</span>: <span class="hljs-string">'My second post'</span>,
<span class="linenos">69</span>	            <span class="hljs-string">'text'</span>: <span class="hljs-string">'This is my second blog post'</span>,
<span class="linenos">70</span>	            <span class="hljs-string">'pub_date_0'</span>: <span class="hljs-string">'2013-12-28'</span>,
<span class="linenos">71</span>	            <span class="hljs-string">'pub_date_1'</span>: <span class="hljs-string">'22:00:04'</span>,
<span class="linenos">72</span>	            <span class="hljs-string">'slug'</span>: <span class="hljs-string">'my-second-post'</span>,
<span class="linenos">73</span>	            <span class="hljs-string">'site'</span>: <span class="hljs-string">'1'</span>,
<span class="linenos">74</span>	            <span class="hljs-string">'category'</span>: <span class="hljs-string">'1'</span>
<span class="linenos">75</span>	        },
<span class="linenos">76</span>	        follow=<span class="hljs-keyword">True</span>
<span class="linenos">77</span>	        )
<span class="linenos">78</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">79</span>	
<span class="linenos">80</span>	        <span class="hljs-comment"># Check changed successfully</span>
<span class="linenos">81</span>	        self.assertTrue(<span class="hljs-string">'changed successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">82</span>	
<span class="linenos">83</span>	        <span class="hljs-comment"># Check post amended</span>
<span class="linenos">84</span>	        all_posts = Post.objects.all()
<span class="linenos">85</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">86</span>	        only_post = all_posts[<span class="hljs-number">0</span>]
<span class="linenos">87</span>	        self.assertEquals(only_post.title, <span class="hljs-string">'My second post'</span>)
<span class="linenos">88</span>	        self.assertEquals(only_post.text, <span class="hljs-string">'This is my second blog post'</span>)
<span class="linenos">89</span>	
<span class="linenos">90</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_delete_post</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">91</span>	        <span class="hljs-comment"># Create the category</span>
<span class="linenos">92</span>	        category = Category()
<span class="linenos">93</span>	        category.name = <span class="hljs-string">'python'</span>
<span class="linenos">94</span>	        category.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">95</span>	        category.save()
<span class="linenos">96</span>	
<span class="linenos">97</span>	        <span class="hljs-comment"># Create the author</span>
<span class="linenos">98</span>	        author = User.objects.create_user(<span class="hljs-string">'testuser'</span>, <span class="hljs-string">'user@example.com'</span>, <span class="hljs-string">'password'</span>)
<span class="linenos">99</span>	        author.save()
<span class="linenos">100</span>	
<span class="linenos">101</span>	        <span class="hljs-comment"># Create the site</span>
<span class="linenos">102</span>	        site = Site()
<span class="linenos">103</span>	        site.name = <span class="hljs-string">'example.com'</span>
<span class="linenos">104</span>	        site.domain = <span class="hljs-string">'example.com'</span>
<span class="linenos">105</span>	        site.save()
<span class="linenos">106</span>	
<span class="linenos">107</span>	        <span class="hljs-comment"># Create the post</span>
<span class="linenos">108</span>	        post = Post()
<span class="linenos">109</span>	        post.title = <span class="hljs-string">'My first post'</span>
<span class="linenos">110</span>	        post.text = <span class="hljs-string">'This is my first blog post'</span>
<span class="linenos">111</span>	        post.slug = <span class="hljs-string">'my-first-post'</span>
<span class="linenos">112</span>	        post.pub_date = timezone.now()
<span class="linenos">113</span>	        post.site = site
<span class="linenos">114</span>	        post.author = author
<span class="linenos">115</span>	        post.category = category
<span class="linenos">116</span>	        post.save()
<span class="linenos">117</span>	
<span class="linenos">118</span>	        <span class="hljs-comment"># Check new post saved</span>
<span class="linenos">119</span>	        all_posts = Post.objects.all()
<span class="linenos">120</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">121</span>	
<span class="linenos">122</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">123</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">124</span>	
<span class="linenos">125</span>	        <span class="hljs-comment"># Delete the post</span>
<span class="linenos">126</span>	        response = self.client.post(<span class="hljs-string">'/admin/blogengine/post/1/delete/'</span>, {
<span class="linenos">127</span>	            <span class="hljs-string">'post'</span>: <span class="hljs-string">'yes'</span>
<span class="linenos">128</span>	        }, follow=<span class="hljs-keyword">True</span>)
<span class="linenos">129</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">130</span>	
<span class="linenos">131</span>	        <span class="hljs-comment"># Check deleted successfully</span>
<span class="linenos">132</span>	        self.assertTrue(<span class="hljs-string">'deleted successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">133</span>	
<span class="linenos">134</span>	        <span class="hljs-comment"># Check post deleted</span>
<span class="linenos">135</span>	        all_posts = Post.objects.all()
<span class="linenos">136</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">0</span>)</code></pre>
<p>Here we basically take our existing post tests in the admin interface and add the category to them. Finally, we edit the <code>PostViewTest</code> class to include categories:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">PostViewTest</span><span class="hljs-params">(BaseAcceptanceTest)</span>:</span>
<span class="linenos">2</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_index</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">3</span>	        <span class="hljs-comment"># Create the category</span>
<span class="linenos">4</span>	        category = Category()
<span class="linenos">5</span>	        category.name = <span class="hljs-string">'python'</span>
<span class="linenos">6</span>	        category.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">7</span>	        category.save()
<span class="linenos">8</span>	
<span class="linenos">9</span>	        <span class="hljs-comment"># Create the author</span>
<span class="linenos">10</span>	        author = User.objects.create_user(<span class="hljs-string">'testuser'</span>, <span class="hljs-string">'user@example.com'</span>, <span class="hljs-string">'password'</span>)
<span class="linenos">11</span>	        author.save()
<span class="linenos">12</span>	
<span class="linenos">13</span>	        <span class="hljs-comment"># Create the site</span>
<span class="linenos">14</span>	        site = Site()
<span class="linenos">15</span>	        site.name = <span class="hljs-string">'example.com'</span>
<span class="linenos">16</span>	        site.domain = <span class="hljs-string">'example.com'</span>
<span class="linenos">17</span>	        site.save()
<span class="linenos">18</span>	
<span class="linenos">19</span>	        <span class="hljs-comment"># Create the post</span>
<span class="linenos">20</span>	        post = Post()
<span class="linenos">21</span>	        post.title = <span class="hljs-string">'My first post'</span>
<span class="linenos">22</span>	        post.text = <span class="hljs-string">'This is [my first blog post](http://127.0.0.1:8000/)'</span>
<span class="linenos">23</span>	        post.slug = <span class="hljs-string">'my-first-post'</span>
<span class="linenos">24</span>	        post.pub_date = timezone.now()
<span class="linenos">25</span>	        post.author = author
<span class="linenos">26</span>	        post.site = site
<span class="linenos">27</span>	        post.category = category
<span class="linenos">28</span>	        post.save()
<span class="linenos">29</span>	
<span class="linenos">30</span>	        <span class="hljs-comment"># Check new post saved</span>
<span class="linenos">31</span>	        all_posts = Post.objects.all()
<span class="linenos">32</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">33</span>	
<span class="linenos">34</span>	        <span class="hljs-comment"># Fetch the index</span>
<span class="linenos">35</span>	        response = self.client.get(<span class="hljs-string">'/'</span>)
<span class="linenos">36</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">37</span>	
<span class="linenos">38</span>	        <span class="hljs-comment"># Check the post title is in the response</span>
<span class="linenos">39</span>	        self.assertTrue(post.title <span class="hljs-keyword">in</span> response.content)
<span class="linenos">40</span>	
<span class="linenos">41</span>	        <span class="hljs-comment"># Check the post text is in the response</span>
<span class="linenos">42</span>	        self.assertTrue(markdown.markdown(post.text) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">43</span>	
<span class="linenos">44</span>	        <span class="hljs-comment"># Check the post date is in the response</span>
<span class="linenos">45</span>	        self.assertTrue(str(post.pub_date.year) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">46</span>	        self.assertTrue(post.pub_date.strftime(<span class="hljs-string">'%b'</span>) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">47</span>	        self.assertTrue(str(post.pub_date.day) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">48</span>	
<span class="linenos">49</span>	        <span class="hljs-comment"># Check the link is marked up properly</span>
<span class="linenos">50</span>	        self.assertTrue(<span class="hljs-string">'&lt;a href="http://127.0.0.1:8000/"&gt;my first blog post&lt;/a&gt;'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">51</span>	
<span class="linenos">52</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_post_page</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">53</span>	        <span class="hljs-comment"># Create the category</span>
<span class="linenos">54</span>	        category = Category()
<span class="linenos">55</span>	        category.name = <span class="hljs-string">'python'</span>
<span class="linenos">56</span>	        category.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">57</span>	        category.save()
<span class="linenos">58</span>	
<span class="linenos">59</span>	        <span class="hljs-comment"># Create the author</span>
<span class="linenos">60</span>	        author = User.objects.create_user(<span class="hljs-string">'testuser'</span>, <span class="hljs-string">'user@example.com'</span>, <span class="hljs-string">'password'</span>)
<span class="linenos">61</span>	        author.save()
<span class="linenos">62</span>	
<span class="linenos">63</span>	        <span class="hljs-comment"># Create the site</span>
<span class="linenos">64</span>	        site = Site()
<span class="linenos">65</span>	        site.name = <span class="hljs-string">'example.com'</span>
<span class="linenos">66</span>	        site.domain = <span class="hljs-string">'example.com'</span>
<span class="linenos">67</span>	        site.save()
<span class="linenos">68</span>	
<span class="linenos">69</span>	        <span class="hljs-comment"># Create the post</span>
<span class="linenos">70</span>	        post = Post()
<span class="linenos">71</span>	        post.title = <span class="hljs-string">'My first post'</span>
<span class="linenos">72</span>	        post.text = <span class="hljs-string">'This is [my first blog post](http://127.0.0.1:8000/)'</span>
<span class="linenos">73</span>	        post.slug = <span class="hljs-string">'my-first-post'</span>
<span class="linenos">74</span>	        post.pub_date = timezone.now()
<span class="linenos">75</span>	        post.author = author
<span class="linenos">76</span>	        post.site = site
<span class="linenos">77</span>	        post.category = category
<span class="linenos">78</span>	        post.save()
<span class="linenos">79</span>	
<span class="linenos">80</span>	        <span class="hljs-comment"># Check new post saved</span>
<span class="linenos">81</span>	        all_posts = Post.objects.all()
<span class="linenos">82</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">83</span>	        only_post = all_posts[<span class="hljs-number">0</span>]
<span class="linenos">84</span>	        self.assertEquals(only_post, post)
<span class="linenos">85</span>	
<span class="linenos">86</span>	        <span class="hljs-comment"># Get the post URL</span>
<span class="linenos">87</span>	        post_url = only_post.get_absolute_url()
<span class="linenos">88</span>	
<span class="linenos">89</span>	        <span class="hljs-comment"># Fetch the post</span>
<span class="linenos">90</span>	        response = self.client.get(post_url)
<span class="linenos">91</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">92</span>	
<span class="linenos">93</span>	        <span class="hljs-comment"># Check the post title is in the response</span>
<span class="linenos">94</span>	        self.assertTrue(post.title <span class="hljs-keyword">in</span> response.content)
<span class="linenos">95</span>	
<span class="linenos">96</span>	        <span class="hljs-comment"># Check the post text is in the response</span>
<span class="linenos">97</span>	        self.assertTrue(markdown.markdown(post.text) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">98</span>	
<span class="linenos">99</span>	        <span class="hljs-comment"># Check the post date is in the response</span>
<span class="linenos">100</span>	        self.assertTrue(str(post.pub_date.year) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">101</span>	        self.assertTrue(post.pub_date.strftime(<span class="hljs-string">'%b'</span>) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">102</span>	        self.assertTrue(str(post.pub_date.day) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">103</span>	
<span class="linenos">104</span>	        <span class="hljs-comment"># Check the link is marked up properly</span>
<span class="linenos">105</span>	        self.assertTrue(<span class="hljs-string">'&lt;a href="http://127.0.0.1:8000/"&gt;my first blog post&lt;/a&gt;'</span> <span class="hljs-keyword">in</span> response.content)</code></pre>
<p>Now it’s time to run our tests:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	(venv)Smith:django_tutorial_blog_ng matthewdaly$ python manage.py <span class="hljs-built_in">test</span> blogengine
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	E
<span class="linenos">4</span>	======================================================================
<span class="linenos">5</span>	ERROR: blogengine.tests (unittest.loader.ModuleImportFailure)
<span class="linenos">6</span>	----------------------------------------------------------------------
<span class="linenos">7</span>	ImportError: Failed to import <span class="hljs-built_in">test</span> module: blogengine.tests
<span class="linenos">8</span>	Traceback (most recent call last):
<span class="linenos">9</span>	  File <span class="hljs-string">"/usr/local/Cellar/python/2.7.6/Frameworks/Python.framework/Versions/2.7/lib/python2.7/unittest/loader.py"</span>, line <span class="hljs-number">254</span>, <span class="hljs-keyword">in</span> _find_tests
<span class="linenos">10</span>	    module = self._get_module_from_name(name)
<span class="linenos">11</span>	  File <span class="hljs-string">"/usr/local/Cellar/python/2.7.6/Frameworks/Python.framework/Versions/2.7/lib/python2.7/unittest/loader.py"</span>, line <span class="hljs-number">232</span>, <span class="hljs-keyword">in</span> _get_module_from_name
<span class="linenos">12</span>	    __import__(name)
<span class="linenos">13</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">3</span>, <span class="hljs-keyword">in</span> &lt;module&gt;
<span class="linenos">14</span>	    from blogengine.models import Post, Category
<span class="linenos">15</span>	ImportError: cannot import name Category
<span class="linenos">16</span>	
<span class="linenos">17</span>	
<span class="linenos">18</span>	----------------------------------------------------------------------
<span class="linenos">19</span>	Ran <span class="hljs-number">1</span> <span class="hljs-built_in">test</span> <span class="hljs-keyword">in</span> <span class="hljs-number">0.000</span>s
<span class="linenos">20</span>	
<span class="linenos">21</span>	FAILED (errors=<span class="hljs-number">1</span>)
<span class="linenos">22</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>This is the expected result. We need to create our Category model. So let’s do that:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-keyword">from</span> django.db <span class="hljs-keyword">import</span> models
<span class="linenos">2</span>	<span class="hljs-keyword">from</span> django.contrib.auth.models <span class="hljs-keyword">import</span> User
<span class="linenos">3</span>	<span class="hljs-keyword">from</span> django.contrib.sites.models <span class="hljs-keyword">import</span> Site
<span class="linenos">4</span>	
<span class="linenos">5</span>	<span class="hljs-comment"># Create your models here.</span>
<span class="linenos">6</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Category</span><span class="hljs-params">(models.Model)</span>:</span>
<span class="linenos">7</span>	    name = models.CharField(max_length=<span class="hljs-number">200</span>)
<span class="linenos">8</span>	    description = models.TextField()
<span class="linenos">9</span>	
<span class="linenos">10</span>	
<span class="linenos">11</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Post</span><span class="hljs-params">(models.Model)</span>:</span>
<span class="linenos">12</span>	    title = models.CharField(max_length=<span class="hljs-number">200</span>)
<span class="linenos">13</span>	    pub_date = models.DateTimeField()
<span class="linenos">14</span>	    text = models.TextField()
<span class="linenos">15</span>	    slug = models.SlugField(max_length=<span class="hljs-number">40</span>, unique=<span class="hljs-keyword">True</span>)
<span class="linenos">16</span>	    author = models.ForeignKey(User)
<span class="linenos">17</span>	    site = models.ForeignKey(Site)
<span class="linenos">18</span>	    category = models.ForeignKey(Category, blank=<span class="hljs-keyword">True</span>, null=<span class="hljs-keyword">True</span>)
<span class="linenos">19</span>	
<span class="linenos">20</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">get_absolute_url</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">21</span>	        <span class="hljs-keyword">return</span> <span class="hljs-string">"/%s/%s/%s/"</span> % (self.pub_date.year, self.pub_date.month, self.slug)
<span class="linenos">22</span>	
<span class="linenos">23</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">__unicode__</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">24</span>	        <span class="hljs-keyword">return</span> self.title
<span class="linenos">25</span>	
<span class="linenos">26</span>	    <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Meta</span>:</span>
<span class="linenos">27</span>	        ordering = [<span class="hljs-string">"-pub_date"</span>]</code></pre>
<p>Note that we add <code>Category</code> before <code>Post</code> - this is because <code>Category</code> is a foreign key in <code>Post</code>, and must be defined in order to be used. Also, note that we add the <code>category</code> attribute as a <code>ForeignKey</code> field, like <code>User</code> and <code>Site</code>, indicating that it is an item in another table being references.</p>
<p>We also allow for <code>category</code> to be blank or null, so the user does not have to apply a category if they don’t wish to.</p>
<p>If we run our tests, they should still fail:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	(venv)Smith:django_tutorial_blog_ng matthewdaly$ python manage.py <span class="hljs-built_in">test</span> blogengine
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	EEFEEEEE...EE
<span class="linenos">4</span>	======================================================================
<span class="linenos">5</span>	ERROR: <span class="hljs-built_in">test</span>_create_category (blogengine.tests.PostTest)
<span class="linenos">6</span>	----------------------------------------------------------------------
<span class="linenos">7</span>	Traceback (most recent call last):
<span class="linenos">8</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">20</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_create_category
<span class="linenos">9</span>	    category.save()
<span class="linenos">10</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">545</span>, <span class="hljs-keyword">in</span> save
<span class="linenos">11</span>	    force_update=force_update, update_fields=update_fields)
<span class="linenos">12</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">573</span>, <span class="hljs-keyword">in</span> save_base
<span class="linenos">13</span>	    updated = self._save_table(raw, cls, force_insert, force_update, using, update_fields)
<span class="linenos">14</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">654</span>, <span class="hljs-keyword">in</span> _save_table
<span class="linenos">15</span>	    result = self._<span class="hljs-keyword">do</span>_insert(cls._base_manager, using, fields, update_pk, raw)
<span class="linenos">16</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">687</span>, <span class="hljs-keyword">in</span> _<span class="hljs-keyword">do</span>_insert
<span class="linenos">17</span>	    using=using, raw=raw)
<span class="linenos">18</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/manager.py"</span>, line <span class="hljs-number">232</span>, <span class="hljs-keyword">in</span> _insert
<span class="linenos">19</span>	    <span class="hljs-built_in">return</span> insert_query(self.model, objs, fields, **kwargs)
<span class="linenos">20</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/query.py"</span>, line <span class="hljs-number">1511</span>, <span class="hljs-keyword">in</span> insert_query
<span class="linenos">21</span>	    <span class="hljs-built_in">return</span> query.get_compiler(using=using).execute_sql(<span class="hljs-built_in">return</span>_id)
<span class="linenos">22</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/sql/compiler.py"</span>, line <span class="hljs-number">898</span>, <span class="hljs-keyword">in</span> execute_sql
<span class="linenos">23</span>	    cursor.execute(sql, params)
<span class="linenos">24</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">25</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">26</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/utils.py"</span>, line <span class="hljs-number">99</span>, <span class="hljs-keyword">in</span> __<span class="hljs-built_in">exit</span>__
<span class="linenos">27</span>	    six.reraise(dj_exc_<span class="hljs-built_in">type</span>, dj_exc_value, traceback)
<span class="linenos">28</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">29</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">30</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/sqlite3/base.py"</span>, line <span class="hljs-number">450</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">31</span>	    <span class="hljs-built_in">return</span> Database.Cursor.execute(self, query, params)
<span class="linenos">32</span>	OperationalError: no such table: blogengine_category
<span class="linenos">33</span>	
<span class="linenos">34</span>	======================================================================
<span class="linenos">35</span>	ERROR: <span class="hljs-built_in">test</span>_create_post (blogengine.tests.PostTest)
<span class="linenos">36</span>	----------------------------------------------------------------------
<span class="linenos">37</span>	Traceback (most recent call last):
<span class="linenos">38</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">37</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_create_post
<span class="linenos">39</span>	    category.save()
<span class="linenos">40</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">545</span>, <span class="hljs-keyword">in</span> save
<span class="linenos">41</span>	    force_update=force_update, update_fields=update_fields)
<span class="linenos">42</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">573</span>, <span class="hljs-keyword">in</span> save_base
<span class="linenos">43</span>	    updated = self._save_table(raw, cls, force_insert, force_update, using, update_fields)
<span class="linenos">44</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">654</span>, <span class="hljs-keyword">in</span> _save_table
<span class="linenos">45</span>	    result = self._<span class="hljs-keyword">do</span>_insert(cls._base_manager, using, fields, update_pk, raw)
<span class="linenos">46</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">687</span>, <span class="hljs-keyword">in</span> _<span class="hljs-keyword">do</span>_insert
<span class="linenos">47</span>	    using=using, raw=raw)
<span class="linenos">48</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/manager.py"</span>, line <span class="hljs-number">232</span>, <span class="hljs-keyword">in</span> _insert
<span class="linenos">49</span>	    <span class="hljs-built_in">return</span> insert_query(self.model, objs, fields, **kwargs)
<span class="linenos">50</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/query.py"</span>, line <span class="hljs-number">1511</span>, <span class="hljs-keyword">in</span> insert_query
<span class="linenos">51</span>	    <span class="hljs-built_in">return</span> query.get_compiler(using=using).execute_sql(<span class="hljs-built_in">return</span>_id)
<span class="linenos">52</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/sql/compiler.py"</span>, line <span class="hljs-number">898</span>, <span class="hljs-keyword">in</span> execute_sql
<span class="linenos">53</span>	    cursor.execute(sql, params)
<span class="linenos">54</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">55</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">56</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/utils.py"</span>, line <span class="hljs-number">99</span>, <span class="hljs-keyword">in</span> __<span class="hljs-built_in">exit</span>__
<span class="linenos">57</span>	    six.reraise(dj_exc_<span class="hljs-built_in">type</span>, dj_exc_value, traceback)
<span class="linenos">58</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">59</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">60</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/sqlite3/base.py"</span>, line <span class="hljs-number">450</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">61</span>	    <span class="hljs-built_in">return</span> Database.Cursor.execute(self, query, params)
<span class="linenos">62</span>	OperationalError: no such table: blogengine_category
<span class="linenos">63</span>	
<span class="linenos">64</span>	======================================================================
<span class="linenos">65</span>	ERROR: <span class="hljs-built_in">test</span>_create_post (blogengine.tests.AdminTest)
<span class="linenos">66</span>	----------------------------------------------------------------------
<span class="linenos">67</span>	Traceback (most recent call last):
<span class="linenos">68</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">215</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_create_post
<span class="linenos">69</span>	    category.save()
<span class="linenos">70</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">545</span>, <span class="hljs-keyword">in</span> save
<span class="linenos">71</span>	    force_update=force_update, update_fields=update_fields)
<span class="linenos">72</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">573</span>, <span class="hljs-keyword">in</span> save_base
<span class="linenos">73</span>	    updated = self._save_table(raw, cls, force_insert, force_update, using, update_fields)
<span class="linenos">74</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">654</span>, <span class="hljs-keyword">in</span> _save_table
<span class="linenos">75</span>	    result = self._<span class="hljs-keyword">do</span>_insert(cls._base_manager, using, fields, update_pk, raw)
<span class="linenos">76</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">687</span>, <span class="hljs-keyword">in</span> _<span class="hljs-keyword">do</span>_insert
<span class="linenos">77</span>	    using=using, raw=raw)
<span class="linenos">78</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/manager.py"</span>, line <span class="hljs-number">232</span>, <span class="hljs-keyword">in</span> _insert
<span class="linenos">79</span>	    <span class="hljs-built_in">return</span> insert_query(self.model, objs, fields, **kwargs)
<span class="linenos">80</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/query.py"</span>, line <span class="hljs-number">1511</span>, <span class="hljs-keyword">in</span> insert_query
<span class="linenos">81</span>	    <span class="hljs-built_in">return</span> query.get_compiler(using=using).execute_sql(<span class="hljs-built_in">return</span>_id)
<span class="linenos">82</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/sql/compiler.py"</span>, line <span class="hljs-number">898</span>, <span class="hljs-keyword">in</span> execute_sql
<span class="linenos">83</span>	    cursor.execute(sql, params)
<span class="linenos">84</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">85</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">86</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/utils.py"</span>, line <span class="hljs-number">99</span>, <span class="hljs-keyword">in</span> __<span class="hljs-built_in">exit</span>__
<span class="linenos">87</span>	    six.reraise(dj_exc_<span class="hljs-built_in">type</span>, dj_exc_value, traceback)
<span class="linenos">88</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">89</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">90</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/sqlite3/base.py"</span>, line <span class="hljs-number">450</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">91</span>	    <span class="hljs-built_in">return</span> Database.Cursor.execute(self, query, params)
<span class="linenos">92</span>	OperationalError: no such table: blogengine_category
<span class="linenos">93</span>	
<span class="linenos">94</span>	======================================================================
<span class="linenos">95</span>	ERROR: <span class="hljs-built_in">test</span>_delete_category (blogengine.tests.AdminTest)
<span class="linenos">96</span>	----------------------------------------------------------------------
<span class="linenos">97</span>	Traceback (most recent call last):
<span class="linenos">98</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">192</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_delete_category
<span class="linenos">99</span>	    category.save()
<span class="linenos">100</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">545</span>, <span class="hljs-keyword">in</span> save
<span class="linenos">101</span>	    force_update=force_update, update_fields=update_fields)
<span class="linenos">102</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">573</span>, <span class="hljs-keyword">in</span> save_base
<span class="linenos">103</span>	    updated = self._save_table(raw, cls, force_insert, force_update, using, update_fields)
<span class="linenos">104</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">654</span>, <span class="hljs-keyword">in</span> _save_table
<span class="linenos">105</span>	    result = self._<span class="hljs-keyword">do</span>_insert(cls._base_manager, using, fields, update_pk, raw)
<span class="linenos">106</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">687</span>, <span class="hljs-keyword">in</span> _<span class="hljs-keyword">do</span>_insert
<span class="linenos">107</span>	    using=using, raw=raw)
<span class="linenos">108</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/manager.py"</span>, line <span class="hljs-number">232</span>, <span class="hljs-keyword">in</span> _insert
<span class="linenos">109</span>	    <span class="hljs-built_in">return</span> insert_query(self.model, objs, fields, **kwargs)
<span class="linenos">110</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/query.py"</span>, line <span class="hljs-number">1511</span>, <span class="hljs-keyword">in</span> insert_query
<span class="linenos">111</span>	    <span class="hljs-built_in">return</span> query.get_compiler(using=using).execute_sql(<span class="hljs-built_in">return</span>_id)
<span class="linenos">112</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/sql/compiler.py"</span>, line <span class="hljs-number">898</span>, <span class="hljs-keyword">in</span> execute_sql
<span class="linenos">113</span>	    cursor.execute(sql, params)
<span class="linenos">114</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">115</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">116</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/utils.py"</span>, line <span class="hljs-number">99</span>, <span class="hljs-keyword">in</span> __<span class="hljs-built_in">exit</span>__
<span class="linenos">117</span>	    six.reraise(dj_exc_<span class="hljs-built_in">type</span>, dj_exc_value, traceback)
<span class="linenos">118</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">119</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">120</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/sqlite3/base.py"</span>, line <span class="hljs-number">450</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">121</span>	    <span class="hljs-built_in">return</span> Database.Cursor.execute(self, query, params)
<span class="linenos">122</span>	OperationalError: no such table: blogengine_category
<span class="linenos">123</span>	
<span class="linenos">124</span>	======================================================================
<span class="linenos">125</span>	ERROR: <span class="hljs-built_in">test</span>_delete_post (blogengine.tests.AdminTest)
<span class="linenos">126</span>	----------------------------------------------------------------------
<span class="linenos">127</span>	Traceback (most recent call last):
<span class="linenos">128</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">304</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_delete_post
<span class="linenos">129</span>	    category.save()
<span class="linenos">130</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">545</span>, <span class="hljs-keyword">in</span> save
<span class="linenos">131</span>	    force_update=force_update, update_fields=update_fields)
<span class="linenos">132</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">573</span>, <span class="hljs-keyword">in</span> save_base
<span class="linenos">133</span>	    updated = self._save_table(raw, cls, force_insert, force_update, using, update_fields)
<span class="linenos">134</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">654</span>, <span class="hljs-keyword">in</span> _save_table
<span class="linenos">135</span>	    result = self._<span class="hljs-keyword">do</span>_insert(cls._base_manager, using, fields, update_pk, raw)
<span class="linenos">136</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">687</span>, <span class="hljs-keyword">in</span> _<span class="hljs-keyword">do</span>_insert
<span class="linenos">137</span>	    using=using, raw=raw)
<span class="linenos">138</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/manager.py"</span>, line <span class="hljs-number">232</span>, <span class="hljs-keyword">in</span> _insert
<span class="linenos">139</span>	    <span class="hljs-built_in">return</span> insert_query(self.model, objs, fields, **kwargs)
<span class="linenos">140</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/query.py"</span>, line <span class="hljs-number">1511</span>, <span class="hljs-keyword">in</span> insert_query
<span class="linenos">141</span>	    <span class="hljs-built_in">return</span> query.get_compiler(using=using).execute_sql(<span class="hljs-built_in">return</span>_id)
<span class="linenos">142</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/sql/compiler.py"</span>, line <span class="hljs-number">898</span>, <span class="hljs-keyword">in</span> execute_sql
<span class="linenos">143</span>	    cursor.execute(sql, params)
<span class="linenos">144</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">145</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">146</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/utils.py"</span>, line <span class="hljs-number">99</span>, <span class="hljs-keyword">in</span> __<span class="hljs-built_in">exit</span>__
<span class="linenos">147</span>	    six.reraise(dj_exc_<span class="hljs-built_in">type</span>, dj_exc_value, traceback)
<span class="linenos">148</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">149</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">150</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/sqlite3/base.py"</span>, line <span class="hljs-number">450</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">151</span>	    <span class="hljs-built_in">return</span> Database.Cursor.execute(self, query, params)
<span class="linenos">152</span>	OperationalError: no such table: blogengine_category
<span class="linenos">153</span>	
<span class="linenos">154</span>	======================================================================
<span class="linenos">155</span>	ERROR: <span class="hljs-built_in">test</span>_edit_category (blogengine.tests.AdminTest)
<span class="linenos">156</span>	----------------------------------------------------------------------
<span class="linenos">157</span>	Traceback (most recent call last):
<span class="linenos">158</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">165</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_edit_category
<span class="linenos">159</span>	    category.save()
<span class="linenos">160</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">545</span>, <span class="hljs-keyword">in</span> save
<span class="linenos">161</span>	    force_update=force_update, update_fields=update_fields)
<span class="linenos">162</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">573</span>, <span class="hljs-keyword">in</span> save_base
<span class="linenos">163</span>	    updated = self._save_table(raw, cls, force_insert, force_update, using, update_fields)
<span class="linenos">164</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">654</span>, <span class="hljs-keyword">in</span> _save_table
<span class="linenos">165</span>	    result = self._<span class="hljs-keyword">do</span>_insert(cls._base_manager, using, fields, update_pk, raw)
<span class="linenos">166</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">687</span>, <span class="hljs-keyword">in</span> _<span class="hljs-keyword">do</span>_insert
<span class="linenos">167</span>	    using=using, raw=raw)
<span class="linenos">168</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/manager.py"</span>, line <span class="hljs-number">232</span>, <span class="hljs-keyword">in</span> _insert
<span class="linenos">169</span>	    <span class="hljs-built_in">return</span> insert_query(self.model, objs, fields, **kwargs)
<span class="linenos">170</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/query.py"</span>, line <span class="hljs-number">1511</span>, <span class="hljs-keyword">in</span> insert_query
<span class="linenos">171</span>	    <span class="hljs-built_in">return</span> query.get_compiler(using=using).execute_sql(<span class="hljs-built_in">return</span>_id)
<span class="linenos">172</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/sql/compiler.py"</span>, line <span class="hljs-number">898</span>, <span class="hljs-keyword">in</span> execute_sql
<span class="linenos">173</span>	    cursor.execute(sql, params)
<span class="linenos">174</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">175</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">176</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/utils.py"</span>, line <span class="hljs-number">99</span>, <span class="hljs-keyword">in</span> __<span class="hljs-built_in">exit</span>__
<span class="linenos">177</span>	    six.reraise(dj_exc_<span class="hljs-built_in">type</span>, dj_exc_value, traceback)
<span class="linenos">178</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">179</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">180</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/sqlite3/base.py"</span>, line <span class="hljs-number">450</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">181</span>	    <span class="hljs-built_in">return</span> Database.Cursor.execute(self, query, params)
<span class="linenos">182</span>	OperationalError: no such table: blogengine_category
<span class="linenos">183</span>	
<span class="linenos">184</span>	======================================================================
<span class="linenos">185</span>	ERROR: <span class="hljs-built_in">test</span>_edit_post (blogengine.tests.AdminTest)
<span class="linenos">186</span>	----------------------------------------------------------------------
<span class="linenos">187</span>	Traceback (most recent call last):
<span class="linenos">188</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">250</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_edit_post
<span class="linenos">189</span>	    category.save()
<span class="linenos">190</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">545</span>, <span class="hljs-keyword">in</span> save
<span class="linenos">191</span>	    force_update=force_update, update_fields=update_fields)
<span class="linenos">192</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">573</span>, <span class="hljs-keyword">in</span> save_base
<span class="linenos">193</span>	    updated = self._save_table(raw, cls, force_insert, force_update, using, update_fields)
<span class="linenos">194</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">654</span>, <span class="hljs-keyword">in</span> _save_table
<span class="linenos">195</span>	    result = self._<span class="hljs-keyword">do</span>_insert(cls._base_manager, using, fields, update_pk, raw)
<span class="linenos">196</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">687</span>, <span class="hljs-keyword">in</span> _<span class="hljs-keyword">do</span>_insert
<span class="linenos">197</span>	    using=using, raw=raw)
<span class="linenos">198</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/manager.py"</span>, line <span class="hljs-number">232</span>, <span class="hljs-keyword">in</span> _insert
<span class="linenos">199</span>	    <span class="hljs-built_in">return</span> insert_query(self.model, objs, fields, **kwargs)
<span class="linenos">200</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/query.py"</span>, line <span class="hljs-number">1511</span>, <span class="hljs-keyword">in</span> insert_query
<span class="linenos">201</span>	    <span class="hljs-built_in">return</span> query.get_compiler(using=using).execute_sql(<span class="hljs-built_in">return</span>_id)
<span class="linenos">202</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/sql/compiler.py"</span>, line <span class="hljs-number">898</span>, <span class="hljs-keyword">in</span> execute_sql
<span class="linenos">203</span>	    cursor.execute(sql, params)
<span class="linenos">204</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">205</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">206</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/utils.py"</span>, line <span class="hljs-number">99</span>, <span class="hljs-keyword">in</span> __<span class="hljs-built_in">exit</span>__
<span class="linenos">207</span>	    six.reraise(dj_exc_<span class="hljs-built_in">type</span>, dj_exc_value, traceback)
<span class="linenos">208</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">209</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">210</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/sqlite3/base.py"</span>, line <span class="hljs-number">450</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">211</span>	    <span class="hljs-built_in">return</span> Database.Cursor.execute(self, query, params)
<span class="linenos">212</span>	OperationalError: no such table: blogengine_category
<span class="linenos">213</span>	
<span class="linenos">214</span>	======================================================================
<span class="linenos">215</span>	ERROR: <span class="hljs-built_in">test</span>_index (blogengine.tests.PostViewTest)
<span class="linenos">216</span>	----------------------------------------------------------------------
<span class="linenos">217</span>	Traceback (most recent call last):
<span class="linenos">218</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">353</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_index
<span class="linenos">219</span>	    category.save()
<span class="linenos">220</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">545</span>, <span class="hljs-keyword">in</span> save
<span class="linenos">221</span>	    force_update=force_update, update_fields=update_fields)
<span class="linenos">222</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">573</span>, <span class="hljs-keyword">in</span> save_base
<span class="linenos">223</span>	    updated = self._save_table(raw, cls, force_insert, force_update, using, update_fields)
<span class="linenos">224</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">654</span>, <span class="hljs-keyword">in</span> _save_table
<span class="linenos">225</span>	    result = self._<span class="hljs-keyword">do</span>_insert(cls._base_manager, using, fields, update_pk, raw)
<span class="linenos">226</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">687</span>, <span class="hljs-keyword">in</span> _<span class="hljs-keyword">do</span>_insert
<span class="linenos">227</span>	    using=using, raw=raw)
<span class="linenos">228</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/manager.py"</span>, line <span class="hljs-number">232</span>, <span class="hljs-keyword">in</span> _insert
<span class="linenos">229</span>	    <span class="hljs-built_in">return</span> insert_query(self.model, objs, fields, **kwargs)
<span class="linenos">230</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/query.py"</span>, line <span class="hljs-number">1511</span>, <span class="hljs-keyword">in</span> insert_query
<span class="linenos">231</span>	    <span class="hljs-built_in">return</span> query.get_compiler(using=using).execute_sql(<span class="hljs-built_in">return</span>_id)
<span class="linenos">232</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/sql/compiler.py"</span>, line <span class="hljs-number">898</span>, <span class="hljs-keyword">in</span> execute_sql
<span class="linenos">233</span>	    cursor.execute(sql, params)
<span class="linenos">234</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">235</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">236</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/utils.py"</span>, line <span class="hljs-number">99</span>, <span class="hljs-keyword">in</span> __<span class="hljs-built_in">exit</span>__
<span class="linenos">237</span>	    six.reraise(dj_exc_<span class="hljs-built_in">type</span>, dj_exc_value, traceback)
<span class="linenos">238</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">239</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">240</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/sqlite3/base.py"</span>, line <span class="hljs-number">450</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">241</span>	    <span class="hljs-built_in">return</span> Database.Cursor.execute(self, query, params)
<span class="linenos">242</span>	OperationalError: no such table: blogengine_category
<span class="linenos">243</span>	
<span class="linenos">244</span>	======================================================================
<span class="linenos">245</span>	ERROR: <span class="hljs-built_in">test</span>_post_page (blogengine.tests.PostViewTest)
<span class="linenos">246</span>	----------------------------------------------------------------------
<span class="linenos">247</span>	Traceback (most recent call last):
<span class="linenos">248</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">403</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_post_page
<span class="linenos">249</span>	    category.save()
<span class="linenos">250</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">545</span>, <span class="hljs-keyword">in</span> save
<span class="linenos">251</span>	    force_update=force_update, update_fields=update_fields)
<span class="linenos">252</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">573</span>, <span class="hljs-keyword">in</span> save_base
<span class="linenos">253</span>	    updated = self._save_table(raw, cls, force_insert, force_update, using, update_fields)
<span class="linenos">254</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">654</span>, <span class="hljs-keyword">in</span> _save_table
<span class="linenos">255</span>	    result = self._<span class="hljs-keyword">do</span>_insert(cls._base_manager, using, fields, update_pk, raw)
<span class="linenos">256</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">687</span>, <span class="hljs-keyword">in</span> _<span class="hljs-keyword">do</span>_insert
<span class="linenos">257</span>	    using=using, raw=raw)
<span class="linenos">258</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/manager.py"</span>, line <span class="hljs-number">232</span>, <span class="hljs-keyword">in</span> _insert
<span class="linenos">259</span>	    <span class="hljs-built_in">return</span> insert_query(self.model, objs, fields, **kwargs)
<span class="linenos">260</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/query.py"</span>, line <span class="hljs-number">1511</span>, <span class="hljs-keyword">in</span> insert_query
<span class="linenos">261</span>	    <span class="hljs-built_in">return</span> query.get_compiler(using=using).execute_sql(<span class="hljs-built_in">return</span>_id)
<span class="linenos">262</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/sql/compiler.py"</span>, line <span class="hljs-number">898</span>, <span class="hljs-keyword">in</span> execute_sql
<span class="linenos">263</span>	    cursor.execute(sql, params)
<span class="linenos">264</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">265</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">266</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/utils.py"</span>, line <span class="hljs-number">99</span>, <span class="hljs-keyword">in</span> __<span class="hljs-built_in">exit</span>__
<span class="linenos">267</span>	    six.reraise(dj_exc_<span class="hljs-built_in">type</span>, dj_exc_value, traceback)
<span class="linenos">268</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">269</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">270</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/sqlite3/base.py"</span>, line <span class="hljs-number">450</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">271</span>	    <span class="hljs-built_in">return</span> Database.Cursor.execute(self, query, params)
<span class="linenos">272</span>	OperationalError: no such table: blogengine_category
<span class="linenos">273</span>	
<span class="linenos">274</span>	======================================================================
<span class="linenos">275</span>	FAIL: <span class="hljs-built_in">test</span>_create_category (blogengine.tests.AdminTest)
<span class="linenos">276</span>	----------------------------------------------------------------------
<span class="linenos">277</span>	Traceback (most recent call last):
<span class="linenos">278</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">142</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_create_category
<span class="linenos">279</span>	    self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">280</span>	AssertionError: <span class="hljs-number">404</span> != <span class="hljs-number">200</span>
<span class="linenos">281</span>	
<span class="linenos">282</span>	----------------------------------------------------------------------
<span class="linenos">283</span>	Ran <span class="hljs-number">13</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">3.393</span>s
<span class="linenos">284</span>	
<span class="linenos">285</span>	FAILED (failures=<span class="hljs-number">1</span>, errors=<span class="hljs-number">9</span>)
<span class="linenos">286</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>The category table hasn’t yet been created, so we need to use South to create and run the migrations:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py schemamigration --auto blogengine
<span class="linenos">2</span>	$ python manage.py migrate</code></pre>
<p>If we then run our tests again, some of them will still fail:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">2</span>	..F.F.F......
<span class="linenos">3</span>	======================================================================
<span class="linenos">4</span>	FAIL: <span class="hljs-built_in">test</span>_create_category (blogengine.tests.AdminTest)
<span class="linenos">5</span>	----------------------------------------------------------------------
<span class="linenos">6</span>	Traceback (most recent call last):
<span class="linenos">7</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">142</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_create_category
<span class="linenos">8</span>	    self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">9</span>	AssertionError: <span class="hljs-number">404</span> != <span class="hljs-number">200</span>
<span class="linenos">10</span>	
<span class="linenos">11</span>	======================================================================
<span class="linenos">12</span>	FAIL: <span class="hljs-built_in">test</span>_delete_category (blogengine.tests.AdminTest)
<span class="linenos">13</span>	----------------------------------------------------------------------
<span class="linenos">14</span>	Traceback (most recent call last):
<span class="linenos">15</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">201</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_delete_category
<span class="linenos">16</span>	    self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">17</span>	AssertionError: <span class="hljs-number">404</span> != <span class="hljs-number">200</span>
<span class="linenos">18</span>	
<span class="linenos">19</span>	======================================================================
<span class="linenos">20</span>	FAIL: <span class="hljs-built_in">test</span>_edit_category (blogengine.tests.AdminTest)
<span class="linenos">21</span>	----------------------------------------------------------------------
<span class="linenos">22</span>	Traceback (most recent call last):
<span class="linenos">23</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">175</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_edit_category
<span class="linenos">24</span>	    self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">25</span>	AssertionError: <span class="hljs-number">404</span> != <span class="hljs-number">200</span>
<span class="linenos">26</span>	
<span class="linenos">27</span>	----------------------------------------------------------------------
<span class="linenos">28</span>	Ran <span class="hljs-number">13</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">4.047</span>s
<span class="linenos">29</span>	
<span class="linenos">30</span>	FAILED (failures=<span class="hljs-number">3</span>)
<span class="linenos">31</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>That’s because we haven’t registered the categories in the admin. So, that’s our next job:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-keyword">import</span> models
<span class="linenos">2</span>	<span class="hljs-keyword">from</span> django.contrib <span class="hljs-keyword">import</span> admin
<span class="linenos">3</span>	<span class="hljs-keyword">from</span> django.contrib.auth.models <span class="hljs-keyword">import</span> User
<span class="linenos">4</span>	
<span class="linenos">5</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">PostAdmin</span><span class="hljs-params">(admin.ModelAdmin)</span>:</span>
<span class="linenos">6</span>	    prepopulated_fields = {<span class="hljs-string">"slug"</span>: (<span class="hljs-string">"title"</span>,)}
<span class="linenos">7</span>	    exclude = (<span class="hljs-string">'author'</span>,)
<span class="linenos">8</span>	
<span class="linenos">9</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">save_model</span><span class="hljs-params">(self, request, obj, form, change)</span>:</span>
<span class="linenos">10</span>	        obj.author = request.user
<span class="linenos">11</span>	        obj.save()
<span class="linenos">12</span>	
<span class="linenos">13</span>	admin.site.register(models.Category)
<span class="linenos">14</span>	admin.site.register(models.Post, PostAdmin)</code></pre>
<p>Now we try again:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	(venv)Smith:django_tutorial_blog_ng matthewdaly$ python manage.py <span class="hljs-built_in">test</span> blogengine
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	.............
<span class="linenos">4</span>	----------------------------------------------------------------------
<span class="linenos">5</span>	Ran <span class="hljs-number">13</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">4.092</span>s
<span class="linenos">6</span>	
<span class="linenos">7</span>	OK
<span class="linenos">8</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>It passes! Let’s do a quick sense check before committing. Run the server:</p>
<pre><code class="hljs lang-bash singleline">$ python manage.py runserver</code></pre>
<p>If you visit the <a href="http://127.0.0.1:8000/admin/">admin</a>, you’ll see the text for category is <code>Categorys</code>, which is incorrect. We also don’t have a good representation of the category in the admin. Let’s fix that:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-keyword">from</span> django.db <span class="hljs-keyword">import</span> models
<span class="linenos">2</span>	<span class="hljs-keyword">from</span> django.contrib.auth.models <span class="hljs-keyword">import</span> User
<span class="linenos">3</span>	<span class="hljs-keyword">from</span> django.contrib.sites.models <span class="hljs-keyword">import</span> Site
<span class="linenos">4</span>	
<span class="linenos">5</span>	<span class="hljs-comment"># Create your models here.</span>
<span class="linenos">6</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Category</span><span class="hljs-params">(models.Model)</span>:</span>
<span class="linenos">7</span>	    name = models.CharField(max_length=<span class="hljs-number">200</span>)
<span class="linenos">8</span>	    description = models.TextField()
<span class="linenos">9</span>	
<span class="linenos">10</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">__unicode__</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">11</span>	        <span class="hljs-keyword">return</span> self.name
<span class="linenos">12</span>	
<span class="linenos">13</span>	    <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Meta</span>:</span>
<span class="linenos">14</span>	        verbose_name_plural = <span class="hljs-string">'categories'</span>
<span class="linenos">15</span>	
<span class="linenos">16</span>	
<span class="linenos">17</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Post</span><span class="hljs-params">(models.Model)</span>:</span>
<span class="linenos">18</span>	    title = models.CharField(max_length=<span class="hljs-number">200</span>)
<span class="linenos">19</span>	    pub_date = models.DateTimeField()
<span class="linenos">20</span>	    text = models.TextField()
<span class="linenos">21</span>	    slug = models.SlugField(max_length=<span class="hljs-number">40</span>, unique=<span class="hljs-keyword">True</span>)
<span class="linenos">22</span>	    author = models.ForeignKey(User)
<span class="linenos">23</span>	    site = models.ForeignKey(Site)
<span class="linenos">24</span>	    category = models.ForeignKey(Category, blank=<span class="hljs-keyword">True</span>, null=<span class="hljs-keyword">True</span>)
<span class="linenos">25</span>	
<span class="linenos">26</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">get_absolute_url</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">27</span>	        <span class="hljs-keyword">return</span> <span class="hljs-string">"/%s/%s/%s/"</span> % (self.pub_date.year, self.pub_date.month, self.slug)
<span class="linenos">28</span>	
<span class="linenos">29</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">__unicode__</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">30</span>	        <span class="hljs-keyword">return</span> self.title
<span class="linenos">31</span>	
<span class="linenos">32</span>	    <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Meta</span>:</span>
<span class="linenos">33</span>	        ordering = [<span class="hljs-string">"-pub_date"</span>]</code></pre>
<p>Let’s commit our changes:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add blogengine/
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Implemented categories'</span></code></pre>
<p>Now, as yet our categories don’t actually do all that much. We would like to be able to:</p>
<ul>
<li>List all posts under a category</li>
<li>Show the post category at the base of the post</li>
</ul>
<p>So, let’s implement that. First, as usual, we implement tests first:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">PostViewTest</span><span class="hljs-params">(BaseAcceptanceTest)</span>:</span>
<span class="linenos">2</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_index</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">3</span>	        <span class="hljs-comment"># Create the category</span>
<span class="linenos">4</span>	        category = Category()
<span class="linenos">5</span>	        category.name = <span class="hljs-string">'python'</span>
<span class="linenos">6</span>	        category.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">7</span>	        category.save()
<span class="linenos">8</span>	
<span class="linenos">9</span>	        <span class="hljs-comment"># Create the author</span>
<span class="linenos">10</span>	        author = User.objects.create_user(<span class="hljs-string">'testuser'</span>, <span class="hljs-string">'user@example.com'</span>, <span class="hljs-string">'password'</span>)
<span class="linenos">11</span>	        author.save()
<span class="linenos">12</span>	
<span class="linenos">13</span>	        <span class="hljs-comment"># Create the site</span>
<span class="linenos">14</span>	        site = Site()
<span class="linenos">15</span>	        site.name = <span class="hljs-string">'example.com'</span>
<span class="linenos">16</span>	        site.domain = <span class="hljs-string">'example.com'</span>
<span class="linenos">17</span>	        site.save()
<span class="linenos">18</span>	
<span class="linenos">19</span>	        <span class="hljs-comment"># Create the post</span>
<span class="linenos">20</span>	        post = Post()
<span class="linenos">21</span>	        post.title = <span class="hljs-string">'My first post'</span>
<span class="linenos">22</span>	        post.text = <span class="hljs-string">'This is [my first blog post](http://127.0.0.1:8000/)'</span>
<span class="linenos">23</span>	        post.slug = <span class="hljs-string">'my-first-post'</span>
<span class="linenos">24</span>	        post.pub_date = timezone.now()
<span class="linenos">25</span>	        post.author = author
<span class="linenos">26</span>	        post.site = site
<span class="linenos">27</span>	        post.category = category
<span class="linenos">28</span>	        post.save()
<span class="linenos">29</span>	
<span class="linenos">30</span>	        <span class="hljs-comment"># Check new post saved</span>
<span class="linenos">31</span>	        all_posts = Post.objects.all()
<span class="linenos">32</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">33</span>	
<span class="linenos">34</span>	        <span class="hljs-comment"># Fetch the index</span>
<span class="linenos">35</span>	        response = self.client.get(<span class="hljs-string">'/'</span>)
<span class="linenos">36</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">37</span>	
<span class="linenos">38</span>	        <span class="hljs-comment"># Check the post title is in the response</span>
<span class="linenos">39</span>	        self.assertTrue(post.title <span class="hljs-keyword">in</span> response.content)
<span class="linenos">40</span>	
<span class="linenos">41</span>	        <span class="hljs-comment"># Check the post text is in the response</span>
<span class="linenos">42</span>	        self.assertTrue(markdown.markdown(post.text) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">43</span>	
<span class="linenos">44</span>	        <span class="hljs-comment"># Check the post category is in the response</span>
<span class="linenos">45</span>	        self.assertTrue(post.category.name <span class="hljs-keyword">in</span> response.content)
<span class="linenos">46</span>	
<span class="linenos">47</span>	        <span class="hljs-comment"># Check the post date is in the response</span>
<span class="linenos">48</span>	        self.assertTrue(str(post.pub_date.year) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">49</span>	        self.assertTrue(post.pub_date.strftime(<span class="hljs-string">'%b'</span>) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">50</span>	        self.assertTrue(str(post.pub_date.day) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">51</span>	
<span class="linenos">52</span>	        <span class="hljs-comment"># Check the link is marked up properly</span>
<span class="linenos">53</span>	        self.assertTrue(<span class="hljs-string">'&lt;a href="http://127.0.0.1:8000/"&gt;my first blog post&lt;/a&gt;'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">54</span>	
<span class="linenos">55</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_post_page</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">56</span>	        <span class="hljs-comment"># Create the category</span>
<span class="linenos">57</span>	        category = Category()
<span class="linenos">58</span>	        category.name = <span class="hljs-string">'python'</span>
<span class="linenos">59</span>	        category.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">60</span>	        category.save()
<span class="linenos">61</span>	
<span class="linenos">62</span>	        <span class="hljs-comment"># Create the author</span>
<span class="linenos">63</span>	        author = User.objects.create_user(<span class="hljs-string">'testuser'</span>, <span class="hljs-string">'user@example.com'</span>, <span class="hljs-string">'password'</span>)
<span class="linenos">64</span>	        author.save()
<span class="linenos">65</span>	
<span class="linenos">66</span>	        <span class="hljs-comment"># Create the site</span>
<span class="linenos">67</span>	        site = Site()
<span class="linenos">68</span>	        site.name = <span class="hljs-string">'example.com'</span>
<span class="linenos">69</span>	        site.domain = <span class="hljs-string">'example.com'</span>
<span class="linenos">70</span>	        site.save()
<span class="linenos">71</span>	
<span class="linenos">72</span>	        <span class="hljs-comment"># Create the post</span>
<span class="linenos">73</span>	        post = Post()
<span class="linenos">74</span>	        post.title = <span class="hljs-string">'My first post'</span>
<span class="linenos">75</span>	        post.text = <span class="hljs-string">'This is [my first blog post](http://127.0.0.1:8000/)'</span>
<span class="linenos">76</span>	        post.slug = <span class="hljs-string">'my-first-post'</span>
<span class="linenos">77</span>	        post.pub_date = timezone.now()
<span class="linenos">78</span>	        post.author = author
<span class="linenos">79</span>	        post.site = site
<span class="linenos">80</span>	        post.category = category
<span class="linenos">81</span>	        post.save()
<span class="linenos">82</span>	
<span class="linenos">83</span>	        <span class="hljs-comment"># Check new post saved</span>
<span class="linenos">84</span>	        all_posts = Post.objects.all()
<span class="linenos">85</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">86</span>	        only_post = all_posts[<span class="hljs-number">0</span>]
<span class="linenos">87</span>	        self.assertEquals(only_post, post)
<span class="linenos">88</span>	
<span class="linenos">89</span>	        <span class="hljs-comment"># Get the post URL</span>
<span class="linenos">90</span>	        post_url = only_post.get_absolute_url()
<span class="linenos">91</span>	
<span class="linenos">92</span>	        <span class="hljs-comment"># Fetch the post</span>
<span class="linenos">93</span>	        response = self.client.get(post_url)
<span class="linenos">94</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">95</span>	
<span class="linenos">96</span>	        <span class="hljs-comment"># Check the post title is in the response</span>
<span class="linenos">97</span>	        self.assertTrue(post.title <span class="hljs-keyword">in</span> response.content)
<span class="linenos">98</span>	
<span class="linenos">99</span>	        <span class="hljs-comment"># Check the post category is in the response</span>
<span class="linenos">100</span>	        self.assertTrue(post.category.name <span class="hljs-keyword">in</span> response.content)
<span class="linenos">101</span>	
<span class="linenos">102</span>	        <span class="hljs-comment"># Check the post text is in the response</span>
<span class="linenos">103</span>	        self.assertTrue(markdown.markdown(post.text) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">104</span>	
<span class="linenos">105</span>	        <span class="hljs-comment"># Check the post date is in the response</span>
<span class="linenos">106</span>	        self.assertTrue(str(post.pub_date.year) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">107</span>	        self.assertTrue(post.pub_date.strftime(<span class="hljs-string">'%b'</span>) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">108</span>	        self.assertTrue(str(post.pub_date.day) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">109</span>	
<span class="linenos">110</span>	        <span class="hljs-comment"># Check the link is marked up properly</span>
<span class="linenos">111</span>	        self.assertTrue(<span class="hljs-string">'&lt;a href="http://127.0.0.1:8000/"&gt;my first blog post&lt;/a&gt;'</span> <span class="hljs-keyword">in</span> response.content)</code></pre>
<p>All we do here is assert that for both the post pages and the index, the text from the category name is shown in the response. We also need to check the category-specific route works. Add this method to <code>PostViewTest</code>:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_category_page</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">2</span>	        <span class="hljs-comment"># Create the category</span>
<span class="linenos">3</span>	        category = Category()
<span class="linenos">4</span>	        category.name = <span class="hljs-string">'python'</span>
<span class="linenos">5</span>	        category.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">6</span>	        category.save()
<span class="linenos">7</span>	
<span class="linenos">8</span>	        <span class="hljs-comment"># Create the author</span>
<span class="linenos">9</span>	        author = User.objects.create_user(<span class="hljs-string">'testuser'</span>, <span class="hljs-string">'user@example.com'</span>, <span class="hljs-string">'password'</span>)
<span class="linenos">10</span>	        author.save()
<span class="linenos">11</span>	
<span class="linenos">12</span>	        <span class="hljs-comment"># Create the site</span>
<span class="linenos">13</span>	        site = Site()
<span class="linenos">14</span>	        site.name = <span class="hljs-string">'example.com'</span>
<span class="linenos">15</span>	        site.domain = <span class="hljs-string">'example.com'</span>
<span class="linenos">16</span>	        site.save()
<span class="linenos">17</span>	
<span class="linenos">18</span>	        <span class="hljs-comment"># Create the post</span>
<span class="linenos">19</span>	        post = Post()
<span class="linenos">20</span>	        post.title = <span class="hljs-string">'My first post'</span>
<span class="linenos">21</span>	        post.text = <span class="hljs-string">'This is [my first blog post](http://127.0.0.1:8000/)'</span>
<span class="linenos">22</span>	        post.slug = <span class="hljs-string">'my-first-post'</span>
<span class="linenos">23</span>	        post.pub_date = timezone.now()
<span class="linenos">24</span>	        post.author = author
<span class="linenos">25</span>	        post.site = site
<span class="linenos">26</span>	        post.category = category
<span class="linenos">27</span>	        post.save()
<span class="linenos">28</span>	
<span class="linenos">29</span>	        <span class="hljs-comment"># Check new post saved</span>
<span class="linenos">30</span>	        all_posts = Post.objects.all()
<span class="linenos">31</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">32</span>	        only_post = all_posts[<span class="hljs-number">0</span>]
<span class="linenos">33</span>	        self.assertEquals(only_post, post)
<span class="linenos">34</span>	
<span class="linenos">35</span>	        <span class="hljs-comment"># Get the category URL</span>
<span class="linenos">36</span>	        category_url = post.category.get_absolute_url()
<span class="linenos">37</span>	
<span class="linenos">38</span>	        <span class="hljs-comment"># Fetch the category</span>
<span class="linenos">39</span>	        response = self.client.get(category_url)
<span class="linenos">40</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">41</span>	
<span class="linenos">42</span>	        <span class="hljs-comment"># Check the category name is in the response</span>
<span class="linenos">43</span>	        self.assertTrue(post.category.name <span class="hljs-keyword">in</span> response.content)
<span class="linenos">44</span>	
<span class="linenos">45</span>	        <span class="hljs-comment"># Check the post text is in the response</span>
<span class="linenos">46</span>	        self.assertTrue(markdown.markdown(post.text) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">47</span>	
<span class="linenos">48</span>	        <span class="hljs-comment"># Check the post date is in the response</span>
<span class="linenos">49</span>	        self.assertTrue(str(post.pub_date.year) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">50</span>	        self.assertTrue(post.pub_date.strftime(<span class="hljs-string">'%b'</span>) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">51</span>	        self.assertTrue(str(post.pub_date.day) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">52</span>	
<span class="linenos">53</span>	        <span class="hljs-comment"># Check the link is marked up properly</span>
<span class="linenos">54</span>	        self.assertTrue(<span class="hljs-string">'&lt;a href="http://127.0.0.1:8000/"&gt;my first blog post&lt;/a&gt;'</span> <span class="hljs-keyword">in</span> response.content)</code></pre>
<p>This is very similar to the previous tests, but fetches the absolute URL for the category, and ensures the category name and post content are shown. Now, let’s run our new tests:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	(venv)Smith:django_tutorial_blog_ng matthewdaly$ python manage.py <span class="hljs-built_in">test</span> blogengine
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	...........EFF
<span class="linenos">4</span>	======================================================================
<span class="linenos">5</span>	ERROR: <span class="hljs-built_in">test</span>_category_page (blogengine.tests.PostViewTest)
<span class="linenos">6</span>	----------------------------------------------------------------------
<span class="linenos">7</span>	Traceback (most recent call last):
<span class="linenos">8</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">494</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_category_page
<span class="linenos">9</span>	    category_url = post.category.get_absolute_url()
<span class="linenos">10</span>	AttributeError: <span class="hljs-string">'Category'</span> object has no attribute <span class="hljs-string">'get_absolute_url'</span>
<span class="linenos">11</span>	
<span class="linenos">12</span>	======================================================================
<span class="linenos">13</span>	FAIL: <span class="hljs-built_in">test</span>_index (blogengine.tests.PostViewTest)
<span class="linenos">14</span>	----------------------------------------------------------------------
<span class="linenos">15</span>	Traceback (most recent call last):
<span class="linenos">16</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">391</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_index
<span class="linenos">17</span>	    self.assertTrue(post.category.name <span class="hljs-keyword">in</span> response.content)
<span class="linenos">18</span>	AssertionError: False is not <span class="hljs-literal">true</span>
<span class="linenos">19</span>	
<span class="linenos">20</span>	======================================================================
<span class="linenos">21</span>	FAIL: <span class="hljs-built_in">test</span>_post_page (blogengine.tests.PostViewTest)
<span class="linenos">22</span>	----------------------------------------------------------------------
<span class="linenos">23</span>	Traceback (most recent call last):
<span class="linenos">24</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">446</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_post_page
<span class="linenos">25</span>	    self.assertTrue(post.category.name <span class="hljs-keyword">in</span> response.content)
<span class="linenos">26</span>	AssertionError: False is not <span class="hljs-literal">true</span>
<span class="linenos">27</span>	
<span class="linenos">28</span>	----------------------------------------------------------------------
<span class="linenos">29</span>	Ran <span class="hljs-number">14</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">5.017</span>s
<span class="linenos">30</span>	
<span class="linenos">31</span>	FAILED (failures=<span class="hljs-number">2</span>, errors=<span class="hljs-number">1</span>)
<span class="linenos">32</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>Let’s take a look at why they failed. <code>test_category_page</code> failed because the <code>Category</code> object had no method <code>get_absolute_url</code>. So we need to implement one. To do so, we really need to add a slug field, like the posts already have. Ideally, we want this to be populated automatically, but with the option to create one manually. So, edit the models as follows:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-keyword">from</span> django.db <span class="hljs-keyword">import</span> models
<span class="linenos">2</span>	<span class="hljs-keyword">from</span> django.contrib.auth.models <span class="hljs-keyword">import</span> User
<span class="linenos">3</span>	<span class="hljs-keyword">from</span> django.contrib.sites.models <span class="hljs-keyword">import</span> Site
<span class="linenos">4</span>	<span class="hljs-keyword">from</span> django.utils.text <span class="hljs-keyword">import</span> slugify
<span class="linenos">5</span>	
<span class="linenos">6</span>	<span class="hljs-comment"># Create your models here.</span>
<span class="linenos">7</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Category</span><span class="hljs-params">(models.Model)</span>:</span>
<span class="linenos">8</span>	    name = models.CharField(max_length=<span class="hljs-number">200</span>)
<span class="linenos">9</span>	    description = models.TextField()
<span class="linenos">10</span>	    slug = models.SlugField(max_length=<span class="hljs-number">40</span>, unique=<span class="hljs-keyword">True</span>, blank=<span class="hljs-keyword">True</span>, null=<span class="hljs-keyword">True</span>)
<span class="linenos">11</span>	
<span class="linenos">12</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">save</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">13</span>	        <span class="hljs-keyword">if</span> <span class="hljs-keyword">not</span> self.slug:
<span class="linenos">14</span>	            self.slug = slugify(unicode(self.name))
<span class="linenos">15</span>	        super(Category, self).save()
<span class="linenos">16</span>	
<span class="linenos">17</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">get_absolute_url</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">18</span>	        <span class="hljs-keyword">return</span> <span class="hljs-string">"/category/%s/"</span> % (self.slug)
<span class="linenos">19</span>	
<span class="linenos">20</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">__unicode__</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">21</span>	        <span class="hljs-keyword">return</span> self.name
<span class="linenos">22</span>	
<span class="linenos">23</span>	    <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Meta</span>:</span>
<span class="linenos">24</span>	        verbose_name_plural = <span class="hljs-string">'categories'</span>
<span class="linenos">25</span>	
<span class="linenos">26</span>	
<span class="linenos">27</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Post</span><span class="hljs-params">(models.Model)</span>:</span>
<span class="linenos">28</span>	    title = models.CharField(max_length=<span class="hljs-number">200</span>)
<span class="linenos">29</span>	    pub_date = models.DateTimeField()
<span class="linenos">30</span>	    text = models.TextField()
<span class="linenos">31</span>	    slug = models.SlugField(max_length=<span class="hljs-number">40</span>, unique=<span class="hljs-keyword">True</span>)
<span class="linenos">32</span>	    author = models.ForeignKey(User)
<span class="linenos">33</span>	    site = models.ForeignKey(Site)
<span class="linenos">34</span>	    category = models.ForeignKey(Category, blank=<span class="hljs-keyword">True</span>, null=<span class="hljs-keyword">True</span>)
<span class="linenos">35</span>	
<span class="linenos">36</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">get_absolute_url</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">37</span>	        <span class="hljs-keyword">return</span> <span class="hljs-string">"/%s/%s/%s/"</span> % (self.pub_date.year, self.pub_date.month, self.slug)
<span class="linenos">38</span>	
<span class="linenos">39</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">__unicode__</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">40</span>	        <span class="hljs-keyword">return</span> self.title
<span class="linenos">41</span>	
<span class="linenos">42</span>	    <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Meta</span>:</span>
<span class="linenos">43</span>	        ordering = [<span class="hljs-string">"-pub_date"</span>]</code></pre>
<p>We’re adding the <code>slug</code> attribute to the <code>Category</code> model here. However, we’re also overriding the <code>save</code> method to detect if the slug is set, and if not, to create a slug using the <code>slugify</code> function, and set it as the category’s slug. We also define an absolute URL for the category.</p>
<p>Now, if you run the tests, they will fail because we haven’t made the changes to the database. So, we use South again:</p>
<pre><code class="hljs lang-bash singleline">$ python manage.py schemamigration --auto blogengine</code></pre>
<p>Then run the migration:</p>
<pre><code class="hljs lang-bash singleline">$ python manage.py migrate</code></pre>
<p>Now, running our tests will show that the tables are in place, but we still have some work to do. The index and post pages don’t show our categories, so we’ll fix that. First, we’ll fix our post list:</p>
<pre><code class="hljs lang-django"><span class="linenos">1</span>	<span class="xml"></span><span class="hljs-template_tag">{% <span class="hljs-keyword">extends</span> "blogengine/includes/base.html" %}</span><span class="xml">
<span class="linenos">2</span>	
<span class="linenos">3</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">load</span> custom_markdown %}</span><span class="xml">
<span class="linenos">4</span>	
<span class="linenos">5</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">block</span> content %}</span><span class="xml">
<span class="linenos">6</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">for</span> post <span class="hljs-keyword">in</span> object_list %}</span><span class="xml">
<span class="linenos">7</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"post"</span>&gt;</span>
<span class="linenos">8</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">h1</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ post.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span></span><span class="hljs-variable">{{ post.title }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">h1</span>&gt;</span>
<span class="linenos">9</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">h3</span>&gt;</span></span><span class="hljs-variable">{{ post.pub_date }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">h3</span>&gt;</span>
<span class="linenos">10</span>	        </span><span class="hljs-variable">{{ post.text<span class="hljs-filter">|custom</span>_markdown }}</span><span class="xml">
<span class="linenos">11</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">12</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ post.category.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span></span><span class="hljs-variable">{{ post.category.name }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">13</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endfor</span> %}</span><span class="xml">
<span class="linenos">14</span>	
<span class="linenos">15</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> page_obj.has_previous %}</span><span class="xml">
<span class="linenos">16</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/</span></span></span><span class="hljs-variable">{{ page_obj.previous_page_number }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">/"</span>&gt;</span>Previous Page<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">17</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">18</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> page_obj.has_next %}</span><span class="xml">
<span class="linenos">19</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/</span></span></span><span class="hljs-variable">{{ page_obj.next_page_number }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">/"</span>&gt;</span>Next Page<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">20</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">21</span>	
<span class="linenos">22</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endblock</span> %}</span><span class="xml"></span></code></pre>
<p>Next, we’ll take care of our post detail page:</p>
<pre><code class="hljs lang-django"><span class="linenos">1</span>	<span class="xml"></span><span class="hljs-template_tag">{% <span class="hljs-keyword">extends</span> "blogengine/includes/base.html" %}</span><span class="xml">
<span class="linenos">2</span>	
<span class="linenos">3</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">load</span> custom_markdown %}</span><span class="xml">
<span class="linenos">4</span>	
<span class="linenos">5</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">block</span> content %}</span><span class="xml">
<span class="linenos">6</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"post"</span>&gt;</span>
<span class="linenos">7</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">h1</span>&gt;</span></span><span class="hljs-variable">{{ object.title }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">h1</span>&gt;</span>
<span class="linenos">8</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">h3</span>&gt;</span></span><span class="hljs-variable">{{ object.pub_date }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">h3</span>&gt;</span>
<span class="linenos">9</span>	        </span><span class="hljs-variable">{{ object.text<span class="hljs-filter">|custom</span>_markdown }}</span><span class="xml">
<span class="linenos">10</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ object.category.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span></span><span class="hljs-variable">{{ object.category.name }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">11</span>	
<span class="linenos">12</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">h4</span>&gt;</span>Comments<span class="hljs-tag">&lt;/<span class="hljs-title">h4</span>&gt;</span>
<span class="linenos">13</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"fb-comments"</span> <span class="hljs-attribute">data-href</span>=<span class="hljs-value">"http://</span></span></span><span class="hljs-variable">{{ post.site }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value"></span></span></span><span class="hljs-variable">{{ post.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span> <span class="hljs-attribute">data-width</span>=<span class="hljs-value">"470"</span> <span class="hljs-attribute">data-num-posts</span>=<span class="hljs-value">"10"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">14</span>	
<span class="linenos">15</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">16</span>	
<span class="linenos">17</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endblock</span> %}</span><span class="xml"></span></code></pre>
<p>Note that in both cases we include a link to the category URL.</p>
<p>Now, we should only have one failing test outstanding - the category page. For this, generic views aren’t sufficient as we need to limit the queryset to only show those posts with a specific category. Fortunately, we can extend Django’s generic views to add this functionality. First, we edit our URLconfs:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-keyword">from</span> django.conf.urls <span class="hljs-keyword">import</span> patterns, url
<span class="linenos">2</span>	<span class="hljs-keyword">from</span> django.views.generic <span class="hljs-keyword">import</span> ListView, DetailView
<span class="linenos">3</span>	<span class="hljs-keyword">from</span> blogengine.models <span class="hljs-keyword">import</span> Post, Category
<span class="linenos">4</span>	<span class="hljs-keyword">from</span> blogengine.views <span class="hljs-keyword">import</span> CategoryListView
<span class="linenos">5</span>	
<span class="linenos">6</span>	urlpatterns = patterns(<span class="hljs-string">''</span>,
<span class="linenos">7</span>	    <span class="hljs-comment"># Index</span>
<span class="linenos">8</span>	    url(<span class="hljs-string">r'^(?P&lt;page&gt;\d+)?/?$'</span>, ListView.as_view(
<span class="linenos">9</span>	        model=Post,
<span class="linenos">10</span>	        paginate_by=<span class="hljs-number">5</span>,
<span class="linenos">11</span>	        )),
<span class="linenos">12</span>	
<span class="linenos">13</span>	    <span class="hljs-comment"># Individual posts</span>
<span class="linenos">14</span>	    url(<span class="hljs-string">r'^(?P&lt;pub_date__year&gt;\d{4})/(?P&lt;pub_date__month&gt;\d{1,2})/(?P&lt;slug&gt;[a-zA-Z0-9-]+)/?$'</span>, DetailView.as_view(
<span class="linenos">15</span>	        model=Post,
<span class="linenos">16</span>	        )),
<span class="linenos">17</span>	
<span class="linenos">18</span>	    <span class="hljs-comment"># Categories</span>
<span class="linenos">19</span>	    url(<span class="hljs-string">r'^category/(?P&lt;slug&gt;[a-zA-Z0-9-]+)/?$'</span>, CategoryListView.as_view(
<span class="linenos">20</span>	        paginate_by=<span class="hljs-number">5</span>,
<span class="linenos">21</span>	        model=Category,
<span class="linenos">22</span>	        )),
<span class="linenos">23</span>	)</code></pre>
<p>Note we import a new view from <code>blogengine.views</code> called <code>CategoryListView</code>. Next, we create that listview:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-keyword">from</span> django.shortcuts <span class="hljs-keyword">import</span> render
<span class="linenos">2</span>	<span class="hljs-keyword">from</span> django.views.generic <span class="hljs-keyword">import</span> ListView
<span class="linenos">3</span>	<span class="hljs-keyword">from</span> blogengine.models <span class="hljs-keyword">import</span> Category, Post
<span class="linenos">4</span>	
<span class="linenos">5</span>	<span class="hljs-comment"># Create your views here.</span>
<span class="linenos">6</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">CategoryListView</span><span class="hljs-params">(ListView)</span>:</span>
<span class="linenos">7</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">get_queryset</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">8</span>	        slug = self.kwargs[<span class="hljs-string">'slug'</span>]
<span class="linenos">9</span>	        <span class="hljs-keyword">try</span>:
<span class="linenos">10</span>	            category = Category.objects.get(slug=slug)
<span class="linenos">11</span>	            <span class="hljs-keyword">return</span> Post.objects.filter(category=category)
<span class="linenos">12</span>	        <span class="hljs-keyword">except</span> Category.DoesNotExist:
<span class="linenos">13</span>	            <span class="hljs-keyword">return</span> Post.objects.none()</code></pre>
<p>This is quite simple. We import the <code>ListView</code>, as well as our models. Then we extend <code>ListView</code> by getting the slug from the request, fetching the appropriate category, and returning only those posts that have that category. If the category does not exist, we return the empty <code>Post</code> object list. We haven’t had to set the template manually as it is inherited from <code>ListView</code>.</p>
<p>If you run the tests, they should now pass:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">2</span>	..............
<span class="linenos">3</span>	----------------------------------------------------------------------
<span class="linenos">4</span>	Ran <span class="hljs-number">14</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">5.083</span>s
<span class="linenos">5</span>	
<span class="linenos">6</span>	OK
<span class="linenos">7</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>So let’s commit our changes:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add blogengine/ templates/
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Categories are now shown'</span></code></pre>
<h2 id="tags">Tags</h2>
<p>Tags are fairly similar to categories, but more complex. The relationship they have is called <em>many-to-many</em> - in other words, a tag can be applied to many posts, and one post can have many tags. This is more difficult to model with a relational database. The usual way to do so is to create an intermediate table between the posts and tags, to identify mappings between the two. Fortunately, Django makes this quite easy.</p>
<p>Let’s write the tests for our tagging system. As with the categories, we’ll write the tests for creating and editing them first, and add in tests for them being visible later. First we’ll create a test for creating a new tag object:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_create_tag</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">2</span>	        <span class="hljs-comment"># Create the tag</span>
<span class="linenos">3</span>	        tag = Tag()
<span class="linenos">4</span>	
<span class="linenos">5</span>	        <span class="hljs-comment"># Add attributes</span>
<span class="linenos">6</span>	        tag.name = <span class="hljs-string">'python'</span>
<span class="linenos">7</span>	        tag.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">8</span>	
<span class="linenos">9</span>	        <span class="hljs-comment"># Save it</span>
<span class="linenos">10</span>	        tag.save()
<span class="linenos">11</span>	
<span class="linenos">12</span>	        <span class="hljs-comment"># Check we can find it</span>
<span class="linenos">13</span>	        all_tags = Tag.objects.all()
<span class="linenos">14</span>	        self.assertEquals(len(all_tags), <span class="hljs-number">1</span>)
<span class="linenos">15</span>	        only_tag = all_tags[<span class="hljs-number">0</span>]
<span class="linenos">16</span>	        self.assertEquals(only_tag, tag)
<span class="linenos">17</span>	
<span class="linenos">18</span>	        <span class="hljs-comment"># Check attributes</span>
<span class="linenos">19</span>	        self.assertEquals(only_tag.name, <span class="hljs-string">'python'</span>)
<span class="linenos">20</span>	        self.assertEquals(only_tag.description, <span class="hljs-string">'The Python programming language'</span>)</code></pre>
<p>Next, we’ll amend the test for creating a post to include tags:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_create_post</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">2</span>	        <span class="hljs-comment"># Create the category</span>
<span class="linenos">3</span>	        category = Category()
<span class="linenos">4</span>	        category.name = <span class="hljs-string">'python'</span>
<span class="linenos">5</span>	        category.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">6</span>	        category.save()
<span class="linenos">7</span>	
<span class="linenos">8</span>	        <span class="hljs-comment"># Create the tag</span>
<span class="linenos">9</span>	        tag = Tag()
<span class="linenos">10</span>	        tag.name = <span class="hljs-string">'python'</span>
<span class="linenos">11</span>	        tag.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">12</span>	        tag.save()
<span class="linenos">13</span>	
<span class="linenos">14</span>	        <span class="hljs-comment"># Create the author</span>
<span class="linenos">15</span>	        author = User.objects.create_user(<span class="hljs-string">'testuser'</span>, <span class="hljs-string">'user@example.com'</span>, <span class="hljs-string">'password'</span>)
<span class="linenos">16</span>	        author.save()
<span class="linenos">17</span>	
<span class="linenos">18</span>	        <span class="hljs-comment"># Create the site</span>
<span class="linenos">19</span>	        site = Site()
<span class="linenos">20</span>	        site.name = <span class="hljs-string">'example.com'</span>
<span class="linenos">21</span>	        site.domain = <span class="hljs-string">'example.com'</span>
<span class="linenos">22</span>	        site.save()
<span class="linenos">23</span>	
<span class="linenos">24</span>	        <span class="hljs-comment"># Create the post</span>
<span class="linenos">25</span>	        post = Post()
<span class="linenos">26</span>	
<span class="linenos">27</span>	        <span class="hljs-comment"># Set the attributes</span>
<span class="linenos">28</span>	        post.title = <span class="hljs-string">'My first post'</span>
<span class="linenos">29</span>	        post.text = <span class="hljs-string">'This is my first blog post'</span>
<span class="linenos">30</span>	        post.slug = <span class="hljs-string">'my-first-post'</span>
<span class="linenos">31</span>	        post.pub_date = timezone.now()
<span class="linenos">32</span>	        post.author = author
<span class="linenos">33</span>	        post.site = site
<span class="linenos">34</span>	        post.category = category
<span class="linenos">35</span>	
<span class="linenos">36</span>	        <span class="hljs-comment"># Save it</span>
<span class="linenos">37</span>	        post.save()
<span class="linenos">38</span>	
<span class="linenos">39</span>	        <span class="hljs-comment"># Add the tag</span>
<span class="linenos">40</span>	        post.tags.add(tag)
<span class="linenos">41</span>	        post.save()
<span class="linenos">42</span>	
<span class="linenos">43</span>	        <span class="hljs-comment"># Check we can find it</span>
<span class="linenos">44</span>	        all_posts = Post.objects.all()
<span class="linenos">45</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">46</span>	        only_post = all_posts[<span class="hljs-number">0</span>]
<span class="linenos">47</span>	        self.assertEquals(only_post, post)
<span class="linenos">48</span>	
<span class="linenos">49</span>	        <span class="hljs-comment"># Check attributes</span>
<span class="linenos">50</span>	        self.assertEquals(only_post.title, <span class="hljs-string">'My first post'</span>)
<span class="linenos">51</span>	        self.assertEquals(only_post.text, <span class="hljs-string">'This is my first blog post'</span>)
<span class="linenos">52</span>	        self.assertEquals(only_post.slug, <span class="hljs-string">'my-first-post'</span>)
<span class="linenos">53</span>	        self.assertEquals(only_post.site.name, <span class="hljs-string">'example.com'</span>)
<span class="linenos">54</span>	        self.assertEquals(only_post.site.domain, <span class="hljs-string">'example.com'</span>)
<span class="linenos">55</span>	        self.assertEquals(only_post.pub_date.day, post.pub_date.day)
<span class="linenos">56</span>	        self.assertEquals(only_post.pub_date.month, post.pub_date.month)
<span class="linenos">57</span>	        self.assertEquals(only_post.pub_date.year, post.pub_date.year)
<span class="linenos">58</span>	        self.assertEquals(only_post.pub_date.hour, post.pub_date.hour)
<span class="linenos">59</span>	        self.assertEquals(only_post.pub_date.minute, post.pub_date.minute)
<span class="linenos">60</span>	        self.assertEquals(only_post.pub_date.second, post.pub_date.second)
<span class="linenos">61</span>	        self.assertEquals(only_post.author.username, <span class="hljs-string">'testuser'</span>)
<span class="linenos">62</span>	        self.assertEquals(only_post.author.email, <span class="hljs-string">'user@example.com'</span>)
<span class="linenos">63</span>	        self.assertEquals(only_post.category.name, <span class="hljs-string">'python'</span>)
<span class="linenos">64</span>	        self.assertEquals(only_post.category.description, <span class="hljs-string">'The Python programming language'</span>)
<span class="linenos">65</span>	
<span class="linenos">66</span>	        <span class="hljs-comment"># Check tags</span>
<span class="linenos">67</span>	        post_tags = only_post.tags.all()
<span class="linenos">68</span>	        self.assertEquals(len(post_tags), <span class="hljs-number">1</span>)
<span class="linenos">69</span>	        only_post_tag = post_tags[<span class="hljs-number">0</span>]
<span class="linenos">70</span>	        self.assertEquals(only_post_tag, tag)
<span class="linenos">71</span>	        self.assertEquals(only_post_tag.name, <span class="hljs-string">'python'</span>)
<span class="linenos">72</span>	        self.assertEquals(only_post_tag.description, <span class="hljs-string">'The Python programming language'</span>)</code></pre>
<p>Note the difference in how we apply the tags. Because a post can have more than one tag, we can’t just define <code>post.tag</code> in the same way. Instead, we have <code>post.tags</code>, which you can think of as a list, and we use the <code>add</code> method to add a new tag. Note also that the post must already exist before we can add a tag.</p>
<p>We also need to create acceptance tests for creating, editing and deleting tags:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_create_tag</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">2</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">3</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">4</span>	
<span class="linenos">5</span>	        <span class="hljs-comment"># Check response code</span>
<span class="linenos">6</span>	        response = self.client.get(<span class="hljs-string">'/admin/blogengine/tag/add/'</span>)
<span class="linenos">7</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">8</span>	
<span class="linenos">9</span>	        <span class="hljs-comment"># Create the new tag</span>
<span class="linenos">10</span>	        response = self.client.post(<span class="hljs-string">'/admin/blogengine/tag/add/'</span>, {
<span class="linenos">11</span>	            <span class="hljs-string">'name'</span>: <span class="hljs-string">'python'</span>,
<span class="linenos">12</span>	            <span class="hljs-string">'description'</span>: <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">13</span>	            },
<span class="linenos">14</span>	            follow=<span class="hljs-keyword">True</span>
<span class="linenos">15</span>	        )
<span class="linenos">16</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">17</span>	
<span class="linenos">18</span>	        <span class="hljs-comment"># Check added successfully</span>
<span class="linenos">19</span>	        self.assertTrue(<span class="hljs-string">'added successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">20</span>	
<span class="linenos">21</span>	        <span class="hljs-comment"># Check new tag now in database</span>
<span class="linenos">22</span>	        all_tags = Tag.objects.all()
<span class="linenos">23</span>	        self.assertEquals(len(all_tags), <span class="hljs-number">1</span>)
<span class="linenos">24</span>	
<span class="linenos">25</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_edit_tag</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">26</span>	        <span class="hljs-comment"># Create the tag</span>
<span class="linenos">27</span>	        tag = Tag()
<span class="linenos">28</span>	        tag.name = <span class="hljs-string">'python'</span>
<span class="linenos">29</span>	        tag.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">30</span>	        tag.save()
<span class="linenos">31</span>	
<span class="linenos">32</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">33</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">34</span>	
<span class="linenos">35</span>	        <span class="hljs-comment"># Edit the tag</span>
<span class="linenos">36</span>	        response = self.client.post(<span class="hljs-string">'/admin/blogengine/tag/1/'</span>, {
<span class="linenos">37</span>	            <span class="hljs-string">'name'</span>: <span class="hljs-string">'perl'</span>,
<span class="linenos">38</span>	            <span class="hljs-string">'description'</span>: <span class="hljs-string">'The Perl programming language'</span>
<span class="linenos">39</span>	            }, follow=<span class="hljs-keyword">True</span>)
<span class="linenos">40</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">41</span>	
<span class="linenos">42</span>	        <span class="hljs-comment"># Check changed successfully</span>
<span class="linenos">43</span>	        self.assertTrue(<span class="hljs-string">'changed successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">44</span>	
<span class="linenos">45</span>	        <span class="hljs-comment"># Check tag amended</span>
<span class="linenos">46</span>	        all_tags = Tag.objects.all()
<span class="linenos">47</span>	        self.assertEquals(len(all_tags), <span class="hljs-number">1</span>)
<span class="linenos">48</span>	        only_tag = all_tags[<span class="hljs-number">0</span>]
<span class="linenos">49</span>	        self.assertEquals(only_tag.name, <span class="hljs-string">'perl'</span>)
<span class="linenos">50</span>	        self.assertEquals(only_tag.description, <span class="hljs-string">'The Perl programming language'</span>)
<span class="linenos">51</span>	
<span class="linenos">52</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_delete_tag</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">53</span>	        <span class="hljs-comment"># Create the tag</span>
<span class="linenos">54</span>	        tag = Tag()
<span class="linenos">55</span>	        tag.name = <span class="hljs-string">'python'</span>
<span class="linenos">56</span>	        tag.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">57</span>	        tag.save()
<span class="linenos">58</span>	
<span class="linenos">59</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">60</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">61</span>	
<span class="linenos">62</span>	        <span class="hljs-comment"># Delete the tag</span>
<span class="linenos">63</span>	        response = self.client.post(<span class="hljs-string">'/admin/blogengine/tag/1/delete/'</span>, {
<span class="linenos">64</span>	            <span class="hljs-string">'post'</span>: <span class="hljs-string">'yes'</span>
<span class="linenos">65</span>	        }, follow=<span class="hljs-keyword">True</span>)
<span class="linenos">66</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">67</span>	
<span class="linenos">68</span>	        <span class="hljs-comment"># Check deleted successfully</span>
<span class="linenos">69</span>	        self.assertTrue(<span class="hljs-string">'deleted successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">70</span>	
<span class="linenos">71</span>	        <span class="hljs-comment"># Check tag deleted</span>
<span class="linenos">72</span>	        all_tags = Tag.objects.all()
<span class="linenos">73</span>	        self.assertEquals(len(all_tags), <span class="hljs-number">0</span>)</code></pre>
<p>These tests are virtually identical to those for the <code>Category</code> objects, as we plan for our <code>Tag</code> objects to be very similar. Finally, we need to amend the acceptance tests for <code>Post</code> objects to include a tag:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_create_post</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">2</span>	        <span class="hljs-comment"># Create the category</span>
<span class="linenos">3</span>	        category = Category()
<span class="linenos">4</span>	        category.name = <span class="hljs-string">'python'</span>
<span class="linenos">5</span>	        category.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">6</span>	        category.save()
<span class="linenos">7</span>	
<span class="linenos">8</span>	        <span class="hljs-comment"># Create the tag</span>
<span class="linenos">9</span>	        tag = Tag()
<span class="linenos">10</span>	        tag.name = <span class="hljs-string">'python'</span>
<span class="linenos">11</span>	        tag.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">12</span>	        tag.save()
<span class="linenos">13</span>	
<span class="linenos">14</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">15</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">16</span>	
<span class="linenos">17</span>	        <span class="hljs-comment"># Check response code</span>
<span class="linenos">18</span>	        response = self.client.get(<span class="hljs-string">'/admin/blogengine/post/add/'</span>)
<span class="linenos">19</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">20</span>	
<span class="linenos">21</span>	        <span class="hljs-comment"># Create the new post</span>
<span class="linenos">22</span>	        response = self.client.post(<span class="hljs-string">'/admin/blogengine/post/add/'</span>, {
<span class="linenos">23</span>	            <span class="hljs-string">'title'</span>: <span class="hljs-string">'My first post'</span>,
<span class="linenos">24</span>	            <span class="hljs-string">'text'</span>: <span class="hljs-string">'This is my first post'</span>,
<span class="linenos">25</span>	            <span class="hljs-string">'pub_date_0'</span>: <span class="hljs-string">'2013-12-28'</span>,
<span class="linenos">26</span>	            <span class="hljs-string">'pub_date_1'</span>: <span class="hljs-string">'22:00:04'</span>,
<span class="linenos">27</span>	            <span class="hljs-string">'slug'</span>: <span class="hljs-string">'my-first-post'</span>,
<span class="linenos">28</span>	            <span class="hljs-string">'site'</span>: <span class="hljs-string">'1'</span>,
<span class="linenos">29</span>	            <span class="hljs-string">'category'</span>: <span class="hljs-string">'1'</span>,
<span class="linenos">30</span>	            <span class="hljs-string">'tags'</span>: <span class="hljs-string">'1'</span>
<span class="linenos">31</span>	        },
<span class="linenos">32</span>	        follow=<span class="hljs-keyword">True</span>
<span class="linenos">33</span>	        )
<span class="linenos">34</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">35</span>	
<span class="linenos">36</span>	        <span class="hljs-comment"># Check added successfully</span>
<span class="linenos">37</span>	        self.assertTrue(<span class="hljs-string">'added successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">38</span>	
<span class="linenos">39</span>	        <span class="hljs-comment"># Check new post now in database</span>
<span class="linenos">40</span>	        all_posts = Post.objects.all()
<span class="linenos">41</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">42</span>	
<span class="linenos">43</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_edit_post</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">44</span>	        <span class="hljs-comment"># Create the category</span>
<span class="linenos">45</span>	        category = Category()
<span class="linenos">46</span>	        category.name = <span class="hljs-string">'python'</span>
<span class="linenos">47</span>	        category.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">48</span>	        category.save()
<span class="linenos">49</span>	
<span class="linenos">50</span>	        <span class="hljs-comment"># Create the tag</span>
<span class="linenos">51</span>	        tag = Tag()
<span class="linenos">52</span>	        tag.name = <span class="hljs-string">'python'</span>
<span class="linenos">53</span>	        tag.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">54</span>	        tag.save()
<span class="linenos">55</span>	
<span class="linenos">56</span>	        <span class="hljs-comment"># Create the author</span>
<span class="linenos">57</span>	        author = User.objects.create_user(<span class="hljs-string">'testuser'</span>, <span class="hljs-string">'user@example.com'</span>, <span class="hljs-string">'password'</span>)
<span class="linenos">58</span>	        author.save()
<span class="linenos">59</span>	
<span class="linenos">60</span>	        <span class="hljs-comment"># Create the site</span>
<span class="linenos">61</span>	        site = Site()
<span class="linenos">62</span>	        site.name = <span class="hljs-string">'example.com'</span>
<span class="linenos">63</span>	        site.domain = <span class="hljs-string">'example.com'</span>
<span class="linenos">64</span>	        site.save()
<span class="linenos">65</span>	
<span class="linenos">66</span>	        <span class="hljs-comment"># Create the post</span>
<span class="linenos">67</span>	        post = Post()
<span class="linenos">68</span>	        post.title = <span class="hljs-string">'My first post'</span>
<span class="linenos">69</span>	        post.text = <span class="hljs-string">'This is my first blog post'</span>
<span class="linenos">70</span>	        post.slug = <span class="hljs-string">'my-first-post'</span>
<span class="linenos">71</span>	        post.pub_date = timezone.now()
<span class="linenos">72</span>	        post.author = author
<span class="linenos">73</span>	        post.site = site
<span class="linenos">74</span>	        post.save()
<span class="linenos">75</span>	        post.tags.add(tag)
<span class="linenos">76</span>	        post.save()
<span class="linenos">77</span>	
<span class="linenos">78</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">79</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">80</span>	
<span class="linenos">81</span>	        <span class="hljs-comment"># Edit the post</span>
<span class="linenos">82</span>	        response = self.client.post(<span class="hljs-string">'/admin/blogengine/post/1/'</span>, {
<span class="linenos">83</span>	            <span class="hljs-string">'title'</span>: <span class="hljs-string">'My second post'</span>,
<span class="linenos">84</span>	            <span class="hljs-string">'text'</span>: <span class="hljs-string">'This is my second blog post'</span>,
<span class="linenos">85</span>	            <span class="hljs-string">'pub_date_0'</span>: <span class="hljs-string">'2013-12-28'</span>,
<span class="linenos">86</span>	            <span class="hljs-string">'pub_date_1'</span>: <span class="hljs-string">'22:00:04'</span>,
<span class="linenos">87</span>	            <span class="hljs-string">'slug'</span>: <span class="hljs-string">'my-second-post'</span>,
<span class="linenos">88</span>	            <span class="hljs-string">'site'</span>: <span class="hljs-string">'1'</span>,
<span class="linenos">89</span>	            <span class="hljs-string">'category'</span>: <span class="hljs-string">'1'</span>,
<span class="linenos">90</span>	            <span class="hljs-string">'tags'</span>: <span class="hljs-string">'1'</span>
<span class="linenos">91</span>	        },
<span class="linenos">92</span>	        follow=<span class="hljs-keyword">True</span>
<span class="linenos">93</span>	        )
<span class="linenos">94</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">95</span>	
<span class="linenos">96</span>	        <span class="hljs-comment"># Check changed successfully</span>
<span class="linenos">97</span>	        self.assertTrue(<span class="hljs-string">'changed successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">98</span>	
<span class="linenos">99</span>	        <span class="hljs-comment"># Check post amended</span>
<span class="linenos">100</span>	        all_posts = Post.objects.all()
<span class="linenos">101</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">102</span>	        only_post = all_posts[<span class="hljs-number">0</span>]
<span class="linenos">103</span>	        self.assertEquals(only_post.title, <span class="hljs-string">'My second post'</span>)
<span class="linenos">104</span>	        self.assertEquals(only_post.text, <span class="hljs-string">'This is my second blog post'</span>)
<span class="linenos">105</span>	
<span class="linenos">106</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_delete_post</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">107</span>	        <span class="hljs-comment"># Create the category</span>
<span class="linenos">108</span>	        category = Category()
<span class="linenos">109</span>	        category.name = <span class="hljs-string">'python'</span>
<span class="linenos">110</span>	        category.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">111</span>	        category.save()
<span class="linenos">112</span>	
<span class="linenos">113</span>	        <span class="hljs-comment"># Create the tag</span>
<span class="linenos">114</span>	        tag = Tag()
<span class="linenos">115</span>	        tag.name = <span class="hljs-string">'python'</span>
<span class="linenos">116</span>	        tag.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">117</span>	        tag.save()
<span class="linenos">118</span>	
<span class="linenos">119</span>	        <span class="hljs-comment"># Create the author</span>
<span class="linenos">120</span>	        author = User.objects.create_user(<span class="hljs-string">'testuser'</span>, <span class="hljs-string">'user@example.com'</span>, <span class="hljs-string">'password'</span>)
<span class="linenos">121</span>	        author.save()
<span class="linenos">122</span>	
<span class="linenos">123</span>	        <span class="hljs-comment"># Create the site</span>
<span class="linenos">124</span>	        site = Site()
<span class="linenos">125</span>	        site.name = <span class="hljs-string">'example.com'</span>
<span class="linenos">126</span>	        site.domain = <span class="hljs-string">'example.com'</span>
<span class="linenos">127</span>	        site.save()
<span class="linenos">128</span>	
<span class="linenos">129</span>	        <span class="hljs-comment"># Create the post</span>
<span class="linenos">130</span>	        post = Post()
<span class="linenos">131</span>	        post.title = <span class="hljs-string">'My first post'</span>
<span class="linenos">132</span>	        post.text = <span class="hljs-string">'This is my first blog post'</span>
<span class="linenos">133</span>	        post.slug = <span class="hljs-string">'my-first-post'</span>
<span class="linenos">134</span>	        post.pub_date = timezone.now()
<span class="linenos">135</span>	        post.site = site
<span class="linenos">136</span>	        post.author = author
<span class="linenos">137</span>	        post.category = category
<span class="linenos">138</span>	        post.save()
<span class="linenos">139</span>	        post.tags.add(tag)
<span class="linenos">140</span>	        post.save()
<span class="linenos">141</span>	
<span class="linenos">142</span>	        <span class="hljs-comment"># Check new post saved</span>
<span class="linenos">143</span>	        all_posts = Post.objects.all()
<span class="linenos">144</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">145</span>	
<span class="linenos">146</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">147</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">148</span>	
<span class="linenos">149</span>	        <span class="hljs-comment"># Delete the post</span>
<span class="linenos">150</span>	        response = self.client.post(<span class="hljs-string">'/admin/blogengine/post/1/delete/'</span>, {
<span class="linenos">151</span>	            <span class="hljs-string">'post'</span>: <span class="hljs-string">'yes'</span>
<span class="linenos">152</span>	        }, follow=<span class="hljs-keyword">True</span>)
<span class="linenos">153</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">154</span>	
<span class="linenos">155</span>	        <span class="hljs-comment"># Check deleted successfully</span>
<span class="linenos">156</span>	        self.assertTrue(<span class="hljs-string">'deleted successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">157</span>	
<span class="linenos">158</span>	        <span class="hljs-comment"># Check post deleted</span>
<span class="linenos">159</span>	        all_posts = Post.objects.all()
<span class="linenos">160</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">0</span>)</code></pre>
<p>Here we’re just adding tags to our <code>Post</code> objects.</p>
<p>Now it’s time to run our tests to make sure they fail as expected:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	(venv)Smith:django_tutorial_blog_ng matthewdaly$ python manage.py <span class="hljs-built_in">test</span> blogengine
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	E
<span class="linenos">4</span>	======================================================================
<span class="linenos">5</span>	ERROR: blogengine.tests (unittest.loader.ModuleImportFailure)
<span class="linenos">6</span>	----------------------------------------------------------------------
<span class="linenos">7</span>	ImportError: Failed to import <span class="hljs-built_in">test</span> module: blogengine.tests
<span class="linenos">8</span>	Traceback (most recent call last):
<span class="linenos">9</span>	  File <span class="hljs-string">"/usr/local/Cellar/python/2.7.6/Frameworks/Python.framework/Versions/2.7/lib/python2.7/unittest/loader.py"</span>, line <span class="hljs-number">254</span>, <span class="hljs-keyword">in</span> _find_tests
<span class="linenos">10</span>	    module = self._get_module_from_name(name)
<span class="linenos">11</span>	  File <span class="hljs-string">"/usr/local/Cellar/python/2.7.6/Frameworks/Python.framework/Versions/2.7/lib/python2.7/unittest/loader.py"</span>, line <span class="hljs-number">232</span>, <span class="hljs-keyword">in</span> _get_module_from_name
<span class="linenos">12</span>	    __import__(name)
<span class="linenos">13</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">3</span>, <span class="hljs-keyword">in</span> &lt;module&gt;
<span class="linenos">14</span>	    from blogengine.models import Post, Category, Tag
<span class="linenos">15</span>	ImportError: cannot import name Tag
<span class="linenos">16</span>	
<span class="linenos">17</span>	
<span class="linenos">18</span>	----------------------------------------------------------------------
<span class="linenos">19</span>	Ran <span class="hljs-number">1</span> <span class="hljs-built_in">test</span> <span class="hljs-keyword">in</span> <span class="hljs-number">0.000</span>s
<span class="linenos">20</span>	
<span class="linenos">21</span>	FAILED (errors=<span class="hljs-number">1</span>)
<span class="linenos">22</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>So here we can’t import our <code>Tag</code> model, because we haven’t created it. So, we’ll do that:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Tag</span><span class="hljs-params">(models.Model)</span>:</span>
<span class="linenos">2</span>	    name = models.CharField(max_length=<span class="hljs-number">200</span>)
<span class="linenos">3</span>	    description = models.TextField()
<span class="linenos">4</span>	    slug = models.SlugField(max_length=<span class="hljs-number">40</span>, unique=<span class="hljs-keyword">True</span>, blank=<span class="hljs-keyword">True</span>, null=<span class="hljs-keyword">True</span>)
<span class="linenos">5</span>	
<span class="linenos">6</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">save</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">7</span>	        <span class="hljs-keyword">if</span> <span class="hljs-keyword">not</span> self.slug:
<span class="linenos">8</span>	            self.slug = slugify(unicode(self.name))
<span class="linenos">9</span>	        super(Tag, self).save()
<span class="linenos">10</span>	
<span class="linenos">11</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">get_absolute_url</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">12</span>	        <span class="hljs-keyword">return</span> <span class="hljs-string">"/tag/%s/"</span> % (self.slug)
<span class="linenos">13</span>	
<span class="linenos">14</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">__unicode__</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">15</span>	        <span class="hljs-keyword">return</span> self.name</code></pre>
<p>Our <code>Tag</code> model is very much like our <code>Category</code> model, but we don’t need to change the <code>verbose_name_plural</code> value, and we amend the absolute URL to show it as a tag rather than a category.</p>
<p>We also need to amend our <code>Post</code> model to include a <code>tags</code> field:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Post</span><span class="hljs-params">(models.Model)</span>:</span>
<span class="linenos">2</span>	    title = models.CharField(max_length=<span class="hljs-number">200</span>)
<span class="linenos">3</span>	    pub_date = models.DateTimeField()
<span class="linenos">4</span>	    text = models.TextField()
<span class="linenos">5</span>	    slug = models.SlugField(max_length=<span class="hljs-number">40</span>, unique=<span class="hljs-keyword">True</span>)
<span class="linenos">6</span>	    author = models.ForeignKey(User)
<span class="linenos">7</span>	    site = models.ForeignKey(Site)
<span class="linenos">8</span>	    category = models.ForeignKey(Category, blank=<span class="hljs-keyword">True</span>, null=<span class="hljs-keyword">True</span>)
<span class="linenos">9</span>	    tags = models.ManyToManyField(Tag)
<span class="linenos">10</span>	
<span class="linenos">11</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">get_absolute_url</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">12</span>	        <span class="hljs-keyword">return</span> <span class="hljs-string">"/%s/%s/%s/"</span> % (self.pub_date.year, self.pub_date.month, self.slug)
<span class="linenos">13</span>	
<span class="linenos">14</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">__unicode__</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">15</span>	        <span class="hljs-keyword">return</span> self.title
<span class="linenos">16</span>	
<span class="linenos">17</span>	    <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Meta</span>:</span>
<span class="linenos">18</span>	        ordering = [<span class="hljs-string">"-pub_date"</span>]</code></pre>
<p>Note that <code>tags</code> is a <code>ManyToManyField</code>, and we pass through the model we wish to use, much like we did with the categories. The difference is that one tag can be applied to many posts and a post can have many tags, so we need an intermediate database table to handle the relationship between the two. With Django’s ORM we can handle this quickly and easily.</p>
<p>Run our tests and they should still fail:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	(venv)Smith:django_tutorial_blog_ng matthewdaly$ python manage.py <span class="hljs-built_in">test</span> blogengine
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	.EE.EF.EE.EE......
<span class="linenos">4</span>	======================================================================
<span class="linenos">5</span>	ERROR: <span class="hljs-built_in">test</span>_create_post (blogengine.tests.PostTest)
<span class="linenos">6</span>	----------------------------------------------------------------------
<span class="linenos">7</span>	Traceback (most recent call last):
<span class="linenos">8</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">64</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_create_post
<span class="linenos">9</span>	    tag.save()
<span class="linenos">10</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/models.py"</span>, line <span class="hljs-number">34</span>, <span class="hljs-keyword">in</span> save
<span class="linenos">11</span>	    super(Tag, self).save()
<span class="linenos">12</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">545</span>, <span class="hljs-keyword">in</span> save
<span class="linenos">13</span>	    force_update=force_update, update_fields=update_fields)
<span class="linenos">14</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">573</span>, <span class="hljs-keyword">in</span> save_base
<span class="linenos">15</span>	    updated = self._save_table(raw, cls, force_insert, force_update, using, update_fields)
<span class="linenos">16</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">654</span>, <span class="hljs-keyword">in</span> _save_table
<span class="linenos">17</span>	    result = self._<span class="hljs-keyword">do</span>_insert(cls._base_manager, using, fields, update_pk, raw)
<span class="linenos">18</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">687</span>, <span class="hljs-keyword">in</span> _<span class="hljs-keyword">do</span>_insert
<span class="linenos">19</span>	    using=using, raw=raw)
<span class="linenos">20</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/manager.py"</span>, line <span class="hljs-number">232</span>, <span class="hljs-keyword">in</span> _insert
<span class="linenos">21</span>	    <span class="hljs-built_in">return</span> insert_query(self.model, objs, fields, **kwargs)
<span class="linenos">22</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/query.py"</span>, line <span class="hljs-number">1511</span>, <span class="hljs-keyword">in</span> insert_query
<span class="linenos">23</span>	    <span class="hljs-built_in">return</span> query.get_compiler(using=using).execute_sql(<span class="hljs-built_in">return</span>_id)
<span class="linenos">24</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/sql/compiler.py"</span>, line <span class="hljs-number">898</span>, <span class="hljs-keyword">in</span> execute_sql
<span class="linenos">25</span>	    cursor.execute(sql, params)
<span class="linenos">26</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">27</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">28</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/utils.py"</span>, line <span class="hljs-number">99</span>, <span class="hljs-keyword">in</span> __<span class="hljs-built_in">exit</span>__
<span class="linenos">29</span>	    six.reraise(dj_exc_<span class="hljs-built_in">type</span>, dj_exc_value, traceback)
<span class="linenos">30</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">31</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">32</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/sqlite3/base.py"</span>, line <span class="hljs-number">450</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">33</span>	    <span class="hljs-built_in">return</span> Database.Cursor.execute(self, query, params)
<span class="linenos">34</span>	OperationalError: no such table: blogengine_tag
<span class="linenos">35</span>	
<span class="linenos">36</span>	======================================================================
<span class="linenos">37</span>	ERROR: <span class="hljs-built_in">test</span>_create_tag (blogengine.tests.PostTest)
<span class="linenos">38</span>	----------------------------------------------------------------------
<span class="linenos">39</span>	Traceback (most recent call last):
<span class="linenos">40</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">41</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_create_tag
<span class="linenos">41</span>	    tag.save()
<span class="linenos">42</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/models.py"</span>, line <span class="hljs-number">34</span>, <span class="hljs-keyword">in</span> save
<span class="linenos">43</span>	    super(Tag, self).save()
<span class="linenos">44</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">545</span>, <span class="hljs-keyword">in</span> save
<span class="linenos">45</span>	    force_update=force_update, update_fields=update_fields)
<span class="linenos">46</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">573</span>, <span class="hljs-keyword">in</span> save_base
<span class="linenos">47</span>	    updated = self._save_table(raw, cls, force_insert, force_update, using, update_fields)
<span class="linenos">48</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">654</span>, <span class="hljs-keyword">in</span> _save_table
<span class="linenos">49</span>	    result = self._<span class="hljs-keyword">do</span>_insert(cls._base_manager, using, fields, update_pk, raw)
<span class="linenos">50</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">687</span>, <span class="hljs-keyword">in</span> _<span class="hljs-keyword">do</span>_insert
<span class="linenos">51</span>	    using=using, raw=raw)
<span class="linenos">52</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/manager.py"</span>, line <span class="hljs-number">232</span>, <span class="hljs-keyword">in</span> _insert
<span class="linenos">53</span>	    <span class="hljs-built_in">return</span> insert_query(self.model, objs, fields, **kwargs)
<span class="linenos">54</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/query.py"</span>, line <span class="hljs-number">1511</span>, <span class="hljs-keyword">in</span> insert_query
<span class="linenos">55</span>	    <span class="hljs-built_in">return</span> query.get_compiler(using=using).execute_sql(<span class="hljs-built_in">return</span>_id)
<span class="linenos">56</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/sql/compiler.py"</span>, line <span class="hljs-number">898</span>, <span class="hljs-keyword">in</span> execute_sql
<span class="linenos">57</span>	    cursor.execute(sql, params)
<span class="linenos">58</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">59</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">60</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/utils.py"</span>, line <span class="hljs-number">99</span>, <span class="hljs-keyword">in</span> __<span class="hljs-built_in">exit</span>__
<span class="linenos">61</span>	    six.reraise(dj_exc_<span class="hljs-built_in">type</span>, dj_exc_value, traceback)
<span class="linenos">62</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">63</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">64</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/sqlite3/base.py"</span>, line <span class="hljs-number">450</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">65</span>	    <span class="hljs-built_in">return</span> Database.Cursor.execute(self, query, params)
<span class="linenos">66</span>	OperationalError: no such table: blogengine_tag
<span class="linenos">67</span>	
<span class="linenos">68</span>	======================================================================
<span class="linenos">69</span>	ERROR: <span class="hljs-built_in">test</span>_create_post (blogengine.tests.AdminTest)
<span class="linenos">70</span>	----------------------------------------------------------------------
<span class="linenos">71</span>	Traceback (most recent call last):
<span class="linenos">72</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">335</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_create_post
<span class="linenos">73</span>	    tag.save()
<span class="linenos">74</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/models.py"</span>, line <span class="hljs-number">34</span>, <span class="hljs-keyword">in</span> save
<span class="linenos">75</span>	    super(Tag, self).save()
<span class="linenos">76</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">545</span>, <span class="hljs-keyword">in</span> save
<span class="linenos">77</span>	    force_update=force_update, update_fields=update_fields)
<span class="linenos">78</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">573</span>, <span class="hljs-keyword">in</span> save_base
<span class="linenos">79</span>	    updated = self._save_table(raw, cls, force_insert, force_update, using, update_fields)
<span class="linenos">80</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">654</span>, <span class="hljs-keyword">in</span> _save_table
<span class="linenos">81</span>	    result = self._<span class="hljs-keyword">do</span>_insert(cls._base_manager, using, fields, update_pk, raw)
<span class="linenos">82</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">687</span>, <span class="hljs-keyword">in</span> _<span class="hljs-keyword">do</span>_insert
<span class="linenos">83</span>	    using=using, raw=raw)
<span class="linenos">84</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/manager.py"</span>, line <span class="hljs-number">232</span>, <span class="hljs-keyword">in</span> _insert
<span class="linenos">85</span>	    <span class="hljs-built_in">return</span> insert_query(self.model, objs, fields, **kwargs)
<span class="linenos">86</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/query.py"</span>, line <span class="hljs-number">1511</span>, <span class="hljs-keyword">in</span> insert_query
<span class="linenos">87</span>	    <span class="hljs-built_in">return</span> query.get_compiler(using=using).execute_sql(<span class="hljs-built_in">return</span>_id)
<span class="linenos">88</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/sql/compiler.py"</span>, line <span class="hljs-number">898</span>, <span class="hljs-keyword">in</span> execute_sql
<span class="linenos">89</span>	    cursor.execute(sql, params)
<span class="linenos">90</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">91</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">92</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/utils.py"</span>, line <span class="hljs-number">99</span>, <span class="hljs-keyword">in</span> __<span class="hljs-built_in">exit</span>__
<span class="linenos">93</span>	    six.reraise(dj_exc_<span class="hljs-built_in">type</span>, dj_exc_value, traceback)
<span class="linenos">94</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">95</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">96</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/sqlite3/base.py"</span>, line <span class="hljs-number">450</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">97</span>	    <span class="hljs-built_in">return</span> Database.Cursor.execute(self, query, params)
<span class="linenos">98</span>	OperationalError: no such table: blogengine_tag
<span class="linenos">99</span>	
<span class="linenos">100</span>	======================================================================
<span class="linenos">101</span>	ERROR: <span class="hljs-built_in">test</span>_delete_post (blogengine.tests.AdminTest)
<span class="linenos">102</span>	----------------------------------------------------------------------
<span class="linenos">103</span>	Traceback (most recent call last):
<span class="linenos">104</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">440</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_delete_post
<span class="linenos">105</span>	    tag.save()
<span class="linenos">106</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/models.py"</span>, line <span class="hljs-number">34</span>, <span class="hljs-keyword">in</span> save
<span class="linenos">107</span>	    super(Tag, self).save()
<span class="linenos">108</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">545</span>, <span class="hljs-keyword">in</span> save
<span class="linenos">109</span>	    force_update=force_update, update_fields=update_fields)
<span class="linenos">110</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">573</span>, <span class="hljs-keyword">in</span> save_base
<span class="linenos">111</span>	    updated = self._save_table(raw, cls, force_insert, force_update, using, update_fields)
<span class="linenos">112</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">654</span>, <span class="hljs-keyword">in</span> _save_table
<span class="linenos">113</span>	    result = self._<span class="hljs-keyword">do</span>_insert(cls._base_manager, using, fields, update_pk, raw)
<span class="linenos">114</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">687</span>, <span class="hljs-keyword">in</span> _<span class="hljs-keyword">do</span>_insert
<span class="linenos">115</span>	    using=using, raw=raw)
<span class="linenos">116</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/manager.py"</span>, line <span class="hljs-number">232</span>, <span class="hljs-keyword">in</span> _insert
<span class="linenos">117</span>	    <span class="hljs-built_in">return</span> insert_query(self.model, objs, fields, **kwargs)
<span class="linenos">118</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/query.py"</span>, line <span class="hljs-number">1511</span>, <span class="hljs-keyword">in</span> insert_query
<span class="linenos">119</span>	    <span class="hljs-built_in">return</span> query.get_compiler(using=using).execute_sql(<span class="hljs-built_in">return</span>_id)
<span class="linenos">120</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/sql/compiler.py"</span>, line <span class="hljs-number">898</span>, <span class="hljs-keyword">in</span> execute_sql
<span class="linenos">121</span>	    cursor.execute(sql, params)
<span class="linenos">122</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">123</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">124</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/utils.py"</span>, line <span class="hljs-number">99</span>, <span class="hljs-keyword">in</span> __<span class="hljs-built_in">exit</span>__
<span class="linenos">125</span>	    six.reraise(dj_exc_<span class="hljs-built_in">type</span>, dj_exc_value, traceback)
<span class="linenos">126</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">127</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">128</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/sqlite3/base.py"</span>, line <span class="hljs-number">450</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">129</span>	    <span class="hljs-built_in">return</span> Database.Cursor.execute(self, query, params)
<span class="linenos">130</span>	OperationalError: no such table: blogengine_tag
<span class="linenos">131</span>	
<span class="linenos">132</span>	======================================================================
<span class="linenos">133</span>	ERROR: <span class="hljs-built_in">test</span>_delete_tag (blogengine.tests.AdminTest)
<span class="linenos">134</span>	----------------------------------------------------------------------
<span class="linenos">135</span>	Traceback (most recent call last):
<span class="linenos">136</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">306</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_delete_tag
<span class="linenos">137</span>	    tag.save()
<span class="linenos">138</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/models.py"</span>, line <span class="hljs-number">34</span>, <span class="hljs-keyword">in</span> save
<span class="linenos">139</span>	    super(Tag, self).save()
<span class="linenos">140</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">545</span>, <span class="hljs-keyword">in</span> save
<span class="linenos">141</span>	    force_update=force_update, update_fields=update_fields)
<span class="linenos">142</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">573</span>, <span class="hljs-keyword">in</span> save_base
<span class="linenos">143</span>	    updated = self._save_table(raw, cls, force_insert, force_update, using, update_fields)
<span class="linenos">144</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">654</span>, <span class="hljs-keyword">in</span> _save_table
<span class="linenos">145</span>	    result = self._<span class="hljs-keyword">do</span>_insert(cls._base_manager, using, fields, update_pk, raw)
<span class="linenos">146</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">687</span>, <span class="hljs-keyword">in</span> _<span class="hljs-keyword">do</span>_insert
<span class="linenos">147</span>	    using=using, raw=raw)
<span class="linenos">148</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/manager.py"</span>, line <span class="hljs-number">232</span>, <span class="hljs-keyword">in</span> _insert
<span class="linenos">149</span>	    <span class="hljs-built_in">return</span> insert_query(self.model, objs, fields, **kwargs)
<span class="linenos">150</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/query.py"</span>, line <span class="hljs-number">1511</span>, <span class="hljs-keyword">in</span> insert_query
<span class="linenos">151</span>	    <span class="hljs-built_in">return</span> query.get_compiler(using=using).execute_sql(<span class="hljs-built_in">return</span>_id)
<span class="linenos">152</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/sql/compiler.py"</span>, line <span class="hljs-number">898</span>, <span class="hljs-keyword">in</span> execute_sql
<span class="linenos">153</span>	    cursor.execute(sql, params)
<span class="linenos">154</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">155</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">156</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/utils.py"</span>, line <span class="hljs-number">99</span>, <span class="hljs-keyword">in</span> __<span class="hljs-built_in">exit</span>__
<span class="linenos">157</span>	    six.reraise(dj_exc_<span class="hljs-built_in">type</span>, dj_exc_value, traceback)
<span class="linenos">158</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">159</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">160</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/sqlite3/base.py"</span>, line <span class="hljs-number">450</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">161</span>	    <span class="hljs-built_in">return</span> Database.Cursor.execute(self, query, params)
<span class="linenos">162</span>	OperationalError: no such table: blogengine_tag
<span class="linenos">163</span>	
<span class="linenos">164</span>	======================================================================
<span class="linenos">165</span>	ERROR: <span class="hljs-built_in">test</span>_edit_post (blogengine.tests.AdminTest)
<span class="linenos">166</span>	----------------------------------------------------------------------
<span class="linenos">167</span>	Traceback (most recent call last):
<span class="linenos">168</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">377</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_edit_post
<span class="linenos">169</span>	    tag.save()
<span class="linenos">170</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/models.py"</span>, line <span class="hljs-number">34</span>, <span class="hljs-keyword">in</span> save
<span class="linenos">171</span>	    super(Tag, self).save()
<span class="linenos">172</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">545</span>, <span class="hljs-keyword">in</span> save
<span class="linenos">173</span>	    force_update=force_update, update_fields=update_fields)
<span class="linenos">174</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">573</span>, <span class="hljs-keyword">in</span> save_base
<span class="linenos">175</span>	    updated = self._save_table(raw, cls, force_insert, force_update, using, update_fields)
<span class="linenos">176</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">654</span>, <span class="hljs-keyword">in</span> _save_table
<span class="linenos">177</span>	    result = self._<span class="hljs-keyword">do</span>_insert(cls._base_manager, using, fields, update_pk, raw)
<span class="linenos">178</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">687</span>, <span class="hljs-keyword">in</span> _<span class="hljs-keyword">do</span>_insert
<span class="linenos">179</span>	    using=using, raw=raw)
<span class="linenos">180</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/manager.py"</span>, line <span class="hljs-number">232</span>, <span class="hljs-keyword">in</span> _insert
<span class="linenos">181</span>	    <span class="hljs-built_in">return</span> insert_query(self.model, objs, fields, **kwargs)
<span class="linenos">182</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/query.py"</span>, line <span class="hljs-number">1511</span>, <span class="hljs-keyword">in</span> insert_query
<span class="linenos">183</span>	    <span class="hljs-built_in">return</span> query.get_compiler(using=using).execute_sql(<span class="hljs-built_in">return</span>_id)
<span class="linenos">184</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/sql/compiler.py"</span>, line <span class="hljs-number">898</span>, <span class="hljs-keyword">in</span> execute_sql
<span class="linenos">185</span>	    cursor.execute(sql, params)
<span class="linenos">186</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">187</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">188</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/utils.py"</span>, line <span class="hljs-number">99</span>, <span class="hljs-keyword">in</span> __<span class="hljs-built_in">exit</span>__
<span class="linenos">189</span>	    six.reraise(dj_exc_<span class="hljs-built_in">type</span>, dj_exc_value, traceback)
<span class="linenos">190</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">191</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">192</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/sqlite3/base.py"</span>, line <span class="hljs-number">450</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">193</span>	    <span class="hljs-built_in">return</span> Database.Cursor.execute(self, query, params)
<span class="linenos">194</span>	OperationalError: no such table: blogengine_tag
<span class="linenos">195</span>	
<span class="linenos">196</span>	======================================================================
<span class="linenos">197</span>	ERROR: <span class="hljs-built_in">test</span>_edit_tag (blogengine.tests.AdminTest)
<span class="linenos">198</span>	----------------------------------------------------------------------
<span class="linenos">199</span>	Traceback (most recent call last):
<span class="linenos">200</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">279</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_edit_tag
<span class="linenos">201</span>	    tag.save()
<span class="linenos">202</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/models.py"</span>, line <span class="hljs-number">34</span>, <span class="hljs-keyword">in</span> save
<span class="linenos">203</span>	    super(Tag, self).save()
<span class="linenos">204</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">545</span>, <span class="hljs-keyword">in</span> save
<span class="linenos">205</span>	    force_update=force_update, update_fields=update_fields)
<span class="linenos">206</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">573</span>, <span class="hljs-keyword">in</span> save_base
<span class="linenos">207</span>	    updated = self._save_table(raw, cls, force_insert, force_update, using, update_fields)
<span class="linenos">208</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">654</span>, <span class="hljs-keyword">in</span> _save_table
<span class="linenos">209</span>	    result = self._<span class="hljs-keyword">do</span>_insert(cls._base_manager, using, fields, update_pk, raw)
<span class="linenos">210</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/base.py"</span>, line <span class="hljs-number">687</span>, <span class="hljs-keyword">in</span> _<span class="hljs-keyword">do</span>_insert
<span class="linenos">211</span>	    using=using, raw=raw)
<span class="linenos">212</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/manager.py"</span>, line <span class="hljs-number">232</span>, <span class="hljs-keyword">in</span> _insert
<span class="linenos">213</span>	    <span class="hljs-built_in">return</span> insert_query(self.model, objs, fields, **kwargs)
<span class="linenos">214</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/query.py"</span>, line <span class="hljs-number">1511</span>, <span class="hljs-keyword">in</span> insert_query
<span class="linenos">215</span>	    <span class="hljs-built_in">return</span> query.get_compiler(using=using).execute_sql(<span class="hljs-built_in">return</span>_id)
<span class="linenos">216</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/models/sql/compiler.py"</span>, line <span class="hljs-number">898</span>, <span class="hljs-keyword">in</span> execute_sql
<span class="linenos">217</span>	    cursor.execute(sql, params)
<span class="linenos">218</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">219</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">220</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/utils.py"</span>, line <span class="hljs-number">99</span>, <span class="hljs-keyword">in</span> __<span class="hljs-built_in">exit</span>__
<span class="linenos">221</span>	    six.reraise(dj_exc_<span class="hljs-built_in">type</span>, dj_exc_value, traceback)
<span class="linenos">222</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/util.py"</span>, line <span class="hljs-number">53</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">223</span>	    <span class="hljs-built_in">return</span> self.cursor.execute(sql, params)
<span class="linenos">224</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/db/backends/sqlite3/base.py"</span>, line <span class="hljs-number">450</span>, <span class="hljs-keyword">in</span> execute
<span class="linenos">225</span>	    <span class="hljs-built_in">return</span> Database.Cursor.execute(self, query, params)
<span class="linenos">226</span>	OperationalError: no such table: blogengine_tag
<span class="linenos">227</span>	
<span class="linenos">228</span>	======================================================================
<span class="linenos">229</span>	FAIL: <span class="hljs-built_in">test</span>_create_tag (blogengine.tests.AdminTest)
<span class="linenos">230</span>	----------------------------------------------------------------------
<span class="linenos">231</span>	Traceback (most recent call last):
<span class="linenos">232</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">256</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_create_tag
<span class="linenos">233</span>	    self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">234</span>	AssertionError: <span class="hljs-number">404</span> != <span class="hljs-number">200</span>
<span class="linenos">235</span>	
<span class="linenos">236</span>	----------------------------------------------------------------------
<span class="linenos">237</span>	Ran <span class="hljs-number">18</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">3.981</span>s
<span class="linenos">238</span>	
<span class="linenos">239</span>	FAILED (failures=<span class="hljs-number">1</span>, errors=<span class="hljs-number">7</span>)
<span class="linenos">240</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>Again, we can easily see why they failed - the <code>blogengine_tag</code> table is not in place. So let’s create and run our migrations to fix that:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ python manage.py schemamigration --auto blogengine
<span class="linenos">2</span>	$ python manage.py migrate</code></pre>
<p>Now, we run our tests again:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	(venv)Smith:django_tutorial_blog_ng matthewdaly$ python manage.py <span class="hljs-built_in">test</span> blogengine
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	.....F..F..F......
<span class="linenos">4</span>	======================================================================
<span class="linenos">5</span>	FAIL: <span class="hljs-built_in">test</span>_create_tag (blogengine.tests.AdminTest)
<span class="linenos">6</span>	----------------------------------------------------------------------
<span class="linenos">7</span>	Traceback (most recent call last):
<span class="linenos">8</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">256</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_create_tag
<span class="linenos">9</span>	    self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">10</span>	AssertionError: <span class="hljs-number">404</span> != <span class="hljs-number">200</span>
<span class="linenos">11</span>	
<span class="linenos">12</span>	======================================================================
<span class="linenos">13</span>	FAIL: <span class="hljs-built_in">test</span>_delete_tag (blogengine.tests.AdminTest)
<span class="linenos">14</span>	----------------------------------------------------------------------
<span class="linenos">15</span>	Traceback (most recent call last):
<span class="linenos">16</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">315</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_delete_tag
<span class="linenos">17</span>	    self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">18</span>	AssertionError: <span class="hljs-number">404</span> != <span class="hljs-number">200</span>
<span class="linenos">19</span>	
<span class="linenos">20</span>	======================================================================
<span class="linenos">21</span>	FAIL: <span class="hljs-built_in">test</span>_edit_tag (blogengine.tests.AdminTest)
<span class="linenos">22</span>	----------------------------------------------------------------------
<span class="linenos">23</span>	Traceback (most recent call last):
<span class="linenos">24</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">289</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_edit_tag
<span class="linenos">25</span>	    self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">26</span>	AssertionError: <span class="hljs-number">404</span> != <span class="hljs-number">200</span>
<span class="linenos">27</span>	
<span class="linenos">28</span>	----------------------------------------------------------------------
<span class="linenos">29</span>	Ran <span class="hljs-number">18</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">5.124</span>s
<span class="linenos">30</span>	
<span class="linenos">31</span>	FAILED (failures=<span class="hljs-number">3</span>)
<span class="linenos">32</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>We can’t yet amend our tags in the admin, because we haven’t registered them. So we do that next:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-keyword">import</span> models
<span class="linenos">2</span>	<span class="hljs-keyword">from</span> django.contrib <span class="hljs-keyword">import</span> admin
<span class="linenos">3</span>	<span class="hljs-keyword">from</span> django.contrib.auth.models <span class="hljs-keyword">import</span> User
<span class="linenos">4</span>	
<span class="linenos">5</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">PostAdmin</span><span class="hljs-params">(admin.ModelAdmin)</span>:</span>
<span class="linenos">6</span>	    prepopulated_fields = {<span class="hljs-string">"slug"</span>: (<span class="hljs-string">"title"</span>,)}
<span class="linenos">7</span>	    exclude = (<span class="hljs-string">'author'</span>,)
<span class="linenos">8</span>	
<span class="linenos">9</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">save_model</span><span class="hljs-params">(self, request, obj, form, change)</span>:</span>
<span class="linenos">10</span>	        obj.author = request.user
<span class="linenos">11</span>	        obj.save()
<span class="linenos">12</span>	
<span class="linenos">13</span>	admin.site.register(models.Category)
<span class="linenos">14</span>	admin.site.register(models.Tag)
<span class="linenos">15</span>	admin.site.register(models.Post, PostAdmin)</code></pre>
<p>Now, if we run our tests, they should pass:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	(venv)Smith:django_tutorial_blog_ng matthewdaly$ python manage.py <span class="hljs-built_in">test</span> blogengine
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	..................
<span class="linenos">4</span>	----------------------------------------------------------------------
<span class="linenos">5</span>	Ran <span class="hljs-number">18</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">5.444</span>s
<span class="linenos">6</span>	
<span class="linenos">7</span>	OK
<span class="linenos">8</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>Time to commit our changes:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add blogengine/
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Implemented tags'</span></code></pre>
<p>Now, like with the categories beforehand, we want to be able to show the tags applied to a post at the base of it, and list all posts for a specific tag. So, first of all, we’ll amend our <code>PostViewTest</code> class to check for the tags:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">PostViewTest</span><span class="hljs-params">(BaseAcceptanceTest)</span>:</span>
<span class="linenos">2</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_index</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">3</span>	        <span class="hljs-comment"># Create the category</span>
<span class="linenos">4</span>	        category = Category()
<span class="linenos">5</span>	        category.name = <span class="hljs-string">'python'</span>
<span class="linenos">6</span>	        category.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">7</span>	        category.save()
<span class="linenos">8</span>	
<span class="linenos">9</span>	        <span class="hljs-comment"># Create the tag</span>
<span class="linenos">10</span>	        tag = Tag()
<span class="linenos">11</span>	        tag.name = <span class="hljs-string">'perl'</span>
<span class="linenos">12</span>	        tag.description = <span class="hljs-string">'The Perl programming language'</span>
<span class="linenos">13</span>	        tag.save()
<span class="linenos">14</span>	
<span class="linenos">15</span>	        <span class="hljs-comment"># Create the author</span>
<span class="linenos">16</span>	        author = User.objects.create_user(<span class="hljs-string">'testuser'</span>, <span class="hljs-string">'user@example.com'</span>, <span class="hljs-string">'password'</span>)
<span class="linenos">17</span>	        author.save()
<span class="linenos">18</span>	
<span class="linenos">19</span>	        <span class="hljs-comment"># Create the site</span>
<span class="linenos">20</span>	        site = Site()
<span class="linenos">21</span>	        site.name = <span class="hljs-string">'example.com'</span>
<span class="linenos">22</span>	        site.domain = <span class="hljs-string">'example.com'</span>
<span class="linenos">23</span>	        site.save()
<span class="linenos">24</span>	
<span class="linenos">25</span>	        <span class="hljs-comment"># Create the post</span>
<span class="linenos">26</span>	        post = Post()
<span class="linenos">27</span>	        post.title = <span class="hljs-string">'My first post'</span>
<span class="linenos">28</span>	        post.text = <span class="hljs-string">'This is [my first blog post](http://127.0.0.1:8000/)'</span>
<span class="linenos">29</span>	        post.slug = <span class="hljs-string">'my-first-post'</span>
<span class="linenos">30</span>	        post.pub_date = timezone.now()
<span class="linenos">31</span>	        post.author = author
<span class="linenos">32</span>	        post.site = site
<span class="linenos">33</span>	        post.category = category
<span class="linenos">34</span>	        post.save()
<span class="linenos">35</span>	        post.tags.add(tag)
<span class="linenos">36</span>	
<span class="linenos">37</span>	        <span class="hljs-comment"># Check new post saved</span>
<span class="linenos">38</span>	        all_posts = Post.objects.all()
<span class="linenos">39</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">40</span>	
<span class="linenos">41</span>	        <span class="hljs-comment"># Fetch the index</span>
<span class="linenos">42</span>	        response = self.client.get(<span class="hljs-string">'/'</span>)
<span class="linenos">43</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">44</span>	
<span class="linenos">45</span>	        <span class="hljs-comment"># Check the post title is in the response</span>
<span class="linenos">46</span>	        self.assertTrue(post.title <span class="hljs-keyword">in</span> response.content)
<span class="linenos">47</span>	
<span class="linenos">48</span>	        <span class="hljs-comment"># Check the post text is in the response</span>
<span class="linenos">49</span>	        self.assertTrue(markdown.markdown(post.text) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">50</span>	
<span class="linenos">51</span>	        <span class="hljs-comment"># Check the post category is in the response</span>
<span class="linenos">52</span>	        self.assertTrue(post.category.name <span class="hljs-keyword">in</span> response.content)
<span class="linenos">53</span>	
<span class="linenos">54</span>	        <span class="hljs-comment"># Check the post tag is in the response</span>
<span class="linenos">55</span>	        post_tag = all_posts[<span class="hljs-number">0</span>].tags.all()[<span class="hljs-number">0</span>]
<span class="linenos">56</span>	        self.assertTrue(post_tag.name <span class="hljs-keyword">in</span> response.content)
<span class="linenos">57</span>	
<span class="linenos">58</span>	        <span class="hljs-comment"># Check the post date is in the response</span>
<span class="linenos">59</span>	        self.assertTrue(str(post.pub_date.year) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">60</span>	        self.assertTrue(post.pub_date.strftime(<span class="hljs-string">'%b'</span>) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">61</span>	        self.assertTrue(str(post.pub_date.day) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">62</span>	
<span class="linenos">63</span>	        <span class="hljs-comment"># Check the link is marked up properly</span>
<span class="linenos">64</span>	        self.assertTrue(<span class="hljs-string">'&lt;a href="http://127.0.0.1:8000/"&gt;my first blog post&lt;/a&gt;'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">65</span>	
<span class="linenos">66</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_post_page</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">67</span>	        <span class="hljs-comment"># Create the category</span>
<span class="linenos">68</span>	        category = Category()
<span class="linenos">69</span>	        category.name = <span class="hljs-string">'python'</span>
<span class="linenos">70</span>	        category.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">71</span>	        category.save()
<span class="linenos">72</span>	
<span class="linenos">73</span>	        <span class="hljs-comment"># Create the tag</span>
<span class="linenos">74</span>	        tag = Tag()
<span class="linenos">75</span>	        tag.name = <span class="hljs-string">'perl'</span>
<span class="linenos">76</span>	        tag.description = <span class="hljs-string">'The Perl programming language'</span>
<span class="linenos">77</span>	        tag.save()
<span class="linenos">78</span>	
<span class="linenos">79</span>	        <span class="hljs-comment"># Create the author</span>
<span class="linenos">80</span>	        author = User.objects.create_user(<span class="hljs-string">'testuser'</span>, <span class="hljs-string">'user@example.com'</span>, <span class="hljs-string">'password'</span>)
<span class="linenos">81</span>	        author.save()
<span class="linenos">82</span>	
<span class="linenos">83</span>	        <span class="hljs-comment"># Create the site</span>
<span class="linenos">84</span>	        site = Site()
<span class="linenos">85</span>	        site.name = <span class="hljs-string">'example.com'</span>
<span class="linenos">86</span>	        site.domain = <span class="hljs-string">'example.com'</span>
<span class="linenos">87</span>	        site.save()
<span class="linenos">88</span>	
<span class="linenos">89</span>	        <span class="hljs-comment"># Create the post</span>
<span class="linenos">90</span>	        post = Post()
<span class="linenos">91</span>	        post.title = <span class="hljs-string">'My first post'</span>
<span class="linenos">92</span>	        post.text = <span class="hljs-string">'This is [my first blog post](http://127.0.0.1:8000/)'</span>
<span class="linenos">93</span>	        post.slug = <span class="hljs-string">'my-first-post'</span>
<span class="linenos">94</span>	        post.pub_date = timezone.now()
<span class="linenos">95</span>	        post.author = author
<span class="linenos">96</span>	        post.site = site
<span class="linenos">97</span>	        post.category = category
<span class="linenos">98</span>	        post.save()
<span class="linenos">99</span>	        post.tags.add(tag)
<span class="linenos">100</span>	        post.save()
<span class="linenos">101</span>	
<span class="linenos">102</span>	        <span class="hljs-comment"># Check new post saved</span>
<span class="linenos">103</span>	        all_posts = Post.objects.all()
<span class="linenos">104</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">105</span>	        only_post = all_posts[<span class="hljs-number">0</span>]
<span class="linenos">106</span>	        self.assertEquals(only_post, post)
<span class="linenos">107</span>	
<span class="linenos">108</span>	        <span class="hljs-comment"># Get the post URL</span>
<span class="linenos">109</span>	        post_url = only_post.get_absolute_url()
<span class="linenos">110</span>	
<span class="linenos">111</span>	        <span class="hljs-comment"># Fetch the post</span>
<span class="linenos">112</span>	        response = self.client.get(post_url)
<span class="linenos">113</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">114</span>	
<span class="linenos">115</span>	        <span class="hljs-comment"># Check the post title is in the response</span>
<span class="linenos">116</span>	        self.assertTrue(post.title <span class="hljs-keyword">in</span> response.content)
<span class="linenos">117</span>	
<span class="linenos">118</span>	        <span class="hljs-comment"># Check the post category is in the response</span>
<span class="linenos">119</span>	        self.assertTrue(post.category.name <span class="hljs-keyword">in</span> response.content)
<span class="linenos">120</span>	
<span class="linenos">121</span>	        <span class="hljs-comment"># Check the post tag is in the response</span>
<span class="linenos">122</span>	        post_tag = all_posts[<span class="hljs-number">0</span>].tags.all()[<span class="hljs-number">0</span>]
<span class="linenos">123</span>	        self.assertTrue(post_tag.name <span class="hljs-keyword">in</span> response.content)
<span class="linenos">124</span>	
<span class="linenos">125</span>	        <span class="hljs-comment"># Check the post text is in the response</span>
<span class="linenos">126</span>	        self.assertTrue(markdown.markdown(post.text) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">127</span>	
<span class="linenos">128</span>	        <span class="hljs-comment"># Check the post date is in the response</span>
<span class="linenos">129</span>	        self.assertTrue(str(post.pub_date.year) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">130</span>	        self.assertTrue(post.pub_date.strftime(<span class="hljs-string">'%b'</span>) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">131</span>	        self.assertTrue(str(post.pub_date.day) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">132</span>	
<span class="linenos">133</span>	        <span class="hljs-comment"># Check the link is marked up properly</span>
<span class="linenos">134</span>	        self.assertTrue(<span class="hljs-string">'&lt;a href="http://127.0.0.1:8000/"&gt;my first blog post&lt;/a&gt;'</span> <span class="hljs-keyword">in</span> response.content)</code></pre>
<p>We create a tag near the top, and check for the text in the page (note that to avoid false positives from the categories, we set the name of the tags to something different). We do this on both the index and post pages.</p>
<p>We also need to put a test in place for the tag-specific page:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_tag_page</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">2</span>	        <span class="hljs-comment"># Create the tag</span>
<span class="linenos">3</span>	        tag = Tag()
<span class="linenos">4</span>	        tag.name = <span class="hljs-string">'python'</span>
<span class="linenos">5</span>	        tag.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">6</span>	        tag.save()
<span class="linenos">7</span>	
<span class="linenos">8</span>	        <span class="hljs-comment"># Create the author</span>
<span class="linenos">9</span>	        author = User.objects.create_user(<span class="hljs-string">'testuser'</span>, <span class="hljs-string">'user@example.com'</span>, <span class="hljs-string">'password'</span>)
<span class="linenos">10</span>	        author.save()
<span class="linenos">11</span>	
<span class="linenos">12</span>	        <span class="hljs-comment"># Create the site</span>
<span class="linenos">13</span>	        site = Site()
<span class="linenos">14</span>	        site.name = <span class="hljs-string">'example.com'</span>
<span class="linenos">15</span>	        site.domain = <span class="hljs-string">'example.com'</span>
<span class="linenos">16</span>	        site.save()
<span class="linenos">17</span>	
<span class="linenos">18</span>	        <span class="hljs-comment"># Create the post</span>
<span class="linenos">19</span>	        post = Post()
<span class="linenos">20</span>	        post.title = <span class="hljs-string">'My first post'</span>
<span class="linenos">21</span>	        post.text = <span class="hljs-string">'This is [my first blog post](http://127.0.0.1:8000/)'</span>
<span class="linenos">22</span>	        post.slug = <span class="hljs-string">'my-first-post'</span>
<span class="linenos">23</span>	        post.pub_date = timezone.now()
<span class="linenos">24</span>	        post.author = author
<span class="linenos">25</span>	        post.site = site
<span class="linenos">26</span>	        post.save()
<span class="linenos">27</span>	        post.tags.add(tag)
<span class="linenos">28</span>	
<span class="linenos">29</span>	        <span class="hljs-comment"># Check new post saved</span>
<span class="linenos">30</span>	        all_posts = Post.objects.all()
<span class="linenos">31</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">32</span>	        only_post = all_posts[<span class="hljs-number">0</span>]
<span class="linenos">33</span>	        self.assertEquals(only_post, post)
<span class="linenos">34</span>	
<span class="linenos">35</span>	        <span class="hljs-comment"># Get the tag URL</span>
<span class="linenos">36</span>	        tag_url = post.tags.all()[<span class="hljs-number">0</span>].get_absolute_url()
<span class="linenos">37</span>	
<span class="linenos">38</span>	        <span class="hljs-comment"># Fetch the tag</span>
<span class="linenos">39</span>	        response = self.client.get(tag_url)
<span class="linenos">40</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">41</span>	
<span class="linenos">42</span>	        <span class="hljs-comment"># Check the tag name is in the response</span>
<span class="linenos">43</span>	        self.assertTrue(post.tags.all()[<span class="hljs-number">0</span>].name <span class="hljs-keyword">in</span> response.content)
<span class="linenos">44</span>	
<span class="linenos">45</span>	        <span class="hljs-comment"># Check the post text is in the response</span>
<span class="linenos">46</span>	        self.assertTrue(markdown.markdown(post.text) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">47</span>	
<span class="linenos">48</span>	        <span class="hljs-comment"># Check the post date is in the response</span>
<span class="linenos">49</span>	        self.assertTrue(str(post.pub_date.year) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">50</span>	        self.assertTrue(post.pub_date.strftime(<span class="hljs-string">'%b'</span>) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">51</span>	        self.assertTrue(str(post.pub_date.day) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">52</span>	
<span class="linenos">53</span>	        <span class="hljs-comment"># Check the link is marked up properly</span>
<span class="linenos">54</span>	        self.assertTrue(<span class="hljs-string">'&lt;a href="http://127.0.0.1:8000/"&gt;my first blog post&lt;/a&gt;'</span> <span class="hljs-keyword">in</span> response.content)</code></pre>
<p>Again, this is virtually identical to the category page, adjusted to allow for the fact that we need to get a specific tag. If we now run our tests, they should fail as expected:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	(venv)Smith:django_tutorial_blog_ng matthewdaly$ python manage.py <span class="hljs-built_in">test</span> blogengine
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	................FFF
<span class="linenos">4</span>	======================================================================
<span class="linenos">5</span>	FAIL: <span class="hljs-built_in">test</span>_index (blogengine.tests.PostViewTest)
<span class="linenos">6</span>	----------------------------------------------------------------------
<span class="linenos">7</span>	Traceback (most recent call last):
<span class="linenos">8</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">540</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_index
<span class="linenos">9</span>	    self.assertTrue(post_tag.name <span class="hljs-keyword">in</span> response.content)
<span class="linenos">10</span>	AssertionError: False is not <span class="hljs-literal">true</span>
<span class="linenos">11</span>	
<span class="linenos">12</span>	======================================================================
<span class="linenos">13</span>	FAIL: <span class="hljs-built_in">test</span>_post_page (blogengine.tests.PostViewTest)
<span class="linenos">14</span>	----------------------------------------------------------------------
<span class="linenos">15</span>	Traceback (most recent call last):
<span class="linenos">16</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">607</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_post_page
<span class="linenos">17</span>	    self.assertTrue(post_tag.name <span class="hljs-keyword">in</span> response.content)
<span class="linenos">18</span>	AssertionError: False is not <span class="hljs-literal">true</span>
<span class="linenos">19</span>	
<span class="linenos">20</span>	======================================================================
<span class="linenos">21</span>	FAIL: <span class="hljs-built_in">test</span>_tag_page (blogengine.tests.PostViewTest)
<span class="linenos">22</span>	----------------------------------------------------------------------
<span class="linenos">23</span>	Traceback (most recent call last):
<span class="linenos">24</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">714</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_tag_page
<span class="linenos">25</span>	    self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">26</span>	AssertionError: <span class="hljs-number">404</span> != <span class="hljs-number">200</span>
<span class="linenos">27</span>	
<span class="linenos">28</span>	----------------------------------------------------------------------
<span class="linenos">29</span>	Ran <span class="hljs-number">19</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">5.375</span>s
<span class="linenos">30</span>	
<span class="linenos">31</span>	FAILED (failures=<span class="hljs-number">3</span>)
<span class="linenos">32</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>So, we need to implement the following things:</p>
<ul>
<li>Show tags on the index page</li>
<li>Show tags on the post pages</li>
<li>Create a page listing the posts with a specific tag</li>
</ul>
<p>As we have seen already with the categories, this is actually quite simple. First, we’ll sort out the tags on the index page:</p>
<pre><code class="hljs lang-django"><span class="linenos">1</span>	<span class="xml"></span><span class="hljs-template_tag">{% <span class="hljs-keyword">extends</span> "blogengine/includes/base.html" %}</span><span class="xml">
<span class="linenos">2</span>	
<span class="linenos">3</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">load</span> custom_markdown %}</span><span class="xml">
<span class="linenos">4</span>	
<span class="linenos">5</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">block</span> content %}</span><span class="xml">
<span class="linenos">6</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">for</span> post <span class="hljs-keyword">in</span> object_list %}</span><span class="xml">
<span class="linenos">7</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"post"</span>&gt;</span>
<span class="linenos">8</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">h1</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ post.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span></span><span class="hljs-variable">{{ post.title }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">h1</span>&gt;</span>
<span class="linenos">9</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">h3</span>&gt;</span></span><span class="hljs-variable">{{ post.pub_date }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">h3</span>&gt;</span>
<span class="linenos">10</span>	        </span><span class="hljs-variable">{{ post.text<span class="hljs-filter">|custom</span>_markdown }}</span><span class="xml">
<span class="linenos">11</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">12</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ post.category.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span></span><span class="hljs-variable">{{ post.category.name }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">13</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">for</span> tag <span class="hljs-keyword">in</span> post.tags.all %}</span><span class="xml">
<span class="linenos">14</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ tag.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span></span><span class="hljs-variable">{{ tag.name }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">15</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endfor</span> %}</span><span class="xml">
<span class="linenos">16</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endfor</span> %}</span><span class="xml">
<span class="linenos">17</span>	
<span class="linenos">18</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> page_obj.has_previous %}</span><span class="xml">
<span class="linenos">19</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/</span></span></span><span class="hljs-variable">{{ page_obj.previous_page_number }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">/"</span>&gt;</span>Previous Page<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">20</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">21</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">if</span> page_obj.has_next %}</span><span class="xml">
<span class="linenos">22</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/</span></span></span><span class="hljs-variable">{{ page_obj.next_page_number }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">/"</span>&gt;</span>Next Page<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">23</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endif</span> %}</span><span class="xml">
<span class="linenos">24</span>	
<span class="linenos">25</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endblock</span> %}</span><span class="xml"></span></code></pre>
<p>This is quite simple. We retrieve all the tags with <code>post.tags.all</code> and loop through them. We then do basically the same for the individual post pages:</p>
<pre><code class="hljs lang-django"><span class="linenos">1</span>	<span class="xml"></span><span class="hljs-template_tag">{% <span class="hljs-keyword">extends</span> "blogengine/includes/base.html" %}</span><span class="xml">
<span class="linenos">2</span>	
<span class="linenos">3</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">load</span> custom_markdown %}</span><span class="xml">
<span class="linenos">4</span>	
<span class="linenos">5</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">block</span> content %}</span><span class="xml">
<span class="linenos">6</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"post"</span>&gt;</span>
<span class="linenos">7</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">h1</span>&gt;</span></span><span class="hljs-variable">{{ object.title }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">h1</span>&gt;</span>
<span class="linenos">8</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">h3</span>&gt;</span></span><span class="hljs-variable">{{ object.pub_date }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">h3</span>&gt;</span>
<span class="linenos">9</span>	        </span><span class="hljs-variable">{{ object.text<span class="hljs-filter">|custom</span>_markdown }}</span><span class="xml">
<span class="linenos">10</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ object.category.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span></span><span class="hljs-variable">{{ object.category.name }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">11</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">for</span> tag <span class="hljs-keyword">in</span> post.tags.all %}</span><span class="xml">
<span class="linenos">12</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"</span></span></span><span class="hljs-variable">{{ tag.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span>&gt;</span></span><span class="hljs-variable">{{ tag.name }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">13</span>	        </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endfor</span> %}</span><span class="xml">
<span class="linenos">14</span>	
<span class="linenos">15</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">h4</span>&gt;</span>Comments<span class="hljs-tag">&lt;/<span class="hljs-title">h4</span>&gt;</span>
<span class="linenos">16</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"fb-comments"</span> <span class="hljs-attribute">data-href</span>=<span class="hljs-value">"http://</span></span></span><span class="hljs-variable">{{ post.site }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value"></span></span></span><span class="hljs-variable">{{ post.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span> <span class="hljs-attribute">data-width</span>=<span class="hljs-value">"470"</span> <span class="hljs-attribute">data-num-posts</span>=<span class="hljs-value">"10"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">17</span>	
<span class="linenos">18</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">19</span>	
<span class="linenos">20</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endblock</span> %}</span><span class="xml"></span></code></pre>
<p>This should resolve two of our outstanding tests:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	(venv)Smith:django_tutorial_blog_ng matthewdaly$ python manage.py <span class="hljs-built_in">test</span> blogengine
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	..................F
<span class="linenos">4</span>	======================================================================
<span class="linenos">5</span>	FAIL: <span class="hljs-built_in">test</span>_tag_page (blogengine.tests.PostViewTest)
<span class="linenos">6</span>	----------------------------------------------------------------------
<span class="linenos">7</span>	Traceback (most recent call last):
<span class="linenos">8</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">714</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_tag_page
<span class="linenos">9</span>	    self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">10</span>	AssertionError: <span class="hljs-number">404</span> != <span class="hljs-number">200</span>
<span class="linenos">11</span>	
<span class="linenos">12</span>	----------------------------------------------------------------------
<span class="linenos">13</span>	Ran <span class="hljs-number">19</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">5.440</span>s
<span class="linenos">14</span>	
<span class="linenos">15</span>	FAILED (failures=<span class="hljs-number">1</span>)
<span class="linenos">16</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>The final test is for the tag pages. As we saw with the categories, we can limit our querysets on specific pages. So we’ll extend the <code>ListView</code> generic view again to handle tags:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-keyword">from</span> django.shortcuts <span class="hljs-keyword">import</span> render
<span class="linenos">2</span>	<span class="hljs-keyword">from</span> django.views.generic <span class="hljs-keyword">import</span> ListView
<span class="linenos">3</span>	<span class="hljs-keyword">from</span> blogengine.models <span class="hljs-keyword">import</span> Category, Post, Tag
<span class="linenos">4</span>	
<span class="linenos">5</span>	<span class="hljs-comment"># Create your views here.</span>
<span class="linenos">6</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">CategoryListView</span><span class="hljs-params">(ListView)</span>:</span>
<span class="linenos">7</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">get_queryset</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">8</span>	        slug = self.kwargs[<span class="hljs-string">'slug'</span>]
<span class="linenos">9</span>	        <span class="hljs-keyword">try</span>:
<span class="linenos">10</span>	            category = Category.objects.get(slug=slug)
<span class="linenos">11</span>	            <span class="hljs-keyword">return</span> Post.objects.filter(category=category)
<span class="linenos">12</span>	        <span class="hljs-keyword">except</span> Category.DoesNotExist:
<span class="linenos">13</span>	            <span class="hljs-keyword">return</span> Post.objects.none()
<span class="linenos">14</span>	
<span class="linenos">15</span>	
<span class="linenos">16</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">TagListView</span><span class="hljs-params">(ListView)</span>:</span>
<span class="linenos">17</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">get_queryset</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">18</span>	        slug = self.kwargs[<span class="hljs-string">'slug'</span>]
<span class="linenos">19</span>	        <span class="hljs-keyword">try</span>:
<span class="linenos">20</span>	            tag = Tag.objects.get(slug=slug)
<span class="linenos">21</span>	            <span class="hljs-keyword">return</span> tag.post_set.all()
<span class="linenos">22</span>	        <span class="hljs-keyword">except</span> Tag.DoesNotExist:
<span class="linenos">23</span>	            <span class="hljs-keyword">return</span> Post.objects.none()</code></pre>
<p>Note that here, <code>Tag</code> objects have access to their assigned <code>Post</code> objects - we just use <code>post_set</code> to refer to them and get all of the posts associated with that tag. Next we’ll add the URLconfs:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-keyword">from</span> django.conf.urls <span class="hljs-keyword">import</span> patterns, url
<span class="linenos">2</span>	<span class="hljs-keyword">from</span> django.views.generic <span class="hljs-keyword">import</span> ListView, DetailView
<span class="linenos">3</span>	<span class="hljs-keyword">from</span> blogengine.models <span class="hljs-keyword">import</span> Post, Category, Tag
<span class="linenos">4</span>	<span class="hljs-keyword">from</span> blogengine.views <span class="hljs-keyword">import</span> CategoryListView, TagListView
<span class="linenos">5</span>	
<span class="linenos">6</span>	urlpatterns = patterns(<span class="hljs-string">''</span>,
<span class="linenos">7</span>	    <span class="hljs-comment"># Index</span>
<span class="linenos">8</span>	    url(<span class="hljs-string">r'^(?P&lt;page&gt;\d+)?/?$'</span>, ListView.as_view(
<span class="linenos">9</span>	        model=Post,
<span class="linenos">10</span>	        paginate_by=<span class="hljs-number">5</span>,
<span class="linenos">11</span>	        )),
<span class="linenos">12</span>	
<span class="linenos">13</span>	    <span class="hljs-comment"># Individual posts</span>
<span class="linenos">14</span>	    url(<span class="hljs-string">r'^(?P&lt;pub_date__year&gt;\d{4})/(?P&lt;pub_date__month&gt;\d{1,2})/(?P&lt;slug&gt;[a-zA-Z0-9-]+)/?$'</span>, DetailView.as_view(
<span class="linenos">15</span>	        model=Post,
<span class="linenos">16</span>	        )),
<span class="linenos">17</span>	
<span class="linenos">18</span>	    <span class="hljs-comment"># Categories</span>
<span class="linenos">19</span>	    url(<span class="hljs-string">r'^category/(?P&lt;slug&gt;[a-zA-Z0-9-]+)/?$'</span>, CategoryListView.as_view(
<span class="linenos">20</span>	        paginate_by=<span class="hljs-number">5</span>,
<span class="linenos">21</span>	        model=Category,
<span class="linenos">22</span>	        )),
<span class="linenos">23</span>	
<span class="linenos">24</span>	    <span class="hljs-comment"># Tags</span>
<span class="linenos">25</span>	    url(<span class="hljs-string">r'^tag/(?P&lt;slug&gt;[a-zA-Z0-9-]+)/?$'</span>, TagListView.as_view(
<span class="linenos">26</span>	        paginate_by=<span class="hljs-number">5</span>,
<span class="linenos">27</span>	        model=Tag,
<span class="linenos">28</span>	        )),
<span class="linenos">29</span>	)</code></pre>
<p>We import the <code>Tag</code> model and the <code>TagListView</code> view, and use them to set up the tag page.</p>
<p>If we now run our tests again, they should pass:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	(venv)Smith:django_tutorial_blog_ng matthewdaly$ python manage.py <span class="hljs-built_in">test</span> blogengine
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	...................
<span class="linenos">4</span>	----------------------------------------------------------------------
<span class="linenos">5</span>	Ran <span class="hljs-number">19</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">5.473</span>s
<span class="linenos">6</span>	
<span class="linenos">7</span>	OK
<span class="linenos">8</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>Well done! Time to commit:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add templates/ blogengine/
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Tags are now shown'</span></code></pre>
<h2 id="rss-feed">RSS Feed</h2>
<p>For the final task today, we’ll implement an RSS feed for our posts. Django ships with a handy syndication framework that makes it easy to implement this kind of functionality.</p>
<p>As usual, we’ll create some tests first. In this case, we won’t be adding any new models, so we don’t need to test them. Instead we can jump straight into creating acceptance tests for our feed. For now we’ll just create one type of feed: a feed of all the blog posts. In a later instalment we’ll add feeds for categories and tags.</p>
<p>First of all, we’ll implement our test. Now, in order to test our feed, we need to have a solution in place for parsing an RSS feed. Django won’t do this natively, so we’ll install the <code>feedparser</code> Python module. Run the following commands:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ pip install feedparser
<span class="linenos">2</span>	$ pip freeze &gt; requirements.txt</code></pre>
<p>Once that’s done, feedparser should be available. You may wish to refer to the <a href="http://pythonhosted.org/feedparser/">documentation</a> as we go to help.</p>
<p>Let’s write our test for the RSS feed. First, we import feedparser near the top of the file:</p>
<pre><code class="hljs lang-python singleline"><span class="hljs-keyword">import</span> feedparser</code></pre>
<p>Then we define a new class for our feed tests. Put this towards the end of the file - I put it just before the flat page tests:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">FeedTest</span><span class="hljs-params">(BaseAcceptanceTest)</span>:</span>
<span class="linenos">2</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_all_post_feed</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">3</span>	        <span class="hljs-comment"># Create the category</span>
<span class="linenos">4</span>	        category = Category()
<span class="linenos">5</span>	        category.name = <span class="hljs-string">'python'</span>
<span class="linenos">6</span>	        category.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">7</span>	        category.save()
<span class="linenos">8</span>	
<span class="linenos">9</span>	        <span class="hljs-comment"># Create the tag</span>
<span class="linenos">10</span>	        tag = Tag()
<span class="linenos">11</span>	        tag.name = <span class="hljs-string">'python'</span>
<span class="linenos">12</span>	        tag.description = <span class="hljs-string">'The Python programming language'</span>
<span class="linenos">13</span>	        tag.save()
<span class="linenos">14</span>	
<span class="linenos">15</span>	        <span class="hljs-comment"># Create the author</span>
<span class="linenos">16</span>	        author = User.objects.create_user(<span class="hljs-string">'testuser'</span>, <span class="hljs-string">'user@example.com'</span>, <span class="hljs-string">'password'</span>)
<span class="linenos">17</span>	        author.save()
<span class="linenos">18</span>	
<span class="linenos">19</span>	        <span class="hljs-comment"># Create the site</span>
<span class="linenos">20</span>	        site = Site()
<span class="linenos">21</span>	        site.name = <span class="hljs-string">'example.com'</span>
<span class="linenos">22</span>	        site.domain = <span class="hljs-string">'example.com'</span>
<span class="linenos">23</span>	        site.save()
<span class="linenos">24</span>	
<span class="linenos">25</span>	        <span class="hljs-comment"># Create a post</span>
<span class="linenos">26</span>	        post = Post()
<span class="linenos">27</span>	        post.title = <span class="hljs-string">'My first post'</span>
<span class="linenos">28</span>	        post.text = <span class="hljs-string">'This is my first blog post'</span>
<span class="linenos">29</span>	        post.slug = <span class="hljs-string">'my-first-post'</span>
<span class="linenos">30</span>	        post.pub_date = timezone.now()
<span class="linenos">31</span>	        post.author = author
<span class="linenos">32</span>	        post.site = site
<span class="linenos">33</span>	        post.category = category
<span class="linenos">34</span>	
<span class="linenos">35</span>	        <span class="hljs-comment"># Save it</span>
<span class="linenos">36</span>	        post.save()
<span class="linenos">37</span>	
<span class="linenos">38</span>	        <span class="hljs-comment"># Add the tag</span>
<span class="linenos">39</span>	        post.tags.add(tag)
<span class="linenos">40</span>	        post.save()
<span class="linenos">41</span>	
<span class="linenos">42</span>	        <span class="hljs-comment"># Check we can find it</span>
<span class="linenos">43</span>	        all_posts = Post.objects.all()
<span class="linenos">44</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">45</span>	        only_post = all_posts[<span class="hljs-number">0</span>]
<span class="linenos">46</span>	        self.assertEquals(only_post, post)
<span class="linenos">47</span>	
<span class="linenos">48</span>	        <span class="hljs-comment"># Fetch the feed</span>
<span class="linenos">49</span>	        response = self.client.get(<span class="hljs-string">'/feeds/posts/'</span>)
<span class="linenos">50</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">51</span>	
<span class="linenos">52</span>	        <span class="hljs-comment"># Parse the feed</span>
<span class="linenos">53</span>	        feed = feedparser.parse(response.content)
<span class="linenos">54</span>	
<span class="linenos">55</span>	        <span class="hljs-comment"># Check length</span>
<span class="linenos">56</span>	        self.assertEquals(len(feed.entries), <span class="hljs-number">1</span>)
<span class="linenos">57</span>	
<span class="linenos">58</span>	        <span class="hljs-comment"># Check post retrieved is the correct one</span>
<span class="linenos">59</span>	        feed_post = feed.entries[<span class="hljs-number">0</span>]
<span class="linenos">60</span>	        self.assertEquals(feed_post.title, post.title)
<span class="linenos">61</span>	        self.assertEquals(feed_post.description, post.text)</code></pre>
<p>Run the tests and you’ll see something like this:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	(venv)Smith:django_tutorial_blog_ng matthewdaly$ python manage.py <span class="hljs-built_in">test</span> blogengine
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	..............F.....
<span class="linenos">4</span>	======================================================================
<span class="linenos">5</span>	FAIL: <span class="hljs-built_in">test</span>_all_post_feed (blogengine.tests.FeedTest)
<span class="linenos">6</span>	----------------------------------------------------------------------
<span class="linenos">7</span>	Traceback (most recent call last):
<span class="linenos">8</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">781</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_all_post_feed
<span class="linenos">9</span>	    self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">10</span>	AssertionError: <span class="hljs-number">404</span> != <span class="hljs-number">200</span>
<span class="linenos">11</span>	
<span class="linenos">12</span>	----------------------------------------------------------------------
<span class="linenos">13</span>	Ran <span class="hljs-number">20</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">6.743</span>s
<span class="linenos">14</span>	
<span class="linenos">15</span>	FAILED (failures=<span class="hljs-number">1</span>)
<span class="linenos">16</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>We’re getting a 404 error because the post feed isn’t implemented. So let’s implement it. We’re going to use Django’s syndication framework, which will make it easy, but we need to enable it. Open up <code>django_tutorial_blog_ng/settings/py</code> and add the following under <code>INSTALLED_APPS</code>:</p>
<pre><code class="hljs lang-python singleline">    <span class="hljs-string">'django.contrib.syndication'</span>,</code></pre>
<p>Next, we need to enable the URLconf for this RSS feed. Open up <code>blogengine/urls.py and amend the import from</code>blogengine.views` near the top:</p>
<pre><code class="hljs lang-python singleline"><span class="hljs-keyword">from</span> blogengine.views <span class="hljs-keyword">import</span> CategoryListView, TagListView, PostsFeed</code></pre>
<p>Further down, add in the following code to define the URL for the feed:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	    <span class="hljs-comment"># Post RSS feed</span>
<span class="linenos">2</span>	    url(<span class="hljs-string">r'^feeds/posts/$'</span>, PostsFeed()),</code></pre>
<p>Note that we imported the PostsFeed class, but that hasn’t yet been defined. So we need to do that. First of all, add this line near the top:</p>
<pre><code class="hljs lang-python singleline"><span class="hljs-keyword">from</span> django.contrib.syndication.views <span class="hljs-keyword">import</span> Feed</code></pre>
<p>This imports the syndication views - yes, they’re another generic view! Our <code>PostsFeed</code> class is going to inherit from <code>Feed</code>. Next, we define the class:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">PostsFeed</span><span class="hljs-params">(Feed)</span>:</span>
<span class="linenos">2</span>	    title = <span class="hljs-string">"RSS feed - posts"</span>
<span class="linenos">3</span>	    link = <span class="hljs-string">"feeds/posts/"</span>
<span class="linenos">4</span>	    description = <span class="hljs-string">"RSS feed - blog posts"</span>
<span class="linenos">5</span>	
<span class="linenos">6</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">items</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">7</span>	        <span class="hljs-keyword">return</span> Post.objects.order_by(<span class="hljs-string">'-pub_date'</span>)
<span class="linenos">8</span>	
<span class="linenos">9</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">item_title</span><span class="hljs-params">(self, item)</span>:</span>
<span class="linenos">10</span>	        <span class="hljs-keyword">return</span> item.title
<span class="linenos">11</span>	
<span class="linenos">12</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">item_description</span><span class="hljs-params">(self, item)</span>:</span>
<span class="linenos">13</span>	        <span class="hljs-keyword">return</span> item.text</code></pre>
<p>This is fairly straightforward. We define our title, link, and description for the feed inside the class definition. We define the <code>items</code> method which sets what items are returned by the RSS feed. We also define the <code>item_title</code> and <code>item_description</code> methods.</p>
<p>Now, if we run our tests, they should pass:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	(venv)Smith:django_tutorial_blog_ng matthewdaly$ python manage.py <span class="hljs-built_in">test</span> blogengine
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	....................
<span class="linenos">4</span>	----------------------------------------------------------------------
<span class="linenos">5</span>	Ran <span class="hljs-number">20</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">5.933</span>s
<span class="linenos">6</span>	
<span class="linenos">7</span>	OK
<span class="linenos">8</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>Let’s commit our changes:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add blogengine/ django_tutorial_blog_ng/ requirements.txt
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'RSS feed implemented'</span></code></pre>
<p>And that’s enough for now. Don’t forget, you can get the code for this lesson by cloning the repository from <a href="https://github.com/matthewbdaly/django_tutorial_blog_ng">Github</a> and running <code>git checkout lesson-4</code> to switch to this lesson.</p>
<p>Next time we’ll:</p>
<ul>
<li>Implement a search system</li>
<li>Add feeds for categories and posts</li>
<li>Tidy up the user interface</li>
</ul>
<p>Hope to see you then!</p>
]]></description>
            <link>http://matthewdaly.co.uk/blog/2014/02/15/django-blog-tutorial-the-next-generation-part-4/</link>
            <guid isPermaLink="true">http://matthewdaly.co.uk/blog/2014/02/15/django-blog-tutorial-the-next-generation-part-4/</guid>
            <pubDate>Sat, 15 Feb 2014 17:45:00 GMT</pubDate>
        </item>
        <item>
            <title><![CDATA[My first Yeoman generator]]></title>
            <description><![CDATA[<p>At work I use the <a href="http://www.getskeleton.com/">Skeleton boilerplate</a> a lot - my boss, who handles most of the design work, likes it and generally uses it for his designs. I’ve also been using <a href="http://gruntjs.com/">Grunt</a> a lot lately, so it was inevitable that I’d probably start to look for a <a href="http://yeoman.io/">Yeoman</a> generator for working with it.</p>
<p>There was an existing Yeoman generator for Skeleton, but it didn’t really do what I wanted. I wanted something that:</p>
<ul>
<li>Included jQuery and Modernizr</li>
<li>Automatically concatenates and minifies all the JavaScript and CSS</li>
<li>Will automatically rebuild on changes</li>
<li>Includes LiveReload and a development server</li>
<li>Includes automatic deployment via FTP</li>
</ul>
<p>After looking through the documentation for Yeoman, it was actually quick and easy to throw together my own generator and put it up. It’s available <a href="https://npmjs.org/package/generator-skellington">here</a>, and the GitHub repository is <a href="https://github.com/matthewbdaly/generator-skellington">here</a>.</p>
<p>Future plans for it include:</p>
<ul>
<li>Adding auto-prefixing for CSS</li>
<li>Removing redundant CSS rules automatically</li>
<li>Possibly, alternate deployment methods</li>
</ul>
<p>Frustratingly, NPM seems to be playing up at present - it’s not picking up the README file, and the Yeoman site isn’t pulling it in. Any idea why, anyone?</p>
]]></description>
            <link>http://matthewdaly.co.uk/blog/2014/01/25/my-first-yeoman-generator/</link>
            <guid isPermaLink="true">http://matthewdaly.co.uk/blog/2014/01/25/my-first-yeoman-generator/</guid>
            <pubDate>Sat, 25 Jan 2014 11:38:40 GMT</pubDate>
        </item>
        <item>
            <title><![CDATA[Django Blog Tutorial - the Next Generation - Part 3]]></title>
            <description><![CDATA[<p>Hello again! In this instalment, we’re going to do the following:</p>
<ul>
<li>Add support for flat pages</li>
<li>Add support for multiple authors</li>
<li>Add a third-party comment system</li>
</ul>
<h2 id="flat-pages">Flat pages</h2>
<p>Django ships with a number of useful apps - we’ve already used the admin interface. The flat pages app is another very handy app that comes with Django, and we’ll use it to allow the blog author to create a handful of flat pages.</p>
<p>First of all, you’ll need to install the flatpages app. Edit the <code>INSTALLED_APPS</code> setting as follows:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	INSTALLED_APPS = (
<span class="linenos">2</span>	    <span class="hljs-string">'django.contrib.admin'</span>,
<span class="linenos">3</span>	    <span class="hljs-string">'django.contrib.auth'</span>,
<span class="linenos">4</span>	    <span class="hljs-string">'django.contrib.contenttypes'</span>,
<span class="linenos">5</span>	    <span class="hljs-string">'django.contrib.sessions'</span>,
<span class="linenos">6</span>	    <span class="hljs-string">'django.contrib.messages'</span>,
<span class="linenos">7</span>	    <span class="hljs-string">'django.contrib.staticfiles'</span>,
<span class="linenos">8</span>	    <span class="hljs-string">'south'</span>,
<span class="linenos">9</span>	    <span class="hljs-string">'blogengine'</span>,
<span class="linenos">10</span>	    <span class="hljs-string">'django.contrib.sites'</span>,
<span class="linenos">11</span>	    <span class="hljs-string">'django.contrib.flatpages'</span>,
<span class="linenos">12</span>	)</code></pre>
<p>Note that we needed to enable the <code>sites</code> framework as well. You’ll also need to set the <code>SITE_ID</code> setting:</p>
<pre><code class="hljs lang-python singleline">SITE_ID = <span class="hljs-number">1</span></code></pre>
<p>With that done, run <code>python manage.py syncdb</code> to create the required database tables. Now, let’s use the <code>sqlall</code> command to take a look at the database structure generated for the flat pages:</p>
<pre><code class="hljs lang-sql"><span class="linenos">1</span>	<span class="hljs-operator"><span class="hljs-keyword">BEGIN</span>;</span>
<span class="linenos">2</span>	<span class="hljs-operator"><span class="hljs-keyword">CREATE</span> <span class="hljs-keyword">TABLE</span> <span class="hljs-string">"django_flatpage_sites"</span> (
<span class="linenos">3</span>	    <span class="hljs-string">"id"</span> <span class="hljs-built_in">integer</span> <span class="hljs-keyword">NOT</span> <span class="hljs-literal">NULL</span> <span class="hljs-keyword">PRIMARY</span> <span class="hljs-keyword">KEY</span>,
<span class="linenos">4</span>	    <span class="hljs-string">"flatpage_id"</span> <span class="hljs-built_in">integer</span> <span class="hljs-keyword">NOT</span> <span class="hljs-literal">NULL</span>,
<span class="linenos">5</span>	    <span class="hljs-string">"site_id"</span> <span class="hljs-built_in">integer</span> <span class="hljs-keyword">NOT</span> <span class="hljs-literal">NULL</span> <span class="hljs-keyword">REFERENCES</span> <span class="hljs-string">"django_site"</span> (<span class="hljs-string">"id"</span>),
<span class="linenos">6</span>	    <span class="hljs-keyword">UNIQUE</span> (<span class="hljs-string">"flatpage_id"</span>, <span class="hljs-string">"site_id"</span>)
<span class="linenos">7</span>	)
<span class="linenos">8</span>	;</span>
<span class="linenos">9</span>	<span class="hljs-operator"><span class="hljs-keyword">CREATE</span> <span class="hljs-keyword">TABLE</span> <span class="hljs-string">"django_flatpage"</span> (
<span class="linenos">10</span>	    <span class="hljs-string">"id"</span> <span class="hljs-built_in">integer</span> <span class="hljs-keyword">NOT</span> <span class="hljs-literal">NULL</span> <span class="hljs-keyword">PRIMARY</span> <span class="hljs-keyword">KEY</span>,
<span class="linenos">11</span>	    <span class="hljs-string">"url"</span> <span class="hljs-built_in">varchar</span>(<span class="hljs-number">100</span>) <span class="hljs-keyword">NOT</span> <span class="hljs-literal">NULL</span>,
<span class="linenos">12</span>	    <span class="hljs-string">"title"</span> <span class="hljs-built_in">varchar</span>(<span class="hljs-number">200</span>) <span class="hljs-keyword">NOT</span> <span class="hljs-literal">NULL</span>,
<span class="linenos">13</span>	    <span class="hljs-string">"content"</span> <span class="hljs-built_in">text</span> <span class="hljs-keyword">NOT</span> <span class="hljs-literal">NULL</span>,
<span class="linenos">14</span>	    <span class="hljs-string">"enable_comments"</span> bool <span class="hljs-keyword">NOT</span> <span class="hljs-literal">NULL</span>,
<span class="linenos">15</span>	    <span class="hljs-string">"template_name"</span> <span class="hljs-built_in">varchar</span>(<span class="hljs-number">70</span>) <span class="hljs-keyword">NOT</span> <span class="hljs-literal">NULL</span>,
<span class="linenos">16</span>	    <span class="hljs-string">"registration_required"</span> bool <span class="hljs-keyword">NOT</span> <span class="hljs-literal">NULL</span>
<span class="linenos">17</span>	)
<span class="linenos">18</span>	;</span>
<span class="linenos">19</span>	<span class="hljs-operator"><span class="hljs-keyword">CREATE</span> <span class="hljs-keyword">INDEX</span> <span class="hljs-string">"django_flatpage_sites_872c4601"</span> <span class="hljs-keyword">ON</span> <span class="hljs-string">"django_flatpage_sites"</span> (<span class="hljs-string">"flatpage_id"</span>);</span>
<span class="linenos">20</span>	<span class="hljs-operator"><span class="hljs-keyword">CREATE</span> <span class="hljs-keyword">INDEX</span> <span class="hljs-string">"django_flatpage_sites_99732b5c"</span> <span class="hljs-keyword">ON</span> <span class="hljs-string">"django_flatpage_sites"</span> (<span class="hljs-string">"site_id"</span>);</span>
<span class="linenos">21</span>	<span class="hljs-operator"><span class="hljs-keyword">CREATE</span> <span class="hljs-keyword">INDEX</span> <span class="hljs-string">"django_flatpage_c379dc61"</span> <span class="hljs-keyword">ON</span> <span class="hljs-string">"django_flatpage"</span> (<span class="hljs-string">"url"</span>);</span>
<span class="linenos">22</span>	
<span class="linenos">23</span>	<span class="hljs-operator"><span class="hljs-keyword">COMMIT</span>;</span></code></pre>
<p>As mentioned previously, all models in Django have an <code>id</code> attribute by default. Each flat page also has a URL, title, and content. </p>
<p>Also note the separate <code>django_flatpage_sites</code> table, which maps sites to flat pages. Django can run multiple sites from the same web app, and so flat pages must be allocated to a specific site. This relationship is a many-to-many relationship, so one flat page can appear on more than one site.</p>
<p>The other fields <a href="http://127.0.0.1:8000/admin/flatpages/flatpage/add/">are hidden by default in the admin</a> and can be ignored. Let’s have a go with Django’s handy shell to explore the flatpage. Run <code>python manage.py shell</code> and you’ll be able to interact with your Django application interactively:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	(venv)Smith:django_tutorial_blog_ng matthewdaly$ python manage.py shell
<span class="linenos">2</span>	Python 2.7.6 (default, Nov 23 2013, 13:53:45)
<span class="linenos">3</span>	[GCC 4.2.1 (Apple Inc. build 5666) (dot 3)] on darwin
<span class="linenos">4</span>	Type "help", "copyright", "credits" or "license" for more information.
<span class="linenos">5</span>	(InteractiveConsole)
<span class="linenos">6</span>	&gt;&gt;&gt; from django.contrib.flatpages.models import *
<span class="linenos">7</span>	&gt;&gt;&gt; FlatPage
<span class="linenos">8</span>	&lt;class 'django.contrib.flatpages.models.FlatPage'&gt;
<span class="linenos">9</span>	&gt;&gt;&gt; from django.contrib.sites.models import Site
<span class="linenos">10</span>	&gt;&gt;&gt; Site.objects.all()
<span class="linenos">11</span>	[&lt;Site: example.com&gt;]</code></pre>
<p>As you can see, <code>flatpages</code> is a Django app similar to the <code>blogengine</code> one, with its own models, as is <code>sites</code>. You can see that the <code>FlatPage</code> class is a model. We can create an instance of it and save it interactively:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-prompt">&gt;&gt;&gt; </span>f = FlatPage()
<span class="linenos">2</span>	<span class="hljs-prompt">&gt;&gt;&gt; </span>f.url = <span class="hljs-string">'/about/'</span>
<span class="linenos">3</span>	<span class="hljs-prompt">&gt;&gt;&gt; </span>f.title = <span class="hljs-string">'About me'</span>
<span class="linenos">4</span>	<span class="hljs-prompt">&gt;&gt;&gt; </span>f.content = <span class="hljs-string">'All about me'</span>
<span class="linenos">5</span>	<span class="hljs-prompt">&gt;&gt;&gt; </span>f.save()
<span class="linenos">6</span>	<span class="hljs-prompt">&gt;&gt;&gt; </span>f.sites.add(Site.objects.all()[<span class="hljs-number">0</span>])
<span class="linenos">7</span>	<span class="hljs-prompt">&gt;&gt;&gt; </span>f.save()</code></pre>
<p>Note that because the relationship between the site and the flat page is a many-to-many relationship, we need to save it first, then use the <code>add</code> method to add the site to the list of sites.</p>
<p>We can retrieve it:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-prompt">&gt;&gt;&gt; </span>FlatPage.objects.all()
<span class="linenos">2</span>	[&lt;FlatPage: /about/ -- About me&gt;]
<span class="linenos">3</span>	<span class="hljs-prompt">&gt;&gt;&gt; </span>FlatPage.objects.all()[<span class="hljs-number">0</span>]
<span class="linenos">4</span>	&lt;FlatPage: /about/ -- About me&gt;
<span class="linenos">5</span>	<span class="hljs-prompt">&gt;&gt;&gt; </span>FlatPage.objects.all()[<span class="hljs-number">0</span>].title
<span class="linenos">6</span>	<span class="hljs-string">u'About me'</span></code></pre>
<p>This command is often handy for debugging problems with your models interactively. If you now run the server and visit the admin, you should notice that the Flatpages app is now visible there, and the ‘About me’ flat page is now shown in there.</p>
<p>Let’s also take a look at the SQL required for the <code>Site</code> model. Run <code>python manage.py sqlall sites</code>:</p>
<pre><code class="hljs lang-sql"><span class="linenos">1</span>	<span class="hljs-operator"><span class="hljs-keyword">BEGIN</span>;</span>
<span class="linenos">2</span>	<span class="hljs-operator"><span class="hljs-keyword">CREATE</span> <span class="hljs-keyword">TABLE</span> <span class="hljs-string">"django_site"</span> (
<span class="linenos">3</span>	    <span class="hljs-string">"id"</span> <span class="hljs-built_in">integer</span> <span class="hljs-keyword">NOT</span> <span class="hljs-literal">NULL</span> <span class="hljs-keyword">PRIMARY</span> <span class="hljs-keyword">KEY</span>,
<span class="linenos">4</span>	    <span class="hljs-string">"domain"</span> <span class="hljs-built_in">varchar</span>(<span class="hljs-number">100</span>) <span class="hljs-keyword">NOT</span> <span class="hljs-literal">NULL</span>,
<span class="linenos">5</span>	    <span class="hljs-string">"name"</span> <span class="hljs-built_in">varchar</span>(<span class="hljs-number">50</span>) <span class="hljs-keyword">NOT</span> <span class="hljs-literal">NULL</span>
<span class="linenos">6</span>	)
<span class="linenos">7</span>	;</span>
<span class="linenos">8</span>	
<span class="linenos">9</span>	<span class="hljs-operator"><span class="hljs-keyword">COMMIT</span>;</span></code></pre>
<p>Again, very simple - just a domain and a name.</p>
<p>So, now that we have a good idea of how the flat page system works, we can write a test for it. We don’t need to write unit tests for the model because Django already does that, but we do need to write an acceptance test to ensure we can create flat pages and they will be where we expect them to be. Add the following to the top of the test file:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-keyword">from</span> django.contrib.flatpages.models <span class="hljs-keyword">import</span> FlatPage
<span class="linenos">2</span>	<span class="hljs-keyword">from</span> django.contrib.sites.models <span class="hljs-keyword">import</span> Site</code></pre>
<p>Now, before we write this test, there’s some duplication to resolve. We have two tests that subclass <code>LiveServerTestCase</code>, and both have the same method, <code>setUp</code>. We can save ourselves some hassle by creating a new class containing this method and having both these tests inherit from it. We’ll do that now because the flat page test can also be based on it. Create the following class just after <code>PostTest</code>:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">BaseAcceptanceTest</span><span class="hljs-params">(LiveServerTestCase)</span>:</span>
<span class="linenos">2</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">setUp</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">3</span>	        self.client = Client()</code></pre>
<p>Then remove the setUp method from each of the two tests based on <code>LiveServerTestCase</code>, and change their parent class to <code>BaseAcceptanceTest</code>:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">AdminTest</span><span class="hljs-params">(BaseAcceptanceTest)</span>:</span>
<span class="linenos">2</span>	
<span class="linenos">3</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">PostViewTest</span><span class="hljs-params">(BaseAcceptanceTest)</span>:</span></code></pre>
<p>With that done, run the tests and they should pass. Commit your changes:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add blogengine/tests.py django_tutorial_blog_ng/settings.py
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Added flatpages to installed apps'</span></code></pre>
<p>Now we can get started in earnest on our test for the flat pages:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">FlatPageViewTest</span><span class="hljs-params">(BaseAcceptanceTest)</span>:</span>
<span class="linenos">2</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_create_flat_page</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">3</span>	        <span class="hljs-comment"># Create flat page</span>
<span class="linenos">4</span>	        page = FlatPage()
<span class="linenos">5</span>	        page.url = <span class="hljs-string">'/about/'</span>
<span class="linenos">6</span>	        page.title = <span class="hljs-string">'About me'</span>
<span class="linenos">7</span>	        page.content = <span class="hljs-string">'All about me'</span>
<span class="linenos">8</span>	        page.save()
<span class="linenos">9</span>	
<span class="linenos">10</span>	        <span class="hljs-comment"># Add the site</span>
<span class="linenos">11</span>	        page.sites.add(Site.objects.all()[<span class="hljs-number">0</span>])
<span class="linenos">12</span>	        page.save()
<span class="linenos">13</span>	
<span class="linenos">14</span>	        <span class="hljs-comment"># Check new page saved</span>
<span class="linenos">15</span>	        all_pages = FlatPage.objects.all()
<span class="linenos">16</span>	        self.assertEquals(len(all_pages), <span class="hljs-number">1</span>)
<span class="linenos">17</span>	        only_page = all_pages[<span class="hljs-number">0</span>]
<span class="linenos">18</span>	        self.assertEquals(only_page, page)
<span class="linenos">19</span>	
<span class="linenos">20</span>	        <span class="hljs-comment"># Check data correct</span>
<span class="linenos">21</span>	        self.assertEquals(only_page.url, <span class="hljs-string">'/about/'</span>)
<span class="linenos">22</span>	        self.assertEquals(only_page.title, <span class="hljs-string">'About me'</span>)
<span class="linenos">23</span>	        self.assertEquals(only_page.content, <span class="hljs-string">'All about me'</span>)
<span class="linenos">24</span>	
<span class="linenos">25</span>	        <span class="hljs-comment"># Get URL</span>
<span class="linenos">26</span>	        page_url = only_page.get_absolute_url()
<span class="linenos">27</span>	
<span class="linenos">28</span>	        <span class="hljs-comment"># Get the page</span>
<span class="linenos">29</span>	        response = self.client.get(page_url)
<span class="linenos">30</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">31</span>	
<span class="linenos">32</span>	        <span class="hljs-comment"># Check title and content in response</span>
<span class="linenos">33</span>	        self.assertTrue(<span class="hljs-string">'About me'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">34</span>	        self.assertTrue(<span class="hljs-string">'All about me'</span> <span class="hljs-keyword">in</span> response.content)</code></pre>
<p>Let’s run our tests:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	(venv)Smith:django_tutorial_blog_ng matthewdaly$ python manage.py <span class="hljs-built_in">test</span>
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	......F..
<span class="linenos">4</span>	======================================================================
<span class="linenos">5</span>	FAIL: <span class="hljs-built_in">test</span>_create_flat_page (blogengine.tests.FlatPageViewTest)
<span class="linenos">6</span>	----------------------------------------------------------------------
<span class="linenos">7</span>	Traceback (most recent call last):
<span class="linenos">8</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">272</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_create_flat_page
<span class="linenos">9</span>	    self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">10</span>	AssertionError: <span class="hljs-number">404</span> != <span class="hljs-number">200</span>
<span class="linenos">11</span>	
<span class="linenos">12</span>	----------------------------------------------------------------------
<span class="linenos">13</span>	Ran <span class="hljs-number">9</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">2.760</span>s
<span class="linenos">14</span>	
<span class="linenos">15</span>	FAILED (failures=<span class="hljs-number">1</span>)</code></pre>
<p>We can see why it’s failed - in our flat page test, the status code is 404, indicating the page was not found. This just means we haven’t put flat page support into our URLconf. So let’s fix that:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-keyword">from</span> django.conf.urls <span class="hljs-keyword">import</span> patterns, include, url
<span class="linenos">2</span>	
<span class="linenos">3</span>	<span class="hljs-keyword">from</span> django.contrib <span class="hljs-keyword">import</span> admin
<span class="linenos">4</span>	admin.autodiscover()
<span class="linenos">5</span>	
<span class="linenos">6</span>	urlpatterns = patterns(<span class="hljs-string">''</span>,
<span class="linenos">7</span>	    <span class="hljs-comment"># Examples:</span>
<span class="linenos">8</span>	    <span class="hljs-comment"># url(r'^$', 'django_tutorial_blog_ng.views.home', name='home'),</span>
<span class="linenos">9</span>	    <span class="hljs-comment"># url(r'^blog/', include('blog.urls')),</span>
<span class="linenos">10</span>	
<span class="linenos">11</span>	    url(<span class="hljs-string">r'^admin/'</span>, include(admin.site.urls)),
<span class="linenos">12</span>	
<span class="linenos">13</span>	    <span class="hljs-comment"># Blog URLs</span>
<span class="linenos">14</span>	    url(<span class="hljs-string">r''</span>, include(<span class="hljs-string">'blogengine.urls'</span>)),
<span class="linenos">15</span>	
<span class="linenos">16</span>	    <span class="hljs-comment"># Flat pages</span>
<span class="linenos">17</span>	    url(<span class="hljs-string">r''</span>, include(<span class="hljs-string">'django.contrib.flatpages.urls'</span>)),
<span class="linenos">18</span>	)</code></pre>
<p>Let’s run our tests again:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	(venv)Smith:django_tutorial_blog_ng matthewdaly$ python manage.py <span class="hljs-built_in">test</span>
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	......E..
<span class="linenos">4</span>	======================================================================
<span class="linenos">5</span>	ERROR: <span class="hljs-built_in">test</span>_create_flat_page (blogengine.tests.FlatPageViewTest)
<span class="linenos">6</span>	----------------------------------------------------------------------
<span class="linenos">7</span>	Traceback (most recent call last):
<span class="linenos">8</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">276</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_create_flat_page
<span class="linenos">9</span>	    response = self.client.get(page_url)
<span class="linenos">10</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/test/client.py"</span>, line <span class="hljs-number">473</span>, <span class="hljs-keyword">in</span> get
<span class="linenos">11</span>	    response = super(Client, self).get(path, data=data, **extra)
<span class="linenos">12</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/test/client.py"</span>, line <span class="hljs-number">280</span>, <span class="hljs-keyword">in</span> get
<span class="linenos">13</span>	    <span class="hljs-built_in">return</span> self.request(**r)
<span class="linenos">14</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/test/client.py"</span>, line <span class="hljs-number">444</span>, <span class="hljs-keyword">in</span> request
<span class="linenos">15</span>	    six.reraise(*exc_info)
<span class="linenos">16</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/core/handlers/base.py"</span>, line <span class="hljs-number">114</span>, <span class="hljs-keyword">in</span> get_response
<span class="linenos">17</span>	    response = wrapped_callback(request, *callback_args, **callback_kwargs)
<span class="linenos">18</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/contrib/flatpages/views.py"</span>, line <span class="hljs-number">45</span>, <span class="hljs-keyword">in</span> flatpage
<span class="linenos">19</span>	    <span class="hljs-built_in">return</span> render_flatpage(request, f)
<span class="linenos">20</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/utils/decorators.py"</span>, line <span class="hljs-number">99</span>, <span class="hljs-keyword">in</span> _wrapped_view
<span class="linenos">21</span>	    response = view_func(request, *args, **kwargs)
<span class="linenos">22</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/contrib/flatpages/views.py"</span>, line <span class="hljs-number">60</span>, <span class="hljs-keyword">in</span> render_flatpage
<span class="linenos">23</span>	    t = loader.get_template(DEFAULT_TEMPLATE)
<span class="linenos">24</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/template/loader.py"</span>, line <span class="hljs-number">138</span>, <span class="hljs-keyword">in</span> get_template
<span class="linenos">25</span>	    template, origin = find_template(template_name)
<span class="linenos">26</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/venv/lib/python2.7/site-packages/django/template/loader.py"</span>, line <span class="hljs-number">131</span>, <span class="hljs-keyword">in</span> find_template
<span class="linenos">27</span>	    raise TemplateDoesNotExist(name)
<span class="linenos">28</span>	TemplateDoesNotExist: flatpages/default.html
<span class="linenos">29</span>	
<span class="linenos">30</span>	----------------------------------------------------------------------
<span class="linenos">31</span>	Ran <span class="hljs-number">9</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">3.557</span>s
<span class="linenos">32</span>	
<span class="linenos">33</span>	FAILED (errors=<span class="hljs-number">1</span>)
<span class="linenos">34</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>Our test still fails, but we can easily see  why - the template <code>flatpages/default.html</code> doesn’t exist. So we create it:</p>
<pre><code class="hljs lang-django"><span class="linenos">1</span>	<span class="xml"></span><span class="hljs-template_tag">{% <span class="hljs-keyword">extends</span> "blogengine/includes/base.html" %}</span><span class="xml">
<span class="linenos">2</span>	
<span class="linenos">3</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">load</span> custom_markdown %}</span><span class="xml">
<span class="linenos">4</span>	
<span class="linenos">5</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">block</span> content %}</span><span class="xml">
<span class="linenos">6</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"post"</span>&gt;</span>
<span class="linenos">7</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">h1</span>&gt;</span></span><span class="hljs-variable">{{ flatpage.title }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">h1</span>&gt;</span>
<span class="linenos">8</span>	        </span><span class="hljs-variable">{{ flatpage.content<span class="hljs-filter">|custom</span>_markdown }}</span><span class="xml">
<span class="linenos">9</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">10</span>	
<span class="linenos">11</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endblock</span> %}</span><span class="xml"></span></code></pre>
<p>This template is based on the blog post one, and just changes a handful of variable names. Note that it can still inherit from the blogengine base template, and in this case we’re using that for the sake of consistency.</p>
<p>If you run your tests, you should now see that they pass, so we’ll commit our changes:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add templates/ django_tutorial_blog_ng/ blogengine/
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Implemented flat page support'</span></code></pre>
<h2 id="multiple-authors">Multiple authors</h2>
<p>Next we’ll add support for multiple authors. Now, Django already has a User model, and we’ll leverage that to represent the authors. But first we’ll write our test:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-keyword">from</span> django.test <span class="hljs-keyword">import</span> TestCase, LiveServerTestCase, Client
<span class="linenos">2</span>	<span class="hljs-keyword">from</span> django.utils <span class="hljs-keyword">import</span> timezone
<span class="linenos">3</span>	<span class="hljs-keyword">from</span> blogengine.models <span class="hljs-keyword">import</span> Post
<span class="linenos">4</span>	<span class="hljs-keyword">from</span> django.contrib.flatpages.models <span class="hljs-keyword">import</span> FlatPage
<span class="linenos">5</span>	<span class="hljs-keyword">from</span> django.contrib.sites.models <span class="hljs-keyword">import</span> Site
<span class="linenos">6</span>	<span class="hljs-keyword">from</span> django.contrib.auth.models <span class="hljs-keyword">import</span> User
<span class="linenos">7</span>	<span class="hljs-keyword">import</span> markdown
<span class="linenos">8</span>	
<span class="linenos">9</span>	<span class="hljs-comment"># Create your tests here.</span>
<span class="linenos">10</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">PostTest</span><span class="hljs-params">(TestCase)</span>:</span>
<span class="linenos">11</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_create_post</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">12</span>	        <span class="hljs-comment"># Create the author</span>
<span class="linenos">13</span>	        author = User.objects.create_user(<span class="hljs-string">'testuser'</span>, <span class="hljs-string">'user@example.com'</span>, <span class="hljs-string">'password'</span>)
<span class="linenos">14</span>	        author.save()
<span class="linenos">15</span>	
<span class="linenos">16</span>	        <span class="hljs-comment"># Create the post</span>
<span class="linenos">17</span>	        post = Post()
<span class="linenos">18</span>	
<span class="linenos">19</span>	        <span class="hljs-comment"># Set the attributes</span>
<span class="linenos">20</span>	        post.title = <span class="hljs-string">'My first post'</span>
<span class="linenos">21</span>	        post.text = <span class="hljs-string">'This is my first blog post'</span>
<span class="linenos">22</span>	        post.slug = <span class="hljs-string">'my-first-post'</span>
<span class="linenos">23</span>	        post.pub_date = timezone.now()
<span class="linenos">24</span>	        post.author = author
<span class="linenos">25</span>	
<span class="linenos">26</span>	        <span class="hljs-comment"># Save it</span>
<span class="linenos">27</span>	        post.save()
<span class="linenos">28</span>	
<span class="linenos">29</span>	        <span class="hljs-comment"># Check we can find it</span>
<span class="linenos">30</span>	        all_posts = Post.objects.all()
<span class="linenos">31</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">32</span>	        only_post = all_posts[<span class="hljs-number">0</span>]
<span class="linenos">33</span>	        self.assertEquals(only_post, post)
<span class="linenos">34</span>	
<span class="linenos">35</span>	        <span class="hljs-comment"># Check attributes</span>
<span class="linenos">36</span>	        self.assertEquals(only_post.title, <span class="hljs-string">'My first post'</span>)
<span class="linenos">37</span>	        self.assertEquals(only_post.text, <span class="hljs-string">'This is my first blog post'</span>)
<span class="linenos">38</span>	        self.assertEquals(only_post.slug, <span class="hljs-string">'my-first-post'</span>)
<span class="linenos">39</span>	        self.assertEquals(only_post.pub_date.day, post.pub_date.day)
<span class="linenos">40</span>	        self.assertEquals(only_post.pub_date.month, post.pub_date.month)
<span class="linenos">41</span>	        self.assertEquals(only_post.pub_date.year, post.pub_date.year)
<span class="linenos">42</span>	        self.assertEquals(only_post.pub_date.hour, post.pub_date.hour)
<span class="linenos">43</span>	        self.assertEquals(only_post.pub_date.minute, post.pub_date.minute)
<span class="linenos">44</span>	        self.assertEquals(only_post.pub_date.second, post.pub_date.second)
<span class="linenos">45</span>	        self.assertEquals(only_post.author.username, <span class="hljs-string">'testuser'</span>)
<span class="linenos">46</span>	        self.assertEquals(only_post.author.email, <span class="hljs-string">'user@example.com'</span>)
<span class="linenos">47</span>	
<span class="linenos">48</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">BaseAcceptanceTest</span><span class="hljs-params">(LiveServerTestCase)</span>:</span>
<span class="linenos">49</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">setUp</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">50</span>	        self.client = Client()
<span class="linenos">51</span>	
<span class="linenos">52</span>	
<span class="linenos">53</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">AdminTest</span><span class="hljs-params">(BaseAcceptanceTest)</span>:</span>
<span class="linenos">54</span>	    fixtures = [<span class="hljs-string">'users.json'</span>]
<span class="linenos">55</span>	
<span class="linenos">56</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_login</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">57</span>	        <span class="hljs-comment"># Get login page</span>
<span class="linenos">58</span>	        response = self.client.get(<span class="hljs-string">'/admin/'</span>)
<span class="linenos">59</span>	
<span class="linenos">60</span>	        <span class="hljs-comment"># Check response code</span>
<span class="linenos">61</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">62</span>	
<span class="linenos">63</span>	        <span class="hljs-comment"># Check 'Log in' in response</span>
<span class="linenos">64</span>	        self.assertTrue(<span class="hljs-string">'Log in'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">65</span>	
<span class="linenos">66</span>	        <span class="hljs-comment"># Log the user in</span>
<span class="linenos">67</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">68</span>	
<span class="linenos">69</span>	        <span class="hljs-comment"># Check response code</span>
<span class="linenos">70</span>	        response = self.client.get(<span class="hljs-string">'/admin/'</span>)
<span class="linenos">71</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">72</span>	
<span class="linenos">73</span>	        <span class="hljs-comment"># Check 'Log out' in response</span>
<span class="linenos">74</span>	        self.assertTrue(<span class="hljs-string">'Log out'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">75</span>	
<span class="linenos">76</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_logout</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">77</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">78</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">79</span>	
<span class="linenos">80</span>	        <span class="hljs-comment"># Check response code</span>
<span class="linenos">81</span>	        response = self.client.get(<span class="hljs-string">'/admin/'</span>)
<span class="linenos">82</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">83</span>	
<span class="linenos">84</span>	        <span class="hljs-comment"># Check 'Log out' in response</span>
<span class="linenos">85</span>	        self.assertTrue(<span class="hljs-string">'Log out'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">86</span>	
<span class="linenos">87</span>	        <span class="hljs-comment"># Log out</span>
<span class="linenos">88</span>	        self.client.logout()
<span class="linenos">89</span>	
<span class="linenos">90</span>	        <span class="hljs-comment"># Check response code</span>
<span class="linenos">91</span>	        response = self.client.get(<span class="hljs-string">'/admin/'</span>)
<span class="linenos">92</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">93</span>	
<span class="linenos">94</span>	        <span class="hljs-comment"># Check 'Log in' in response</span>
<span class="linenos">95</span>	        self.assertTrue(<span class="hljs-string">'Log in'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">96</span>	
<span class="linenos">97</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_create_post</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">98</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">99</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">100</span>	
<span class="linenos">101</span>	        <span class="hljs-comment"># Check response code</span>
<span class="linenos">102</span>	        response = self.client.get(<span class="hljs-string">'/admin/blogengine/post/add/'</span>)
<span class="linenos">103</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">104</span>	
<span class="linenos">105</span>	        <span class="hljs-comment"># Create the new post</span>
<span class="linenos">106</span>	        response = self.client.post(<span class="hljs-string">'/admin/blogengine/post/add/'</span>, {
<span class="linenos">107</span>	            <span class="hljs-string">'title'</span>: <span class="hljs-string">'My first post'</span>,
<span class="linenos">108</span>	            <span class="hljs-string">'text'</span>: <span class="hljs-string">'This is my first post'</span>,
<span class="linenos">109</span>	            <span class="hljs-string">'pub_date_0'</span>: <span class="hljs-string">'2013-12-28'</span>,
<span class="linenos">110</span>	            <span class="hljs-string">'pub_date_1'</span>: <span class="hljs-string">'22:00:04'</span>,
<span class="linenos">111</span>	            <span class="hljs-string">'slug'</span>: <span class="hljs-string">'my-first-post'</span>
<span class="linenos">112</span>	        },
<span class="linenos">113</span>	        follow=<span class="hljs-keyword">True</span>
<span class="linenos">114</span>	        )
<span class="linenos">115</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">116</span>	
<span class="linenos">117</span>	        <span class="hljs-comment"># Check added successfully</span>
<span class="linenos">118</span>	        self.assertTrue(<span class="hljs-string">'added successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">119</span>	
<span class="linenos">120</span>	        <span class="hljs-comment"># Check new post now in database</span>
<span class="linenos">121</span>	        all_posts = Post.objects.all()
<span class="linenos">122</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">123</span>	
<span class="linenos">124</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_edit_post</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">125</span>	        <span class="hljs-comment"># Create the author</span>
<span class="linenos">126</span>	        author = User.objects.create_user(<span class="hljs-string">'testuser'</span>, <span class="hljs-string">'user@example.com'</span>, <span class="hljs-string">'password'</span>)
<span class="linenos">127</span>	        author.save()
<span class="linenos">128</span>	
<span class="linenos">129</span>	        <span class="hljs-comment"># Create the post</span>
<span class="linenos">130</span>	        post = Post()
<span class="linenos">131</span>	        post.title = <span class="hljs-string">'My first post'</span>
<span class="linenos">132</span>	        post.text = <span class="hljs-string">'This is my first blog post'</span>
<span class="linenos">133</span>	        post.slug = <span class="hljs-string">'my-first-post'</span>
<span class="linenos">134</span>	        post.pub_date = timezone.now()
<span class="linenos">135</span>	        post.author = author
<span class="linenos">136</span>	        post.save()
<span class="linenos">137</span>	
<span class="linenos">138</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">139</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">140</span>	
<span class="linenos">141</span>	        <span class="hljs-comment"># Edit the post</span>
<span class="linenos">142</span>	        response = self.client.post(<span class="hljs-string">'/admin/blogengine/post/1/'</span>, {
<span class="linenos">143</span>	            <span class="hljs-string">'title'</span>: <span class="hljs-string">'My second post'</span>,
<span class="linenos">144</span>	            <span class="hljs-string">'text'</span>: <span class="hljs-string">'This is my second blog post'</span>,
<span class="linenos">145</span>	            <span class="hljs-string">'pub_date_0'</span>: <span class="hljs-string">'2013-12-28'</span>,
<span class="linenos">146</span>	            <span class="hljs-string">'pub_date_1'</span>: <span class="hljs-string">'22:00:04'</span>,
<span class="linenos">147</span>	            <span class="hljs-string">'slug'</span>: <span class="hljs-string">'my-second-post'</span>
<span class="linenos">148</span>	        },
<span class="linenos">149</span>	        follow=<span class="hljs-keyword">True</span>
<span class="linenos">150</span>	        )
<span class="linenos">151</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">152</span>	
<span class="linenos">153</span>	        <span class="hljs-comment"># Check changed successfully</span>
<span class="linenos">154</span>	        self.assertTrue(<span class="hljs-string">'changed successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">155</span>	
<span class="linenos">156</span>	        <span class="hljs-comment"># Check post amended</span>
<span class="linenos">157</span>	        all_posts = Post.objects.all()
<span class="linenos">158</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">159</span>	        only_post = all_posts[<span class="hljs-number">0</span>]
<span class="linenos">160</span>	        self.assertEquals(only_post.title, <span class="hljs-string">'My second post'</span>)
<span class="linenos">161</span>	        self.assertEquals(only_post.text, <span class="hljs-string">'This is my second blog post'</span>)
<span class="linenos">162</span>	
<span class="linenos">163</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_delete_post</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">164</span>	        <span class="hljs-comment"># Create the author</span>
<span class="linenos">165</span>	        author = User.objects.create_user(<span class="hljs-string">'testuser'</span>, <span class="hljs-string">'user@example.com'</span>, <span class="hljs-string">'password'</span>)
<span class="linenos">166</span>	        author.save()
<span class="linenos">167</span>	
<span class="linenos">168</span>	        <span class="hljs-comment"># Create the post</span>
<span class="linenos">169</span>	        post = Post()
<span class="linenos">170</span>	        post.title = <span class="hljs-string">'My first post'</span>
<span class="linenos">171</span>	        post.text = <span class="hljs-string">'This is my first blog post'</span>
<span class="linenos">172</span>	        post.slug = <span class="hljs-string">'my-first-post'</span>
<span class="linenos">173</span>	        post.pub_date = timezone.now()
<span class="linenos">174</span>	        post.author = author
<span class="linenos">175</span>	        post.save()
<span class="linenos">176</span>	
<span class="linenos">177</span>	        <span class="hljs-comment"># Check new post saved</span>
<span class="linenos">178</span>	        all_posts = Post.objects.all()
<span class="linenos">179</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">180</span>	
<span class="linenos">181</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">182</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">183</span>	
<span class="linenos">184</span>	        <span class="hljs-comment"># Delete the post</span>
<span class="linenos">185</span>	        response = self.client.post(<span class="hljs-string">'/admin/blogengine/post/1/delete/'</span>, {
<span class="linenos">186</span>	            <span class="hljs-string">'post'</span>: <span class="hljs-string">'yes'</span>
<span class="linenos">187</span>	        }, follow=<span class="hljs-keyword">True</span>)
<span class="linenos">188</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">189</span>	
<span class="linenos">190</span>	        <span class="hljs-comment"># Check deleted successfully</span>
<span class="linenos">191</span>	        self.assertTrue(<span class="hljs-string">'deleted successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">192</span>	
<span class="linenos">193</span>	        <span class="hljs-comment"># Check post amended</span>
<span class="linenos">194</span>	        all_posts = Post.objects.all()
<span class="linenos">195</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">0</span>)
<span class="linenos">196</span>	
<span class="linenos">197</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">PostViewTest</span><span class="hljs-params">(BaseAcceptanceTest)</span>:</span>
<span class="linenos">198</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_index</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">199</span>	        <span class="hljs-comment"># Create the author</span>
<span class="linenos">200</span>	        author = User.objects.create_user(<span class="hljs-string">'testuser'</span>, <span class="hljs-string">'user@example.com'</span>, <span class="hljs-string">'password'</span>)
<span class="linenos">201</span>	        author.save()
<span class="linenos">202</span>	
<span class="linenos">203</span>	        <span class="hljs-comment"># Create the post</span>
<span class="linenos">204</span>	        post = Post()
<span class="linenos">205</span>	        post.title = <span class="hljs-string">'My first post'</span>
<span class="linenos">206</span>	        post.text = <span class="hljs-string">'This is [my first blog post](http://127.0.0.1:8000/)'</span>
<span class="linenos">207</span>	        post.slug = <span class="hljs-string">'my-first-post'</span>
<span class="linenos">208</span>	        post.pub_date = timezone.now()
<span class="linenos">209</span>	        post.author = author
<span class="linenos">210</span>	        post.save()
<span class="linenos">211</span>	
<span class="linenos">212</span>	        <span class="hljs-comment"># Check new post saved</span>
<span class="linenos">213</span>	        all_posts = Post.objects.all()
<span class="linenos">214</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">215</span>	
<span class="linenos">216</span>	        <span class="hljs-comment"># Fetch the index</span>
<span class="linenos">217</span>	        response = self.client.get(<span class="hljs-string">'/'</span>)
<span class="linenos">218</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">219</span>	
<span class="linenos">220</span>	        <span class="hljs-comment"># Check the post title is in the response</span>
<span class="linenos">221</span>	        self.assertTrue(post.title <span class="hljs-keyword">in</span> response.content)
<span class="linenos">222</span>	
<span class="linenos">223</span>	        <span class="hljs-comment"># Check the post text is in the response</span>
<span class="linenos">224</span>	        self.assertTrue(markdown.markdown(post.text) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">225</span>	
<span class="linenos">226</span>	        <span class="hljs-comment"># Check the post date is in the response</span>
<span class="linenos">227</span>	        self.assertTrue(str(post.pub_date.year) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">228</span>	        self.assertTrue(post.pub_date.strftime(<span class="hljs-string">'%b'</span>) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">229</span>	        self.assertTrue(str(post.pub_date.day) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">230</span>	
<span class="linenos">231</span>	        <span class="hljs-comment"># Check the link is marked up properly</span>
<span class="linenos">232</span>	        self.assertTrue(<span class="hljs-string">'&lt;a href="http://127.0.0.1:8000/"&gt;my first blog post&lt;/a&gt;'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">233</span>	
<span class="linenos">234</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_post_page</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">235</span>	        <span class="hljs-comment"># Create the author</span>
<span class="linenos">236</span>	        author = User.objects.create_user(<span class="hljs-string">'testuser'</span>, <span class="hljs-string">'user@example.com'</span>, <span class="hljs-string">'password'</span>)
<span class="linenos">237</span>	        author.save()
<span class="linenos">238</span>	
<span class="linenos">239</span>	        <span class="hljs-comment"># Create the post</span>
<span class="linenos">240</span>	        post = Post()
<span class="linenos">241</span>	        post.title = <span class="hljs-string">'My first post'</span>
<span class="linenos">242</span>	        post.text = <span class="hljs-string">'This is [my first blog post](http://127.0.0.1:8000/)'</span>
<span class="linenos">243</span>	        post.slug = <span class="hljs-string">'my-first-post'</span>
<span class="linenos">244</span>	        post.pub_date = timezone.now()
<span class="linenos">245</span>	        post.author = author
<span class="linenos">246</span>	        post.save()
<span class="linenos">247</span>	
<span class="linenos">248</span>	        <span class="hljs-comment"># Check new post saved</span>
<span class="linenos">249</span>	        all_posts = Post.objects.all()
<span class="linenos">250</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">251</span>	        only_post = all_posts[<span class="hljs-number">0</span>]
<span class="linenos">252</span>	        self.assertEquals(only_post, post)
<span class="linenos">253</span>	
<span class="linenos">254</span>	        <span class="hljs-comment"># Get the post URL</span>
<span class="linenos">255</span>	        post_url = only_post.get_absolute_url()
<span class="linenos">256</span>	
<span class="linenos">257</span>	        <span class="hljs-comment"># Fetch the post</span>
<span class="linenos">258</span>	        response = self.client.get(post_url)
<span class="linenos">259</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">260</span>	
<span class="linenos">261</span>	        <span class="hljs-comment"># Check the post title is in the response</span>
<span class="linenos">262</span>	        self.assertTrue(post.title <span class="hljs-keyword">in</span> response.content)
<span class="linenos">263</span>	
<span class="linenos">264</span>	        <span class="hljs-comment"># Check the post text is in the response</span>
<span class="linenos">265</span>	        self.assertTrue(markdown.markdown(post.text) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">266</span>	
<span class="linenos">267</span>	        <span class="hljs-comment"># Check the post date is in the response</span>
<span class="linenos">268</span>	        self.assertTrue(str(post.pub_date.year) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">269</span>	        self.assertTrue(post.pub_date.strftime(<span class="hljs-string">'%b'</span>) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">270</span>	        self.assertTrue(str(post.pub_date.day) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">271</span>	
<span class="linenos">272</span>	        <span class="hljs-comment"># Check the link is marked up properly</span>
<span class="linenos">273</span>	        self.assertTrue(<span class="hljs-string">'&lt;a href="http://127.0.0.1:8000/"&gt;my first blog post&lt;/a&gt;'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">274</span>	
<span class="linenos">275</span>	
<span class="linenos">276</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">FlatPageViewTest</span><span class="hljs-params">(BaseAcceptanceTest)</span>:</span>
<span class="linenos">277</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_create_flat_page</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">278</span>	        <span class="hljs-comment"># Create flat page</span>
<span class="linenos">279</span>	        page = FlatPage()
<span class="linenos">280</span>	        page.url = <span class="hljs-string">'/about/'</span>
<span class="linenos">281</span>	        page.title = <span class="hljs-string">'About me'</span>
<span class="linenos">282</span>	        page.content = <span class="hljs-string">'All about me'</span>
<span class="linenos">283</span>	        page.save()
<span class="linenos">284</span>	
<span class="linenos">285</span>	        <span class="hljs-comment"># Add the site</span>
<span class="linenos">286</span>	        page.sites.add(Site.objects.all()[<span class="hljs-number">0</span>])
<span class="linenos">287</span>	        page.save()
<span class="linenos">288</span>	
<span class="linenos">289</span>	        <span class="hljs-comment"># Check new page saved</span>
<span class="linenos">290</span>	        all_pages = FlatPage.objects.all()
<span class="linenos">291</span>	        self.assertEquals(len(all_pages), <span class="hljs-number">1</span>)
<span class="linenos">292</span>	        only_page = all_pages[<span class="hljs-number">0</span>]
<span class="linenos">293</span>	        self.assertEquals(only_page, page)
<span class="linenos">294</span>	
<span class="linenos">295</span>	        <span class="hljs-comment"># Check data correct</span>
<span class="linenos">296</span>	        self.assertEquals(only_page.url, <span class="hljs-string">'/about/'</span>)
<span class="linenos">297</span>	        self.assertEquals(only_page.title, <span class="hljs-string">'About me'</span>)
<span class="linenos">298</span>	        self.assertEquals(only_page.content, <span class="hljs-string">'All about me'</span>)
<span class="linenos">299</span>	
<span class="linenos">300</span>	        <span class="hljs-comment"># Get URL</span>
<span class="linenos">301</span>	        page_url = str(only_page.get_absolute_url())
<span class="linenos">302</span>	
<span class="linenos">303</span>	        <span class="hljs-comment"># Get the page</span>
<span class="linenos">304</span>	        response = self.client.get(page_url)
<span class="linenos">305</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">306</span>	
<span class="linenos">307</span>	        <span class="hljs-comment"># Check title and content in response</span>
<span class="linenos">308</span>	        self.assertTrue(<span class="hljs-string">'About me'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">309</span>	        self.assertTrue(<span class="hljs-string">'All about me'</span> <span class="hljs-keyword">in</span> response.content)</code></pre>
<p>Here we create a <code>User</code> object to represent the author. Note the <code>create_user</code> convenience method for creating new users quickly and easily.</p>
<p>We’re going to exclude the author field from the admin - instead it’s going to be automatically populated based on the session data, so that when a user creates a post they are automatically set as the author. We therefore don’t need to make any changes for the acceptance tests for posts - our changes to the unit tests for the <code>Post</code> model are sufficient.</p>
<p>Run the tests, and they should fail:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	(venv)Smith:django_tutorial_blog_ng matthewdaly$ python manage.py test
<span class="linenos">2</span>	Creating test database <span class="hljs-keyword">for</span> alias <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	E........
<span class="linenos">4</span>	======================================================================
<span class="linenos">5</span>	ERROR: test_create_post (blogengine.tests.PostTest)
<span class="linenos">6</span>	----------------------------------------------------------------------
<span class="linenos">7</span>	Traceback (most recent call last):
<span class="linenos">8</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">45</span>, <span class="hljs-keyword">in</span> test_create_post
<span class="linenos">9</span>	    self.assertEquals(only_post.author.username, <span class="hljs-string">'testuser'</span>)
<span class="linenos">10</span>	AttributeError: <span class="hljs-string">'Post'</span> object has no attribute <span class="hljs-string">'author'</span>
<span class="linenos">11</span>	
<span class="linenos">12</span>	----------------------------------------------------------------------
<span class="linenos">13</span>	Ran <span class="hljs-number">9</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">3.620</span>s
<span class="linenos">14</span>	
<span class="linenos">15</span>	FAILED (errors=<span class="hljs-number">1</span>)
<span class="linenos">16</span>	Destroying test database <span class="hljs-keyword">for</span> alias <span class="hljs-string">'default'</span>...</code></pre>
<p>Let’s add the missing <code>author</code> attribute:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-keyword">from</span> django.db <span class="hljs-keyword">import</span> models
<span class="linenos">2</span>	<span class="hljs-keyword">from</span> django.contrib.auth.models <span class="hljs-keyword">import</span> User
<span class="linenos">3</span>	
<span class="linenos">4</span>	<span class="hljs-comment"># Create your models here.</span>
<span class="linenos">5</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Post</span><span class="hljs-params">(models.Model)</span>:</span>
<span class="linenos">6</span>	    title = models.CharField(max_length=<span class="hljs-number">200</span>)
<span class="linenos">7</span>	    pub_date = models.DateTimeField()
<span class="linenos">8</span>	    text = models.TextField()
<span class="linenos">9</span>	    slug = models.SlugField(max_length=<span class="hljs-number">40</span>, unique=<span class="hljs-keyword">True</span>)
<span class="linenos">10</span>	    author = models.ForeignKey(User)
<span class="linenos">11</span>	
<span class="linenos">12</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">get_absolute_url</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">13</span>	        <span class="hljs-keyword">return</span> <span class="hljs-string">"/%s/%s/%s/"</span> % (self.pub_date.year, self.pub_date.month, self.slug)
<span class="linenos">14</span>	
<span class="linenos">15</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">__unicode__</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">16</span>	        <span class="hljs-keyword">return</span> self.title
<span class="linenos">17</span>	
<span class="linenos">18</span>	    <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Meta</span>:</span>
<span class="linenos">19</span>	        ordering = [<span class="hljs-string">"-pub_date"</span>]</code></pre>
<p>Next, create the migrations:</p>
<pre><code class="hljs lang-bash singleline">$ python manage.py schemamigration --auto blogengine</code></pre>
<p>You’ll be prompted to either quit or provide a default author ID - select option 2 to provide the ID, then enter 1, which should be your own user account ID. Then run the migrations:</p>
<pre><code class="hljs lang-bash singleline">$ python manage.py migrate</code></pre>
<p>Let’s run our tests again:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	(venv)Smith:django_tutorial_blog_ng matthewdaly$ python manage.py <span class="hljs-built_in">test</span>
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	.F.F.....
<span class="linenos">4</span>	======================================================================
<span class="linenos">5</span>	FAIL: <span class="hljs-built_in">test</span>_create_post (blogengine.tests.AdminTest)
<span class="linenos">6</span>	----------------------------------------------------------------------
<span class="linenos">7</span>	Traceback (most recent call last):
<span class="linenos">8</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">118</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_create_post
<span class="linenos">9</span>	    self.assertTrue(<span class="hljs-string">'added successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">10</span>	AssertionError: False is not <span class="hljs-literal">true</span>
<span class="linenos">11</span>	
<span class="linenos">12</span>	======================================================================
<span class="linenos">13</span>	FAIL: <span class="hljs-built_in">test</span>_edit_post (blogengine.tests.AdminTest)
<span class="linenos">14</span>	----------------------------------------------------------------------
<span class="linenos">15</span>	Traceback (most recent call last):
<span class="linenos">16</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">154</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_edit_post
<span class="linenos">17</span>	    self.assertTrue(<span class="hljs-string">'changed successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">18</span>	AssertionError: False is not <span class="hljs-literal">true</span>
<span class="linenos">19</span>	
<span class="linenos">20</span>	----------------------------------------------------------------------
<span class="linenos">21</span>	Ran <span class="hljs-number">9</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">3.390</span>s
<span class="linenos">22</span>	
<span class="linenos">23</span>	FAILED (failures=<span class="hljs-number">2</span>)
<span class="linenos">24</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>Our test still fails because the author field isn’t set automatically. So we’ll amend the admin to automatically set the author when the <code>Post</code> object is saved:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-keyword">import</span> models
<span class="linenos">2</span>	<span class="hljs-keyword">from</span> django.contrib <span class="hljs-keyword">import</span> admin
<span class="linenos">3</span>	<span class="hljs-keyword">from</span> django.contrib.auth.models <span class="hljs-keyword">import</span> User
<span class="linenos">4</span>	
<span class="linenos">5</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">PostAdmin</span><span class="hljs-params">(admin.ModelAdmin)</span>:</span>
<span class="linenos">6</span>	    prepopulated_fields = {<span class="hljs-string">"slug"</span>: (<span class="hljs-string">"title"</span>,)}
<span class="linenos">7</span>	    exclude = (<span class="hljs-string">'author'</span>,)
<span class="linenos">8</span>	
<span class="linenos">9</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">save_model</span><span class="hljs-params">(self, request, obj, form, change)</span>:</span>
<span class="linenos">10</span>	        obj.author = request.user
<span class="linenos">11</span>	        obj.save()
<span class="linenos">12</span>	
<span class="linenos">13</span>	admin.site.register(models.Post, PostAdmin)</code></pre>
<p>This tells the admin to exclude the <code>author</code> field from any form for a post, and when the model is saved, to set the author to the user making the HTTP request. Now run the tests, and they should pass:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	(venv)Smith:django_tutorial_blog_ng matthewdaly$ python manage.py <span class="hljs-built_in">test</span>
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	.........
<span class="linenos">4</span>	----------------------------------------------------------------------
<span class="linenos">5</span>	Ran <span class="hljs-number">9</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">4.086</span>s
<span class="linenos">6</span>	
<span class="linenos">7</span>	OK
<span class="linenos">8</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>Time to commit again:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add blogengine/
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Added author field'</span></code></pre>
<h2 id="comments">Comments</h2>
<p>The <a href="http://matthewdaly.co.uk/blog/2012/03/29/yet-another-tutorial-for-building-a-blog-using-python-and-django-part-4/">previous version of this tutorial</a> implemented comments using Django’s own comment system. However, this has since been deprecated from Django and turned into <a href="https://github.com/django/django-contrib-comments">a separate project</a>. So we have two options for how to implement comments:</p>
<ul>
<li>We can use <a href="http://django-contrib-comments.readthedocs.org/en/latest/">the comments system</a></li>
<li>We can use a third-party comments system</li>
</ul>
<p>Now, if you want to use the Django comment system, you can do so, and it shouldn’t be too hard to puzzle out how to implement it using the documentation and my prior post. However, in my humble opinion, using a third-party comment system is the way to go for blog comments - they make it extremely easy for people to log in with multiple services without you having to write lots of additional code. They also make it significantly easier to moderate comments, and they’re generally pretty good at handling comment spam.</p>
<p>Some of the available providers include:</p>
<ul>
<li><a href="http://disqus.com/">Disqus</a></li>
<li><a href="http://intensedebate.com/">IntenseDebate</a></li>
<li><a href="https://developers.facebook.com/docs/plugins/comments/">Facebook</a></li>
</ul>
<p>For demonstration purposes, we’ll use Facebook comments, but this shouldn’t require much work to adapt it to the other providers.</p>
<p>First of all, we need to include the Facebook JavaScript SDK:</p>
<pre><code class="hljs lang-django"><span class="linenos">1</span>	<span class="xml">        <span class="hljs-comment">&lt;!-- Add your site or application content here --&gt;</span>
<span class="linenos">2</span>	
<span class="linenos">3</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">id</span>=<span class="hljs-value">"fb-root"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">4</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">script</span>&gt;</span><span class="clojure"><span class="hljs-list">(<span class="hljs-keyword">function</span><span class="hljs-list">(<span class="hljs-keyword">d</span>, s, id)</span> <span class="hljs-collection">{
<span class="linenos">5</span>	            var js, fjs = d.getElementsByTagName<span class="hljs-list">(<span class="hljs-keyword">s</span>)</span><span class="hljs-collection">[<span class="hljs-number">0</span>]</span><span class="hljs-comment">;</span>
<span class="linenos">6</span>	            if <span class="hljs-list">(<span class="hljs-keyword">d.getElementById</span><span class="hljs-list">(<span class="hljs-keyword">id</span>)</span>)</span> return;
<span class="linenos">7</span>	                js = d.createElement<span class="hljs-list">(<span class="hljs-keyword">s</span>)</span><span class="hljs-comment">; js.id = id;</span>
<span class="linenos">8</span>	                js.src = <span class="hljs-string">"//connect.facebook.net/en_GB/all.js#xfbml=1"</span><span class="hljs-comment">;</span>
<span class="linenos">9</span>	                fjs.parentNode.insertBefore<span class="hljs-list">(<span class="hljs-keyword">js</span>, fjs)</span><span class="hljs-comment">;</span>
<span class="linenos">10</span>	            }</span><span class="hljs-list">(<span class="hljs-keyword">document</span>, 'script', 'facebook-jssdk')</span>)</span><span class="hljs-comment">;</span></span><span class="hljs-tag">&lt;/<span class="hljs-title">script</span>&gt;</span>
<span class="linenos">11</span>	
<span class="linenos">12</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"navbar navbar-static-top navbar-inverse"</span>&gt;</span>
<span class="linenos">13</span>	            <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"navbar-inner"</span>&gt;</span>
<span class="linenos">14</span>	                <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"container"</span>&gt;</span>
<span class="linenos">15</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"btn btn-navbar"</span> <span class="hljs-attribute">data-toggle</span>=<span class="hljs-value">"collapse"</span> <span class="hljs-attribute">data-target</span>=<span class="hljs-value">".nav-collapse"</span>&gt;</span>
<span class="linenos">16</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"icon-bar"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span>
<span class="linenos">17</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"icon-bar"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span>
<span class="linenos">18</span>	                        <span class="hljs-tag">&lt;<span class="hljs-title">span</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"icon-bar"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">span</span>&gt;</span>
<span class="linenos">19</span>	                    <span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">20</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">a</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"brand"</span> <span class="hljs-attribute">href</span>=<span class="hljs-value">"/"</span>&gt;</span>My Django Blog<span class="hljs-tag">&lt;/<span class="hljs-title">a</span>&gt;</span>
<span class="linenos">21</span>	                    <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"nav-collapse collapse"</span>&gt;</span>
<span class="linenos">22</span>	                    <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">23</span>	                <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">24</span>	            <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">25</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span></span></code></pre>
<p>Now, the Facebook comment system requires that you pass through the absolute page URL when initialising the comments. At present we can’t do that without hard-coding the domain name in our template, which we want to avoid. So, we need to add a site field to each post to identify the site it’s associated with.</p>
<p>As usual, we update our tests first:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-keyword">from</span> django.test <span class="hljs-keyword">import</span> TestCase, LiveServerTestCase, Client
<span class="linenos">2</span>	<span class="hljs-keyword">from</span> django.utils <span class="hljs-keyword">import</span> timezone
<span class="linenos">3</span>	<span class="hljs-keyword">from</span> blogengine.models <span class="hljs-keyword">import</span> Post
<span class="linenos">4</span>	<span class="hljs-keyword">from</span> django.contrib.flatpages.models <span class="hljs-keyword">import</span> FlatPage
<span class="linenos">5</span>	<span class="hljs-keyword">from</span> django.contrib.sites.models <span class="hljs-keyword">import</span> Site
<span class="linenos">6</span>	<span class="hljs-keyword">from</span> django.contrib.auth.models <span class="hljs-keyword">import</span> User
<span class="linenos">7</span>	<span class="hljs-keyword">import</span> markdown
<span class="linenos">8</span>	
<span class="linenos">9</span>	<span class="hljs-comment"># Create your tests here.</span>
<span class="linenos">10</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">PostTest</span><span class="hljs-params">(TestCase)</span>:</span>
<span class="linenos">11</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_create_post</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">12</span>	        <span class="hljs-comment"># Create the author</span>
<span class="linenos">13</span>	        author = User.objects.create_user(<span class="hljs-string">'testuser'</span>, <span class="hljs-string">'user@example.com'</span>, <span class="hljs-string">'password'</span>)
<span class="linenos">14</span>	        author.save()
<span class="linenos">15</span>	
<span class="linenos">16</span>	        <span class="hljs-comment"># Create the site</span>
<span class="linenos">17</span>	        site = Site()
<span class="linenos">18</span>	        site.name = <span class="hljs-string">'example.com'</span>
<span class="linenos">19</span>	        site.domain = <span class="hljs-string">'example.com'</span>
<span class="linenos">20</span>	        site.save()
<span class="linenos">21</span>	
<span class="linenos">22</span>	        <span class="hljs-comment"># Create the post</span>
<span class="linenos">23</span>	        post = Post()
<span class="linenos">24</span>	
<span class="linenos">25</span>	        <span class="hljs-comment"># Set the attributes</span>
<span class="linenos">26</span>	        post.title = <span class="hljs-string">'My first post'</span>
<span class="linenos">27</span>	        post.text = <span class="hljs-string">'This is my first blog post'</span>
<span class="linenos">28</span>	        post.slug = <span class="hljs-string">'my-first-post'</span>
<span class="linenos">29</span>	        post.pub_date = timezone.now()
<span class="linenos">30</span>	        post.author = author
<span class="linenos">31</span>	        post.site = site
<span class="linenos">32</span>	
<span class="linenos">33</span>	        <span class="hljs-comment"># Save it</span>
<span class="linenos">34</span>	        post.save()
<span class="linenos">35</span>	
<span class="linenos">36</span>	        <span class="hljs-comment"># Check we can find it</span>
<span class="linenos">37</span>	        all_posts = Post.objects.all()
<span class="linenos">38</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">39</span>	        only_post = all_posts[<span class="hljs-number">0</span>]
<span class="linenos">40</span>	        self.assertEquals(only_post, post)
<span class="linenos">41</span>	
<span class="linenos">42</span>	        <span class="hljs-comment"># Check attributes</span>
<span class="linenos">43</span>	        self.assertEquals(only_post.title, <span class="hljs-string">'My first post'</span>)
<span class="linenos">44</span>	        self.assertEquals(only_post.text, <span class="hljs-string">'This is my first blog post'</span>)
<span class="linenos">45</span>	        self.assertEquals(only_post.slug, <span class="hljs-string">'my-first-post'</span>)
<span class="linenos">46</span>	        self.assertEquals(only_post.site.name, <span class="hljs-string">'example.com'</span>)
<span class="linenos">47</span>	        self.assertEquals(only_post.site.domain, <span class="hljs-string">'example.com'</span>)
<span class="linenos">48</span>	        self.assertEquals(only_post.pub_date.day, post.pub_date.day)
<span class="linenos">49</span>	        self.assertEquals(only_post.pub_date.month, post.pub_date.month)
<span class="linenos">50</span>	        self.assertEquals(only_post.pub_date.year, post.pub_date.year)
<span class="linenos">51</span>	        self.assertEquals(only_post.pub_date.hour, post.pub_date.hour)
<span class="linenos">52</span>	        self.assertEquals(only_post.pub_date.minute, post.pub_date.minute)
<span class="linenos">53</span>	        self.assertEquals(only_post.pub_date.second, post.pub_date.second)
<span class="linenos">54</span>	        self.assertEquals(only_post.author.username, <span class="hljs-string">'testuser'</span>)
<span class="linenos">55</span>	        self.assertEquals(only_post.author.email, <span class="hljs-string">'user@example.com'</span>)
<span class="linenos">56</span>	
<span class="linenos">57</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">BaseAcceptanceTest</span><span class="hljs-params">(LiveServerTestCase)</span>:</span>
<span class="linenos">58</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">setUp</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">59</span>	        self.client = Client()
<span class="linenos">60</span>	
<span class="linenos">61</span>	
<span class="linenos">62</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">AdminTest</span><span class="hljs-params">(BaseAcceptanceTest)</span>:</span>
<span class="linenos">63</span>	    fixtures = [<span class="hljs-string">'users.json'</span>]
<span class="linenos">64</span>	
<span class="linenos">65</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_login</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">66</span>	        <span class="hljs-comment"># Get login page</span>
<span class="linenos">67</span>	        response = self.client.get(<span class="hljs-string">'/admin/'</span>)
<span class="linenos">68</span>	
<span class="linenos">69</span>	        <span class="hljs-comment"># Check response code</span>
<span class="linenos">70</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">71</span>	
<span class="linenos">72</span>	        <span class="hljs-comment"># Check 'Log in' in response</span>
<span class="linenos">73</span>	        self.assertTrue(<span class="hljs-string">'Log in'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">74</span>	
<span class="linenos">75</span>	        <span class="hljs-comment"># Log the user in</span>
<span class="linenos">76</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">77</span>	
<span class="linenos">78</span>	        <span class="hljs-comment"># Check response code</span>
<span class="linenos">79</span>	        response = self.client.get(<span class="hljs-string">'/admin/'</span>)
<span class="linenos">80</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">81</span>	
<span class="linenos">82</span>	        <span class="hljs-comment"># Check 'Log out' in response</span>
<span class="linenos">83</span>	        self.assertTrue(<span class="hljs-string">'Log out'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">84</span>	
<span class="linenos">85</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_logout</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">86</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">87</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">88</span>	
<span class="linenos">89</span>	        <span class="hljs-comment"># Check response code</span>
<span class="linenos">90</span>	        response = self.client.get(<span class="hljs-string">'/admin/'</span>)
<span class="linenos">91</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">92</span>	
<span class="linenos">93</span>	        <span class="hljs-comment"># Check 'Log out' in response</span>
<span class="linenos">94</span>	        self.assertTrue(<span class="hljs-string">'Log out'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">95</span>	
<span class="linenos">96</span>	        <span class="hljs-comment"># Log out</span>
<span class="linenos">97</span>	        self.client.logout()
<span class="linenos">98</span>	
<span class="linenos">99</span>	        <span class="hljs-comment"># Check response code</span>
<span class="linenos">100</span>	        response = self.client.get(<span class="hljs-string">'/admin/'</span>)
<span class="linenos">101</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">102</span>	
<span class="linenos">103</span>	        <span class="hljs-comment"># Check 'Log in' in response</span>
<span class="linenos">104</span>	        self.assertTrue(<span class="hljs-string">'Log in'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">105</span>	
<span class="linenos">106</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_create_post</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">107</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">108</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">109</span>	
<span class="linenos">110</span>	        <span class="hljs-comment"># Check response code</span>
<span class="linenos">111</span>	        response = self.client.get(<span class="hljs-string">'/admin/blogengine/post/add/'</span>)
<span class="linenos">112</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">113</span>	
<span class="linenos">114</span>	        <span class="hljs-comment"># Create the new post</span>
<span class="linenos">115</span>	        response = self.client.post(<span class="hljs-string">'/admin/blogengine/post/add/'</span>, {
<span class="linenos">116</span>	            <span class="hljs-string">'title'</span>: <span class="hljs-string">'My first post'</span>,
<span class="linenos">117</span>	            <span class="hljs-string">'text'</span>: <span class="hljs-string">'This is my first post'</span>,
<span class="linenos">118</span>	            <span class="hljs-string">'pub_date_0'</span>: <span class="hljs-string">'2013-12-28'</span>,
<span class="linenos">119</span>	            <span class="hljs-string">'pub_date_1'</span>: <span class="hljs-string">'22:00:04'</span>,
<span class="linenos">120</span>	            <span class="hljs-string">'slug'</span>: <span class="hljs-string">'my-first-post'</span>,
<span class="linenos">121</span>	            <span class="hljs-string">'site'</span>: <span class="hljs-string">'1'</span>
<span class="linenos">122</span>	        },
<span class="linenos">123</span>	        follow=<span class="hljs-keyword">True</span>
<span class="linenos">124</span>	        )
<span class="linenos">125</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">126</span>	
<span class="linenos">127</span>	        <span class="hljs-comment"># Check added successfully</span>
<span class="linenos">128</span>	        self.assertTrue(<span class="hljs-string">'added successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">129</span>	
<span class="linenos">130</span>	        <span class="hljs-comment"># Check new post now in database</span>
<span class="linenos">131</span>	        all_posts = Post.objects.all()
<span class="linenos">132</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">133</span>	
<span class="linenos">134</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_edit_post</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">135</span>	        <span class="hljs-comment"># Create the author</span>
<span class="linenos">136</span>	        author = User.objects.create_user(<span class="hljs-string">'testuser'</span>, <span class="hljs-string">'user@example.com'</span>, <span class="hljs-string">'password'</span>)
<span class="linenos">137</span>	        author.save()
<span class="linenos">138</span>	
<span class="linenos">139</span>	        <span class="hljs-comment"># Create the site</span>
<span class="linenos">140</span>	        site = Site()
<span class="linenos">141</span>	        site.name = <span class="hljs-string">'example.com'</span>
<span class="linenos">142</span>	        site.domain = <span class="hljs-string">'example.com'</span>
<span class="linenos">143</span>	        site.save()
<span class="linenos">144</span>	
<span class="linenos">145</span>	        <span class="hljs-comment"># Create the post</span>
<span class="linenos">146</span>	        post = Post()
<span class="linenos">147</span>	        post.title = <span class="hljs-string">'My first post'</span>
<span class="linenos">148</span>	        post.text = <span class="hljs-string">'This is my first blog post'</span>
<span class="linenos">149</span>	        post.slug = <span class="hljs-string">'my-first-post'</span>
<span class="linenos">150</span>	        post.pub_date = timezone.now()
<span class="linenos">151</span>	        post.author = author
<span class="linenos">152</span>	        post.site = site
<span class="linenos">153</span>	        post.save()
<span class="linenos">154</span>	
<span class="linenos">155</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">156</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">157</span>	
<span class="linenos">158</span>	        <span class="hljs-comment"># Edit the post</span>
<span class="linenos">159</span>	        response = self.client.post(<span class="hljs-string">'/admin/blogengine/post/1/'</span>, {
<span class="linenos">160</span>	            <span class="hljs-string">'title'</span>: <span class="hljs-string">'My second post'</span>,
<span class="linenos">161</span>	            <span class="hljs-string">'text'</span>: <span class="hljs-string">'This is my second blog post'</span>,
<span class="linenos">162</span>	            <span class="hljs-string">'pub_date_0'</span>: <span class="hljs-string">'2013-12-28'</span>,
<span class="linenos">163</span>	            <span class="hljs-string">'pub_date_1'</span>: <span class="hljs-string">'22:00:04'</span>,
<span class="linenos">164</span>	            <span class="hljs-string">'slug'</span>: <span class="hljs-string">'my-second-post'</span>,
<span class="linenos">165</span>	            <span class="hljs-string">'site'</span>: <span class="hljs-string">'1'</span>
<span class="linenos">166</span>	        },
<span class="linenos">167</span>	        follow=<span class="hljs-keyword">True</span>
<span class="linenos">168</span>	        )
<span class="linenos">169</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">170</span>	
<span class="linenos">171</span>	        <span class="hljs-comment"># Check changed successfully</span>
<span class="linenos">172</span>	        self.assertTrue(<span class="hljs-string">'changed successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">173</span>	
<span class="linenos">174</span>	        <span class="hljs-comment"># Check post amended</span>
<span class="linenos">175</span>	        all_posts = Post.objects.all()
<span class="linenos">176</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">177</span>	        only_post = all_posts[<span class="hljs-number">0</span>]
<span class="linenos">178</span>	        self.assertEquals(only_post.title, <span class="hljs-string">'My second post'</span>)
<span class="linenos">179</span>	        self.assertEquals(only_post.text, <span class="hljs-string">'This is my second blog post'</span>)
<span class="linenos">180</span>	
<span class="linenos">181</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_delete_post</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">182</span>	        <span class="hljs-comment"># Create the author</span>
<span class="linenos">183</span>	        author = User.objects.create_user(<span class="hljs-string">'testuser'</span>, <span class="hljs-string">'user@example.com'</span>, <span class="hljs-string">'password'</span>)
<span class="linenos">184</span>	        author.save()
<span class="linenos">185</span>	
<span class="linenos">186</span>	        <span class="hljs-comment"># Create the site</span>
<span class="linenos">187</span>	        site = Site()
<span class="linenos">188</span>	        site.name = <span class="hljs-string">'example.com'</span>
<span class="linenos">189</span>	        site.domain = <span class="hljs-string">'example.com'</span>
<span class="linenos">190</span>	        site.save()
<span class="linenos">191</span>	
<span class="linenos">192</span>	        <span class="hljs-comment"># Create the post</span>
<span class="linenos">193</span>	        post = Post()
<span class="linenos">194</span>	        post.title = <span class="hljs-string">'My first post'</span>
<span class="linenos">195</span>	        post.text = <span class="hljs-string">'This is my first blog post'</span>
<span class="linenos">196</span>	        post.slug = <span class="hljs-string">'my-first-post'</span>
<span class="linenos">197</span>	        post.pub_date = timezone.now()
<span class="linenos">198</span>	        post.site = site
<span class="linenos">199</span>	        post.author = author
<span class="linenos">200</span>	        post.save()
<span class="linenos">201</span>	
<span class="linenos">202</span>	        <span class="hljs-comment"># Check new post saved</span>
<span class="linenos">203</span>	        all_posts = Post.objects.all()
<span class="linenos">204</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">205</span>	
<span class="linenos">206</span>	        <span class="hljs-comment"># Log in</span>
<span class="linenos">207</span>	        self.client.login(username=<span class="hljs-string">'bobsmith'</span>, password=<span class="hljs-string">"password"</span>)
<span class="linenos">208</span>	
<span class="linenos">209</span>	        <span class="hljs-comment"># Delete the post</span>
<span class="linenos">210</span>	        response = self.client.post(<span class="hljs-string">'/admin/blogengine/post/1/delete/'</span>, {
<span class="linenos">211</span>	            <span class="hljs-string">'post'</span>: <span class="hljs-string">'yes'</span>
<span class="linenos">212</span>	        }, follow=<span class="hljs-keyword">True</span>)
<span class="linenos">213</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">214</span>	
<span class="linenos">215</span>	        <span class="hljs-comment"># Check deleted successfully</span>
<span class="linenos">216</span>	        self.assertTrue(<span class="hljs-string">'deleted successfully'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">217</span>	
<span class="linenos">218</span>	        <span class="hljs-comment"># Check post amended</span>
<span class="linenos">219</span>	        all_posts = Post.objects.all()
<span class="linenos">220</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">0</span>)
<span class="linenos">221</span>	
<span class="linenos">222</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">PostViewTest</span><span class="hljs-params">(BaseAcceptanceTest)</span>:</span>
<span class="linenos">223</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_index</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">224</span>	        <span class="hljs-comment"># Create the author</span>
<span class="linenos">225</span>	        author = User.objects.create_user(<span class="hljs-string">'testuser'</span>, <span class="hljs-string">'user@example.com'</span>, <span class="hljs-string">'password'</span>)
<span class="linenos">226</span>	        author.save()
<span class="linenos">227</span>	
<span class="linenos">228</span>	        <span class="hljs-comment"># Create the site</span>
<span class="linenos">229</span>	        site = Site()
<span class="linenos">230</span>	        site.name = <span class="hljs-string">'example.com'</span>
<span class="linenos">231</span>	        site.domain = <span class="hljs-string">'example.com'</span>
<span class="linenos">232</span>	        site.save()
<span class="linenos">233</span>	
<span class="linenos">234</span>	        <span class="hljs-comment"># Create the post</span>
<span class="linenos">235</span>	        post = Post()
<span class="linenos">236</span>	        post.title = <span class="hljs-string">'My first post'</span>
<span class="linenos">237</span>	        post.text = <span class="hljs-string">'This is [my first blog post](http://127.0.0.1:8000/)'</span>
<span class="linenos">238</span>	        post.slug = <span class="hljs-string">'my-first-post'</span>
<span class="linenos">239</span>	        post.pub_date = timezone.now()
<span class="linenos">240</span>	        post.author = author
<span class="linenos">241</span>	        post.site = site
<span class="linenos">242</span>	        post.save()
<span class="linenos">243</span>	
<span class="linenos">244</span>	        <span class="hljs-comment"># Check new post saved</span>
<span class="linenos">245</span>	        all_posts = Post.objects.all()
<span class="linenos">246</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">247</span>	
<span class="linenos">248</span>	        <span class="hljs-comment"># Fetch the index</span>
<span class="linenos">249</span>	        response = self.client.get(<span class="hljs-string">'/'</span>)
<span class="linenos">250</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">251</span>	
<span class="linenos">252</span>	        <span class="hljs-comment"># Check the post title is in the response</span>
<span class="linenos">253</span>	        self.assertTrue(post.title <span class="hljs-keyword">in</span> response.content)
<span class="linenos">254</span>	
<span class="linenos">255</span>	        <span class="hljs-comment"># Check the post text is in the response</span>
<span class="linenos">256</span>	        self.assertTrue(markdown.markdown(post.text) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">257</span>	
<span class="linenos">258</span>	        <span class="hljs-comment"># Check the post date is in the response</span>
<span class="linenos">259</span>	        self.assertTrue(str(post.pub_date.year) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">260</span>	        self.assertTrue(post.pub_date.strftime(<span class="hljs-string">'%b'</span>) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">261</span>	        self.assertTrue(str(post.pub_date.day) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">262</span>	
<span class="linenos">263</span>	        <span class="hljs-comment"># Check the link is marked up properly</span>
<span class="linenos">264</span>	        self.assertTrue(<span class="hljs-string">'&lt;a href="http://127.0.0.1:8000/"&gt;my first blog post&lt;/a&gt;'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">265</span>	
<span class="linenos">266</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_post_page</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">267</span>	        <span class="hljs-comment"># Create the author</span>
<span class="linenos">268</span>	        author = User.objects.create_user(<span class="hljs-string">'testuser'</span>, <span class="hljs-string">'user@example.com'</span>, <span class="hljs-string">'password'</span>)
<span class="linenos">269</span>	        author.save()
<span class="linenos">270</span>	
<span class="linenos">271</span>	        <span class="hljs-comment"># Create the site</span>
<span class="linenos">272</span>	        site = Site()
<span class="linenos">273</span>	        site.name = <span class="hljs-string">'example.com'</span>
<span class="linenos">274</span>	        site.domain = <span class="hljs-string">'example.com'</span>
<span class="linenos">275</span>	        site.save()
<span class="linenos">276</span>	
<span class="linenos">277</span>	        <span class="hljs-comment"># Create the post</span>
<span class="linenos">278</span>	        post = Post()
<span class="linenos">279</span>	        post.title = <span class="hljs-string">'My first post'</span>
<span class="linenos">280</span>	        post.text = <span class="hljs-string">'This is [my first blog post](http://127.0.0.1:8000/)'</span>
<span class="linenos">281</span>	        post.slug = <span class="hljs-string">'my-first-post'</span>
<span class="linenos">282</span>	        post.pub_date = timezone.now()
<span class="linenos">283</span>	        post.author = author
<span class="linenos">284</span>	        post.site = site
<span class="linenos">285</span>	        post.save()
<span class="linenos">286</span>	
<span class="linenos">287</span>	        <span class="hljs-comment"># Check new post saved</span>
<span class="linenos">288</span>	        all_posts = Post.objects.all()
<span class="linenos">289</span>	        self.assertEquals(len(all_posts), <span class="hljs-number">1</span>)
<span class="linenos">290</span>	        only_post = all_posts[<span class="hljs-number">0</span>]
<span class="linenos">291</span>	        self.assertEquals(only_post, post)
<span class="linenos">292</span>	
<span class="linenos">293</span>	        <span class="hljs-comment"># Get the post URL</span>
<span class="linenos">294</span>	        post_url = only_post.get_absolute_url()
<span class="linenos">295</span>	
<span class="linenos">296</span>	        <span class="hljs-comment"># Fetch the post</span>
<span class="linenos">297</span>	        response = self.client.get(post_url)
<span class="linenos">298</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">299</span>	
<span class="linenos">300</span>	        <span class="hljs-comment"># Check the post title is in the response</span>
<span class="linenos">301</span>	        self.assertTrue(post.title <span class="hljs-keyword">in</span> response.content)
<span class="linenos">302</span>	
<span class="linenos">303</span>	        <span class="hljs-comment"># Check the post text is in the response</span>
<span class="linenos">304</span>	        self.assertTrue(markdown.markdown(post.text) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">305</span>	
<span class="linenos">306</span>	        <span class="hljs-comment"># Check the post date is in the response</span>
<span class="linenos">307</span>	        self.assertTrue(str(post.pub_date.year) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">308</span>	        self.assertTrue(post.pub_date.strftime(<span class="hljs-string">'%b'</span>) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">309</span>	        self.assertTrue(str(post.pub_date.day) <span class="hljs-keyword">in</span> response.content)
<span class="linenos">310</span>	
<span class="linenos">311</span>	        <span class="hljs-comment"># Check the link is marked up properly</span>
<span class="linenos">312</span>	        self.assertTrue(<span class="hljs-string">'&lt;a href="http://127.0.0.1:8000/"&gt;my first blog post&lt;/a&gt;'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">313</span>	
<span class="linenos">314</span>	
<span class="linenos">315</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">FlatPageViewTest</span><span class="hljs-params">(BaseAcceptanceTest)</span>:</span>
<span class="linenos">316</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">test_create_flat_page</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">317</span>	        <span class="hljs-comment"># Create flat page</span>
<span class="linenos">318</span>	        page = FlatPage()
<span class="linenos">319</span>	        page.url = <span class="hljs-string">'/about/'</span>
<span class="linenos">320</span>	        page.title = <span class="hljs-string">'About me'</span>
<span class="linenos">321</span>	        page.content = <span class="hljs-string">'All about me'</span>
<span class="linenos">322</span>	        page.save()
<span class="linenos">323</span>	
<span class="linenos">324</span>	        <span class="hljs-comment"># Add the site</span>
<span class="linenos">325</span>	        page.sites.add(Site.objects.all()[<span class="hljs-number">0</span>])
<span class="linenos">326</span>	        page.save()
<span class="linenos">327</span>	
<span class="linenos">328</span>	        <span class="hljs-comment"># Check new page saved</span>
<span class="linenos">329</span>	        all_pages = FlatPage.objects.all()
<span class="linenos">330</span>	        self.assertEquals(len(all_pages), <span class="hljs-number">1</span>)
<span class="linenos">331</span>	        only_page = all_pages[<span class="hljs-number">0</span>]
<span class="linenos">332</span>	        self.assertEquals(only_page, page)
<span class="linenos">333</span>	
<span class="linenos">334</span>	        <span class="hljs-comment"># Check data correct</span>
<span class="linenos">335</span>	        self.assertEquals(only_page.url, <span class="hljs-string">'/about/'</span>)
<span class="linenos">336</span>	        self.assertEquals(only_page.title, <span class="hljs-string">'About me'</span>)
<span class="linenos">337</span>	        self.assertEquals(only_page.content, <span class="hljs-string">'All about me'</span>)
<span class="linenos">338</span>	
<span class="linenos">339</span>	        <span class="hljs-comment"># Get URL</span>
<span class="linenos">340</span>	        page_url = str(only_page.get_absolute_url())
<span class="linenos">341</span>	
<span class="linenos">342</span>	        <span class="hljs-comment"># Get the page</span>
<span class="linenos">343</span>	        response = self.client.get(page_url)
<span class="linenos">344</span>	        self.assertEquals(response.status_code, <span class="hljs-number">200</span>)
<span class="linenos">345</span>	
<span class="linenos">346</span>	        <span class="hljs-comment"># Check title and content in response</span>
<span class="linenos">347</span>	        self.assertTrue(<span class="hljs-string">'About me'</span> <span class="hljs-keyword">in</span> response.content)
<span class="linenos">348</span>	        self.assertTrue(<span class="hljs-string">'All about me'</span> <span class="hljs-keyword">in</span> response.content)</code></pre>
<p>All we’ve done here is to add the <code>site</code> attribute when creating a new post using the Django database API, and when we create one via the admin, we add an additional <code>site</code> aparameter to the HTTP POST request with a value of 1. Run the tests and they should fail:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">2</span>	E........
<span class="linenos">3</span>	======================================================================
<span class="linenos">4</span>	ERROR: <span class="hljs-built_in">test</span>_create_post (blogengine.tests.PostTest)
<span class="linenos">5</span>	----------------------------------------------------------------------
<span class="linenos">6</span>	Traceback (most recent call last):
<span class="linenos">7</span>	  File <span class="hljs-string">"/Users/matthewdaly/Projects/django_tutorial_blog_ng/blogengine/tests.py"</span>, line <span class="hljs-number">46</span>, <span class="hljs-keyword">in</span> <span class="hljs-built_in">test</span>_create_post
<span class="linenos">8</span>	    self.assertEquals(only_post.site.name, <span class="hljs-string">'example.com'</span>)
<span class="linenos">9</span>	AttributeError: <span class="hljs-string">'Post'</span> object has no attribute <span class="hljs-string">'site'</span>
<span class="linenos">10</span>	
<span class="linenos">11</span>	----------------------------------------------------------------------
<span class="linenos">12</span>	Ran <span class="hljs-number">9</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">4.313</span>s
<span class="linenos">13</span>	
<span class="linenos">14</span>	FAILED (errors=<span class="hljs-number">1</span>)
<span class="linenos">15</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>So we need to add the <code>site</code> attribute to the <code>Post</code> model. Let’s do that:</p>
<pre><code class="hljs lang-python"><span class="linenos">1</span>	<span class="hljs-keyword">from</span> django.db <span class="hljs-keyword">import</span> models
<span class="linenos">2</span>	<span class="hljs-keyword">from</span> django.contrib.auth.models <span class="hljs-keyword">import</span> User
<span class="linenos">3</span>	<span class="hljs-keyword">from</span> django.contrib.sites.models <span class="hljs-keyword">import</span> Site
<span class="linenos">4</span>	
<span class="linenos">5</span>	<span class="hljs-comment"># Create your models here.</span>
<span class="linenos">6</span>	<span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Post</span><span class="hljs-params">(models.Model)</span>:</span>
<span class="linenos">7</span>	    title = models.CharField(max_length=<span class="hljs-number">200</span>)
<span class="linenos">8</span>	    pub_date = models.DateTimeField()
<span class="linenos">9</span>	    text = models.TextField()
<span class="linenos">10</span>	    slug = models.SlugField(max_length=<span class="hljs-number">40</span>, unique=<span class="hljs-keyword">True</span>)
<span class="linenos">11</span>	    author = models.ForeignKey(User)
<span class="linenos">12</span>	    site = models.ForeignKey(Site)
<span class="linenos">13</span>	
<span class="linenos">14</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">get_absolute_url</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">15</span>	        <span class="hljs-keyword">return</span> <span class="hljs-string">"/%s/%s/%s/"</span> % (self.pub_date.year, self.pub_date.month, self.slug)
<span class="linenos">16</span>	
<span class="linenos">17</span>	    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">__unicode__</span><span class="hljs-params">(self)</span>:</span>
<span class="linenos">18</span>	        <span class="hljs-keyword">return</span> self.title
<span class="linenos">19</span>	
<span class="linenos">20</span>	    <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Meta</span>:</span>
<span class="linenos">21</span>	        ordering = [<span class="hljs-string">"-pub_date"</span>]</code></pre>
<p>Now create and run the migrations - you’ll be prompted to create a default value for the site attribute as well:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	(venv)Smith:django_tutorial_blog_ng matthewdaly$ python manage.py schemamigration --auto blogengine
<span class="linenos">2</span>	 ? The field <span class="hljs-string">'Post.site'</span> does not have a default specified, yet is NOT NULL.
<span class="linenos">3</span>	 ? Since you are adding this field, you MUST specify a default
<span class="linenos">4</span>	 ? value to use <span class="hljs-keyword">for</span> existing rows. Would you like to:
<span class="linenos">5</span>	 ?  <span class="hljs-number">1</span>. Quit now, and add a default to the field <span class="hljs-keyword">in</span> models.py
<span class="linenos">6</span>	 ?  <span class="hljs-number">2</span>. Specify a one-off value to use <span class="hljs-keyword">for</span> existing columns now
<span class="linenos">7</span>	 ? Please select a choice: <span class="hljs-number">2</span>
<span class="linenos">8</span>	 ? Please enter Python code <span class="hljs-keyword">for</span> your one-off default value.
<span class="linenos">9</span>	 ? The datetime module is available, so you can <span class="hljs-keyword">do</span> e.g. datetime.date.today()
<span class="linenos">10</span>	 &gt;&gt;&gt; <span class="hljs-number">1</span>
<span class="linenos">11</span>	 + Added field site on blogengine.Post
<span class="linenos">12</span>	Created <span class="hljs-number">0005</span>_auto__add_field_post_site.py. You can now apply this migration with: ./manage.py migrate blogengine
<span class="linenos">13</span>	(venv)Smith:django_tutorial_blog_ng matthewdaly$ python manage.py migrate
<span class="linenos">14</span>	Running migrations <span class="hljs-keyword">for</span> blogengine:
<span class="linenos">15</span>	 - Migrating forwards to <span class="hljs-number">0005</span>_auto__add_field_post_site.
<span class="linenos">16</span>	 &gt; blogengine:<span class="hljs-number">0005</span>_auto__add_field_post_site
<span class="linenos">17</span>	 - Loading initial data <span class="hljs-keyword">for</span> blogengine.
<span class="linenos">18</span>	Installed <span class="hljs-number">0</span> object(s) from <span class="hljs-number">0</span> fixture(s)</code></pre>
<p>Our tests should then pass:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	(venv)Smith:django_tutorial_blog_ng matthewdaly$ python manage.py <span class="hljs-built_in">test</span>
<span class="linenos">2</span>	Creating <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...
<span class="linenos">3</span>	.........
<span class="linenos">4</span>	----------------------------------------------------------------------
<span class="linenos">5</span>	Ran <span class="hljs-number">9</span> tests <span class="hljs-keyword">in</span> <span class="hljs-number">4.261</span>s
<span class="linenos">6</span>	
<span class="linenos">7</span>	OK
<span class="linenos">8</span>	Destroying <span class="hljs-built_in">test</span> database <span class="hljs-keyword">for</span> <span class="hljs-built_in">alias</span> <span class="hljs-string">'default'</span>...</code></pre>
<p>Now we can include our full page URL on the post detail page:</p>
<pre><code class="hljs lang-django"><span class="linenos">1</span>	<span class="xml"></span><span class="hljs-template_tag">{% <span class="hljs-keyword">extends</span> "blogengine/includes/base.html" %}</span><span class="xml">
<span class="linenos">2</span>	
<span class="linenos">3</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">load</span> custom_markdown %}</span><span class="xml">
<span class="linenos">4</span>	
<span class="linenos">5</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">block</span> content %}</span><span class="xml">
<span class="linenos">6</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"post"</span>&gt;</span>
<span class="linenos">7</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">h1</span>&gt;</span></span><span class="hljs-variable">{{ object.title }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">h1</span>&gt;</span>
<span class="linenos">8</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">h3</span>&gt;</span></span><span class="hljs-variable">{{ object.pub_date }}</span><span class="xml"><span class="hljs-tag">&lt;/<span class="hljs-title">h3</span>&gt;</span>
<span class="linenos">9</span>	        </span><span class="hljs-variable">{{ object.text<span class="hljs-filter">|custom</span>_markdown }}</span><span class="xml">
<span class="linenos">10</span>	
<span class="linenos">11</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">h4</span>&gt;</span>Comments<span class="hljs-tag">&lt;/<span class="hljs-title">h4</span>&gt;</span>
<span class="linenos">12</span>	        <span class="hljs-tag">&lt;<span class="hljs-title">div</span> <span class="hljs-attribute">class</span>=<span class="hljs-value">"fb-comments"</span> <span class="hljs-attribute">data-href</span>=<span class="hljs-value">"http://</span></span></span><span class="hljs-variable">{{ post.site }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value"></span></span></span><span class="hljs-variable">{{ post.get_absolute_url }}</span><span class="xml"><span class="hljs-tag"><span class="hljs-value">"</span> <span class="hljs-attribute">data-width</span>=<span class="hljs-value">"470"</span> <span class="hljs-attribute">data-num-posts</span>=<span class="hljs-value">"10"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">13</span>	
<span class="linenos">14</span>	        <span class="hljs-tag">&lt;/<span class="hljs-title">div</span>&gt;</span>
<span class="linenos">15</span>	
<span class="linenos">16</span>	    </span><span class="hljs-template_tag">{% <span class="hljs-keyword">endblock</span> %}</span><span class="xml"></span></code></pre>
<p>If you want to customise the comments, take a look at <a href="https://developers.facebook.com/docs/plugins/comments/">the documentation for Facebook Comments</a>.</p>
<p>With that done, we can commit our changes:</p>
<pre><code class="hljs lang-bash"><span class="linenos">1</span>	$ git add blogengine/ templates/
<span class="linenos">2</span>	$ git commit -m <span class="hljs-string">'Implemented Facebook comments'</span></code></pre>
<p>And that wraps up this lesson. As usual, you can easily switch to today’s lesson with <code>git checkout lesson-3</code>. Next time we’ll implement categories and tags, and create an RSS feed for our blog posts.</p>
]]></description>
            <link>http://matthewdaly.co.uk/blog/2014/01/03/django-blog-tutorial-the-next-generation-part-3/</link>
            <guid isPermaLink="true">http://matthewdaly.co.uk/blog/2014/01/03/django-blog-tutorial-the-next-generation-part-3/</guid>
            <pubDate>Fri, 03 Jan 2014 12:57:30 GMT</pubDate>
        </item>
    </channel>
</rss>